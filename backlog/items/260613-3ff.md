---
schema_version: 1
id: 260613-3ff
kind: story
parent: 260613-yyj
title: Author _shared/dogfooding.md substrate + /plan dogfood-task emission, approval gate, and review checks
type: enhancement
priority: should
status: done
route: skill
dependencies: []
feature_folder: docs/pmos/features/2026-06-13_dogfooding-verification/
plan_doc: docs/pmos/features/2026-06-13_dogfooding-verification/stories/260613-3ff/03_plan.html
tasks: docs/pmos/features/2026-06-13_dogfooding-verification/stories/260613-3ff/tasks.yaml
worktree: ../agent-skills-260613-3ff
claimed_by:
driver_holder:
labels: [pmos-toolkit, plan, dogfooding, sdlc-pipeline]
created: 2026-06-13
updated: 2026-06-13
---

<!-- status: planned at define (Loop 1); plan_doc + tasks.yaml authored. Build via /feature-sdlc build --story 260613-3ff -->

## Context

The load-bearing build story for epic `260613-yyj`. Creates the canonical substrate `_shared/dogfooding.md` (co-designed with and born from `/plan`'s needs — fused into one story per the route:skill split rule) and updates `/plan` to emit the mandatory **TN−1: Dogfood / Utility Verification** task with both objective and subjective eval criteria, an independent-judge dispatch, the iterate-until-satisfied protocol, and a plan-time approval gate. Built against the design contract `docs/pmos/features/2026-06-13_dogfooding-verification/02_design.html` and the standing skill-authoring criteria (`feature-sdlc/reference/skill-patterns.md §A–§L`, repo `CLAUDE.md`). One `/execute` run = one PR.

`/verify`'s gate (story `260613-2m7`) depends on the substrate + the TN−1 task contract this story settles.

## Acceptance Criteria

(Drawn from epic `260613-yyj` — the change-set for this story.)

- [ ] AC1 — `plugins/pmos-toolkit/skills/_shared/dogfooding.md` exists and is the canonical home (§K) for: dogfood task anatomy; the objective+subjective eval-criteria framework (D3); the archetype catalog (D8 — skill→LLM-judge, frontend→Playwright-friction, CLI→real-invocation, data→golden-sample, API→scenario); the independent-judge contract (D4 — fresh blind subagent, structured verdict, no edits); the iterate-until-satisfied protocol (D2 — cap 2, net-worse guard, non-interactive accept-residuals-and-surface); the tier policy (D1); the plan-time approval gate (D7).
- [ ] AC2 — `plan/reference/plan-templates.md` carries a `TN−1: Dogfood / Utility Verification` task template immediately before TN, citing `_shared/dogfooding.md`, with placeholders for archetype, scenario, objective-criteria checklist, subjective-rubric, independent-judge dispatch, and the verdict line `/verify` reads.
- [ ] AC3 — `plan/SKILL.md` Tier Gates require the dogfood task for T2/T3 (offer for T1); a `/plan` phase selects an archetype and drafts the scenario + dual criteria; a plan-time `AskUserQuestion` approves/edits it (with a `(Recommended)` option = the auto-proposed scenario; `defer-only` is NOT used — it has a Recommended).
- [ ] AC4 — A `/plan` review structural check fails any T2/T3 plan whose dogfood task is missing, not load-bearing, or lacks BOTH objective and subjective criteria + a named independent judge; the Exit Criteria list updated to include it.
- [ ] AC5 — TN−1 serializes into `tasks.yaml` as a real task (its position keeps TN last). The iterate loop relies on `/execute`'s existing discovered-work routing — this story makes NO `/execute` edit (D6); the substrate documents how discovered-work routing applies.
- [ ] AC6 — `/plan` stays lean (D9): the SKILL.md and template cite the substrate and state only deltas; no duplicated contract prose.
- [ ] AC7 — Conventions: `--non-interactive` per the W14 contract (canonical inline block byte-identical; the plan-time approval defers to the recommended scenario); §H–§L satisfied; every `AskUserQuestion` carries `(Recommended)` or a `defer-only` tag; argument-hint flags (if any added) handled in the body.
- [ ] AC8 — `/plan` passes `skill-eval.md` (floor 43/47; `name: plan` matches dir); repo hygiene lints green (lint-flags-vs-hints, lint-phase-refs, lint-non-interactive-inline, audit-recommended).
- [ ] AC9 — **Dogfood (D2/D3/D7 — this story's own load-bearing dogfood):** run the revised `/plan` end-to-end on a real T2 and a real T3 spec; confirm it emits a TN−1 task with both objective + subjective criteria, fires the approval gate, and serializes TN−1 into `tasks.yaml` with TN still last. Independent judge scores the emitted dogfood task against the substrate's own anatomy + dual-criteria contract; gaps → fix → re-run until satisfied (cap 2). Objective gates: TN−1 present in both plans; both criteria kinds present; approval `AskUserQuestion` present with `(Recommended)`; lint-phase-refs + skill-eval green.
- [ ] AC10 — Release prerequisites (version bump, changelog, README, manifest sync, learnings header) listed under the plan's `## Release prerequisites` only, NOT as `/execute` wave tasks.

## Notes

Plan + `tasks.yaml` authored at define time (this loop). Build via `/feature-sdlc build --story 260613-3ff` (or `build --next`).

### Build 2026-06-13 (route:skill, `--non-interactive` via `/loop`) — PASS → done

Built on `feat/260613-3ff` (worktree `../agent-skills-260613-3ff`); branch + worktree retained for the Loop-3 release train (`/complete-dev --epic 260613-yyj`). Picked by `build --next` D22 order (priority `should` + id-ascending among the planned set).

- **T1–T5 done.** T1 new substrate `_shared/dogfooding.md` — canonical contract, 8 anchored sections (definition/anatomy/eval-criteria/judge/archetypes/iterate-loop/tier-policy/approval-gate), dual objective+subjective criteria, blind-judge structured-verdict shape, iterate cap-2 + net-worse + NI accept-residuals, T2/T3-mandatory/T1-optional. T2 `plan/reference/plan-templates.md` TN−1 Dogfood/Utility Verification template before TN (+ skeleton intro). T3 `plan/SKILL.md` Tier Gates require TN−1 (T2/3; offer T1) + §Dogfood-task emission (archetype-select + draft + plan-time approval `AskUserQuestion` with `(Recommended)`) + tasks.yaml serialize + Append-mode note. T4 Phase 4 structural check #14 (hard-fail) + Exit-Criteria bullet. T5 conformance + lints + load-bearing dogfood.
- **AC1–AC10: all PASS.** D6 honored — NO `/execute` edit (reuses discovered-work routing). W14 inline block byte-identical (verified vs main). AC10 clean — no release-prereq task in any wave.
- **Gates green:** skill-eval `[D]` 22/22 (final idempotent re-run exit 0; `name: plan` == dir); `[J]` gated checks 5/5 pass (h-gates-deterministic-hard / -judgment-advisory / -arithmetic-scripted, k-one-fact-one-home, l-dispatch-model-tier — all quote-grounded by a blind reviewer); 4 repo lints (lint-flags-vs-hints, lint-phase-refs, lint-non-interactive-inline 41/41, audit-recommended 10 calls/2 Recommended/8 defer-only) all PASS. No accepted residuals.
- **Load-bearing dogfood (AC9 — eat-our-own-cooking):** archetype skill→Use→blind-LLM-judge; ran the revised `/plan` emission on a real **T2** (`/complete-dev` multi-epic) and **T3** (`/research`) deliverable → both emit a correct TN−1 with dual criteria + named judge + Verdict line, TN stays last, approval gate present. Independent blind judge: **overall_satisfied=true** on iteration 1 (cap 2 unused); 1 non-blocking gap (an abbreviated iterate line in the T2 sample) fixed in the dogfood artifact, no `/plan` deliverable change. Record: `stories/260613-3ff/dogfood-run.md`.
