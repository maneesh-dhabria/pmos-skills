---
schema_version: 1
id: 260613-2m7
kind: story
parent: 260613-yyj
title: /verify load-bearing dogfood gate (Phase-7 hard gate) + iterate-loop residual reconciliation
type: enhancement
priority: should
status: done
route: skill
dependencies: [260613-3ff]
feature_folder: docs/pmos/features/2026-06-13_dogfooding-verification/
plan_doc: docs/pmos/features/2026-06-13_dogfooding-verification/stories/260613-2m7/03_plan.html
tasks: docs/pmos/features/2026-06-13_dogfooding-verification/stories/260613-2m7/tasks.yaml
worktree: feat/260613-2m7
claimed_by: build:2m7-loop
driver_holder: build:2m7-loop
build_commit: 3c90b79
labels: [pmos-toolkit, verify, dogfooding, sdlc-pipeline]
created: 2026-06-13
updated: 2026-06-15
---

<!-- status: ready at define (Loop 1) → done at build (Loop 2, 2026-06-15). branch feat/260613-2m7: D9 dep-merge e66c7b0 (3ff dogfooding substrate) + T1+T2 f289724 (verify/SKILL.md) + dogfood 3c90b79. Release prereqs (version bump, changelog, tag) are /complete-dev's at Loop 3 — NOT story tasks. -->

## Context

The consumer build story for epic `260613-yyj`. Updates `/verify` to treat the dogfood task's verdict as a **load-bearing, non-skippable gate** for Tier 2/3 and to reconcile the iterate loop's `accepted_residuals[]`. Depends on story `260613-3ff` — it gates on the `_shared/dogfooding.md` contract and the TN−1 task shape that story settles (D9 claim-time merge makes `260613-3ff`'s branch present in this worktree before `skill-eval`). Built against the design contract `docs/pmos/features/2026-06-13_dogfooding-verification/02_design.html` and the standing skill-authoring criteria. One `/execute` run = one PR.

## Acceptance Criteria

(Drawn from epic `260613-yyj` — the change-set for this story.)

- [x] AC1 — `verify/SKILL.md` adds a Phase-7 hard gate "Dogfood verdict" (peer of the browser-evidence gate): for Tier 2/3, the dogfood task (TN−1) must carry a satisfied verdict (objective gates green AND judge overall-satisfied). A missing/unsatisfied verdict means the run cannot be bare `PASS` → `PASS-WITH-GAPS`/`FAIL`, with each gap enumerated in the verdict block (extend the Phase-8 verdict rule accordingly).
- [x] AC2 — `/verify` reconciles `accepted_residuals[]` produced by the iterate loop the same way skill-eval residuals are reconciled: a still-failing accepted residual is reported `KNOWN / accepted` (non-blocking, surfaced loudly in the `/verify` report AND the `/complete-dev` summary); a newly-failing objective gate blocks normally.
- [x] AC3 — The gate is non-skippable for T2/3 (mirrors the existing non-skippable browser-evidence gate); T1 with no dogfood task is a clean pass (gate N/A).
- [x] AC4 — `/verify` cites `_shared/dogfooding.md` for the contract and states only the verify-side delta (D9 §K) — no duplicated dogfood-protocol prose.
- [x] AC5 — Conventions: `--non-interactive` per the W14 contract (canonical inline block byte-identical); §H–§L satisfied; any new `AskUserQuestion` carries `(Recommended)` or `defer-only`; phase-ref slugs cite the named skill's headings (lint-phase-refs green for any cross-skill cite to `plan/SKILL.md` / `_shared/dogfooding.md`).
- [x] AC6 — `/verify` passes `skill-eval.md` (floor 43/47; `name: verify` matches dir); repo hygiene lints green (lint-flags-vs-hints, lint-phase-refs, lint-non-interactive-inline, audit-recommended).
- [x] AC7 — **Dogfood (this story's own load-bearing dogfood):** construct a fixture feature folder containing a TN−1 dogfood task with (a) a satisfied verdict and (b) an unsatisfied verdict + accepted residual; run `/verify` against each and confirm the gate yields bare `PASS` for (a) and `PASS-WITH-GAPS` with enumerated gaps + the loud `KNOWN / accepted` line for (b). An independent judge scores the gate's report output against AC1–AC2; gaps → fix → re-run until satisfied (cap 2). Objective gates: correct verdict for each fixture; residual surfaced loudly; skill-eval + lint-phase-refs green.
- [x] AC8 — Release prerequisites listed under the plan's `## Release prerequisites` only, NOT as `/execute` wave tasks.

## Build Notes (Loop 2 — 2026-06-15)

Built via `/feature-sdlc build --next` (picked after `260615-dhk` was handed to another session and released back to `planned`). route:skill inner pipeline: skill-tier-resolve (T2 enhancement) → /execute → /skill-eval ([D] hard) → /verify. **D9 dep-merge** of stale `feat/260613-3ff` brought `_shared/dogfooding.md` + the `/plan` TN−1 task into the worktree (merge `e66c7b0`); verified the merge applies only 3ff's deltas-from-base — released files (claim-lock.cjs, mytasks, backlog, complete-dev, feature-sdlc) untouched.

- **Change (T1+T2, `f289724`):** `verify/SKILL.md` only. Phase-7 non-skippable Tier-2/3 "Dogfood verdict" hard gate (peer of the browser-evidence gate) reading the `**Verdict:** satisfied | not-satisfied · accepted_residuals: [...]` line; Phase-8 verdict mapping (missing/not-satisfied → PASS-WITH-GAPS, FAIL on a critical objective gate, per-gap enumeration); accepted-residual reconciliation reusing the route:skill skill-eval pattern (loud `KNOWN / accepted`, non-blocking, carried to `/complete-dev`). Cites `_shared/dogfooding.md` (lean, D9 §K — no restated protocol).
- **Conformance:** skill-eval `[D]` EXIT 0 (2 fails — `c-reference-toc`, `d-progress-tracking` — proven byte-identical on pre-edit `e66c7b0` → accepted pre-existing residuals); 4 hygiene lints green (NI block byte-identical 42/42; phase-refs resolve incl. new `_shared/dogfooding.md#anchor` cites; audit-recommended: 1 call, defer-only, none added).
- **Load-bearing dogfood (`3c90b79`, AC7):** two fixture feature folders (satisfied / not-satisfied+accepted-residual) + a missing-verdict edge fixture; `gate-check.sh` implements the gate verbatim. Objective **8/8 green** (A→PASS, B→PASS-WITH-GAPS + enumerated gap + loud `KNOWN/accepted`, C→missing→PASS-WITH-GAPS). **One cap-2 iteration** fixed a real harness grep bug (`[^\n]*` byte-truncation). Blind judge: `overall_satisfied: true`, 5/5 acceptable, gaps=[]. Evidence: `stories/260613-2m7/dogfood/EVIDENCE.md`.
- **Verify verdict:** PASS (no UI surface → browser gate N/A; all AC rows Verified). Worktree KEPT for Loop 3.
- **Next (Loop 3, NOT this story):** `/complete-dev --epic 260613-yyj` → rides with story `260613-3ff` (pmos-toolkit minor bump); both branches carry the dogfooding feature.

## Notes

Plan + `tasks.yaml` authored at define time (this loop). Build AFTER `260613-3ff` is `done` (dependency). Build via `/feature-sdlc build --story 260613-2m7` (or `build --next`, which honours the dependency ordering).
