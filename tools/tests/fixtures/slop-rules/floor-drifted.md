# Design-slop prevention reference (fixture — DRIFTED)

# The `fixture-beta` DON'T line has been hand-deleted to simulate floor drift:
# its skillGuideline is no longer present, so lint-slop-rules.sh must fail (exit 1)
# and name `fixture-beta`.

## Typography

- **DON'T**: flat type hierarchy _(rule: `fixture-alpha`)_
