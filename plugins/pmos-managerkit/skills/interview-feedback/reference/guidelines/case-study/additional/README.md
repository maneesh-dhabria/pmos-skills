# Case prompt / take-home brief

Drop the role's actual case prompt or take-home brief into this folder per candidate role —
it is the question the candidate received and the ground truth for what a good answer is.

Register each file in the role's `role.json` under `additional_docs[]`. The interviewer reference
cites it as `data-cite-tier="notes"` evidence when grounding scores against the brief.
