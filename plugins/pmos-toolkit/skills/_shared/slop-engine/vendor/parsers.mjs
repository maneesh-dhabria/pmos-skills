var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};

// node_modules/entities/dist/decode-codepoint.js
var decodeMap = /* @__PURE__ */ new Map([
  [0, 65533],
  // C1 Unicode control character reference replacements
  [128, 8364],
  [130, 8218],
  [131, 402],
  [132, 8222],
  [133, 8230],
  [134, 8224],
  [135, 8225],
  [136, 710],
  [137, 8240],
  [138, 352],
  [139, 8249],
  [140, 338],
  [142, 381],
  [145, 8216],
  [146, 8217],
  [147, 8220],
  [148, 8221],
  [149, 8226],
  [150, 8211],
  [151, 8212],
  [152, 732],
  [153, 8482],
  [154, 353],
  [155, 8250],
  [156, 339],
  [158, 382],
  [159, 376]
]);
function replaceCodePoint(codePoint) {
  if (codePoint >= 55296 && codePoint <= 57343 || codePoint > 1114111) {
    return 65533;
  }
  return decodeMap.get(codePoint) ?? codePoint;
}

// node_modules/entities/dist/internal/decode-shared.js
function decodeBase64(input) {
  const binary = atob(input);
  const evenLength = binary.length & ~1;
  const out = new Uint16Array(evenLength / 2);
  for (let index = 0, outIndex = 0; index < evenLength; index += 2) {
    const lo2 = binary.charCodeAt(index);
    const hi2 = binary.charCodeAt(index + 1);
    out[outIndex++] = lo2 | hi2 << 8;
  }
  return out;
}

// node_modules/entities/dist/generated/decode-data-html.js
var htmlDecodeTree = /* @__PURE__ */ decodeBase64("");

// node_modules/entities/dist/generated/decode-data-xml.js
var xmlDecodeTree = /* @__PURE__ */ decodeBase64("AAJhZ2xxBwARABMAFQBtAg0AAAAAAA8AcAAmYG8AcwAnYHQAPmB0ADxg9SFvdCJg");

// node_modules/entities/dist/internal/bin-trie-flags.js
var BinTrieFlags;
(function(BinTrieFlags2) {
  BinTrieFlags2[BinTrieFlags2["VALUE_LENGTH"] = 49152] = "VALUE_LENGTH";
  BinTrieFlags2[BinTrieFlags2["FLAG13"] = 8192] = "FLAG13";
  BinTrieFlags2[BinTrieFlags2["BRANCH_LENGTH"] = 8064] = "BRANCH_LENGTH";
  BinTrieFlags2[BinTrieFlags2["JUMP_TABLE"] = 127] = "JUMP_TABLE";
})(BinTrieFlags || (BinTrieFlags = {}));

// node_modules/entities/dist/decode.js
var CharCodes;
(function(CharCodes3) {
  CharCodes3[CharCodes3["NUM"] = 35] = "NUM";
  CharCodes3[CharCodes3["SEMI"] = 59] = "SEMI";
  CharCodes3[CharCodes3["EQUALS"] = 61] = "EQUALS";
  CharCodes3[CharCodes3["ZERO"] = 48] = "ZERO";
  CharCodes3[CharCodes3["NINE"] = 57] = "NINE";
  CharCodes3[CharCodes3["LOWER_A"] = 97] = "LOWER_A";
  CharCodes3[CharCodes3["LOWER_F"] = 102] = "LOWER_F";
  CharCodes3[CharCodes3["LOWER_X"] = 120] = "LOWER_X";
  CharCodes3[CharCodes3["LOWER_Z"] = 122] = "LOWER_Z";
  CharCodes3[CharCodes3["UPPER_A"] = 65] = "UPPER_A";
  CharCodes3[CharCodes3["UPPER_F"] = 70] = "UPPER_F";
  CharCodes3[CharCodes3["UPPER_Z"] = 90] = "UPPER_Z";
})(CharCodes || (CharCodes = {}));
var TO_LOWER_BIT = 32;
function isNumber(code) {
  return code >= CharCodes.ZERO && code <= CharCodes.NINE;
}
function isHexadecimalCharacter(code) {
  return code >= CharCodes.UPPER_A && code <= CharCodes.UPPER_F || code >= CharCodes.LOWER_A && code <= CharCodes.LOWER_F;
}
function isAsciiAlphaNumeric(code) {
  return code >= CharCodes.UPPER_A && code <= CharCodes.UPPER_Z || code >= CharCodes.LOWER_A && code <= CharCodes.LOWER_Z || isNumber(code);
}
function isEntityInAttributeInvalidEnd(code) {
  return code === CharCodes.EQUALS || isAsciiAlphaNumeric(code);
}
var EntityDecoderState;
(function(EntityDecoderState2) {
  EntityDecoderState2[EntityDecoderState2["EntityStart"] = 0] = "EntityStart";
  EntityDecoderState2[EntityDecoderState2["NumericStart"] = 1] = "NumericStart";
  EntityDecoderState2[EntityDecoderState2["NumericDecimal"] = 2] = "NumericDecimal";
  EntityDecoderState2[EntityDecoderState2["NumericHex"] = 3] = "NumericHex";
  EntityDecoderState2[EntityDecoderState2["NamedEntity"] = 4] = "NamedEntity";
})(EntityDecoderState || (EntityDecoderState = {}));
var DecodingMode;
(function(DecodingMode2) {
  DecodingMode2[DecodingMode2["Legacy"] = 0] = "Legacy";
  DecodingMode2[DecodingMode2["Strict"] = 1] = "Strict";
  DecodingMode2[DecodingMode2["Attribute"] = 2] = "Attribute";
})(DecodingMode || (DecodingMode = {}));
var EntityDecoder = class {
  decodeTree;
  emitCodePoint;
  errors;
  constructor(decodeTree, emitCodePoint, errors) {
    this.decodeTree = decodeTree;
    this.emitCodePoint = emitCodePoint;
    this.errors = errors;
  }
  /** The current state of the decoder. */
  state = EntityDecoderState.EntityStart;
  /** Characters that were consumed while parsing an entity. */
  consumed = 1;
  /**
   * The result of the entity.
   *
   * Either the result index of a numeric entity, or the codepoint of a
   * numeric entity.
   */
  result = 0;
  /** The current index in the decode tree. */
  treeIndex = 0;
  /** The number of characters that were consumed in excess. */
  excess = 1;
  /** The mode in which the decoder is operating. */
  decodeMode = DecodingMode.Strict;
  /** The number of characters that have been consumed in the current run. */
  runConsumed = 0;
  /**
   * Resets the instance to make it reusable.
   * @param decodeMode Entity decoding mode to use.
   */
  startEntity(decodeMode) {
    this.decodeMode = decodeMode;
    this.state = EntityDecoderState.EntityStart;
    this.result = 0;
    this.treeIndex = 0;
    this.excess = 1;
    this.consumed = 1;
    this.runConsumed = 0;
  }
  /**
   * Write an entity to the decoder. This can be called multiple times with partial entities.
   * If the entity is incomplete, the decoder will return -1.
   *
   * Mirrors the implementation of `getDecoder`, but with the ability to stop decoding if the
   * entity is incomplete, and resume when the next string is written.
   * @param input The string containing the entity (or a continuation of the entity).
   * @param offset The offset at which the entity begins. Should be 0 if this is not the first call.
   * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
   */
  write(input, offset) {
    switch (this.state) {
      case EntityDecoderState.EntityStart: {
        if (input.charCodeAt(offset) === CharCodes.NUM) {
          this.state = EntityDecoderState.NumericStart;
          this.consumed += 1;
          return this.stateNumericStart(input, offset + 1);
        }
        this.state = EntityDecoderState.NamedEntity;
        return this.stateNamedEntity(input, offset);
      }
      case EntityDecoderState.NumericStart: {
        return this.stateNumericStart(input, offset);
      }
      case EntityDecoderState.NumericDecimal: {
        return this.stateNumericDecimal(input, offset);
      }
      case EntityDecoderState.NumericHex: {
        return this.stateNumericHex(input, offset);
      }
      case EntityDecoderState.NamedEntity: {
        return this.stateNamedEntity(input, offset);
      }
    }
  }
  /**
   * Switches between the numeric decimal and hexadecimal states.
   *
   * Equivalent to the `Numeric character reference state` in the HTML spec.
   * @param input The string containing the entity (or a continuation of the entity).
   * @param offset The current offset.
   * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
   */
  stateNumericStart(input, offset) {
    if (offset >= input.length) {
      return -1;
    }
    if ((input.charCodeAt(offset) | TO_LOWER_BIT) === CharCodes.LOWER_X) {
      this.state = EntityDecoderState.NumericHex;
      this.consumed += 1;
      return this.stateNumericHex(input, offset + 1);
    }
    this.state = EntityDecoderState.NumericDecimal;
    return this.stateNumericDecimal(input, offset);
  }
  /**
   * Parses a hexadecimal numeric entity.
   *
   * Equivalent to the `Hexademical character reference state` in the HTML spec.
   * @param input The string containing the entity (or a continuation of the entity).
   * @param offset The current offset.
   * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
   */
  stateNumericHex(input, offset) {
    while (offset < input.length) {
      const char = input.charCodeAt(offset);
      if (isNumber(char) || isHexadecimalCharacter(char)) {
        const digit = char <= CharCodes.NINE ? char - CharCodes.ZERO : (char | TO_LOWER_BIT) - CharCodes.LOWER_A + 10;
        this.result = this.result * 16 + digit;
        this.consumed++;
        offset++;
      } else {
        return this.emitNumericEntity(char, 3);
      }
    }
    return -1;
  }
  /**
   * Parses a decimal numeric entity.
   *
   * Equivalent to the `Decimal character reference state` in the HTML spec.
   * @param input The string containing the entity (or a continuation of the entity).
   * @param offset The current offset.
   * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
   */
  stateNumericDecimal(input, offset) {
    while (offset < input.length) {
      const char = input.charCodeAt(offset);
      if (isNumber(char)) {
        this.result = this.result * 10 + (char - CharCodes.ZERO);
        this.consumed++;
        offset++;
      } else {
        return this.emitNumericEntity(char, 2);
      }
    }
    return -1;
  }
  /**
   * Validate and emit a numeric entity.
   *
   * Implements the logic from the `Hexademical character reference start
   * state` and `Numeric character reference end state` in the HTML spec.
   * @param lastCp The last code point of the entity. Used to see if the
   *               entity was terminated with a semicolon.
   * @param expectedLength The minimum number of characters that should be
   *                       consumed. Used to validate that at least one digit
   *                       was consumed.
   * @returns The number of characters that were consumed.
   */
  emitNumericEntity(lastCp, expectedLength) {
    if (this.consumed <= expectedLength) {
      this.errors?.absenceOfDigitsInNumericCharacterReference(this.consumed);
      return 0;
    }
    if (lastCp === CharCodes.SEMI) {
      this.consumed += 1;
    } else if (this.decodeMode === DecodingMode.Strict) {
      return 0;
    }
    this.emitCodePoint(replaceCodePoint(this.result), this.consumed);
    if (this.errors) {
      if (lastCp !== CharCodes.SEMI) {
        this.errors.missingSemicolonAfterCharacterReference();
      }
      this.errors.validateNumericCharacterReference(this.result);
    }
    return this.consumed;
  }
  /**
   * Parses a named entity.
   *
   * Equivalent to the `Named character reference state` in the HTML spec.
   * @param input The string containing the entity (or a continuation of the entity).
   * @param offset The current offset.
   * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
   */
  stateNamedEntity(input, offset) {
    const { decodeTree } = this;
    let current = decodeTree[this.treeIndex];
    let valueLength = (current & BinTrieFlags.VALUE_LENGTH) >> 14;
    while (offset < input.length) {
      if (valueLength === 0 && (current & BinTrieFlags.FLAG13) !== 0) {
        const runLength = (current & BinTrieFlags.BRANCH_LENGTH) >> 7;
        if (this.runConsumed === 0) {
          const firstChar = current & BinTrieFlags.JUMP_TABLE;
          if (input.charCodeAt(offset) !== firstChar) {
            return this.result === 0 ? 0 : this.emitNotTerminatedNamedEntity();
          }
          offset++;
          this.excess++;
          this.runConsumed++;
        }
        while (this.runConsumed < runLength) {
          if (offset >= input.length) {
            return -1;
          }
          const charIndexInPacked = this.runConsumed - 1;
          const packedWord = decodeTree[this.treeIndex + 1 + (charIndexInPacked >> 1)];
          const expectedChar = charIndexInPacked % 2 === 0 ? packedWord & 255 : packedWord >> 8 & 255;
          if (input.charCodeAt(offset) !== expectedChar) {
            this.runConsumed = 0;
            return this.result === 0 ? 0 : this.emitNotTerminatedNamedEntity();
          }
          offset++;
          this.excess++;
          this.runConsumed++;
        }
        this.runConsumed = 0;
        this.treeIndex += 1 + (runLength >> 1);
        current = decodeTree[this.treeIndex];
        valueLength = (current & BinTrieFlags.VALUE_LENGTH) >> 14;
      }
      if (offset >= input.length)
        break;
      const char = input.charCodeAt(offset);
      if (char === CharCodes.SEMI && valueLength !== 0 && (current & BinTrieFlags.FLAG13) !== 0) {
        return this.emitNamedEntityData(this.treeIndex, valueLength, this.consumed + this.excess);
      }
      this.treeIndex = determineBranch(decodeTree, current, this.treeIndex + Math.max(1, valueLength), char);
      if (this.treeIndex < 0) {
        return this.result === 0 || // If we are parsing an attribute
        this.decodeMode === DecodingMode.Attribute && // We shouldn't have consumed any characters after the entity,
        (valueLength === 0 || // And there should be no invalid characters.
        isEntityInAttributeInvalidEnd(char)) ? 0 : this.emitNotTerminatedNamedEntity();
      }
      current = decodeTree[this.treeIndex];
      valueLength = (current & BinTrieFlags.VALUE_LENGTH) >> 14;
      if (valueLength !== 0) {
        if (char === CharCodes.SEMI) {
          return this.emitNamedEntityData(this.treeIndex, valueLength, this.consumed + this.excess);
        }
        if (this.decodeMode !== DecodingMode.Strict && (current & BinTrieFlags.FLAG13) === 0) {
          this.result = this.treeIndex;
          this.consumed += this.excess;
          this.excess = 0;
        }
      }
      offset++;
      this.excess++;
    }
    return -1;
  }
  /**
   * Emit a named entity that was not terminated with a semicolon.
   * @returns The number of characters consumed.
   */
  emitNotTerminatedNamedEntity() {
    const { result, decodeTree } = this;
    const valueLength = (decodeTree[result] & BinTrieFlags.VALUE_LENGTH) >> 14;
    this.emitNamedEntityData(result, valueLength, this.consumed);
    this.errors?.missingSemicolonAfterCharacterReference();
    return this.consumed;
  }
  /**
   * Emit a named entity.
   * @param result The index of the entity in the decode tree.
   * @param valueLength The number of bytes in the entity.
   * @param consumed The number of characters consumed.
   * @returns The number of characters consumed.
   */
  emitNamedEntityData(result, valueLength, consumed) {
    const { decodeTree } = this;
    this.emitCodePoint(valueLength === 1 ? decodeTree[result] & ~(BinTrieFlags.VALUE_LENGTH | BinTrieFlags.FLAG13) : decodeTree[result + 1], consumed);
    if (valueLength === 3) {
      this.emitCodePoint(decodeTree[result + 2], consumed);
    }
    return consumed;
  }
  /**
   * Signal to the parser that the end of the input was reached.
   *
   * Remaining data will be emitted and relevant errors will be produced.
   * @returns The number of characters consumed.
   */
  end() {
    switch (this.state) {
      case EntityDecoderState.NamedEntity: {
        return this.result !== 0 && (this.decodeMode !== DecodingMode.Attribute || this.result === this.treeIndex) ? this.emitNotTerminatedNamedEntity() : 0;
      }
      // Otherwise, emit a numeric entity if we have one.
      case EntityDecoderState.NumericDecimal: {
        return this.emitNumericEntity(0, 2);
      }
      case EntityDecoderState.NumericHex: {
        return this.emitNumericEntity(0, 3);
      }
      case EntityDecoderState.NumericStart: {
        this.errors?.absenceOfDigitsInNumericCharacterReference(this.consumed);
        return 0;
      }
      case EntityDecoderState.EntityStart: {
        return 0;
      }
    }
  }
};
function determineBranch(decodeTree, current, nodeIndex, char) {
  const branchCount = (current & BinTrieFlags.BRANCH_LENGTH) >> 7;
  const jumpOffset = current & BinTrieFlags.JUMP_TABLE;
  if (branchCount === 0) {
    return jumpOffset !== 0 && char === jumpOffset ? nodeIndex : -1;
  }
  if (jumpOffset) {
    const value = char - jumpOffset;
    return value < 0 || value >= branchCount ? -1 : decodeTree[nodeIndex + value] - 1;
  }
  const packedKeySlots = branchCount + 1 >> 1;
  let lo2 = 0;
  let hi2 = branchCount - 1;
  while (lo2 <= hi2) {
    const mid = lo2 + hi2 >>> 1;
    const slot = mid >> 1;
    const packed = decodeTree[nodeIndex + slot];
    const midKey = packed >> (mid & 1) * 8 & 255;
    if (midKey < char) {
      lo2 = mid + 1;
    } else if (midKey > char) {
      hi2 = mid - 1;
    } else {
      return decodeTree[nodeIndex + packedKeySlots + mid];
    }
  }
  return -1;
}

// node_modules/htmlparser2/dist/Tokenizer.js
var CharCodes2;
(function(CharCodes3) {
  CharCodes3[CharCodes3["Tab"] = 9] = "Tab";
  CharCodes3[CharCodes3["NewLine"] = 10] = "NewLine";
  CharCodes3[CharCodes3["FormFeed"] = 12] = "FormFeed";
  CharCodes3[CharCodes3["CarriageReturn"] = 13] = "CarriageReturn";
  CharCodes3[CharCodes3["Space"] = 32] = "Space";
  CharCodes3[CharCodes3["ExclamationMark"] = 33] = "ExclamationMark";
  CharCodes3[CharCodes3["Number"] = 35] = "Number";
  CharCodes3[CharCodes3["Amp"] = 38] = "Amp";
  CharCodes3[CharCodes3["SingleQuote"] = 39] = "SingleQuote";
  CharCodes3[CharCodes3["DoubleQuote"] = 34] = "DoubleQuote";
  CharCodes3[CharCodes3["Dash"] = 45] = "Dash";
  CharCodes3[CharCodes3["Slash"] = 47] = "Slash";
  CharCodes3[CharCodes3["Zero"] = 48] = "Zero";
  CharCodes3[CharCodes3["Nine"] = 57] = "Nine";
  CharCodes3[CharCodes3["Semi"] = 59] = "Semi";
  CharCodes3[CharCodes3["Lt"] = 60] = "Lt";
  CharCodes3[CharCodes3["Eq"] = 61] = "Eq";
  CharCodes3[CharCodes3["Gt"] = 62] = "Gt";
  CharCodes3[CharCodes3["Questionmark"] = 63] = "Questionmark";
  CharCodes3[CharCodes3["UpperA"] = 65] = "UpperA";
  CharCodes3[CharCodes3["LowerA"] = 97] = "LowerA";
  CharCodes3[CharCodes3["UpperF"] = 70] = "UpperF";
  CharCodes3[CharCodes3["LowerF"] = 102] = "LowerF";
  CharCodes3[CharCodes3["UpperZ"] = 90] = "UpperZ";
  CharCodes3[CharCodes3["LowerZ"] = 122] = "LowerZ";
  CharCodes3[CharCodes3["LowerX"] = 120] = "LowerX";
  CharCodes3[CharCodes3["OpeningSquareBracket"] = 91] = "OpeningSquareBracket";
})(CharCodes2 || (CharCodes2 = {}));
var State;
(function(State2) {
  State2[State2["Text"] = 1] = "Text";
  State2[State2["BeforeTagName"] = 2] = "BeforeTagName";
  State2[State2["InTagName"] = 3] = "InTagName";
  State2[State2["InSelfClosingTag"] = 4] = "InSelfClosingTag";
  State2[State2["BeforeClosingTagName"] = 5] = "BeforeClosingTagName";
  State2[State2["InClosingTagName"] = 6] = "InClosingTagName";
  State2[State2["AfterClosingTagName"] = 7] = "AfterClosingTagName";
  State2[State2["BeforeAttributeName"] = 8] = "BeforeAttributeName";
  State2[State2["InAttributeName"] = 9] = "InAttributeName";
  State2[State2["AfterAttributeName"] = 10] = "AfterAttributeName";
  State2[State2["BeforeAttributeValue"] = 11] = "BeforeAttributeValue";
  State2[State2["InAttributeValueDq"] = 12] = "InAttributeValueDq";
  State2[State2["InAttributeValueSq"] = 13] = "InAttributeValueSq";
  State2[State2["InAttributeValueNq"] = 14] = "InAttributeValueNq";
  State2[State2["BeforeDeclaration"] = 15] = "BeforeDeclaration";
  State2[State2["InDeclaration"] = 16] = "InDeclaration";
  State2[State2["InProcessingInstruction"] = 17] = "InProcessingInstruction";
  State2[State2["BeforeComment"] = 18] = "BeforeComment";
  State2[State2["CDATASequence"] = 19] = "CDATASequence";
  State2[State2["DeclarationSequence"] = 20] = "DeclarationSequence";
  State2[State2["InSpecialComment"] = 21] = "InSpecialComment";
  State2[State2["InCommentLike"] = 22] = "InCommentLike";
  State2[State2["SpecialStartSequence"] = 23] = "SpecialStartSequence";
  State2[State2["InSpecialTag"] = 24] = "InSpecialTag";
  State2[State2["InPlainText"] = 25] = "InPlainText";
  State2[State2["InEntity"] = 26] = "InEntity";
})(State || (State = {}));
function isWhitespace(c) {
  return c === CharCodes2.Space || c === CharCodes2.NewLine || c === CharCodes2.Tab || c === CharCodes2.FormFeed || c === CharCodes2.CarriageReturn;
}
function isEndOfTagSection(c) {
  return c === CharCodes2.Slash || c === CharCodes2.Gt || isWhitespace(c);
}
function isASCIIAlpha(c) {
  return c >= CharCodes2.LowerA && c <= CharCodes2.LowerZ || c >= CharCodes2.UpperA && c <= CharCodes2.UpperZ;
}
var QuoteType;
(function(QuoteType2) {
  QuoteType2[QuoteType2["NoValue"] = 0] = "NoValue";
  QuoteType2[QuoteType2["Unquoted"] = 1] = "Unquoted";
  QuoteType2[QuoteType2["Single"] = 2] = "Single";
  QuoteType2[QuoteType2["Double"] = 3] = "Double";
})(QuoteType || (QuoteType = {}));
var Sequences = {
  Empty: new Uint8Array(0),
  Cdata: new Uint8Array([67, 68, 65, 84, 65, 91]),
  // CDATA[
  CdataEnd: new Uint8Array([93, 93, 62]),
  // ]]>
  CommentEnd: new Uint8Array([45, 45, 33, 62]),
  // `--!>`
  Doctype: new Uint8Array([100, 111, 99, 116, 121, 112, 101]),
  // `doctype`
  IframeEnd: new Uint8Array([60, 47, 105, 102, 114, 97, 109, 101]),
  // `</iframe`
  NoembedEnd: new Uint8Array([
    60,
    47,
    110,
    111,
    101,
    109,
    98,
    101,
    100
  ]),
  // `</noembed`
  NoframesEnd: new Uint8Array([
    60,
    47,
    110,
    111,
    102,
    114,
    97,
    109,
    101,
    115
  ]),
  // `</noframes`
  Plaintext: new Uint8Array([
    60,
    47,
    112,
    108,
    97,
    105,
    110,
    116,
    101,
    120,
    116
  ]),
  // `</plaintext`
  ScriptEnd: new Uint8Array([60, 47, 115, 99, 114, 105, 112, 116]),
  // `</script`
  StyleEnd: new Uint8Array([60, 47, 115, 116, 121, 108, 101]),
  // `</style`
  TitleEnd: new Uint8Array([60, 47, 116, 105, 116, 108, 101]),
  // `</title`
  TextareaEnd: new Uint8Array([
    60,
    47,
    116,
    101,
    120,
    116,
    97,
    114,
    101,
    97
  ]),
  // `</textarea`
  XmpEnd: new Uint8Array([60, 47, 120, 109, 112])
  // `</xmp`
};
var specialStartSequences = /* @__PURE__ */ new Map([
  [Sequences.IframeEnd[2], Sequences.IframeEnd],
  [Sequences.NoembedEnd[2], Sequences.NoembedEnd],
  [Sequences.Plaintext[2], Sequences.Plaintext],
  [Sequences.ScriptEnd[2], Sequences.ScriptEnd],
  [Sequences.TitleEnd[2], Sequences.TitleEnd],
  [Sequences.XmpEnd[2], Sequences.XmpEnd]
]);
var Tokenizer = class {
  cbs;
  /** The current state the tokenizer is in. */
  state = State.Text;
  /** The read buffer. */
  buffer = "";
  /** The beginning of the section that is currently being read. */
  sectionStart = 0;
  /** The index within the buffer that we are currently looking at. */
  index = 0;
  /** The start of the last entity. */
  entityStart = 0;
  /** Some behavior, eg. when decoding entities, is done while we are in another state. This keeps track of the other state type. */
  baseState = State.Text;
  /** For special parsing behavior inside of script and style tags. */
  isSpecial = false;
  /** Indicates whether the tokenizer has been paused. */
  running = true;
  /** The offset of the current buffer. */
  offset = 0;
  xmlMode;
  decodeEntities;
  recognizeSelfClosing;
  entityDecoder;
  constructor({ xmlMode = false, decodeEntities = true, recognizeSelfClosing = xmlMode }, cbs) {
    this.cbs = cbs;
    this.xmlMode = xmlMode;
    this.decodeEntities = decodeEntities;
    this.recognizeSelfClosing = recognizeSelfClosing;
    this.entityDecoder = new EntityDecoder(xmlMode ? xmlDecodeTree : htmlDecodeTree, (cp2, consumed) => this.emitCodePoint(cp2, consumed));
  }
  reset() {
    this.state = State.Text;
    this.buffer = "";
    this.sectionStart = 0;
    this.index = 0;
    this.baseState = State.Text;
    this.isSpecial = false;
    this.currentSequence = Sequences.Empty;
    this.sequenceIndex = 0;
    this.running = true;
    this.offset = 0;
  }
  write(chunk) {
    this.offset += this.buffer.length;
    this.buffer = chunk;
    this.parse();
  }
  end() {
    if (this.running)
      this.finish();
  }
  pause() {
    this.running = false;
  }
  resume() {
    this.running = true;
    if (this.index < this.buffer.length + this.offset) {
      this.parse();
    }
  }
  stateText(c) {
    if (c === CharCodes2.Lt || !this.decodeEntities && this.fastForwardTo(CharCodes2.Lt)) {
      if (this.index > this.sectionStart) {
        this.cbs.ontext(this.sectionStart, this.index);
      }
      this.state = State.BeforeTagName;
      this.sectionStart = this.index;
    } else if (this.decodeEntities && c === CharCodes2.Amp) {
      this.startEntity();
    }
  }
  currentSequence = Sequences.Empty;
  sequenceIndex = 0;
  enterTagBody() {
    if (this.currentSequence === Sequences.Plaintext) {
      this.currentSequence = Sequences.Empty;
      this.state = State.InPlainText;
    } else if (this.isSpecial) {
      this.state = State.InSpecialTag;
      this.sequenceIndex = 0;
    } else {
      this.state = State.Text;
    }
  }
  /**
   * Match the opening tag name against an HTML text-only tag sequence.
   *
   * Some tags share an initial prefix (`script`/`style`, `title`/`textarea`,
   * `noembed`/`noframes`), so we may switch to an alternate sequence at the
   * first distinguishing byte.  On a successful full match we fall back to
   * the normal tag-name state; a later `>` will enter raw-text, RCDATA, or
   * plaintext mode based on `currentSequence` / `isSpecial`.
   * @param c Current character code point.
   */
  stateSpecialStartSequence(c) {
    const lower = c | 32;
    if (this.sequenceIndex < this.currentSequence.length) {
      if (lower === this.currentSequence[this.sequenceIndex]) {
        this.sequenceIndex++;
        return;
      }
      if (this.sequenceIndex === 3) {
        if (this.currentSequence === Sequences.ScriptEnd && lower === Sequences.StyleEnd[3]) {
          this.currentSequence = Sequences.StyleEnd;
          this.sequenceIndex = 4;
          return;
        }
        if (this.currentSequence === Sequences.TitleEnd && lower === Sequences.TextareaEnd[3]) {
          this.currentSequence = Sequences.TextareaEnd;
          this.sequenceIndex = 4;
          return;
        }
      } else if (this.sequenceIndex === 4 && this.currentSequence === Sequences.NoembedEnd && lower === Sequences.NoframesEnd[4]) {
        this.currentSequence = Sequences.NoframesEnd;
        this.sequenceIndex = 5;
        return;
      }
    } else if (isEndOfTagSection(c)) {
      this.sequenceIndex = 0;
      this.state = State.InTagName;
      this.stateInTagName(c);
      return;
    }
    this.isSpecial = false;
    this.currentSequence = Sequences.Empty;
    this.sequenceIndex = 0;
    this.state = State.InTagName;
    this.stateInTagName(c);
  }
  stateCDATASequence(c) {
    if (c === Sequences.Cdata[this.sequenceIndex]) {
      if (++this.sequenceIndex === Sequences.Cdata.length) {
        this.state = State.InCommentLike;
        this.currentSequence = Sequences.CdataEnd;
        this.sequenceIndex = 0;
        this.sectionStart = this.index + 1;
      }
    } else {
      this.sequenceIndex = 0;
      if (this.xmlMode) {
        this.state = State.InDeclaration;
        this.stateInDeclaration(c);
      } else {
        this.state = State.InSpecialComment;
        this.stateInSpecialComment(c);
      }
    }
  }
  /**
   * When we wait for one specific character, we can speed things up
   * by skipping through the buffer until we find it.
   * @param c Current character code point.
   * @returns Whether the character was found.
   */
  fastForwardTo(c) {
    while (++this.index < this.buffer.length + this.offset) {
      if (this.buffer.charCodeAt(this.index - this.offset) === c) {
        return true;
      }
    }
    this.index = this.buffer.length + this.offset - 1;
    return false;
  }
  /**
   * Emit a comment token and return to the text state.
   * @param offset Number of characters in the end sequence that have already been matched.
   */
  emitComment(offset) {
    this.cbs.oncomment(this.sectionStart, this.index, offset);
    this.sequenceIndex = 0;
    this.sectionStart = this.index + 1;
    this.state = State.Text;
  }
  /**
   * Comments and CDATA end with `-->` and `]]>`.
   *
   * Their common qualities are:
   * - Their end sequences have a distinct character they start with.
   * - That character is then repeated, so we have to check multiple repeats.
   * - All characters but the start character of the sequence can be skipped.
   * @param c Current character code point.
   */
  stateInCommentLike(c) {
    if (!this.xmlMode && this.currentSequence === Sequences.CommentEnd && this.sequenceIndex <= 1 && /*
     * We're still at the very start of the comment: the only
     * characters consumed since `<!--` are the dashes that
     * advanced sequenceIndex (0 for `<!-->`, 1 for `<!--->`).
     */
    this.index === this.sectionStart + this.sequenceIndex && c === CharCodes2.Gt) {
      this.emitComment(this.sequenceIndex);
    } else if (this.currentSequence === Sequences.CommentEnd && this.sequenceIndex === 2 && c === CharCodes2.Gt) {
      this.emitComment(2);
    } else if (this.currentSequence === Sequences.CommentEnd && this.sequenceIndex === this.currentSequence.length - 1 && c !== CharCodes2.Gt) {
      this.sequenceIndex = Number(c === CharCodes2.Dash);
    } else if (c === this.currentSequence[this.sequenceIndex]) {
      if (++this.sequenceIndex === this.currentSequence.length) {
        if (this.currentSequence === Sequences.CdataEnd) {
          this.cbs.oncdata(this.sectionStart, this.index, 2);
        } else {
          this.cbs.oncomment(this.sectionStart, this.index, 3);
        }
        this.sequenceIndex = 0;
        this.sectionStart = this.index + 1;
        this.state = State.Text;
      }
    } else if (this.sequenceIndex === 0) {
      if (this.fastForwardTo(this.currentSequence[0])) {
        this.sequenceIndex = 1;
      }
    } else if (c !== this.currentSequence[this.sequenceIndex - 1]) {
      this.sequenceIndex = 0;
    }
  }
  /**
   * HTML only allows ASCII alpha characters (a-z and A-Z) at the beginning of a tag name.
   *
   * XML allows a lot more characters here (@see https://www.w3.org/TR/REC-xml/#NT-NameStartChar).
   * We allow anything that wouldn't end the tag.
   * @param c Current character code point.
   */
  isTagStartChar(c) {
    return this.xmlMode ? !isEndOfTagSection(c) : isASCIIAlpha(c);
  }
  /**
   * Scan raw-text / RCDATA content for the matching end tag.
   *
   * For RCDATA tags (`<title>`, `<textarea>`) entities are decoded inline.
   * For raw-text tags (`<script>`, `<style>`, etc.) we fast-forward to `<`.
   * @param c Current character code point.
   */
  stateInSpecialTag(c) {
    if (this.sequenceIndex === this.currentSequence.length) {
      if (isEndOfTagSection(c)) {
        const endOfText = this.index - this.currentSequence.length;
        if (this.sectionStart < endOfText) {
          const actualIndex = this.index;
          this.index = endOfText;
          this.cbs.ontext(this.sectionStart, endOfText);
          this.index = actualIndex;
        }
        this.isSpecial = false;
        this.sectionStart = endOfText + 2;
        this.stateInClosingTagName(c);
        return;
      }
      this.sequenceIndex = 0;
    }
    if ((c | 32) === this.currentSequence[this.sequenceIndex]) {
      this.sequenceIndex += 1;
    } else if (this.sequenceIndex === 0) {
      if (this.currentSequence === Sequences.TitleEnd || this.currentSequence === Sequences.TextareaEnd) {
        if (this.decodeEntities && c === CharCodes2.Amp) {
          this.startEntity();
        }
      } else if (this.fastForwardTo(CharCodes2.Lt)) {
        this.sequenceIndex = 1;
      }
    } else {
      this.sequenceIndex = Number(c === CharCodes2.Lt);
    }
  }
  stateBeforeTagName(c) {
    if (c === CharCodes2.ExclamationMark) {
      this.state = State.BeforeDeclaration;
      this.sectionStart = this.index + 1;
    } else if (c === CharCodes2.Questionmark) {
      if (this.xmlMode) {
        this.state = State.InProcessingInstruction;
        this.sequenceIndex = 0;
        this.sectionStart = this.index + 1;
      } else {
        this.state = State.InSpecialComment;
        this.sectionStart = this.index;
      }
    } else if (this.isTagStartChar(c)) {
      this.sectionStart = this.index;
      const special = this.xmlMode || this.cbs.isInForeignContext?.() ? void 0 : specialStartSequences.get(c | 32);
      if (special === void 0) {
        this.state = State.InTagName;
      } else {
        this.isSpecial = true;
        this.currentSequence = special;
        this.sequenceIndex = 3;
        this.state = State.SpecialStartSequence;
      }
    } else if (c === CharCodes2.Slash) {
      this.state = State.BeforeClosingTagName;
    } else {
      this.state = State.Text;
      this.stateText(c);
    }
  }
  stateInTagName(c) {
    if (isEndOfTagSection(c)) {
      this.cbs.onopentagname(this.sectionStart, this.index);
      this.sectionStart = -1;
      this.state = State.BeforeAttributeName;
      this.stateBeforeAttributeName(c);
    }
  }
  stateBeforeClosingTagName(c) {
    if (isWhitespace(c)) {
      if (this.xmlMode) {
      } else {
        this.state = State.InSpecialComment;
        this.sectionStart = this.index;
      }
    } else if (c === CharCodes2.Gt) {
      this.state = State.Text;
      if (!this.xmlMode) {
        this.sectionStart = this.index + 1;
      }
    } else {
      this.state = this.isTagStartChar(c) ? State.InClosingTagName : State.InSpecialComment;
      this.sectionStart = this.index;
    }
  }
  stateInClosingTagName(c) {
    if (isEndOfTagSection(c)) {
      this.cbs.onclosetag(this.sectionStart, this.index);
      this.sectionStart = -1;
      this.state = State.AfterClosingTagName;
      this.stateAfterClosingTagName(c);
    }
  }
  stateAfterClosingTagName(c) {
    if (c === CharCodes2.Gt || this.fastForwardTo(CharCodes2.Gt)) {
      this.state = State.Text;
      this.sectionStart = this.index + 1;
    }
  }
  stateBeforeAttributeName(c) {
    if (c === CharCodes2.Gt) {
      this.cbs.onopentagend(this.index);
      this.enterTagBody();
      this.sectionStart = this.index + 1;
    } else if (c === CharCodes2.Slash) {
      this.state = State.InSelfClosingTag;
    } else if (!isWhitespace(c)) {
      this.state = State.InAttributeName;
      this.sectionStart = this.index;
    }
  }
  /**
   * Handle `/` before `>` in an opening tag.
   *
   * In HTML mode, text-only tags ignore the self-closing flag and still enter
   * their raw-text/RCDATA/plaintext state unless self-closing tags are being
   * recognized. In XML mode, or for ordinary tags, the tokenizer returns to
   * regular text parsing after emitting the self-closing callback.
   * @param c Current character code point.
   */
  stateInSelfClosingTag(c) {
    if (c === CharCodes2.Gt) {
      this.cbs.onselfclosingtag(this.index);
      this.sectionStart = this.index + 1;
      if (!this.recognizeSelfClosing) {
        this.enterTagBody();
        return;
      }
      this.state = State.Text;
      this.isSpecial = false;
      this.currentSequence = Sequences.Empty;
    } else if (!isWhitespace(c)) {
      this.state = State.BeforeAttributeName;
      this.stateBeforeAttributeName(c);
    }
  }
  stateInAttributeName(c) {
    if (c === CharCodes2.Eq || isEndOfTagSection(c)) {
      this.cbs.onattribname(this.sectionStart, this.index);
      this.sectionStart = this.index;
      this.state = State.AfterAttributeName;
      this.stateAfterAttributeName(c);
    }
  }
  stateAfterAttributeName(c) {
    if (c === CharCodes2.Eq) {
      this.state = State.BeforeAttributeValue;
    } else if (c === CharCodes2.Slash || c === CharCodes2.Gt) {
      this.cbs.onattribend(QuoteType.NoValue, this.sectionStart);
      this.sectionStart = -1;
      this.state = State.BeforeAttributeName;
      this.stateBeforeAttributeName(c);
    } else if (!isWhitespace(c)) {
      this.cbs.onattribend(QuoteType.NoValue, this.sectionStart);
      this.state = State.InAttributeName;
      this.sectionStart = this.index;
    }
  }
  stateBeforeAttributeValue(c) {
    if (c === CharCodes2.DoubleQuote) {
      this.state = State.InAttributeValueDq;
      this.sectionStart = this.index + 1;
    } else if (c === CharCodes2.SingleQuote) {
      this.state = State.InAttributeValueSq;
      this.sectionStart = this.index + 1;
    } else if (!isWhitespace(c)) {
      this.sectionStart = this.index;
      this.state = State.InAttributeValueNq;
      this.stateInAttributeValueNoQuotes(c);
    }
  }
  handleInAttributeValue(c, quote) {
    if (c === quote || !this.decodeEntities && this.fastForwardTo(quote)) {
      this.cbs.onattribdata(this.sectionStart, this.index);
      this.sectionStart = -1;
      this.cbs.onattribend(quote === CharCodes2.DoubleQuote ? QuoteType.Double : QuoteType.Single, this.index + 1);
      this.state = State.BeforeAttributeName;
    } else if (this.decodeEntities && c === CharCodes2.Amp) {
      this.startEntity();
    }
  }
  stateInAttributeValueDoubleQuotes(c) {
    this.handleInAttributeValue(c, CharCodes2.DoubleQuote);
  }
  stateInAttributeValueSingleQuotes(c) {
    this.handleInAttributeValue(c, CharCodes2.SingleQuote);
  }
  stateInAttributeValueNoQuotes(c) {
    if (isWhitespace(c) || c === CharCodes2.Gt) {
      this.cbs.onattribdata(this.sectionStart, this.index);
      this.sectionStart = -1;
      this.cbs.onattribend(QuoteType.Unquoted, this.index);
      this.state = State.BeforeAttributeName;
      this.stateBeforeAttributeName(c);
    } else if (this.decodeEntities && c === CharCodes2.Amp) {
      this.startEntity();
    }
  }
  /**
   * Distinguish between CDATA, declarations, HTML comments, and HTML bogus
   * comments after `<!`.
   *
   * In HTML mode, only real comments and doctypes stay on declaration paths;
   * everything else becomes a bogus comment terminated by the next `>`.
   * @param c Current character code point.
   */
  stateBeforeDeclaration(c) {
    if (c === CharCodes2.OpeningSquareBracket) {
      this.state = State.CDATASequence;
      this.sequenceIndex = 0;
    } else if (this.xmlMode) {
      this.state = c === CharCodes2.Dash ? State.BeforeComment : State.InDeclaration;
    } else if ((c | 32) === Sequences.Doctype[0]) {
      this.state = State.DeclarationSequence;
      this.currentSequence = Sequences.Doctype;
      this.sequenceIndex = 1;
    } else if (c === CharCodes2.Gt) {
      this.cbs.oncomment(this.sectionStart, this.index, 0);
      this.state = State.Text;
      this.sectionStart = this.index + 1;
    } else if (c === CharCodes2.Dash) {
      this.state = State.BeforeComment;
    } else {
      this.state = State.InSpecialComment;
    }
  }
  /**
   * Continue matching `doctype` after `<!d`.
   *
   * A full `doctype` match stays on the declaration path; any other name falls
   * back to an HTML bogus comment, which matches browser behavior for
   * non-doctype `<!...>` constructs.
   * @param c Current character code point.
   */
  stateDeclarationSequence(c) {
    if (this.sequenceIndex === this.currentSequence.length) {
      this.state = State.InDeclaration;
      this.stateInDeclaration(c);
    } else if ((c | 32) === this.currentSequence[this.sequenceIndex]) {
      this.sequenceIndex += 1;
    } else if (c === CharCodes2.Gt) {
      this.cbs.oncomment(this.sectionStart, this.index, 0);
      this.state = State.Text;
      this.sectionStart = this.index + 1;
    } else {
      this.state = State.InSpecialComment;
    }
  }
  stateInDeclaration(c) {
    if (c === CharCodes2.Gt || this.fastForwardTo(CharCodes2.Gt)) {
      this.cbs.ondeclaration(this.sectionStart, this.index);
      this.state = State.Text;
      this.sectionStart = this.index + 1;
    }
  }
  /**
   * XML processing instructions (`<?...?>`).
   *
   * In HTML mode `<?` is routed to `InSpecialComment` instead, so this
   * state is only reachable in XML mode.
   * @param c Current character code point.
   */
  stateInProcessingInstruction(c) {
    if (c === CharCodes2.Questionmark) {
      this.sequenceIndex = 1;
    } else if (c === CharCodes2.Gt && this.sequenceIndex === 1) {
      this.cbs.onprocessinginstruction(this.sectionStart, this.index - 1);
      this.sequenceIndex = 0;
      this.state = State.Text;
      this.sectionStart = this.index + 1;
    } else {
      this.sequenceIndex = Number(this.fastForwardTo(CharCodes2.Questionmark));
    }
  }
  stateBeforeComment(c) {
    if (c === CharCodes2.Dash) {
      this.state = State.InCommentLike;
      this.currentSequence = Sequences.CommentEnd;
      this.sequenceIndex = 0;
      this.sectionStart = this.index + 1;
    } else if (this.xmlMode) {
      this.state = State.InDeclaration;
    } else if (c === CharCodes2.Gt) {
      this.cbs.oncomment(this.sectionStart, this.index, 0);
      this.state = State.Text;
      this.sectionStart = this.index + 1;
    } else {
      this.state = State.InSpecialComment;
    }
  }
  stateInSpecialComment(c) {
    if (c === CharCodes2.Gt || this.fastForwardTo(CharCodes2.Gt)) {
      this.cbs.oncomment(this.sectionStart, this.index, 0);
      this.state = State.Text;
      this.sectionStart = this.index + 1;
    }
  }
  startEntity() {
    this.baseState = this.state;
    this.state = State.InEntity;
    this.entityStart = this.index;
    this.entityDecoder.startEntity(this.xmlMode ? DecodingMode.Strict : this.baseState === State.Text || this.baseState === State.InSpecialTag ? DecodingMode.Legacy : DecodingMode.Attribute);
  }
  stateInEntity() {
    const indexInBuffer = this.index - this.offset;
    const length = this.entityDecoder.write(this.buffer, indexInBuffer);
    if (length >= 0) {
      this.state = this.baseState;
      if (length === 0) {
        this.index -= 1;
      }
    } else {
      if (indexInBuffer < this.buffer.length && this.buffer.charCodeAt(indexInBuffer) === CharCodes2.Amp) {
        this.state = this.baseState;
        this.index -= 1;
        return;
      }
      this.index = this.offset + this.buffer.length - 1;
    }
  }
  /**
   * Remove data that has already been consumed from the buffer.
   */
  cleanup() {
    if (this.running && this.sectionStart !== this.index) {
      if (this.state === State.Text || this.state === State.InPlainText || this.state === State.InSpecialTag && this.sequenceIndex === 0) {
        this.cbs.ontext(this.sectionStart, this.index);
        this.sectionStart = this.index;
      } else if (this.state === State.InAttributeValueDq || this.state === State.InAttributeValueSq || this.state === State.InAttributeValueNq) {
        this.cbs.onattribdata(this.sectionStart, this.index);
        this.sectionStart = this.index;
      }
    }
  }
  shouldContinue() {
    return this.index < this.buffer.length + this.offset && this.running;
  }
  /**
   * Iterates through the buffer, calling the function corresponding to the current state.
   *
   * States that are more likely to be hit are higher up, as a performance improvement.
   */
  parse() {
    while (this.shouldContinue()) {
      const c = this.buffer.charCodeAt(this.index - this.offset);
      switch (this.state) {
        case State.Text: {
          this.stateText(c);
          break;
        }
        case State.InPlainText: {
          this.index = this.buffer.length + this.offset - 1;
          break;
        }
        case State.SpecialStartSequence: {
          this.stateSpecialStartSequence(c);
          break;
        }
        case State.InSpecialTag: {
          this.stateInSpecialTag(c);
          break;
        }
        case State.CDATASequence: {
          this.stateCDATASequence(c);
          break;
        }
        case State.DeclarationSequence: {
          this.stateDeclarationSequence(c);
          break;
        }
        case State.InAttributeValueDq: {
          this.stateInAttributeValueDoubleQuotes(c);
          break;
        }
        case State.InAttributeName: {
          this.stateInAttributeName(c);
          break;
        }
        case State.InCommentLike: {
          this.stateInCommentLike(c);
          break;
        }
        case State.InSpecialComment: {
          this.stateInSpecialComment(c);
          break;
        }
        case State.BeforeAttributeName: {
          this.stateBeforeAttributeName(c);
          break;
        }
        case State.InTagName: {
          this.stateInTagName(c);
          break;
        }
        case State.InClosingTagName: {
          this.stateInClosingTagName(c);
          break;
        }
        case State.BeforeTagName: {
          this.stateBeforeTagName(c);
          break;
        }
        case State.AfterAttributeName: {
          this.stateAfterAttributeName(c);
          break;
        }
        case State.InAttributeValueSq: {
          this.stateInAttributeValueSingleQuotes(c);
          break;
        }
        case State.BeforeAttributeValue: {
          this.stateBeforeAttributeValue(c);
          break;
        }
        case State.BeforeClosingTagName: {
          this.stateBeforeClosingTagName(c);
          break;
        }
        case State.AfterClosingTagName: {
          this.stateAfterClosingTagName(c);
          break;
        }
        case State.InAttributeValueNq: {
          this.stateInAttributeValueNoQuotes(c);
          break;
        }
        case State.InSelfClosingTag: {
          this.stateInSelfClosingTag(c);
          break;
        }
        case State.InDeclaration: {
          this.stateInDeclaration(c);
          break;
        }
        case State.BeforeDeclaration: {
          this.stateBeforeDeclaration(c);
          break;
        }
        case State.BeforeComment: {
          this.stateBeforeComment(c);
          break;
        }
        case State.InProcessingInstruction: {
          this.stateInProcessingInstruction(c);
          break;
        }
        case State.InEntity: {
          this.stateInEntity();
          break;
        }
      }
      this.index++;
    }
    this.cleanup();
  }
  finish() {
    if (this.state === State.InEntity) {
      this.entityDecoder.end();
      this.state = this.baseState;
    }
    this.handleTrailingData();
    this.cbs.onend();
  }
  handleTrailingCommentLikeData(endIndex) {
    if (this.state !== State.InCommentLike) {
      return false;
    }
    if (this.currentSequence === Sequences.CdataEnd) {
      if (this.xmlMode) {
        if (this.sectionStart < endIndex) {
          this.cbs.oncdata(this.sectionStart, endIndex, 0);
        }
      } else {
        const cdataStart = this.sectionStart - Sequences.Cdata.length - 1;
        this.cbs.oncomment(cdataStart, endIndex, 0);
      }
    } else {
      const offset = this.xmlMode ? 0 : Math.min(this.sequenceIndex, Sequences.CommentEnd.length - 1);
      this.cbs.oncomment(this.sectionStart, endIndex, offset);
    }
    return true;
  }
  handleTrailingMarkupDeclaration(endIndex) {
    if (this.xmlMode) {
      switch (this.state) {
        case State.InSpecialComment:
        case State.BeforeComment:
        case State.CDATASequence:
        case State.DeclarationSequence:
        case State.InDeclaration: {
          this.cbs.ontext(this.sectionStart, endIndex);
          return true;
        }
        default: {
          return false;
        }
      }
    }
    switch (this.state) {
      case State.BeforeDeclaration:
      case State.InSpecialComment:
      case State.BeforeComment:
      case State.CDATASequence: {
        this.cbs.oncomment(this.sectionStart, endIndex, 0);
        return true;
      }
      case State.DeclarationSequence: {
        if (this.sequenceIndex !== Sequences.Doctype.length) {
          this.cbs.oncomment(this.sectionStart, endIndex, 0);
        }
        return true;
      }
      case State.InDeclaration: {
        return true;
      }
      default: {
        return false;
      }
    }
  }
  /** Handle any trailing data. */
  handleTrailingData() {
    const endIndex = this.buffer.length + this.offset;
    if (this.handleTrailingCommentLikeData(endIndex) || this.handleTrailingMarkupDeclaration(endIndex)) {
      return;
    }
    if (this.sectionStart >= endIndex) {
      return;
    }
    switch (this.state) {
      case State.InTagName:
      case State.BeforeAttributeName:
      case State.BeforeAttributeValue:
      case State.AfterAttributeName:
      case State.InAttributeName:
      case State.InAttributeValueSq:
      case State.InAttributeValueDq:
      case State.InAttributeValueNq:
      case State.InClosingTagName: {
        break;
      }
      default: {
        this.cbs.ontext(this.sectionStart, endIndex);
      }
    }
  }
  emitCodePoint(cp2, consumed) {
    if (this.baseState !== State.Text && this.baseState !== State.InSpecialTag) {
      if (this.sectionStart < this.entityStart) {
        this.cbs.onattribdata(this.sectionStart, this.entityStart);
      }
      this.sectionStart = this.entityStart + consumed;
      this.index = this.sectionStart - 1;
      this.cbs.onattribentity(cp2);
    } else {
      if (this.sectionStart < this.entityStart) {
        this.cbs.ontext(this.sectionStart, this.entityStart);
      }
      this.sectionStart = this.entityStart + consumed;
      this.index = this.sectionStart - 1;
      this.cbs.ontextentity(cp2, this.sectionStart);
    }
  }
};

// node_modules/htmlparser2/dist/Parser.js
var { fromCodePoint } = String;
var formTags = /* @__PURE__ */ new Set([
  "input",
  "option",
  "optgroup",
  "select",
  "button",
  "datalist",
  "textarea"
]);
var pTag = /* @__PURE__ */ new Set(["p"]);
var headingTags = /* @__PURE__ */ new Set(["h1", "h2", "h3", "h4", "h5", "h6", "p"]);
var tableSectionTags = /* @__PURE__ */ new Set(["thead", "tbody"]);
var ddtTags = /* @__PURE__ */ new Set(["dd", "dt"]);
var rtpTags = /* @__PURE__ */ new Set(["rt", "rp"]);
var openImpliesClose = /* @__PURE__ */ new Map([
  ["tr", /* @__PURE__ */ new Set(["tr", "th", "td"])],
  ["th", /* @__PURE__ */ new Set(["th"])],
  ["td", /* @__PURE__ */ new Set(["thead", "th", "td"])],
  ["body", /* @__PURE__ */ new Set(["head", "link", "script"])],
  ["a", /* @__PURE__ */ new Set(["a"])],
  ["li", /* @__PURE__ */ new Set(["li"])],
  ["p", pTag],
  ["h1", headingTags],
  ["h2", headingTags],
  ["h3", headingTags],
  ["h4", headingTags],
  ["h5", headingTags],
  ["h6", headingTags],
  ["select", formTags],
  ["input", formTags],
  ["output", formTags],
  ["button", formTags],
  ["datalist", formTags],
  ["textarea", formTags],
  ["option", /* @__PURE__ */ new Set(["option"])],
  ["optgroup", /* @__PURE__ */ new Set(["optgroup", "option"])],
  ["dd", ddtTags],
  ["dt", ddtTags],
  ["address", pTag],
  ["article", pTag],
  ["aside", pTag],
  ["blockquote", pTag],
  ["details", pTag],
  ["div", pTag],
  ["dl", pTag],
  ["fieldset", pTag],
  ["figcaption", pTag],
  ["figure", pTag],
  ["footer", pTag],
  ["form", pTag],
  ["header", pTag],
  ["hr", pTag],
  ["main", pTag],
  ["nav", pTag],
  ["ol", pTag],
  ["pre", pTag],
  ["section", pTag],
  ["table", pTag],
  ["ul", pTag],
  ["rt", rtpTags],
  ["rp", rtpTags],
  ["tbody", tableSectionTags],
  ["tfoot", tableSectionTags]
]);
var DOCUMENT_TYPE = "doctype";
var voidElements = /* @__PURE__ */ new Set([
  "area",
  "base",
  "basefont",
  "br",
  "col",
  "command",
  "embed",
  "frame",
  "hr",
  "img",
  "input",
  "isindex",
  "keygen",
  "link",
  "meta",
  "param",
  "source",
  "track",
  "wbr"
]);
var foreignContextElements = /* @__PURE__ */ new Set(["math", "svg"]);
var htmlIntegrationElements = /* @__PURE__ */ new Set([
  "mi",
  "mo",
  "mn",
  "ms",
  "mtext",
  "annotation-xml",
  "foreignObject",
  "desc",
  "title"
]);
var svgTagNameAdjustments = /* @__PURE__ */ new Map([
  ["altglyph", "altGlyph"],
  ["altglyphdef", "altGlyphDef"],
  ["altglyphitem", "altGlyphItem"],
  ["animatecolor", "animateColor"],
  ["animatemotion", "animateMotion"],
  ["animatetransform", "animateTransform"],
  ["clippath", "clipPath"],
  ["feblend", "feBlend"],
  ["fecolormatrix", "feColorMatrix"],
  ["fecomponenttransfer", "feComponentTransfer"],
  ["fecomposite", "feComposite"],
  ["feconvolvematrix", "feConvolveMatrix"],
  ["fediffuselighting", "feDiffuseLighting"],
  ["fedisplacementmap", "feDisplacementMap"],
  ["fedistantlight", "feDistantLight"],
  ["fedropshadow", "feDropShadow"],
  ["feflood", "feFlood"],
  ["fefunca", "feFuncA"],
  ["fefuncb", "feFuncB"],
  ["fefuncg", "feFuncG"],
  ["fefuncr", "feFuncR"],
  ["fegaussianblur", "feGaussianBlur"],
  ["feimage", "feImage"],
  ["femerge", "feMerge"],
  ["femergenode", "feMergeNode"],
  ["femorphology", "feMorphology"],
  ["feoffset", "feOffset"],
  ["fepointlight", "fePointLight"],
  ["fespecularlighting", "feSpecularLighting"],
  ["fespotlight", "feSpotLight"],
  ["fetile", "feTile"],
  ["feturbulence", "feTurbulence"],
  ["foreignobject", "foreignObject"],
  ["glyphref", "glyphRef"],
  ["lineargradient", "linearGradient"],
  ["radialgradient", "radialGradient"],
  ["textpath", "textPath"]
]);
var ForeignContext;
(function(ForeignContext2) {
  ForeignContext2[ForeignContext2["None"] = 0] = "None";
  ForeignContext2[ForeignContext2["Svg"] = 1] = "Svg";
  ForeignContext2[ForeignContext2["MathML"] = 2] = "MathML";
})(ForeignContext || (ForeignContext = {}));
var reNameEnd = /\s|\//;
var Parser = class {
  options;
  /** The start index of the last event. */
  startIndex = 0;
  /** The end index of the last event. */
  endIndex = 0;
  /**
   * Store the start index of the current open tag,
   * so we can update the start index for attributes.
   */
  openTagStart = 0;
  tagname = "";
  attribname = "";
  attribvalue = "";
  attribs = null;
  stack = [];
  foreignContext;
  cbs;
  lowerCaseTagNames;
  lowerCaseAttributeNames;
  recognizeSelfClosing;
  /** We are parsing HTML. Inverse of the `xmlMode` option. */
  htmlMode;
  tokenizer;
  buffers = [];
  bufferOffset = 0;
  /** The index of the last written buffer. Used when resuming after a `pause()`. */
  writeIndex = 0;
  /** Indicates whether the parser has finished running / `.end` has been called. */
  ended = false;
  constructor(cbs, options = {}) {
    this.options = options;
    this.cbs = cbs ?? {};
    this.htmlMode = !this.options.xmlMode;
    this.lowerCaseTagNames = options.lowerCaseTags ?? this.htmlMode;
    this.lowerCaseAttributeNames = options.lowerCaseAttributeNames ?? this.htmlMode;
    this.recognizeSelfClosing = options.recognizeSelfClosing ?? !this.htmlMode;
    this.tokenizer = new (options.Tokenizer ?? Tokenizer)(this.options, this);
    this.foreignContext = [ForeignContext.None];
    this.cbs.onparserinit?.(this);
  }
  // Tokenizer event handlers
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  ontext(start, endIndex) {
    const data = this.getSlice(start, endIndex);
    this.endIndex = endIndex - 1;
    this.cbs.ontext?.(data);
    this.startIndex = endIndex;
  }
  /**
   * @param cp Current Unicode code point.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  ontextentity(cp2, endIndex) {
    this.endIndex = endIndex - 1;
    this.cbs.ontext?.(fromCodePoint(cp2));
    this.startIndex = endIndex;
  }
  /** @internal */
  isInForeignContext() {
    return this.foreignContext[0] !== ForeignContext.None;
  }
  /**
   * Checks if the current tag is a void element. Override this if you want
   * to specify your own additional void elements.
   * @param name Name of the pseudo selector.
   */
  isVoidElement(name) {
    return this.htmlMode && voidElements.has(name);
  }
  /**
   * Read a tag name from the buffer.
   *
   * When `lowerCaseTagNames` is enabled (the default in HTML mode), the name
   * is lowercased and may be adjusted for SVG casing or the `image` → `img`
   * alias.
   * @param start Start index of the tag name in the buffer.
   * @param endIndex End index of the tag name in the buffer.
   */
  readTagName(start, endIndex) {
    const name = this.lowerCaseTagNames ? this.getSlice(start, endIndex).toLowerCase() : this.getSlice(start, endIndex);
    if (!(this.lowerCaseTagNames && this.htmlMode)) {
      return name;
    }
    if (this.foreignContext[0] === ForeignContext.Svg) {
      return svgTagNameAdjustments.get(name) ?? name;
    }
    if (this.foreignContext.length > 1) {
      const adjusted = svgTagNameAdjustments.get(name);
      if (adjusted !== void 0 && this.stack.includes(adjusted)) {
        return adjusted;
      }
    }
    if (!this.isInForeignContext()) {
      return name === "image" ? "img" : name;
    }
    return name;
  }
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  onopentagname(start, endIndex) {
    this.endIndex = endIndex;
    this.emitOpenTag(this.readTagName(start, endIndex));
  }
  emitOpenTag(name) {
    this.openTagStart = this.startIndex;
    this.tagname = name;
    if (this.htmlMode && name === "form" && this.stack.includes("form")) {
      this.tagname = "";
      return;
    }
    const impliesClose = this.htmlMode && openImpliesClose.get(name);
    if (impliesClose) {
      while (this.stack.length > 0 && impliesClose.has(this.stack[0])) {
        this.popElement(true);
      }
    }
    if (!this.isVoidElement(name)) {
      this.stack.unshift(name);
      if (this.htmlMode) {
        if (name === "svg") {
          this.foreignContext.unshift(ForeignContext.Svg);
        } else if (name === "math") {
          this.foreignContext.unshift(ForeignContext.MathML);
        } else if (htmlIntegrationElements.has(name)) {
          this.foreignContext.unshift(ForeignContext.None);
        }
      }
    }
    this.cbs.onopentagname?.(name);
    if (this.cbs.onopentag)
      this.attribs = {};
  }
  endOpenTag(isImplied) {
    this.startIndex = this.openTagStart;
    if (this.attribs) {
      this.cbs.onopentag?.(this.tagname, this.attribs, isImplied);
      this.attribs = null;
    }
    if (this.cbs.onclosetag && this.isVoidElement(this.tagname)) {
      this.cbs.onclosetag(this.tagname, true);
    }
    this.tagname = "";
  }
  /**
   * @param endIndex End index for the current parser event.
   * @internal
   */
  onopentagend(endIndex) {
    this.endIndex = endIndex;
    this.endOpenTag(false);
    this.startIndex = endIndex + 1;
  }
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  onclosetag(start, endIndex) {
    this.endIndex = endIndex;
    const name = this.readTagName(start, endIndex);
    if (!this.isVoidElement(name)) {
      const pos = this.stack.indexOf(name);
      if (pos !== -1) {
        for (let index = 0; index < pos; index++) {
          this.popElement(true);
        }
        this.popElement(false);
      } else if (this.htmlMode && name === "p") {
        this.emitOpenTag("p");
        this.closeCurrentTag(true);
      }
    } else if (this.htmlMode && name === "br") {
      this.cbs.onopentagname?.("br");
      this.cbs.onopentag?.("br", {}, true);
      this.cbs.onclosetag?.("br", false);
    }
    this.startIndex = endIndex + 1;
  }
  /**
   * @param endIndex End index for the current parser event.
   * @internal
   */
  onselfclosingtag(endIndex) {
    this.endIndex = endIndex;
    if (this.recognizeSelfClosing || this.isInForeignContext()) {
      this.closeCurrentTag(false);
      this.startIndex = endIndex + 1;
    } else {
      this.onopentagend(endIndex);
    }
  }
  /**
   * Pop the top element off the stack, emit a close event, and maintain
   * the foreign context stack.
   * @param implied Whether this close is implied (not from an explicit end tag).
   */
  popElement(implied) {
    const element = this.stack.shift();
    if (this.htmlMode && (foreignContextElements.has(element) || htmlIntegrationElements.has(element))) {
      this.foreignContext.shift();
    }
    this.cbs.onclosetag?.(element, implied);
  }
  closeCurrentTag(isOpenImplied) {
    const name = this.tagname;
    this.endOpenTag(isOpenImplied);
    if (this.stack[0] === name) {
      this.popElement(!isOpenImplied);
    }
  }
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  onattribname(start, endIndex) {
    this.startIndex = start;
    const name = this.getSlice(start, endIndex);
    this.attribname = this.lowerCaseAttributeNames ? name.toLowerCase() : name;
  }
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  onattribdata(start, endIndex) {
    this.attribvalue += this.getSlice(start, endIndex);
  }
  /**
   * @param cp Current Unicode code point.
   * @internal
   */
  onattribentity(cp2) {
    this.attribvalue += fromCodePoint(cp2);
  }
  /**
   * @param quote Quote type used for the current attribute.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  onattribend(quote, endIndex) {
    this.endIndex = endIndex;
    this.cbs.onattribute?.(this.attribname, this.attribvalue, quote === QuoteType.Double ? '"' : quote === QuoteType.Single ? "'" : quote === QuoteType.NoValue ? void 0 : null);
    if (this.attribs && !Object.hasOwn(this.attribs, this.attribname)) {
      this.attribs[this.attribname] = this.attribvalue;
    }
    this.attribvalue = "";
  }
  getInstructionName(value) {
    const index = value.search(reNameEnd);
    let name = index < 0 ? value : value.substr(0, index);
    if (this.lowerCaseTagNames) {
      name = name.toLowerCase();
    }
    return name;
  }
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  ondeclaration(start, endIndex) {
    this.endIndex = endIndex;
    const value = this.getSlice(start, endIndex);
    if (this.cbs.onprocessinginstruction) {
      const name = this.htmlMode ? this.lowerCaseTagNames ? DOCUMENT_TYPE : value.slice(0, DOCUMENT_TYPE.length) : this.getInstructionName(value);
      this.cbs.onprocessinginstruction(`!${name}`, `!${value}`);
    }
    this.startIndex = endIndex + 1;
  }
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @internal
   */
  onprocessinginstruction(start, endIndex) {
    this.endIndex = endIndex;
    const value = this.getSlice(start, endIndex);
    if (this.cbs.onprocessinginstruction) {
      const name = this.getInstructionName(value);
      this.cbs.onprocessinginstruction(`?${name}`, `?${value}`);
    }
    this.startIndex = endIndex + 1;
  }
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @param offset Offset applied when computing parser indices.
   * @internal
   */
  oncomment(start, endIndex, offset) {
    this.endIndex = endIndex;
    this.cbs.oncomment?.(this.getSlice(start, endIndex - offset));
    this.cbs.oncommentend?.();
    this.startIndex = endIndex + 1;
  }
  /**
   * @param start Start index for the current parser event.
   * @param endIndex End index for the current parser event.
   * @param offset Offset applied when computing parser indices.
   * @internal
   */
  oncdata(start, endIndex, offset) {
    this.endIndex = endIndex;
    const value = this.getSlice(start, endIndex - offset);
    if (!this.htmlMode || this.options.recognizeCDATA) {
      this.cbs.oncdatastart?.();
      this.cbs.ontext?.(value);
      this.cbs.oncdataend?.();
    } else if (this.isInForeignContext()) {
      this.cbs.ontext?.(value);
    } else {
      this.cbs.oncomment?.(`[CDATA[${value}]]`);
      this.cbs.oncommentend?.();
    }
    this.startIndex = endIndex + 1;
  }
  /** @internal */
  onend() {
    if (this.cbs.onclosetag) {
      this.endIndex = this.startIndex;
      for (let index = 0; index < this.stack.length; index++) {
        this.cbs.onclosetag(this.stack[index], true);
      }
    }
    this.cbs.onend?.();
  }
  /**
   * Resets the parser to a blank state, ready to parse a new HTML document
   */
  reset() {
    this.cbs.onreset?.();
    this.tokenizer.reset();
    this.tagname = "";
    this.attribname = "";
    this.attribvalue = "";
    this.attribs = null;
    this.stack.length = 0;
    this.startIndex = 0;
    this.endIndex = 0;
    this.cbs.onparserinit?.(this);
    this.buffers.length = 0;
    this.foreignContext.length = 0;
    this.foreignContext.unshift(ForeignContext.None);
    this.bufferOffset = 0;
    this.writeIndex = 0;
    this.ended = false;
  }
  /**
   * Resets the parser, then parses a complete document and
   * pushes it to the handler.
   * @param data Document to parse.
   */
  parseComplete(data) {
    this.reset();
    this.end(data);
  }
  getSlice(start, end) {
    if (start === end) {
      return "";
    }
    while (start - this.bufferOffset >= this.buffers[0].length) {
      this.shiftBuffer();
    }
    let slice = this.buffers[0].slice(start - this.bufferOffset, end - this.bufferOffset);
    while (end - this.bufferOffset > this.buffers[0].length) {
      this.shiftBuffer();
      slice += this.buffers[0].slice(0, end - this.bufferOffset);
    }
    return slice;
  }
  shiftBuffer() {
    this.bufferOffset += this.buffers[0].length;
    this.writeIndex--;
    this.buffers.shift();
  }
  /**
   * Parses a chunk of data and calls the corresponding callbacks.
   * @param chunk Chunk to parse.
   */
  write(chunk) {
    if (this.ended) {
      this.cbs.onerror?.(new Error(".write() after done!"));
      return;
    }
    this.buffers.push(chunk);
    if (this.tokenizer.running) {
      this.tokenizer.write(chunk);
      this.writeIndex++;
    }
  }
  /**
   * Parses the end of the buffer and clears the stack, calls onend.
   * @param chunk Optional final chunk to parse.
   */
  end(chunk) {
    if (this.ended) {
      this.cbs.onerror?.(new Error(".end() after done!"));
      return;
    }
    if (chunk)
      this.write(chunk);
    this.ended = true;
    this.tokenizer.end();
  }
  /**
   * Pauses parsing. The parser won't emit events until `resume` is called.
   */
  pause() {
    this.tokenizer.pause();
  }
  /**
   * Resumes parsing after `pause` was called.
   */
  resume() {
    this.tokenizer.resume();
    while (this.tokenizer.running && this.writeIndex < this.buffers.length) {
      this.tokenizer.write(this.buffers[this.writeIndex++]);
    }
    if (this.ended)
      this.tokenizer.end();
  }
};

// node_modules/domelementtype/dist/index.js
var ElementType;
(function(ElementType2) {
  ElementType2["Root"] = "root";
  ElementType2["Text"] = "text";
  ElementType2["Directive"] = "directive";
  ElementType2["Comment"] = "comment";
  ElementType2["Script"] = "script";
  ElementType2["Style"] = "style";
  ElementType2["Tag"] = "tag";
  ElementType2["CDATA"] = "cdata";
  ElementType2["Doctype"] = "doctype";
})(ElementType || (ElementType = {}));
function isTag(element) {
  return element.type === ElementType.Tag || element.type === ElementType.Script || element.type === ElementType.Style;
}
var Root = ElementType.Root;
var Text = ElementType.Text;
var Directive = ElementType.Directive;
var Comment = ElementType.Comment;
var Script = ElementType.Script;
var Style = ElementType.Style;
var Tag = ElementType.Tag;
var CDATA = ElementType.CDATA;
var Doctype = ElementType.Doctype;

// node_modules/domhandler/dist/node.js
var Node = class {
  /** Parent of the node */
  parent = null;
  /** Previous sibling */
  prev = null;
  /** Next sibling */
  next = null;
  /** The start index of the node. Requires `withStartIndices` on the handler to be `true. */
  startIndex = null;
  /** The end index of the node. Requires `withEndIndices` on the handler to be `true. */
  endIndex = null;
  // Read-write aliases for properties
  /**
   * Same as {@link parent}.
   * [DOM spec](https://dom.spec.whatwg.org)-compatible alias.
   */
  get parentNode() {
    return this.parent;
  }
  set parentNode(parent) {
    this.parent = parent;
  }
  /**
   * Same as {@link prev}.
   * [DOM spec](https://dom.spec.whatwg.org)-compatible alias.
   */
  get previousSibling() {
    return this.prev;
  }
  set previousSibling(previous) {
    this.prev = previous;
  }
  /**
   * Same as {@link next}.
   * [DOM spec](https://dom.spec.whatwg.org)-compatible alias.
   */
  get nextSibling() {
    return this.next;
  }
  set nextSibling(next) {
    this.next = next;
  }
  /**
   * Clone this node, and optionally its children.
   * @param recursive Clone child nodes as well.
   * @returns A clone of the node.
   */
  cloneNode(recursive = false) {
    return cloneNode(this, recursive);
  }
};
var DataNode = class extends Node {
  data;
  /**
   * @param data The content of the data node
   */
  constructor(data) {
    super();
    this.data = data;
  }
  /**
   * Same as {@link data}.
   * [DOM spec](https://dom.spec.whatwg.org)-compatible alias.
   */
  get nodeValue() {
    return this.data;
  }
  set nodeValue(data) {
    this.data = data;
  }
};
var Text2 = class extends DataNode {
  type = ElementType.Text;
  get nodeType() {
    return 3;
  }
};
var Comment2 = class extends DataNode {
  type = ElementType.Comment;
  get nodeType() {
    return 8;
  }
};
var ProcessingInstruction = class extends DataNode {
  type = ElementType.Directive;
  name;
  constructor(name, data) {
    super(data);
    this.name = name;
  }
  get nodeType() {
    return 1;
  }
  /** If this is a doctype, the document type name (parse5 only). */
  "x-name";
  /** If this is a doctype, the document type public identifier (parse5 only). */
  "x-publicId";
  /** If this is a doctype, the document type system identifier (parse5 only). */
  "x-systemId";
};
var NodeWithChildren = class extends Node {
  children;
  /**
   * @param children Children of the node. Only certain node types can have children.
   */
  constructor(children) {
    super();
    this.children = children;
  }
  // Aliases
  /** First child of the node. */
  get firstChild() {
    return this.children[0] ?? null;
  }
  /** Last child of the node. */
  get lastChild() {
    return this.children.length > 0 ? this.children[this.children.length - 1] : null;
  }
  /**
   * Same as {@link children}.
   * [DOM spec](https://dom.spec.whatwg.org)-compatible alias.
   */
  get childNodes() {
    return this.children;
  }
  set childNodes(children) {
    this.children = children;
  }
};
var CDATA2 = class extends NodeWithChildren {
  type = ElementType.CDATA;
  get nodeType() {
    return 4;
  }
};
var Document = class extends NodeWithChildren {
  type = ElementType.Root;
  get nodeType() {
    return 9;
  }
};
var Element = class extends NodeWithChildren {
  name;
  attribs;
  type;
  /**
   * @param name Name of the tag, eg. `div`, `span`.
   * @param attribs Object mapping attribute names to attribute values.
   * @param children Children of the node.
   * @param type Node type used for the new node instance.
   */
  constructor(name, attribs, children = [], type = name === "script" ? ElementType.Script : name === "style" ? ElementType.Style : ElementType.Tag) {
    super(children);
    this.name = name;
    this.attribs = attribs;
    this.type = type;
  }
  get nodeType() {
    return 1;
  }
  // DOM Level 1 aliases
  /**
   * Same as {@link name}.
   * [DOM spec](https://dom.spec.whatwg.org)-compatible alias.
   */
  get tagName() {
    return this.name;
  }
  set tagName(name) {
    this.name = name;
  }
  get attributes() {
    return Object.keys(this.attribs).map((name) => ({
      name,
      value: this.attribs[name],
      namespace: this["x-attribsNamespace"]?.[name],
      prefix: this["x-attribsPrefix"]?.[name]
    }));
  }
  /** Element namespace (parse5 only). */
  namespace;
  /** Element attribute namespaces (parse5 only). */
  "x-attribsNamespace";
  /** Element attribute namespace-related prefixes (parse5 only). */
  "x-attribsPrefix";
};
function isTag2(node) {
  return isTag(node);
}
function isCDATA(node) {
  return node.type === ElementType.CDATA;
}
function isText(node) {
  return node.type === ElementType.Text;
}
function isComment(node) {
  return node.type === ElementType.Comment;
}
function isDirective(node) {
  return node.type === ElementType.Directive;
}
function isDocument(node) {
  return node.type === ElementType.Root;
}
function hasChildren(node) {
  return Object.hasOwn(node, "children");
}
function cloneNode(node, recursive = false) {
  let result;
  if (isText(node)) {
    result = new Text2(node.data);
  } else if (isComment(node)) {
    result = new Comment2(node.data);
  } else if (isTag2(node)) {
    const children = recursive ? cloneChildren(node.children) : [];
    const clone = new Element(node.name, { ...node.attribs }, children);
    for (const child of children) {
      child.parent = clone;
    }
    if (node.namespace != null) {
      clone.namespace = node.namespace;
    }
    if (node["x-attribsNamespace"]) {
      clone["x-attribsNamespace"] = { ...node["x-attribsNamespace"] };
    }
    if (node["x-attribsPrefix"]) {
      clone["x-attribsPrefix"] = { ...node["x-attribsPrefix"] };
    }
    result = clone;
  } else if (isCDATA(node)) {
    const children = recursive ? cloneChildren(node.children) : [];
    const clone = new CDATA2(children);
    for (const child of children) {
      child.parent = clone;
    }
    result = clone;
  } else if (isDocument(node)) {
    const children = recursive ? cloneChildren(node.children) : [];
    const clone = new Document(children);
    for (const child of children) {
      child.parent = clone;
    }
    if (node["x-mode"]) {
      clone["x-mode"] = node["x-mode"];
    }
    result = clone;
  } else if (isDirective(node)) {
    const instruction = new ProcessingInstruction(node.name, node.data);
    if (node["x-name"] != null) {
      instruction["x-name"] = node["x-name"];
      instruction["x-publicId"] = node["x-publicId"];
      instruction["x-systemId"] = node["x-systemId"];
    }
    result = instruction;
  } else {
    throw new Error(`Not implemented yet: ${node.type}`);
  }
  result.startIndex = node.startIndex;
  result.endIndex = node.endIndex;
  if (node.sourceCodeLocation != null) {
    result.sourceCodeLocation = node.sourceCodeLocation;
  }
  return result;
}
function cloneChildren(childs) {
  const children = childs.map((child) => cloneNode(child, true));
  for (let index = 1; index < children.length; index++) {
    children[index].prev = children[index - 1];
    children[index - 1].next = children[index];
  }
  return children;
}

// node_modules/domhandler/dist/index.js
var defaultOptions = {
  withStartIndices: false,
  withEndIndices: false,
  xmlMode: false
};
var DomHandler = class {
  /** The elements of the DOM */
  dom = [];
  /** The root element for the DOM */
  root = new Document(this.dom);
  /** Called once parsing has completed. */
  callback;
  /** Settings for the handler. */
  options;
  /** Callback whenever a tag is closed. */
  elementCB;
  /** Indicated whether parsing has been completed. */
  done = false;
  /** Stack of open tags. */
  tagStack = [this.root];
  /** A data node that is still being written to. */
  lastNode = null;
  /** Reference to the parser instance. Used for location information. */
  parser = null;
  /**
   * @param callback Called once parsing has completed.
   * @param options Settings for the handler.
   * @param elementCB Callback whenever a tag is closed.
   */
  constructor(callback, options, elementCB) {
    if (typeof options === "function") {
      elementCB = options;
      options = defaultOptions;
    }
    if (typeof callback === "object") {
      options = callback;
      callback = void 0;
    }
    this.callback = callback ?? null;
    this.options = options ?? defaultOptions;
    this.elementCB = elementCB ?? null;
  }
  onparserinit(parser) {
    this.parser = parser;
  }
  // Resets the handler back to starting state
  onreset() {
    this.dom = [];
    this.root = new Document(this.dom);
    this.done = false;
    this.tagStack = [this.root];
    this.lastNode = null;
    this.parser = null;
  }
  // Signals the handler that parsing is done
  onend() {
    if (this.done)
      return;
    this.done = true;
    this.parser = null;
    this.handleCallback(null);
  }
  onerror(error) {
    this.handleCallback(error);
  }
  onclosetag() {
    this.lastNode = null;
    const element = this.tagStack.pop();
    if (this.options.withEndIndices && this.parser) {
      element.endIndex = this.parser.endIndex;
    }
    if (this.elementCB)
      this.elementCB(element);
  }
  onopentag(name, attribs) {
    const type = this.options.xmlMode ? ElementType.Tag : void 0;
    const element = new Element(name, attribs, void 0, type);
    this.addNode(element);
    this.tagStack.push(element);
  }
  ontext(data) {
    const { lastNode } = this;
    if (lastNode && lastNode.type === ElementType.Text) {
      lastNode.data += data;
      if (this.options.withEndIndices && this.parser) {
        lastNode.endIndex = this.parser.endIndex;
      }
    } else {
      const node = new Text2(data);
      this.addNode(node);
      this.lastNode = node;
    }
  }
  oncomment(data) {
    if (this.lastNode && this.lastNode.type === ElementType.Comment) {
      this.lastNode.data += data;
      return;
    }
    const node = new Comment2(data);
    this.addNode(node);
    this.lastNode = node;
  }
  oncommentend() {
    this.lastNode = null;
  }
  oncdatastart() {
    const text = new Text2("");
    const node = new CDATA2([text]);
    this.addNode(node);
    text.parent = node;
    this.lastNode = text;
  }
  oncdataend() {
    this.lastNode = null;
  }
  onprocessinginstruction(name, data) {
    const node = new ProcessingInstruction(name, data);
    this.addNode(node);
  }
  handleCallback(error) {
    if (typeof this.callback === "function") {
      this.callback(error, this.dom);
    } else if (error) {
      throw error;
    }
  }
  addNode(node) {
    const parent = this.tagStack[this.tagStack.length - 1];
    const previousSibling = parent.children[parent.children.length - 1];
    if (this.options.withStartIndices && this.parser) {
      node.startIndex = this.parser.startIndex;
    }
    if (this.options.withEndIndices && this.parser) {
      node.endIndex = this.parser.endIndex;
    }
    parent.children.push(node);
    if (previousSibling) {
      node.prev = previousSibling;
      previousSibling.next = node;
    }
    node.parent = parent;
    this.lastNode = null;
  }
};

// node_modules/domutils/dist/index.js
var dist_exports2 = {};
__export(dist_exports2, {
  DocumentPosition: () => DocumentPosition,
  append: () => append,
  appendChild: () => appendChild,
  compareDocumentPosition: () => compareDocumentPosition,
  existsOne: () => existsOne,
  filter: () => filter,
  find: () => find,
  findAll: () => findAll,
  findOne: () => findOne,
  getAttributeValue: () => getAttributeValue,
  getChildren: () => getChildren,
  getElementById: () => getElementById,
  getElements: () => getElements,
  getElementsByClassName: () => getElementsByClassName,
  getElementsByTagName: () => getElementsByTagName,
  getElementsByTagType: () => getElementsByTagType,
  getFeed: () => getFeed,
  getInnerHTML: () => getInnerHTML,
  getName: () => getName,
  getOuterHTML: () => getOuterHTML,
  getParent: () => getParent,
  getSiblings: () => getSiblings,
  getText: () => getText,
  hasAttrib: () => hasAttrib,
  innerText: () => innerText,
  nextElementSibling: () => nextElementSibling,
  prepend: () => prepend,
  prependChild: () => prependChild,
  prevElementSibling: () => prevElementSibling,
  removeElement: () => removeElement,
  removeSubsets: () => removeSubsets,
  replaceElement: () => replaceElement,
  testElement: () => testElement,
  textContent: () => textContent,
  uniqueSort: () => uniqueSort
});

// node_modules/domutils/dist/querying.js
function filter(test, node, recurse = true, limit = Number.POSITIVE_INFINITY) {
  return find(test, Array.isArray(node) ? node : [node], recurse, limit);
}
function find(test, nodes, recurse, limit) {
  const result = [];
  const nodeStack = [Array.isArray(nodes) ? nodes : [nodes]];
  const indexStack = [0];
  for (; ; ) {
    if (indexStack[0] >= nodeStack[0].length) {
      if (indexStack.length === 1) {
        return result;
      }
      nodeStack.shift();
      indexStack.shift();
      continue;
    }
    const element = nodeStack[0][indexStack[0]++];
    if (test(element)) {
      result.push(element);
      if (--limit <= 0)
        return result;
    }
    if (recurse && hasChildren(element) && element.children.length > 0) {
      indexStack.unshift(0);
      nodeStack.unshift(element.children);
    }
  }
}
function findOne(test, nodes, recurse = true) {
  const searchedNodes = Array.isArray(nodes) ? nodes : [nodes];
  for (const node of searchedNodes) {
    if (isTag2(node) && test(node)) {
      return node;
    }
    if (recurse && hasChildren(node) && node.children.length > 0) {
      const found = findOne(test, node.children, true);
      if (found)
        return found;
    }
  }
  return null;
}
function existsOne(test, nodes) {
  return (Array.isArray(nodes) ? nodes : [nodes]).some((node) => isTag2(node) && test(node) || hasChildren(node) && existsOne(test, node.children));
}
function findAll(test, nodes) {
  const result = [];
  const nodeStack = [Array.isArray(nodes) ? nodes : [nodes]];
  const indexStack = [0];
  for (; ; ) {
    if (indexStack[0] >= nodeStack[0].length) {
      if (nodeStack.length === 1) {
        return result;
      }
      nodeStack.shift();
      indexStack.shift();
      continue;
    }
    const element = nodeStack[0][indexStack[0]++];
    if (isTag2(element) && test(element))
      result.push(element);
    if (hasChildren(element) && element.children.length > 0) {
      indexStack.unshift(0);
      nodeStack.unshift(element.children);
    }
  }
}

// node_modules/domutils/dist/legacy.js
var Checks = {
  tag_name(name) {
    if (typeof name === "function") {
      return (element) => isTag2(element) && name(element.name);
    }
    if (name === "*") {
      return isTag2;
    }
    return (element) => isTag2(element) && element.name === name;
  },
  tag_type(type) {
    if (typeof type === "function") {
      return (element) => type(element.type);
    }
    return (element) => element.type === type;
  },
  tag_contains(data) {
    if (typeof data === "function") {
      return (element) => isText(element) && data(element.data);
    }
    return (element) => isText(element) && element.data === data;
  }
};
function getAttribCheck(attrib, value) {
  if (typeof value === "function") {
    return (element) => isTag2(element) && value(element.attribs[attrib]);
  }
  return (element) => isTag2(element) && element.attribs[attrib] === value;
}
function combineFuncs(a, b) {
  return (element) => a(element) || b(element);
}
function compileTest(options) {
  const funcs = Object.keys(options).map((key) => {
    const value = options[key];
    return Object.hasOwn(Checks, key) ? Checks[key](value) : getAttribCheck(key, value);
  });
  return funcs.length === 0 ? null : funcs.reduce(combineFuncs);
}
function testElement(options, node) {
  const test = compileTest(options);
  return test ? test(node) : true;
}
function getElements(options, nodes, recurse, limit = Number.POSITIVE_INFINITY) {
  const test = compileTest(options);
  return test ? filter(test, nodes, recurse, limit) : [];
}
function getElementById(id2, nodes, recurse = true) {
  if (!Array.isArray(nodes))
    nodes = [nodes];
  return findOne(getAttribCheck("id", id2), nodes, recurse);
}
function getElementsByTagName(tagName, nodes, recurse = true, limit = Number.POSITIVE_INFINITY) {
  return filter(Checks["tag_name"](tagName), nodes, recurse, limit);
}
function getElementsByClassName(className, nodes, recurse = true, limit = Number.POSITIVE_INFINITY) {
  return filter(getAttribCheck("class", className), nodes, recurse, limit);
}
function getElementsByTagType(type, nodes, recurse = true, limit = Number.POSITIVE_INFINITY) {
  return filter(Checks["tag_type"](type), nodes, recurse, limit);
}

// node_modules/entities/dist/escape.js
var xmlCodeMap = /* @__PURE__ */ new Map([
  [34, "&quot;"],
  [38, "&amp;"],
  [39, "&apos;"],
  [60, "&lt;"],
  [62, "&gt;"]
]);
var getCodePoint = typeof String.prototype.codePointAt === "function" ? (input, index) => input.codePointAt(index) : (
  // http://mathiasbynens.be/notes/javascript-encoding#surrogate-formulae
  (c, index) => (c.charCodeAt(index) & 64512) === 55296 ? (c.charCodeAt(index) - 55296) * 1024 + c.charCodeAt(index + 1) - 56320 + 65536 : c.charCodeAt(index)
);
var XML_BITSET_VALUE = 1342177476;
function encodeXML(input) {
  let out;
  let last = 0;
  const { length } = input;
  for (let index = 0; index < length; index++) {
    const char = input.charCodeAt(index);
    if (char < 128 && ((XML_BITSET_VALUE >>> char & 1) === 0 || char >= 64 || char < 32)) {
      continue;
    }
    if (out === void 0)
      out = input.substring(0, index);
    else if (last !== index)
      out += input.substring(last, index);
    if (char < 64) {
      out += xmlCodeMap.get(char);
      last = index + 1;
      continue;
    }
    const cp2 = getCodePoint(input, index);
    out += `&#x${cp2.toString(16)};`;
    if (cp2 !== char)
      index++;
    last = index + 1;
  }
  if (out === void 0)
    return input;
  if (last < length)
    out += input.substr(last);
  return out;
}
function getEscaper(regex, map) {
  return function escape2(data) {
    let match;
    let lastIndex = 0;
    let result = "";
    while (match = regex.exec(data)) {
      if (lastIndex !== match.index) {
        result += data.substring(lastIndex, match.index);
      }
      result += map.get(match[0].charCodeAt(0));
      lastIndex = match.index + 1;
    }
    return result + data.substring(lastIndex);
  };
}
var escapeAttribute = /* @__PURE__ */ getEscaper(/["&\u00A0]/g, /* @__PURE__ */ new Map([
  [34, "&quot;"],
  [38, "&amp;"],
  [160, "&nbsp;"]
]));
var escapeText = /* @__PURE__ */ getEscaper(/[&<>\u00A0]/g, /* @__PURE__ */ new Map([
  [38, "&amp;"],
  [60, "&lt;"],
  [62, "&gt;"],
  [160, "&nbsp;"]
]));

// node_modules/entities/dist/index.js
var EntityLevel;
(function(EntityLevel2) {
  EntityLevel2[EntityLevel2["XML"] = 0] = "XML";
  EntityLevel2[EntityLevel2["HTML"] = 1] = "HTML";
})(EntityLevel || (EntityLevel = {}));
var EncodingMode;
(function(EncodingMode2) {
  EncodingMode2[EncodingMode2["UTF8"] = 0] = "UTF8";
  EncodingMode2[EncodingMode2["ASCII"] = 1] = "ASCII";
  EncodingMode2[EncodingMode2["Extensive"] = 2] = "Extensive";
  EncodingMode2[EncodingMode2["Attribute"] = 3] = "Attribute";
  EncodingMode2[EncodingMode2["Text"] = 4] = "Text";
})(EncodingMode || (EncodingMode = {}));

// node_modules/dom-serializer/dist/foreign-names.js
var elementNames = new Map("altGlyph altGlyphDef altGlyphItem animateColor animateMotion animateTransform clipPath feBlend feColorMatrix feComponentTransfer feComposite feConvolveMatrix feDiffuseLighting feDisplacementMap feDistantLight feDropShadow feFlood feFuncA feFuncB feFuncG feFuncR feGaussianBlur feImage feMerge feMergeNode feMorphology feOffset fePointLight feSpecularLighting feSpotLight feTile feTurbulence foreignObject glyphRef linearGradient radialGradient textPath".split(" ").map((name) => [name.toLowerCase(), name]));
var attributeNames = new Map("definitionURL attributeName attributeType baseFrequency baseProfile calcMode clipPathUnits diffuseConstant edgeMode filterUnits glyphRef gradientTransform gradientUnits kernelMatrix kernelUnitLength keyPoints keySplines keyTimes lengthAdjust limitingConeAngle markerHeight markerUnits markerWidth maskContentUnits maskUnits numOctaves pathLength patternContentUnits patternTransform patternUnits pointsAtX pointsAtY pointsAtZ preserveAlpha preserveAspectRatio primitiveUnits refX refY repeatCount repeatDur requiredExtensions requiredFeatures specularConstant specularExponent spreadMethod startOffset stdDeviation stitchTiles surfaceScale systemLanguage tableValues targetX targetY textLength viewBox viewTarget xChannelSelector yChannelSelector zoomAndPan".split(" ").map((name) => [name.toLowerCase(), name]));

// node_modules/dom-serializer/dist/index.js
var unencodedElements = new Set("style script xmp iframe noembed noframes plaintext noscript".split(" "));
var voidElements2 = new Set("area base basefont br col command embed frame hr img input isindex keygen link meta param source track wbr".split(" "));
var foreignElements = /* @__PURE__ */ new Set(["svg", "math"]);
var foreignModeIntegrationPoints = new Set("mi mo mn ms mtext annotation-xml foreignObject desc title".split(" "));
function render(node, options = {}) {
  const nodes = "length" in node ? node : [node];
  const xmlMode = options.xmlMode ?? false;
  let output = "";
  for (let index = 0; index < nodes.length; index++) {
    output += renderNode(nodes[index], options, xmlMode);
  }
  return output;
}
var dist_default = render;
function renderChildren(children, options, xmlMode) {
  let output = "";
  for (let index = 0; index < children.length; index++) {
    output += renderNode(children[index], options, xmlMode);
  }
  return output;
}
function renderNode(node, options, xmlMode) {
  switch (node.type) {
    case Root: {
      return renderChildren(node.children, options, xmlMode);
    }
    case Directive: {
      return `<${node.data}>`;
    }
    case Comment: {
      return `<!--${node.data}-->`;
    }
    case CDATA: {
      return `<![CDATA[${node.children[0].data}]]>`;
    }
    case Script:
    case Style:
    case Tag: {
      return renderTag(node, options, xmlMode);
    }
    case Text: {
      const element = node;
      const data = element.data || "";
      if ((options.encodeEntities ?? options.decodeEntities) !== false && !(!xmlMode && element.parent && unencodedElements.has(element.parent.name))) {
        return xmlMode || options.encodeEntities !== "utf8" ? encodeXML(data) : escapeText(data);
      }
      return data;
    }
  }
}
function renderTag(element, options, xmlMode) {
  if (xmlMode === "foreign") {
    element.name = elementNames.get(element.name) ?? element.name;
    if (element.parent && foreignModeIntegrationPoints.has(element.parent.name)) {
      xmlMode = false;
    }
  }
  if (!xmlMode && foreignElements.has(element.name)) {
    xmlMode = "foreign";
  }
  const { name, children } = element;
  const isVoid = !xmlMode && voidElements2.has(name);
  let tag = `<${name}${formatAttributes(element.attribs, options, xmlMode)}`;
  if (children.length === 0 && (xmlMode ? options.selfClosingTags !== false : options.selfClosingTags && isVoid)) {
    tag += xmlMode ? "/>" : " />";
  } else {
    tag += ">";
    if (children.length > 0) {
      tag += renderChildren(children, options, xmlMode);
    }
    if (!isVoid) {
      tag += `</${name}>`;
    }
  }
  return tag;
}
function replaceQuotes(value) {
  return value.replaceAll('"', "&quot;");
}
function formatAttributes(attributes, options, xmlMode) {
  if (!attributes)
    return "";
  const encode = (options.encodeEntities ?? options.decodeEntities) === false ? replaceQuotes : xmlMode || options.encodeEntities !== "utf8" ? encodeXML : escapeAttribute;
  const isForeign = xmlMode === "foreign";
  const showEmpty = !!(options.emptyAttrs ?? xmlMode);
  let result = "";
  for (const key in attributes) {
    if (!Object.hasOwn(attributes, key))
      continue;
    const value = attributes[key];
    const k2 = isForeign ? attributeNames.get(key) ?? key : key;
    result += !showEmpty && (value == null || value === "") ? ` ${k2}` : ` ${k2}="${encode(value == null ? "" : String(value))}"`;
  }
  return result;
}

// node_modules/domutils/dist/stringify.js
function getOuterHTML(node, options) {
  return dist_default(node, options);
}
function getInnerHTML(node, options) {
  return hasChildren(node) ? node.children.map((node2) => getOuterHTML(node2, options)).join("") : "";
}
function getText(node) {
  if (Array.isArray(node))
    return node.map(getText).join("");
  if (isTag2(node))
    return node.name === "br" ? "\n" : getText(node.children);
  if (isCDATA(node))
    return getText(node.children);
  if (isText(node))
    return node.data;
  return "";
}
function textContent(node) {
  if (Array.isArray(node))
    return node.map(textContent).join("");
  if (hasChildren(node) && !isComment(node)) {
    return textContent(node.children);
  }
  if (isText(node))
    return node.data;
  return "";
}
function innerText(node) {
  if (Array.isArray(node))
    return node.map(innerText).join("");
  if (hasChildren(node) && (node.type === ElementType.Tag || isCDATA(node))) {
    return innerText(node.children);
  }
  if (isText(node))
    return node.data;
  return "";
}

// node_modules/domutils/dist/feeds.js
function getFeed(document) {
  const feedRoot = getOneElement(isValidFeed, document);
  return feedRoot ? feedRoot.name === "feed" ? getAtomFeed(feedRoot) : getRssFeed(feedRoot) : null;
}
function getAtomFeed(feedRoot) {
  const childs = feedRoot.children;
  const feed = {
    type: "atom",
    items: getElementsByTagName("entry", childs).map((item) => {
      const { children } = item;
      const entry = { media: getMediaElements(children) };
      addConditionally(entry, "id", "id", children);
      addConditionally(entry, "title", "title", children);
      const href2 = getOneElement("link", children)?.attribs["href"];
      if (href2) {
        entry.link = href2;
      }
      const description = fetch("summary", children) || fetch("content", children);
      if (description) {
        entry.description = description;
      }
      const pubDate = fetch("updated", children);
      if (pubDate) {
        entry.pubDate = new Date(pubDate);
      }
      return entry;
    })
  };
  addConditionally(feed, "id", "id", childs);
  addConditionally(feed, "title", "title", childs);
  const href = getOneElement("link", childs)?.attribs["href"];
  if (href) {
    feed.link = href;
  }
  addConditionally(feed, "description", "subtitle", childs);
  const updated = fetch("updated", childs);
  if (updated) {
    feed.updated = new Date(updated);
  }
  addConditionally(feed, "author", "email", childs, true);
  return feed;
}
function getRssFeed(feedRoot) {
  const childs = getOneElement("channel", feedRoot.children)?.children ?? [];
  const feed = {
    type: feedRoot.name.substr(0, 3),
    id: "",
    items: getElementsByTagName("item", feedRoot.children).map((item) => {
      const { children } = item;
      const entry = { media: getMediaElements(children) };
      addConditionally(entry, "id", "guid", children);
      addConditionally(entry, "title", "title", children);
      addConditionally(entry, "link", "link", children);
      addConditionally(entry, "description", "description", children);
      const pubDate = fetch("pubDate", children) || fetch("dc:date", children);
      if (pubDate)
        entry.pubDate = new Date(pubDate);
      return entry;
    })
  };
  addConditionally(feed, "title", "title", childs);
  addConditionally(feed, "link", "link", childs);
  addConditionally(feed, "description", "description", childs);
  const updated = fetch("lastBuildDate", childs);
  if (updated) {
    feed.updated = new Date(updated);
  }
  addConditionally(feed, "author", "managingEditor", childs, true);
  return feed;
}
var MEDIA_KEYS_STRING = ["url", "type", "lang"];
var MEDIA_KEYS_INT = [
  "fileSize",
  "bitrate",
  "framerate",
  "samplingrate",
  "channels",
  "duration",
  "height",
  "width"
];
function getMediaElements(where) {
  return getElementsByTagName("media:content", where).map((element) => {
    const { attribs } = element;
    const media = {
      medium: attribs["medium"],
      isDefault: !!attribs["isDefault"]
    };
    for (const attrib of MEDIA_KEYS_STRING) {
      if (attribs[attrib]) {
        media[attrib] = attribs[attrib];
      }
    }
    for (const attrib of MEDIA_KEYS_INT) {
      if (attribs[attrib]) {
        media[attrib] = Number.parseInt(attribs[attrib], 10);
      }
    }
    if (attribs["expression"]) {
      media.expression = attribs["expression"];
    }
    return media;
  });
}
function getOneElement(tagName, node) {
  return getElementsByTagName(tagName, node, true, 1)[0];
}
function fetch(tagName, where, recurse = false) {
  return textContent(getElementsByTagName(tagName, where, recurse, 1)).trim();
}
function addConditionally(object, property, tagName, where, recurse = false) {
  const value = fetch(tagName, where, recurse);
  if (value)
    object[property] = value;
}
function isValidFeed(value) {
  return value === "rss" || value === "feed" || value === "rdf:RDF";
}

// node_modules/domutils/dist/helpers.js
function removeSubsets(nodes) {
  let index = nodes.length;
  while (--index >= 0) {
    const node = nodes[index];
    if (index > 0 && nodes.lastIndexOf(node, index - 1) >= 0) {
      nodes.splice(index, 1);
      continue;
    }
    for (let ancestor = node.parent; ancestor; ancestor = ancestor.parent) {
      if (nodes.includes(ancestor)) {
        nodes.splice(index, 1);
        break;
      }
    }
  }
  return nodes;
}
var DocumentPosition;
(function(DocumentPosition2) {
  DocumentPosition2[DocumentPosition2["DISCONNECTED"] = 1] = "DISCONNECTED";
  DocumentPosition2[DocumentPosition2["PRECEDING"] = 2] = "PRECEDING";
  DocumentPosition2[DocumentPosition2["FOLLOWING"] = 4] = "FOLLOWING";
  DocumentPosition2[DocumentPosition2["CONTAINS"] = 8] = "CONTAINS";
  DocumentPosition2[DocumentPosition2["CONTAINED_BY"] = 16] = "CONTAINED_BY";
})(DocumentPosition || (DocumentPosition = {}));
function compareDocumentPosition(nodeA, nodeB) {
  const aParents = [];
  const bParents = [];
  if (nodeA === nodeB) {
    return 0;
  }
  let current = hasChildren(nodeA) ? nodeA : nodeA.parent;
  while (current) {
    aParents.unshift(current);
    current = current.parent;
  }
  current = hasChildren(nodeB) ? nodeB : nodeB.parent;
  while (current) {
    bParents.unshift(current);
    current = current.parent;
  }
  const maxIndex = Math.min(aParents.length, bParents.length);
  let index = 0;
  while (index < maxIndex && aParents[index] === bParents[index]) {
    index++;
  }
  if (index === 0) {
    return DocumentPosition.DISCONNECTED;
  }
  const sharedParent = aParents[index - 1];
  const siblings = sharedParent.children;
  const aSibling = aParents[index];
  const bSibling = bParents[index];
  if (siblings.indexOf(aSibling) > siblings.indexOf(bSibling)) {
    if (sharedParent === nodeB) {
      return DocumentPosition.FOLLOWING | DocumentPosition.CONTAINED_BY;
    }
    return DocumentPosition.FOLLOWING;
  }
  if (sharedParent === nodeA) {
    return DocumentPosition.PRECEDING | DocumentPosition.CONTAINS;
  }
  return DocumentPosition.PRECEDING;
}
function uniqueSort(nodes) {
  nodes = nodes.filter((node, index, array) => !array.includes(node, index + 1));
  nodes.sort((a, b) => {
    const relative = compareDocumentPosition(a, b);
    if (relative & DocumentPosition.PRECEDING) {
      return -1;
    }
    if (relative & DocumentPosition.FOLLOWING) {
      return 1;
    }
    return 0;
  });
  return nodes;
}

// node_modules/domutils/dist/manipulation.js
function removeElement(element) {
  if (element.prev)
    element.prev.next = element.next;
  if (element.next)
    element.next.prev = element.prev;
  if (element.parent) {
    const childs = element.parent.children;
    const childsIndex = childs.lastIndexOf(element);
    if (childsIndex !== -1) {
      childs.splice(childsIndex, 1);
    }
  }
  element.next = null;
  element.prev = null;
  element.parent = null;
}
function replaceElement(element, replacement) {
  replacement.prev = element.prev;
  if (replacement.prev) {
    replacement.prev.next = replacement;
  }
  replacement.next = element.next;
  if (replacement.next) {
    replacement.next.prev = replacement;
  }
  replacement.parent = element.parent;
  if (replacement.parent) {
    const { children } = replacement.parent;
    const elementIndex = children.lastIndexOf(element);
    if (elementIndex === -1) {
      return;
    }
    children[elementIndex] = replacement;
    element.parent = null;
  }
}
function appendChild(parent, child) {
  removeElement(child);
  child.next = null;
  child.parent = parent;
  if (parent.children.push(child) > 1) {
    const sibling = parent.children[parent.children.length - 2];
    sibling.next = child;
    child.prev = sibling;
  } else {
    child.prev = null;
  }
}
function append(element, next) {
  removeElement(next);
  const { parent } = element;
  const currentNext = element.next;
  next.next = currentNext;
  next.prev = element;
  element.next = next;
  next.parent = parent;
  if (currentNext) {
    currentNext.prev = next;
    if (parent) {
      const childs = parent.children;
      childs.splice(childs.lastIndexOf(currentNext), 0, next);
    }
  } else if (parent) {
    parent.children.push(next);
  }
}
function prependChild(parent, child) {
  removeElement(child);
  child.parent = parent;
  child.prev = null;
  if (parent.children.unshift(child) === 1) {
    child.next = null;
  } else {
    const sibling = parent.children[1];
    sibling.prev = child;
    child.next = sibling;
  }
}
function prepend(element, previous) {
  removeElement(previous);
  const { parent } = element;
  if (parent) {
    const childs = parent.children;
    childs.splice(childs.indexOf(element), 0, previous);
  }
  if (element.prev) {
    element.prev.next = previous;
  }
  previous.parent = parent;
  previous.prev = element.prev;
  previous.next = element;
  element.prev = previous;
}

// node_modules/domutils/dist/traversal.js
function getChildren(element) {
  return hasChildren(element) ? element.children : [];
}
function getParent(element) {
  return element.parent || null;
}
function getSiblings(element) {
  const parent = getParent(element);
  if (parent != null)
    return getChildren(parent);
  const siblings = [element];
  let { prev, next } = element;
  while (prev != null) {
    siblings.unshift(prev);
    ({ prev } = prev);
  }
  while (next != null) {
    siblings.push(next);
    ({ next } = next);
  }
  return siblings;
}
function getAttributeValue(element, name) {
  const { attribs } = element;
  return attribs?.[name];
}
function hasAttrib(element, name) {
  const { attribs } = element;
  return attribs != null && Object.hasOwn(attribs, name) && attribs[name] != null;
}
function getName(element) {
  return element.name;
}
function nextElementSibling(element) {
  let { next } = element;
  while (next !== null && !isTag2(next))
    ({ next } = next);
  return next;
}
function prevElementSibling(element) {
  let { prev } = element;
  while (prev !== null && !isTag2(prev))
    ({ prev } = prev);
  return prev;
}

// node_modules/htmlparser2/dist/index.js
function parseDocument(data, options) {
  const handler = new DomHandler(void 0, options);
  new Parser(handler, options).end(data);
  return handler.root;
}

// node_modules/boolbase/dist/index.js
function trueFunc() {
  return true;
}
function falseFunc() {
  return false;
}

// node_modules/css-what/dist/types.js
var SelectorType;
(function(SelectorType2) {
  SelectorType2["Attribute"] = "attribute";
  SelectorType2["Pseudo"] = "pseudo";
  SelectorType2["PseudoElement"] = "pseudo-element";
  SelectorType2["Tag"] = "tag";
  SelectorType2["Universal"] = "universal";
  SelectorType2["Adjacent"] = "adjacent";
  SelectorType2["Child"] = "child";
  SelectorType2["Descendant"] = "descendant";
  SelectorType2["Parent"] = "parent";
  SelectorType2["Sibling"] = "sibling";
  SelectorType2["ColumnCombinator"] = "column-combinator";
})(SelectorType || (SelectorType = {}));
var AttributeAction;
(function(AttributeAction2) {
  AttributeAction2["Any"] = "any";
  AttributeAction2["Element"] = "element";
  AttributeAction2["End"] = "end";
  AttributeAction2["Equals"] = "equals";
  AttributeAction2["Exists"] = "exists";
  AttributeAction2["Hyphen"] = "hyphen";
  AttributeAction2["Not"] = "not";
  AttributeAction2["Start"] = "start";
})(AttributeAction || (AttributeAction = {}));

// node_modules/css-what/dist/parse.js
var reName = /^[^#\\]?(?:\\(?:[\da-f]{1,6}\s?|.)|[\w\u00B0-\uFFFF-])+/;
var reEscape = /\\([\da-f]{1,6}\s?|(\s)|.)/gi;
var CharCode;
(function(CharCode2) {
  CharCode2[CharCode2["LeftParenthesis"] = 40] = "LeftParenthesis";
  CharCode2[CharCode2["RightParenthesis"] = 41] = "RightParenthesis";
  CharCode2[CharCode2["LeftSquareBracket"] = 91] = "LeftSquareBracket";
  CharCode2[CharCode2["RightSquareBracket"] = 93] = "RightSquareBracket";
  CharCode2[CharCode2["Comma"] = 44] = "Comma";
  CharCode2[CharCode2["Period"] = 46] = "Period";
  CharCode2[CharCode2["Colon"] = 58] = "Colon";
  CharCode2[CharCode2["SingleQuote"] = 39] = "SingleQuote";
  CharCode2[CharCode2["DoubleQuote"] = 34] = "DoubleQuote";
  CharCode2[CharCode2["Plus"] = 43] = "Plus";
  CharCode2[CharCode2["Tilde"] = 126] = "Tilde";
  CharCode2[CharCode2["QuestionMark"] = 63] = "QuestionMark";
  CharCode2[CharCode2["ExclamationMark"] = 33] = "ExclamationMark";
  CharCode2[CharCode2["Slash"] = 47] = "Slash";
  CharCode2[CharCode2["Equal"] = 61] = "Equal";
  CharCode2[CharCode2["Dollar"] = 36] = "Dollar";
  CharCode2[CharCode2["Pipe"] = 124] = "Pipe";
  CharCode2[CharCode2["Circumflex"] = 94] = "Circumflex";
  CharCode2[CharCode2["Asterisk"] = 42] = "Asterisk";
  CharCode2[CharCode2["GreaterThan"] = 62] = "GreaterThan";
  CharCode2[CharCode2["LessThan"] = 60] = "LessThan";
  CharCode2[CharCode2["Hash"] = 35] = "Hash";
  CharCode2[CharCode2["LowerI"] = 105] = "LowerI";
  CharCode2[CharCode2["LowerS"] = 115] = "LowerS";
  CharCode2[CharCode2["BackSlash"] = 92] = "BackSlash";
  CharCode2[CharCode2["Space"] = 32] = "Space";
  CharCode2[CharCode2["Tab"] = 9] = "Tab";
  CharCode2[CharCode2["NewLine"] = 10] = "NewLine";
  CharCode2[CharCode2["FormFeed"] = 12] = "FormFeed";
  CharCode2[CharCode2["CarriageReturn"] = 13] = "CarriageReturn";
})(CharCode || (CharCode = {}));
var actionTypes = /* @__PURE__ */ new Map([
  [CharCode.Tilde, AttributeAction.Element],
  [CharCode.Circumflex, AttributeAction.Start],
  [CharCode.Dollar, AttributeAction.End],
  [CharCode.Asterisk, AttributeAction.Any],
  [CharCode.ExclamationMark, AttributeAction.Not],
  [CharCode.Pipe, AttributeAction.Hyphen]
]);
var unpackPseudos = /* @__PURE__ */ new Set([
  "has",
  "not",
  "matches",
  "is",
  "where",
  "host",
  "host-context"
]);
var pseudosToPseudoElements = /* @__PURE__ */ new Set([
  "before",
  "after",
  "first-line",
  "first-letter"
]);
function isTraversal(selector) {
  switch (selector.type) {
    case SelectorType.Adjacent:
    case SelectorType.Child:
    case SelectorType.Descendant:
    case SelectorType.Parent:
    case SelectorType.Sibling:
    case SelectorType.ColumnCombinator: {
      return true;
    }
    case SelectorType.Attribute:
    case SelectorType.Pseudo:
    case SelectorType.PseudoElement:
    case SelectorType.Tag:
    case SelectorType.Universal: {
      return false;
    }
  }
}
var stripQuotesFromPseudos = /* @__PURE__ */ new Set(["contains", "icontains"]);
function funescape(_2, escaped, escapedWhitespace) {
  const high = Number.parseInt(escaped, 16) - 65536;
  return Number.isNaN(high) || escapedWhitespace ? escaped : high < 0 ? (
    // BMP codepoint
    String.fromCharCode(high + 65536)
  ) : (
    // Supplemental Plane codepoint (surrogate pair)
    String.fromCharCode(high >> 10 | 55296, high & 1023 | 56320)
  );
}
function unescapeCSS(cssString) {
  return cssString.replace(reEscape, funescape);
}
function isQuote(c) {
  return c === CharCode.SingleQuote || c === CharCode.DoubleQuote;
}
function isWhitespace2(c) {
  return c === CharCode.Space || c === CharCode.Tab || c === CharCode.NewLine || c === CharCode.FormFeed || c === CharCode.CarriageReturn;
}
function parse(selector) {
  const subselects2 = [];
  const endIndex = parseSelector(subselects2, `${selector}`, 0);
  if (endIndex < selector.length) {
    throw new Error(`Unmatched selector: ${selector.slice(endIndex)}`);
  }
  return subselects2;
}
function parseSelector(subselects2, selector, selectorIndex) {
  let tokens = [];
  function getName2(offset) {
    const match = selector.slice(selectorIndex + offset).match(reName);
    if (!match) {
      throw new Error(`Expected name, found ${selector.slice(selectorIndex)}`);
    }
    const [name] = match;
    selectorIndex += offset + name.length;
    return unescapeCSS(name);
  }
  function stripWhitespace(offset) {
    selectorIndex += offset;
    while (selectorIndex < selector.length && isWhitespace2(selector.charCodeAt(selectorIndex))) {
      selectorIndex++;
    }
  }
  function readValueWithParenthesis() {
    selectorIndex += 1;
    const start = selectorIndex;
    for (let counter = 1; selectorIndex < selector.length; selectorIndex++) {
      switch (selector.charCodeAt(selectorIndex)) {
        case CharCode.BackSlash: {
          selectorIndex += 1;
          break;
        }
        case CharCode.LeftParenthesis: {
          counter += 1;
          break;
        }
        case CharCode.RightParenthesis: {
          counter -= 1;
          if (counter === 0) {
            return unescapeCSS(selector.slice(start, selectorIndex++));
          }
          break;
        }
      }
    }
    throw new Error("Parenthesis not matched");
  }
  function ensureNotTraversal() {
    if (tokens.length > 0 && isTraversal(tokens[tokens.length - 1])) {
      throw new Error("Did not expect successive traversals.");
    }
  }
  function addTraversal(type) {
    if (tokens.length > 0 && tokens[tokens.length - 1].type === SelectorType.Descendant) {
      tokens[tokens.length - 1].type = type;
      return;
    }
    ensureNotTraversal();
    tokens.push({ type });
  }
  function addSpecialAttribute(name, action) {
    tokens.push({
      type: SelectorType.Attribute,
      name,
      action,
      value: getName2(1),
      namespace: null,
      ignoreCase: "quirks"
    });
  }
  function finalizeSubselector() {
    if (tokens.length > 0 && tokens[tokens.length - 1].type === SelectorType.Descendant) {
      tokens.pop();
    }
    if (tokens.length === 0) {
      throw new Error("Empty sub-selector");
    }
    subselects2.push(tokens);
  }
  stripWhitespace(0);
  if (selector.length === selectorIndex) {
    return selectorIndex;
  }
  loop: while (selectorIndex < selector.length) {
    const firstChar = selector.charCodeAt(selectorIndex);
    switch (firstChar) {
      // Whitespace
      case CharCode.Space:
      case CharCode.Tab:
      case CharCode.NewLine:
      case CharCode.FormFeed:
      case CharCode.CarriageReturn: {
        if (tokens.length === 0 || tokens[0].type !== SelectorType.Descendant) {
          ensureNotTraversal();
          tokens.push({ type: SelectorType.Descendant });
        }
        stripWhitespace(1);
        break;
      }
      // Traversals
      case CharCode.GreaterThan: {
        addTraversal(SelectorType.Child);
        stripWhitespace(1);
        break;
      }
      case CharCode.LessThan: {
        addTraversal(SelectorType.Parent);
        stripWhitespace(1);
        break;
      }
      case CharCode.Tilde: {
        addTraversal(SelectorType.Sibling);
        stripWhitespace(1);
        break;
      }
      case CharCode.Plus: {
        addTraversal(SelectorType.Adjacent);
        stripWhitespace(1);
        break;
      }
      // Special attribute selectors: .class, #id
      case CharCode.Period: {
        addSpecialAttribute("class", AttributeAction.Element);
        break;
      }
      case CharCode.Hash: {
        addSpecialAttribute("id", AttributeAction.Equals);
        break;
      }
      case CharCode.LeftSquareBracket: {
        stripWhitespace(1);
        let name;
        let namespace = null;
        if (selector.charCodeAt(selectorIndex) === CharCode.Pipe) {
          name = getName2(1);
        } else if (selector.startsWith("*|", selectorIndex)) {
          namespace = "*";
          name = getName2(2);
        } else {
          name = getName2(0);
          if (selector.charCodeAt(selectorIndex) === CharCode.Pipe && selector.charCodeAt(selectorIndex + 1) !== CharCode.Equal) {
            namespace = name;
            name = getName2(1);
          }
        }
        stripWhitespace(0);
        let action = AttributeAction.Exists;
        const possibleAction = actionTypes.get(selector.charCodeAt(selectorIndex));
        if (possibleAction) {
          action = possibleAction;
          if (selector.charCodeAt(selectorIndex + 1) !== CharCode.Equal) {
            throw new Error("Expected `=`");
          }
          stripWhitespace(2);
        } else if (selector.charCodeAt(selectorIndex) === CharCode.Equal) {
          action = AttributeAction.Equals;
          stripWhitespace(1);
        }
        let value = "";
        let ignoreCase = null;
        if (action !== "exists") {
          if (isQuote(selector.charCodeAt(selectorIndex))) {
            const quote = selector.charCodeAt(selectorIndex);
            selectorIndex += 1;
            const sectionStart = selectorIndex;
            while (selectorIndex < selector.length && selector.charCodeAt(selectorIndex) !== quote) {
              selectorIndex += // Skip next character if it is escaped
              selector.charCodeAt(selectorIndex) === CharCode.BackSlash ? 2 : 1;
            }
            if (selector.charCodeAt(selectorIndex) !== quote) {
              throw new Error("Attribute value didn't end");
            }
            value = unescapeCSS(selector.slice(sectionStart, selectorIndex));
            selectorIndex += 1;
          } else {
            const valueStart = selectorIndex;
            while (selectorIndex < selector.length && !isWhitespace2(selector.charCodeAt(selectorIndex)) && selector.charCodeAt(selectorIndex) !== CharCode.RightSquareBracket) {
              selectorIndex += // Skip next character if it is escaped
              selector.charCodeAt(selectorIndex) === CharCode.BackSlash ? 2 : 1;
            }
            value = unescapeCSS(selector.slice(valueStart, selectorIndex));
          }
          stripWhitespace(0);
          switch (selector.charCodeAt(selectorIndex) | 32) {
            // If the forceIgnore flag is set (either `i` or `s`), use that value
            case CharCode.LowerI: {
              ignoreCase = true;
              stripWhitespace(1);
              break;
            }
            case CharCode.LowerS: {
              ignoreCase = false;
              stripWhitespace(1);
              break;
            }
          }
        }
        if (selector.charCodeAt(selectorIndex) !== CharCode.RightSquareBracket) {
          throw new Error("Attribute selector didn't terminate");
        }
        selectorIndex += 1;
        const attributeSelector = {
          type: SelectorType.Attribute,
          name,
          action,
          value,
          namespace,
          ignoreCase
        };
        tokens.push(attributeSelector);
        break;
      }
      case CharCode.Colon: {
        if (selector.charCodeAt(selectorIndex + 1) === CharCode.Colon) {
          tokens.push({
            type: SelectorType.PseudoElement,
            name: getName2(2).toLowerCase(),
            data: selector.charCodeAt(selectorIndex) === CharCode.LeftParenthesis ? readValueWithParenthesis() : null
          });
          break;
        }
        const name = getName2(1).toLowerCase();
        if (pseudosToPseudoElements.has(name)) {
          tokens.push({
            type: SelectorType.PseudoElement,
            name,
            data: null
          });
          break;
        }
        let data = null;
        if (selector.charCodeAt(selectorIndex) === CharCode.LeftParenthesis) {
          if (unpackPseudos.has(name)) {
            if (isQuote(selector.charCodeAt(selectorIndex + 1))) {
              throw new Error(`Pseudo-selector ${name} cannot be quoted`);
            }
            data = [];
            selectorIndex = parseSelector(data, selector, selectorIndex + 1);
            if (selector.charCodeAt(selectorIndex) !== CharCode.RightParenthesis) {
              throw new Error(`Missing closing parenthesis in :${name} (${selector})`);
            }
            selectorIndex += 1;
          } else {
            data = readValueWithParenthesis();
            if (stripQuotesFromPseudos.has(name)) {
              const quot = data.charCodeAt(0);
              if (quot === data.charCodeAt(data.length - 1) && isQuote(quot)) {
                data = data.slice(1, -1);
              }
            }
            data = unescapeCSS(data);
          }
        }
        tokens.push({ type: SelectorType.Pseudo, name, data });
        break;
      }
      case CharCode.Comma: {
        finalizeSubselector();
        tokens = [];
        stripWhitespace(1);
        break;
      }
      default: {
        if (selector.startsWith("/*", selectorIndex)) {
          const endIndex = selector.indexOf("*/", selectorIndex + 2);
          if (endIndex === -1) {
            throw new Error("Comment was not terminated");
          }
          selectorIndex = endIndex + 2;
          if (tokens.length === 0) {
            stripWhitespace(0);
          }
          break;
        }
        let namespace = null;
        let name;
        if (firstChar === CharCode.Asterisk) {
          selectorIndex += 1;
          name = "*";
        } else if (firstChar === CharCode.Pipe) {
          name = "";
          if (selector.charCodeAt(selectorIndex + 1) === CharCode.Pipe) {
            addTraversal(SelectorType.ColumnCombinator);
            stripWhitespace(2);
            break;
          }
        } else if (reName.test(selector.slice(selectorIndex))) {
          name = getName2(0);
        } else {
          break loop;
        }
        if (selector.charCodeAt(selectorIndex) === CharCode.Pipe && selector.charCodeAt(selectorIndex + 1) !== CharCode.Pipe) {
          namespace = name;
          if (selector.charCodeAt(selectorIndex + 1) === CharCode.Asterisk) {
            name = "*";
            selectorIndex += 2;
          } else {
            name = getName2(1);
          }
        }
        tokens.push(name === "*" ? { type: SelectorType.Universal, namespace } : { type: SelectorType.Tag, name, namespace });
      }
    }
  }
  finalizeSubselector();
  return selectorIndex;
}

// node_modules/css-select/dist/attributes.js
var reChars = /[-[\]{}()*+?.,\\^$|#\s]/g;
var whitespaceRe = /\s/;
function escapeRegex(value) {
  return value.replace(reChars, "\\$&");
}
var caseInsensitiveAttributes = /* @__PURE__ */ new Set([
  "accept",
  "accept-charset",
  "align",
  "alink",
  "axis",
  "bgcolor",
  "charset",
  "checked",
  "clear",
  "codetype",
  "color",
  "compact",
  "declare",
  "defer",
  "dir",
  "direction",
  "disabled",
  "enctype",
  "face",
  "frame",
  "hreflang",
  "http-equiv",
  "lang",
  "language",
  "link",
  "media",
  "method",
  "multiple",
  "nohref",
  "noresize",
  "noshade",
  "nowrap",
  "readonly",
  "rel",
  "rev",
  "rules",
  "scope",
  "scrolling",
  "selected",
  "shape",
  "target",
  "text",
  "type",
  "valign",
  "valuetype",
  "vlink"
]);
function shouldIgnoreCase(selector, options) {
  return typeof selector.ignoreCase === "boolean" ? selector.ignoreCase : selector.ignoreCase === "quirks" ? !!options.quirksMode : !options.xmlMode && caseInsensitiveAttributes.has(selector.name);
}
var attributeRules = {
  equals(next, data, options) {
    const { adapter } = options;
    const { name } = data;
    let { value } = data;
    if (shouldIgnoreCase(data, options)) {
      value = value.toLowerCase();
      return (element) => {
        const attribute = adapter.getAttributeValue(element, name);
        return attribute != null && attribute.length === value.length && attribute.toLowerCase() === value && next(element);
      };
    }
    return (element) => adapter.getAttributeValue(element, name) === value && next(element);
  },
  hyphen(next, data, options) {
    const { adapter } = options;
    const { name } = data;
    let { value } = data;
    const { length } = value;
    if (shouldIgnoreCase(data, options)) {
      value = value.toLowerCase();
      return function hyphenIC(element) {
        const attribute = adapter.getAttributeValue(element, name);
        return attribute != null && (attribute.length === length || attribute.charAt(length) === "-") && attribute.substr(0, length).toLowerCase() === value && next(element);
      };
    }
    return function hyphen(element) {
      const attribute = adapter.getAttributeValue(element, name);
      return attribute != null && (attribute.length === length || attribute.charAt(length) === "-") && attribute.substr(0, length) === value && next(element);
    };
  },
  element(next, data, options) {
    const { adapter } = options;
    const { name, value } = data;
    if (whitespaceRe.test(value)) {
      return falseFunc;
    }
    const regex = new RegExp(`(?:^|\\s)${escapeRegex(value)}(?:$|\\s)`, shouldIgnoreCase(data, options) ? "i" : "");
    return function element(node) {
      const attribute = adapter.getAttributeValue(node, name);
      return attribute != null && attribute.length >= value.length && regex.test(attribute) && next(node);
    };
  },
  exists(next, { name }, { adapter }) {
    return (element) => adapter.hasAttrib(element, name) && next(element);
  },
  start(next, data, options) {
    const { adapter } = options;
    const { name } = data;
    let { value } = data;
    const { length } = value;
    if (length === 0) {
      return falseFunc;
    }
    if (shouldIgnoreCase(data, options)) {
      value = value.toLowerCase();
      return (element) => {
        const attribute = adapter.getAttributeValue(element, name);
        return attribute != null && attribute.length >= length && attribute.substr(0, length).toLowerCase() === value && next(element);
      };
    }
    return (element) => !!adapter.getAttributeValue(element, name)?.startsWith(value) && next(element);
  },
  end(next, data, options) {
    const { adapter } = options;
    const { name } = data;
    let { value } = data;
    const length = -value.length;
    if (length === 0) {
      return falseFunc;
    }
    if (shouldIgnoreCase(data, options)) {
      value = value.toLowerCase();
      return (element) => adapter.getAttributeValue(element, name)?.substr(length).toLowerCase() === value && next(element);
    }
    return (element) => !!adapter.getAttributeValue(element, name)?.endsWith(value) && next(element);
  },
  any(next, data, options) {
    const { adapter } = options;
    const { name, value } = data;
    if (value === "") {
      return falseFunc;
    }
    if (shouldIgnoreCase(data, options)) {
      const regex = new RegExp(escapeRegex(value), "i");
      return function anyIC(element) {
        const attribute = adapter.getAttributeValue(element, name);
        return attribute != null && attribute.length >= value.length && regex.test(attribute) && next(element);
      };
    }
    return (element) => !!adapter.getAttributeValue(element, name)?.includes(value) && next(element);
  },
  not(next, data, options) {
    const { adapter } = options;
    const { name } = data;
    let { value } = data;
    if (value === "") {
      return (element) => !!adapter.getAttributeValue(element, name) && next(element);
    }
    if (shouldIgnoreCase(data, options)) {
      value = value.toLowerCase();
      return (element) => {
        const attribute = adapter.getAttributeValue(element, name);
        return (attribute == null || attribute.length !== value.length || attribute.toLowerCase() !== value) && next(element);
      };
    }
    return (element) => adapter.getAttributeValue(element, name) !== value && next(element);
  }
};

// node_modules/css-select/dist/helpers/querying.js
function findAll2(query, nodes, options) {
  const { adapter, xmlMode = false } = options;
  const result = [];
  const nodeStack = [nodes];
  const indexStack = [0];
  for (; ; ) {
    if (indexStack[0] >= nodeStack[0].length) {
      if (nodeStack.length === 1) {
        return result;
      }
      nodeStack.shift();
      indexStack.shift();
      continue;
    }
    const element = nodeStack[0][indexStack[0]++];
    if (!adapter.isTag(element)) {
      continue;
    }
    if (query(element)) {
      result.push(element);
    }
    if (xmlMode || adapter.getName(element) !== "template") {
      const children = adapter.getChildren(element);
      if (children.length > 0) {
        nodeStack.unshift(children);
        indexStack.unshift(0);
      }
    }
  }
}
function findOne2(query, nodes, options) {
  const { adapter, xmlMode = false } = options;
  const nodeStack = [nodes];
  const indexStack = [0];
  for (; ; ) {
    if (indexStack[0] >= nodeStack[0].length) {
      if (nodeStack.length === 1) {
        return null;
      }
      nodeStack.shift();
      indexStack.shift();
      continue;
    }
    const element = nodeStack[0][indexStack[0]++];
    if (!adapter.isTag(element)) {
      continue;
    }
    if (query(element)) {
      return element;
    }
    if (xmlMode || adapter.getName(element) !== "template") {
      const children = adapter.getChildren(element);
      if (children.length > 0) {
        nodeStack.unshift(children);
        indexStack.unshift(0);
      }
    }
  }
}
function getNextSiblings(element, adapter) {
  const siblings = adapter.getSiblings(element);
  if (siblings.length <= 1) {
    return [];
  }
  const elementIndex = siblings.indexOf(element);
  if (elementIndex === -1 || elementIndex === siblings.length - 1) {
    return [];
  }
  return siblings.slice(elementIndex + 1).filter(adapter.isTag);
}
function getElementParent(node, adapter) {
  const parent = adapter.getParent(node);
  return parent != null && adapter.isTag(parent) ? parent : null;
}

// node_modules/css-select/dist/pseudo-selectors/aliases.js
var textControl = "input:is([type=text i],[type=search i],[type=url i],[type=tel i],[type=email i],[type=password i],[type=date i],[type=month i],[type=week i],[type=time i],[type=datetime-local i],[type=number i])";
var aliases = {
  // Links
  "any-link": ":is(a, area, link)[href]",
  link: ":any-link:not(:visited)",
  // Forms
  // https://html.spec.whatwg.org/multipage/scripting.html#disabled-elements
  disabled: `:is(
        :is(button, input, select, textarea, optgroup, option)[disabled],
        optgroup[disabled] > option,
        fieldset[disabled]:not(fieldset[disabled] legend:first-of-type *)
    )`,
  enabled: ":is(button, input, select, textarea, optgroup, option, fieldset):not(:disabled)",
  checked: ":is(:is(input[type=radio], input[type=checkbox])[checked], :selected)",
  required: ":is(input, select, textarea)[required]",
  optional: ":is(input, select, textarea):not([required])",
  "read-only": `[readonly]:is(textarea, ${textControl})`,
  "read-write": `:not([readonly]):is(textarea, ${textControl})`,
  // JQuery extensions
  /**
   * `:selected` matches option elements that have the `selected` attribute,
   * or are the first option element in a select element that does not have
   * the `multiple` attribute and does not have any option elements with the
   * `selected` attribute.
   * @see https://html.spec.whatwg.org/multipage/form-elements.html#concept-option-selectedness
   */
  selected: "option:is([selected], select:not([multiple]):not(:has(> option[selected])) > :first-of-type)",
  checkbox: "[type=checkbox]",
  file: "[type=file]",
  password: "[type=password]",
  radio: "[type=radio]",
  reset: "[type=reset]",
  image: "[type=image]",
  submit: "[type=submit]",
  parent: ":not(:empty)",
  header: ":is(h1, h2, h3, h4, h5, h6)",
  button: ":is(button, input[type=button])",
  input: ":is(input, textarea, select, button)",
  text: "input:is(:not([type!='']), [type=text])"
};

// node_modules/nth-check/dist/compile.js
function compile(parsed) {
  const a = parsed[0];
  const b = parsed[1] - 1;
  if (b < 0 && a <= 0)
    return falseFunc;
  if (a === -1)
    return (index) => index <= b;
  if (a === 0)
    return (index) => index === b;
  if (a === 1)
    return b < 0 ? trueFunc : (index) => index >= b;
  const absA = Math.abs(a);
  const bModulo = (b % absA + absA) % absA;
  return a > 1 ? (index) => index >= b && index % absA === bModulo : (index) => index <= b && index % absA === bModulo;
}

// node_modules/nth-check/dist/parse.js
var whitespace = /* @__PURE__ */ new Set([9, 10, 12, 13, 32]);
var ZERO = "0".charCodeAt(0);
var NINE = "9".charCodeAt(0);
function parse2(formula) {
  formula = formula.trim().toLowerCase();
  switch (formula) {
    case "even": {
      return [2, 0];
    }
    case "odd": {
      return [2, 1];
    }
  }
  let index = 0;
  let a = 0;
  let sign = readSign();
  let number = readNumber();
  if (index < formula.length && formula.charAt(index) === "n") {
    index++;
    a = sign * (number ?? 1);
    skipWhitespace();
    if (index < formula.length) {
      sign = readSign();
      skipWhitespace();
      number = readNumber();
    } else {
      sign = number = 0;
    }
  }
  if (number === null || index < formula.length) {
    throw new Error(`n-th rule couldn't be parsed ('${formula}')`);
  }
  return [a, sign * number];
  function readSign() {
    switch (formula.charAt(index)) {
      case "-": {
        index++;
        return -1;
      }
      case "+": {
        index++;
        break;
      }
    }
    return 1;
  }
  function readNumber() {
    const start = index;
    let value = 0;
    while (index < formula.length && formula.charCodeAt(index) >= ZERO && formula.charCodeAt(index) <= NINE) {
      value = value * 10 + (formula.charCodeAt(index) - ZERO);
      index++;
    }
    return index === start ? null : value;
  }
  function skipWhitespace() {
    while (index < formula.length && whitespace.has(formula.charCodeAt(index))) {
      index++;
    }
  }
}

// node_modules/nth-check/dist/index.js
function nthCheck(formula) {
  return compile(parse2(formula));
}

// node_modules/css-select/dist/helpers/cache.js
function cacheParentResults(next, { adapter, cacheResults }, matches) {
  if (cacheResults === false || typeof WeakMap === "undefined") {
    return (element) => next(element) && matches(element);
  }
  const resultCache = /* @__PURE__ */ new WeakMap();
  function addResultToCache(element) {
    const result = matches(element);
    resultCache.set(element, result);
    return result;
  }
  return function cachedMatcher(element) {
    if (!next(element)) {
      return false;
    }
    if (resultCache.has(element)) {
      return resultCache.get(element) ?? false;
    }
    let node = element;
    do {
      const parent = getElementParent(node, adapter);
      if (parent === null) {
        return addResultToCache(element);
      }
      node = parent;
    } while (!resultCache.has(node));
    return resultCache.get(node) ? addResultToCache(element) : false;
  };
}

// node_modules/css-select/dist/helpers/options.js
function copyOptions(options) {
  const { context: _2, rootFunc: __, ...copied } = options;
  return copied;
}

// node_modules/css-select/dist/pseudo-selectors/filters.js
function extendedFilter(tag, range) {
  if (range[0] !== "*" && range[0] !== tag[0])
    return false;
  let tagIndex = 1;
  for (let rangeIndex = 1; rangeIndex < range.length; rangeIndex++) {
    if (range[rangeIndex] === "*")
      continue;
    while (tagIndex < tag.length && tag[tagIndex] !== range[rangeIndex]) {
      if (tag[tagIndex++].length <= 1)
        return false;
    }
    if (tagIndex >= tag.length)
      return false;
    tagIndex++;
  }
  return true;
}
var nthOfRegex = /^(.+?)\s+of\s+(.+)$/is;
function compileNth(reverse, ofType) {
  return function nth(next, rule, options, context, compileToken2) {
    const { adapter, equals } = options;
    const ofMatch = ofType ? null : rule.match(nthOfRegex);
    const nthCheck2 = nthCheck(ofMatch ? ofMatch[1].trim() : rule);
    if (nthCheck2 === falseFunc)
      return falseFunc;
    const ofSelector = ofMatch && compileToken2 ? compileToken2(parse(ofMatch[2].trim()), copyOptions(options), context) : void 0;
    if (ofSelector === falseFunc)
      return falseFunc;
    if (nthCheck2 === trueFunc && !ofSelector) {
      return (element) => getElementParent(element, adapter) !== null && next(element);
    }
    const shouldCount = ofSelector ? (_element, sibling) => ofSelector(sibling) : ofType ? (element, sibling) => adapter.getName(sibling) === adapter.getName(element) : trueFunc;
    if (reverse) {
      return function nthLast(element) {
        if (ofSelector && !ofSelector(element))
          return false;
        const siblings = adapter.getSiblings(element);
        let pos = 0;
        for (let index = siblings.length - 1; index >= 0; index--) {
          const sibling = siblings[index];
          if (equals(element, sibling))
            break;
          if (adapter.isTag(sibling) && shouldCount(element, sibling))
            pos++;
        }
        return nthCheck2(pos) && next(element);
      };
    }
    return function nth2(element) {
      if (ofSelector && !ofSelector(element))
        return false;
      const siblings = adapter.getSiblings(element);
      let pos = 0;
      for (const sibling of siblings) {
        if (equals(element, sibling))
          break;
        if (adapter.isTag(sibling) && shouldCount(element, sibling))
          pos++;
      }
      return nthCheck2(pos) && next(element);
    };
  };
}
var filters = {
  contains(next, text, options) {
    const { getText: getText2 } = options.adapter;
    return cacheParentResults(next, options, (element) => getText2(element).includes(text));
  },
  icontains(next, text, options) {
    const itext = text.toLowerCase();
    const { getText: getText2 } = options.adapter;
    return cacheParentResults(next, options, (element) => getText2(element).toLowerCase().includes(itext));
  },
  // Location specific methods
  "nth-child": compileNth(false, false),
  "nth-last-child": compileNth(true, false),
  "nth-of-type": compileNth(false, true),
  "nth-last-of-type": compileNth(true, true),
  // TODO determine the actual root element
  root(next, _rule, { adapter }) {
    return (element) => getElementParent(element, adapter) === null && next(element);
  },
  scope(next, rule, options, context) {
    const { equals } = options;
    if (!context || context.length === 0) {
      return filters["root"](next, rule, options);
    }
    if (context.length === 1) {
      return (element) => equals(context[0], element) && next(element);
    }
    return (element) => context.includes(element) && next(element);
  },
  lang(next, code, { adapter }) {
    const ranges = code.split(",").map((r) => r.trim()).filter((r) => r.length > 0).map((r) => r.replace(/^['"]|['"]$/g, "").toLowerCase().split("-"));
    return function lang(element) {
      let node = element;
      while (node != null) {
        const value = adapter.getAttributeValue(node, "xml:lang") ?? adapter.getAttributeValue(node, "lang");
        if (value != null) {
          if (!value) {
            return ranges.some((r) => r[0] === "") && next(element);
          }
          const tag = value.toLowerCase().split("-");
          return ranges.some((r) => extendedFilter(tag, r)) && next(element);
        }
        const parent = adapter.getParent(node);
        node = parent != null && adapter.isTag(parent) ? parent : null;
      }
      return ranges.some((r) => r[0] === "") && next(element);
    };
  },
  hover: dynamicStatePseudo("isHovered"),
  visited: dynamicStatePseudo("isVisited"),
  active: dynamicStatePseudo("isActive")
};
function dynamicStatePseudo(name) {
  return function dynamicPseudo(next, _rule, { adapter }) {
    const filterFunction = adapter[name];
    if (typeof filterFunction !== "function") {
      return falseFunc;
    }
    return function active(element) {
      return filterFunction(element) && next(element);
    };
  };
}

// node_modules/css-select/dist/pseudo-selectors/pseudos.js
var isDocumentWhiteSpace = /^[ \t\r\n]*$/;
var pseudos = {
  empty(element, { adapter }) {
    const children = adapter.getChildren(element);
    return (
      // First, make sure the tag does not have any element children.
      children.every((element2) => !adapter.isTag(element2)) && // Then, check that the text content is only whitespace.
      children.every((element2) => (
        // FIXME: `getText` call is potentially expensive.
        isDocumentWhiteSpace.test(adapter.getText(element2))
      ))
    );
  },
  "first-child"(element, { adapter, equals }) {
    if (adapter.prevElementSibling) {
      return adapter.prevElementSibling(element) == null;
    }
    const firstChild = adapter.getSiblings(element).find((sibling) => adapter.isTag(sibling));
    return firstChild != null && equals(element, firstChild);
  },
  "last-child"(element, { adapter, equals }) {
    const siblings = adapter.getSiblings(element);
    for (let index = siblings.length - 1; index >= 0; index--) {
      if (equals(element, siblings[index])) {
        return true;
      }
      if (adapter.isTag(siblings[index])) {
        break;
      }
    }
    return false;
  },
  "first-of-type"(element, { adapter, equals }) {
    const siblings = adapter.getSiblings(element);
    const elementName = adapter.getName(element);
    for (const currentSibling of siblings) {
      if (equals(element, currentSibling)) {
        return true;
      }
      if (adapter.isTag(currentSibling) && adapter.getName(currentSibling) === elementName) {
        break;
      }
    }
    return false;
  },
  "last-of-type"(element, { adapter, equals }) {
    const siblings = adapter.getSiblings(element);
    const elementName = adapter.getName(element);
    for (let index = siblings.length - 1; index >= 0; index--) {
      const currentSibling = siblings[index];
      if (equals(element, currentSibling)) {
        return true;
      }
      if (adapter.isTag(currentSibling) && adapter.getName(currentSibling) === elementName) {
        break;
      }
    }
    return false;
  },
  "only-of-type"(element, { adapter, equals }) {
    const elementName = adapter.getName(element);
    return adapter.getSiblings(element).every((sibling) => equals(element, sibling) || !adapter.isTag(sibling) || adapter.getName(sibling) !== elementName);
  },
  "only-child"(element, { adapter, equals }) {
    return adapter.getSiblings(element).every((sibling) => equals(element, sibling) || !adapter.isTag(sibling));
  }
};
function verifyPseudoArguments(pseudoClassCondition, name, subselect, argumentIndex) {
  if (subselect === null) {
    if (pseudoClassCondition.length > argumentIndex) {
      throw new Error(`Pseudo-class :${name} requires an argument`);
    }
  } else if (pseudoClassCondition.length === argumentIndex) {
    throw new Error(`Pseudo-class :${name} doesn't have any arguments`);
  }
}

// node_modules/css-select/dist/helpers/selectors.js
function isTraversal2(token) {
  return token.type === "_flexibleDescendant" || isTraversal(token);
}
function sortRules(array) {
  const ratings = array.map(getQuality);
  for (let index = 1; index < array.length; index++) {
    const procNew = ratings[index];
    if (procNew < 0) {
      continue;
    }
    for (let currentIndex = index; currentIndex > 0 && procNew < ratings[currentIndex - 1]; currentIndex--) {
      const token = array[currentIndex];
      array[currentIndex] = array[currentIndex - 1];
      array[currentIndex - 1] = token;
      ratings[currentIndex] = ratings[currentIndex - 1];
      ratings[currentIndex - 1] = procNew;
    }
  }
}
function getAttributeQuality(token) {
  switch (token.action) {
    case AttributeAction.Exists: {
      return 10;
    }
    case AttributeAction.Equals: {
      return token.name === "id" ? 9 : 8;
    }
    case AttributeAction.Not: {
      return 7;
    }
    case AttributeAction.Start: {
      return 6;
    }
    case AttributeAction.End: {
      return 6;
    }
    case AttributeAction.Any: {
      return 5;
    }
    case AttributeAction.Hyphen: {
      return 4;
    }
    case AttributeAction.Element: {
      return 3;
    }
  }
}
function getQuality(token) {
  switch (token.type) {
    case SelectorType.Universal: {
      return 50;
    }
    case SelectorType.Tag: {
      return 30;
    }
    case SelectorType.Attribute: {
      return Math.floor(getAttributeQuality(token) / // `ignoreCase` adds some overhead, half the result if applicable.
      (token.ignoreCase ? 2 : 1));
    }
    case SelectorType.Pseudo: {
      return token.data ? token.name === "has" || token.name === "contains" || token.name === "icontains" ? (
        // Expensive in any case — run as late as possible.
        0
      ) : Array.isArray(token.data) ? (
        // Eg. `:is`, `:not`
        Math.max(
          // If we have traversals, try to avoid executing this selector
          0,
          Math.min(...token.data.map((d) => Math.min(...d.map(getQuality))))
        )
      ) : 2 : 3;
    }
    default: {
      return -1;
    }
  }
}
function includesScopePseudo(t) {
  return t.type === SelectorType.Pseudo && (t.name === "scope" || Array.isArray(t.data) && t.data.some((data) => data.some(includesScopePseudo)));
}

// node_modules/css-select/dist/pseudo-selectors/subselects.js
var PLACEHOLDER_ELEMENT = {};
function hasDependsOnCurrentElement(selector) {
  return selector.some((sel) => sel.length > 0 && (isTraversal2(sel[0]) || sel.some(includesScopePseudo)));
}
var is = (next, token, options, context, compileToken2) => {
  const compiledToken = compileToken2(token, copyOptions(options), context);
  return compiledToken === trueFunc ? next : compiledToken === falseFunc ? falseFunc : (element) => compiledToken(element) && next(element);
};
var subselects = {
  is,
  /**
   * `:matches` and `:where` are aliases for `:is`.
   */
  matches: is,
  where: is,
  not(next, token, options, context, compileToken2) {
    const compiledToken = compileToken2(token, copyOptions(options), context);
    return compiledToken === falseFunc ? next : compiledToken === trueFunc ? falseFunc : (element) => !compiledToken(element) && next(element);
  },
  has(next, subselect, options, _context, compileToken2) {
    const { adapter } = options;
    const copiedOptions = copyOptions(options);
    copiedOptions.relativeSelector = true;
    const context = subselect.some((s) => s.some(isTraversal2)) ? (
      // Used as a placeholder. Will be replaced with the actual element.
      [PLACEHOLDER_ELEMENT]
    ) : void 0;
    const skipCache = hasDependsOnCurrentElement(subselect);
    const compiled = compileToken2(subselect, copiedOptions, context);
    if (compiled === falseFunc) {
      return falseFunc;
    }
    if (context && compiled !== trueFunc) {
      return skipCache ? (element) => {
        if (!next(element)) {
          return false;
        }
        context[0] = element;
        const childs = adapter.getChildren(element);
        return findOne2(compiled, compiled.shouldTestNextSiblings ? [
          ...childs,
          ...getNextSiblings(element, adapter)
        ] : childs, options) !== null;
      } : cacheParentResults(next, options, (element) => {
        context[0] = element;
        return findOne2(compiled, adapter.getChildren(element), options) !== null;
      });
    }
    const hasOne = (element) => findOne2(compiled, adapter.getChildren(element), options) !== null;
    return skipCache ? (element) => next(element) && hasOne(element) : cacheParentResults(next, options, hasOne);
  }
};

// node_modules/css-select/dist/pseudo-selectors/index.js
function compilePseudoSelector(next, selector, options, context, compileToken2) {
  const { name, data } = selector;
  if (Array.isArray(data)) {
    if (!(name in subselects)) {
      throw new Error(`Unknown pseudo-class :${name}(${data})`);
    }
    return subselects[name](next, data, options, context, compileToken2);
  }
  const userPseudo = options.pseudos?.[name];
  const stringPseudo = typeof userPseudo === "string" ? userPseudo : aliases[name];
  if (typeof stringPseudo === "string") {
    if (data != null) {
      throw new Error(`Pseudo ${name} doesn't have any arguments`);
    }
    const alias = parse(stringPseudo);
    return subselects["is"](next, alias, options, context, compileToken2);
  }
  if (typeof userPseudo === "function") {
    verifyPseudoArguments(userPseudo, name, data, 1);
    return (element) => userPseudo(element, data) && next(element);
  }
  if (name in filters) {
    return filters[name](next, data, options, context, compileToken2);
  }
  if (name in pseudos) {
    const pseudo = pseudos[name];
    verifyPseudoArguments(pseudo, name, data, 2);
    return (element) => pseudo(element, options, data) && next(element);
  }
  throw new Error(`Unknown pseudo-class :${name}`);
}

// node_modules/css-select/dist/general.js
function compileGeneralSelector(next, selector, options, context, compileToken2, hasExpensiveSubselector) {
  const { adapter, equals, cacheResults } = options;
  switch (selector.type) {
    case SelectorType.PseudoElement: {
      throw new Error("Pseudo-elements are not supported by css-select");
    }
    case SelectorType.ColumnCombinator: {
      throw new Error("Column combinators are not yet supported by css-select");
    }
    case SelectorType.Attribute: {
      if (selector.namespace != null) {
        throw new Error("Namespaced attributes are not yet supported by css-select");
      }
      if (!options.xmlMode || options.lowerCaseAttributeNames) {
        selector.name = selector.name.toLowerCase();
      }
      return attributeRules[selector.action](next, selector, options);
    }
    case SelectorType.Pseudo: {
      return compilePseudoSelector(next, selector, options, context, compileToken2);
    }
    // Tags
    case SelectorType.Tag: {
      if (selector.namespace != null) {
        throw new Error("Namespaced tag names are not yet supported by css-select");
      }
      let { name } = selector;
      if (!options.xmlMode || options.lowerCaseTags) {
        name = name.toLowerCase();
      }
      return function tag(element) {
        return adapter.getName(element) === name && next(element);
      };
    }
    // Traversal
    case SelectorType.Descendant: {
      if (!hasExpensiveSubselector || cacheResults === false || typeof WeakMap === "undefined") {
        return function descendant(element) {
          let current = element;
          while (current = getElementParent(current, adapter)) {
            if (next(current)) {
              return true;
            }
          }
          return false;
        };
      }
      const resultCache = /* @__PURE__ */ new WeakMap();
      return function cachedDescendant(element) {
        let current = element;
        let result;
        while (current = getElementParent(current, adapter)) {
          const cached = resultCache.get(current);
          if (cached === void 0) {
            result ??= { matches: false };
            result.matches = next(current);
            resultCache.set(current, result);
            if (result.matches) {
              return true;
            }
          } else {
            if (result) {
              result.matches = cached.matches;
            }
            return cached.matches;
          }
        }
        return false;
      };
    }
    case "_flexibleDescendant": {
      return function flexibleDescendant(element) {
        let current = element;
        do {
          if (next(current)) {
            return true;
          }
          current = getElementParent(current, adapter);
        } while (current);
        return false;
      };
    }
    case SelectorType.Parent: {
      return function parent(element) {
        return adapter.getChildren(element).some((element2) => adapter.isTag(element2) && next(element2));
      };
    }
    case SelectorType.Child: {
      return function child(element) {
        const parent = getElementParent(element, adapter);
        return parent !== null && next(parent);
      };
    }
    case SelectorType.Sibling: {
      return function sibling(element) {
        const siblings = adapter.getSiblings(element);
        for (const currentSibling of siblings) {
          if (equals(element, currentSibling)) {
            break;
          }
          if (adapter.isTag(currentSibling) && next(currentSibling)) {
            return true;
          }
        }
        return false;
      };
    }
    case SelectorType.Adjacent: {
      if (adapter.prevElementSibling) {
        return function adjacent(element) {
          const previous = adapter.prevElementSibling(element);
          return previous != null && next(previous);
        };
      }
      return function adjacent(element) {
        const siblings = adapter.getSiblings(element);
        let lastElement;
        for (const currentSibling of siblings) {
          if (equals(element, currentSibling)) {
            break;
          }
          if (adapter.isTag(currentSibling)) {
            lastElement = currentSibling;
          }
        }
        return !!lastElement && next(lastElement);
      };
    }
    case SelectorType.Universal: {
      if (selector.namespace != null && selector.namespace !== "*") {
        throw new Error("Namespaced universal selectors are not yet supported by css-select");
      }
      return next;
    }
  }
}

// node_modules/css-select/dist/compile.js
var DESCENDANT_TOKEN = { type: SelectorType.Descendant };
var FLEXIBLE_DESCENDANT_TOKEN = {
  type: "_flexibleDescendant"
};
var SCOPE_TOKEN = {
  type: SelectorType.Pseudo,
  name: "scope",
  data: null
};
function absolutize(token, { adapter }, context) {
  const hasContext = !!context?.every((element) => element === PLACEHOLDER_ELEMENT || adapter.isTag(element) && getElementParent(element, adapter) !== null);
  for (const t of token) {
    if (t.length > 0 && isTraversal2(t[0]) && t[0].type !== SelectorType.Descendant) {
    } else if (hasContext && !t.some(includesScopePseudo)) {
      t.unshift(DESCENDANT_TOKEN);
    } else {
      continue;
    }
    t.unshift(SCOPE_TOKEN);
  }
}
function compileToken(token, options, compilationContext) {
  for (const rules of token) {
    sortRules(rules);
  }
  const { context = compilationContext, rootFunc: rootFunction = trueFunc } = options;
  const isArrayContext = Array.isArray(context);
  const finalContext = context && (Array.isArray(context) ? context : [context]);
  if (options.relativeSelector !== false) {
    absolutize(token, options, finalContext);
  } else if (token.some((t) => t.length > 0 && isTraversal2(t[0]))) {
    throw new Error("Relative selectors are not allowed when the `relativeSelector` option is disabled");
  }
  let shouldTestNextSiblings = false;
  let query = falseFunc;
  combineLoop: for (const rules of token) {
    if (rules.length >= 2) {
      const [first, second] = rules;
      if (first.type !== SelectorType.Pseudo || first.name !== "scope") {
      } else if (isArrayContext && second.type === SelectorType.Descendant) {
        rules[1] = FLEXIBLE_DESCENDANT_TOKEN;
      } else if (second.type === SelectorType.Adjacent || second.type === SelectorType.Sibling) {
        shouldTestNextSiblings = true;
      }
    }
    let next = rootFunction;
    let hasExpensiveSubselector = false;
    for (const rule of rules) {
      next = compileGeneralSelector(next, rule, options, finalContext, compileToken, hasExpensiveSubselector);
      const quality = getQuality(rule);
      if (quality === 0) {
        hasExpensiveSubselector = true;
      }
      if (next === falseFunc) {
        continue combineLoop;
      }
    }
    if (next === rootFunction) {
      return rootFunction;
    }
    query = query === falseFunc ? next : or(query, next);
  }
  query.shouldTestNextSiblings = shouldTestNextSiblings;
  return query;
}
function or(a, b) {
  return (element) => a(element) || b(element);
}

// node_modules/css-select/dist/index.js
var defaultEquals = (a, b) => a === b;
var defaultOptions2 = {
  adapter: { ...dist_exports2, isTag: isTag2 },
  equals: defaultEquals
};
function convertOptionFormats(options) {
  const finalOptions = options ?? defaultOptions2;
  finalOptions.adapter ??= defaultOptions2.adapter;
  finalOptions.equals ??= finalOptions.adapter?.equals ?? defaultEquals;
  return finalOptions;
}
function compile2(selector, options, context) {
  const convertedOptions = convertOptionFormats(options);
  const next = _compileUnsafe(selector, convertedOptions, context);
  return next === falseFunc ? falseFunc : (element) => convertedOptions.adapter.isTag(element) && next(element);
}
function _compileUnsafe(selector, options, context) {
  return compileToken(typeof selector === "string" ? parse(selector) : selector, convertOptionFormats(options), context);
}
function getSelectorFunction(searchFunction) {
  return function select(query, elements, options) {
    const convertedOptions = convertOptionFormats(options);
    if (typeof query !== "function") {
      query = _compileUnsafe(query, convertedOptions, elements);
    }
    const filteredElements = prepareContext(elements, convertedOptions.adapter, query.shouldTestNextSiblings);
    return searchFunction(query, filteredElements, convertedOptions);
  };
}
function prepareContext(elements, adapter, shouldTestNextSiblings = false) {
  if (shouldTestNextSiblings) {
    elements = appendNextSiblings(elements, adapter);
  }
  return Array.isArray(elements) ? adapter.removeSubsets(elements) : adapter.getChildren(elements);
}
function appendNextSiblings(element, adapter) {
  const elements = Array.isArray(element) ? [...element] : [element];
  const elementsLength = elements.length;
  for (let index = 0; index < elementsLength; index++) {
    const nextSiblings = getNextSiblings(elements[index], adapter);
    elements.push(...nextSiblings);
  }
  return elements;
}
var selectAll = getSelectorFunction((query, elements, options) => query === falseFunc || !elements || elements.length === 0 ? [] : findAll2(query, elements, options));
var selectOne = getSelectorFunction((query, elements, options) => query === falseFunc || !elements || elements.length === 0 ? null : findOne2(query, elements, options));
function is2(element, query, options) {
  return (typeof query === "function" ? query : compile2(query, options))(element);
}

// node_modules/css-tree/dist/csstree.esm.js
var csstree_esm_exports = {};
__export(csstree_esm_exports, {
  Lexer: () => rt,
  List: () => Q,
  OffsetToLocation: () => lt,
  TokenStream: () => ct,
  clone: () => go,
  createLexer: () => dy,
  createSyntax: () => sn,
  definitionSyntax: () => za,
  find: () => gy,
  findAll: () => yy,
  findLast: () => by,
  fork: () => wy,
  fromPlainObject: () => ky,
  generate: () => hy,
  ident: () => js,
  isCustomProperty: () => Yt,
  keyword: () => Gt,
  lexer: () => my,
  parse: () => py,
  property: () => Er,
  string: () => ji,
  toPlainObject: () => xy,
  tokenNames: () => Ne,
  tokenTypes: () => ur,
  tokenize: () => uy,
  url: () => io,
  vendorPrefix: () => uf,
  version: () => iy,
  walk: () => fy
});
var Us = Object.create;
var cr = Object.defineProperty;
var Hs = Object.getOwnPropertyDescriptor;
var Gs = Object.getOwnPropertyNames;
var Ys = Object.getPrototypeOf;
var Ks = Object.prototype.hasOwnProperty;
var Be = (e2, t) => () => (t || e2((t = { exports: {} }).exports, t), t.exports);
var f = (e2, t) => {
  for (var r in t) cr(e2, r, { get: t[r], enumerable: true });
};
var Vs = (e2, t, r, n) => {
  if (t && typeof t == "object" || typeof t == "function") for (let i of Gs(t)) !Ks.call(e2, i) && i !== r && cr(e2, i, { get: () => t[i], enumerable: !(n = Hs(t, i)) || n.enumerable });
  return e2;
};
var Qs = (e2, t, r) => (r = e2 != null ? Us(Ys(e2)) : {}, Vs(t || !e2 || !e2.__esModule ? cr(r, "default", { value: e2, enumerable: true }) : r, e2));
var Po = Be((xr) => {
  var zo = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/".split("");
  xr.encode = function(e2) {
    if (0 <= e2 && e2 < zo.length) return zo[e2];
    throw new TypeError("Must be between 0 and 63: " + e2);
  };
  xr.decode = function(e2) {
    var t = 65, r = 90, n = 97, i = 122, o = 48, a = 57, c = 43, u = 47, s = 26, l = 52;
    return t <= e2 && e2 <= r ? e2 - t : n <= e2 && e2 <= i ? e2 - n + s : o <= e2 && e2 <= a ? e2 - o + l : e2 == c ? 62 : e2 == u ? 63 : -1;
  };
});
var Fo = Be((wr) => {
  var Io = Po(), kr = 5, No = 1 << kr, Do = No - 1, Oo = No;
  function ll(e2) {
    return e2 < 0 ? (-e2 << 1) + 1 : (e2 << 1) + 0;
  }
  function cl(e2) {
    var t = (e2 & 1) === 1, r = e2 >> 1;
    return t ? -r : r;
  }
  wr.encode = function(t) {
    var r = "", n, i = ll(t);
    do
      n = i & Do, i >>>= kr, i > 0 && (n |= Oo), r += Io.encode(n);
    while (i > 0);
    return r;
  };
  wr.decode = function(t, r, n) {
    var i = t.length, o = 0, a = 0, c, u;
    do {
      if (r >= i) throw new Error("Expected more digits in base 64 VLQ value.");
      if (u = Io.decode(t.charCodeAt(r++)), u === -1) throw new Error("Invalid base64 digit: " + t.charAt(r - 1));
      c = !!(u & Oo), u &= Do, o = o + (u << a), a += kr;
    } while (c);
    n.value = cl(o), n.rest = r;
  };
});
var Mt = Be((W) => {
  function ul(e2, t, r) {
    if (t in e2) return e2[t];
    if (arguments.length === 3) return r;
    throw new Error('"' + t + '" is a required argument.');
  }
  W.getArg = ul;
  var Ro = /^(?:([\w+\-.]+):)?\/\/(?:(\w+:\w+)@)?([\w.-]*)(?::(\d+))?(.*)$/, pl = /^data:.+\,.+$/;
  function pt(e2) {
    var t = e2.match(Ro);
    return t ? { scheme: t[1], auth: t[2], host: t[3], port: t[4], path: t[5] } : null;
  }
  W.urlParse = pt;
  function Ke(e2) {
    var t = "";
    return e2.scheme && (t += e2.scheme + ":"), t += "//", e2.auth && (t += e2.auth + "@"), e2.host && (t += e2.host), e2.port && (t += ":" + e2.port), e2.path && (t += e2.path), t;
  }
  W.urlGenerate = Ke;
  var hl = 32;
  function ml(e2) {
    var t = [];
    return function(r) {
      for (var n = 0; n < t.length; n++) if (t[n].input === r) {
        var i = t[0];
        return t[0] = t[n], t[n] = i, t[0].result;
      }
      var o = e2(r);
      return t.unshift({ input: r, result: o }), t.length > hl && t.pop(), o;
    };
  }
  var vr = ml(function(t) {
    var r = t, n = pt(t);
    if (n) {
      if (!n.path) return t;
      r = n.path;
    }
    for (var i = W.isAbsolute(r), o = [], a = 0, c = 0; ; ) if (a = c, c = r.indexOf("/", a), c === -1) {
      o.push(r.slice(a));
      break;
    } else for (o.push(r.slice(a, c)); c < r.length && r[c] === "/"; ) c++;
    for (var u, s = 0, c = o.length - 1; c >= 0; c--) u = o[c], u === "." ? o.splice(c, 1) : u === ".." ? s++ : s > 0 && (u === "" ? (o.splice(c + 1, s), s = 0) : (o.splice(c, 2), s--));
    return r = o.join("/"), r === "" && (r = i ? "/" : "."), n ? (n.path = r, Ke(n)) : r;
  });
  W.normalize = vr;
  function Mo(e2, t) {
    e2 === "" && (e2 = "."), t === "" && (t = ".");
    var r = pt(t), n = pt(e2);
    if (n && (e2 = n.path || "/"), r && !r.scheme) return n && (r.scheme = n.scheme), Ke(r);
    if (r || t.match(pl)) return t;
    if (n && !n.host && !n.path) return n.host = t, Ke(n);
    var i = t.charAt(0) === "/" ? t : vr(e2.replace(/\/+$/, "") + "/" + t);
    return n ? (n.path = i, Ke(n)) : i;
  }
  W.join = Mo;
  W.isAbsolute = function(e2) {
    return e2.charAt(0) === "/" || Ro.test(e2);
  };
  function dl(e2, t) {
    e2 === "" && (e2 = "."), e2 = e2.replace(/\/$/, "");
    for (var r = 0; t.indexOf(e2 + "/") !== 0; ) {
      var n = e2.lastIndexOf("/");
      if (n < 0 || (e2 = e2.slice(0, n), e2.match(/^([^\/]+:\/)?\/*$/))) return t;
      ++r;
    }
    return Array(r + 1).join("../") + t.substr(e2.length + 1);
  }
  W.relative = dl;
  var Bo = (function() {
    var e2 = /* @__PURE__ */ Object.create(null);
    return !("__proto__" in e2);
  })();
  function _o(e2) {
    return e2;
  }
  function fl(e2) {
    return Wo(e2) ? "$" + e2 : e2;
  }
  W.toSetString = Bo ? _o : fl;
  function gl(e2) {
    return Wo(e2) ? e2.slice(1) : e2;
  }
  W.fromSetString = Bo ? _o : gl;
  function Wo(e2) {
    if (!e2) return false;
    var t = e2.length;
    if (t < 9 || e2.charCodeAt(t - 1) !== 95 || e2.charCodeAt(t - 2) !== 95 || e2.charCodeAt(t - 3) !== 111 || e2.charCodeAt(t - 4) !== 116 || e2.charCodeAt(t - 5) !== 111 || e2.charCodeAt(t - 6) !== 114 || e2.charCodeAt(t - 7) !== 112 || e2.charCodeAt(t - 8) !== 95 || e2.charCodeAt(t - 9) !== 95) return false;
    for (var r = t - 10; r >= 0; r--) if (e2.charCodeAt(r) !== 36) return false;
    return true;
  }
  function bl(e2, t, r) {
    var n = ke(e2.source, t.source);
    return n !== 0 || (n = e2.originalLine - t.originalLine, n !== 0) || (n = e2.originalColumn - t.originalColumn, n !== 0 || r) || (n = e2.generatedColumn - t.generatedColumn, n !== 0) || (n = e2.generatedLine - t.generatedLine, n !== 0) ? n : ke(e2.name, t.name);
  }
  W.compareByOriginalPositions = bl;
  function yl(e2, t, r) {
    var n;
    return n = e2.originalLine - t.originalLine, n !== 0 || (n = e2.originalColumn - t.originalColumn, n !== 0 || r) || (n = e2.generatedColumn - t.generatedColumn, n !== 0) || (n = e2.generatedLine - t.generatedLine, n !== 0) ? n : ke(e2.name, t.name);
  }
  W.compareByOriginalPositionsNoSource = yl;
  function xl(e2, t, r) {
    var n = e2.generatedLine - t.generatedLine;
    return n !== 0 || (n = e2.generatedColumn - t.generatedColumn, n !== 0 || r) || (n = ke(e2.source, t.source), n !== 0) || (n = e2.originalLine - t.originalLine, n !== 0) || (n = e2.originalColumn - t.originalColumn, n !== 0) ? n : ke(e2.name, t.name);
  }
  W.compareByGeneratedPositionsDeflated = xl;
  function kl(e2, t, r) {
    var n = e2.generatedColumn - t.generatedColumn;
    return n !== 0 || r || (n = ke(e2.source, t.source), n !== 0) || (n = e2.originalLine - t.originalLine, n !== 0) || (n = e2.originalColumn - t.originalColumn, n !== 0) ? n : ke(e2.name, t.name);
  }
  W.compareByGeneratedPositionsDeflatedNoLine = kl;
  function ke(e2, t) {
    return e2 === t ? 0 : e2 === null ? 1 : t === null ? -1 : e2 > t ? 1 : -1;
  }
  function wl(e2, t) {
    var r = e2.generatedLine - t.generatedLine;
    return r !== 0 || (r = e2.generatedColumn - t.generatedColumn, r !== 0) || (r = ke(e2.source, t.source), r !== 0) || (r = e2.originalLine - t.originalLine, r !== 0) || (r = e2.originalColumn - t.originalColumn, r !== 0) ? r : ke(e2.name, t.name);
  }
  W.compareByGeneratedPositionsInflated = wl;
  function vl(e2) {
    return JSON.parse(e2.replace(/^\)]}'[^\n]*\n/, ""));
  }
  W.parseSourceMapInput = vl;
  function Sl(e2, t, r) {
    if (t = t || "", e2 && (e2[e2.length - 1] !== "/" && t[0] !== "/" && (e2 += "/"), t = e2 + t), r) {
      var n = pt(r);
      if (!n) throw new Error("sourceMapURL could not be parsed");
      if (n.path) {
        var i = n.path.lastIndexOf("/");
        i >= 0 && (n.path = n.path.substring(0, i + 1));
      }
      t = Mo(Ke(n), t);
    }
    return vr(t);
  }
  W.computeSourceURL = Sl;
});
var jo = Be((qo) => {
  var Sr = Mt(), Cr = Object.prototype.hasOwnProperty, Oe = typeof Map < "u";
  function we() {
    this._array = [], this._set = Oe ? /* @__PURE__ */ new Map() : /* @__PURE__ */ Object.create(null);
  }
  we.fromArray = function(t, r) {
    for (var n = new we(), i = 0, o = t.length; i < o; i++) n.add(t[i], r);
    return n;
  };
  we.prototype.size = function() {
    return Oe ? this._set.size : Object.getOwnPropertyNames(this._set).length;
  };
  we.prototype.add = function(t, r) {
    var n = Oe ? t : Sr.toSetString(t), i = Oe ? this.has(t) : Cr.call(this._set, n), o = this._array.length;
    (!i || r) && this._array.push(t), i || (Oe ? this._set.set(t, o) : this._set[n] = o);
  };
  we.prototype.has = function(t) {
    if (Oe) return this._set.has(t);
    var r = Sr.toSetString(t);
    return Cr.call(this._set, r);
  };
  we.prototype.indexOf = function(t) {
    if (Oe) {
      var r = this._set.get(t);
      if (r >= 0) return r;
    } else {
      var n = Sr.toSetString(t);
      if (Cr.call(this._set, n)) return this._set[n];
    }
    throw new Error('"' + t + '" is not in the set.');
  };
  we.prototype.at = function(t) {
    if (t >= 0 && t < this._array.length) return this._array[t];
    throw new Error("No element indexed by " + t);
  };
  we.prototype.toArray = function() {
    return this._array.slice();
  };
  qo.ArraySet = we;
});
var Go = Be((Ho) => {
  var Uo = Mt();
  function Cl(e2, t) {
    var r = e2.generatedLine, n = t.generatedLine, i = e2.generatedColumn, o = t.generatedColumn;
    return n > r || n == r && o >= i || Uo.compareByGeneratedPositionsInflated(e2, t) <= 0;
  }
  function Bt() {
    this._array = [], this._sorted = true, this._last = { generatedLine: -1, generatedColumn: 0 };
  }
  Bt.prototype.unsortedForEach = function(t, r) {
    this._array.forEach(t, r);
  };
  Bt.prototype.add = function(t) {
    Cl(this._last, t) ? (this._last = t, this._array.push(t)) : (this._sorted = false, this._array.push(t));
  };
  Bt.prototype.toArray = function() {
    return this._sorted || (this._array.sort(Uo.compareByGeneratedPositionsInflated), this._sorted = true), this._array;
  };
  Ho.MappingList = Bt;
});
var Ko = Be((Yo) => {
  var ht = Fo(), F = Mt(), _t = jo().ArraySet, Tl = Go().MappingList;
  function oe(e2) {
    e2 || (e2 = {}), this._file = F.getArg(e2, "file", null), this._sourceRoot = F.getArg(e2, "sourceRoot", null), this._skipValidation = F.getArg(e2, "skipValidation", false), this._ignoreInvalidMapping = F.getArg(e2, "ignoreInvalidMapping", false), this._sources = new _t(), this._names = new _t(), this._mappings = new Tl(), this._sourcesContents = null;
  }
  oe.prototype._version = 3;
  oe.fromSourceMap = function(t, r) {
    var n = t.sourceRoot, i = new oe(Object.assign(r || {}, { file: t.file, sourceRoot: n }));
    return t.eachMapping(function(o) {
      var a = { generated: { line: o.generatedLine, column: o.generatedColumn } };
      o.source != null && (a.source = o.source, n != null && (a.source = F.relative(n, a.source)), a.original = { line: o.originalLine, column: o.originalColumn }, o.name != null && (a.name = o.name)), i.addMapping(a);
    }), t.sources.forEach(function(o) {
      var a = o;
      n !== null && (a = F.relative(n, o)), i._sources.has(a) || i._sources.add(a);
      var c = t.sourceContentFor(o);
      c != null && i.setSourceContent(o, c);
    }), i;
  };
  oe.prototype.addMapping = function(t) {
    var r = F.getArg(t, "generated"), n = F.getArg(t, "original", null), i = F.getArg(t, "source", null), o = F.getArg(t, "name", null);
    !this._skipValidation && this._validateMapping(r, n, i, o) === false || (i != null && (i = String(i), this._sources.has(i) || this._sources.add(i)), o != null && (o = String(o), this._names.has(o) || this._names.add(o)), this._mappings.add({ generatedLine: r.line, generatedColumn: r.column, originalLine: n != null && n.line, originalColumn: n != null && n.column, source: i, name: o }));
  };
  oe.prototype.setSourceContent = function(t, r) {
    var n = t;
    this._sourceRoot != null && (n = F.relative(this._sourceRoot, n)), r != null ? (this._sourcesContents || (this._sourcesContents = /* @__PURE__ */ Object.create(null)), this._sourcesContents[F.toSetString(n)] = r) : this._sourcesContents && (delete this._sourcesContents[F.toSetString(n)], Object.keys(this._sourcesContents).length === 0 && (this._sourcesContents = null));
  };
  oe.prototype.applySourceMap = function(t, r, n) {
    var i = r;
    if (r == null) {
      if (t.file == null) throw new Error(`SourceMapGenerator.prototype.applySourceMap requires either an explicit source file, or the source map's "file" property. Both were omitted.`);
      i = t.file;
    }
    var o = this._sourceRoot;
    o != null && (i = F.relative(o, i));
    var a = new _t(), c = new _t();
    this._mappings.unsortedForEach(function(u) {
      if (u.source === i && u.originalLine != null) {
        var s = t.originalPositionFor({ line: u.originalLine, column: u.originalColumn });
        s.source != null && (u.source = s.source, n != null && (u.source = F.join(n, u.source)), o != null && (u.source = F.relative(o, u.source)), u.originalLine = s.line, u.originalColumn = s.column, s.name != null && (u.name = s.name));
      }
      var l = u.source;
      l != null && !a.has(l) && a.add(l);
      var p = u.name;
      p != null && !c.has(p) && c.add(p);
    }, this), this._sources = a, this._names = c, t.sources.forEach(function(u) {
      var s = t.sourceContentFor(u);
      s != null && (n != null && (u = F.join(n, u)), o != null && (u = F.relative(o, u)), this.setSourceContent(u, s));
    }, this);
  };
  oe.prototype._validateMapping = function(t, r, n, i) {
    if (r && typeof r.line != "number" && typeof r.column != "number") {
      var o = "original.line and original.column are not numbers -- you probably meant to omit the original mapping entirely and only map the generated position. If so, pass null for the original mapping instead of an object with empty or null values.";
      if (this._ignoreInvalidMapping) return typeof console < "u" && console.warn && console.warn(o), false;
      throw new Error(o);
    }
    if (!(t && "line" in t && "column" in t && t.line > 0 && t.column >= 0 && !r && !n && !i)) {
      if (t && "line" in t && "column" in t && r && "line" in r && "column" in r && t.line > 0 && t.column >= 0 && r.line > 0 && r.column >= 0 && n) return;
      var o = "Invalid mapping: " + JSON.stringify({ generated: t, source: n, original: r, name: i });
      if (this._ignoreInvalidMapping) return typeof console < "u" && console.warn && console.warn(o), false;
      throw new Error(o);
    }
  };
  oe.prototype._serializeMappings = function() {
    for (var t = 0, r = 1, n = 0, i = 0, o = 0, a = 0, c = "", u, s, l, p, h = this._mappings.toArray(), d = 0, b = h.length; d < b; d++) {
      if (s = h[d], u = "", s.generatedLine !== r) for (t = 0; s.generatedLine !== r; ) u += ";", r++;
      else if (d > 0) {
        if (!F.compareByGeneratedPositionsInflated(s, h[d - 1])) continue;
        u += ",";
      }
      u += ht.encode(s.generatedColumn - t), t = s.generatedColumn, s.source != null && (p = this._sources.indexOf(s.source), u += ht.encode(p - a), a = p, u += ht.encode(s.originalLine - 1 - i), i = s.originalLine - 1, u += ht.encode(s.originalColumn - n), n = s.originalColumn, s.name != null && (l = this._names.indexOf(s.name), u += ht.encode(l - o), o = l)), c += u;
    }
    return c;
  };
  oe.prototype._generateSourcesContent = function(t, r) {
    return t.map(function(n) {
      if (!this._sourcesContents) return null;
      r != null && (n = F.relative(r, n));
      var i = F.toSetString(n);
      return Object.prototype.hasOwnProperty.call(this._sourcesContents, i) ? this._sourcesContents[i] : null;
    }, this);
  };
  oe.prototype.toJSON = function() {
    var t = { version: this._version, sources: this._sources.toArray(), names: this._names.toArray(), mappings: this._serializeMappings() };
    return this._file != null && (t.file = this._file), this._sourceRoot != null && (t.sourceRoot = this._sourceRoot), this._sourcesContents && (t.sourcesContent = this._generateSourcesContent(t.sources, t.sourceRoot)), t;
  };
  oe.prototype.toString = function() {
    return JSON.stringify(this.toJSON());
  };
  Yo.SourceMapGenerator = oe;
});
var ur = {};
f(ur, { AtKeyword: () => N, BadString: () => _e, BadUrl: () => G, CDC: () => Y, CDO: () => Pe, Colon: () => B, Comma: () => K, Comment: () => D, Delim: () => w, Dimension: () => k, EOF: () => be, Function: () => x, Hash: () => T, Ident: () => m, LeftCurlyBracket: () => R, LeftParenthesis: () => S, LeftSquareBracket: () => $, Number: () => g, Percentage: () => P, RightCurlyBracket: () => Z, RightParenthesis: () => y, RightSquareBracket: () => ee, Semicolon: () => _, String: () => ne, Url: () => M, WhiteSpace: () => L });
var be = 0;
var m = 1;
var x = 2;
var N = 3;
var T = 4;
var ne = 5;
var _e = 6;
var M = 7;
var G = 8;
var w = 9;
var g = 10;
var P = 11;
var k = 12;
var L = 13;
var Pe = 14;
var Y = 15;
var B = 16;
var _ = 17;
var K = 18;
var $ = 19;
var ee = 20;
var S = 21;
var y = 22;
var R = 23;
var Z = 24;
var D = 25;
function O(e2) {
  return e2 >= 48 && e2 <= 57;
}
function te(e2) {
  return O(e2) || e2 >= 65 && e2 <= 70 || e2 >= 97 && e2 <= 102;
}
function zt(e2) {
  return e2 >= 65 && e2 <= 90;
}
function Xs(e2) {
  return e2 >= 97 && e2 <= 122;
}
function $s(e2) {
  return zt(e2) || Xs(e2);
}
function Zs(e2) {
  return e2 >= 128;
}
function Et(e2) {
  return $s(e2) || Zs(e2) || e2 === 95;
}
function We(e2) {
  return Et(e2) || O(e2) || e2 === 45;
}
function Js(e2) {
  return e2 >= 0 && e2 <= 8 || e2 === 11 || e2 >= 14 && e2 <= 31 || e2 === 127;
}
function it(e2) {
  return e2 === 10 || e2 === 13 || e2 === 12;
}
function me(e2) {
  return it(e2) || e2 === 32 || e2 === 9;
}
function V(e2, t) {
  return !(e2 !== 92 || it(t) || t === 0);
}
function qe(e2, t, r) {
  return e2 === 45 ? Et(t) || t === 45 || V(t, r) : Et(e2) ? true : e2 === 92 ? V(e2, t) : false;
}
function Pt(e2, t, r) {
  return e2 === 43 || e2 === 45 ? O(t) ? 2 : t === 46 && O(r) ? 3 : 0 : e2 === 46 ? O(t) ? 2 : 0 : O(e2) ? 1 : 0;
}
function It(e2) {
  return e2 === 65279 || e2 === 65534 ? 1 : 0;
}
var pr = new Array(128);
var el = 128;
var ot = 130;
var hr = 131;
var Nt = 132;
var mr = 133;
for (let e2 = 0; e2 < pr.length; e2++) pr[e2] = me(e2) && ot || O(e2) && hr || Et(e2) && Nt || Js(e2) && mr || e2 || el;
function Dt(e2) {
  return e2 < 128 ? pr[e2] : Nt;
}
function je(e2, t) {
  return t < e2.length ? e2.charCodeAt(t) : 0;
}
function Ot(e2, t, r) {
  return r === 13 && je(e2, t + 1) === 10 ? 2 : 1;
}
function ye(e2, t, r) {
  let n = e2.charCodeAt(t);
  return zt(n) && (n = n | 32), n === r;
}
function xe(e2, t, r, n) {
  if (r - t !== n.length || t < 0 || r > e2.length) return false;
  for (let i = t; i < r; i++) {
    let o = n.charCodeAt(i - t), a = e2.charCodeAt(i);
    if (zt(a) && (a = a | 32), a !== o) return false;
  }
  return true;
}
function bo(e2, t) {
  for (; t >= 0 && me(e2.charCodeAt(t)); t--) ;
  return t + 1;
}
function at(e2, t) {
  for (; t < e2.length && me(e2.charCodeAt(t)); t++) ;
  return t;
}
function dr(e2, t) {
  for (; t < e2.length && O(e2.charCodeAt(t)); t++) ;
  return t;
}
function se(e2, t) {
  if (t += 2, te(je(e2, t - 1))) {
    for (let n = Math.min(e2.length, t + 5); t < n && te(je(e2, t)); t++) ;
    let r = je(e2, t);
    me(r) && (t += Ot(e2, t, r));
  }
  return t;
}
function st(e2, t) {
  for (; t < e2.length; t++) {
    let r = e2.charCodeAt(t);
    if (!We(r)) {
      if (V(r, je(e2, t + 1))) {
        t = se(e2, t) - 1;
        continue;
      }
      break;
    }
  }
  return t;
}
function Ie(e2, t) {
  let r = e2.charCodeAt(t);
  if ((r === 43 || r === 45) && (r = e2.charCodeAt(t += 1)), O(r) && (t = dr(e2, t + 1), r = e2.charCodeAt(t)), r === 46 && O(e2.charCodeAt(t + 1)) && (t += 2, t = dr(e2, t)), ye(e2, t, 101)) {
    let n = 0;
    r = e2.charCodeAt(t + 1), (r === 45 || r === 43) && (n = 1, r = e2.charCodeAt(t + 2)), O(r) && (t = dr(e2, t + 1 + n + 1));
  }
  return t;
}
function Ft(e2, t) {
  for (; t < e2.length; t++) {
    let r = e2.charCodeAt(t);
    if (r === 41) {
      t++;
      break;
    }
    V(r, je(e2, t + 1)) && (t = se(e2, t));
  }
  return t;
}
function Ue(e2) {
  if (e2.length === 1 && !te(e2.charCodeAt(0))) return e2[0];
  let t = parseInt(e2, 16);
  return (t === 0 || t >= 55296 && t <= 57343 || t > 1114111) && (t = 65533), String.fromCodePoint(t);
}
var Ne = ["EOF-token", "ident-token", "function-token", "at-keyword-token", "hash-token", "string-token", "bad-string-token", "url-token", "bad-url-token", "delim-token", "number-token", "percentage-token", "dimension-token", "whitespace-token", "CDO-token", "CDC-token", "colon-token", "semicolon-token", "comma-token", "[-token", "]-token", "(-token", ")-token", "{-token", "}-token", "comment-token"];
function He(e2 = null, t) {
  return e2 === null || e2.length < t ? new Uint32Array(Math.max(t + 1024, 16384)) : e2;
}
var yo = 10;
var tl = 12;
var xo = 13;
function ko(e2) {
  let t = e2.source, r = t.length, n = t.length > 0 ? It(t.charCodeAt(0)) : 0, i = He(e2.lines, r), o = He(e2.columns, r), a = e2.startLine, c = e2.startColumn;
  for (let u = n; u < r; u++) {
    let s = t.charCodeAt(u);
    i[u] = a, o[u] = c++, (s === yo || s === xo || s === tl) && (s === xo && u + 1 < r && t.charCodeAt(u + 1) === yo && (u++, i[u] = a, o[u] = c), a++, c = 1);
  }
  i[r] = a, o[r] = c, e2.lines = i, e2.columns = o, e2.computed = true;
}
var lt = class {
  constructor(t, r, n, i) {
    this.setSource(t, r, n, i), this.lines = null, this.columns = null;
  }
  setSource(t = "", r = 0, n = 1, i = 1) {
    this.source = t, this.startOffset = r, this.startLine = n, this.startColumn = i, this.computed = false;
  }
  getLocation(t, r) {
    return this.computed || ko(this), { source: r, offset: this.startOffset + t, line: this.lines[t], column: this.columns[t] };
  }
  getLocationRange(t, r, n) {
    return this.computed || ko(this), { source: n, start: { offset: this.startOffset + t, line: this.lines[t], column: this.columns[t] }, end: { offset: this.startOffset + r, line: this.lines[r], column: this.columns[r] } };
  }
};
var le = 16777215;
var ce = 24;
var ut = 1;
var Rt = 2;
var Te = new Uint8Array(32);
Te[2] = 22;
Te[21] = 22;
Te[19] = 20;
Te[23] = 24;
var ue = new Uint8Array(32);
ue[2] = ut;
ue[21] = ut;
ue[19] = ut;
ue[23] = ut;
ue[22] = Rt;
ue[20] = Rt;
ue[24] = Rt;
function wo(e2, t, r) {
  return e2 < t ? t : e2 > r ? r : e2;
}
var ct = class {
  constructor(t, r) {
    this.setSource(t, r);
  }
  reset() {
    this.eof = false, this.tokenIndex = -1, this.tokenType = 0, this.tokenStart = this.firstCharOffset, this.tokenEnd = this.firstCharOffset;
  }
  setSource(t = "", r = () => {
  }) {
    t = String(t || "");
    let n = t.length, i = He(this.offsetAndType, t.length + 1), o = He(this.balance, t.length + 1), a = 0, c = -1, u = 0, s = t.length;
    this.offsetAndType = null, this.balance = null, o.fill(0), r(t, (l, p, h) => {
      let d = a++;
      if (i[d] = l << ce | h, c === -1 && (c = p), o[d] = s, l === u) {
        let b = o[s];
        o[s] = d, s = b, u = Te[i[b] >> ce];
      } else this.isBlockOpenerTokenType(l) && (s = d, u = Te[l]);
    }), i[a] = 0 << ce | n, o[a] = a;
    for (let l = 0; l < a; l++) {
      let p = o[l];
      if (p <= l) {
        let h = o[p];
        h !== l && (o[l] = h);
      } else p > a && (o[l] = a);
    }
    this.source = t, this.firstCharOffset = c === -1 ? 0 : c, this.tokenCount = a, this.offsetAndType = i, this.balance = o, this.reset(), this.next();
  }
  lookupType(t) {
    return t += this.tokenIndex, t < this.tokenCount ? this.offsetAndType[t] >> ce : 0;
  }
  lookupTypeNonSC(t) {
    for (let r = this.tokenIndex; r < this.tokenCount; r++) {
      let n = this.offsetAndType[r] >> ce;
      if (n !== 13 && n !== 25 && t-- === 0) return n;
    }
    return 0;
  }
  lookupOffset(t) {
    return t += this.tokenIndex, t < this.tokenCount ? this.offsetAndType[t - 1] & le : this.source.length;
  }
  lookupOffsetNonSC(t) {
    for (let r = this.tokenIndex; r < this.tokenCount; r++) {
      let n = this.offsetAndType[r] >> ce;
      if (n !== 13 && n !== 25 && t-- === 0) return r - this.tokenIndex;
    }
    return 0;
  }
  lookupValue(t, r) {
    return t += this.tokenIndex, t < this.tokenCount ? xe(this.source, this.offsetAndType[t - 1] & le, this.offsetAndType[t] & le, r) : false;
  }
  getTokenStart(t) {
    return t === this.tokenIndex ? this.tokenStart : t > 0 ? t < this.tokenCount ? this.offsetAndType[t - 1] & le : this.offsetAndType[this.tokenCount] & le : this.firstCharOffset;
  }
  getTokenEnd(t) {
    return t === this.tokenIndex ? this.tokenEnd : this.offsetAndType[wo(t, 0, this.tokenCount)] & le;
  }
  getTokenType(t) {
    return t === this.tokenIndex ? this.tokenType : this.offsetAndType[wo(t, 0, this.tokenCount)] >> ce;
  }
  substrToCursor(t) {
    return this.source.substring(t, this.tokenStart);
  }
  isBlockOpenerTokenType(t) {
    return ue[t] === ut;
  }
  isBlockCloserTokenType(t) {
    return ue[t] === Rt;
  }
  getBlockTokenPairIndex(t) {
    let r = this.getTokenType(t);
    if (ue[r] === 1) {
      let n = this.balance[t], i = this.getTokenType(n);
      return Te[r] === i ? n : -1;
    } else if (ue[r] === 2) {
      let n = this.balance[t], i = this.getTokenType(n);
      return Te[i] === r ? n : -1;
    }
    return -1;
  }
  isBalanceEdge(t) {
    return this.balance[this.tokenIndex] < t;
  }
  isDelim(t, r) {
    return r ? this.lookupType(r) === 9 && this.source.charCodeAt(this.lookupOffset(r)) === t : this.tokenType === 9 && this.source.charCodeAt(this.tokenStart) === t;
  }
  skip(t) {
    let r = this.tokenIndex + t;
    r < this.tokenCount ? (this.tokenIndex = r, this.tokenStart = this.offsetAndType[r - 1] & le, r = this.offsetAndType[r], this.tokenType = r >> ce, this.tokenEnd = r & le) : (this.tokenIndex = this.tokenCount, this.next());
  }
  next() {
    let t = this.tokenIndex + 1;
    t < this.tokenCount ? (this.tokenIndex = t, this.tokenStart = this.tokenEnd, t = this.offsetAndType[t], this.tokenType = t >> ce, this.tokenEnd = t & le) : (this.eof = true, this.tokenIndex = this.tokenCount, this.tokenType = 0, this.tokenStart = this.tokenEnd = this.source.length);
  }
  skipSC() {
    for (; this.tokenType === 13 || this.tokenType === 25; ) this.next();
  }
  skipUntilBalanced(t, r) {
    let n = t, i = 0, o = 0;
    e: for (; n < this.tokenCount; n++) {
      if (i = this.balance[n], i < t) break e;
      switch (o = n > 0 ? this.offsetAndType[n - 1] & le : this.firstCharOffset, r(this.source.charCodeAt(o))) {
        case 1:
          break e;
        case 2:
          n++;
          break e;
        default:
          this.isBlockOpenerTokenType(this.offsetAndType[n] >> ce) && (n = i);
      }
    }
    this.skip(n - this.tokenIndex);
  }
  forEachToken(t) {
    for (let r = 0, n = this.firstCharOffset; r < this.tokenCount; r++) {
      let i = n, o = this.offsetAndType[r], a = o & le, c = o >> ce;
      n = a, t(c, i, a, r);
    }
  }
  dump() {
    let t = new Array(this.tokenCount);
    return this.forEachToken((r, n, i, o) => {
      t[o] = { idx: o, type: Ne[r], chunk: this.source.substring(n, i), balance: this.balance[o] };
    }), t;
  }
};
function Le(e2, t) {
  function r(p) {
    return p < c ? e2.charCodeAt(p) : 0;
  }
  function n() {
    if (s = Ie(e2, s), qe(r(s), r(s + 1), r(s + 2))) {
      l = 12, s = st(e2, s);
      return;
    }
    if (r(s) === 37) {
      l = 11, s++;
      return;
    }
    l = 10;
  }
  function i() {
    let p = s;
    if (s = st(e2, s), xe(e2, p, s, "url") && r(s) === 40) {
      if (s = at(e2, s + 1), r(s) === 34 || r(s) === 39) {
        l = 2, s = p + 4;
        return;
      }
      a();
      return;
    }
    if (r(s) === 40) {
      l = 2, s++;
      return;
    }
    l = 1;
  }
  function o(p) {
    for (p || (p = r(s++)), l = 5; s < e2.length; s++) {
      let h = e2.charCodeAt(s);
      switch (Dt(h)) {
        case p:
          s++;
          return;
        case ot:
          if (it(h)) {
            s += Ot(e2, s, h), l = 6;
            return;
          }
          break;
        case 92:
          if (s === e2.length - 1) break;
          let d = r(s + 1);
          it(d) ? s += Ot(e2, s + 1, d) : V(h, d) && (s = se(e2, s) - 1);
          break;
      }
    }
  }
  function a() {
    for (l = 7, s = at(e2, s); s < e2.length; s++) {
      let p = e2.charCodeAt(s);
      switch (Dt(p)) {
        case 41:
          s++;
          return;
        case ot:
          if (s = at(e2, s), r(s) === 41 || s >= e2.length) {
            s < e2.length && s++;
            return;
          }
          s = Ft(e2, s), l = 8;
          return;
        case 34:
        case 39:
        case 40:
        case mr:
          s = Ft(e2, s), l = 8;
          return;
        case 92:
          if (V(p, r(s + 1))) {
            s = se(e2, s) - 1;
            break;
          }
          s = Ft(e2, s), l = 8;
          return;
      }
    }
  }
  e2 = String(e2 || "");
  let c = e2.length, u = It(r(0)), s = u, l;
  for (; s < c; ) {
    let p = e2.charCodeAt(s);
    switch (Dt(p)) {
      case ot:
        l = 13, s = at(e2, s + 1);
        break;
      case 34:
        o();
        break;
      case 35:
        We(r(s + 1)) || V(r(s + 1), r(s + 2)) ? (l = 4, s = st(e2, s + 1)) : (l = 9, s++);
        break;
      case 39:
        o();
        break;
      case 40:
        l = 21, s++;
        break;
      case 41:
        l = 22, s++;
        break;
      case 43:
        Pt(p, r(s + 1), r(s + 2)) ? n() : (l = 9, s++);
        break;
      case 44:
        l = 18, s++;
        break;
      case 45:
        Pt(p, r(s + 1), r(s + 2)) ? n() : r(s + 1) === 45 && r(s + 2) === 62 ? (l = 15, s = s + 3) : qe(p, r(s + 1), r(s + 2)) ? i() : (l = 9, s++);
        break;
      case 46:
        Pt(p, r(s + 1), r(s + 2)) ? n() : (l = 9, s++);
        break;
      case 47:
        r(s + 1) === 42 ? (l = 25, s = e2.indexOf("*/", s + 2), s = s === -1 ? e2.length : s + 2) : (l = 9, s++);
        break;
      case 58:
        l = 16, s++;
        break;
      case 59:
        l = 17, s++;
        break;
      case 60:
        r(s + 1) === 33 && r(s + 2) === 45 && r(s + 3) === 45 ? (l = 14, s = s + 4) : (l = 9, s++);
        break;
      case 64:
        qe(r(s + 1), r(s + 2), r(s + 3)) ? (l = 3, s = st(e2, s + 1)) : (l = 9, s++);
        break;
      case 91:
        l = 19, s++;
        break;
      case 92:
        V(p, r(s + 1)) ? i() : (l = 9, s++);
        break;
      case 93:
        l = 20, s++;
        break;
      case 123:
        l = 23, s++;
        break;
      case 125:
        l = 24, s++;
        break;
      case hr:
        n();
        break;
      case Nt:
        i();
        break;
      default:
        l = 9, s++;
    }
    t(l, u, u = s);
  }
}
var Ge = null;
var Q = class e {
  static createItem(t) {
    return { prev: null, next: null, data: t };
  }
  constructor() {
    this.head = null, this.tail = null, this.cursor = null;
  }
  createItem(t) {
    return e.createItem(t);
  }
  allocateCursor(t, r) {
    let n;
    return Ge !== null ? (n = Ge, Ge = Ge.cursor, n.prev = t, n.next = r, n.cursor = this.cursor) : n = { prev: t, next: r, cursor: this.cursor }, this.cursor = n, n;
  }
  releaseCursor() {
    let { cursor: t } = this;
    this.cursor = t.cursor, t.prev = null, t.next = null, t.cursor = Ge, Ge = t;
  }
  updateCursors(t, r, n, i) {
    let { cursor: o } = this;
    for (; o !== null; ) o.prev === t && (o.prev = r), o.next === n && (o.next = i), o = o.cursor;
  }
  *[Symbol.iterator]() {
    for (let t = this.head; t !== null; t = t.next) yield t.data;
  }
  get size() {
    let t = 0;
    for (let r = this.head; r !== null; r = r.next) t++;
    return t;
  }
  get isEmpty() {
    return this.head === null;
  }
  get first() {
    return this.head && this.head.data;
  }
  get last() {
    return this.tail && this.tail.data;
  }
  fromArray(t) {
    let r = null;
    this.head = null;
    for (let n of t) {
      let i = e.createItem(n);
      r !== null ? r.next = i : this.head = i, i.prev = r, r = i;
    }
    return this.tail = r, this;
  }
  toArray() {
    return [...this];
  }
  toJSON() {
    return [...this];
  }
  forEach(t, r = this) {
    let n = this.allocateCursor(null, this.head);
    for (; n.next !== null; ) {
      let i = n.next;
      n.next = i.next, t.call(r, i.data, i, this);
    }
    this.releaseCursor();
  }
  forEachRight(t, r = this) {
    let n = this.allocateCursor(this.tail, null);
    for (; n.prev !== null; ) {
      let i = n.prev;
      n.prev = i.prev, t.call(r, i.data, i, this);
    }
    this.releaseCursor();
  }
  reduce(t, r, n = this) {
    let i = this.allocateCursor(null, this.head), o = r, a;
    for (; i.next !== null; ) a = i.next, i.next = a.next, o = t.call(n, o, a.data, a, this);
    return this.releaseCursor(), o;
  }
  reduceRight(t, r, n = this) {
    let i = this.allocateCursor(this.tail, null), o = r, a;
    for (; i.prev !== null; ) a = i.prev, i.prev = a.prev, o = t.call(n, o, a.data, a, this);
    return this.releaseCursor(), o;
  }
  some(t, r = this) {
    for (let n = this.head; n !== null; n = n.next) if (t.call(r, n.data, n, this)) return true;
    return false;
  }
  map(t, r = this) {
    let n = new e();
    for (let i = this.head; i !== null; i = i.next) n.appendData(t.call(r, i.data, i, this));
    return n;
  }
  filter(t, r = this) {
    let n = new e();
    for (let i = this.head; i !== null; i = i.next) t.call(r, i.data, i, this) && n.appendData(i.data);
    return n;
  }
  nextUntil(t, r, n = this) {
    if (t === null) return;
    let i = this.allocateCursor(null, t);
    for (; i.next !== null; ) {
      let o = i.next;
      if (i.next = o.next, r.call(n, o.data, o, this)) break;
    }
    this.releaseCursor();
  }
  prevUntil(t, r, n = this) {
    if (t === null) return;
    let i = this.allocateCursor(t, null);
    for (; i.prev !== null; ) {
      let o = i.prev;
      if (i.prev = o.prev, r.call(n, o.data, o, this)) break;
    }
    this.releaseCursor();
  }
  clear() {
    this.head = null, this.tail = null;
  }
  copy() {
    let t = new e();
    for (let r of this) t.appendData(r);
    return t;
  }
  prepend(t) {
    return this.updateCursors(null, t, this.head, t), this.head !== null ? (this.head.prev = t, t.next = this.head) : this.tail = t, this.head = t, this;
  }
  prependData(t) {
    return this.prepend(e.createItem(t));
  }
  append(t) {
    return this.insert(t);
  }
  appendData(t) {
    return this.insert(e.createItem(t));
  }
  insert(t, r = null) {
    if (r !== null) if (this.updateCursors(r.prev, t, r, t), r.prev === null) {
      if (this.head !== r) throw new Error("before doesn't belong to list");
      this.head = t, r.prev = t, t.next = r, this.updateCursors(null, t);
    } else r.prev.next = t, t.prev = r.prev, r.prev = t, t.next = r;
    else this.updateCursors(this.tail, t, null, t), this.tail !== null ? (this.tail.next = t, t.prev = this.tail) : this.head = t, this.tail = t;
    return this;
  }
  insertData(t, r) {
    return this.insert(e.createItem(t), r);
  }
  remove(t) {
    if (this.updateCursors(t, t.prev, t, t.next), t.prev !== null) t.prev.next = t.next;
    else {
      if (this.head !== t) throw new Error("item doesn't belong to list");
      this.head = t.next;
    }
    if (t.next !== null) t.next.prev = t.prev;
    else {
      if (this.tail !== t) throw new Error("item doesn't belong to list");
      this.tail = t.prev;
    }
    return t.prev = null, t.next = null, t;
  }
  push(t) {
    this.insert(e.createItem(t));
  }
  pop() {
    return this.tail !== null ? this.remove(this.tail) : null;
  }
  unshift(t) {
    this.prepend(e.createItem(t));
  }
  shift() {
    return this.head !== null ? this.remove(this.head) : null;
  }
  prependList(t) {
    return this.insertList(t, this.head);
  }
  appendList(t) {
    return this.insertList(t);
  }
  insertList(t, r) {
    return t.head === null ? this : (r != null ? (this.updateCursors(r.prev, t.tail, r, t.head), r.prev !== null ? (r.prev.next = t.head, t.head.prev = r.prev) : this.head = t.head, r.prev = t.tail, t.tail.next = r) : (this.updateCursors(this.tail, t.tail, null, t.head), this.tail !== null ? (this.tail.next = t.head, t.head.prev = this.tail) : this.head = t.head, this.tail = t.tail), t.head = null, t.tail = null, this);
  }
  replace(t, r) {
    "head" in r ? this.insertList(r, t) : this.insert(r, t), this.remove(t);
  }
};
function De(e2, t) {
  let r = Object.create(SyntaxError.prototype), n = new Error();
  return Object.assign(r, { name: e2, message: t, get stack() {
    return (n.stack || "").replace(/^(.+\n){1,3}/, `${e2}: ${t}
`);
  } });
}
var fr = 100;
var vo = 60;
var So = "    ";
function Co({ source: e2, line: t, column: r, baseLine: n, baseColumn: i }, o) {
  function a(b, j) {
    return s.slice(b, j).map((J, v) => String(b + v + 1).padStart(h) + " |" + J).join(`
`);
  }
  let c = `
`.repeat(Math.max(n - 1, 0)), u = " ".repeat(Math.max(i - 1, 0)), s = (c + u + e2).split(/\r\n?|\n|\f/), l = Math.max(1, t - o) - 1, p = Math.min(t + o, s.length + 1), h = Math.max(4, String(p).length) + 1, d = 0;
  r += (So.length - 1) * (s[t - 1].substr(0, r - 1).match(/\t/g) || []).length, r > fr && (d = r - vo + 3, r = vo - 2);
  for (let b = l; b <= p; b++) b >= 0 && b < s.length && (s[b] = s[b].replace(/\t/g, So), s[b] = (d > 0 && s[b].length > d ? "\u2026" : "") + s[b].substr(d, fr - 2) + (s[b].length > d + fr - 1 ? "\u2026" : ""));
  return [a(l, t), new Array(r + h + 2).join("-") + "^", a(t, p)].filter(Boolean).join(`
`).replace(/^(\s+\d+\s+\|\n)+/, "").replace(/\n(\s+\d+\s+\|)+$/, "");
}
function gr(e2, t, r, n, i, o = 1, a = 1) {
  return Object.assign(De("SyntaxError", e2), { source: t, offset: r, line: n, column: i, sourceFragment(u) {
    return Co({ source: t, line: n, column: i, baseLine: o, baseColumn: a }, isNaN(u) ? 0 : u);
  }, get formattedMessage() {
    return `Parse error: ${e2}
` + Co({ source: t, line: n, column: i, baseLine: o, baseColumn: a }, 2);
  } });
}
function To(e2) {
  let t = this.createList(), r = false, n = { recognizer: e2 };
  for (; !this.eof; ) {
    switch (this.tokenType) {
      case 25:
        this.next();
        continue;
      case 13:
        r = true, this.next();
        continue;
    }
    let i = e2.getNode.call(this, n);
    if (i === void 0) break;
    r && (e2.onWhiteSpace && e2.onWhiteSpace.call(this, i, t, n), r = false), t.push(i);
  }
  return r && e2.onWhiteSpace && e2.onWhiteSpace.call(this, null, t, n), t;
}
var Ye = () => {
};
var rl = 33;
var nl = 35;
var br = 59;
var Lo = 123;
var Ao = 0;
var il = { createList() {
  return [];
}, createSingleNodeList(e2) {
  return [e2];
}, getFirstListNode(e2) {
  return e2 && e2[0] || null;
}, getLastListNode(e2) {
  return e2 && e2.length > 0 ? e2[e2.length - 1] : null;
} };
var ol = { createList() {
  return new Q();
}, createSingleNodeList(e2) {
  return new Q().appendData(e2);
}, getFirstListNode(e2) {
  return e2 && e2.first;
}, getLastListNode(e2) {
  return e2 && e2.last;
} };
function al(e2) {
  return function() {
    return this[e2]();
  };
}
function yr(e2) {
  let t = /* @__PURE__ */ Object.create(null);
  for (let r of Object.keys(e2)) {
    let n = e2[r], i = n.parse || n;
    i && (t[r] = i);
  }
  return t;
}
function sl(e2) {
  let t = { context: /* @__PURE__ */ Object.create(null), features: Object.assign(/* @__PURE__ */ Object.create(null), e2.features), scope: Object.assign(/* @__PURE__ */ Object.create(null), e2.scope), atrule: yr(e2.atrule), pseudo: yr(e2.pseudo), node: yr(e2.node) };
  for (let [r, n] of Object.entries(e2.parseContext)) switch (typeof n) {
    case "function":
      t.context[r] = n;
      break;
    case "string":
      t.context[r] = al(n);
      break;
  }
  return { config: t, ...t, ...t.node };
}
function Eo(e2) {
  let t = "", r = "<unknown>", n = false, i = Ye, o = false, a = new lt(), c = Object.assign(new ct(), sl(e2 || {}), { parseAtrulePrelude: true, parseRulePrelude: true, parseValue: true, parseCustomProperty: false, readSequence: To, consumeUntilBalanceEnd: () => 0, consumeUntilLeftCurlyBracket(l) {
    return l === Lo ? 1 : 0;
  }, consumeUntilLeftCurlyBracketOrSemicolon(l) {
    return l === Lo || l === br ? 1 : 0;
  }, consumeUntilExclamationMarkOrSemicolon(l) {
    return l === rl || l === br ? 1 : 0;
  }, consumeUntilSemicolonIncluded(l) {
    return l === br ? 2 : 0;
  }, createList: Ye, createSingleNodeList: Ye, getFirstListNode: Ye, getLastListNode: Ye, parseWithFallback(l, p) {
    let h = this.tokenIndex;
    try {
      return l.call(this);
    } catch (d) {
      if (o) throw d;
      this.skip(h - this.tokenIndex);
      let b = p.call(this);
      return o = true, i(d, b), o = false, b;
    }
  }, lookupNonWSType(l) {
    let p;
    do
      if (p = this.lookupType(l++), p !== 13 && p !== 25) return p;
    while (p !== Ao);
    return Ao;
  }, charCodeAt(l) {
    return l >= 0 && l < t.length ? t.charCodeAt(l) : 0;
  }, substring(l, p) {
    return t.substring(l, p);
  }, substrToCursor(l) {
    return this.source.substring(l, this.tokenStart);
  }, cmpChar(l, p) {
    return ye(t, l, p);
  }, cmpStr(l, p, h) {
    return xe(t, l, p, h);
  }, consume(l) {
    let p = this.tokenStart;
    return this.eat(l), this.substrToCursor(p);
  }, consumeFunctionName() {
    let l = t.substring(this.tokenStart, this.tokenEnd - 1);
    return this.eat(2), l;
  }, consumeNumber(l) {
    let p = t.substring(this.tokenStart, Ie(t, this.tokenStart));
    return this.eat(l), p;
  }, eat(l) {
    if (this.tokenType !== l) {
      let p = Ne[l].slice(0, -6).replace(/-/g, " ").replace(/^./, (b) => b.toUpperCase()), h = `${/[[\](){}]/.test(p) ? `"${p}"` : p} is expected`, d = this.tokenStart;
      switch (l) {
        case 1:
          this.tokenType === 2 || this.tokenType === 7 ? (d = this.tokenEnd - 1, h = "Identifier is expected but function found") : h = "Identifier is expected";
          break;
        case 4:
          this.isDelim(nl) && (this.next(), d++, h = "Name is expected");
          break;
        case 11:
          this.tokenType === 10 && (d = this.tokenEnd, h = "Percent sign is expected");
          break;
      }
      this.error(h, d);
    }
    this.next();
  }, eatIdent(l) {
    (this.tokenType !== 1 || this.lookupValue(0, l) === false) && this.error(`Identifier "${l}" is expected`), this.next();
  }, eatDelim(l) {
    this.isDelim(l) || this.error(`Delim "${String.fromCharCode(l)}" is expected`), this.next();
  }, getLocation(l, p) {
    return n ? a.getLocationRange(l, p, r) : null;
  }, getLocationFromList(l) {
    if (n) {
      let p = this.getFirstListNode(l), h = this.getLastListNode(l);
      return a.getLocationRange(p !== null ? p.loc.start.offset - a.startOffset : this.tokenStart, h !== null ? h.loc.end.offset - a.startOffset : this.tokenStart, r);
    }
    return null;
  }, error(l, p) {
    let h = typeof p < "u" && p < t.length ? a.getLocation(p) : this.eof ? a.getLocation(bo(t, t.length - 1)) : a.getLocation(this.tokenStart);
    throw new gr(l || "Unexpected input", t, h.offset, h.line, h.column, a.startLine, a.startColumn);
  } }), u = () => ({ filename: r, source: t, tokenCount: c.tokenCount, getTokenType: (l) => c.getTokenType(l), getTokenTypeName: (l) => Ne[c.getTokenType(l)], getTokenStart: (l) => c.getTokenStart(l), getTokenEnd: (l) => c.getTokenEnd(l), getTokenValue: (l) => c.source.substring(c.getTokenStart(l), c.getTokenEnd(l)), substring: (l, p) => c.source.substring(l, p), balance: c.balance.subarray(0, c.tokenCount + 1), isBlockOpenerTokenType: c.isBlockOpenerTokenType, isBlockCloserTokenType: c.isBlockCloserTokenType, getBlockTokenPairIndex: (l) => c.getBlockTokenPairIndex(l), getLocation: (l) => a.getLocation(l, r), getRangeLocation: (l, p) => a.getLocationRange(l, p, r) });
  return Object.assign(function(l, p) {
    t = l, p = p || {}, c.setSource(t, Le), a.setSource(t, p.offset, p.line, p.column), r = p.filename || "<unknown>", n = !!p.positions, i = typeof p.onParseError == "function" ? p.onParseError : Ye, o = false, c.parseAtrulePrelude = "parseAtrulePrelude" in p ? !!p.parseAtrulePrelude : true, c.parseRulePrelude = "parseRulePrelude" in p ? !!p.parseRulePrelude : true, c.parseValue = "parseValue" in p ? !!p.parseValue : true, c.parseCustomProperty = "parseCustomProperty" in p ? !!p.parseCustomProperty : false;
    let { context: h = "default", list: d = true, onComment: b, onToken: j } = p;
    if (!(h in c.context)) throw new Error("Unknown context `" + h + "`");
    Object.assign(c, d ? ol : il), Array.isArray(j) ? c.forEachToken((v, E, U) => {
      j.push({ type: v, start: E, end: U });
    }) : typeof j == "function" && c.forEachToken(j.bind(u())), typeof b == "function" && c.forEachToken((v, E, U) => {
      if (v === 25) {
        let z = c.getLocation(E, U), re = xe(t, U - 2, U, "*/") ? t.slice(E + 2, U - 2) : t.slice(E + 2, U);
        b(re, z);
      }
    });
    let J = c.context[h].call(c, p);
    return c.eof || c.error(), J;
  }, { SyntaxError: gr, config: c.config });
}
var Qo = Qs(Ko(), 1);
var Vo = /* @__PURE__ */ new Set(["Atrule", "Selector", "Declaration"]);
function Xo(e2) {
  let t = new Qo.SourceMapGenerator(), r = { line: 1, column: 0 }, n = { line: 0, column: 0 }, i = { line: 1, column: 0 }, o = { generated: i }, a = 1, c = 0, u = false, s = e2.node;
  e2.node = function(h) {
    if (h.loc && h.loc.start && Vo.has(h.type)) {
      let d = h.loc.start.line, b = h.loc.start.column - 1;
      (n.line !== d || n.column !== b) && (n.line = d, n.column = b, r.line = a, r.column = c, u && (u = false, (r.line !== i.line || r.column !== i.column) && t.addMapping(o)), u = true, t.addMapping({ source: h.loc.source, original: n, generated: r }));
    }
    s.call(this, h), u && Vo.has(h.type) && (i.line = a, i.column = c);
  };
  let l = e2.emit;
  e2.emit = function(h, d, b) {
    for (let j = 0; j < h.length; j++) h.charCodeAt(j) === 10 ? (a++, c = 0) : c++;
    l(h, d, b);
  };
  let p = e2.result;
  return e2.result = function() {
    return u && t.addMapping(o), { css: p(), map: t };
  }, e2;
}
var Wt = {};
f(Wt, { safe: () => Lr, spec: () => zl });
var Ll = 43;
var Al = 45;
var Tr = (e2, t) => (e2 === 9 && (e2 = t), typeof e2 == "string" && (e2 = Math.min(e2.charCodeAt(0), 128) << 6), e2 << 1);
var $o = [[1, 1], [1, 2], [1, 7], [1, 8], [1, "-"], [1, 10], [1, 11], [1, 12], [1, 15], [1, 21], [3, 1], [3, 2], [3, 7], [3, 8], [3, "-"], [3, 10], [3, 11], [3, 12], [3, 15], [4, 1], [4, 2], [4, 7], [4, 8], [4, "-"], [4, 10], [4, 11], [4, 12], [4, 15], [12, 1], [12, 2], [12, 7], [12, 8], [12, "-"], [12, 10], [12, 11], [12, 12], [12, 15], ["#", 1], ["#", 2], ["#", 7], ["#", 8], ["#", "-"], ["#", 10], ["#", 11], ["#", 12], ["#", 15], ["-", 1], ["-", 2], ["-", 7], ["-", 8], ["-", "-"], ["-", 10], ["-", 11], ["-", 12], ["-", 15], [10, 1], [10, 2], [10, 7], [10, 8], [10, 10], [10, 11], [10, 12], [10, "%"], [10, 15], ["@", 1], ["@", 2], ["@", 7], ["@", 8], ["@", "-"], ["@", 15], [".", 10], [".", 11], [".", 12], ["+", 10], ["+", 11], ["+", 12], ["/", "*"]];
var El = $o.concat([[1, 4], [12, 4], [4, 4], [3, 21], [3, 5], [3, 16], [11, 11], [11, 12], [11, 2], [11, "-"], [22, 1], [22, 2], [22, 11], [22, 12], [22, 4], [22, "-"]]);
function Zo(e2) {
  let t = new Set(e2.map(([r, n]) => Tr(r) << 16 | Tr(n)));
  return function(r, n, i) {
    let o = Tr(n, i), a = i.charCodeAt(0), c = a === Al && n !== 1 && n !== 2 && n !== 15 || a === Ll ? t.has((r & 65534) << 16 | a << 7) : t.has((r & 65534) << 16 | o);
    return o | c;
  };
}
var zl = Zo($o);
var Lr = Zo(El);
var Pl = 92;
function Il(e2, t) {
  if (typeof t == "function") {
    let r = null;
    e2.children.forEach((n) => {
      r !== null && t.call(this, r), this.node(n), r = n;
    });
    return;
  }
  e2.children.forEach(this.node, this);
}
function Jo(e2) {
  let t = /* @__PURE__ */ new Map();
  for (let [r, n] of Object.entries(e2.node)) typeof (n.generate || n) == "function" && t.set(r, n.generate || n);
  return function(r, n) {
    let i = "", o = 0, a = { node(u) {
      if (t.has(u.type)) t.get(u.type).call(c, u);
      else throw new Error("Unknown node type: " + u.type);
    }, tokenBefore: Lr, token(u, s, l) {
      o = this.tokenBefore(o, u, s), !l && o & 1 && this.emit(" ", 13, true), this.emit(s, u, false), u === 9 && s.charCodeAt(0) === Pl && this.emit(`
`, 13, true);
    }, emit(u) {
      i += u;
    }, result() {
      return i;
    } };
    n && (typeof n.decorator == "function" && (a = n.decorator(a)), n.sourceMap && (a = Xo(a)), n.mode in Wt && (a.tokenBefore = Wt[n.mode]));
    let c = { node: (u) => a.node(u), children: Il, token: (u, s) => a.token(u, s), tokenize: (u) => Le(u, (s, l, p) => {
      a.token(s, u.slice(l, p), l !== 0);
    }) };
    return a.node(r), a.result();
  };
}
function ea(e2) {
  return { fromPlainObject(t) {
    return e2(t, { enter(r) {
      r.children && !(r.children instanceof Q) && (r.children = new Q().fromArray(r.children));
    } }), t;
  }, toPlainObject(t) {
    return e2(t, { leave(r) {
      r.children && r.children instanceof Q && (r.children = r.children.toArray());
    } }), t;
  } };
}
var { hasOwnProperty: Ar } = Object.prototype;
var mt = function() {
};
function ta(e2) {
  return typeof e2 == "function" ? e2 : mt;
}
function ra(e2, t) {
  return function(r, n, i) {
    r.type === t && e2.call(this, r, n, i);
  };
}
function Nl(e2, t) {
  let r = t.structure, n = [];
  for (let i in r) {
    if (Ar.call(r, i) === false) continue;
    let o = r[i], a = { name: i, type: false, nullable: false };
    Array.isArray(o) || (o = [o]);
    for (let c of o) c === null ? a.nullable = true : typeof c == "string" ? a.type = "node" : Array.isArray(c) && (a.type = "list");
    a.type && n.push(a);
  }
  return n.length ? { context: t.walkContext, fields: n } : null;
}
function Dl(e2) {
  let t = {};
  for (let r in e2.node) if (Ar.call(e2.node, r)) {
    let n = e2.node[r];
    if (!n.structure) throw new Error("Missed `structure` field in `" + r + "` node type definition");
    t[r] = Nl(r, n);
  }
  return t;
}
function na(e2, t) {
  let r = e2.fields.slice(), n = e2.context, i = typeof n == "string";
  return t && r.reverse(), function(o, a, c, u) {
    let s;
    i && (s = a[n], a[n] = o);
    for (let l of r) {
      let p = o[l.name];
      if (!l.nullable || p) {
        if (l.type === "list") {
          if (t ? p.reduceRight(u, false) : p.reduce(u, false)) return true;
        } else if (c(p)) return true;
      }
    }
    i && (a[n] = s);
  };
}
function ia({ StyleSheet: e2, Atrule: t, Rule: r, Block: n, DeclarationList: i }) {
  return { Atrule: { StyleSheet: e2, Atrule: t, Rule: r, Block: n }, Rule: { StyleSheet: e2, Atrule: t, Rule: r, Block: n }, Declaration: { StyleSheet: e2, Atrule: t, Rule: r, Block: n, DeclarationList: i } };
}
function oa(e2) {
  let t = Dl(e2), r = {}, n = {}, i = /* @__PURE__ */ Symbol("break-walk"), o = /* @__PURE__ */ Symbol("skip-node");
  for (let s in t) Ar.call(t, s) && t[s] !== null && (r[s] = na(t[s], false), n[s] = na(t[s], true));
  let a = ia(r), c = ia(n), u = function(s, l) {
    function p(v, E, U) {
      let z = h.call(J, v, E, U);
      return z === i ? true : z === o ? false : !!(b.hasOwnProperty(v.type) && b[v.type](v, J, p, j) || d.call(J, v, E, U) === i);
    }
    let h = mt, d = mt, b = r, j = (v, E, U, z) => v || p(E, U, z), J = { break: i, skip: o, root: s, stylesheet: null, atrule: null, atrulePrelude: null, rule: null, selector: null, block: null, declaration: null, function: null };
    if (typeof l == "function") h = l;
    else if (l && (h = ta(l.enter), d = ta(l.leave), l.reverse && (b = n), l.visit)) {
      if (a.hasOwnProperty(l.visit)) b = l.reverse ? c[l.visit] : a[l.visit];
      else if (!t.hasOwnProperty(l.visit)) throw new Error("Bad value `" + l.visit + "` for `visit` option (should be: " + Object.keys(t).sort().join(", ") + ")");
      h = ra(h, l.visit), d = ra(d, l.visit);
    }
    if (h === mt && d === mt) throw new Error("Neither `enter` nor `leave` walker handler is set or both aren't a function");
    p(s);
  };
  return u.break = i, u.skip = o, u.find = function(s, l) {
    let p = null;
    return u(s, function(h, d, b) {
      if (l.call(this, h, d, b)) return p = h, i;
    }), p;
  }, u.findLast = function(s, l) {
    let p = null;
    return u(s, { reverse: true, enter(h, d, b) {
      if (l.call(this, h, d, b)) return p = h, i;
    } }), p;
  }, u.findAll = function(s, l) {
    let p = [];
    return u(s, function(h, d, b) {
      l.call(this, h, d, b) && p.push(h);
    }), p;
  }, u;
}
function Ol(e2) {
  return e2;
}
function Fl(e2) {
  let { min: t, max: r, comma: n } = e2;
  return t === 0 && r === 0 ? n ? "#?" : "*" : t === 0 && r === 1 ? "?" : t === 1 && r === 0 ? n ? "#" : "+" : t === 1 && r === 1 ? "" : (n ? "#" : "") + (t === r ? "{" + t + "}" : "{" + t + "," + (r !== 0 ? r : "") + "}");
}
function Rl(e2) {
  if (e2.type === "Range") return " [" + (e2.min === null ? "-\u221E" : e2.min) + "," + (e2.max === null ? "\u221E" : e2.max) + "]";
  throw new Error("Unknown node type `" + e2.type + "`");
}
function Ml(e2, t, r, n) {
  let i = e2.combinator === " " || n ? e2.combinator : " " + e2.combinator + " ", o = e2.terms.map((a) => qt(a, t, r, n)).join(i);
  return e2.explicit || r ? (n || o[0] === "," ? "[" : "[ ") + o + (n ? "]" : " ]") : o;
}
function qt(e2, t, r, n) {
  let i;
  switch (e2.type) {
    case "Group":
      i = Ml(e2, t, r, n) + (e2.disallowEmpty ? "!" : "");
      break;
    case "Multiplier":
      return qt(e2.term, t, r, n) + t(Fl(e2), e2);
    case "Boolean":
      i = "<boolean-expr[" + qt(e2.term, t, r, n) + "]>";
      break;
    case "Type":
      i = "<" + e2.name + (e2.opts ? t(Rl(e2.opts), e2.opts) : "") + ">";
      break;
    case "Property":
      i = "<'" + e2.name + "'>";
      break;
    case "Keyword":
      i = e2.name;
      break;
    case "AtKeyword":
      i = "@" + e2.name;
      break;
    case "Function":
      i = e2.name + "(";
      break;
    case "String":
    case "Token":
      i = e2.value;
      break;
    case "Comma":
      i = ",";
      break;
    default:
      throw new Error("Unknown node type `" + e2.type + "`");
  }
  return t(i, e2);
}
function Fe(e2, t) {
  let r = Ol, n = false, i = false;
  return typeof t == "function" ? r = t : t && (n = !!t.forceBraces, i = !!t.compact, typeof t.decorate == "function" && (r = t.decorate)), qt(e2, r, n, i);
}
var aa = { offset: 0, line: 1, column: 1 };
function Bl(e2, t) {
  let r = e2.tokens, n = e2.longestMatch, i = n < r.length && r[n].node || null, o = i !== t ? i : null, a = 0, c = 0, u = 0, s = "", l, p;
  for (let h = 0; h < r.length; h++) {
    let d = r[h].value;
    h === n && (c = d.length, a = s.length), o !== null && r[h].node === o && (h <= n ? u++ : u = 0), s += d;
  }
  return n === r.length || u > 1 ? (l = jt(o || t, "end") || dt(aa, s), p = dt(l)) : (l = jt(o, "start") || dt(jt(t, "start") || aa, s.slice(0, a)), p = jt(o, "end") || dt(l, s.substr(a, c))), { css: s, mismatchOffset: a, mismatchLength: c, start: l, end: p };
}
function jt(e2, t) {
  let r = e2 && e2.loc && e2.loc[t];
  return r ? "line" in r ? dt(r) : r : null;
}
function dt({ offset: e2, line: t, column: r }, n) {
  let i = { offset: e2, line: t, column: r };
  if (n) {
    let o = n.split(/\n|\r\n?|\f/);
    i.offset += n.length, i.line += o.length - 1, i.column = o.length === 1 ? i.column + n.length : o.pop().length + 1;
  }
  return i;
}
var Ve = function(e2, t) {
  let r = De("SyntaxReferenceError", e2 + (t ? " `" + t + "`" : ""));
  return r.reference = t, r;
};
var sa = function(e2, t, r, n) {
  let i = De("SyntaxMatchError", e2), { css: o, mismatchOffset: a, mismatchLength: c, start: u, end: s } = Bl(n, r);
  return i.rawMessage = e2, i.syntax = t ? Fe(t) : "<generic>", i.css = o, i.mismatchOffset = a, i.mismatchLength = c, i.message = e2 + `
  syntax: ` + i.syntax + `
   value: ` + (o || "<empty string>") + `
  --------` + new Array(i.mismatchOffset + 1).join("-") + "^", Object.assign(i, u), i.loc = { source: r && r.loc && r.loc.source || "<unknown>", start: u, end: s }, i;
};
var Ut = /* @__PURE__ */ new Map();
var Qe = /* @__PURE__ */ new Map();
var Ht = 45;
var Gt = _l;
var Er = Wl;
var uf = zr;
function Yt(e2, t) {
  return t = t || 0, e2.length - t >= 2 && e2.charCodeAt(t) === Ht && e2.charCodeAt(t + 1) === Ht;
}
function zr(e2, t) {
  if (t = t || 0, e2.length - t >= 3 && e2.charCodeAt(t) === Ht && e2.charCodeAt(t + 1) !== Ht) {
    let r = e2.indexOf("-", t + 2);
    if (r !== -1) return e2.substring(t, r + 1);
  }
  return "";
}
function _l(e2) {
  if (Ut.has(e2)) return Ut.get(e2);
  let t = e2.toLowerCase(), r = Ut.get(t);
  if (r === void 0) {
    let n = Yt(t, 0), i = n ? "" : zr(t, 0);
    r = Object.freeze({ basename: t.substr(i.length), name: t, prefix: i, vendor: i, custom: n });
  }
  return Ut.set(e2, r), r;
}
function Wl(e2) {
  if (Qe.has(e2)) return Qe.get(e2);
  let t = e2, r = e2[0];
  r === "/" ? r = e2[1] === "/" ? "//" : "/" : r !== "_" && r !== "*" && r !== "$" && r !== "#" && r !== "+" && r !== "&" && (r = "");
  let n = Yt(t, r.length);
  if (!n && (t = t.toLowerCase(), Qe.has(t))) {
    let c = Qe.get(t);
    return Qe.set(e2, c), c;
  }
  let i = n ? "" : zr(t, r.length), o = t.substr(0, r.length + i.length), a = Object.freeze({ basename: t.substr(o.length), name: t.substr(r.length), hack: r, vendor: i, prefix: o, custom: n });
  return Qe.set(e2, a), a;
}
var Xe = ["initial", "inherit", "unset", "revert", "revert-layer"];
var gt = 43;
var de = 45;
var Pr = 110;
var $e = true;
var jl = false;
function Nr(e2, t) {
  return e2 !== null && e2.type === 9 && e2.value.charCodeAt(0) === t;
}
function ft(e2, t, r) {
  for (; e2 !== null && (e2.type === 13 || e2.type === 25); ) e2 = r(++t);
  return t;
}
function Ae(e2, t, r, n) {
  if (!e2) return 0;
  let i = e2.value.charCodeAt(t);
  if (i === gt || i === de) {
    if (r) return 0;
    t++;
  }
  for (; t < e2.value.length; t++) if (!O(e2.value.charCodeAt(t))) return 0;
  return n + 1;
}
function Ir(e2, t, r) {
  let n = false, i = ft(e2, t, r);
  if (e2 = r(i), e2 === null) return t;
  if (e2.type !== 10) if (Nr(e2, gt) || Nr(e2, de)) {
    if (n = true, i = ft(r(++i), i, r), e2 = r(i), e2 === null || e2.type !== 10) return 0;
  } else return t;
  if (!n) {
    let o = e2.value.charCodeAt(0);
    if (o !== gt && o !== de) return 0;
  }
  return Ae(e2, n ? 0 : 1, n, i);
}
function Dr(e2, t) {
  let r = 0;
  if (!e2) return 0;
  if (e2.type === 10) return Ae(e2, 0, jl, r);
  if (e2.type === 1 && e2.value.charCodeAt(0) === de) {
    if (!ye(e2.value, 1, Pr)) return 0;
    switch (e2.value.length) {
      case 2:
        return Ir(t(++r), r, t);
      case 3:
        return e2.value.charCodeAt(2) !== de ? 0 : (r = ft(t(++r), r, t), e2 = t(r), Ae(e2, 0, $e, r));
      default:
        return e2.value.charCodeAt(2) !== de ? 0 : Ae(e2, 3, $e, r);
    }
  } else if (e2.type === 1 || Nr(e2, gt) && t(r + 1).type === 1) {
    if (e2.type !== 1 && (e2 = t(++r)), e2 === null || !ye(e2.value, 0, Pr)) return 0;
    switch (e2.value.length) {
      case 1:
        return Ir(t(++r), r, t);
      case 2:
        return e2.value.charCodeAt(1) !== de ? 0 : (r = ft(t(++r), r, t), e2 = t(r), Ae(e2, 0, $e, r));
      default:
        return e2.value.charCodeAt(1) !== de ? 0 : Ae(e2, 2, $e, r);
    }
  } else if (e2.type === 12) {
    let n = e2.value.charCodeAt(0), i = n === gt || n === de ? 1 : 0, o = i;
    for (; o < e2.value.length && O(e2.value.charCodeAt(o)); o++) ;
    return o === i || !ye(e2.value, o, Pr) ? 0 : o + 1 === e2.value.length ? Ir(t(++r), r, t) : e2.value.charCodeAt(o + 1) !== de ? 0 : o + 2 === e2.value.length ? (r = ft(t(++r), r, t), e2 = t(r), Ae(e2, 0, $e, r)) : Ae(e2, o + 2, $e, r);
  }
  return 0;
}
var Ul = 43;
var la = 45;
var ca = 63;
var Hl = 117;
function Or(e2, t) {
  return e2 !== null && e2.type === 9 && e2.value.charCodeAt(0) === t;
}
function Gl(e2, t) {
  return e2.value.charCodeAt(0) === t;
}
function bt(e2, t, r) {
  let n = 0;
  for (let i = t; i < e2.value.length; i++) {
    let o = e2.value.charCodeAt(i);
    if (o === la && r && n !== 0) return bt(e2, t + n + 1, false), 6;
    if (!te(o) || ++n > 6) return 0;
  }
  return n;
}
function Kt(e2, t, r) {
  if (!e2) return 0;
  for (; Or(r(t), ca); ) {
    if (++e2 > 6) return 0;
    t++;
  }
  return t;
}
function Fr(e2, t) {
  let r = 0;
  if (e2 === null || e2.type !== 1 || !ye(e2.value, 0, Hl) || (e2 = t(++r), e2 === null)) return 0;
  if (Or(e2, Ul)) return e2 = t(++r), e2 === null ? 0 : e2.type === 1 ? Kt(bt(e2, 0, true), ++r, t) : Or(e2, ca) ? Kt(1, ++r, t) : 0;
  if (e2.type === 10) {
    let n = bt(e2, 1, true);
    return n === 0 ? 0 : (e2 = t(++r), e2 === null ? r : e2.type === 12 || e2.type === 10 ? !Gl(e2, la) || !bt(e2, 1, false) ? 0 : r + 1 : Kt(n, r, t));
  }
  return e2.type === 12 ? Kt(bt(e2, 1, true), ++r, t) : 0;
}
var Rr = ["calc(", "-moz-calc(", "-webkit-calc("];
var Mr = ["min(", "max(", "clamp("];
var Br = ["round(", "mod(", "rem("];
var Yl = ["sin(", "cos(", "tan("];
var Kl = ["asin(", "acos(", "atan(", "atan2("];
var Vl = ["pow(", "sqrt(", "log(", "exp(", "sign("];
var _r = ["hypot("];
var Wr = ["abs("];
var ua = [...Rr, ...Mr, ...Br, ...Yl, ...Vl, ..._r, ...Wr];
var Ql = [...Rr, ...Mr, ...Br, ..._r, ...Wr];
var ve = [...Rr, ...Mr, ...Br, ...Kl, ..._r, ...Wr];
var qr = /* @__PURE__ */ new Map([[2, 22], [21, 22], [19, 20], [23, 24]]);
function pe(e2, t) {
  return t < e2.length ? e2.charCodeAt(t) : 0;
}
function pa(e2, t) {
  return xe(e2, 0, e2.length, t);
}
function ha(e2, t) {
  for (let r = 0; r < t.length; r++) if (pa(e2, t[r])) return true;
  return false;
}
function ma(e2, t) {
  return t !== e2.length - 2 ? false : pe(e2, t) === 92 && O(pe(e2, t + 1));
}
function Vt(e2, t, r) {
  if (e2 && e2.type === "Range") {
    let n = Number(r !== void 0 && r !== t.length ? t.substr(0, r) : t);
    if (isNaN(n) || e2.min !== null && n < e2.min && typeof e2.min != "string" || e2.max !== null && n > e2.max && typeof e2.max != "string") return true;
  }
  return false;
}
function Xl(e2, t) {
  let r = 0, n = [], i = 0;
  e: do {
    switch (e2.type) {
      case 24:
      case 22:
      case 20:
        if (e2.type !== r) break e;
        if (r = n.pop(), n.length === 0) {
          i++;
          break e;
        }
        break;
      case 2:
      case 21:
      case 19:
      case 23:
        n.push(r), r = qr.get(e2.type);
        break;
    }
    i++;
  } while (e2 = t(i));
  return i;
}
function ae(e2, t) {
  return function(r, n, i) {
    return r === null ? 0 : r.type === 2 && ha(r.value, t) ? Xl(r, n) : e2(r, n, i);
  };
}
function I(e2) {
  return function(t) {
    return t === null || t.type !== e2 ? 0 : 1;
  };
}
function $l(e2) {
  if (e2 === null || e2.type !== 1) return 0;
  let t = e2.value.toLowerCase();
  return ha(t, Xe) || pa(t, "default") ? 0 : 1;
}
function da(e2) {
  return e2 === null || e2.type !== 1 || pe(e2.value, 0) !== 45 || pe(e2.value, 1) !== 45 ? 0 : 1;
}
function Zl(e2) {
  return !da(e2) || e2.value === "--" ? 0 : 1;
}
function Jl(e2) {
  if (e2 === null || e2.type !== 4) return 0;
  let t = e2.value.length;
  if (t !== 4 && t !== 5 && t !== 7 && t !== 9) return 0;
  for (let r = 1; r < t; r++) if (!te(pe(e2.value, r))) return 0;
  return 1;
}
function ec(e2) {
  return e2 === null || e2.type !== 4 || !qe(pe(e2.value, 1), pe(e2.value, 2), pe(e2.value, 3)) ? 0 : 1;
}
function tc(e2, t) {
  if (!e2) return 0;
  let r = 0, n = [], i = 0;
  e: do {
    switch (e2.type) {
      case 6:
      case 8:
        break e;
      case 24:
      case 22:
      case 20:
        if (e2.type !== r) break e;
        r = n.pop();
        break;
      case 17:
        if (r === 0) break e;
        break;
      case 9:
        if (r === 0 && e2.value === "!") break e;
        break;
      case 2:
      case 21:
      case 19:
      case 23:
        n.push(r), r = qr.get(e2.type);
        break;
    }
    i++;
  } while (e2 = t(i));
  return i;
}
function rc(e2, t) {
  if (!e2) return 0;
  let r = 0, n = [], i = 0;
  e: do {
    switch (e2.type) {
      case 6:
      case 8:
        break e;
      case 24:
      case 22:
      case 20:
        if (e2.type !== r) break e;
        r = n.pop();
        break;
      case 2:
      case 21:
      case 19:
      case 23:
        n.push(r), r = qr.get(e2.type);
        break;
    }
    i++;
  } while (e2 = t(i));
  return i;
}
function Se(e2) {
  return e2 && (e2 = new Set(e2)), function(t, r, n) {
    if (t === null || t.type !== 12) return 0;
    let i = Ie(t.value, 0);
    if (e2 !== null) {
      let o = t.value.indexOf("\\", i), a = o === -1 || !ma(t.value, o) ? t.value.substr(i) : t.value.substring(i, o);
      if (e2.has(a.toLowerCase()) === false) return 0;
    }
    return Vt(n, t.value, i) ? 0 : 1;
  };
}
function nc(e2, t, r) {
  return e2 === null || e2.type !== 11 || Vt(r, e2.value, e2.value.length - 1) ? 0 : 1;
}
function fa(e2) {
  return typeof e2 != "function" && (e2 = function() {
    return 0;
  }), function(t, r, n) {
    return t !== null && t.type === 10 && Number(t.value) === 0 ? 1 : e2(t, r, n);
  };
}
function ic(e2, t, r) {
  if (e2 === null) return 0;
  let n = Ie(e2.value, 0);
  return !(n === e2.value.length) && !ma(e2.value, n) || Vt(r, e2.value, n) ? 0 : 1;
}
function oc(e2, t, r) {
  if (e2 === null || e2.type !== 10) return 0;
  let n = pe(e2.value, 0) === 43 || pe(e2.value, 0) === 45 ? 1 : 0;
  for (; n < e2.value.length; n++) if (!O(pe(e2.value, n))) return 0;
  return Vt(r, e2.value, n) ? 0 : 1;
}
var ac = { "ident-token": I(1), "function-token": I(2), "at-keyword-token": I(3), "hash-token": I(4), "string-token": I(5), "bad-string-token": I(6), "url-token": I(7), "bad-url-token": I(8), "delim-token": I(9), "number-token": I(10), "percentage-token": I(11), "dimension-token": I(12), "whitespace-token": I(13), "CDO-token": I(14), "CDC-token": I(15), "colon-token": I(16), "semicolon-token": I(17), "comma-token": I(18), "[-token": I(19), "]-token": I(20), "(-token": I(21), ")-token": I(22), "{-token": I(23), "}-token": I(24) };
var sc = { string: I(5), ident: I(1), percentage: ae(nc, Ql), zero: fa(), number: ae(ic, ua), integer: ae(oc, ua), "custom-ident": $l, "dashed-ident": da, "custom-property-name": Zl, "hex-color": Jl, "id-selector": ec, "an-plus-b": Dr, urange: Fr, "declaration-value": tc, "any-value": rc };
var lc = ["length", "angle", "time", "frequency", "resolution", "flex", "decibel", "semitones"];
function cc(e2) {
  let { angle: t, decibel: r, frequency: n, flex: i, length: o, resolution: a, semitones: c, time: u } = e2 || {};
  return { dimension: ae(Se(null), ve), angle: ae(Se(t), ve), decibel: ae(Se(r), ve), frequency: ae(Se(n), ve), flex: ae(Se(i), ve), length: ae(fa(Se(o)), ve), resolution: ae(Se(a), ve), semitones: ae(Se(c), ve), time: ae(Se(u), ve) };
}
function uc(e2) {
  let t = /* @__PURE__ */ new Set();
  for (let r of lc) if (Array.isArray(e2[r])) for (let n of e2[r]) t.add(n.toLowerCase());
  return function(n) {
    return n === null ? 0 : n.type === 9 && n.value === "%" || n.type === 1 && t.has(n.value.toLowerCase()) ? 1 : 0;
  };
}
function ga(e2) {
  return { ...ac, ...sc, ...cc(e2), "attr-unit": uc(e2) };
}
var Qt = {};
f(Qt, { angle: () => hc, decibel: () => bc, flex: () => gc, frequency: () => dc, length: () => pc, resolution: () => fc, semitones: () => yc, time: () => mc });
var pc = ["cm", "mm", "q", "in", "pt", "pc", "px", "em", "rem", "ex", "rex", "cap", "rcap", "ch", "rch", "ic", "ric", "lh", "rlh", "vw", "svw", "lvw", "dvw", "vh", "svh", "lvh", "dvh", "vi", "svi", "lvi", "dvi", "vb", "svb", "lvb", "dvb", "vmin", "svmin", "lvmin", "dvmin", "vmax", "svmax", "lvmax", "dvmax", "cqw", "cqh", "cqi", "cqb", "cqmin", "cqmax"];
var hc = ["deg", "grad", "rad", "turn"];
var mc = ["s", "ms"];
var dc = ["hz", "khz"];
var fc = ["dpi", "dpcm", "dppx", "x"];
var gc = ["fr"];
var bc = ["db"];
var yc = ["st"];
var za = {};
f(za, { SyntaxError: () => Xt, generate: () => Fe, parse: () => Je, walk: () => rr });
function Xt(e2, t, r) {
  return Object.assign(De("SyntaxError", e2), { input: t, offset: r, rawMessage: e2, message: e2 + `
  ` + t + `
--` + new Array((r || t.length) + 1).join("-") + "^" });
}
var xc = 9;
var kc = 10;
var wc = 12;
var vc = 13;
var Sc = 32;
var ba = new Uint8Array(128).map((e2, t) => /[a-zA-Z0-9\-]/.test(String.fromCharCode(t)) ? 1 : 0);
var $t = class {
  constructor(t) {
    this.str = t, this.pos = 0;
  }
  charCodeAt(t) {
    return t < this.str.length ? this.str.charCodeAt(t) : 0;
  }
  charCode() {
    return this.charCodeAt(this.pos);
  }
  isNameCharCode(t = this.charCode()) {
    return t < 128 && ba[t] === 1;
  }
  nextCharCode() {
    return this.charCodeAt(this.pos + 1);
  }
  nextNonWsCode(t) {
    return this.charCodeAt(this.findWsEnd(t));
  }
  skipWs() {
    this.pos = this.findWsEnd(this.pos);
  }
  findWsEnd(t) {
    for (; t < this.str.length; t++) {
      let r = this.str.charCodeAt(t);
      if (r !== vc && r !== kc && r !== wc && r !== Sc && r !== xc) break;
    }
    return t;
  }
  substringToPos(t) {
    return this.str.substring(this.pos, this.pos = t);
  }
  eat(t) {
    this.charCode() !== t && this.error("Expect `" + String.fromCharCode(t) + "`"), this.pos++;
  }
  peek() {
    return this.pos < this.str.length ? this.str.charAt(this.pos++) : "";
  }
  error(t) {
    throw new Xt(t, this.str, this.pos);
  }
  scanSpaces() {
    return this.substringToPos(this.findWsEnd(this.pos));
  }
  scanWord() {
    let t = this.pos;
    for (; t < this.str.length; t++) {
      let r = this.str.charCodeAt(t);
      if (r >= 128 || ba[r] === 0) break;
    }
    return this.pos === t && this.error("Expect a keyword"), this.substringToPos(t);
  }
  scanNumber() {
    let t = this.pos;
    for (; t < this.str.length; t++) {
      let r = this.str.charCodeAt(t);
      if (r < 48 || r > 57) break;
    }
    return this.pos === t && this.error("Expect a number"), this.substringToPos(t);
  }
  scanString() {
    let t = this.str.indexOf("'", this.pos + 1);
    return t === -1 && (this.pos = this.str.length, this.error("Expect an apostrophe")), this.substringToPos(t + 1);
  }
};
var Cc = 9;
var Tc = 10;
var Lc = 12;
var Ac = 13;
var Ec = 32;
var Ca = 33;
var Gr = 35;
var ya = 38;
var Zt = 39;
var Ta = 40;
var La = 41;
var Aa = 42;
var Yr = 43;
var Kr = 44;
var xa = 45;
var Vr = 60;
var Ur = 62;
var Jt = 63;
var zc = 64;
var yt = 91;
var Ze = 93;
var er = 123;
var ka = 124;
var Hr = 125;
var wa = 8734;
var va = { " ": 1, "&&": 2, "||": 3, "|": 4 };
function Sa(e2) {
  let t = null, r = null;
  return e2.eat(er), e2.skipWs(), t = e2.scanNumber(e2), e2.skipWs(), e2.charCode() === Kr ? (e2.pos++, e2.skipWs(), e2.charCode() !== Hr && (r = e2.scanNumber(e2), e2.skipWs())) : r = t, e2.eat(Hr), { min: Number(t), max: r ? Number(r) : 0 };
}
function Pc(e2) {
  let t = null, r = false;
  switch (e2.charCode()) {
    case Aa:
      e2.pos++, t = { min: 0, max: 0 };
      break;
    case Yr:
      e2.pos++, t = { min: 1, max: 0 };
      break;
    case Jt:
      e2.pos++, t = { min: 0, max: 1 };
      break;
    case Gr:
      e2.pos++, r = true, e2.charCode() === er ? t = Sa(e2) : e2.charCode() === Jt ? (e2.pos++, t = { min: 0, max: 0 }) : t = { min: 1, max: 0 };
      break;
    case er:
      t = Sa(e2);
      break;
    default:
      return null;
  }
  return { type: "Multiplier", comma: r, min: t.min, max: t.max, term: null };
}
function Ce(e2, t) {
  let r = Pc(e2);
  return r !== null ? (r.term = t, e2.charCode() === Gr && e2.charCodeAt(e2.pos - 1) === Yr || e2.charCode() === Jt && e2.charCodeAt(e2.pos - 1) === Hr ? Ce(e2, r) : r) : t;
}
function jr(e2) {
  let t = e2.peek();
  return t === "" ? null : Ce(e2, { type: "Token", value: t });
}
function Ic(e2) {
  let t;
  return e2.eat(Vr), e2.eat(Zt), t = e2.scanWord(), e2.eat(Zt), e2.eat(Ur), Ce(e2, { type: "Property", name: t });
}
function Nc(e2) {
  let t = null, r = null, n = 1;
  return e2.eat(yt), e2.charCode() === xa && (e2.peek(), n = -1), n == -1 && e2.charCode() === wa ? e2.peek() : (t = n * Number(e2.scanNumber(e2)), e2.isNameCharCode() && (t += e2.scanWord())), e2.skipWs(), e2.eat(Kr), e2.skipWs(), e2.charCode() === wa ? e2.peek() : (n = 1, e2.charCode() === xa && (e2.peek(), n = -1), r = n * Number(e2.scanNumber(e2)), e2.isNameCharCode() && (r += e2.scanWord())), e2.eat(Ze), { type: "Range", min: t, max: r };
}
function Dc(e2) {
  let t, r = null;
  if (e2.eat(Vr), t = e2.scanWord(), t === "boolean-expr") {
    e2.eat(yt);
    let n = tr(e2, Ze);
    return e2.eat(Ze), e2.eat(Ur), Ce(e2, { type: "Boolean", term: n.terms.length === 1 ? n.terms[0] : n });
  }
  return e2.charCode() === Ta && e2.nextCharCode() === La && (e2.pos += 2, t += "()"), e2.charCodeAt(e2.findWsEnd(e2.pos)) === yt && (e2.skipWs(), r = Nc(e2)), e2.eat(Ur), Ce(e2, { type: "Type", name: t, opts: r });
}
function Oc(e2) {
  let t = e2.scanWord();
  return e2.charCode() === Ta ? (e2.pos++, { type: "Function", name: t }) : Ce(e2, { type: "Keyword", name: t });
}
function Fc(e2, t) {
  function r(i, o) {
    return { type: "Group", terms: i, combinator: o, disallowEmpty: false, explicit: false };
  }
  let n;
  for (t = Object.keys(t).sort((i, o) => va[i] - va[o]); t.length > 0; ) {
    n = t.shift();
    let i = 0, o = 0;
    for (; i < e2.length; i++) {
      let a = e2[i];
      a.type === "Combinator" && (a.value === n ? (o === -1 && (o = i - 1), e2.splice(i, 1), i--) : (o !== -1 && i - o > 1 && (e2.splice(o, i - o, r(e2.slice(o, i), n)), i = o + 1), o = -1));
    }
    o !== -1 && t.length && e2.splice(o, i - o, r(e2.slice(o, i), n));
  }
  return n;
}
function tr(e2, t = -1) {
  let r = /* @__PURE__ */ Object.create(null), n = [], i = null, o = e2.pos, a = false;
  for (; e2.charCode() !== t; ) {
    let c = a ? tr(e2, La) : Mc(e2);
    if (!c) break;
    if (c.type !== "Spaces") {
      if (a) {
        if (c.terms.length === 0) {
          a = false;
          continue;
        }
        if (c.combinator === " ") {
          for (; c.terms.length > 1; ) r[" "] = true, n.push({ type: "Combinator", value: " " }, c.terms.shift());
          c = c.terms[0];
        }
      }
      c.type === "Combinator" ? ((i === null || i.type === "Combinator") && (e2.pos = o, e2.error("Unexpected combinator")), r[c.value] = true) : i !== null && i.type !== "Combinator" && (r[" "] = true, n.push({ type: "Combinator", value: " " })), n.push(c), i = c, o = e2.pos, a = c.type === "Function";
    }
  }
  return i !== null && i.type === "Combinator" && (e2.pos -= o, e2.error("Unexpected combinator")), { type: "Group", terms: n, combinator: Fc(n, r) || " ", disallowEmpty: false, explicit: false };
}
function Rc(e2) {
  let t;
  return e2.eat(yt), t = tr(e2, Ze), e2.eat(Ze), t.explicit = true, e2.charCode() === Ca && (e2.pos++, t.disallowEmpty = true), t;
}
function Mc(e2) {
  let t = e2.charCode();
  switch (t) {
    case Ze:
      break;
    case yt:
      return Ce(e2, Rc(e2));
    case Vr:
      return e2.nextCharCode() === Zt ? Ic(e2) : Dc(e2);
    case ka:
      return { type: "Combinator", value: e2.substringToPos(e2.pos + (e2.nextCharCode() === ka ? 2 : 1)) };
    case ya:
      return e2.pos++, e2.eat(ya), { type: "Combinator", value: "&&" };
    case Kr:
      return e2.pos++, { type: "Comma" };
    case Zt:
      return Ce(e2, { type: "String", value: e2.scanString() });
    case Ec:
    case Cc:
    case Tc:
    case Ac:
    case Lc:
      return { type: "Spaces", value: e2.scanSpaces() };
    case zc:
      return t = e2.nextCharCode(), e2.isNameCharCode(t) ? (e2.pos++, { type: "AtKeyword", name: e2.scanWord() }) : jr(e2);
    case Aa:
    case Yr:
    case Jt:
    case Gr:
    case Ca:
      break;
    case er:
      if (t = e2.nextCharCode(), t < 48 || t > 57) return jr(e2);
      break;
    default:
      return e2.isNameCharCode(t) ? Oc(e2) : jr(e2);
  }
}
function Je(e2) {
  let t = new $t(e2), r = tr(t);
  return t.pos !== e2.length && t.error("Unexpected input"), r.terms.length === 1 && r.terms[0].type === "Group" ? r.terms[0] : r;
}
var xt = function() {
};
function Ea(e2) {
  return typeof e2 == "function" ? e2 : xt;
}
function rr(e2, t, r) {
  function n(a) {
    switch (i.call(r, a), a.type) {
      case "Group":
        a.terms.forEach(n);
        break;
      case "Multiplier":
      case "Boolean":
        n(a.term);
        break;
      case "Type":
      case "Property":
      case "Keyword":
      case "AtKeyword":
      case "Function":
      case "String":
      case "Token":
      case "Comma":
        break;
      default:
        throw new Error("Unknown type: " + a.type);
    }
    o.call(r, a);
  }
  let i = xt, o = xt;
  if (typeof t == "function" ? i = t : t && (i = Ea(t.enter), o = Ea(t.leave)), i === xt && o === xt) throw new Error("Neither `enter` nor `leave` walker handler is set or both aren't a function");
  n(e2, r);
}
var Bc = { decorator(e2) {
  let t = [], r = null;
  return { ...e2, node(n) {
    let i = r;
    r = n, e2.node.call(this, n), r = i;
  }, emit(n, i, o) {
    t.push({ type: i, value: n, node: o ? null : r });
  }, result() {
    return t;
  } };
} };
function _c(e2) {
  let t = [];
  return Le(e2, (r, n, i) => t.push({ type: r, value: e2.slice(n, i), node: null })), t;
}
function Pa(e2, t) {
  return typeof e2 == "string" ? _c(e2) : t.generate(e2, Bc);
}
var C = { type: "Match" };
var A = { type: "Mismatch" };
var nr = { type: "DisallowEmpty" };
var Wc = 40;
var qc = 41;
function X(e2, t, r) {
  return t === C && r === A || e2 === C && t === C && r === C ? e2 : (e2.type === "If" && e2.else === A && t === C && (t = e2.then, e2 = e2.match), { type: "If", match: e2, then: t, else: r });
}
function Na(e2) {
  return e2.length > 2 && e2.charCodeAt(e2.length - 2) === Wc && e2.charCodeAt(e2.length - 1) === qc;
}
function Ia(e2) {
  return e2.type === "Keyword" || e2.type === "AtKeyword" || e2.type === "Function" || e2.type === "Type" && Na(e2.name);
}
function Ee(e2, t = " ", r = false) {
  return { type: "Group", terms: e2, combinator: t, disallowEmpty: false, explicit: r };
}
function kt(e2, t, r = /* @__PURE__ */ new Set()) {
  if (!r.has(e2)) switch (r.add(e2), e2.type) {
    case "If":
      e2.match = kt(e2.match, t, r), e2.then = kt(e2.then, t, r), e2.else = kt(e2.else, t, r);
      break;
    case "Type":
      return t[e2.name] || e2;
  }
  return e2;
}
function Qr(e2, t, r) {
  switch (e2) {
    case " ": {
      let n = C;
      for (let i = t.length - 1; i >= 0; i--) {
        let o = t[i];
        n = X(o, n, A);
      }
      return n;
    }
    case "|": {
      let n = A, i = null;
      for (let o = t.length - 1; o >= 0; o--) {
        let a = t[o];
        if (Ia(a) && (i === null && o > 0 && Ia(t[o - 1]) && (i = /* @__PURE__ */ Object.create(null), n = X({ type: "Enum", map: i }, C, n)), i !== null)) {
          let c = (Na(a.name) ? a.name.slice(0, -1) : a.name).toLowerCase();
          if (!(c in i)) {
            i[c] = a;
            continue;
          }
        }
        i = null, n = X(a, C, n);
      }
      return n;
    }
    case "&&": {
      if (t.length > 5) return { type: "MatchOnce", terms: t, all: true };
      let n = A;
      for (let i = t.length - 1; i >= 0; i--) {
        let o = t[i], a;
        t.length > 1 ? a = Qr(e2, t.filter(function(c) {
          return c !== o;
        }), false) : a = C, n = X(o, a, n);
      }
      return n;
    }
    case "||": {
      if (t.length > 5) return { type: "MatchOnce", terms: t, all: false };
      let n = r ? C : A;
      for (let i = t.length - 1; i >= 0; i--) {
        let o = t[i], a;
        t.length > 1 ? a = Qr(e2, t.filter(function(c) {
          return c !== o;
        }), true) : a = C, n = X(o, a, n);
      }
      return n;
    }
  }
}
function jc(e2) {
  let t = C, r = et(e2.term);
  if (e2.max === 0) r = X(r, nr, A), t = X(r, null, A), t.then = X(C, C, t), e2.comma && (t.then.else = X({ type: "Comma", syntax: e2 }, t, A));
  else for (let n = e2.min || 1; n <= e2.max; n++) e2.comma && t !== C && (t = X({ type: "Comma", syntax: e2 }, t, A)), t = X(r, X(C, C, t), A);
  if (e2.min === 0) t = X(C, C, t);
  else for (let n = 0; n < e2.min - 1; n++) e2.comma && t !== C && (t = X({ type: "Comma", syntax: e2 }, t, A)), t = X(r, t, A);
  return t;
}
function et(e2) {
  if (typeof e2 == "function") return { type: "Generic", fn: e2 };
  switch (e2.type) {
    case "Group": {
      let t = Qr(e2.combinator, e2.terms.map(et), false);
      return e2.disallowEmpty && (t = X(t, nr, A)), t;
    }
    case "Multiplier":
      return jc(e2);
    case "Boolean": {
      let t = et(e2.term), r = et(Ee([Ee([{ type: "Keyword", name: "not" }, { type: "Type", name: "!boolean-group" }]), Ee([{ type: "Type", name: "!boolean-group" }, Ee([{ type: "Multiplier", comma: false, min: 0, max: 0, term: Ee([{ type: "Keyword", name: "and" }, { type: "Type", name: "!boolean-group" }]) }, { type: "Multiplier", comma: false, min: 0, max: 0, term: Ee([{ type: "Keyword", name: "or" }, { type: "Type", name: "!boolean-group" }]) }], "|")])], "|")), n = et(Ee([{ type: "Type", name: "!term" }, Ee([{ type: "Token", value: "(" }, { type: "Type", name: "!self" }, { type: "Token", value: ")" }]), { type: "Type", name: "general-enclosed" }], "|"));
      return kt(n, { "!term": t, "!self": r }), kt(r, { "!boolean-group": n }), r;
    }
    case "Type":
    case "Property":
      return { type: e2.type, name: e2.name, syntax: e2 };
    case "Keyword":
      return { type: e2.type, name: e2.name.toLowerCase(), syntax: e2 };
    case "AtKeyword":
      return { type: e2.type, name: "@" + e2.name.toLowerCase(), syntax: e2 };
    case "Function":
      return { type: e2.type, name: e2.name.toLowerCase() + "(", syntax: e2 };
    case "String":
      return e2.value.length === 3 ? { type: "Token", value: e2.value.charAt(1), syntax: e2 } : { type: e2.type, value: e2.value.substr(1, e2.value.length - 2).replace(/\\'/g, "'"), syntax: e2 };
    case "Token":
      return { type: e2.type, value: e2.value, syntax: e2 };
    case "Comma":
      return { type: e2.type, syntax: e2 };
    default:
      throw new Error("Unknown node type:", e2.type);
  }
}
function wt(e2, t) {
  return typeof e2 == "string" && (e2 = Je(e2)), { type: "MatchGraph", match: et(e2), syntax: t || null, source: e2 };
}
var { hasOwnProperty: Da } = Object.prototype;
var Uc = 0;
var Hc = 1;
var $r = 2;
var Ba = 3;
var Oa = "Match";
var Gc = "Mismatch";
var Yc = "Maximum iteration number exceeded (please fill an issue on https://github.com/csstree/csstree/issues)";
var Fa = 15e3;
var Kc = 0;
function Vc(e2) {
  let t = null, r = null, n = e2;
  for (; n !== null; ) r = n.prev, n.prev = t, t = n, n = r;
  return t;
}
function Xr(e2, t) {
  if (e2.length !== t.length) return false;
  for (let r = 0; r < e2.length; r++) {
    let n = t.charCodeAt(r), i = e2.charCodeAt(r);
    if (i >= 65 && i <= 90 && (i = i | 32), i !== n) return false;
  }
  return true;
}
function Qc(e2) {
  return e2.type !== 9 ? false : e2.value !== "?";
}
function Ra(e2) {
  return e2 === null ? true : e2.type === 18 || e2.type === 2 || e2.type === 21 || e2.type === 19 || e2.type === 23 || Qc(e2);
}
function Ma(e2) {
  return e2 === null ? true : e2.type === 22 || e2.type === 20 || e2.type === 24 || e2.type === 9 && e2.value === "/";
}
function Xc(e2, t, r) {
  function n() {
    do
      E++, v = E < e2.length ? e2[E] : null;
    while (v !== null && (v.type === 13 || v.type === 25));
  }
  function i(re) {
    let ge = E + re;
    return ge < e2.length ? e2[ge] : null;
  }
  function o(re, ge) {
    return { nextState: re, matchStack: z, syntaxStack: p, thenStack: h, tokenIndex: E, prev: ge };
  }
  function a(re) {
    h = { nextState: re, matchStack: z, syntaxStack: p, prev: h };
  }
  function c(re) {
    d = o(re, d);
  }
  function u() {
    z = { type: Hc, syntax: t.syntax, token: v, prev: z }, n(), b = null, E > U && (U = E);
  }
  function s() {
    p = { syntax: t.syntax, opts: t.syntax.opts || p !== null && p.opts || null, prev: p }, z = { type: $r, syntax: t.syntax, token: z.token, prev: z };
  }
  function l() {
    z.type === $r ? z = z.prev : z = { type: Ba, syntax: p.syntax, token: z.token, prev: z }, p = p.prev;
  }
  let p = null, h = null, d = null, b = null, j = 0, J = null, v = null, E = -1, U = 0, z = { type: Uc, syntax: null, token: null, prev: null };
  for (n(); J === null && ++j < Fa; ) switch (t.type) {
    case "Match":
      if (h === null) {
        if (v !== null && (E !== e2.length - 1 || v.value !== "\\0" && v.value !== "\\9")) {
          t = A;
          break;
        }
        J = Oa;
        break;
      }
      if (t = h.nextState, t === nr) if (h.matchStack === z) {
        t = A;
        break;
      } else t = C;
      for (; h.syntaxStack !== p; ) l();
      h = h.prev;
      break;
    case "Mismatch":
      if (b !== null && b !== false) (d === null || E > d.tokenIndex) && (d = b, b = false);
      else if (d === null) {
        J = Gc;
        break;
      }
      t = d.nextState, h = d.thenStack, p = d.syntaxStack, z = d.matchStack, E = d.tokenIndex, v = E < e2.length ? e2[E] : null, d = d.prev;
      break;
    case "MatchGraph":
      t = t.match;
      break;
    case "If":
      t.else !== A && c(t.else), t.then !== C && a(t.then), t = t.match;
      break;
    case "MatchOnce":
      t = { type: "MatchOnceBuffer", syntax: t, index: 0, mask: 0 };
      break;
    case "MatchOnceBuffer": {
      let q = t.syntax.terms;
      if (t.index === q.length) {
        if (t.mask === 0 || t.syntax.all) {
          t = A;
          break;
        }
        t = C;
        break;
      }
      if (t.mask === (1 << q.length) - 1) {
        t = C;
        break;
      }
      for (; t.index < q.length; t.index++) {
        let H = 1 << t.index;
        if ((t.mask & H) === 0) {
          c(t), a({ type: "AddMatchOnce", syntax: t.syntax, mask: t.mask | H }), t = q[t.index++];
          break;
        }
      }
      break;
    }
    case "AddMatchOnce":
      t = { type: "MatchOnceBuffer", syntax: t.syntax, index: 0, mask: t.mask };
      break;
    case "Enum":
      if (v !== null) {
        let q = v.value.toLowerCase();
        if (q.indexOf("\\") !== -1 && (q = q.replace(/\\[09].*$/, "")), Da.call(t.map, q)) {
          t = t.map[q];
          break;
        }
      }
      t = A;
      break;
    case "Generic": {
      let q = p !== null ? p.opts : null, H = E + Math.floor(t.fn(v, i, q));
      if (!isNaN(H) && H > E) {
        for (; E < H; ) u();
        t = C;
      } else t = A;
      break;
    }
    case "Type":
    case "Property": {
      let q = t.type === "Type" ? "types" : "properties", H = Da.call(r, q) ? r[q][t.name] : null;
      if (!H || !H.match) throw new Error("Bad syntax reference: " + (t.type === "Type" ? "<" + t.name + ">" : "<'" + t.name + "'>"));
      if (b !== false && v !== null && t.type === "Type" && (t.name === "custom-ident" && v.type === 1 || t.name === "length" && v.value === "0")) {
        b === null && (b = o(t, d)), t = A;
        break;
      }
      s(), t = H.matchRef || H.match;
      break;
    }
    case "Keyword": {
      let q = t.name;
      if (v !== null) {
        let H = v.value;
        if (H.indexOf("\\") !== -1 && (H = H.replace(/\\[09].*$/, "")), Xr(H, q)) {
          u(), t = C;
          break;
        }
      }
      t = A;
      break;
    }
    case "AtKeyword":
    case "Function":
      if (v !== null && Xr(v.value, t.name)) {
        u(), t = C;
        break;
      }
      t = A;
      break;
    case "Token":
      if (v !== null && v.value === t.value) {
        u(), t = C;
        break;
      }
      t = A;
      break;
    case "Comma":
      v !== null && v.type === 18 ? Ra(z.token) ? t = A : (u(), t = Ma(v) ? A : C) : t = Ra(z.token) || Ma(v) ? C : A;
      break;
    case "String":
      let re = "", ge = E;
      for (; ge < e2.length && re.length < t.value.length; ge++) re += e2[ge].value;
      if (Xr(re, t.value)) {
        for (; E < ge; ) u();
        t = C;
      } else t = A;
      break;
    default:
      throw new Error("Unknown node type: " + t.type);
  }
  switch (Kc += j, J) {
    case null:
      console.warn("[csstree-match] BREAK after " + Fa + " iterations"), J = Yc, z = null;
      break;
    case Oa:
      for (; p !== null; ) l();
      break;
    default:
      z = null;
  }
  return { tokens: e2, reason: J, iterations: j, match: z, longestMatch: U };
}
function Zr(e2, t, r) {
  let n = Xc(e2, t, r || {});
  if (n.match === null) return n;
  let i = n.match, o = n.match = { syntax: t.syntax || null, match: [] }, a = [o];
  for (i = Vc(i).prev; i !== null; ) {
    switch (i.type) {
      case $r:
        o.match.push(o = { syntax: i.syntax, match: [] }), a.push(o);
        break;
      case Ba:
        a.pop(), o = a[a.length - 1];
        break;
      default:
        o.match.push({ syntax: i.syntax || null, token: i.token.value, node: i.token.node });
    }
    i = i.prev;
  }
  return n;
}
var en = {};
f(en, { getTrace: () => _a, isKeyword: () => Jc, isProperty: () => Zc, isType: () => $c });
function _a(e2) {
  function t(i) {
    return i === null ? false : i.type === "Type" || i.type === "Property" || i.type === "Keyword";
  }
  function r(i) {
    if (Array.isArray(i.match)) {
      for (let o = 0; o < i.match.length; o++) if (r(i.match[o])) return t(i.syntax) && n.unshift(i.syntax), true;
    } else if (i.node === e2) return n = t(i.syntax) ? [i.syntax] : [], true;
    return false;
  }
  let n = null;
  return this.matched !== null && r(this.matched), n;
}
function $c(e2, t) {
  return Jr(this, e2, (r) => r.type === "Type" && r.name === t);
}
function Zc(e2, t) {
  return Jr(this, e2, (r) => r.type === "Property" && r.name === t);
}
function Jc(e2) {
  return Jr(this, e2, (t) => t.type === "Keyword");
}
function Jr(e2, t, r) {
  let n = _a.call(e2, t);
  return n === null ? false : n.some(r);
}
function Wa(e2) {
  return "node" in e2 ? e2.node : Wa(e2.match[0]);
}
function qa(e2) {
  return "node" in e2 ? e2.node : qa(e2.match[e2.match.length - 1]);
}
function tn(e2, t, r, n, i) {
  function o(c) {
    if (c.syntax !== null && c.syntax.type === n && c.syntax.name === i) {
      let u = Wa(c), s = qa(c);
      e2.syntax.walk(t, function(l, p, h) {
        if (l === u) {
          let d = new Q();
          do {
            if (d.appendData(p.data), p.data === s) break;
            p = p.next;
          } while (p !== null);
          a.push({ parent: h, nodes: d });
        }
      });
    }
    Array.isArray(c.match) && c.match.forEach(o);
  }
  let a = [];
  return r.matched !== null && o(r.matched), a;
}
var { hasOwnProperty: vt } = Object.prototype;
function rn(e2) {
  return typeof e2 == "number" && isFinite(e2) && Math.floor(e2) === e2 && e2 >= 0;
}
function ja(e2) {
  return !!e2 && rn(e2.offset) && rn(e2.line) && rn(e2.column);
}
function eu(e2, t) {
  return function(n, i) {
    if (!n || n.constructor !== Object) return i(n, "Type of node should be an Object");
    for (let o in n) {
      let a = true;
      if (vt.call(n, o) !== false) {
        if (o === "type") n.type !== e2 && i(n, "Wrong node type `" + n.type + "`, expected `" + e2 + "`");
        else if (o === "loc") {
          if (n.loc === null) continue;
          if (n.loc && n.loc.constructor === Object) if (typeof n.loc.source != "string") o += ".source";
          else if (!ja(n.loc.start)) o += ".start";
          else if (!ja(n.loc.end)) o += ".end";
          else continue;
          a = false;
        } else if (t.hasOwnProperty(o)) {
          a = false;
          for (let c = 0; !a && c < t[o].length; c++) {
            let u = t[o][c];
            switch (u) {
              case String:
                a = typeof n[o] == "string";
                break;
              case Boolean:
                a = typeof n[o] == "boolean";
                break;
              case null:
                a = n[o] === null;
                break;
              default:
                typeof u == "string" ? a = n[o] && n[o].type === u : Array.isArray(u) && (a = n[o] instanceof Q);
            }
          }
        } else i(n, "Unknown field `" + o + "` for " + e2 + " node type");
        a || i(n, "Bad value for `" + e2 + "." + o + "`");
      }
    }
    for (let o in t) vt.call(t, o) && vt.call(n, o) === false && i(n, "Field `" + e2 + "." + o + "` is missed");
  };
}
function Ua(e2, t) {
  let r = [];
  for (let n = 0; n < e2.length; n++) {
    let i = e2[n];
    if (i === String || i === Boolean) r.push(i.name.toLowerCase());
    else if (i === null) r.push("null");
    else if (typeof i == "string") r.push(i);
    else if (Array.isArray(i)) r.push("List<" + (Ua(i, t) || "any") + ">");
    else throw new Error("Wrong value `" + i + "` in `" + t + "` structure definition");
  }
  return r.join(" | ");
}
function tu(e2, t) {
  let r = t.structure, n = { type: String, loc: true }, i = { type: '"' + e2 + '"' };
  for (let o in r) {
    if (vt.call(r, o) === false) continue;
    let a = n[o] = Array.isArray(r[o]) ? r[o].slice() : [r[o]];
    i[o] = Ua(a, e2 + "." + o);
  }
  return { docs: i, check: eu(e2, n) };
}
function Ha(e2) {
  let t = {};
  if (e2.node) {
    for (let r in e2.node) if (vt.call(e2.node, r)) {
      let n = e2.node[r];
      if (n.structure) t[r] = tu(r, n);
      else throw new Error("Missed `structure` field in `" + r + "` node type definition");
    }
  }
  return t;
}
function nn(e2, t, r) {
  let n = {};
  for (let i in e2) e2[i].syntax && (n[i] = r ? e2[i].syntax : Fe(e2[i].syntax, { compact: t }));
  return n;
}
function ru(e2, t, r) {
  let n = {};
  for (let [i, o] of Object.entries(e2)) n[i] = { prelude: o.prelude && (r ? o.prelude.syntax : Fe(o.prelude.syntax, { compact: t })), descriptors: o.descriptors && nn(o.descriptors, t, r) };
  return n;
}
function nu(e2) {
  for (let t = 0; t < e2.length; t++) if (e2[t].value.toLowerCase() === "var(") return true;
  return false;
}
function iu(e2) {
  let t = e2.terms[0];
  return e2.explicit === false && e2.terms.length === 1 && t.type === "Multiplier" && t.comma === true;
}
function he(e2, t, r) {
  return { matched: e2, iterations: r, error: t, ...en };
}
function tt(e2, t, r, n) {
  let i = Pa(r, e2.syntax), o;
  return nu(i) ? he(null, new Error("Matching for a tree with var() is not supported")) : (n && (o = Zr(i, e2.cssWideKeywordsSyntax, e2)), (!n || !o.match) && (o = Zr(i, t.match, e2), !o.match) ? he(null, new sa(o.reason, t.syntax, r, o), o.iterations) : he(o.match, null, o.iterations));
}
var rt = class {
  constructor(t, r, n) {
    if (this.cssWideKeywords = Xe, this.syntax = r, this.generic = false, this.units = { ...Qt }, this.atrules = /* @__PURE__ */ Object.create(null), this.properties = /* @__PURE__ */ Object.create(null), this.types = /* @__PURE__ */ Object.create(null), this.structure = n || Ha(t), t) {
      if (t.cssWideKeywords && (this.cssWideKeywords = t.cssWideKeywords), t.units) for (let i of Object.keys(Qt)) Array.isArray(t.units[i]) && (this.units[i] = t.units[i]);
      if (t.types) for (let [i, o] of Object.entries(t.types)) this.addType_(i, o);
      if (t.generic) {
        this.generic = true;
        for (let [i, o] of Object.entries(ga(this.units))) this.addType_(i, o);
      }
      if (t.atrules) for (let [i, o] of Object.entries(t.atrules)) this.addAtrule_(i, o);
      if (t.properties) for (let [i, o] of Object.entries(t.properties)) this.addProperty_(i, o);
    }
    this.cssWideKeywordsSyntax = wt(this.cssWideKeywords.join(" |  "));
  }
  checkStructure(t) {
    function r(o, a) {
      i.push({ node: o, message: a });
    }
    let n = this.structure, i = [];
    return this.syntax.walk(t, function(o) {
      n.hasOwnProperty(o.type) ? n[o.type].check(o, r) : r(o, "Unknown node type `" + o.type + "`");
    }), i.length ? i : false;
  }
  createDescriptor(t, r, n, i = null) {
    let o = { type: r, name: n }, a = { type: r, name: n, parent: i, serializable: typeof t == "string" || t && typeof t.type == "string", syntax: null, match: null, matchRef: null };
    return typeof t == "function" ? a.match = wt(t, o) : (typeof t == "string" ? Object.defineProperty(a, "syntax", { get() {
      return Object.defineProperty(a, "syntax", { value: Je(t) }), a.syntax;
    } }) : a.syntax = t, Object.defineProperty(a, "match", { get() {
      return Object.defineProperty(a, "match", { value: wt(a.syntax, o) }), a.match;
    } }), r === "Property" && Object.defineProperty(a, "matchRef", { get() {
      let c = a.syntax, u = iu(c) ? wt({ ...c, terms: [c.terms[0].term] }, o) : null;
      return Object.defineProperty(a, "matchRef", { value: u }), u;
    } })), a;
  }
  addAtrule_(t, r) {
    r && (this.atrules[t] = { type: "Atrule", name: t, prelude: r.prelude ? this.createDescriptor(r.prelude, "AtrulePrelude", t) : null, descriptors: r.descriptors ? Object.keys(r.descriptors).reduce((n, i) => (n[i] = this.createDescriptor(r.descriptors[i], "AtruleDescriptor", i, t), n), /* @__PURE__ */ Object.create(null)) : null });
  }
  addProperty_(t, r) {
    r && (this.properties[t] = this.createDescriptor(r, "Property", t));
  }
  addType_(t, r) {
    r && (this.types[t] = this.createDescriptor(r, "Type", t));
  }
  checkAtruleName(t) {
    if (!this.getAtrule(t)) return new Ve("Unknown at-rule", "@" + t);
  }
  checkAtrulePrelude(t, r) {
    let n = this.checkAtruleName(t);
    if (n) return n;
    let i = this.getAtrule(t);
    if (!i.prelude && r) return new SyntaxError("At-rule `@" + t + "` should not contain a prelude");
    if (i.prelude && !r && !tt(this, i.prelude, "", false).matched) return new SyntaxError("At-rule `@" + t + "` should contain a prelude");
  }
  checkAtruleDescriptorName(t, r) {
    let n = this.checkAtruleName(t);
    if (n) return n;
    let i = this.getAtrule(t), o = Gt(r);
    if (!i.descriptors) return new SyntaxError("At-rule `@" + t + "` has no known descriptors");
    if (!i.descriptors[o.name] && !i.descriptors[o.basename]) return new Ve("Unknown at-rule descriptor", r);
  }
  checkPropertyName(t) {
    if (!this.getProperty(t)) return new Ve("Unknown property", t);
  }
  matchAtrulePrelude(t, r) {
    let n = this.checkAtrulePrelude(t, r);
    if (n) return he(null, n);
    let i = this.getAtrule(t);
    return i.prelude ? tt(this, i.prelude, r || "", false) : he(null, null);
  }
  matchAtruleDescriptor(t, r, n) {
    let i = this.checkAtruleDescriptorName(t, r);
    if (i) return he(null, i);
    let o = this.getAtrule(t), a = Gt(r);
    return tt(this, o.descriptors[a.name] || o.descriptors[a.basename], n, false);
  }
  matchDeclaration(t) {
    return t.type !== "Declaration" ? he(null, new Error("Not a Declaration node")) : this.matchProperty(t.property, t.value);
  }
  matchProperty(t, r) {
    if (Er(t).custom) return he(null, new Error("Lexer matching doesn't applicable for custom properties"));
    let n = this.checkPropertyName(t);
    return n ? he(null, n) : tt(this, this.getProperty(t), r, true);
  }
  matchType(t, r) {
    let n = this.getType(t);
    return n ? tt(this, n, r, false) : he(null, new Ve("Unknown type", t));
  }
  match(t, r) {
    return typeof t != "string" && (!t || !t.type) ? he(null, new Ve("Bad syntax")) : ((typeof t == "string" || !t.match) && (t = this.createDescriptor(t, "Type", "anonymous")), tt(this, t, r, false));
  }
  findValueFragments(t, r, n, i) {
    return tn(this, r, this.matchProperty(t, r), n, i);
  }
  findDeclarationValueFragments(t, r, n) {
    return tn(this, t.value, this.matchDeclaration(t), r, n);
  }
  findAllFragments(t, r, n) {
    let i = [];
    return this.syntax.walk(t, { visit: "Declaration", enter: (o) => {
      i.push.apply(i, this.findDeclarationValueFragments(o, r, n));
    } }), i;
  }
  getAtrule(t, r = true) {
    let n = Gt(t);
    return (n.vendor && r ? this.atrules[n.name] || this.atrules[n.basename] : this.atrules[n.name]) || null;
  }
  getAtrulePrelude(t, r = true) {
    let n = this.getAtrule(t, r);
    return n && n.prelude || null;
  }
  getAtruleDescriptor(t, r) {
    return this.atrules.hasOwnProperty(t) && this.atrules.declarators && this.atrules[t].declarators[r] || null;
  }
  getProperty(t, r = true) {
    let n = Er(t);
    return (n.vendor && r ? this.properties[n.name] || this.properties[n.basename] : this.properties[n.name]) || null;
  }
  getType(t) {
    return hasOwnProperty.call(this.types, t) ? this.types[t] : null;
  }
  validate() {
    function t(u, s) {
      return s ? `<${u}>` : `<'${u}'>`;
    }
    function r(u, s, l, p) {
      if (l.has(s)) return l.get(s);
      l.set(s, false), p.syntax !== null && rr(p.syntax, function(h) {
        if (h.type !== "Type" && h.type !== "Property") return;
        let d = h.type === "Type" ? u.types : u.properties, b = h.type === "Type" ? i : o;
        hasOwnProperty.call(d, h.name) ? r(u, h.name, b, d[h.name]) && (n.push(`${t(s, l === i)} used broken syntax definition ${t(h.name, h.type === "Type")}`), l.set(s, true)) : (n.push(`${t(s, l === i)} used missed syntax definition ${t(h.name, h.type === "Type")}`), l.set(s, true));
      }, this);
    }
    let n = [], i = /* @__PURE__ */ new Map(), o = /* @__PURE__ */ new Map();
    for (let u in this.types) r(this, u, i, this.types[u]);
    for (let u in this.properties) r(this, u, o, this.properties[u]);
    let a = [...i.keys()].filter((u) => i.get(u)), c = [...o.keys()].filter((u) => o.get(u));
    return a.length || c.length ? { errors: n, types: a, properties: c } : null;
  }
  dump(t, r) {
    return { generic: this.generic, cssWideKeywords: this.cssWideKeywords, units: this.units, types: nn(this.types, !r, t), properties: nn(this.properties, !r, t), atrules: ru(this.atrules, !r, t) };
  }
  toString() {
    return JSON.stringify(this.dump());
  }
};
function on(e2, t) {
  return typeof t == "string" && /^\s*\|/.test(t) ? typeof e2 == "string" ? e2 + t : t.replace(/^\s*\|\s*/, "") : t || null;
}
function ou(e2, t) {
  let r = /* @__PURE__ */ Object.create(null);
  for (let n of Object.keys(e2)) t.includes(n) && (r[n] = e2[n]);
  return r;
}
function an(e2, t, r) {
  let n = { ...e2 };
  for (let [i, o] of Object.entries(t)) n[i] = { ...n[i], ...r ? ou(o, r) : o };
  return n;
}
function St(e2, t) {
  let r = { ...e2 };
  for (let [n, i] of Object.entries(t)) switch (n) {
    case "generic":
      r[n] = !!i;
      break;
    case "cssWideKeywords":
      r[n] = e2[n] ? [...e2[n], ...i] : i || [];
      break;
    case "units":
      r[n] = { ...e2[n] };
      for (let [o, a] of Object.entries(i)) r[n][o] = Array.isArray(a) ? a : [];
      break;
    case "atrules":
      r[n] = { ...e2[n] };
      for (let [o, a] of Object.entries(i)) {
        let c = r[n][o] || {}, u = r[n][o] = { prelude: c.prelude || null, descriptors: { ...c.descriptors } };
        if (a) {
          u.prelude = a.prelude ? on(u.prelude, a.prelude) : u.prelude || null;
          for (let [s, l] of Object.entries(a.descriptors || {})) u.descriptors[s] = l ? on(u.descriptors[s], l) : null;
          Object.keys(u.descriptors).length || (u.descriptors = null);
        }
      }
      break;
    case "types":
    case "properties":
      r[n] = { ...e2[n] };
      for (let [o, a] of Object.entries(i)) r[n][o] = on(r[n][o], a);
      break;
    case "parseContext":
      r[n] = { ...e2[n], ...i };
      break;
    case "scope":
    case "features":
      r[n] = an(e2[n], i);
      break;
    case "atrule":
    case "pseudo":
      r[n] = an(e2[n], i, ["parse"]);
      break;
    case "node":
      r[n] = an(e2[n], i, ["name", "structure", "parse", "generate", "walkContext"]);
      break;
  }
  return r;
}
function Ga(e2) {
  let t = Eo(e2), r = oa(e2), n = Jo(e2), { fromPlainObject: i, toPlainObject: o } = ea(r), a = { lexer: null, createLexer: (c) => new rt(c, a, a.lexer.structure), tokenize: Le, parse: t, generate: n, walk: r, find: r.find, findLast: r.findLast, findAll: r.findAll, fromPlainObject: i, toPlainObject: o, fork(c) {
    let u = St({}, e2);
    return Ga(typeof c == "function" ? c(u) : St(u, c));
  } };
  return a.lexer = new rt({ generic: e2.generic, cssWideKeywords: e2.cssWideKeywords, units: e2.units, types: e2.types, atrules: e2.atrules, properties: e2.properties, node: e2.node }, a), a;
}
var sn = (e2) => Ga(St({}, e2));
var Ya = { generic: true, cssWideKeywords: ["initial", "inherit", "unset", "revert", "revert-layer"], units: { angle: ["deg", "grad", "rad", "turn"], decibel: ["db"], flex: ["fr"], frequency: ["hz", "khz"], length: ["cm", "mm", "q", "in", "pt", "pc", "px", "em", "rem", "ex", "rex", "cap", "rcap", "ch", "rch", "ic", "ric", "lh", "rlh", "vw", "svw", "lvw", "dvw", "vh", "svh", "lvh", "dvh", "vi", "svi", "lvi", "dvi", "vb", "svb", "lvb", "dvb", "vmin", "svmin", "lvmin", "dvmin", "vmax", "svmax", "lvmax", "dvmax", "cqw", "cqh", "cqi", "cqb", "cqmin", "cqmax"], resolution: ["dpi", "dpcm", "dppx", "x"], semitones: ["st"], time: ["s", "ms"] }, types: { "abs()": "abs( <calc-sum> )", "absolute-size": "xx-small|x-small|small|medium|large|x-large|xx-large|xxx-large", "acos()": "acos( <calc-sum> )", "alpha-value": "<number>|<percentage>", "an+b": "odd|even|<integer>|<n-dimension>|'+'? \u2020 n|-n|<ndashdigit-dimension>|'+'? \u2020 <ndashdigit-ident>|<dashndashdigit-ident>|<n-dimension> <signed-integer>|'+'? \u2020 n <signed-integer>|-n <signed-integer>|<ndash-dimension> <signless-integer>|'+'? \u2020 n- <signless-integer>|-n- <signless-integer>|<n-dimension> ['+'|'-'] <signless-integer>|'+'? \u2020 n ['+'|'-'] <signless-integer>|-n ['+'|'-'] <signless-integer>", "anchor()": "anchor( <anchor-name>?&&<anchor-side> , <length-percentage>? )", "anchor-name": "<dashed-ident>", "anchor-side": "inside|outside|top|left|right|bottom|start|end|self-start|self-end|<percentage>|center", "anchor-size": "width|height|block|inline|self-block|self-inline", "anchor-size()": "anchor-size( [<anchor-name>||<anchor-size>]? , <length-percentage>? )", "angle-percentage": "<angle>|<percentage>", "angular-color-hint": "<angle-percentage>|<zero>", "angular-color-stop": "<color> <color-stop-angle>?", "angular-color-stop-list": "<angular-color-stop> , [<angular-color-hint>? , <angular-color-stop>]#?", "animateable-feature": "scroll-position|contents|<custom-ident>", "animation-action": "none|play|play-once|play-forwards|play-backwards|pause|reset|replay", "asin()": "asin( <calc-sum> )", "atan()": "atan( <calc-sum> )", "atan2()": "atan2( <calc-sum> , <calc-sum> )", attachment: "scroll|fixed|local", "attr()": "attr( <attr-name> <attr-type>? , <declaration-value>? )", "attr-matcher": "['~'|'|'|'^'|'$'|'*']? '='", "attr-modifier": "i|s", "attr-type": "type( <syntax> )|raw-string|number|<attr-unit>", "attribute-selector": "'[' <wq-name> ']'|'[' <wq-name> <attr-matcher> [<string-token>|<ident-token>] <attr-modifier>? ']'", "auto-repeat": "repeat( [auto-fill|auto-fit] , [<line-names>? <fixed-size>]+ <line-names>? )", "auto-track-list": "[<line-names>? [<fixed-size>|<fixed-repeat>]]* <line-names>? <auto-repeat> [<line-names>? [<fixed-size>|<fixed-repeat>]]* <line-names>?", axis: "block|inline|x|y", "baseline-position": "[first|last]? baseline", "basic-shape": "<inset()>|<xywh()>|<rect()>|<circle()>|<ellipse()>|<polygon()>|<path()>", "basic-shape-rect": "<inset()>|<rect()>|<xywh()>", "bg-clip": "<visual-box>|border-area|text", "bg-image": "<image>|none", "bg-layer": "<bg-image>||<bg-position> [/ <bg-size>]?||<repeat-style>||<attachment>||<visual-box>||<visual-box>", "bg-position": "[[left|center|right|top|bottom|<length-percentage>]|[left|center|right|<length-percentage>] [top|center|bottom|<length-percentage>]|[center|[left|right] <length-percentage>?]&&[center|[top|bottom] <length-percentage>?]]", "bg-size": "[<length-percentage [0,\u221E]>|auto]{1,2}|cover|contain", "blend-mode": "normal|multiply|screen|overlay|darken|lighten|color-dodge|color-burn|hard-light|soft-light|difference|exclusion|hue|saturation|color|luminosity", "blur()": "blur( <length>? )", "brightness()": "brightness( [<number>|<percentage>]? )", "calc()": "calc( <calc-sum> )", "calc-constant": "e|pi|infinity|-infinity|NaN", "calc-product": "<calc-value> ['*' <calc-value>|'/' <number>]*", "calc-size()": "calc-size( <calc-size-basis> , <calc-sum> )", "calc-size-basis": "<intrinsic-size-keyword>|<calc-size()>|any|<calc-sum>", "calc-sum": "<calc-product> [['+'|'-'] <calc-product>]*", "calc-value": "<number>|<dimension>|<percentage>|<calc-constant>|( <calc-sum> )", "cf-final-image": "<image>|<color>", "cf-mixing-image": "<percentage>?&&<image>", "circle()": "circle( <radial-size>? [at <position>]? )", "clamp()": "clamp( <calc-sum>#{3} )", "class-selector": "'.' <ident-token>", "clip-source": "<url>", color: "<color-base>|currentColor|<system-color>|<device-cmyk()>|<light-dark()>|<-non-standard-color>", "color()": "color( <colorspace-params> [/ [<alpha-value>|none]]? )", "color-base": "<hex-color>|<color-function>|<named-color>|<color-mix()>|transparent", "color-function": "<rgb()>|<rgba()>|<hsl()>|<hsla()>|<hwb()>|<lab()>|<lch()>|<oklab()>|<oklch()>|<color()>", "color-interpolation-method": "in [<rectangular-color-space>|<polar-color-space> <hue-interpolation-method>?|<custom-color-space>]", "color-mix()": "color-mix( <color-interpolation-method> , [<color>&&<percentage [0,100]>?]#{2} )", "color-stop": "<color-stop-length>|<color-stop-angle>", "color-stop-angle": "[<angle-percentage>|<zero>]{1,2}", "color-stop-length": "<length-percentage>{1,2}", "color-stop-list": "<linear-color-stop> , [<linear-color-hint>? , <linear-color-stop>]#?", "colorspace-params": "[<predefined-rgb-params>|<xyz-params>]", combinator: "'>'|'+'|'~'|['|' '|']", "common-lig-values": "[common-ligatures|no-common-ligatures]", "compat-auto": "searchfield|textarea|checkbox|radio|menulist|listbox|meter|progress-bar|button", "compat-special": "textfield|menulist-button", "complex-selector": "<complex-selector-unit> [<combinator>? <complex-selector-unit>]*", "complex-selector-list": "<complex-selector>#", "composite-style": "clear|copy|source-over|source-in|source-out|source-atop|destination-over|destination-in|destination-out|destination-atop|xor", "compositing-operator": "add|subtract|intersect|exclude", "compound-selector": "[<type-selector>? <subclass-selector>*]!", "compound-selector-list": "<compound-selector>#", "conic-gradient()": "conic-gradient( [<conic-gradient-syntax>] )", "conic-gradient-syntax": "[[[from [<angle>|<zero>]]? [at <position>]?]||<color-interpolation-method>]? , <angular-color-stop-list>", "container-condition": "not <query-in-parens>|<query-in-parens> [[and <query-in-parens>]*|[or <query-in-parens>]*]", "container-name": "<custom-ident>", "container-query": "not <query-in-parens>|<query-in-parens> [[and <query-in-parens>]*|[or <query-in-parens>]*]", "content-distribution": "space-between|space-around|space-evenly|stretch", "content-list": "[<string>|contents|<image>|<counter>|<quote>|<target>|<leader()>|<attr()>]+", "content-position": "center|start|end|flex-start|flex-end", "content-replacement": "<image>", "contextual-alt-values": "[contextual|no-contextual]", "contrast()": "contrast( [<number>|<percentage>]? )", "coord-box": "content-box|padding-box|border-box|fill-box|stroke-box|view-box", "corner-shape-value": "round|scoop|bevel|notch|square|squircle|<superellipse()>", "cos()": "cos( <calc-sum> )", counter: "<counter()>|<counters()>", "counter()": "counter( <counter-name> , <counter-style>? )", "counter-name": "<custom-ident>", "counter-style": "<counter-style-name>|symbols( )", "counter-style-name": "<custom-ident>", "counters()": "counters( <counter-name> , <string> , <counter-style>? )", "cross-fade()": "cross-fade( <cf-mixing-image> , <cf-final-image>? )", "cubic-bezier()": "cubic-bezier( [<number [0,1]> , <number>]#{2} )", "cubic-bezier-easing-function": "ease|ease-in|ease-out|ease-in-out|cubic-bezier( <number [0,1]> , <number> , <number [0,1]> , <number> )", "cursor-predefined": "auto|default|none|context-menu|help|pointer|progress|wait|cell|crosshair|text|vertical-text|alias|copy|move|no-drop|not-allowed|e-resize|n-resize|ne-resize|nw-resize|s-resize|se-resize|sw-resize|w-resize|ew-resize|ns-resize|nesw-resize|nwse-resize|col-resize|row-resize|all-scroll|zoom-in|zoom-out|grab|grabbing", "custom-color-space": "<dashed-ident>", "custom-params": "<dashed-ident> [<number>|<percentage>|none]+", dasharray: "[[<length-percentage>|<number>]+]#", "dashndashdigit-ident": "<ident-token>", "deprecated-system-color": "ActiveBorder|ActiveCaption|AppWorkspace|Background|ButtonHighlight|ButtonShadow|CaptionText|InactiveBorder|InactiveCaption|InactiveCaptionText|InfoBackground|InfoText|Menu|MenuText|Scrollbar|ThreeDDarkShadow|ThreeDFace|ThreeDHighlight|ThreeDLightShadow|ThreeDShadow|Window|WindowFrame|WindowText", "discretionary-lig-values": "[discretionary-ligatures|no-discretionary-ligatures]", "display-box": "contents|none", "display-inside": "flow|flow-root|table|flex|grid|ruby", "display-internal": "table-row-group|table-header-group|table-footer-group|table-row|table-cell|table-column-group|table-column|table-caption|ruby-base|ruby-text|ruby-base-container|ruby-text-container", "display-legacy": "inline-block|inline-list-item|inline-table|inline-flex|inline-grid", "display-listitem": "<display-outside>?&&[flow|flow-root]?&&list-item", "display-outside": "block|inline|run-in", "drop-shadow()": "drop-shadow( [<color>?&&<length>{2,3}] )", "dynamic-range-limit-mix()": "dynamic-range-limit-mix( [<'dynamic-range-limit'>&&<percentage [0,100]>]#{2,} )", "easing-function": "<linear-easing-function>|<cubic-bezier-easing-function>|<step-easing-function>", "east-asian-variant-values": "[jis78|jis83|jis90|jis04|simplified|traditional]", "east-asian-width-values": "[full-width|proportional-width]", "element()": "element( <custom-ident> , [first|start|last|first-except]? )|element( <id-selector> )", "ellipse()": "ellipse( <radial-size>? [at <position>]? )", "env()": "env( <custom-ident> , <declaration-value>? )", "exp()": "exp( <calc-sum> )", "explicit-track-list": "[<line-names>? <track-size>]+ <line-names>?", "family-name": "<string>|<custom-ident>+", "feature-tag-value": "<string> [<integer>|on|off]?", "feature-type": "@stylistic|@historical-forms|@styleset|@character-variant|@swash|@ornaments|@annotation", "feature-value-block": "<feature-type> '{' <feature-value-declaration-list> '}'", "feature-value-block-list": "<feature-value-block>+", "feature-value-declaration": "<custom-ident> : <integer>+ ;", "feature-value-declaration-list": "<feature-value-declaration>", "feature-value-name": "<custom-ident>", "filter-function": "<blur()>|<brightness()>|<contrast()>|<drop-shadow()>|<grayscale()>|<hue-rotate()>|<invert()>|<opacity()>|<saturate()>|<sepia()>", "filter-value-list": "[<filter-function>|<url>]+", "final-bg-layer": "<bg-image>||<bg-position> [/ <bg-size>]?||<repeat-style>||<attachment>||<visual-box>||<visual-box>||<'background-color'>", "fit-content()": "fit-content( <length-percentage [0,\u221E]> )", "fixed-breadth": "<length-percentage>", "fixed-repeat": "repeat( [<integer [1,\u221E]>] , [<line-names>? <fixed-size>]+ <line-names>? )", "fixed-size": "<fixed-breadth>|minmax( <fixed-breadth> , <track-breadth> )|minmax( <inflexible-breadth> , <fixed-breadth> )", "font-stretch-absolute": "normal|ultra-condensed|extra-condensed|condensed|semi-condensed|semi-expanded|expanded|extra-expanded|ultra-expanded|<percentage>", "font-variant-css2": "normal|small-caps", "font-weight-absolute": "normal|bold|<number [1,1000]>", "font-width-css3": "normal|ultra-condensed|extra-condensed|condensed|semi-condensed|semi-expanded|expanded|extra-expanded|ultra-expanded", "form-control-identifier": "select", "frequency-percentage": "<frequency>|<percentage>", "generic-complete": "serif|sans-serif|system-ui|cursive|fantasy|math|monospace", "general-enclosed": "[<function-token> <any-value>? )]|[( <any-value>? )]", "generic-family": "<generic-script-specific>|<generic-complete>|<generic-incomplete>|<-non-standard-generic-family>", "generic-incomplete": "ui-serif|ui-sans-serif|ui-monospace|ui-rounded", "geometry-box": "<shape-box>|fill-box|stroke-box|view-box", gradient: "<linear-gradient()>|<repeating-linear-gradient()>|<radial-gradient()>|<repeating-radial-gradient()>|<conic-gradient()>|<repeating-conic-gradient()>|<-legacy-gradient>", "grayscale()": "grayscale( [<number>|<percentage>]? )", "grid-line": "auto|<custom-ident>|[<integer>&&<custom-ident>?]|[span&&[<integer>||<custom-ident>]]", "historical-lig-values": "[historical-ligatures|no-historical-ligatures]", "hsl()": "hsl( <hue> , <percentage> , <percentage> , <alpha-value>? )|hsl( [<hue>|none] [<percentage>|<number>|none] [<percentage>|<number>|none] [/ [<alpha-value>|none]]? )", "hsla()": "hsla( <hue> , <percentage> , <percentage> , <alpha-value>? )|hsla( [<hue>|none] [<percentage>|<number>|none] [<percentage>|<number>|none] [/ [<alpha-value>|none]]? )", hue: "<number>|<angle>", "hue-interpolation-method": "[shorter|longer|increasing|decreasing] hue", "hue-rotate()": "hue-rotate( [<angle>|<zero>]? )", "hwb()": "hwb( [<hue>|none] [<percentage>|<number>|none] [<percentage>|<number>|none] [/ [<alpha-value>|none]]? )", "hypot()": "hypot( <calc-sum># )", image: "<url>|<image()>|<image-set()>|<element()>|<paint()>|<cross-fade()>|<gradient>", "image()": "image( <image-tags>? [<image-src>? , <color>?]! )", "image-set()": "image-set( <image-set-option># )", "image-set-option": "[<image>|<string>] [<resolution>||type( <string> )]", "image-src": "<url>|<string>", "image-tags": "ltr|rtl", "inflexible-breadth": "<length-percentage>|min-content|max-content|auto", "inset()": "inset( <length-percentage>{1,4} [round <'border-radius'>]? )", "invert()": "invert( [<number>|<percentage>]? )", "keyframe-block": "<keyframe-selector># { <declaration-list> }", "keyframe-selector": "from|to|<percentage [0,100]>|<timeline-range-name> <percentage>", "keyframes-name": "<custom-ident>|<string>", "lab()": "lab( [<percentage>|<number>|none] [<percentage>|<number>|none] [<percentage>|<number>|none] [/ [<alpha-value>|none]]? )", "layer()": "layer( <layer-name> )", "layer-name": "<ident> ['.' <ident>]*", "lch()": "lch( [<percentage>|<number>|none] [<percentage>|<number>|none] [<hue>|none] [/ [<alpha-value>|none]]? )", "leader()": "leader( <leader-type> )", "leader-type": "dotted|solid|space|<string>", "length-percentage": "<length>|<percentage>", "light-dark()": "light-dark( <color> , <color> )", "line-name-list": "[<line-names>|<name-repeat>]+", "line-names": "'[' <custom-ident>* ']'", "line-style": "none|hidden|dotted|dashed|solid|double|groove|ridge|inset|outset", "line-width": "<length>|thin|medium|thick", "linear()": "linear( [<number>&&<percentage>{0,2}]# )", "linear-color-hint": "<length-percentage>", "linear-color-stop": "<color> <color-stop-length>?", "linear-easing-function": "linear|<linear()>", "linear-gradient()": "linear-gradient( [<linear-gradient-syntax>] )", "linear-gradient-syntax": "[[<angle>|<zero>|to <side-or-corner>]||<color-interpolation-method>]? , <color-stop-list>", "log()": "log( <calc-sum> , <calc-sum>? )", "mask-layer": "<mask-reference>||<position> [/ <bg-size>]?||<repeat-style>||<geometry-box>||[<geometry-box>|no-clip]||<compositing-operator>||<masking-mode>", "mask-position": "[<length-percentage>|left|center|right] [<length-percentage>|top|center|bottom]?", "mask-reference": "none|<image>|<mask-source>", "mask-source": "<url>", "masking-mode": "alpha|luminance|match-source", "matrix()": "matrix( <number>#{6} )", "matrix3d()": "matrix3d( <number>#{16} )", "max()": "max( <calc-sum># )", "media-and": "<media-in-parens> [and <media-in-parens>]+", "media-condition": "<media-not>|<media-and>|<media-or>|<media-in-parens>", "media-condition-without-or": "<media-not>|<media-and>|<media-in-parens>", "media-feature": "( [<mf-plain>|<mf-boolean>|<mf-range>] )", "media-in-parens": "( <media-condition> )|<media-feature>|<general-enclosed>", "media-not": "not <media-in-parens>", "media-or": "<media-in-parens> [or <media-in-parens>]+", "media-query": "<media-condition>|[not|only]? <media-type> [and <media-condition-without-or>]?", "media-query-list": "<media-query>#", "media-type": "<ident>", "mf-boolean": "<mf-name>", "mf-name": "<ident>", "mf-plain": "<mf-name> : <mf-value>", "mf-range": "<mf-name> ['<'|'>']? '='? <mf-value>|<mf-value> ['<'|'>']? '='? <mf-name>|<mf-value> '<' '='? <mf-name> '<' '='? <mf-value>|<mf-value> '>' '='? <mf-name> '>' '='? <mf-value>", "mf-value": "<number>|<dimension>|<ident>|<ratio>", "min()": "min( <calc-sum># )", "minmax()": "minmax( [<length-percentage>|min-content|max-content|auto] , [<length-percentage>|<flex>|min-content|max-content|auto] )", "mod()": "mod( <calc-sum> , <calc-sum> )", "n-dimension": "<dimension-token>", "name-repeat": "repeat( [<integer [1,\u221E]>|auto-fill] , <line-names>+ )", "named-color": "aliceblue|antiquewhite|aqua|aquamarine|azure|beige|bisque|black|blanchedalmond|blue|blueviolet|brown|burlywood|cadetblue|chartreuse|chocolate|coral|cornflowerblue|cornsilk|crimson|cyan|darkblue|darkcyan|darkgoldenrod|darkgray|darkgreen|darkgrey|darkkhaki|darkmagenta|darkolivegreen|darkorange|darkorchid|darkred|darksalmon|darkseagreen|darkslateblue|darkslategray|darkslategrey|darkturquoise|darkviolet|deeppink|deepskyblue|dimgray|dimgrey|dodgerblue|firebrick|floralwhite|forestgreen|fuchsia|gainsboro|ghostwhite|gold|goldenrod|gray|green|greenyellow|grey|honeydew|hotpink|indianred|indigo|ivory|khaki|lavender|lavenderblush|lawngreen|lemonchiffon|lightblue|lightcoral|lightcyan|lightgoldenrodyellow|lightgray|lightgreen|lightgrey|lightpink|lightsalmon|lightseagreen|lightskyblue|lightslategray|lightslategrey|lightsteelblue|lightyellow|lime|limegreen|linen|magenta|maroon|mediumaquamarine|mediumblue|mediumorchid|mediumpurple|mediumseagreen|mediumslateblue|mediumspringgreen|mediumturquoise|mediumvioletred|midnightblue|mintcream|mistyrose|moccasin|navajowhite|navy|oldlace|olive|olivedrab|orange|orangered|orchid|palegoldenrod|palegreen|paleturquoise|palevioletred|papayawhip|peachpuff|peru|pink|plum|powderblue|purple|rebeccapurple|red|rosybrown|royalblue|saddlebrown|salmon|sandybrown|seagreen|seashell|sienna|silver|skyblue|slateblue|slategray|slategrey|snow|springgreen|steelblue|tan|teal|thistle|tomato|turquoise|violet|wheat|white|whitesmoke|yellow|yellowgreen", "namespace-prefix": "<ident>", "ndash-dimension": "<dimension-token>", "ndashdigit-dimension": "<dimension-token>", "ndashdigit-ident": "<ident-token>", "ns-prefix": "[<ident-token>|'*']? '|'", "number-percentage": "<number>|<percentage>", "numeric-figure-values": "[lining-nums|oldstyle-nums]", "numeric-fraction-values": "[diagonal-fractions|stacked-fractions]", "numeric-spacing-values": "[proportional-nums|tabular-nums]", "offset-path": "<ray()>|<url>|<basic-shape>", "oklab()": "oklab( [<percentage>|<number>|none] [<percentage>|<number>|none] [<percentage>|<number>|none] [/ [<alpha-value>|none]]? )", "oklch()": "oklch( [<percentage>|<number>|none] [<percentage>|<number>|none] [<hue>|none] [/ [<alpha-value>|none]]? )", "opacity()": "opacity( [<number>|<percentage>]? )", "opacity-value": "<number>|<percentage>", "outline-line-style": "none|dotted|dashed|solid|double|groove|ridge|inset|outset", "outline-radius": "<length>|<percentage>", "overflow-position": "unsafe|safe", "page-body": "<declaration>? [; <page-body>]?|<page-margin-box> <page-body>", "page-margin-box": "<page-margin-box-type> '{' <declaration-list> '}'", "page-margin-box-type": "@top-left-corner|@top-left|@top-center|@top-right|@top-right-corner|@bottom-left-corner|@bottom-left|@bottom-center|@bottom-right|@bottom-right-corner|@left-top|@left-middle|@left-bottom|@right-top|@right-middle|@right-bottom", "page-selector": "<pseudo-page>+|<ident> <pseudo-page>*", "page-selector-list": "[<page-selector>#]?", "page-size": "A5|A4|A3|B5|B4|JIS-B5|JIS-B4|letter|legal|ledger", paint: "none|<color>|<url> [none|<color>]?|context-fill|context-stroke", "paint()": "paint( <ident> , <declaration-value>? )", "paint-box": "<visual-box>|fill-box|stroke-box", "palette-identifier": "<dashed-ident>", "palette-mix()": "palette-mix( <color-interpolation-method> , [[normal|light|dark|<palette-identifier>|<palette-mix()>]&&<percentage [0,100]>?]#{2} )", "path()": "path( <'fill-rule'>? , <string> )", "perspective()": "perspective( [<length [0,\u221E]>|none] )", "polar-color-space": "hsl|hwb|lch|oklch", "polygon()": "polygon( <'fill-rule'>? , [<length-percentage> <length-percentage>]# )", position: "[[left|center|right]||[top|center|bottom]|[left|center|right|<length-percentage>] [top|center|bottom|<length-percentage>]?|[[left|right] <length-percentage>]&&[[top|bottom] <length-percentage>]]", "position-area": "[[left|center|right|span-left|span-right|x-start|x-end|span-x-start|span-x-end|x-self-start|x-self-end|span-x-self-start|span-x-self-end|span-all]||[top|center|bottom|span-top|span-bottom|y-start|y-end|span-y-start|span-y-end|y-self-start|y-self-end|span-y-self-start|span-y-self-end|span-all]|[block-start|center|block-end|span-block-start|span-block-end|span-all]||[inline-start|center|inline-end|span-inline-start|span-inline-end|span-all]|[self-block-start|center|self-block-end|span-self-block-start|span-self-block-end|span-all]||[self-inline-start|center|self-inline-end|span-self-inline-start|span-self-inline-end|span-all]|[start|center|end|span-start|span-end|span-all]{1,2}|[self-start|center|self-end|span-self-start|span-self-end|span-all]{1,2}]", "pow()": "pow( <calc-sum> , <calc-sum> )", "predefined-rgb": "srgb|srgb-linear|display-p3|display-p3-linear|a98-rgb|prophoto-rgb|rec2020", "predefined-rgb-params": "<predefined-rgb> [<number>|<percentage>|none]{3}", "pseudo-class-selector": "':' <ident-token>|':' <function-token> <any-value> ')'", "pseudo-element-selector": "':' <pseudo-class-selector>|<legacy-pseudo-element-selector>", "pseudo-page": ": [left|right|first|blank]", "query-in-parens": "( <container-condition> )|( <size-feature> )|style( <style-query> )|<general-enclosed>", quote: "open-quote|close-quote|no-open-quote|no-close-quote", "radial-extent": "closest-corner|closest-side|farthest-corner|farthest-side", "radial-gradient()": "radial-gradient( [<radial-gradient-syntax>] )", "radial-gradient-syntax": "[[[<radial-shape>||<radial-size>]? [at <position>]?]||<color-interpolation-method>]? , <color-stop-list>", "radial-shape": "circle|ellipse", "radial-size": "<radial-extent>|<length [0,\u221E]>|<length-percentage [0,\u221E]>{2}", ratio: "<number [0,\u221E]> [/ <number [0,\u221E]>]?", "ray()": "ray( <angle>&&<ray-size>?&&contain?&&[at <position>]? )", "ray-size": "closest-side|closest-corner|farthest-side|farthest-corner|sides", "rect()": "rect( [<length-percentage>|auto]{4} [round <'border-radius'>]? )", "rectangular-color-space": "srgb|srgb-linear|display-p3|display-p3-linear|a98-rgb|prophoto-rgb|rec2020|lab|oklab|xyz|xyz-d50|xyz-d65", "relative-selector": "<combinator>? <complex-selector>", "relative-selector-list": "<relative-selector>#", "relative-size": "larger|smaller", "rem()": "rem( <calc-sum> , <calc-sum> )", "repeat-style": "repeat-x|repeat-y|[repeat|space|round|no-repeat]{1,2}", "repeating-conic-gradient()": "repeating-conic-gradient( [<conic-gradient-syntax>] )", "repeating-linear-gradient()": "repeating-linear-gradient( [<linear-gradient-syntax>] )", "repeating-radial-gradient()": "repeating-radial-gradient( [<radial-gradient-syntax>] )", "reversed-counter-name": "reversed( <counter-name> )", "rgb()": "rgb( <percentage>#{3} , <alpha-value>? )|rgb( <number>#{3} , <alpha-value>? )|rgb( [<number>|<percentage>|none]{3} [/ [<alpha-value>|none]]? )", "rgba()": "rgba( <percentage>#{3} , <alpha-value>? )|rgba( <number>#{3} , <alpha-value>? )|rgba( [<number>|<percentage>|none]{3} [/ [<alpha-value>|none]]? )", "rotate()": "rotate( [<angle>|<zero>] )", "rotate3d()": "rotate3d( <number> , <number> , <number> , [<angle>|<zero>] )", "rotateX()": "rotateX( [<angle>|<zero>] )", "rotateY()": "rotateY( [<angle>|<zero>] )", "rotateZ()": "rotateZ( [<angle>|<zero>] )", "round()": "round( <rounding-strategy>? , <calc-sum> , <calc-sum> )", "rounding-strategy": "nearest|up|down|to-zero", "saturate()": "saturate( [<number>|<percentage>]? )", "scale()": "scale( [<number>|<percentage>]#{1,2} )", "scale3d()": "scale3d( [<number>|<percentage>]#{3} )", "scaleX()": "scaleX( [<number>|<percentage>] )", "scaleY()": "scaleY( [<number>|<percentage>] )", "scaleZ()": "scaleZ( [<number>|<percentage>] )", "scope-end": "<forgiving-selector-list>", "scope-start": "<forgiving-selector-list>", "scroll()": "scroll( [<scroller>||<axis>]? )", scroller: "root|nearest|self", "scroll-state-feature": "<media-query-list>", "scroll-state-in-parens": "( <scroll-state-query> )|( <scroll-state-feature> )|<general-enclosed>", "scroll-state-query": "not <scroll-state-in-parens>|<scroll-state-in-parens> [[and <scroll-state-in-parens>]*|[or <scroll-state-in-parens>]*]|<scroll-state-feature>", "selector-list": "<complex-selector-list>", "self-position": "center|start|end|self-start|self-end|flex-start|flex-end", "sepia()": "sepia( [<number>|<percentage>]? )", shadow: "inset?&&<length>{2,4}&&<color>?", "shadow-t": "[<length>{2,3}&&<color>?]", shape: "rect( <top> , <right> , <bottom> , <left> )|rect( <top> <right> <bottom> <left> )", "shape-box": "<visual-box>|margin-box", "side-or-corner": "[left|right]||[top|bottom]", "sign()": "sign( <calc-sum> )", "signed-integer": "<number-token>", "signless-integer": "<number-token>", "sin()": "sin( <calc-sum> )", "single-animation": "<'animation-duration'>||<easing-function>||<'animation-delay'>||<single-animation-iteration-count>||<single-animation-direction>||<single-animation-fill-mode>||<single-animation-play-state>||[none|<keyframes-name>]||<single-animation-timeline>", "single-animation-composition": "replace|add|accumulate", "single-animation-direction": "normal|reverse|alternate|alternate-reverse", "single-animation-fill-mode": "none|forwards|backwards|both", "single-animation-iteration-count": "infinite|<number>", "single-animation-play-state": "running|paused", "single-animation-timeline": "auto|none|<dashed-ident>|<scroll()>|<view()>", "single-transition": "[none|<single-transition-property>]||<time>||<easing-function>||<time>||<transition-behavior-value>", "single-transition-property": "all|<custom-ident>", size: "closest-side|farthest-side|closest-corner|farthest-corner|<length>|<length-percentage>{2}", "size-feature": "<mf-plain>|<mf-boolean>|<mf-range>", "skew()": "skew( [<angle>|<zero>] , [<angle>|<zero>]? )", "skewX()": "skewX( [<angle>|<zero>] )", "skewY()": "skewY( [<angle>|<zero>] )", "sqrt()": "sqrt( <calc-sum> )", "step-position": "jump-start|jump-end|jump-none|jump-both|start|end", "step-easing-function": "step-start|step-end|<steps()>", "steps()": "steps( <integer> , <step-position>? )", "style-feature": "<declaration>", "style-in-parens": "( <style-condition> )|( <style-feature> )|<general-enclosed>", "style-query": "<style-condition>|<style-feature>", "subclass-selector": "<id-selector>|<class-selector>|<attribute-selector>|<pseudo-class-selector>", "superellipse()": "superellipse( [<number>|infinity|-infinity] )", "supports-condition": "not <supports-in-parens>|<supports-in-parens> [and <supports-in-parens>]*|<supports-in-parens> [or <supports-in-parens>]*", "supports-decl": "( <declaration> )", "supports-feature": "<supports-decl>|<supports-selector-fn>", "supports-in-parens": "( <supports-condition> )|<supports-feature>|<general-enclosed>", "supports-selector-fn": "selector( <complex-selector> )", symbol: "<string>|<image>|<custom-ident>", "symbols()": "symbols( <symbols-type>? [<string>|<image>]+ )", "symbols-type": "cyclic|numeric|alphabetic|symbolic|fixed", "system-color": "AccentColor|AccentColorText|ActiveText|ButtonBorder|ButtonFace|ButtonText|Canvas|CanvasText|Field|FieldText|GrayText|Highlight|HighlightText|LinkText|Mark|MarkText|SelectedItem|SelectedItemText|VisitedText", "system-family-name": "caption|icon|menu|message-box|small-caption|status-bar", "tan()": "tan( <calc-sum> )", target: "<target-counter()>|<target-counters()>|<target-text()>", "target-counter()": "target-counter( [<string>|<url>] , <custom-ident> , <counter-style>? )", "target-counters()": "target-counters( [<string>|<url>] , <custom-ident> , <string> , <counter-style>? )", "target-text()": "target-text( [<string>|<url>] , [content|before|after|first-letter]? )", "text-edge": "[text|cap|ex|ideographic|ideographic-ink] [text|alphabetic|ideographic|ideographic-ink]?", "time-percentage": "<time>|<percentage>", "timeline-range-name": "cover|contain|entry|exit|entry-crossing|exit-crossing", "track-breadth": "<length-percentage>|<flex>|min-content|max-content|auto", "track-list": "[<line-names>? [<track-size>|<track-repeat>]]+ <line-names>?", "track-repeat": "repeat( [<integer [1,\u221E]>] , [<line-names>? <track-size>]+ <line-names>? )", "track-size": "<track-breadth>|minmax( <inflexible-breadth> , <track-breadth> )|fit-content( <length-percentage> )", "transform-function": "<matrix()>|<translate()>|<translateX()>|<translateY()>|<scale()>|<scaleX()>|<scaleY()>|<rotate()>|<skew()>|<skewX()>|<skewY()>|<matrix3d()>|<translate3d()>|<translateZ()>|<scale3d()>|<scaleZ()>|<rotate3d()>|<rotateX()>|<rotateY()>|<rotateZ()>|<perspective()>", "transform-list": "<transform-function>+", "transition-behavior-value": "normal|allow-discrete", "translate()": "translate( <length-percentage> , <length-percentage>? )", "translate3d()": "translate3d( <length-percentage> , <length-percentage> , <length> )", "translateX()": "translateX( <length-percentage> )", "translateY()": "translateY( <length-percentage> )", "translateZ()": "translateZ( <length> )", "try-size": "most-width|most-height|most-block-size|most-inline-size", "try-tactic": "flip-block||flip-inline||flip-start", "type-or-unit": "string|color|url|integer|number|length|angle|time|frequency|cap|ch|em|ex|ic|lh|rlh|rem|vb|vi|vw|vh|vmin|vmax|mm|Q|cm|in|pt|pc|px|deg|grad|rad|turn|ms|s|Hz|kHz|%", "type-selector": "<wq-name>|<ns-prefix>? '*'", "var()": "var( <custom-property-name> , <declaration-value>? )", "view()": "view( [<axis>||<'view-timeline-inset'>]? )", "viewport-length": "auto|<length-percentage>", "visual-box": "content-box|padding-box|border-box", "wq-name": "<ns-prefix>? <ident-token>", "xywh()": "xywh( <length-percentage>{2} <length-percentage [0,\u221E]>{2} [round <'border-radius'>]? )", xyz: "xyz|xyz-d50|xyz-d65", "xyz-params": "<xyz-space> [<number>|<percentage>|none]{3}", "-legacy-gradient": "<-webkit-gradient()>|<-legacy-linear-gradient>|<-legacy-repeating-linear-gradient>|<-legacy-radial-gradient>|<-legacy-repeating-radial-gradient>", "-legacy-linear-gradient": "-moz-linear-gradient( <-legacy-linear-gradient-arguments> )|-webkit-linear-gradient( <-legacy-linear-gradient-arguments> )|-o-linear-gradient( <-legacy-linear-gradient-arguments> )", "-legacy-repeating-linear-gradient": "-moz-repeating-linear-gradient( <-legacy-linear-gradient-arguments> )|-webkit-repeating-linear-gradient( <-legacy-linear-gradient-arguments> )|-o-repeating-linear-gradient( <-legacy-linear-gradient-arguments> )", "-legacy-linear-gradient-arguments": "[<angle>|<side-or-corner>]? , <color-stop-list>", "-legacy-radial-gradient": "-moz-radial-gradient( <-legacy-radial-gradient-arguments> )|-webkit-radial-gradient( <-legacy-radial-gradient-arguments> )|-o-radial-gradient( <-legacy-radial-gradient-arguments> )", "-legacy-repeating-radial-gradient": "-moz-repeating-radial-gradient( <-legacy-radial-gradient-arguments> )|-webkit-repeating-radial-gradient( <-legacy-radial-gradient-arguments> )|-o-repeating-radial-gradient( <-legacy-radial-gradient-arguments> )", "-legacy-radial-gradient-arguments": "[<position> ,]? [[[<-legacy-radial-gradient-shape>||<-legacy-radial-gradient-size>]|[<length>|<percentage>]{2}] ,]? <color-stop-list>", "-legacy-radial-gradient-size": "closest-side|closest-corner|farthest-side|farthest-corner|contain|cover", "-legacy-radial-gradient-shape": "circle|ellipse", "-non-standard-font": "-apple-system-body|-apple-system-headline|-apple-system-subheadline|-apple-system-caption1|-apple-system-caption2|-apple-system-footnote|-apple-system-short-body|-apple-system-short-headline|-apple-system-short-subheadline|-apple-system-short-caption1|-apple-system-short-footnote|-apple-system-tall-body", "-non-standard-color": "-moz-ButtonDefault|-moz-ButtonHoverFace|-moz-ButtonHoverText|-moz-CellHighlight|-moz-CellHighlightText|-moz-Combobox|-moz-ComboboxText|-moz-Dialog|-moz-DialogText|-moz-dragtargetzone|-moz-EvenTreeRow|-moz-Field|-moz-FieldText|-moz-html-CellHighlight|-moz-html-CellHighlightText|-moz-mac-accentdarkestshadow|-moz-mac-accentdarkshadow|-moz-mac-accentface|-moz-mac-accentlightesthighlight|-moz-mac-accentlightshadow|-moz-mac-accentregularhighlight|-moz-mac-accentregularshadow|-moz-mac-chrome-active|-moz-mac-chrome-inactive|-moz-mac-focusring|-moz-mac-menuselect|-moz-mac-menushadow|-moz-mac-menutextselect|-moz-MenuHover|-moz-MenuHoverText|-moz-MenuBarText|-moz-MenuBarHoverText|-moz-nativehyperlinktext|-moz-OddTreeRow|-moz-win-communicationstext|-moz-win-mediatext|-moz-activehyperlinktext|-moz-default-background-color|-moz-default-color|-moz-hyperlinktext|-moz-visitedhyperlinktext|-webkit-activelink|-webkit-focus-ring-color|-webkit-link|-webkit-text", "-non-standard-image-rendering": "optimize-contrast|-moz-crisp-edges|-o-crisp-edges|-webkit-optimize-contrast", "-non-standard-overflow": "overlay|-moz-scrollbars-none|-moz-scrollbars-horizontal|-moz-scrollbars-vertical|-moz-hidden-unscrollable", "-non-standard-size": "intrinsic|min-intrinsic|-webkit-fill-available|-webkit-fit-content|-webkit-min-content|-webkit-max-content|-moz-available|-moz-fit-content|-moz-min-content|-moz-max-content", "-webkit-gradient()": "-webkit-gradient( <-webkit-gradient-type> , <-webkit-gradient-point> [, <-webkit-gradient-point>|, <-webkit-gradient-radius> , <-webkit-gradient-point>] [, <-webkit-gradient-radius>]? [, <-webkit-gradient-color-stop>]* )", "-webkit-gradient-color-stop": "from( <color> )|color-stop( [<number-zero-one>|<percentage>] , <color> )|to( <color> )", "-webkit-gradient-point": "[left|center|right|<length-percentage>] [top|center|bottom|<length-percentage>]", "-webkit-gradient-radius": "<length>|<percentage>", "-webkit-gradient-type": "linear|radial", "-webkit-mask-box-repeat": "repeat|stretch|round", "-ms-filter-function-list": "<-ms-filter-function>+", "-ms-filter-function": "<-ms-filter-function-progid>|<-ms-filter-function-legacy>", "-ms-filter-function-progid": "'progid:' [<ident-token> '.']* [<ident-token>|<function-token> <any-value>? )]", "-ms-filter-function-legacy": "<ident-token>|<function-token> <any-value>? )", age: "child|young|old", "attr-name": "<wq-name>", "attr-fallback": "<any-value>", autospace: "no-autospace|[ideograph-alpha||ideograph-numeric||punctuation]||[insert|replace]", bottom: "<length>|auto", "generic-voice": "[<age>? <gender> <integer>?]", gender: "male|female|neutral", "generic-script-specific": "generic( kai )|generic( fangsong )|generic( nastaliq )", "-non-standard-generic-family": "-apple-system|BlinkMacSystemFont", "intrinsic-size-keyword": "min-content|max-content|fit-content", left: "<length>|auto", "device-cmyk()": "<legacy-device-cmyk-syntax>|<modern-device-cmyk-syntax>", "legacy-device-cmyk-syntax": "device-cmyk( <number>#{4} )", "modern-device-cmyk-syntax": "device-cmyk( <cmyk-component>{4} [/ [<alpha-value>|none]]? )", "cmyk-component": "<number>|<percentage>|none", "color-space": "<rectangular-color-space>|<polar-color-space>|<custom-color-space>", right: "<length>|auto", "forgiving-selector-list": "<complex-real-selector-list>", "forgiving-relative-selector-list": "<relative-real-selector-list>", "complex-real-selector-list": "<complex-real-selector>#", "simple-selector-list": "<simple-selector>#", "relative-real-selector-list": "<relative-real-selector>#", "complex-selector-unit": "[<compound-selector>? <pseudo-compound-selector>*]!", "complex-real-selector": "<compound-selector> [<combinator>? <compound-selector>]*", "relative-real-selector": "<combinator>? <complex-real-selector>", "pseudo-compound-selector": "<pseudo-element-selector> <pseudo-class-selector>*", "simple-selector": "<type-selector>|<subclass-selector>", "legacy-pseudo-element-selector": "':' [before|after|first-line|first-letter]", "svg-length": "<percentage>|<length>|<number>", "svg-writing-mode": "lr-tb|rl-tb|tb-rl|lr|rl|tb", top: "<length>|auto", x: "<number>", y: "<number>", declaration: "<ident-token> : <declaration-value>? ['!' important]?", "declaration-list": "[<declaration>? ';']* <declaration>?", url: "url( <string> <url-modifier>* )|<url-token>", "url-modifier": "<ident>|<function-token> <any-value> )", "number-zero-one": "<number [0,1]>", "number-one-or-greater": "<number [1,\u221E]>", "xyz-space": "xyz|xyz-d50|xyz-d65", "style-condition": "not <style-in-parens>|<style-in-parens> [[and <style-in-parens>]*|[or <style-in-parens>]*]", "-non-standard-display": "-ms-inline-flexbox|-ms-grid|-ms-inline-grid|-webkit-flex|-webkit-inline-flex|-webkit-box|-webkit-inline-box|-moz-inline-stack|-moz-box|-moz-inline-box", "inset-area": "[[left|center|right|span-left|span-right|x-start|x-end|span-x-start|span-x-end|x-self-start|x-self-end|span-x-self-start|span-x-self-end|span-all]||[top|center|bottom|span-top|span-bottom|y-start|y-end|span-y-start|span-y-end|y-self-start|y-self-end|span-y-self-start|span-y-self-end|span-all]|[block-start|center|block-end|span-block-start|span-block-end|span-all]||[inline-start|center|inline-end|span-inline-start|span-inline-end|span-all]|[self-block-start|self-block-end|span-self-block-start|span-self-block-end|span-all]||[self-inline-start|self-inline-end|span-self-inline-start|span-self-inline-end|span-all]|[start|center|end|span-start|span-end|span-all]{1,2}|[self-start|center|self-end|span-self-start|span-self-end|span-all]{1,2}]", syntax: "'*'|<syntax-component> [<syntax-combinator> <syntax-component>]*|<syntax-string>", "syntax-component": "<syntax-single-component> <syntax-multiplier>?|'<' transform-list '>'", "syntax-single-component": "'<' <syntax-type-name> '>'|<ident>", "syntax-type-name": "angle|color|custom-ident|image|integer|length|length-percentage|number|percentage|resolution|string|time|url|transform-function", "syntax-combinator": "'|'", "syntax-multiplier": "'#'|'+'", "syntax-string": "<string>" }, properties: { "--*": "<declaration-value>", "-ms-accelerator": "false|true", "-ms-block-progression": "tb|rl|bt|lr", "-ms-content-zoom-chaining": "none|chained", "-ms-content-zoom-limit": "<'-ms-content-zoom-limit-min'> <'-ms-content-zoom-limit-max'>", "-ms-content-zoom-limit-max": "<percentage>", "-ms-content-zoom-limit-min": "<percentage>", "-ms-content-zoom-snap": "<'-ms-content-zoom-snap-type'>||<'-ms-content-zoom-snap-points'>", "-ms-content-zoom-snap-points": "snapInterval( <percentage> , <percentage> )|snapList( <percentage># )", "-ms-content-zoom-snap-type": "none|proximity|mandatory", "-ms-content-zooming": "none|zoom", "-ms-filter": "<string>", "-ms-flow-from": "[none|<custom-ident>]#", "-ms-flow-into": "[none|<custom-ident>]#", "-ms-grid-columns": "none|<track-list>|<auto-track-list>", "-ms-grid-rows": "none|<track-list>|<auto-track-list>", "-ms-high-contrast-adjust": "auto|none", "-ms-hyphenate-limit-chars": "auto|<integer>{1,3}", "-ms-hyphenate-limit-lines": "no-limit|<integer>", "-ms-hyphenate-limit-zone": "<percentage>|<length>", "-ms-ime-align": "auto|after", "-ms-overflow-style": "auto|none|scrollbar|-ms-autohiding-scrollbar", "-ms-scroll-chaining": "chained|none", "-ms-scroll-limit": "<'-ms-scroll-limit-x-min'> <'-ms-scroll-limit-y-min'> <'-ms-scroll-limit-x-max'> <'-ms-scroll-limit-y-max'>", "-ms-scroll-limit-x-max": "auto|<length>", "-ms-scroll-limit-x-min": "<length>", "-ms-scroll-limit-y-max": "auto|<length>", "-ms-scroll-limit-y-min": "<length>", "-ms-scroll-rails": "none|railed", "-ms-scroll-snap-points-x": "snapInterval( <length-percentage> , <length-percentage> )|snapList( <length-percentage># )", "-ms-scroll-snap-points-y": "snapInterval( <length-percentage> , <length-percentage> )|snapList( <length-percentage># )", "-ms-scroll-snap-type": "none|proximity|mandatory", "-ms-scroll-snap-x": "<'-ms-scroll-snap-type'> <'-ms-scroll-snap-points-x'>", "-ms-scroll-snap-y": "<'-ms-scroll-snap-type'> <'-ms-scroll-snap-points-y'>", "-ms-scroll-translation": "none|vertical-to-horizontal", "-ms-scrollbar-3dlight-color": "<color>", "-ms-scrollbar-arrow-color": "<color>", "-ms-scrollbar-base-color": "<color>", "-ms-scrollbar-darkshadow-color": "<color>", "-ms-scrollbar-face-color": "<color>", "-ms-scrollbar-highlight-color": "<color>", "-ms-scrollbar-shadow-color": "<color>", "-ms-scrollbar-track-color": "<color>", "-ms-text-autospace": "none|ideograph-alpha|ideograph-numeric|ideograph-parenthesis|ideograph-space", "-ms-touch-select": "grippers|none", "-ms-user-select": "none|element|text", "-ms-wrap-flow": "auto|both|start|end|maximum|clear", "-ms-wrap-margin": "<length>", "-ms-wrap-through": "wrap|none", "-moz-appearance": "none|button|button-arrow-down|button-arrow-next|button-arrow-previous|button-arrow-up|button-bevel|button-focus|caret|checkbox|checkbox-container|checkbox-label|checkmenuitem|dualbutton|groupbox|listbox|listitem|menuarrow|menubar|menucheckbox|menuimage|menuitem|menuitemtext|menulist|menulist-button|menulist-text|menulist-textfield|menupopup|menuradio|menuseparator|meterbar|meterchunk|progressbar|progressbar-vertical|progresschunk|progresschunk-vertical|radio|radio-container|radio-label|radiomenuitem|range|range-thumb|resizer|resizerpanel|scale-horizontal|scalethumbend|scalethumb-horizontal|scalethumbstart|scalethumbtick|scalethumb-vertical|scale-vertical|scrollbarbutton-down|scrollbarbutton-left|scrollbarbutton-right|scrollbarbutton-up|scrollbarthumb-horizontal|scrollbarthumb-vertical|scrollbartrack-horizontal|scrollbartrack-vertical|searchfield|separator|sheet|spinner|spinner-downbutton|spinner-textfield|spinner-upbutton|splitter|statusbar|statusbarpanel|tab|tabpanel|tabpanels|tab-scroll-arrow-back|tab-scroll-arrow-forward|textfield|textfield-multiline|toolbar|toolbarbutton|toolbarbutton-dropdown|toolbargripper|toolbox|tooltip|treeheader|treeheadercell|treeheadersortarrow|treeitem|treeline|treetwisty|treetwistyopen|treeview|-moz-mac-unified-toolbar|-moz-win-borderless-glass|-moz-win-browsertabbar-toolbox|-moz-win-communicationstext|-moz-win-communications-toolbox|-moz-win-exclude-glass|-moz-win-glass|-moz-win-mediatext|-moz-win-media-toolbox|-moz-window-button-box|-moz-window-button-box-maximized|-moz-window-button-close|-moz-window-button-maximize|-moz-window-button-minimize|-moz-window-button-restore|-moz-window-frame-bottom|-moz-window-frame-left|-moz-window-frame-right|-moz-window-titlebar|-moz-window-titlebar-maximized", "-moz-binding": "<url>|none", "-moz-border-bottom-colors": "<color>+|none", "-moz-border-left-colors": "<color>+|none", "-moz-border-right-colors": "<color>+|none", "-moz-border-top-colors": "<color>+|none", "-moz-context-properties": "none|[fill|fill-opacity|stroke|stroke-opacity]#", "-moz-float-edge": "border-box|content-box|margin-box|padding-box", "-moz-force-broken-image-icon": "0|1", "-moz-orient": "inline|block|horizontal|vertical", "-moz-outline-radius": "<outline-radius>{1,4} [/ <outline-radius>{1,4}]?", "-moz-outline-radius-bottomleft": "<outline-radius>", "-moz-outline-radius-bottomright": "<outline-radius>", "-moz-outline-radius-topleft": "<outline-radius>", "-moz-outline-radius-topright": "<outline-radius>", "-moz-stack-sizing": "ignore|stretch-to-fit", "-moz-text-blink": "none|blink", "-moz-user-focus": "ignore|normal|select-after|select-before|select-menu|select-same|select-all|none", "-moz-user-input": "auto|none|enabled|disabled", "-moz-user-modify": "read-only|read-write|write-only", "-moz-window-dragging": "drag|no-drag", "-moz-window-shadow": "default|menu|tooltip|sheet|none", "-webkit-appearance": "none|button|button-bevel|caps-lock-indicator|caret|checkbox|default-button|inner-spin-button|listbox|listitem|media-controls-background|media-controls-fullscreen-background|media-current-time-display|media-enter-fullscreen-button|media-exit-fullscreen-button|media-fullscreen-button|media-mute-button|media-overlay-play-button|media-play-button|media-seek-back-button|media-seek-forward-button|media-slider|media-sliderthumb|media-time-remaining-display|media-toggle-closed-captions-button|media-volume-slider|media-volume-slider-container|media-volume-sliderthumb|menulist|menulist-button|menulist-text|menulist-textfield|meter|progress-bar|progress-bar-value|push-button|radio|scrollbarbutton-down|scrollbarbutton-left|scrollbarbutton-right|scrollbarbutton-up|scrollbargripper-horizontal|scrollbargripper-vertical|scrollbarthumb-horizontal|scrollbarthumb-vertical|scrollbartrack-horizontal|scrollbartrack-vertical|searchfield|searchfield-cancel-button|searchfield-decoration|searchfield-results-button|searchfield-results-decoration|slider-horizontal|slider-vertical|sliderthumb-horizontal|sliderthumb-vertical|square-button|textarea|textfield|-apple-pay-button", "-webkit-border-before": "<'border-width'>||<'border-style'>||<color>", "-webkit-border-before-color": "<color>", "-webkit-border-before-style": "<'border-style'>", "-webkit-border-before-width": "<'border-width'>", "-webkit-box-reflect": "[above|below|right|left]? <length>? <image>?", "-webkit-line-clamp": "none|<integer>", "-webkit-mask": "[<mask-reference>||<position> [/ <bg-size>]?||<repeat-style>||[<visual-box>|border|padding|content|text]||[<visual-box>|border|padding|content]]#", "-webkit-mask-attachment": "<attachment>#", "-webkit-mask-clip": "[<coord-box>|no-clip|border|padding|content|text]#", "-webkit-mask-composite": "<composite-style>#", "-webkit-mask-image": "<mask-reference>#", "-webkit-mask-origin": "[<coord-box>|border|padding|content]#", "-webkit-mask-position": "<position>#", "-webkit-mask-position-x": "[<length-percentage>|left|center|right]#", "-webkit-mask-position-y": "[<length-percentage>|top|center|bottom]#", "-webkit-mask-repeat": "<repeat-style>#", "-webkit-mask-repeat-x": "repeat|no-repeat|space|round", "-webkit-mask-repeat-y": "repeat|no-repeat|space|round", "-webkit-mask-size": "<bg-size>#", "-webkit-overflow-scrolling": "auto|touch", "-webkit-tap-highlight-color": "<color>", "-webkit-text-fill-color": "<color>", "-webkit-text-stroke": "<length>||<color>", "-webkit-text-stroke-color": "<color>", "-webkit-text-stroke-width": "<length>", "-webkit-touch-callout": "default|none", "-webkit-user-modify": "read-only|read-write|read-write-plaintext-only", "-webkit-user-select": "auto|none|text|all", "accent-color": "auto|<color>", "align-content": "normal|<baseline-position>|<content-distribution>|<overflow-position>? <content-position>", "align-items": "normal|stretch|<baseline-position>|[<overflow-position>? <self-position>]|anchor-center", "align-self": "auto|normal|stretch|<baseline-position>|<overflow-position>? <self-position>|anchor-center", "align-tracks": "[normal|<baseline-position>|<content-distribution>|<overflow-position>? <content-position>]#", "alignment-baseline": "auto|baseline|before-edge|text-before-edge|middle|central|after-edge|text-after-edge|ideographic|alphabetic|hanging|mathematical", all: "initial|inherit|unset|revert|revert-layer", "anchor-name": "none|<dashed-ident>#", "anchor-scope": "none|all|<dashed-ident>#", animation: "<single-animation>#", "animation-composition": "<single-animation-composition>#", "animation-delay": "<time>#", "animation-direction": "<single-animation-direction>#", "animation-duration": "[auto|<time [0s,\u221E]>]#", "animation-fill-mode": "<single-animation-fill-mode>#", "animation-iteration-count": "<single-animation-iteration-count>#", "animation-name": "[none|<keyframes-name>]#", "animation-play-state": "<single-animation-play-state>#", "animation-range": "[<'animation-range-start'> <'animation-range-end'>?]#", "animation-range-end": "[normal|<length-percentage>|<timeline-range-name> <length-percentage>?]#", "animation-range-start": "[normal|<length-percentage>|<timeline-range-name> <length-percentage>?]#", "animation-timeline": "<single-animation-timeline>#", "animation-timing-function": "<easing-function>#", "animation-trigger": "[none|[<dashed-ident> <animation-action>+]+]#", appearance: "none|auto|<compat-auto>|<compat-special>", "aspect-ratio": "auto||<ratio>", "backdrop-filter": "none|<filter-value-list>", "backface-visibility": "visible|hidden", background: "<bg-layer>#? , <final-bg-layer>", "background-attachment": "<attachment>#", "background-blend-mode": "<blend-mode>#", "background-clip": "<bg-clip>#", "background-color": "<color>", "background-image": "<bg-image>#", "background-origin": "<visual-box>#", "background-position": "<bg-position>#", "background-position-x": "[center|[[left|right|x-start|x-end]? <length-percentage>?]!]#", "background-position-y": "[center|[[top|bottom|y-start|y-end]? <length-percentage>?]!]#", "background-repeat": "<repeat-style>#", "background-size": "<bg-size>#", "baseline-shift": "baseline|sub|super|<svg-length>", "baseline-source": "auto|first|last", "block-size": "<'width'>", border: "<line-width>||<line-style>||<color>", "border-block": "<'border-block-start'>", "border-block-color": "<'border-top-color'>{1,2}", "border-block-end": "<'border-top-width'>||<'border-top-style'>||<color>", "border-block-end-color": "<'border-top-color'>", "border-block-end-style": "<'border-top-style'>", "border-block-end-width": "<'border-top-width'>", "border-block-start": "<'border-top-width'>||<'border-top-style'>||<color>", "border-block-start-color": "<'border-top-color'>", "border-block-start-style": "<'border-top-style'>", "border-block-start-width": "<'border-top-width'>", "border-block-style": "<'border-top-style'>{1,2}", "border-block-width": "<'border-top-width'>{1,2}", "border-bottom": "<line-width>||<line-style>||<color>", "border-bottom-color": "<'border-top-color'>", "border-bottom-left-radius": "<length-percentage [0,\u221E]>{1,2}", "border-bottom-right-radius": "<length-percentage [0,\u221E]>{1,2}", "border-bottom-style": "<line-style>", "border-bottom-width": "<line-width>", "border-collapse": "separate|collapse", "border-color": "<color>{1,4}", "border-end-end-radius": "<'border-top-left-radius'>", "border-end-start-radius": "<'border-top-left-radius'>", "border-image": "<'border-image-source'>||<'border-image-slice'> [/ <'border-image-width'>|/ <'border-image-width'>? / <'border-image-outset'>]?||<'border-image-repeat'>", "border-image-outset": "[<length [0,\u221E]>|<number [0,\u221E]>]{1,4}", "border-image-repeat": "[stretch|repeat|round|space]{1,2}", "border-image-slice": "[<number [0,\u221E]>|<percentage [0,\u221E]>]{1,4}&&fill?", "border-image-source": "none|<image>", "border-image-width": "[<length-percentage [0,\u221E]>|<number [0,\u221E]>|auto]{1,4}", "border-inline": "<'border-block-start'>", "border-inline-color": "<'border-top-color'>{1,2}", "border-inline-end": "<'border-top-width'>||<'border-top-style'>||<color>", "border-inline-end-color": "<'border-top-color'>", "border-inline-end-style": "<'border-top-style'>", "border-inline-end-width": "<'border-top-width'>", "border-inline-start": "<'border-top-width'>||<'border-top-style'>||<color>", "border-inline-start-color": "<'border-top-color'>", "border-inline-start-style": "<'border-top-style'>", "border-inline-start-width": "<'border-top-width'>", "border-inline-style": "<'border-top-style'>{1,2}", "border-inline-width": "<'border-top-width'>{1,2}", "border-left": "<line-width>||<line-style>||<color>", "border-left-color": "<color>", "border-left-style": "<line-style>", "border-left-width": "<line-width>", "border-radius": "<length-percentage [0,\u221E]>{1,4} [/ <length-percentage [0,\u221E]>{1,4}]?", "border-right": "<line-width>||<line-style>||<color>", "border-right-color": "<color>", "border-right-style": "<line-style>", "border-right-width": "<line-width>", "border-spacing": "<length>{1,2}", "border-start-end-radius": "<'border-top-left-radius'>", "border-start-start-radius": "<'border-top-left-radius'>", "border-style": "<line-style>{1,4}", "border-top": "<line-width>||<line-style>||<color>", "border-top-color": "<color>", "border-top-left-radius": "<length-percentage [0,\u221E]>{1,2}", "border-top-right-radius": "<length-percentage [0,\u221E]>{1,2}", "border-top-style": "<line-style>", "border-top-width": "<line-width>", "border-width": "<line-width>{1,4}", bottom: "auto|<length-percentage>|<anchor()>|<anchor-size()>", "box-align": "start|center|end|baseline|stretch", "box-decoration-break": "slice|clone", "box-direction": "normal|reverse|inherit", "box-flex": "<number>", "box-flex-group": "<integer>", "box-lines": "single|multiple", "box-ordinal-group": "<integer>", "box-orient": "horizontal|vertical|inline-axis|block-axis|inherit", "box-pack": "start|center|end|justify", "box-shadow": "none|<shadow>#", "box-sizing": "content-box|border-box", "break-after": "auto|avoid|always|all|avoid-page|page|left|right|recto|verso|avoid-column|column|avoid-region|region", "break-before": "auto|avoid|always|all|avoid-page|page|left|right|recto|verso|avoid-column|column|avoid-region|region", "break-inside": "auto|avoid|avoid-page|avoid-column|avoid-region", "caption-side": "top|bottom", caret: "<'caret-color'>||<'caret-animation'>||<'caret-shape'>", "caret-animation": "auto|manual", "caret-color": "auto|<color>", "caret-shape": "auto|bar|block|underscore", clear: "none|left|right|both|inline-start|inline-end", clip: "<shape>|auto", "clip-path": "<clip-source>|[<basic-shape>||<geometry-box>]|none", "clip-rule": "nonzero|evenodd", color: "<color>", "color-interpolation-filters": "auto|sRGB|linearRGB", "color-scheme": "normal|[light|dark|<custom-ident>]+&&only?", "column-count": "<integer>|auto", "column-fill": "auto|balance", "column-gap": "normal|<length-percentage>", "column-height": "auto|<length [0,\u221E]>", "column-rule": "<'column-rule-width'>||<'column-rule-style'>||<'column-rule-color'>", "column-rule-color": "<color>", "column-rule-style": "<'border-style'>", "column-rule-width": "<'border-width'>", "column-span": "none|all", "column-width": "auto|<length [0,\u221E]>", "column-wrap": "auto|nowrap|wrap", columns: "[<'column-width'>||<'column-count'>] [/ <'column-height'>]?", contain: "none|strict|content|[[size||inline-size]||layout||style||paint]", "contain-intrinsic-block-size": "auto? [none|<length>]", "contain-intrinsic-height": "auto? [none|<length>]", "contain-intrinsic-inline-size": "auto? [none|<length>]", "contain-intrinsic-size": "[auto? [none|<length>]]{1,2}", "contain-intrinsic-width": "auto? [none|<length>]", container: "<'container-name'> [/ <'container-type'>]?", "container-name": "none|<custom-ident>+", "container-type": "normal||[size|inline-size]", content: "normal|none|[<content-replacement>|<content-list>] [/ [<string>|<counter>|<attr()>]+]?", "content-visibility": "visible|auto|hidden", "corner-block-end-shape": "<corner-shape-value>{1,2}", "corner-block-start-shape": "<corner-shape-value>{1,2}", "corner-bottom-shape": "<corner-shape-value>{1,2}", "corner-bottom-left-shape": "<corner-shape-value>", "corner-bottom-right-shape": "<corner-shape-value>", "corner-end-end-shape": "<corner-shape-value>", "corner-end-start-shape": "<corner-shape-value>", "corner-inline-end-shape": "<corner-shape-value>{1,2}", "corner-inline-start-shape": "<corner-shape-value>{1,2}", "corner-left-shape": "<corner-shape-value>{1,2}", "corner-right-shape": "<corner-shape-value>{1,2}", "corner-shape": "<corner-shape-value>{1,4}", "corner-start-start-shape": "<corner-shape-value>", "corner-start-end-shape": "<corner-shape-value>", "corner-top-shape": "<corner-shape-value>{1,2}", "corner-top-left-shape": "<corner-shape-value>", "corner-top-right-shape": "<corner-shape-value>", "counter-increment": "[<counter-name> <integer>?]+|none", "counter-reset": "[<counter-name> <integer>?|<reversed-counter-name> <integer>?]+|none", "counter-set": "[<counter-name> <integer>?]+|none", cursor: "[[<url> [<x> <y>]? ,]* [auto|default|none|context-menu|help|pointer|progress|wait|cell|crosshair|text|vertical-text|alias|copy|move|no-drop|not-allowed|e-resize|n-resize|ne-resize|nw-resize|s-resize|se-resize|sw-resize|w-resize|ew-resize|ns-resize|nesw-resize|nwse-resize|col-resize|row-resize|all-scroll|zoom-in|zoom-out|grab|grabbing|hand|-webkit-grab|-webkit-grabbing|-webkit-zoom-in|-webkit-zoom-out|-moz-grab|-moz-grabbing|-moz-zoom-in|-moz-zoom-out]]", cx: "<length>|<percentage>", cy: "<length>|<percentage>", d: "none|path( <string> )", direction: "ltr|rtl", display: "[<display-outside>||<display-inside>]|<display-listitem>|<display-internal>|<display-box>|<display-legacy>|<-non-standard-display>", "dominant-baseline": "auto|use-script|no-change|reset-size|ideographic|alphabetic|hanging|mathematical|central|middle|text-after-edge|text-before-edge", "dynamic-range-limit": "standard|no-limit|constrained|<dynamic-range-limit-mix()>", "empty-cells": "show|hide", "field-sizing": "content|fixed", fill: "<paint>", "fill-opacity": "<number-zero-one>|<percentage>", "fill-rule": "nonzero|evenodd", filter: "none|<filter-value-list>|<-ms-filter-function-list>", flex: "none|[<'flex-grow'> <'flex-shrink'>?||<'flex-basis'>]", "flex-basis": "content|<'width'>", "flex-direction": "row|row-reverse|column|column-reverse", "flex-flow": "<'flex-direction'>||<'flex-wrap'>", "flex-grow": "<number>", "flex-shrink": "<number>", "flex-wrap": "nowrap|wrap|wrap-reverse", float: "left|right|none|inline-start|inline-end", "flood-color": "<color>", "flood-opacity": "<'opacity'>", font: "[[<'font-style'>||<font-variant-css2>||<'font-weight'>||<font-width-css3>]? <'font-size'> [/ <'line-height'>]? <'font-family'>#]|<system-family-name>|<-non-standard-font>", "font-family": "[<family-name>|<generic-family>]#", "font-feature-settings": "normal|<feature-tag-value>#", "font-kerning": "auto|normal|none", "font-language-override": "normal|<string>", "font-optical-sizing": "auto|none", "font-palette": "normal|light|dark|<palette-identifier>|<palette-mix()>", "font-size": "<absolute-size>|<relative-size>|<length-percentage [0,\u221E]>|math", "font-size-adjust": "none|[ex-height|cap-height|ch-width|ic-width|ic-height]? [from-font|<number>]", "font-smooth": "auto|never|always|<absolute-size>|<length>", "font-stretch": "<font-stretch-absolute>", "font-style": "normal|italic|oblique <angle>?", "font-synthesis": "none|[weight||style||small-caps||position]", "font-synthesis-position": "auto|none", "font-synthesis-small-caps": "auto|none", "font-synthesis-style": "auto|none", "font-synthesis-weight": "auto|none", "font-variant": "normal|none|[<common-lig-values>||<discretionary-lig-values>||<historical-lig-values>||<contextual-alt-values>||stylistic( <feature-value-name> )||historical-forms||styleset( <feature-value-name># )||character-variant( <feature-value-name># )||swash( <feature-value-name> )||ornaments( <feature-value-name> )||annotation( <feature-value-name> )||[small-caps|all-small-caps|petite-caps|all-petite-caps|unicase|titling-caps]||<numeric-figure-values>||<numeric-spacing-values>||<numeric-fraction-values>||ordinal||slashed-zero||<east-asian-variant-values>||<east-asian-width-values>||ruby]", "font-variant-alternates": "normal|[stylistic( <feature-value-name> )||historical-forms||styleset( <feature-value-name># )||character-variant( <feature-value-name># )||swash( <feature-value-name> )||ornaments( <feature-value-name> )||annotation( <feature-value-name> )]", "font-variant-caps": "normal|small-caps|all-small-caps|petite-caps|all-petite-caps|unicase|titling-caps", "font-variant-east-asian": "normal|[<east-asian-variant-values>||<east-asian-width-values>||ruby]", "font-variant-emoji": "normal|text|emoji|unicode", "font-variant-ligatures": "normal|none|[<common-lig-values>||<discretionary-lig-values>||<historical-lig-values>||<contextual-alt-values>]", "font-variant-numeric": "normal|[<numeric-figure-values>||<numeric-spacing-values>||<numeric-fraction-values>||ordinal||slashed-zero]", "font-variant-position": "normal|sub|super", "font-variation-settings": "normal|[<string> <number>]#", "font-weight": "<font-weight-absolute>|bolder|lighter", "font-width": "normal|<percentage [0,\u221E]>|ultra-condensed|extra-condensed|condensed|semi-condensed|semi-expanded|expanded|extra-expanded|ultra-expanded", "forced-color-adjust": "auto|none|preserve-parent-color", gap: "<'row-gap'> <'column-gap'>?", grid: "<'grid-template'>|<'grid-template-rows'> / [auto-flow&&dense?] <'grid-auto-columns'>?|[auto-flow&&dense?] <'grid-auto-rows'>? / <'grid-template-columns'>", "grid-area": "<grid-line> [/ <grid-line>]{0,3}", "grid-auto-columns": "<track-size>+", "grid-auto-flow": "[row|column]||dense", "grid-auto-rows": "<track-size>+", "grid-column": "<grid-line> [/ <grid-line>]?", "grid-column-end": "<grid-line>", "grid-column-gap": "<length-percentage>", "grid-column-start": "<grid-line>", "grid-gap": "<'grid-row-gap'> <'grid-column-gap'>?", "grid-row": "<grid-line> [/ <grid-line>]?", "grid-row-end": "<grid-line>", "grid-row-gap": "<length-percentage>", "grid-row-start": "<grid-line>", "grid-template": "none|[<'grid-template-rows'> / <'grid-template-columns'>]|[<line-names>? <string> <track-size>? <line-names>?]+ [/ <explicit-track-list>]?", "grid-template-areas": "none|<string>+", "grid-template-columns": "none|<track-list>|<auto-track-list>|subgrid <line-name-list>?", "grid-template-rows": "none|<track-list>|<auto-track-list>|subgrid <line-name-list>?", "hanging-punctuation": "none|[first||[force-end|allow-end]||last]", height: "auto|<length-percentage [0,\u221E]>|min-content|max-content|fit-content|fit-content( <length-percentage [0,\u221E]> )|<calc-size()>|<anchor-size()>|stretch|<-non-standard-size>", "hyphenate-character": "auto|<string>", "hyphenate-limit-chars": "[auto|<integer>]{1,3}", hyphens: "none|manual|auto", "image-orientation": "from-image|<angle>|[<angle>? flip]", "image-rendering": "auto|crisp-edges|pixelated|smooth|optimizeSpeed|optimizeQuality|<-non-standard-image-rendering>", "image-resolution": "[from-image||<resolution>]&&snap?", "ime-mode": "auto|normal|active|inactive|disabled", "initial-letter": "normal|[<number> <integer>?]", "initial-letter-align": "[auto|alphabetic|hanging|ideographic]", "inline-size": "<'width'>", inset: "<'top'>{1,4}", "inset-block": "<'top'>{1,2}", "inset-block-end": "<'top'>", "inset-block-start": "<'top'>", "inset-inline": "<'top'>{1,2}", "inset-inline-end": "<'top'>", "inset-inline-start": "<'top'>", "interpolate-size": "numeric-only|allow-keywords", isolation: "auto|isolate", interactivity: "auto|inert", "interest-delay": "<'interest-delay-start'>{1,2}", "interest-delay-end": "normal|<time>", "interest-delay-start": "normal|<time>", "justify-content": "normal|<content-distribution>|<overflow-position>? [<content-position>|left|right]", "justify-items": "normal|stretch|<baseline-position>|<overflow-position>? [<self-position>|left|right]|legacy|legacy&&[left|right|center]|anchor-center", "justify-self": "auto|normal|stretch|<baseline-position>|<overflow-position>? [<self-position>|left|right]|anchor-center", "justify-tracks": "[normal|<content-distribution>|<overflow-position>? [<content-position>|left|right]]#", left: "auto|<length-percentage>|<anchor()>|<anchor-size()>", "letter-spacing": "normal|<length-percentage>", "lighting-color": "<color>", "line-break": "auto|loose|normal|strict|anywhere", "line-clamp": "none|<integer>", "line-height": "normal|<number>|<length>|<percentage>", "line-height-step": "<length>", "list-style": "<'list-style-type'>||<'list-style-position'>||<'list-style-image'>", "list-style-image": "<image>|none", "list-style-position": "inside|outside", "list-style-type": "<counter-style>|<string>|none", margin: "<'margin-top'>{1,4}", "margin-block": "<'margin-top'>{1,2}", "margin-block-end": "<'margin-top'>", "margin-block-start": "<'margin-top'>", "margin-bottom": "<length-percentage>|auto|<anchor-size()>", "margin-inline": "<'margin-top'>{1,2}", "margin-inline-end": "<'margin-top'>", "margin-inline-start": "<'margin-top'>", "margin-left": "<length-percentage>|auto|<anchor-size()>", "margin-right": "<length-percentage>|auto|<anchor-size()>", "margin-top": "<length-percentage>|auto|<anchor-size()>", "margin-trim": "none|in-flow|all", marker: "none|<url>", "marker-end": "none|<url>", "marker-mid": "none|<url>", "marker-start": "none|<url>", mask: "<mask-layer>#", "mask-border": "<'mask-border-source'>||<'mask-border-slice'> [/ <'mask-border-width'>? [/ <'mask-border-outset'>]?]?||<'mask-border-repeat'>||<'mask-border-mode'>", "mask-border-mode": "luminance|alpha", "mask-border-outset": "[<length>|<number>]{1,4}", "mask-border-repeat": "[stretch|repeat|round|space]{1,2}", "mask-border-slice": "<number-percentage>{1,4} fill?", "mask-border-source": "none|<image>", "mask-border-width": "[<length-percentage>|<number>|auto]{1,4}", "mask-clip": "[<coord-box>|no-clip]#", "mask-composite": "<compositing-operator>#", "mask-image": "<mask-reference>#", "mask-mode": "<masking-mode>#", "mask-origin": "<coord-box>#", "mask-position": "<position>#", "mask-repeat": "<repeat-style>#", "mask-size": "<bg-size>#", "mask-type": "luminance|alpha", "masonry-auto-flow": "[pack|next]||[definite-first|ordered]", "math-depth": "auto-add|add( <integer> )|<integer>", "math-shift": "normal|compact", "math-style": "normal|compact", "max-block-size": "<'max-width'>", "max-height": "none|<length-percentage [0,\u221E]>|min-content|max-content|fit-content|fit-content( <length-percentage [0,\u221E]> )|<calc-size()>|<anchor-size()>|stretch|<-non-standard-size>", "max-inline-size": "<'max-width'>", "max-lines": "none|<integer>", "max-width": "none|<length-percentage [0,\u221E]>|min-content|max-content|fit-content|fit-content( <length-percentage [0,\u221E]> )|<calc-size()>|<anchor-size()>|stretch|<-non-standard-size>", "min-block-size": "<'min-width'>", "min-height": "auto|<length-percentage [0,\u221E]>|min-content|max-content|fit-content|fit-content( <length-percentage [0,\u221E]> )|<calc-size()>|<anchor-size()>|stretch|<-non-standard-size>", "min-inline-size": "<'min-width'>", "min-width": "auto|<length-percentage [0,\u221E]>|min-content|max-content|fit-content|fit-content( <length-percentage [0,\u221E]> )|<calc-size()>|<anchor-size()>|stretch|<-non-standard-size>", "mix-blend-mode": "<blend-mode>|plus-darker|plus-lighter", "object-fit": "fill|contain|cover|none|scale-down", "object-position": "<position>", "object-view-box": "none|<basic-shape-rect>", offset: "[<'offset-position'>? [<'offset-path'> [<'offset-distance'>||<'offset-rotate'>]?]?]! [/ <'offset-anchor'>]?", "offset-anchor": "auto|<position>", "offset-distance": "<length-percentage>", "offset-path": "none|<offset-path>||<coord-box>", "offset-position": "normal|auto|<position>", "offset-rotate": "[auto|reverse]||<angle>", opacity: "<opacity-value>", order: "<integer>", orphans: "<integer>", outline: "<'outline-width'>||<'outline-style'>||<'outline-color'>", "outline-color": "auto|<color>", "outline-offset": "<length>", "outline-style": "auto|<outline-line-style>", "outline-width": "<line-width>", overflow: "[visible|hidden|clip|scroll|auto]{1,2}|<-non-standard-overflow>", "overflow-anchor": "auto|none", "overflow-block": "visible|hidden|clip|scroll|auto|<-non-standard-overflow>", "overflow-clip-box": "padding-box|content-box", "overflow-clip-margin": "<visual-box>||<length [0,\u221E]>", "overflow-inline": "visible|hidden|clip|scroll|auto|<-non-standard-overflow>", "overflow-wrap": "normal|break-word|anywhere", "overflow-x": "visible|hidden|clip|scroll|auto|<-non-standard-overflow>", "overflow-y": "visible|hidden|clip|scroll|auto|<-non-standard-overflow>", overlay: "none|auto", "overscroll-behavior": "[contain|none|auto]{1,2}", "overscroll-behavior-block": "contain|none|auto", "overscroll-behavior-inline": "contain|none|auto", "overscroll-behavior-x": "contain|none|auto", "overscroll-behavior-y": "contain|none|auto", padding: "<'padding-top'>{1,4}", "padding-block": "<'padding-top'>{1,2}", "padding-block-end": "<'padding-top'>", "padding-block-start": "<'padding-top'>", "padding-bottom": "<length-percentage [0,\u221E]>", "padding-inline": "<'padding-top'>{1,2}", "padding-inline-end": "<'padding-top'>", "padding-inline-start": "<'padding-top'>", "padding-left": "<length-percentage [0,\u221E]>", "padding-right": "<length-percentage [0,\u221E]>", "padding-top": "<length-percentage [0,\u221E]>", page: "auto|<custom-ident>", "page-break-after": "auto|always|avoid|left|right|recto|verso", "page-break-before": "auto|always|avoid|left|right|recto|verso", "page-break-inside": "auto|avoid", "paint-order": "normal|[fill||stroke||markers]", perspective: "none|<length>", "perspective-origin": "<position>", "place-content": "<'align-content'> <'justify-content'>?", "place-items": "<'align-items'> <'justify-items'>?", "place-self": "<'align-self'> <'justify-self'>?", "pointer-events": "auto|none|visiblePainted|visibleFill|visibleStroke|visible|painted|fill|stroke|all|inherit", position: "static|relative|absolute|sticky|fixed|-webkit-sticky", "position-anchor": "auto|none|<anchor-name>", "position-area": "none|<position-area>", "position-try": "<'position-try-order'>? <'position-try-fallbacks'>", "position-try-fallbacks": "none|[[<dashed-ident>||<try-tactic>]|<'position-area'>]#", "position-try-order": "normal|<try-size>", "position-visibility": "always|[anchors-valid||anchors-visible||no-overflow]", "print-color-adjust": "economy|exact", quotes: "none|auto|[<string> <string>]+", r: "<length>|<percentage>", "reading-flow": "normal|source-order|flex-visual|flex-flow|grid-rows|grid-columns|grid-order", "reading-order": "<integer>", resize: "none|both|horizontal|vertical|block|inline", right: "auto|<length-percentage>|<anchor()>|<anchor-size()>", rotate: "none|<angle>|[x|y|z|<number>{3}]&&<angle>", "row-gap": "normal|<length-percentage>", "ruby-align": "start|center|space-between|space-around", "ruby-merge": "separate|collapse|auto", "ruby-overhang": "auto|none", "ruby-position": "[alternate||[over|under]]|inter-character", rx: "<length>|<percentage>", ry: "<length>|<percentage>", scale: "none|[<number>|<percentage>]{1,3}", "scroll-behavior": "auto|smooth", "scroll-initial-target": "none|nearest", "scroll-margin": "<length>{1,4}", "scroll-margin-block": "<length>{1,2}", "scroll-margin-block-end": "<length>", "scroll-margin-block-start": "<length>", "scroll-margin-bottom": "<length>", "scroll-margin-inline": "<length>{1,2}", "scroll-margin-inline-end": "<length>", "scroll-margin-inline-start": "<length>", "scroll-margin-left": "<length>", "scroll-margin-right": "<length>", "scroll-margin-top": "<length>", "scroll-marker-group": "none|before|after", "scroll-padding": "[auto|<length-percentage>]{1,4}", "scroll-padding-block": "[auto|<length-percentage>]{1,2}", "scroll-padding-block-end": "auto|<length-percentage>", "scroll-padding-block-start": "auto|<length-percentage>", "scroll-padding-bottom": "auto|<length-percentage>", "scroll-padding-inline": "[auto|<length-percentage>]{1,2}", "scroll-padding-inline-end": "auto|<length-percentage>", "scroll-padding-inline-start": "auto|<length-percentage>", "scroll-padding-left": "auto|<length-percentage>", "scroll-padding-right": "auto|<length-percentage>", "scroll-padding-top": "auto|<length-percentage>", "scroll-snap-align": "[none|start|end|center]{1,2}", "scroll-snap-coordinate": "none|<position>#", "scroll-snap-destination": "<position>", "scroll-snap-points-x": "none|repeat( <length-percentage> )", "scroll-snap-points-y": "none|repeat( <length-percentage> )", "scroll-snap-stop": "normal|always", "scroll-snap-type": "none|[x|y|block|inline|both] [mandatory|proximity]?", "scroll-snap-type-x": "none|mandatory|proximity", "scroll-snap-type-y": "none|mandatory|proximity", "scroll-target-group": "none|auto", "scroll-timeline": "[<'scroll-timeline-name'> <'scroll-timeline-axis'>?]#", "scroll-timeline-axis": "[block|inline|x|y]#", "scroll-timeline-name": "[none|<dashed-ident>]#", "scrollbar-color": "auto|<color>{2}", "scrollbar-gutter": "auto|stable&&both-edges?", "scrollbar-width": "auto|thin|none", "shape-image-threshold": "<opacity-value>", "shape-margin": "<length-percentage>", "shape-outside": "none|[<shape-box>||<basic-shape>]|<image>", "shape-rendering": "auto|optimizeSpeed|crispEdges|geometricPrecision", "speak-as": "normal|spell-out||digits||[literal-punctuation|no-punctuation]", "stop-color": "<'color'>", "stop-opacity": "<'opacity'>", stroke: "<paint>", "stroke-color": "<color>", "stroke-dasharray": "none|[<svg-length>+]#", "stroke-dashoffset": "<svg-length>", "stroke-linecap": "butt|round|square", "stroke-linejoin": "miter|round|bevel", "stroke-miterlimit": "<number-one-or-greater>", "stroke-opacity": "<'opacity'>", "stroke-width": "<svg-length>", "tab-size": "<integer>|<length>", "table-layout": "auto|fixed", "text-align": "start|end|left|right|center|justify|match-parent", "text-align-last": "auto|start|end|left|right|center|justify", "text-anchor": "start|middle|end", "text-autospace": "normal|<autospace>|auto", "text-box": "normal|<'text-box-trim'>||<'text-box-edge'>", "text-box-edge": "auto|<text-edge>", "text-box-trim": "none|trim-start|trim-end|trim-both", "text-combine-upright": "none|all|[digits <integer>?]", "text-decoration": "<'text-decoration-line'>||<'text-decoration-style'>||<'text-decoration-color'>||<'text-decoration-thickness'>", "text-decoration-color": "<color>", "text-decoration-inset": "<length>{1,2}|auto", "text-decoration-line": "none|[underline||overline||line-through||blink]|spelling-error|grammar-error", "text-decoration-skip": "none|[objects||[spaces|[leading-spaces||trailing-spaces]]||edges||box-decoration]", "text-decoration-skip-ink": "auto|all|none", "text-decoration-style": "solid|double|dotted|dashed|wavy", "text-decoration-thickness": "auto|from-font|<length>|<percentage>", "text-emphasis": "<'text-emphasis-style'>||<'text-emphasis-color'>", "text-emphasis-color": "<color>", "text-emphasis-position": "auto|[over|under]&&[right|left]?", "text-emphasis-style": "none|[[filled|open]||[dot|circle|double-circle|triangle|sesame]]|<string>", "text-indent": "<length-percentage>&&hanging?&&each-line?", "text-justify": "auto|inter-character|inter-word|none", "text-orientation": "mixed|upright|sideways", "text-overflow": "[clip|ellipsis|<string>]{1,2}", "text-rendering": "auto|optimizeSpeed|optimizeLegibility|geometricPrecision", "text-shadow": "none|<shadow-t>#", "text-size-adjust": "none|auto|<percentage>", "text-spacing-trim": "space-all|normal|space-first|trim-start", "text-transform": "none|[capitalize|uppercase|lowercase]||full-width||full-size-kana|math-auto", "text-underline-offset": "auto|<length>|<percentage>", "text-underline-position": "auto|from-font|[under||[left|right]]", "text-wrap": "<'text-wrap-mode'>||<'text-wrap-style'>", "text-wrap-mode": "wrap|nowrap", "text-wrap-style": "auto|balance|stable|pretty", "timeline-scope": "none|<dashed-ident>#", "timeline-trigger": "none|[<'timeline-trigger-name'> <'timeline-trigger-source'> <'timeline-trigger-range'> ['/' <'timeline-trigger-exit-range'>]?]#", "timeline-trigger-name": "none|<dashed-ident>#", "timeline-trigger-exit-range": "[<'timeline-trigger-exit-range-start'> <'timeline-trigger-exit-range-end'>?]#", "timeline-trigger-exit-range-end": "[auto|normal|<length-percentage>|<timeline-range-name> <length-percentage>?]#", "timeline-trigger-exit-range-start": "[auto|normal|<length-percentage>|<timeline-range-name> <length-percentage>?]#", "timeline-trigger-range": "[<'timeline-trigger-range-start'> <'timeline-trigger-range-end'>?]#", "timeline-trigger-range-end": "[normal|<length-percentage>|<timeline-range-name> <length-percentage>?]#", "timeline-trigger-range-start": "[normal|<length-percentage>|<timeline-range-name> <length-percentage>?]#", "timeline-trigger-source": "<single-animation-timeline>#", top: "auto|<length-percentage>|<anchor()>|<anchor-size()>", "touch-action": "auto|none|[[pan-x|pan-left|pan-right]||[pan-y|pan-up|pan-down]||pinch-zoom]|manipulation", transform: "none|<transform-list>", "transform-box": "content-box|border-box|fill-box|stroke-box|view-box", "transform-origin": "[<length-percentage>|left|center|right|top|bottom]|[[<length-percentage>|left|center|right]&&[<length-percentage>|top|center|bottom]] <length>?", "transform-style": "flat|preserve-3d", transition: "<single-transition>#", "transition-behavior": "<transition-behavior-value>#", "transition-delay": "<time>#", "transition-duration": "<time>#", "transition-property": "none|<single-transition-property>#", "transition-timing-function": "<easing-function>#", translate: "none|<length-percentage> [<length-percentage> <length>?]?", "trigger-scope": "none|all|<dashed-ident>#", "unicode-bidi": "normal|embed|isolate|bidi-override|isolate-override|plaintext|-moz-isolate|-moz-isolate-override|-moz-plaintext|-webkit-isolate|-webkit-isolate-override|-webkit-plaintext", "user-select": "auto|text|none|all", "vector-effect": "none|non-scaling-stroke|non-scaling-size|non-rotation|fixed-position", "vertical-align": "baseline|sub|super|text-top|text-bottom|middle|top|bottom|<percentage>|<length>", "view-timeline": "[<'view-timeline-name'> [<'view-timeline-axis'>||<'view-timeline-inset'>]?]#", "view-timeline-axis": "[block|inline|x|y]#", "view-timeline-inset": "[[auto|<length-percentage>]{1,2}]#", "view-timeline-name": "[none|<dashed-ident>]#", "view-transition-class": "none|<custom-ident>+", "view-transition-name": "none|<custom-ident>|match-element", visibility: "visible|hidden|collapse", "white-space": "normal|pre|pre-wrap|pre-line|<'white-space-collapse'>||<'text-wrap-mode'>", "white-space-collapse": "collapse|preserve|preserve-breaks|preserve-spaces|break-spaces", widows: "<integer>", width: "auto|<length-percentage [0,\u221E]>|min-content|max-content|fit-content|fit-content( <length-percentage [0,\u221E]> )|<calc-size()>|<anchor-size()>|stretch|<-non-standard-size>", "will-change": "auto|<animateable-feature>#", "word-break": "normal|break-all|keep-all|break-word|auto-phrase", "word-spacing": "normal|<length>", "word-wrap": "normal|break-word", "writing-mode": "horizontal-tb|vertical-rl|vertical-lr|sideways-rl|sideways-lr|<svg-writing-mode>", x: "<length>|<percentage>", y: "<length>|<percentage>", "z-index": "auto|<integer>", zoom: "normal|reset|<number [0,\u221E]>||<percentage [0,\u221E]>", "-moz-background-clip": "padding|border", "-moz-border-radius-bottomleft": "<'border-bottom-left-radius'>", "-moz-border-radius-bottomright": "<'border-bottom-right-radius'>", "-moz-border-radius-topleft": "<'border-top-left-radius'>", "-moz-border-radius-topright": "<'border-bottom-right-radius'>", "-moz-control-character-visibility": "visible|hidden", "-moz-osx-font-smoothing": "auto|grayscale", "-moz-user-select": "none|text|all|-moz-none", "-ms-flex-align": "start|end|center|baseline|stretch", "-ms-flex-item-align": "auto|start|end|center|baseline|stretch", "-ms-flex-line-pack": "start|end|center|justify|distribute|stretch", "-ms-flex-negative": "<'flex-shrink'>", "-ms-flex-pack": "start|end|center|justify|distribute", "-ms-flex-order": "<integer>", "-ms-flex-positive": "<'flex-grow'>", "-ms-flex-preferred-size": "<'flex-basis'>", "-ms-interpolation-mode": "nearest-neighbor|bicubic", "-ms-grid-column-align": "start|end|center|stretch", "-ms-grid-row-align": "start|end|center|stretch", "-ms-hyphenate-limit-last": "none|always|column|page|spread", "-webkit-background-clip": "[<visual-box>|border|padding|content|text]#", "-webkit-column-break-after": "always|auto|avoid", "-webkit-column-break-before": "always|auto|avoid", "-webkit-column-break-inside": "always|auto|avoid", "-webkit-font-smoothing": "auto|none|antialiased|subpixel-antialiased", "-webkit-mask-box-image": "[<url>|<gradient>|none] [<length-percentage>{4} <-webkit-mask-box-repeat>{2}]?", "-webkit-print-color-adjust": "economy|exact", "-webkit-text-security": "none|circle|disc|square", "-webkit-user-drag": "none|element|auto", behavior: "<url>+", cue: "<'cue-before'> <'cue-after'>?", "cue-after": "<url> <decibel>?|none", "cue-before": "<url> <decibel>?|none", "glyph-orientation-horizontal": "<angle>", "glyph-orientation-vertical": "<angle>", kerning: "auto|<svg-length>", pause: "<'pause-before'> <'pause-after'>?", "pause-after": "<time>|none|x-weak|weak|medium|strong|x-strong", "pause-before": "<time>|none|x-weak|weak|medium|strong|x-strong", "position-try-options": "<'position-try-fallbacks'>", rest: "<'rest-before'> <'rest-after'>?", "rest-after": "<time>|none|x-weak|weak|medium|strong|x-strong", "rest-before": "<time>|none|x-weak|weak|medium|strong|x-strong", speak: "auto|never|always", "voice-balance": "<number>|left|center|right|leftwards|rightwards", "voice-duration": "auto|<time>", "voice-family": "[[<family-name>|<generic-voice>] ,]* [<family-name>|<generic-voice>]|preserve", "voice-pitch": "<frequency>&&absolute|[[x-low|low|medium|high|x-high]||[<frequency>|<semitones>|<percentage>]]", "voice-range": "<frequency>&&absolute|[[x-low|low|medium|high|x-high]||[<frequency>|<semitones>|<percentage>]]", "voice-rate": "[normal|x-slow|slow|medium|fast|x-fast]||<percentage>", "voice-stress": "normal|strong|moderate|none|reduced", "voice-volume": "silent|[[x-soft|soft|medium|loud|x-loud]||<decibel>]", "white-space-trim": "none|discard-before||discard-after||discard-inner" }, atrules: { charset: { prelude: "<string>", descriptors: null }, "counter-style": { prelude: "<counter-style-name>", descriptors: { "additive-symbols": "[<integer [0,\u221E]>&&<symbol>]#", fallback: "<counter-style-name>", negative: "<symbol> <symbol>?", pad: "<integer [0,\u221E]>&&<symbol>", prefix: "<symbol>", range: "[[<integer>|infinite]{2}]#|auto", "speak-as": "auto|bullets|numbers|words|spell-out|<counter-style-name>", suffix: "<symbol>", symbols: "<symbol>+", system: "cyclic|numeric|alphabetic|symbolic|additive|[fixed <integer>?]|[extends <counter-style-name>]" } }, container: { prelude: "[<container-name>]? <container-condition>", descriptors: null }, document: { prelude: "[<url>|url-prefix( <string> )|domain( <string> )|media-document( <string> )|regexp( <string> )]#", descriptors: null }, "font-face": { prelude: null, descriptors: { "ascent-override": "normal|<percentage>", "descent-override": "normal|<percentage>", "font-display": "auto|block|swap|fallback|optional", "font-family": "<family-name>", "font-feature-settings": "normal|<feature-tag-value>#", "font-stretch": "<font-stretch-absolute>{1,2}", "font-style": "normal|italic|oblique <angle>{0,2}", "font-variation-settings": "normal|[<string> <number>]#", "font-weight": "<font-weight-absolute>{1,2}", "line-gap-override": "normal|<percentage>", "size-adjust": "<percentage>", src: "[<url> [format( <string># )]?|local( <family-name> )]#", "unicode-range": "<urange>#" } }, "font-feature-values": { prelude: "<family-name>#", descriptors: null }, "font-palette-values": { prelude: "<dashed-ident>", descriptors: { "base-palette": "light|dark|<integer [0,\u221E]>", "font-family": "<family-name>#", "override-colors": "[<integer [0,\u221E]> <color>]#" } }, import: { prelude: "[<string>|<url>] [layer|layer( <layer-name> )]? [supports( [<supports-condition>|<declaration>] )]? <media-query-list>?", descriptors: null }, keyframes: { prelude: "<keyframes-name>", descriptors: null }, layer: { prelude: "[<layer-name>#|<layer-name>?]", descriptors: null }, media: { prelude: "<media-query-list>", descriptors: null }, namespace: { prelude: "<namespace-prefix>? [<string>|<url>]", descriptors: null }, page: { prelude: "<page-selector-list>", descriptors: { bleed: "auto|<length>", marks: "none|[crop||cross]", "page-orientation": "upright|rotate-left|rotate-right", size: "<length [0,\u221E]>{1,2}|auto|[<page-size>||[portrait|landscape]]" } }, "position-try": { prelude: "<dashed-ident>", descriptors: { top: "<'top'>", left: "<'left'>", bottom: "<'bottom'>", right: "<'right'>", "inset-block-start": "<'inset-block-start'>", "inset-block-end": "<'inset-block-end'>", "inset-inline-start": "<'inset-inline-start'>", "inset-inline-end": "<'inset-inline-end'>", "inset-block": "<'inset-block'>", "inset-inline": "<'inset-inline'>", inset: "<'inset'>", "margin-top": "<'margin-top'>", "margin-left": "<'margin-left'>", "margin-bottom": "<'margin-bottom'>", "margin-right": "<'margin-right'>", "margin-block-start": "<'margin-block-start'>", "margin-block-end": "<'margin-block-end'>", "margin-inline-start": "<'margin-inline-start'>", "margin-inline-end": "<'margin-inline-end'>", margin: "<'margin'>", "margin-block": "<'margin-block'>", "margin-inline": "<'margin-inline'>", width: "<'width'>", height: "<'height'>", "min-width": "<'min-width'>", "min-height": "<'min-height'>", "max-width": "<'max-width'>", "max-height": "<'max-height'>", "block-size": "<'block-size'>", "inline-size": "<'inline-size'>", "min-block-size": "<'min-block-size'>", "min-inline-size": "<'min-inline-size'>", "max-block-size": "<'max-block-size'>", "max-inline-size": "<'max-inline-size'>", "align-self": "<'align-self'>|anchor-center", "justify-self": "<'justify-self'>|anchor-center" } }, property: { prelude: "<custom-property-name>", descriptors: { inherits: "true|false", "initial-value": "<declaration-value>?", syntax: "<string>" } }, scope: { prelude: "[( <scope-start> )]? [to ( <scope-end> )]?", descriptors: null }, "starting-style": { prelude: null, descriptors: null }, supports: { prelude: "<supports-condition>", descriptors: null }, "view-transition": { prelude: null, descriptors: { navigation: "auto|none", types: "none|<custom-ident>+" } }, "font-features-values": { prelude: "[<string>|<custom-ident>]+", descriptors: { "font-display": "auto|block|swap|fallback|optional" } } } };
var Lt = {};
f(Lt, { AnPlusB: () => un, Atrule: () => hn, AtrulePrelude: () => dn, AttributeSelector: () => bn, Block: () => xn, Brackets: () => wn, CDC: () => Sn, CDO: () => Tn, ClassSelector: () => An, Combinator: () => zn, Comment: () => In, Condition: () => Dn, Declaration: () => Fn, DeclarationList: () => Bn, Dimension: () => Wn, Feature: () => jn, FeatureFunction: () => Hn, FeatureRange: () => Kn, Function: () => Qn, GeneralEnclosed: () => $n, Hash: () => Jn, IdSelector: () => ni, Identifier: () => ti, Layer: () => oi, LayerList: () => si, MediaQuery: () => ci, MediaQueryList: () => pi, NestingSelector: () => mi, Nth: () => fi, Number: () => bi, Operator: () => xi, Parentheses: () => wi, Percentage: () => Si, PseudoClassSelector: () => Ti, PseudoElementSelector: () => Ai, Ratio: () => zi, Raw: () => Ii, Rule: () => Di, Scope: () => Fi, Selector: () => Mi, SelectorList: () => _i, String: () => Hi, StyleSheet: () => Yi, SupportsDeclaration: () => Vi, TypeSelector: () => $i, UnicodeRange: () => eo, Url: () => ao, Value: () => lo, WhiteSpace: () => uo });
var un = {};
f(un, { generate: () => cu, name: () => su, parse: () => cn, structure: () => lu });
var fe = 43;
var ie = 45;
var ir = 110;
var Re = true;
var au = false;
function or2(e2, t) {
  let r = this.tokenStart + e2, n = this.charCodeAt(r);
  for ((n === fe || n === ie) && (t && this.error("Number sign is not allowed"), r++); r < this.tokenEnd; r++) O(this.charCodeAt(r)) || this.error("Integer is expected", r);
}
function nt(e2) {
  return or2.call(this, 0, e2);
}
function ze(e2, t) {
  if (!this.cmpChar(this.tokenStart + e2, t)) {
    let r = "";
    switch (t) {
      case ir:
        r = "N is expected";
        break;
      case ie:
        r = "HyphenMinus is expected";
        break;
    }
    this.error(r, this.tokenStart + e2);
  }
}
function ln() {
  let e2 = 0, t = 0, r = this.tokenType;
  for (; r === 13 || r === 25; ) r = this.lookupType(++e2);
  if (r !== 10) if (this.isDelim(fe, e2) || this.isDelim(ie, e2)) {
    t = this.isDelim(fe, e2) ? fe : ie;
    do
      r = this.lookupType(++e2);
    while (r === 13 || r === 25);
    r !== 10 && (this.skip(e2), nt.call(this, Re));
  } else return null;
  return e2 > 0 && this.skip(e2), t === 0 && (r = this.charCodeAt(this.tokenStart), r !== fe && r !== ie && this.error("Number sign is expected")), nt.call(this, t !== 0), t === ie ? "-" + this.consume(10) : this.consume(10);
}
var su = "AnPlusB";
var lu = { a: [String, null], b: [String, null] };
function cn() {
  let e2 = this.tokenStart, t = null, r = null;
  if (this.tokenType === 10) nt.call(this, au), r = this.consume(10);
  else if (this.tokenType === 1 && this.cmpChar(this.tokenStart, ie)) switch (t = "-1", ze.call(this, 1, ir), this.tokenEnd - this.tokenStart) {
    case 2:
      this.next(), r = ln.call(this);
      break;
    case 3:
      ze.call(this, 2, ie), this.next(), this.skipSC(), nt.call(this, Re), r = "-" + this.consume(10);
      break;
    default:
      ze.call(this, 2, ie), or2.call(this, 3, Re), this.next(), r = this.substrToCursor(e2 + 2);
  }
  else if (this.tokenType === 1 || this.isDelim(fe) && this.lookupType(1) === 1) {
    let n = 0;
    switch (t = "1", this.isDelim(fe) && (n = 1, this.next()), ze.call(this, 0, ir), this.tokenEnd - this.tokenStart) {
      case 1:
        this.next(), r = ln.call(this);
        break;
      case 2:
        ze.call(this, 1, ie), this.next(), this.skipSC(), nt.call(this, Re), r = "-" + this.consume(10);
        break;
      default:
        ze.call(this, 1, ie), or2.call(this, 2, Re), this.next(), r = this.substrToCursor(e2 + n + 1);
    }
  } else if (this.tokenType === 12) {
    let n = this.charCodeAt(this.tokenStart), i = n === fe || n === ie, o = this.tokenStart + i;
    for (; o < this.tokenEnd && O(this.charCodeAt(o)); o++) ;
    o === this.tokenStart + i && this.error("Integer is expected", this.tokenStart + i), ze.call(this, o - this.tokenStart, ir), t = this.substring(e2, o), o + 1 === this.tokenEnd ? (this.next(), r = ln.call(this)) : (ze.call(this, o - this.tokenStart + 1, ie), o + 2 === this.tokenEnd ? (this.next(), this.skipSC(), nt.call(this, Re), r = "-" + this.consume(10)) : (or2.call(this, o - this.tokenStart + 2, Re), this.next(), r = this.substrToCursor(o + 1)));
  } else this.error();
  return t !== null && t.charCodeAt(0) === fe && (t = t.substr(1)), r !== null && r.charCodeAt(0) === fe && (r = r.substr(1)), { type: "AnPlusB", loc: this.getLocation(e2, this.tokenStart), a: t, b: r };
}
function cu(e2) {
  if (e2.a) {
    let t = e2.a === "+1" && "n" || e2.a === "1" && "n" || e2.a === "-1" && "-n" || e2.a + "n";
    if (e2.b) {
      let r = e2.b[0] === "-" || e2.b[0] === "+" ? e2.b : "+" + e2.b;
      this.tokenize(t + r);
    } else this.tokenize(t);
  } else this.tokenize(e2.b);
}
var hn = {};
f(hn, { generate: () => du, name: () => pu, parse: () => pn, structure: () => mu, walkContext: () => hu });
function Ka() {
  return this.Raw(this.consumeUntilLeftCurlyBracketOrSemicolon, true);
}
function uu() {
  for (let e2 = 1, t; t = this.lookupType(e2); e2++) {
    if (t === 24) return true;
    if (t === 23 || t === 3) return false;
  }
  return false;
}
var pu = "Atrule";
var hu = "atrule";
var mu = { name: String, prelude: ["AtrulePrelude", "Raw", null], block: ["Block", null] };
function pn(e2 = false) {
  let t = this.tokenStart, r, n, i = null, o = null;
  switch (this.eat(3), r = this.substrToCursor(t + 1), n = r.toLowerCase(), this.skipSC(), this.eof === false && this.tokenType !== 23 && this.tokenType !== 17 && (this.parseAtrulePrelude ? i = this.parseWithFallback(this.AtrulePrelude.bind(this, r, e2), Ka) : i = Ka.call(this, this.tokenIndex), this.skipSC()), this.tokenType) {
    case 17:
      this.next();
      break;
    case 23:
      hasOwnProperty.call(this.atrule, n) && typeof this.atrule[n].block == "function" ? o = this.atrule[n].block.call(this, e2) : o = this.Block(uu.call(this));
      break;
  }
  return { type: "Atrule", loc: this.getLocation(t, this.tokenStart), name: r, prelude: i, block: o };
}
function du(e2) {
  this.token(3, "@" + e2.name), e2.prelude !== null && this.node(e2.prelude), e2.block ? this.node(e2.block) : this.token(17, ";");
}
var dn = {};
f(dn, { generate: () => yu, name: () => fu, parse: () => mn, structure: () => bu, walkContext: () => gu });
var fu = "AtrulePrelude";
var gu = "atrulePrelude";
var bu = { children: [[]] };
function mn(e2) {
  let t = null;
  return e2 !== null && (e2 = e2.toLowerCase()), this.skipSC(), hasOwnProperty.call(this.atrule, e2) && typeof this.atrule[e2].prelude == "function" ? t = this.atrule[e2].prelude.call(this) : t = this.readSequence(this.scope.AtrulePrelude), this.skipSC(), this.eof !== true && this.tokenType !== 23 && this.tokenType !== 17 && this.error("Semicolon or block is expected"), { type: "AtrulePrelude", loc: this.getLocationFromList(t), children: t };
}
function yu(e2) {
  this.children(e2);
}
var bn = {};
f(bn, { generate: () => Lu, name: () => Cu, parse: () => gn, structure: () => Tu });
var xu = 36;
var Va = 42;
var ar = 61;
var ku = 94;
var fn = 124;
var wu = 126;
function vu() {
  this.eof && this.error("Unexpected end of input");
  let e2 = this.tokenStart, t = false;
  return this.isDelim(Va) ? (t = true, this.next()) : this.isDelim(fn) || this.eat(1), this.isDelim(fn) ? this.charCodeAt(this.tokenStart + 1) !== ar ? (this.next(), this.eat(1)) : t && this.error("Identifier is expected", this.tokenEnd) : t && this.error("Vertical line is expected"), { type: "Identifier", loc: this.getLocation(e2, this.tokenStart), name: this.substrToCursor(e2) };
}
function Su() {
  let e2 = this.tokenStart, t = this.charCodeAt(e2);
  return t !== ar && t !== wu && t !== ku && t !== xu && t !== Va && t !== fn && this.error("Attribute selector (=, ~=, ^=, $=, *=, |=) is expected"), this.next(), t !== ar && (this.isDelim(ar) || this.error("Equal sign is expected"), this.next()), this.substrToCursor(e2);
}
var Cu = "AttributeSelector";
var Tu = { name: "Identifier", matcher: [String, null], value: ["String", "Identifier", null], flags: [String, null] };
function gn() {
  let e2 = this.tokenStart, t, r = null, n = null, i = null;
  return this.eat(19), this.skipSC(), t = vu.call(this), this.skipSC(), this.tokenType !== 20 && (this.tokenType !== 1 && (r = Su.call(this), this.skipSC(), n = this.tokenType === 5 ? this.String() : this.Identifier(), this.skipSC()), this.tokenType === 1 && (i = this.consume(1), this.skipSC())), this.eat(20), { type: "AttributeSelector", loc: this.getLocation(e2, this.tokenStart), name: t, matcher: r, value: n, flags: i };
}
function Lu(e2) {
  this.token(9, "["), this.node(e2.name), e2.matcher !== null && (this.tokenize(e2.matcher), this.node(e2.value)), e2.flags !== null && this.token(1, e2.flags), this.token(9, "]");
}
var xn = {};
f(xn, { generate: () => Nu, name: () => zu, parse: () => yn, structure: () => Iu, walkContext: () => Pu });
var Au = 38;
function $a() {
  return this.Raw(null, true);
}
function Qa() {
  return this.parseWithFallback(this.Rule, $a);
}
function Xa() {
  return this.Raw(this.consumeUntilSemicolonIncluded, true);
}
function Eu() {
  if (this.tokenType === 17) return Xa.call(this, this.tokenIndex);
  let e2 = this.parseWithFallback(this.Declaration, Xa);
  return this.tokenType === 17 && this.next(), e2;
}
var zu = "Block";
var Pu = "block";
var Iu = { children: [["Atrule", "Rule", "Declaration"]] };
function yn(e2) {
  let t = e2 ? Eu : Qa, r = this.tokenStart, n = this.createList();
  this.eat(23);
  e: for (; !this.eof; ) switch (this.tokenType) {
    case 24:
      break e;
    case 13:
    case 25:
      this.next();
      break;
    case 3:
      n.push(this.parseWithFallback(this.Atrule.bind(this, e2), $a));
      break;
    default:
      e2 && this.isDelim(Au) ? n.push(Qa.call(this)) : n.push(t.call(this));
  }
  return this.eof || this.eat(24), { type: "Block", loc: this.getLocation(r, this.tokenStart), children: n };
}
function Nu(e2) {
  this.token(23, "{"), this.children(e2, (t) => {
    t.type === "Declaration" && this.token(17, ";");
  }), this.token(24, "}");
}
var wn = {};
f(wn, { generate: () => Fu, name: () => Du, parse: () => kn, structure: () => Ou });
var Du = "Brackets";
var Ou = { children: [[]] };
function kn(e2, t) {
  let r = this.tokenStart, n = null;
  return this.eat(19), n = e2.call(this, t), this.eof || this.eat(20), { type: "Brackets", loc: this.getLocation(r, this.tokenStart), children: n };
}
function Fu(e2) {
  this.token(9, "["), this.children(e2), this.token(9, "]");
}
var Sn = {};
f(Sn, { generate: () => Bu, name: () => Ru, parse: () => vn, structure: () => Mu });
var Ru = "CDC";
var Mu = [];
function vn() {
  let e2 = this.tokenStart;
  return this.eat(15), { type: "CDC", loc: this.getLocation(e2, this.tokenStart) };
}
function Bu() {
  this.token(15, "-->");
}
var Tn = {};
f(Tn, { generate: () => qu, name: () => _u, parse: () => Cn, structure: () => Wu });
var _u = "CDO";
var Wu = [];
function Cn() {
  let e2 = this.tokenStart;
  return this.eat(14), { type: "CDO", loc: this.getLocation(e2, this.tokenStart) };
}
function qu() {
  this.token(14, "<!--");
}
var An = {};
f(An, { generate: () => Gu, name: () => Uu, parse: () => Ln, structure: () => Hu });
var ju = 46;
var Uu = "ClassSelector";
var Hu = { name: String };
function Ln() {
  return this.eatDelim(ju), { type: "ClassSelector", loc: this.getLocation(this.tokenStart - 1, this.tokenEnd), name: this.consume(1) };
}
function Gu(e2) {
  this.token(9, "."), this.token(1, e2.name);
}
var zn = {};
f(zn, { generate: () => $u, name: () => Qu, parse: () => En, structure: () => Xu });
var Yu = 43;
var Za = 47;
var Ku = 62;
var Vu = 126;
var Qu = "Combinator";
var Xu = { name: String };
function En() {
  let e2 = this.tokenStart, t;
  switch (this.tokenType) {
    case 13:
      t = " ";
      break;
    case 9:
      switch (this.charCodeAt(this.tokenStart)) {
        case Ku:
        case Yu:
        case Vu:
          this.next();
          break;
        case Za:
          this.next(), this.eatIdent("deep"), this.eatDelim(Za);
          break;
        default:
          this.error("Combinator is expected");
      }
      t = this.substrToCursor(e2);
      break;
  }
  return { type: "Combinator", loc: this.getLocation(e2, this.tokenStart), name: t };
}
function $u(e2) {
  this.tokenize(e2.name);
}
var In = {};
f(In, { generate: () => rp, name: () => ep, parse: () => Pn, structure: () => tp });
var Zu = 42;
var Ju = 47;
var ep = "Comment";
var tp = { value: String };
function Pn() {
  let e2 = this.tokenStart, t = this.tokenEnd;
  return this.eat(25), t - e2 + 2 >= 2 && this.charCodeAt(t - 2) === Zu && this.charCodeAt(t - 1) === Ju && (t -= 2), { type: "Comment", loc: this.getLocation(e2, this.tokenStart), value: this.substring(e2 + 2, t) };
}
function rp(e2) {
  this.token(25, "/*" + e2.value + "*/");
}
var Dn = {};
f(Dn, { generate: () => sp, name: () => ip, parse: () => Nn, structure: () => op });
var np = /* @__PURE__ */ new Set([16, 22, 0]);
var ip = "Condition";
var op = { kind: String, children: [["Identifier", "Feature", "FeatureFunction", "FeatureRange", "SupportsDeclaration"]] };
function Ja(e2) {
  return this.lookupTypeNonSC(1) === 1 && np.has(this.lookupTypeNonSC(2)) ? this.Feature(e2) : this.FeatureRange(e2);
}
var ap = { media: Ja, container: Ja, supports() {
  return this.SupportsDeclaration();
} };
function Nn(e2 = "media") {
  let t = this.createList();
  e: for (; !this.eof; ) switch (this.tokenType) {
    case 25:
    case 13:
      this.next();
      continue;
    case 1:
      t.push(this.Identifier());
      break;
    case 21: {
      let r = this.parseWithFallback(() => ap[e2].call(this, e2), () => null);
      r || (r = this.parseWithFallback(() => {
        this.eat(21);
        let n = this.Condition(e2);
        return this.eat(22), n;
      }, () => this.GeneralEnclosed(e2))), t.push(r);
      break;
    }
    case 2: {
      let r = this.parseWithFallback(() => this.FeatureFunction(e2), () => null);
      r || (r = this.GeneralEnclosed(e2)), t.push(r);
      break;
    }
    default:
      break e;
  }
  return t.isEmpty && this.error("Condition is expected"), { type: "Condition", loc: this.getLocationFromList(t), kind: e2, children: t };
}
function sp(e2) {
  e2.children.forEach((t) => {
    t.type === "Condition" ? (this.token(21, "("), this.node(t), this.token(22, ")")) : this.node(t);
  });
}
var Fn = {};
f(Fn, { generate: () => xp, name: () => gp, parse: () => On, structure: () => yp, walkContext: () => bp });
var ts = 33;
var lp = 35;
var cp = 36;
var up = 38;
var pp = 42;
var hp = 43;
var es = 47;
function mp() {
  return this.Raw(this.consumeUntilExclamationMarkOrSemicolon, true);
}
function dp() {
  return this.Raw(this.consumeUntilExclamationMarkOrSemicolon, false);
}
function fp() {
  let e2 = this.tokenIndex, t = this.Value();
  return t.type !== "Raw" && this.eof === false && this.tokenType !== 17 && this.isDelim(ts) === false && this.isBalanceEdge(e2) === false && this.error(), t;
}
var gp = "Declaration";
var bp = "declaration";
var yp = { important: [Boolean, String], property: String, value: ["Value", "Raw"] };
function On() {
  let e2 = this.tokenStart, t = this.tokenIndex, r = kp.call(this), n = Yt(r), i = n ? this.parseCustomProperty : this.parseValue, o = n ? dp : mp, a = false, c;
  this.skipSC(), this.eat(16);
  let u = this.tokenIndex;
  if (n || this.skipSC(), i ? c = this.parseWithFallback(fp, o) : c = o.call(this, this.tokenIndex), n && c.type === "Value" && c.children.isEmpty) {
    for (let s = u - this.tokenIndex; s <= 0; s++) if (this.lookupType(s) === 13) {
      c.children.appendData({ type: "WhiteSpace", loc: null, value: " " });
      break;
    }
  }
  return this.isDelim(ts) && (a = wp.call(this), this.skipSC()), this.eof === false && this.tokenType !== 17 && this.isBalanceEdge(t) === false && this.error(), { type: "Declaration", loc: this.getLocation(e2, this.tokenStart), important: a, property: r, value: c };
}
function xp(e2) {
  this.token(1, e2.property), this.token(16, ":"), this.node(e2.value), e2.important && (this.token(9, "!"), this.token(1, e2.important === true ? "important" : e2.important));
}
function kp() {
  let e2 = this.tokenStart;
  if (this.tokenType === 9) switch (this.charCodeAt(this.tokenStart)) {
    case pp:
    case cp:
    case hp:
    case lp:
    case up:
      this.next();
      break;
    case es:
      this.next(), this.isDelim(es) && this.next();
      break;
  }
  return this.tokenType === 4 ? this.eat(4) : this.eat(1), this.substrToCursor(e2);
}
function wp() {
  this.eat(9), this.skipSC();
  let e2 = this.consume(1);
  return e2 === "important" ? true : e2;
}
var Bn = {};
f(Bn, { generate: () => Tp, name: () => Sp, parse: () => Mn, structure: () => Cp });
var vp = 38;
function Rn() {
  return this.Raw(this.consumeUntilSemicolonIncluded, true);
}
var Sp = "DeclarationList";
var Cp = { children: [["Declaration", "Atrule", "Rule"]] };
function Mn() {
  let e2 = this.createList();
  for (; !this.eof; ) switch (this.tokenType) {
    case 13:
    case 25:
    case 17:
      this.next();
      break;
    case 3:
      e2.push(this.parseWithFallback(this.Atrule.bind(this, true), Rn));
      break;
    default:
      this.isDelim(vp) ? e2.push(this.parseWithFallback(this.Rule, Rn)) : e2.push(this.parseWithFallback(this.Declaration, Rn));
  }
  return { type: "DeclarationList", loc: this.getLocationFromList(e2), children: e2 };
}
function Tp(e2) {
  this.children(e2, (t) => {
    t.type === "Declaration" && this.token(17, ";");
  });
}
var Wn = {};
f(Wn, { generate: () => Ep, name: () => Lp, parse: () => _n, structure: () => Ap });
var Lp = "Dimension";
var Ap = { value: String, unit: String };
function _n() {
  let e2 = this.tokenStart, t = this.consumeNumber(12);
  return { type: "Dimension", loc: this.getLocation(e2, this.tokenStart), value: t, unit: this.substring(e2 + t.length, this.tokenStart) };
}
function Ep(e2) {
  this.token(12, e2.value + e2.unit);
}
var jn = {};
f(jn, { generate: () => Np, name: () => Pp, parse: () => qn, structure: () => Ip });
var zp = 47;
var Pp = "Feature";
var Ip = { kind: String, name: String, value: ["Identifier", "Number", "Dimension", "Ratio", "Function", null] };
function qn(e2) {
  let t = this.tokenStart, r, n = null;
  if (this.eat(21), this.skipSC(), r = this.consume(1), this.skipSC(), this.tokenType !== 22) {
    switch (this.eat(16), this.skipSC(), this.tokenType) {
      case 10:
        this.lookupNonWSType(1) === 9 ? n = this.Ratio() : n = this.Number();
        break;
      case 12:
        n = this.Dimension();
        break;
      case 1:
        n = this.Identifier();
        break;
      case 2:
        n = this.parseWithFallback(() => {
          let i = this.Function(this.readSequence, this.scope.Value);
          return this.skipSC(), this.isDelim(zp) && this.error(), i;
        }, () => this.Ratio());
        break;
      default:
        this.error("Number, dimension, ratio or identifier is expected");
    }
    this.skipSC();
  }
  return this.eof || this.eat(22), { type: "Feature", loc: this.getLocation(t, this.tokenStart), kind: e2, name: r, value: n };
}
function Np(e2) {
  this.token(21, "("), this.token(1, e2.name), e2.value !== null && (this.token(16, ":"), this.node(e2.value)), this.token(22, ")");
}
var Hn = {};
f(Hn, { generate: () => Rp, name: () => Dp, parse: () => Un, structure: () => Op });
var Dp = "FeatureFunction";
var Op = { kind: String, feature: String, value: ["Declaration", "Selector"] };
function Fp(e2, t) {
  let n = (this.features[e2] || {})[t];
  return typeof n != "function" && this.error(`Unknown feature ${t}()`), n;
}
function Un(e2 = "unknown") {
  let t = this.tokenStart, r = this.consumeFunctionName(), n = Fp.call(this, e2, r.toLowerCase());
  this.skipSC();
  let i = this.parseWithFallback(() => {
    let o = this.tokenIndex, a = n.call(this);
    return this.eof === false && this.isBalanceEdge(o) === false && this.error(), a;
  }, () => this.Raw(null, false));
  return this.eof || this.eat(22), { type: "FeatureFunction", loc: this.getLocation(t, this.tokenStart), kind: e2, feature: r, value: i };
}
function Rp(e2) {
  this.token(2, e2.feature + "("), this.node(e2.value), this.token(22, ")");
}
var Kn = {};
f(Kn, { generate: () => qp, name: () => _p, parse: () => Yn, structure: () => Wp });
var rs = 47;
var Mp = 60;
var ns = 61;
var Bp = 62;
var _p = "FeatureRange";
var Wp = { kind: String, left: ["Identifier", "Number", "Dimension", "Ratio", "Function"], leftComparison: String, middle: ["Identifier", "Number", "Dimension", "Ratio", "Function"], rightComparison: [String, null], right: ["Identifier", "Number", "Dimension", "Ratio", "Function", null] };
function Gn() {
  switch (this.skipSC(), this.tokenType) {
    case 10:
      return this.isDelim(rs, this.lookupOffsetNonSC(1)) ? this.Ratio() : this.Number();
    case 12:
      return this.Dimension();
    case 1:
      return this.Identifier();
    case 2:
      return this.parseWithFallback(() => {
        let e2 = this.Function(this.readSequence, this.scope.Value);
        return this.skipSC(), this.isDelim(rs) && this.error(), e2;
      }, () => this.Ratio());
    default:
      this.error("Number, dimension, ratio or identifier is expected");
  }
}
function is3(e2) {
  if (this.skipSC(), this.isDelim(Mp) || this.isDelim(Bp)) {
    let t = this.source[this.tokenStart];
    return this.next(), this.isDelim(ns) ? (this.next(), t + "=") : t;
  }
  if (this.isDelim(ns)) return "=";
  this.error(`Expected ${e2 ? '":", ' : ""}"<", ">", "=" or ")"`);
}
function Yn(e2 = "unknown") {
  let t = this.tokenStart;
  this.skipSC(), this.eat(21);
  let r = Gn.call(this), n = is3.call(this, r.type === "Identifier"), i = Gn.call(this), o = null, a = null;
  return this.lookupNonWSType(0) !== 22 && (o = is3.call(this), a = Gn.call(this)), this.skipSC(), this.eat(22), { type: "FeatureRange", loc: this.getLocation(t, this.tokenStart), kind: e2, left: r, leftComparison: n, middle: i, rightComparison: o, right: a };
}
function qp(e2) {
  this.token(21, "("), this.node(e2.left), this.tokenize(e2.leftComparison), this.node(e2.middle), e2.right && (this.tokenize(e2.rightComparison), this.node(e2.right)), this.token(22, ")");
}
var Qn = {};
f(Qn, { generate: () => Gp, name: () => jp, parse: () => Vn, structure: () => Hp, walkContext: () => Up });
var jp = "Function";
var Up = "function";
var Hp = { name: String, children: [[]] };
function Vn(e2, t) {
  let r = this.tokenStart, n = this.consumeFunctionName(), i = n.toLowerCase(), o;
  return o = t.hasOwnProperty(i) ? t[i].call(this, t) : e2.call(this, t), this.eof || this.eat(22), { type: "Function", loc: this.getLocation(r, this.tokenStart), name: n, children: o };
}
function Gp(e2) {
  this.token(2, e2.name + "("), this.children(e2), this.token(22, ")");
}
var $n = {};
f($n, { generate: () => Vp, name: () => Yp, parse: () => Xn, structure: () => Kp });
var Yp = "GeneralEnclosed";
var Kp = { kind: String, function: [String, null], children: [[]] };
function Xn(e2) {
  let t = this.tokenStart, r = null;
  this.tokenType === 2 ? r = this.consumeFunctionName() : this.eat(21);
  let n = this.parseWithFallback(() => {
    let i = this.tokenIndex, o = this.readSequence(this.scope.Value);
    return this.eof === false && this.isBalanceEdge(i) === false && this.error(), o;
  }, () => this.createSingleNodeList(this.Raw(null, false)));
  return this.eof || this.eat(22), { type: "GeneralEnclosed", loc: this.getLocation(t, this.tokenStart), kind: e2, function: r, children: n };
}
function Vp(e2) {
  e2.function ? this.token(2, e2.function + "(") : this.token(21, "("), this.children(e2), this.token(22, ")");
}
var Jn = {};
f(Jn, { generate: () => Zp, name: () => Xp, parse: () => Zn, structure: () => $p, xxx: () => Qp });
var Qp = "XXX";
var Xp = "Hash";
var $p = { value: String };
function Zn() {
  let e2 = this.tokenStart;
  return this.eat(4), { type: "Hash", loc: this.getLocation(e2, this.tokenStart), value: this.substrToCursor(e2 + 1) };
}
function Zp(e2) {
  this.token(4, "#" + e2.value);
}
var ti = {};
f(ti, { generate: () => th, name: () => Jp, parse: () => ei, structure: () => eh });
var Jp = "Identifier";
var eh = { name: String };
function ei() {
  return { type: "Identifier", loc: this.getLocation(this.tokenStart, this.tokenEnd), name: this.consume(1) };
}
function th(e2) {
  this.token(1, e2.name);
}
var ni = {};
f(ni, { generate: () => ih, name: () => rh, parse: () => ri, structure: () => nh });
var rh = "IdSelector";
var nh = { name: String };
function ri() {
  let e2 = this.tokenStart;
  return this.eat(4), { type: "IdSelector", loc: this.getLocation(e2, this.tokenStart), name: this.substrToCursor(e2 + 1) };
}
function ih(e2) {
  this.token(9, "#" + e2.name);
}
var oi = {};
f(oi, { generate: () => lh, name: () => ah, parse: () => ii, structure: () => sh });
var oh = 46;
var ah = "Layer";
var sh = { name: String };
function ii() {
  let e2 = this.tokenStart, t = this.consume(1);
  for (; this.isDelim(oh); ) this.eat(9), t += "." + this.consume(1);
  return { type: "Layer", loc: this.getLocation(e2, this.tokenStart), name: t };
}
function lh(e2) {
  this.tokenize(e2.name);
}
var si = {};
f(si, { generate: () => ph, name: () => ch, parse: () => ai, structure: () => uh });
var ch = "LayerList";
var uh = { children: [["Layer"]] };
function ai() {
  let e2 = this.createList();
  for (this.skipSC(); !this.eof && (e2.push(this.Layer()), this.lookupTypeNonSC(0) === 18); ) this.skipSC(), this.next(), this.skipSC();
  return { type: "LayerList", loc: this.getLocationFromList(e2), children: e2 };
}
function ph(e2) {
  this.children(e2, () => this.token(18, ","));
}
var ci = {};
f(ci, { generate: () => dh, name: () => hh, parse: () => li, structure: () => mh });
var hh = "MediaQuery";
var mh = { modifier: [String, null], mediaType: [String, null], condition: ["Condition", null] };
function li() {
  let e2 = this.tokenStart, t = null, r = null, n = null;
  if (this.skipSC(), this.tokenType === 1 && this.lookupTypeNonSC(1) !== 21) {
    let i = this.consume(1), o = i.toLowerCase();
    switch (o === "not" || o === "only" ? (this.skipSC(), t = o, r = this.consume(1)) : r = i, this.lookupTypeNonSC(0)) {
      case 1: {
        this.skipSC(), this.eatIdent("and"), n = this.Condition("media");
        break;
      }
      case 23:
      case 17:
      case 18:
      case 0:
        break;
      default:
        this.error("Identifier or parenthesis is expected");
    }
  } else switch (this.tokenType) {
    case 1:
    case 21:
    case 2: {
      n = this.Condition("media");
      break;
    }
    case 23:
    case 17:
    case 0:
      break;
    default:
      this.error("Identifier or parenthesis is expected");
  }
  return { type: "MediaQuery", loc: this.getLocation(e2, this.tokenStart), modifier: t, mediaType: r, condition: n };
}
function dh(e2) {
  e2.mediaType ? (e2.modifier && this.token(1, e2.modifier), this.token(1, e2.mediaType), e2.condition && (this.token(1, "and"), this.node(e2.condition))) : e2.condition && this.node(e2.condition);
}
var pi = {};
f(pi, { generate: () => bh, name: () => fh, parse: () => ui, structure: () => gh });
var fh = "MediaQueryList";
var gh = { children: [["MediaQuery"]] };
function ui() {
  let e2 = this.createList();
  for (this.skipSC(); !this.eof && (e2.push(this.MediaQuery()), this.tokenType === 18); ) this.next();
  return { type: "MediaQueryList", loc: this.getLocationFromList(e2), children: e2 };
}
function bh(e2) {
  this.children(e2, () => this.token(18, ","));
}
var mi = {};
f(mi, { generate: () => wh, name: () => xh, parse: () => hi, structure: () => kh });
var yh = 38;
var xh = "NestingSelector";
var kh = {};
function hi() {
  let e2 = this.tokenStart;
  return this.eatDelim(yh), { type: "NestingSelector", loc: this.getLocation(e2, this.tokenStart) };
}
function wh() {
  this.token(9, "&");
}
var fi = {};
f(fi, { generate: () => Ch, name: () => vh, parse: () => di, structure: () => Sh });
var vh = "Nth";
var Sh = { nth: ["AnPlusB", "Identifier"], selector: ["SelectorList", null] };
function di() {
  this.skipSC();
  let e2 = this.tokenStart, t = e2, r = null, n;
  return this.lookupValue(0, "odd") || this.lookupValue(0, "even") ? n = this.Identifier() : n = this.AnPlusB(), t = this.tokenStart, this.skipSC(), this.lookupValue(0, "of") && (this.next(), r = this.SelectorList(), t = this.tokenStart), { type: "Nth", loc: this.getLocation(e2, t), nth: n, selector: r };
}
function Ch(e2) {
  this.node(e2.nth), e2.selector !== null && (this.token(1, "of"), this.node(e2.selector));
}
var bi = {};
f(bi, { generate: () => Ah, name: () => Th, parse: () => gi, structure: () => Lh });
var Th = "Number";
var Lh = { value: String };
function gi() {
  return { type: "Number", loc: this.getLocation(this.tokenStart, this.tokenEnd), value: this.consume(10) };
}
function Ah(e2) {
  this.token(10, e2.value);
}
var xi = {};
f(xi, { generate: () => Ph, name: () => Eh, parse: () => yi, structure: () => zh });
var Eh = "Operator";
var zh = { value: String };
function yi() {
  let e2 = this.tokenStart;
  return this.next(), { type: "Operator", loc: this.getLocation(e2, this.tokenStart), value: this.substrToCursor(e2) };
}
function Ph(e2) {
  this.tokenize(e2.value);
}
var wi = {};
f(wi, { generate: () => Dh, name: () => Ih, parse: () => ki, structure: () => Nh });
var Ih = "Parentheses";
var Nh = { children: [[]] };
function ki(e2, t) {
  let r = this.tokenStart, n = null;
  return this.eat(21), n = e2.call(this, t), this.eof || this.eat(22), { type: "Parentheses", loc: this.getLocation(r, this.tokenStart), children: n };
}
function Dh(e2) {
  this.token(21, "("), this.children(e2), this.token(22, ")");
}
var Si = {};
f(Si, { generate: () => Rh, name: () => Oh, parse: () => vi, structure: () => Fh });
var Oh = "Percentage";
var Fh = { value: String };
function vi() {
  return { type: "Percentage", loc: this.getLocation(this.tokenStart, this.tokenEnd), value: this.consumeNumber(11) };
}
function Rh(e2) {
  this.token(11, e2.value + "%");
}
var Ti = {};
f(Ti, { generate: () => Wh, name: () => Mh, parse: () => Ci, structure: () => _h, walkContext: () => Bh });
var Mh = "PseudoClassSelector";
var Bh = "function";
var _h = { name: String, children: [["Raw"], null] };
function Ci() {
  let e2 = this.tokenStart, t = null, r, n;
  return this.eat(16), this.tokenType === 2 ? (r = this.consumeFunctionName(), n = r.toLowerCase(), this.lookupNonWSType(0) == 22 ? t = this.createList() : hasOwnProperty.call(this.pseudo, n) ? (this.skipSC(), t = this.pseudo[n].call(this), this.skipSC()) : (t = this.createList(), t.push(this.Raw(null, false))), this.eat(22)) : r = this.consume(1), { type: "PseudoClassSelector", loc: this.getLocation(e2, this.tokenStart), name: r, children: t };
}
function Wh(e2) {
  this.token(16, ":"), e2.children === null ? this.token(1, e2.name) : (this.token(2, e2.name + "("), this.children(e2), this.token(22, ")"));
}
var Ai = {};
f(Ai, { generate: () => Hh, name: () => qh, parse: () => Li, structure: () => Uh, walkContext: () => jh });
var qh = "PseudoElementSelector";
var jh = "function";
var Uh = { name: String, children: [["Raw"], null] };
function Li() {
  let e2 = this.tokenStart, t = null, r, n;
  return this.eat(16), this.eat(16), this.tokenType === 2 ? (r = this.consumeFunctionName(), n = r.toLowerCase(), this.lookupNonWSType(0) == 22 ? t = this.createList() : hasOwnProperty.call(this.pseudo, n) ? (this.skipSC(), t = this.pseudo[n].call(this), this.skipSC()) : (t = this.createList(), t.push(this.Raw(null, false))), this.eat(22)) : r = this.consume(1), { type: "PseudoElementSelector", loc: this.getLocation(e2, this.tokenStart), name: r, children: t };
}
function Hh(e2) {
  this.token(16, ":"), this.token(16, ":"), e2.children === null ? this.token(1, e2.name) : (this.token(2, e2.name + "("), this.children(e2), this.token(22, ")"));
}
var zi = {};
f(zi, { generate: () => Kh, name: () => Gh, parse: () => Ei, structure: () => Yh });
var os = 47;
function as() {
  switch (this.skipSC(), this.tokenType) {
    case 10:
      return this.Number();
    case 2:
      return this.Function(this.readSequence, this.scope.Value);
    default:
      this.error("Number of function is expected");
  }
}
var Gh = "Ratio";
var Yh = { left: ["Number", "Function"], right: ["Number", "Function", null] };
function Ei() {
  let e2 = this.tokenStart, t = as.call(this), r = null;
  return this.skipSC(), this.isDelim(os) && (this.eatDelim(os), r = as.call(this)), { type: "Ratio", loc: this.getLocation(e2, this.tokenStart), left: t, right: r };
}
function Kh(e2) {
  this.node(e2.left), this.token(9, "/"), e2.right ? this.node(e2.right) : this.node(10, 1);
}
var Ii = {};
f(Ii, { generate: () => $h, name: () => Qh, parse: () => Pi, structure: () => Xh });
function Vh() {
  return this.tokenIndex > 0 && this.lookupType(-1) === 13 ? this.tokenIndex > 1 ? this.getTokenStart(this.tokenIndex - 1) : this.firstCharOffset : this.tokenStart;
}
var Qh = "Raw";
var Xh = { value: String };
function Pi(e2, t) {
  let r = this.getTokenStart(this.tokenIndex), n;
  return this.skipUntilBalanced(this.tokenIndex, e2 || this.consumeUntilBalanceEnd), t && this.tokenStart > r ? n = Vh.call(this) : n = this.tokenStart, { type: "Raw", loc: this.getLocation(r, n), value: this.substring(r, n) };
}
function $h(e2) {
  this.tokenize(e2.value);
}
var Di = {};
f(Di, { generate: () => rm, name: () => Jh, parse: () => Ni, structure: () => tm, walkContext: () => em });
function ss() {
  return this.Raw(this.consumeUntilLeftCurlyBracket, true);
}
function Zh() {
  let e2 = this.SelectorList();
  return e2.type !== "Raw" && this.eof === false && this.tokenType !== 23 && this.error(), e2;
}
var Jh = "Rule";
var em = "rule";
var tm = { prelude: ["SelectorList", "Raw"], block: ["Block"] };
function Ni() {
  let e2 = this.tokenIndex, t = this.tokenStart, r, n;
  return this.parseRulePrelude ? r = this.parseWithFallback(Zh, ss) : r = ss.call(this, e2), n = this.Block(true), { type: "Rule", loc: this.getLocation(t, this.tokenStart), prelude: r, block: n };
}
function rm(e2) {
  this.node(e2.prelude), this.node(e2.block);
}
var Fi = {};
f(Fi, { generate: () => om, name: () => nm, parse: () => Oi, structure: () => im });
var nm = "Scope";
var im = { root: ["SelectorList", "Raw", null], limit: ["SelectorList", "Raw", null] };
function Oi() {
  let e2 = null, t = null;
  this.skipSC();
  let r = this.tokenStart;
  return this.tokenType === 21 && (this.next(), this.skipSC(), e2 = this.parseWithFallback(this.SelectorList, () => this.Raw(false, true)), this.skipSC(), this.eat(22)), this.lookupNonWSType(0) === 1 && (this.skipSC(), this.eatIdent("to"), this.skipSC(), this.eat(21), this.skipSC(), t = this.parseWithFallback(this.SelectorList, () => this.Raw(false, true)), this.skipSC(), this.eat(22)), { type: "Scope", loc: this.getLocation(r, this.tokenStart), root: e2, limit: t };
}
function om(e2) {
  e2.root && (this.token(21, "("), this.node(e2.root), this.token(22, ")")), e2.limit && (this.token(1, "to"), this.token(21, "("), this.node(e2.limit), this.token(22, ")"));
}
var Mi = {};
f(Mi, { generate: () => lm, name: () => am, parse: () => Ri, structure: () => sm });
var am = "Selector";
var sm = { children: [["TypeSelector", "IdSelector", "ClassSelector", "AttributeSelector", "PseudoClassSelector", "PseudoElementSelector", "Combinator"]] };
function Ri() {
  let e2 = this.readSequence(this.scope.Selector);
  return this.getFirstListNode(e2) === null && this.error("Selector is expected"), { type: "Selector", loc: this.getLocationFromList(e2), children: e2 };
}
function lm(e2) {
  this.children(e2);
}
var _i = {};
f(_i, { generate: () => hm, name: () => cm, parse: () => Bi, structure: () => pm, walkContext: () => um });
var cm = "SelectorList";
var um = "selector";
var pm = { children: [["Selector", "Raw"]] };
function Bi() {
  let e2 = this.createList();
  for (; !this.eof; ) {
    if (e2.push(this.Selector()), this.tokenType === 18) {
      this.next();
      continue;
    }
    break;
  }
  return { type: "SelectorList", loc: this.getLocationFromList(e2), children: e2 };
}
function hm(e2) {
  this.children(e2, () => this.token(18, ","));
}
var Hi = {};
f(Hi, { generate: () => fm, name: () => mm, parse: () => Ui, structure: () => dm });
var ji = {};
f(ji, { decode: () => Ct, encode: () => qi });
var Wi = 92;
var ls = 34;
var cs = 39;
function Ct(e2) {
  let t = e2.length, r = e2.charCodeAt(0), n = r === ls || r === cs ? 1 : 0, i = n === 1 && t > 1 && e2.charCodeAt(t - 1) === r ? t - 2 : t - 1, o = "";
  for (let a = n; a <= i; a++) {
    let c = e2.charCodeAt(a);
    if (c === Wi) {
      if (a === i) {
        a !== t - 1 && (o = e2.substr(a + 1));
        break;
      }
      if (c = e2.charCodeAt(++a), V(Wi, c)) {
        let u = a - 1, s = se(e2, u);
        a = s - 1, o += Ue(e2.substring(u + 1, s));
      } else c === 13 && e2.charCodeAt(a + 1) === 10 && a++;
    } else o += e2[a];
  }
  return o;
}
function qi(e2, t) {
  let r = t ? "'" : '"', n = t ? cs : ls, i = "", o = false;
  for (let a = 0; a < e2.length; a++) {
    let c = e2.charCodeAt(a);
    if (c === 0) {
      i += "\uFFFD";
      continue;
    }
    if (c <= 31 || c === 127) {
      i += "\\" + c.toString(16), o = true;
      continue;
    }
    c === n || c === Wi ? (i += "\\" + e2.charAt(a), o = false) : (o && (te(c) || me(c)) && (i += " "), i += e2.charAt(a), o = false);
  }
  return r + i + r;
}
var mm = "String";
var dm = { value: String };
function Ui() {
  return { type: "String", loc: this.getLocation(this.tokenStart, this.tokenEnd), value: Ct(this.consume(5)) };
}
function fm(e2) {
  this.token(5, qi(e2.value));
}
var Yi = {};
f(Yi, { generate: () => km, name: () => bm, parse: () => Gi, structure: () => xm, walkContext: () => ym });
var gm = 33;
function us() {
  return this.Raw(null, false);
}
var bm = "StyleSheet";
var ym = "stylesheet";
var xm = { children: [["Comment", "CDO", "CDC", "Atrule", "Rule", "Raw"]] };
function Gi() {
  let e2 = this.tokenStart, t = this.createList(), r;
  for (; !this.eof; ) {
    switch (this.tokenType) {
      case 13:
        this.next();
        continue;
      case 25:
        if (this.charCodeAt(this.tokenStart + 2) !== gm) {
          this.next();
          continue;
        }
        r = this.Comment();
        break;
      case 14:
        r = this.CDO();
        break;
      case 15:
        r = this.CDC();
        break;
      case 3:
        r = this.parseWithFallback(this.Atrule, us);
        break;
      default:
        r = this.parseWithFallback(this.Rule, us);
    }
    t.push(r);
  }
  return { type: "StyleSheet", loc: this.getLocation(e2, this.tokenStart), children: t };
}
function km(e2) {
  this.children(e2);
}
var Vi = {};
f(Vi, { generate: () => Sm, name: () => wm, parse: () => Ki, structure: () => vm });
var wm = "SupportsDeclaration";
var vm = { declaration: "Declaration" };
function Ki() {
  let e2 = this.tokenStart;
  this.eat(21), this.skipSC();
  let t = this.Declaration();
  return this.eof || this.eat(22), { type: "SupportsDeclaration", loc: this.getLocation(e2, this.tokenStart), declaration: t };
}
function Sm(e2) {
  this.token(21, "("), this.node(e2.declaration), this.token(22, ")");
}
var $i = {};
f($i, { generate: () => Am, name: () => Tm, parse: () => Xi, structure: () => Lm });
var Cm = 42;
var ps = 124;
function Qi() {
  this.tokenType !== 1 && this.isDelim(Cm) === false && this.error("Identifier or asterisk is expected"), this.next();
}
var Tm = "TypeSelector";
var Lm = { name: String };
function Xi() {
  let e2 = this.tokenStart;
  return this.isDelim(ps) ? (this.next(), Qi.call(this)) : (Qi.call(this), this.isDelim(ps) && (this.next(), Qi.call(this))), { type: "TypeSelector", loc: this.getLocation(e2, this.tokenStart), name: this.substrToCursor(e2) };
}
function Am(e2) {
  this.tokenize(e2.name);
}
var eo = {};
f(eo, { generate: () => Nm, name: () => Pm, parse: () => Ji, structure: () => Im });
var hs = 43;
var ms = 45;
var Zi = 63;
function Tt(e2, t) {
  let r = 0;
  for (let n = this.tokenStart + e2; n < this.tokenEnd; n++) {
    let i = this.charCodeAt(n);
    if (i === ms && t && r !== 0) return Tt.call(this, e2 + r + 1, false), -1;
    te(i) || this.error(t && r !== 0 ? "Hyphen minus" + (r < 6 ? " or hex digit" : "") + " is expected" : r < 6 ? "Hex digit is expected" : "Unexpected input", n), ++r > 6 && this.error("Too many hex digits", n);
  }
  return this.next(), r;
}
function sr(e2) {
  let t = 0;
  for (; this.isDelim(Zi); ) ++t > e2 && this.error("Too many question marks"), this.next();
}
function Em(e2) {
  this.charCodeAt(this.tokenStart) !== e2 && this.error((e2 === hs ? "Plus sign" : "Hyphen minus") + " is expected");
}
function zm() {
  let e2 = 0;
  switch (this.tokenType) {
    case 10:
      if (e2 = Tt.call(this, 1, true), this.isDelim(Zi)) {
        sr.call(this, 6 - e2);
        break;
      }
      if (this.tokenType === 12 || this.tokenType === 10) {
        Em.call(this, ms), Tt.call(this, 1, false);
        break;
      }
      break;
    case 12:
      e2 = Tt.call(this, 1, true), e2 > 0 && sr.call(this, 6 - e2);
      break;
    default:
      if (this.eatDelim(hs), this.tokenType === 1) {
        e2 = Tt.call(this, 0, true), e2 > 0 && sr.call(this, 6 - e2);
        break;
      }
      if (this.isDelim(Zi)) {
        this.next(), sr.call(this, 5);
        break;
      }
      this.error("Hex digit or question mark is expected");
  }
}
var Pm = "UnicodeRange";
var Im = { value: String };
function Ji() {
  let e2 = this.tokenStart;
  return this.eatIdent("u"), zm.call(this), { type: "UnicodeRange", loc: this.getLocation(e2, this.tokenStart), value: this.substrToCursor(e2) };
}
function Nm(e2) {
  this.tokenize(e2.value);
}
var ao = {};
f(ao, { generate: () => _m, name: () => Mm, parse: () => oo, structure: () => Bm });
var io = {};
f(io, { decode: () => ro, encode: () => no });
var Dm = 32;
var to = 92;
var Om = 34;
var Fm = 39;
var Rm = 40;
var ds = 41;
function ro(e2) {
  let t = e2.length, r = 4, n = e2.charCodeAt(t - 1) === ds ? t - 2 : t - 1, i = "";
  for (; r < n && me(e2.charCodeAt(r)); ) r++;
  for (; r < n && me(e2.charCodeAt(n)); ) n--;
  for (let o = r; o <= n; o++) {
    let a = e2.charCodeAt(o);
    if (a === to) {
      if (o === n) {
        o !== t - 1 && (i = e2.substr(o + 1));
        break;
      }
      if (a = e2.charCodeAt(++o), V(to, a)) {
        let c = o - 1, u = se(e2, c);
        o = u - 1, i += Ue(e2.substring(c + 1, u));
      } else a === 13 && e2.charCodeAt(o + 1) === 10 && o++;
    } else i += e2[o];
  }
  return i;
}
function no(e2) {
  let t = "", r = false;
  for (let n = 0; n < e2.length; n++) {
    let i = e2.charCodeAt(n);
    if (i === 0) {
      t += "\uFFFD";
      continue;
    }
    if (i <= 31 || i === 127) {
      t += "\\" + i.toString(16), r = true;
      continue;
    }
    i === Dm || i === to || i === Om || i === Fm || i === Rm || i === ds ? (t += "\\" + e2.charAt(n), r = false) : (r && te(i) && (t += " "), t += e2.charAt(n), r = false);
  }
  return "url(" + t + ")";
}
var Mm = "Url";
var Bm = { value: String };
function oo() {
  let e2 = this.tokenStart, t;
  switch (this.tokenType) {
    case 7:
      t = ro(this.consume(7));
      break;
    case 2:
      this.cmpStr(this.tokenStart, this.tokenEnd, "url(") || this.error("Function name must be `url`"), this.eat(2), this.skipSC(), t = Ct(this.consume(5)), this.skipSC(), this.eof || this.eat(22);
      break;
    default:
      this.error("Url or Function is expected");
  }
  return { type: "Url", loc: this.getLocation(e2, this.tokenStart), value: t };
}
function _m(e2) {
  this.token(7, no(e2.value));
}
var lo = {};
f(lo, { generate: () => jm, name: () => Wm, parse: () => so, structure: () => qm });
var Wm = "Value";
var qm = { children: [[]] };
function so() {
  let e2 = this.tokenStart, t = this.readSequence(this.scope.Value);
  return { type: "Value", loc: this.getLocation(e2, this.tokenStart), children: t };
}
function jm(e2) {
  this.children(e2);
}
var uo = {};
f(uo, { generate: () => Ym, name: () => Hm, parse: () => co, structure: () => Gm });
var Um = Object.freeze({ type: "WhiteSpace", loc: null, value: " " });
var Hm = "WhiteSpace";
var Gm = { value: String };
function co() {
  return this.eat(13), Um;
}
function Ym(e2) {
  this.token(13, e2.value);
}
var fs = { generic: true, cssWideKeywords: Xe, ...Ya, node: Lt };
var po = {};
f(po, { AtrulePrelude: () => bs, Selector: () => xs, Value: () => Ss });
var Km = 35;
var Vm = 42;
var gs = 43;
var Qm = 45;
var Xm = 47;
var $m = 117;
function At(e2) {
  switch (this.tokenType) {
    case 4:
      return this.Hash();
    case 18:
      return this.Operator();
    case 21:
      return this.Parentheses(this.readSequence, e2.recognizer);
    case 19:
      return this.Brackets(this.readSequence, e2.recognizer);
    case 5:
      return this.String();
    case 12:
      return this.Dimension();
    case 11:
      return this.Percentage();
    case 10:
      return this.Number();
    case 2:
      return this.cmpStr(this.tokenStart, this.tokenEnd, "url(") ? this.Url() : this.Function(this.readSequence, e2.recognizer);
    case 7:
      return this.Url();
    case 1:
      return this.cmpChar(this.tokenStart, $m) && this.cmpChar(this.tokenStart + 1, gs) ? this.UnicodeRange() : this.Identifier();
    case 9: {
      let t = this.charCodeAt(this.tokenStart);
      if (t === Xm || t === Vm || t === gs || t === Qm) return this.Operator();
      t === Km && this.error("Hex or identifier is expected", this.tokenStart + 1);
      break;
    }
  }
}
var bs = { getNode: At };
var Zm = 35;
var Jm = 38;
var ed = 42;
var td = 43;
var rd = 47;
var ys = 46;
var nd = 62;
var id = 124;
var od = 126;
function ad(e2, t) {
  t.last !== null && t.last.type !== "Combinator" && e2 !== null && e2.type !== "Combinator" && t.push({ type: "Combinator", loc: null, name: " " });
}
function sd() {
  switch (this.tokenType) {
    case 19:
      return this.AttributeSelector();
    case 4:
      return this.IdSelector();
    case 16:
      return this.lookupType(1) === 16 ? this.PseudoElementSelector() : this.PseudoClassSelector();
    case 1:
      return this.TypeSelector();
    case 10:
    case 11:
      return this.Percentage();
    case 12:
      this.charCodeAt(this.tokenStart) === ys && this.error("Identifier is expected", this.tokenStart + 1);
      break;
    case 9: {
      switch (this.charCodeAt(this.tokenStart)) {
        case td:
        case nd:
        case od:
        case rd:
          return this.Combinator();
        case ys:
          return this.ClassSelector();
        case ed:
        case id:
          return this.TypeSelector();
        case Zm:
          return this.IdSelector();
        case Jm:
          return this.NestingSelector();
      }
      break;
    }
  }
}
var xs = { onWhiteSpace: ad, getNode: sd };
function ks() {
  return this.createSingleNodeList(this.Raw(null, false));
}
function ws() {
  let e2 = this.createList();
  if (this.skipSC(), e2.push(this.Identifier()), this.skipSC(), this.tokenType === 18) {
    e2.push(this.Operator());
    let t = this.tokenIndex, r = this.parseCustomProperty ? this.Value(null) : this.Raw(this.consumeUntilExclamationMarkOrSemicolon, false);
    if (r.type === "Value" && r.children.isEmpty) {
      for (let n = t - this.tokenIndex; n <= 0; n++) if (this.lookupType(n) === 13) {
        r.children.appendData({ type: "WhiteSpace", loc: null, value: " " });
        break;
      }
    }
    e2.push(r);
  }
  return e2;
}
function vs(e2) {
  return e2 !== null && e2.type === "Operator" && (e2.value[e2.value.length - 1] === "-" || e2.value[e2.value.length - 1] === "+");
}
var Ss = { getNode: At, onWhiteSpace(e2, t) {
  vs(e2) && (e2.value = " " + e2.value), vs(t.last) && (t.last.value += " ");
}, expression: ks, var: ws };
var ld = /* @__PURE__ */ new Set(["none", "and", "not", "or"]);
var Cs = { parse: { prelude() {
  let e2 = this.createList();
  if (this.tokenType === 1) {
    let t = this.substring(this.tokenStart, this.tokenEnd);
    ld.has(t.toLowerCase()) || e2.push(this.Identifier());
  }
  return e2.push(this.Condition("container")), e2;
}, block(e2 = false) {
  return this.Block(e2);
} } };
var Ts = { parse: { prelude: null, block() {
  return this.Block(true);
} } };
function ho(e2, t) {
  return this.parseWithFallback(() => {
    try {
      return e2.call(this);
    } finally {
      this.skipSC(), this.lookupNonWSType(0) !== 22 && this.error();
    }
  }, t || (() => this.Raw(null, true)));
}
var Ls = { layer() {
  this.skipSC();
  let e2 = this.createList(), t = ho.call(this, this.Layer);
  return (t.type !== "Raw" || t.value !== "") && e2.push(t), e2;
}, supports() {
  this.skipSC();
  let e2 = this.createList(), t = ho.call(this, this.Declaration, () => ho.call(this, () => this.Condition("supports")));
  return (t.type !== "Raw" || t.value !== "") && e2.push(t), e2;
} };
var As = { parse: { prelude() {
  let e2 = this.createList();
  switch (this.tokenType) {
    case 5:
      e2.push(this.String());
      break;
    case 7:
    case 2:
      e2.push(this.Url());
      break;
    default:
      this.error("String or url() is expected");
  }
  return this.skipSC(), this.tokenType === 1 && this.cmpStr(this.tokenStart, this.tokenEnd, "layer") ? e2.push(this.Identifier()) : this.tokenType === 2 && this.cmpStr(this.tokenStart, this.tokenEnd, "layer(") && e2.push(this.Function(null, Ls)), this.skipSC(), this.tokenType === 2 && this.cmpStr(this.tokenStart, this.tokenEnd, "supports(") && e2.push(this.Function(null, Ls)), (this.lookupNonWSType(0) === 1 || this.lookupNonWSType(0) === 21) && e2.push(this.MediaQueryList()), e2;
}, block: null } };
var Es = { parse: { prelude() {
  return this.createSingleNodeList(this.LayerList());
}, block() {
  return this.Block(false);
} } };
var zs = { parse: { prelude() {
  return this.createSingleNodeList(this.MediaQueryList());
}, block(e2 = false) {
  return this.Block(e2);
} } };
var Ps = { parse: { prelude() {
  return this.createSingleNodeList(this.SelectorList());
}, block() {
  return this.Block(true);
} } };
var Is = { parse: { prelude() {
  return this.createSingleNodeList(this.SelectorList());
}, block() {
  return this.Block(true);
} } };
var Ns = { parse: { prelude() {
  return this.createSingleNodeList(this.Scope());
}, block(e2 = false) {
  return this.Block(e2);
} } };
var Ds = { parse: { prelude: null, block(e2 = false) {
  return this.Block(e2);
} } };
var Os = { parse: { prelude() {
  return this.createSingleNodeList(this.Condition("supports"));
}, block(e2 = false) {
  return this.Block(e2);
} } };
var Fs = { container: Cs, "font-face": Ts, import: As, layer: Es, media: zs, nest: Ps, page: Is, scope: Ns, "starting-style": Ds, supports: Os };
function Rs() {
  let e2 = this.createList();
  this.skipSC();
  e: for (; !this.eof; ) {
    switch (this.tokenType) {
      case 1:
        e2.push(this.Identifier());
        break;
      case 5:
        e2.push(this.String());
        break;
      case 18:
        e2.push(this.Operator());
        break;
      case 22:
        break e;
      default:
        this.error("Identifier, string or comma is expected");
    }
    this.skipSC();
  }
  return e2;
}
var Me = { parse() {
  return this.createSingleNodeList(this.SelectorList());
} };
var mo = { parse() {
  return this.createSingleNodeList(this.Selector());
} };
var cd = { parse() {
  return this.createSingleNodeList(this.Identifier());
} };
var ud = { parse: Rs };
var lr = { parse() {
  return this.createSingleNodeList(this.Nth());
} };
var Ms = { dir: cd, has: Me, lang: ud, matches: Me, is: Me, "-moz-any": Me, "-webkit-any": Me, where: Me, not: Me, "nth-child": lr, "nth-last-child": lr, "nth-last-of-type": lr, "nth-of-type": lr, slotted: mo, host: mo, "host-context": mo };
var fo = {};
f(fo, { AnPlusB: () => cn, Atrule: () => pn, AtrulePrelude: () => mn, AttributeSelector: () => gn, Block: () => yn, Brackets: () => kn, CDC: () => vn, CDO: () => Cn, ClassSelector: () => Ln, Combinator: () => En, Comment: () => Pn, Condition: () => Nn, Declaration: () => On, DeclarationList: () => Mn, Dimension: () => _n, Feature: () => qn, FeatureFunction: () => Un, FeatureRange: () => Yn, Function: () => Vn, GeneralEnclosed: () => Xn, Hash: () => Zn, IdSelector: () => ri, Identifier: () => ei, Layer: () => ii, LayerList: () => ai, MediaQuery: () => li, MediaQueryList: () => ui, NestingSelector: () => hi, Nth: () => di, Number: () => gi, Operator: () => yi, Parentheses: () => ki, Percentage: () => vi, PseudoClassSelector: () => Ci, PseudoElementSelector: () => Li, Ratio: () => Ei, Raw: () => Pi, Rule: () => Ni, Scope: () => Oi, Selector: () => Ri, SelectorList: () => Bi, String: () => Ui, StyleSheet: () => Gi, SupportsDeclaration: () => Ki, TypeSelector: () => Xi, UnicodeRange: () => Ji, Url: () => oo, Value: () => so, WhiteSpace: () => co });
var Bs = { parseContext: { default: "StyleSheet", stylesheet: "StyleSheet", atrule: "Atrule", atrulePrelude(e2) {
  return this.AtrulePrelude(e2.atrule ? String(e2.atrule) : null);
}, mediaQueryList: "MediaQueryList", mediaQuery: "MediaQuery", condition(e2) {
  return this.Condition(e2.kind);
}, rule: "Rule", selectorList: "SelectorList", selector: "Selector", block() {
  return this.Block(true);
}, declarationList: "DeclarationList", declaration: "Declaration", value: "Value" }, features: { supports: { selector() {
  return this.Selector();
} }, container: { style() {
  return this.Declaration();
} } }, scope: po, atrule: Fs, pseudo: Ms, node: fo };
var _s = { node: Lt };
var Ws = sn({ ...fs, ...Bs, ..._s });
var iy = "3.2.0";
function go(e2) {
  let t = {};
  for (let r of Object.keys(e2)) {
    let n = e2[r];
    n && (Array.isArray(n) || n instanceof Q ? n = n.map(go) : n.constructor === Object && (n = go(n))), t[r] = n;
  }
  return t;
}
var js = {};
f(js, { decode: () => pd, encode: () => hd });
var qs = 92;
function pd(e2) {
  let t = e2.length - 1, r = "";
  for (let n = 0; n < e2.length; n++) {
    let i = e2.charCodeAt(n);
    if (i === qs) {
      if (n === t) break;
      if (i = e2.charCodeAt(++n), V(qs, i)) {
        let o = n - 1, a = se(e2, o);
        n = a - 1, r += Ue(e2.substring(o + 1, a));
      } else i === 13 && e2.charCodeAt(n + 1) === 10 && n++;
    } else r += e2[n];
  }
  return r;
}
function hd(e2) {
  let t = "";
  if (e2.length === 1 && e2.charCodeAt(0) === 45) return "\\-";
  for (let r = 0; r < e2.length; r++) {
    let n = e2.charCodeAt(r);
    if (n === 0) {
      t += "\uFFFD";
      continue;
    }
    if (n <= 31 || n === 127 || n >= 48 && n <= 57 && (r === 0 || r === 1 && e2.charCodeAt(0) === 45)) {
      t += "\\" + n.toString(16) + " ";
      continue;
    }
    We(n) ? t += e2.charAt(r) : t += "\\" + e2.charAt(r);
  }
  return t;
}
var { tokenize: uy, parse: py, generate: hy, lexer: my, createLexer: dy, walk: fy, find: gy, findLast: by, findAll: yy, toPlainObject: xy, fromPlainObject: ky, fork: wy } = Ws;
export {
  csstree_esm_exports as csstree,
  dist_exports2 as domutils,
  is2 as is,
  parseDocument,
  selectAll,
  selectOne
};
