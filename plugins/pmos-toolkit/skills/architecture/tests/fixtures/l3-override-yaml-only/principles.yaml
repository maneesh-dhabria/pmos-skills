schema_version: 2

# L3 override fixture (yaml-only): overrides U001 disposition.
# Used by test-principles-loader.sh case (b).
rules:
  - id: U001
    tier: 1
    stack: null
    disposition: must_fix
    delegate_to: grep
    check: "file_loc_gt:500;exclude_generated"
    source: "tests/fixtures/l3-override-yaml-only/principles.yaml"
    message: "Project override: file LOC cap is a hard fail."
    why: "This project treats large files as blocking — escalated from should_fix."
