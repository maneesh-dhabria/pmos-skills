# Host Style Extraction — SUPERSEDED (tombstone)

> **Superseded as of pmos-toolkit v2.7.0.** `/wireframes` no longer follows this document — the DESIGN.md pipeline replaced it: schema in `design-md-spec.md`, resolution in `design-md-resolver.md`, extraction in `design-md-extractor.md`, CSS generation in `design-md-to-css.md`, component sidecar in `components-md-spec.md`.

This stub is retained only so old plans that link here (e.g. `2026-04-30-wireframes-style-and-screenshot-input-plan.md`) don't 404. The legacy procedure was deleted — do not reconstruct it; it predates DESIGN.md.
