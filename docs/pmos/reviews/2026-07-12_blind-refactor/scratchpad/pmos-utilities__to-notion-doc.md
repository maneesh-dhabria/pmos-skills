## Pass 1 — reviewer findings

### to-notion-doc-F1 [Blocker] `allowed-tools: Bash, Read` locks out the very tools the skill orchestrates
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:6
- Quote: "all page content is written through the Notion **content MCP** (`notion-create-pages`, `notion-update-page`, `notion-fetch`, `notion-search`)"
- Problem: The frontmatter declares `allowed-tools: Bash, Read` — the gamekit launch-a-server template — but this skill's entire write path is Notion MCP calls, plus `AskUserQuestion` prompts in Phases 0–3 and a task-tracking tool per "Track Progress". Where `allowed-tools` is enforced as a restriction (its documented semantics), every MCP write, the parent-pick, and all preference asks are blocked, i.e. the skill cannot perform its core function. Even where the field is advisory, it is an actively wrong contract copied from a skill with a completely different tool surface.

### to-notion-doc-F2 [Blocker] Resume-cursor skip rule (`lastSi ≤ cursor`) is unsound for deferred create-then-append batches
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:223
- Quote: "source + page, skip every batch whose `lastSi ≤` the cursor (idempotent resume, no duplicate blocks)."
- Problem: `chunk-blocks.mjs` emits deferred batches (grandchildren of >2-level nesting, >99-row table overflow) AFTER all flat sibling batches, so their `lastSi` is LOWER than earlier batches' (e.g. doc = 3-level list si 0–2 then paragraph si 3 → batch0 covers si [0,1,3] with lastSi=3, batch1 covers si [2] with lastSi=2). A crash after batch0 persists cursor `last_si=3`; the re-run skips batch1 (2 ≤ 3) and can NEVER write si 2 — verification fails every retry with no path to heal. Symmetrically, once batch1 writes, the cursor moves BACKWARD to 2, so a re-run re-appends batch0 → duplicate blocks. Both directions of the advertised "idempotent resume" contract break in exactly the multi-batch scenarios resume exists for. The cursor should key on batch `seq` (monotone, already emitted by `planWrite`), not max si.

### to-notion-doc-F3 [Should-fix] The chunked write plan is in the REST block model, but the write path is NFM — no script renders a batch to NFM, and non-PAGE `parentRef` targeting is undefined for the MCP
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:219
- Quote: "first `create`-kind batch becomes the `notion-create-pages` `content` (its NFM); every later `append` batch"
- Problem: `chunk-blocks.mjs` batches carry REST-model `blocks`/`rows`, no `nfm` field, and there is no CLI to render a batch's model to NFM (`map-to-notion.mjs` only renders the whole doc from a source tree). So on any multi-batch doc the model must hand-serialize blocks to NFM — exactly the tab-indentation / callout-form / escaping subtleties the scripts exist to make deterministic (§H violation; the `<callout><br>` regression class the reference itself documents). Worse, batches with `parentRef` = a source si ("target the parent block returned by the prior step") have no defined mechanism: `notion-update-page insert_content` per reference §0/§6 appends at page positions and takes NFM; nothing maps an si placeholder to a Notion block for targeting. And since NFM is hierarchical (tab-indented children), the ≤2-nesting REST limit the plan enforces doesn't even apply to the MCP write path — the create-then-append machinery is a REST-era artifact that adds an unimplementable step. The reference's §6 ("REST limits the mapper plans against") sits unreconciled with §0's "Default posture: MCP-only — no REST."

### to-notion-doc-F4 [Should-fix] Verification's key input (`fetchedSi`) is hand-derived by the model — the reconciliation script verifies model-supplied guesses
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:229
- Quote: "Re-fetch the page: `notion-fetch <page>` → NFM. Derive the set of source-indices that landed."
- Problem: Matching fetched NFM blocks back to source indices is the most error-prone step of the whole verification (fuzzy text matching across escaping/normalization), yet it has no script support — `verify-page.mjs` just consumes whatever `fetchedSi` set the model asserts. The description's headline claim "verifies the result block-for-block" therefore rests on model judgment, not the deterministic gate the skill's own §H posture demands. A script that diffs fetched NFM against the rendered NFM (both are strings the pipeline already has) would make the completeness check real; as shipped, a model that hallucinates coverage passes cleanly.

### to-notion-doc-F5 [Should-fix] Annexure grouping promises a "scoped" toggle re-map that no script can perform
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:165
- Quote: "single collapsible parent heading — re-map with `--headings toggle` scoped to the cluster) / *Keep them"
- Problem: `map-to-notion.mjs` has no cluster awareness — `--headings toggle` is global (`nestUnderHeadings` toggles every heading in the doc) and no flag accepts a cluster/si range. Nothing synthesizes the promised "single collapsible parent heading" either (the parse signal is just `{prefix, headingSis, count}`). So the `(Recommended)` option — AUTO-PICKed in every unattended run — commits to an operation the deterministic layer cannot execute; the model must hand-restructure the mapped tree, precisely the drift-prone work the scripts were built to eliminate. Either ship a `--cluster <si...>` mapping path or downgrade the option's promise.

### to-notion-doc-F6 [Should-fix] Markdown paragraph gatherer swallows adjacent tables, images, bookmarks, and equations — the flagship "never drops table columns/rows" claim fails on a common input
- Where: plugins/pmos-utilities/skills/to-notion-doc/scripts/parse-doc.mjs:147
- Quote: "!/^(#{1,6})\\s/.test(lines[i]) && !/^\\s*([-*_])(\\s*\\1){2,}\\s*$/.test(lines[i]) &&"
- Problem: The paragraph loop's stop conditions cover blank/heading/hr/quote/list/fence but NOT table rows (`| … |`), standalone images, bare URLs, or `$$` fences. Verified live: `"Intro line\n| A | B |\n| - | - |\n| 1 | 2 |"` parses to a single `paragraph` — the table is silently flattened into prose text (same for `Intro\n![alt](https://…)`). No blank line before a table is common in real Markdown. Because the table never becomes a `table` node, the entire §3 fidelity contract (and the description's "never drops table columns/rows") is bypassed, and the completeness census can't see the loss — the paragraph si is happily "mapped".

### to-notion-doc-F7 [Should-fix] "four zero-dependency Node scripts" — five ship
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:14
- Quote: "lives in four zero-dependency Node scripts under `scripts/` (each with an embedded"
- Problem: `scripts/` contains five .mjs files (parse-doc, map-to-notion, upload-image, chunk-blocks, verify-page), each with a `--selftest`. The intro's count-claim is stale (upload-image.mjs is real deterministic work — the image ladder, MIME map, size guardrails — not a footnote), and the Platform Adaptation bullet's "the four `node scripts/*.mjs` invocations" inherits the same miscount. This is precisely the enumerated-set drift class that prose-count claims keep shipping; state the set by name or drop the number.

### to-notion-doc-F8 [Should-fix] `--update-mode` is a contract flag with no defined effect at its consumption point
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:211
- Quote: "**`--into <page>` → update an existing page.** Ask the update mode:"
- Problem: The argument-hint and Flags section advertise `--update-mode rewrite|archive|in-place`, but Phase 3 unconditionally asks the update mode — unlike `--toc`, which explicitly "overrides and skips the prompt". A user passing `--update-mode rewrite` interactively still gets the ask (or the model improvises). The flags-vs-hints lint can't see this (the token appears in the body), which is exactly the gap: presence ≠ wiring. One clause — "`--update-mode` overrides and skips this prompt" — closes it.

### to-notion-doc-F9 [Nit] upload-image.mjs header comment still describes the retired dual-write stub shape
- Where: plugins/pmos-utilities/skills/to-notion-doc/scripts/upload-image.mjs:8
- Quote: "STUB (copy to ./to-notion-doc-assets/<slug>/, emit a callout naming the path + an"
- Problem: The header says the stub emits "a callout naming the path + an empty image placeholder", but line 71 of the same file declares "No separate image placeholder block (the dual-write F2 fix)" and the selftests assert single-callout. First-screen doc drift inside the one-home file for the stub shape.

### to-notion-doc-F10 [Nit] Selftest label asserts the opposite of the §3 table-width contract
- Where: plugins/pmos-utilities/skills/to-notion-doc/scripts/map-to-notion.mjs:306
- Quote: "ok(t.table_width === 3, 'table_width = max col count (3)');"
- Problem: The contract (reference §3 and the mapTable comment) is width = header/first row, explicitly "*not* the max column count". The fixture's first row happens to have 3 cells so the assertion passes, but the label documents the rejected semantics — a future reader "fixing" the code to match the test name would reintroduce the widening bug.

### to-notion-doc-F11 [Nit] buildStub's model block and its NFM disagree (color + line joining) despite the byte-for-byte claim
- Where: plugins/pmos-utilities/skills/to-notion-doc/scripts/upload-image.mjs:77
- Quote: "const callout = { type: 'callout', icon: '🖼', color: 'gray_background', rich: [{ content: lines.join(' — ') }] };"
- Problem: The stub's REST-model block carries `color: 'gray_background'` and a single ' — '-joined rich line, but `stubCalloutNfm` (the shape map-to-notion renders) emits no color attribute and multi-line tab-indented body. The "agree byte-for-byte (no dual-write)" claim holds for the NFM string only; the two representations of the same stub diverge, so any integrity check walking `blocks` sees a different callout than the page received.

**Pass 1 verdict:** 2 blockers / 6 should-fix / 3 nits — material findings

## Pass 1 — author response

### to-notion-doc-F1 — Accepted
- Fix: SKILL.md · frontmatter line 6 · remove the `allowed-tools: Bash, Read` line entirely (before: `allowed-tools: Bash, Read`; after: no `allowed-tools` key, so the skill inherits the session's full tool surface — Bash, Read, Write/Edit for `settings.yaml` + learnings I/O, AskUserQuestion, TaskCreate/TodoWrite, and all `mcp__*notion*` tools). Deletion, not enumeration: the Notion MCP tool names vary by host (`mcp__plugin_Notion_notion__notion-create-pages` here, other prefixes elsewhere), so any enumerated allowlist would be host-fragile; the field is copied boilerplate from the gamekit launch-a-server template and is wrong for this skill's tool surface.
- Rationale: Where `allowed-tools` is enforced as a restriction, the skill cannot issue a single MCP write, cannot ask any of its ~8 AskUserQuestion prompts, and cannot persist preferences — the core function is dead on arrival. Where advisory, it is still an actively false contract.
- Blast radius: SKILL.md frontmatter only. No script, reference, or substrate touched. skill-eval frontmatter checks re-run; flags-vs-hints and phase-refs lints unaffected.

### to-notion-doc-F2 — Accepted
- Fix: (1) SKILL.md · Phase 3 step 2 · change the cursor contract from `{ page_id, last_si }` / "skip every batch whose `lastSi ≤` the cursor" to `{ page_id, last_seq }` / "skip every batch whose `seq ≤` the cursor" — `seq` is already emitted monotonically by `planWrite` and reflects true write order regardless of deferred-batch si inversion. (2) chunk-blocks.mjs · header comment lines 10–11 and the line-126 inline comment · reword "resume cursor (last source-index written)" to "resume cursor (seq of the last batch written)"; keep `lastSi` as an informational field or drop it (propose: drop, so no future reader re-keys on it). (3) chunk-blocks.mjs selftest · replace the `lastSi === 199` assertion with `seq` monotonicity + an explicit regression fixture: 3-level list (si 0–2) followed by a paragraph (si 3), assert the deferred batch's `seq` is strictly greater than every prior batch's `seq` even though its max si is lower.
- Rationale: Reviewer's failure trace is correct and reproducible from the code: `emitGroup` flushes flat batches first and emits deferred create-then-append batches after (chunk-blocks.mjs:111–115), so a deferred batch's max si can be lower than an earlier batch's. An si-keyed cursor then either permanently skips the deferred batch (crash-after-batch0) or rolls backward and re-appends (duplicate blocks) — both break the advertised idempotent-resume contract in exactly the multi-batch cases resume exists for. `seq` is the monotone key the plan already carries.
- Blast radius: SKILL.md Phase 3; chunk-blocks.mjs (comments + selftest, plus `lastSi` field removal); no other script imports `lastSi`. Note F3's accepted redesign removes the deferred/parentRef machinery, which removes the si-inversion source — but the seq-keyed cursor is kept regardless (defense in depth; batch order remains the only sound resume key).

### to-notion-doc-F3 — Accepted
- Fix: rework the chunked-write plan to be NFM-native, in three parts. (1) map-to-notion.mjs · NFM renderer · additionally emit `nfmBySi` (or per-top-level-block NFM segments keyed by si) alongside the whole-doc `nfm`, so any contiguous run of top-level blocks has a deterministic NFM rendering by concatenation — no hand-serialization ever. (2) chunk-blocks.mjs · `planWrite`/`emitGroup` · retire the REST-era machinery (`trimToDepth2`, the ≤2-nesting deferral, non-PAGE `parentRef`, `emitTableRows` row-append targeting): the MCP write path is hierarchical NFM where the 2-level REST limit does not apply and there is no mechanism to target a Notion block by an si placeholder. New plan: split at top-level block boundaries only, each batch = a contiguous run of top-level source blocks with their full subtrees, budgeted by total block count (keep MAX_BLOCKS as a conservative payload budget) — every batch carries its concatenated `nfm`, batch 0 is the `notion-create-pages` content, every later batch is `notion-update-page insert_content` position `end` against the page. A single top-level block exceeding the budget (e.g. a 500-row table) is split into successive NFM fragments at row boundaries (the table tag re-opened per fragment is NOT valid NFM — so instead: an oversize table stays whole in its own batch and the budget is advisory for tables, with a stderr WARN; document this in reference §6). (3) SKILL.md · Phase 3 step 2 · rewrite to match (no "create-then-append batches target the parent block returned by the prior step" — that sentence describes an unimplementable step); reference/notion-blocks.md · §6 · re-scope from "REST limits the mapper plans against" to "MCP write-batching: why we split, what the budget is, oversize-table posture", reconciling with §0's MCP-only posture. Selftests updated: coverage/no-gap/no-overlap assertions stay; nesting fixtures now assert full-subtree batches with `nfm` present; the create-then-append fixtures are deleted.
- Rationale: The reviewer is right that the plan model and the write path are in different type systems: batches carry REST `blocks`/`rows` with no `nfm`, nothing renders a batch to NFM, and non-PAGE `parentRef` has no defined MCP mechanism — so on any multi-batch doc the model must hand-write NFM (a §H violation, and the exact `<callout><br>` regression class the reference documents). The ≤2-nesting limit is a REST constraint that doesn't bind the MCP path at all; the deferral machinery adds risk without buying anything.
- Blast radius: largest change in the unit — map-to-notion.mjs (new per-si NFM segments + selftests), chunk-blocks.mjs (planner rewrite + selftests), SKILL.md Phase 3, reference/notion-blocks.md §6 (and a §0 cross-cite). verify-page.mjs unaffected structurally but F4's fix consumes the new per-si NFM segments. No other skill or substrate consumes these scripts.

### to-notion-doc-F4 — Accepted
- Fix: verify-page.mjs · add a deterministic `deriveFetchedSi(fetchedNfm, nfmBySi)` export (plus a CLI mode `node verify-page.mjs --fetched <fetched.txt> --map <map.json>`): normalize both sides (unescape NFM entities, collapse whitespace, strip attribute ordering noise), then for each si test whether its rendered NFM segment appears in the fetched NFM (substring containment on the normalized forms, with a ≥N-char floor for trivially short segments like dividers, which instead match by counted occurrence). Output the derived `fetchedSi` set + a per-si miss list. SKILL.md · Phase 4 step 1 · change "Derive the set of source-indices that landed" (model judgment) to "run `verify-page.mjs --fetched … --map …` to derive `fetchedSi` deterministically; the model never asserts coverage by eye". Selftests: exact-match, escaping-normalization, and a deliberate-drop fixture where one si's segment is absent.
- Rationale: As shipped, `checkCompleteness` verifies whatever `fetchedSi` the model asserts — the headline "verifies the result block-for-block" rests on the single most fuzz-prone judgment step. Both inputs are strings the pipeline already has (fetched NFM from `notion-fetch`; per-si rendered NFM from F3's `nfmBySi`), so the diff is scriptable — precisely the §H "deterministic = hard gate" posture.
- Blast radius: verify-page.mjs (+selftests), SKILL.md Phase 4; depends on F3's per-si NFM segments (sequence the two fixes together). No lint/eval changes.

### to-notion-doc-F5 — Accepted
- Fix: two coordinated parts. (1) map-to-notion.mjs · new repeatable flag `--group-cluster <prefix>:<firstHeadingSi>-<lastHeadingSi>` · synthesizes one toggleable `heading_1` (text = the cluster `prefix` pluralized verbatim from the parse signal, e.g. "Annexures") and nests the cluster's top-level sections (each heading + its body run, identified by the si range) as its children; all other headings keep the run's resolved `--headings` mode — no global toggle flip. Selftest: 3-annexure fixture → one synthesized toggle parent, 3 nested sections, non-cluster headings untouched, all si preserved. (2) SKILL.md · Phase 1 step 5 · replace "re-map with `--headings toggle` scoped to the cluster" with "re-map passing `--group-cluster <prefix>:<siRange>` (from the parse `clusters` signal)". The parse signal already carries `{prefix, headingSis, count}` — enough to build the flag value.
- Rationale: The `(Recommended)` option — AUTO-PICKed in every unattended run — currently promises an operation no script performs: `--headings toggle` is global (`nestUnderHeadings` at map-to-notion.mjs:67 toggles every heading) and nothing synthesizes the parent heading, so the model must hand-restructure the mapped tree. Per §H, ship the deterministic path rather than downgrade the promise — the grouping is pure tree surgery on data the parse already emits.
- Blast radius: map-to-notion.mjs (+selftests), SKILL.md Phase 1. The synthesized parent deepens nesting by one level — harmless on the F3 NFM-native write path (no 2-level limit). `--group-cluster` is internal orchestration (script CLI, not a skill contract flag) so the argument-hint is untouched; flags-vs-hints lint unaffected.

### to-notion-doc-F6 — Accepted
- Fix: parse-doc.mjs · paragraph gatherer (lines 144–150) · add four stop conditions to the while-loop guard: table row `!/^\s*\|.*\|\s*$/.test(lines[i])`, standalone image `!/^\s*!\[[^\]]*\]\([^)\s]+\)\s*$/.test(lines[i])`, bare-URL bookmark `!/^\s*<?https?:\/\/[^\s>]+>?\s*$/.test(lines[i])`, and `$$` fence `!/^\s*\$\$\s*$/.test(lines[i])` — mirroring the four block-start regexes the main loop already recognizes at lines 130–143. Selftests: `"Intro line\n| A | B |\n| - | - |\n| 1 | 2 |"` → paragraph + table (3 columns intact); `"Intro\n![alt](https://x/y.png)"` → paragraph + image; bare-URL and `$$` variants.
- Rationale: Verified in the code: the gatherer stops on blank/heading/hr/quote/list/fence only, so a table (or image/bookmark/equation) with no preceding blank line — common in real Markdown — is swallowed into prose. The table never becomes a `table` node, so the §3 fidelity contract and the description's "never drops table columns/rows" are bypassed invisibly: the paragraph si reads as "mapped" and the completeness census cannot see the loss.
- Blast radius: parse-doc.mjs (+selftests). Downstream mapping/chunk/verify consume the corrected node types with no interface change. The description's flagship claim becomes true for this input class.

### to-notion-doc-F7 — Accepted
- Fix: SKILL.md · intro line 14 · "lives in four zero-dependency Node scripts" → "lives in five zero-dependency Node scripts (`parse-doc`, `map-to-notion`, `upload-image`, `chunk-blocks`, `verify-page`)"; Platform Adaptation "No Bash tool" bullet (line 35) · "the four `node scripts/*.mjs` invocations" → "the `node scripts/*.mjs` invocations" (drop the count there entirely — one enumerated home per §K, and the intro is it).
- Rationale: Five .mjs files ship, each with `--selftest`; the count-claim is stale, and this is the exact enumerated-set drift class the [J] coherence lesson (2026-07-09) exists for — name the set once, don't count it twice.
- Blast radius: SKILL.md prose only; none.

### to-notion-doc-F8 — Accepted
- Fix: SKILL.md · Phase 3 step 1, `--into` branch · after "Ask the update mode:" add the clause "(`--update-mode` overrides and skips this prompt)" — mirroring the exact wiring pattern `--toc` already uses at Phase 1 step 2.
- Rationale: `--update-mode` is an advertised contract flag with no defined effect at its sole consumption point — a user passing it still gets the ask (or the model improvises). The flags-vs-hints lint sees the token in the body and passes; presence ≠ wiring, so the body must state the override explicitly.
- Blast radius: SKILL.md one clause; lints unaffected (already green, now truthfully so).

### to-notion-doc-F9 — Accepted
- Fix: upload-image.mjs · header comment lines 8–9 · "(copy to ./to-notion-doc-assets/<slug>/, emit a callout naming the path + an empty image placeholder)" → "(copy to ./to-notion-doc-assets/<slug>/, emit a SINGLE caption-inline callout naming the copied path — no separate image placeholder block)".
- Rationale: The header describes the retired dual-write shape; line 71 of the same file and the selftests assert single-callout. First-screen doc drift inside the one-home file for the stub shape.
- Blast radius: upload-image.mjs comment only; none.

### to-notion-doc-F10 — Accepted
- Fix: map-to-notion.mjs · selftest line 306 · relabel `'table_width = max col count (3)'` → `'table_width = header/first-row col count (3), not max (fixture max is 4)'`.
- Rationale: The contract (reference §3 + the mapTable comment at line 151) is width-from-first-row, explicitly not max. The fixture's third row has 4 cells (the EXTRA cell), so max-col semantics would give 4 — the assertion passes only because first-row semantics hold; the label documents the rejected behavior and invites a future "fix" that reintroduces the widening bug. The new label makes the fixture's discriminating power explicit.
- Blast radius: one selftest string; none.

### to-notion-doc-F11 — Accepted
- Fix: upload-image.mjs · `buildStub` (line 77) + the comment at lines 38–39 · make the model block derive from the same `lines` array as the NFM: `rich` becomes one entry per line (or the model callout carries the lines as its rendered children), and drop `color: 'gray_background'` (the NFM emits no color attribute; if a color is wanted it must appear in BOTH representations — propose: no color, matching the minimal-style default). Reword the "agree byte-for-byte (no dual-write)" comment to state precisely what agrees: the NFM string is single-home via `stubCalloutNfm`, and the model block is a faithful mirror of it. Selftest: assert model-lines == NFM body lines and that the model carries no attribute absent from the NFM.
- Rationale: Today the model block says `gray_background` + one ' — '-joined line while the NFM says no-color + multi-line tab-indented body — so any integrity check walking `blocks` (verify-page's `checkIntegrity` does exactly that) sees a different callout than the page received, and the byte-for-byte claim holds only for the string. Divergent twins of one artifact is the dual-write bug class in miniature.
- Blast radius: upload-image.mjs (+selftests); verify-page integrity results over stub callouts become truthful. map-to-notion imports `stubCalloutNfm` (unchanged signature) — no change there.

**Pass 1 author verdict:** 11 accepted / 0 rejected / 0 invalid. Sequencing note: F3 → F4 (per-si NFM segments are F4's input); F2's seq-cursor lands with F3's planner rewrite; all others independent.

## Pass 2 — reviewer findings

### to-notion-doc-F12 [Blocker] F6's table stop-guard diverges from the main-loop table start — infinite loop on a pipe row with no delimiter line
- Where: plugins/pmos-utilities/skills/to-notion-doc/scripts/parse-doc.mjs:146
- Quote: "while (i < lines.length && !/^\s*$/.test(lines[i]) &&"
- Problem: The proposal's F6 adds a paragraph-gather stop condition of "table row `/^\s*\|.*\|\s*$/`", but the main loop only consumes a pipe row as a table when the NEXT line is a delimiter row (parse-doc.mjs:102 requires `/^\s*\|?[\s:|-]+\|?\s*$/` at `i+1`). Today the gather guard exactly mirrors the set of line shapes the main loop always consumes, which is the invariant that guarantees progress: at gather entry the current line never matches a stop. With F6 as written, a lone `| a | b |` line NOT followed by a delimiter (or a doc ending in a single pipe row) is rejected by the table branch, falls to the gather, immediately matches the new stop condition with `para=[]`, pushes an empty paragraph, and never advances `i` — an unbounded loop plus a stream of empty paragraph blocks. The image/bookmark/`$$` stops are safe (their main-loop branches consume unconditionally); only the table stop breaks the mirror. The fix must either replicate the full two-line table-start test in the guard or add an explicit progress guarantee (consume the current line into `para` when nothing else claimed it).

### to-notion-doc-F13 [Should-fix] F3's rewritten write plan defines only the create path — the `--into` update modes still have no batch mapping
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:213
- Quote: "`AskUserQuestion`: *Archive then write (Recommended)* (move the page's existing top-level blocks under a"
- Problem: F3's new plan is "batch 0 = `notion-create-pages` content, later batches = `notion-update-page insert_content` position `end`". That is meaningful only when creating a page. On `--into` (update an existing page) there is no create; nothing states what batch 0 becomes, and archive mode promises "then write the new content above" while every later batch is specified as position `end` — appended content would land BELOW the archive toggle, contradicting the promise. Worse, "move the page's existing top-level blocks under a new collapsed toggleable heading_1" has no defined MCP mechanism (the available commands are insert/replace/update content — a "move" is really a fetch + wrap + `replace_content`, i.e. a destructive rewrite sold as non-destructive, which the model must improvise), and *In-place* is glossed only as "(append / reconcile)" with reconcile defined nowhere. F8 wires the `--update-mode` flag to skip the prompt but the three modes it selects among remain promises without mechanisms — the same defect class F5 was accepted for. The update path needs its own batch semantics (what replaces create; insert position per mode; the archive wrap recipe) or the modes should be narrowed to what the MCP can express.

### to-notion-doc-F14 [Should-fix] verify-page.mjs (and upload-image.mjs) are library-only — the invocations SKILL.md prescribes cannot actually run
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:230
- Quote: "2. `node scripts/verify-page.mjs` with the `plan`, the Phase-2 `resolutions`, the mapped `blocks`, and the"
- Problem: verify-page.mjs's only CLI mode is `--selftest`; any other invocation prints "usage: verify-page.mjs --selftest  (library module; the skill imports verifyPage/checkCompleteness/checkIntegrity)" and exits 64 (verify-page.mjs:134). There is no way to pass "the plan, the resolutions, the mapped blocks, and the fetched coverage" from Bash, so the Phase-4 deterministic gate as written is uninvokable — the model must hand-roll `node -e` import glue or silently skip the script, exactly the §H judgment-leak the gate exists to prevent. Same for Phase 2's "`scripts/upload-image.mjs` `resolveRung` decides the rung" (upload-image.mjs:180 has the identical library-only usage line). The proposal's F4 adds a CLI only for the NEW `deriveFetchedSi` mode (`--fetched`/`--map`); `checkCompleteness`/`checkIntegrity`/`resolveRung` remain CLI-less, and the Platform Adaptation "user runs the `node scripts/*.mjs` invocations themselves and pastes the JSON back" story (kept by F7's rewording) is unexecutable for two of the five scripts. Each phase-prescribed script call needs a real argv contract (file-in/JSON-out), not just the new F4 mode.

### to-notion-doc-F15 [Should-fix] F4's substring containment will false-fail on numbered lists — the renderer emits "1." for every item, Notion's fetch renumbers
- Where: plugins/pmos-utilities/skills/to-notion-doc/scripts/map-to-notion.mjs:233
- Quote: "const marker = b.type === 'numbered_list_item' ? '1.' : b.type === 'to_do' ? (b.checked ? '- [x]' : '- [ ]') : '-';"
- Problem: Every numbered_list_item is rendered with the literal marker `1.` (standard markdown auto-numbering), so an `nfmBySi` segment for the third item reads "1. third". Notion's `notion-fetch` NFM round-trip will render the sequence numbers ("3. third" — and may normalize other spellings: attribute order, escaping choices, `<empty-block/>`). F4's specified normalization is only "unescape NFM entities, collapse whitespace", under which normalized-substring containment misses every numbered item after the first → verify reports them "unaccounted-for" and Phase 4 fails loudly on perfectly good pages. The proposal item is vague enough to be implemented wrong in exactly this way: the normalizer needs marker canonicalization (map `\d+\.` list markers to a fixed token on BOTH sides) and ideally a round-trip fixture built from a real notion-fetch capture, not just self-rendered strings, before the derived-coverage gate can be trusted as the thing the headline "verifies the result block-for-block" rests on.

### to-notion-doc-F16 [Should-fix] F5's `<firstHeadingSi>-<lastHeadingSi>` range encoding assumes cluster contiguity that detectClusters never checks
- Where: plugins/pmos-utilities/skills/to-notion-doc/scripts/parse-doc.mjs:418
- Quote: "for (const c of byKey.values()) if (c.headingSis.length >= 3) clusters.push({ ...c, count: c.headingSis.length });"
- Problem: detectClusters groups headings by `level|token` across the WHOLE document with no adjacency requirement — "Annexure A … References … Annexure B … Glossary … Annexure C" emits one cluster whose `headingSis` are non-consecutive. F5's flag shape `--group-cluster <prefix>:<firstHeadingSi>-<lastHeadingSi>` collapses that to a si RANGE, so the interleaved non-Annexure sections (References, Glossary) get swallowed into the synthesized toggle — silent structural corruption of exactly the kind the AUTO-PICKed "(Recommended)" option would commit unattended. Either detectClusters must require the sibling headings to be consecutive same-level sections (reject interleaved clusters), or the flag must carry the explicit si list and the grouper must nest only the listed sections' runs, splicing around the interlopers. The proposal's selftest fixture (3 adjacent annexures) cannot catch this; a fixture with an interleaved foreign section is the discriminating test.

### to-notion-doc-F17 [Should-fix] The resume cursor has no source fingerprint — "same source" is asserted by nobody, and F2's re-key doesn't change that
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:223
- Quote: "`{ page_id, last_si }` to `./to-notion-doc-assets/<slug>/.write-cursor.json`; on a re-run with the same"
- Problem: The resume contract is "on a re-run with the same source + page, skip every batch whose cursor-key ≤ the cursor" — but the cursor file carries only `{page_id, last_seq}` (post-F2). Nothing verifies the source is actually the same: the user edits the document after a crashed run (the most likely reason a run crashed mid-write and got re-run), re-invokes, and the skill silently skips `seq ≤ cursor` batches of a DIFFERENT plan — the page ends up with the old doc's head and the new doc's tail, and Phase 4 (post-F4) then reports a wall of misses with no hint why. The cursor needs a plan fingerprint (e.g., a hash of the concatenated `nfm` F3 already computes, plus batch count) written alongside `page_id`; a mismatch on resume must invalidate the cursor and force a fresh decision rather than a silent partial-skip. F2 fixed WHICH key orders batches; it did not make the "same source" precondition checkable.

### to-notion-doc-F18 [Nit] MAX_BLOCKS is a block-count budget on a path whose real constraint is NFM payload size
- Where: plugins/pmos-utilities/skills/to-notion-doc/scripts/chunk-blocks.mjs:18
- Quote: "const MAX_BLOCKS = 100; // blocks per request (counts nested children too)"
- Problem: The 100-block ceiling is the REST API's request limit; F3 retires the REST model but "MAX_BLOCKS retained as a conservative payload budget" keeps counting blocks while the MCP path's actual constraint is the size of the NFM string per call. A single 2 MB code fence counts as 1 block and sails through unbatched and unwarned (the oversize WARN is specified only for tables), while 100 one-word paragraphs get split needlessly. Since F3's batches carry their concatenated `nfm` anyway, the honest budget is a character ceiling on the batch's NFM (with block count as a secondary cap) — cheap to compute at plan time and it makes the "conservative" claim true instead of asserted.

### to-notion-doc-F19 [Nit] Frozen non-interactive block cites `_shared/non-interactive.md`, but pmos-utilities ships no `_shared/` at all
- Where: plugins/pmos-utilities/skills/to-notion-doc/SKILL.md:60
- Quote: "sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`)"
- Problem: The plugin has no `skills/_shared/` directory (verified: converter, mac-health, reflect, to-notion-doc are the only children), so within pmos-utilities this is a dangling cite — "Section D of this file" resolves to nothing a reader of this plugin can open. Mitigations acknowledged: the block is the repo-frozen byte-identical inline (hand-maintained, lint-exempt), so it is not fixable inside this unit without breaking byte-identity across every skill; the canonical file lives in pmos-toolkit's `_shared/`. Flagged at plugin scope per the repo's own bootstrap-gap rule (a consumer plugin citing a protocol that exists only in another plugin's `_shared/` should carry a byte-identical copy); the proposal does not touch this and no per-skill fix is possible — the remedy, if any, is placing the copy at `plugins/pmos-utilities/skills/_shared/non-interactive.md`.

**Pass 2 verdict:** 1 blockers / 5 should-fix / 2 nits — material findings

## Pass 2 — author response

### to-notion-doc-F12 — Accepted
- Fix: amend F6's parse-doc.mjs change. The paragraph-gather stop condition for tables must replicate the FULL two-line table-start test the main loop uses at line 102 — `/^\s*\|.*\|\s*$/.test(lines[i]) && i + 1 < lines.length && /^\s*\|?[\s:|-]+\|?\s*$/.test(lines[i + 1]) && lines[i + 1].includes('-')` — not the bare pipe-row regex. Additionally, add a structural progress guarantee after the gather loop: if `para.length === 0`, consume the current line into `para` and advance `i` before pushing the paragraph (belt-and-suspenders — the outer loop can then never re-enter the gather at the same index with nothing consumed). Selftests: (a) lone `| a | b |` with NO delimiter line → parses as paragraph text and the parser terminates; (b) doc ending in a single pipe row → terminates; (c) `"Intro\n| A | B |\n| - | - |\n| 1 | 2 |"` → paragraph + 3-column table (the F6 case, retained).
- Rationale: The reviewer's trace is correct. The current gather guard exactly mirrors the set of line shapes the main loop ALWAYS consumes — that mirror is the progress invariant. F6-as-written broke it for tables only (the main loop's table branch is conditional on the next-line delimiter; image/bookmark/$$ branches consume unconditionally, so those three stops are safe). A lone pipe row would be rejected by the table branch, immediately match the new stop with `para=[]`, push an empty paragraph, and never advance `i` — an unbounded loop. Replicating the two-line test restores the mirror; the empty-para guard makes the invariant structural rather than convention.
- Blast radius: parse-doc.mjs gather loop + selftests (supersedes F6's stop-condition wording in the proposal). No downstream interface change.

### to-notion-doc-F13 — Accepted
- Fix: SKILL.md Phase 3 (target resolution + write step) and reference/notion-blocks.md §6 gain explicit per-mode update batch semantics for `--into`; the three mode glosses are narrowed to what the MCP commands (insert_content / replace_content / update_content, reference §0) can express:
  (1) **Rewrite** — batch 0 becomes a `notion-update-page replace_content` whose `new_str` is batch 0's NFM (clears + writes in one call); every later batch is `insert_content` position `end`. Destructive, and the option text already says so.
  (2) **Archive then write** — recipe made explicit as fetch + wrap + replace, and DISCLOSED as such in the option text (before: "move the page's existing top-level blocks under a new collapsed toggleable heading_1" — a "move" no MCP command performs; after: "re-writes the page with your current content preserved under a collapsed Archive toggle (fetch + wrap + rewrite; content is preserved but block identities/comments may not survive)"). Mechanism: (a) `notion-fetch` the page → old NFM; (b) mint a unique archive anchor heading line (e.g. `# Archive — <ISO date> <short-hash>`); (c) one `replace_content` whose `new_str` = batch 0's NFM + the anchor heading rendered toggleable with the old NFM tab-indented under it; (d) every later batch inserts ABOVE the archive via `update_content` with `old_str` = the minted anchor line and `new_str` = batch NFM + `\n` + the anchor line. The minted anchor is unique by construction, so the search/replace is deterministic; content lands above the archive as promised, fixing the position-`end`-lands-below contradiction.
  (3) **In-place** — gloss narrowed from "(append / reconcile)" to "(append to the end of the existing page)"; every batch (including batch 0) is `insert_content` position `end`. "Reconcile" is dropped — it was defined nowhere and is not expressible without a diff engine this skill does not ship.
  Reference §6 (already being re-scoped by F3) carries the per-mode table as the one home; SKILL.md cites it.
- Rationale: F3's rewritten plan defined only the create path; on `--into` nothing said what replaces the create, archive mode's promise contradicted position-`end` appends, the "move" had no mechanism, and "reconcile" was undefined — the same promise-without-mechanism class F5 was accepted for. All three modes above compose from the three documented MCP commands with no model improvisation.
- Blast radius: SKILL.md Phase 3 (option texts + write step), reference/notion-blocks.md §6 (folds into F3's re-scope — sequence with F3), chunk-blocks.mjs planner emits a mode-agnostic batch list (unchanged shape; the mode chooses the verb per batch at orchestration time, stated in SKILL.md). F8's flag wiring unchanged. verify-page unaffected (Phase 4 verifies the new content's si coverage; the archive toggle is page-owned, not source-owned).

### to-notion-doc-F14 — Accepted
- Fix: give both library-only scripts real argv contracts (file-in/JSON-out), keeping the existing exports.
  (1) verify-page.mjs — new CLI mode `node verify-page.mjs --plan <plan.json> --resolutions <res.json> --map <map.json> --fetched <fetched.txt>` → runs `deriveFetchedSi` (F4) then `verifyPage` and prints the full `{completeness, integrity, unaccountedFor, misses}` JSON to stdout; exit 0 on pass, 1 on fail. The F4-proposed `--fetched/--map` mode folds into this single contract (one CLI, both checks).
  (2) upload-image.mjs — new CLI mode `node upload-image.mjs --resolve-rung --image <image-node.json> --image-mode <mcp-only|rest-upload> --token-env <VAR>` → prints the resolved rung + planned ops JSON (token PRESENCE checked via `process.env`, value never printed). The library-only usage strings at verify-page.mjs:134 / upload-image.mjs:180 are updated to document the new modes.
  (3) SKILL.md Phase 2 step 2 and Phase 4 step 2 reworded to the exact invocations; the Platform Adaptation "No Bash tool" bullet's paste-the-JSON-back story becomes true for all five scripts.
- Rationale: SKILL.md prescribes `node scripts/verify-page.mjs` with four inputs, but the script's only CLI mode is `--selftest` (exit 64 otherwise) — the Phase-4 deterministic gate is uninvokable as written, forcing hand-rolled `node -e` import glue or silent skipping: exactly the §H judgment leak the gate exists to prevent. Same for `resolveRung` in Phase 2.
- Blast radius: verify-page.mjs + upload-image.mjs (CLI shims + usage strings + selftests for the arg parsing), SKILL.md Phases 2/4 + Platform Adaptation bullet. Sequences with F4 (shared CLI). No lint/eval changes; `--resolve-rung` etc. are script CLIs, not skill contract flags.

### to-notion-doc-F15 — Accepted
- Fix: amend F4's normalizer specification in verify-page.mjs. Normalization applied to BOTH the fetched NFM and each `nfmBySi` segment before containment: (a) unescape NFM entities; (b) collapse whitespace; (c) **canonicalize list markers** — map `^\s*\d+[.)]\s` to a fixed token (e.g. `<ol> `) and `^\s*[-*+]\s` (non-checkbox) to `<ul> ` line-by-line, so the renderer's uniform `1.` matches Notion's renumbered `3.`; (d) canonicalize checkbox spellings `- [x]`/`- [X]`; (e) treat `<empty-block/>` as a blank line. Selftests: the F4 fixtures PLUS a renumbering fixture (3-item numbered list rendered as `1./1./1.`, "fetched" side as `1./2./3.` → all three si accounted for) and, as a build-time step for the implementer, one fixture captured from a REAL `notion-fetch` round-trip of a seeded page (checked in as a fixture string, not fetched at selftest time — selftests stay offline).
- Rationale: map-to-notion.mjs:233 emits the literal marker `1.` for every numbered item; Notion's fetch renders true sequence numbers. Under F4's original "unescape + collapse whitespace" normalizer, every numbered item after the first would read as a miss → Phase 4 fails loudly on perfectly good pages, poisoning trust in the new deterministic gate on day one.
- Blast radius: verify-page.mjs normalizer + selftests only (folds into F4/F14's work on the same file). No SKILL.md change beyond F4's.

### to-notion-doc-F16 — Accepted
- Fix: amend F5 in two parts. (1) map-to-notion.mjs — the flag carries the EXPLICIT si list, not a range: `--group-cluster <prefix>:<si,si,si,...>` (comma-separated heading si's straight from the parse signal's `headingSis`). The grouper nests exactly the listed headings' section runs (each listed heading + its body up to the next same-or-higher-level heading) under the synthesized toggle, SPLICING AROUND interlopers: non-listed sections that fall between cluster members keep their original top-level position, ordered after the synthesized toggle in original document order (deterministic rule, stated in the flag's header comment). (2) parse-doc.mjs `detectClusters` — additionally emit `contiguous: true|false` (are the member sections adjacent same-level siblings with nothing between). (3) SKILL.md Phase 1 step 5 — when `contiguous: false`, the AskUserQuestion option text discloses it ("N sections are interleaved with M other sections; grouping will pull the N together above the others") so the AUTO-PICKed Recommended option never silently reorders without saying so; the flag value is built from `headingSis` verbatim. Selftests: the discriminating interleaved fixture (Annexure A / References / Annexure B / Glossary / Annexure C → toggle contains exactly the 3 annexures; References + Glossary survive at top level; all si preserved) plus the original 3-adjacent fixture.
- Rationale: `detectClusters` groups by `level|token` across the whole document with no adjacency check (parse-doc.mjs:418), so F5's `<first>-<last>` range encoding would swallow interleaved foreign sections into the toggle — silent structural corruption committed unattended by the AUTO-PICK. Explicit si lists make the grouper's scope exactly the detected members; the contiguity signal makes the residual reordering disclosed rather than silent.
- Blast radius: map-to-notion.mjs (grouper + selftests, supersedes F5's flag shape), parse-doc.mjs (`contiguous` field + selftest), SKILL.md Phase 1 step 5 option text. Still script-internal — argument-hint untouched.

### to-notion-doc-F17 — Accepted
- Fix: (1) chunk-blocks.mjs `planWrite` — emit `planHash` (e.g. FNV/SHA over the concatenated batch `nfm` strings + batch count; zero-dependency-friendly) on the plan object. (2) SKILL.md Phase 3 step 2 — cursor file becomes `{ page_id, plan_hash, batch_count, last_seq }`; the resume rule gains a precondition: "on re-run, resume ONLY if the cursor's `page_id` AND `plan_hash` match the current plan; on mismatch, the cursor is stale — delete it, warn (`source or plan changed since the interrupted run; cannot resume — starting the write decision fresh`), and re-enter target resolution (never silently skip batches of a different plan)". (3) chunk-blocks selftest: same-input determinism (equal hashes) + any-edit sensitivity (one changed word → different hash).
- Rationale: The most likely reason a crashed run is re-run is that the user touched something; with no fingerprint, "on a re-run with the same source" is a precondition nobody checks, and a seq-keyed skip over a DIFFERENT plan writes old-head/new-tail chimera pages that Phase 4 then reports as a wall of misses with no cause. F2 fixed the ordering key; this makes the same-source precondition machine-checkable.
- Blast radius: chunk-blocks.mjs (+selftests), SKILL.md Phase 3 step 2 (folds into F2/F3's rewrite of the same paragraph — sequence together). verify-page unaffected.

### to-notion-doc-F18 — Accepted
- Fix: chunk-blocks.mjs — the batch budget becomes a primary NFM character ceiling (`MAX_NFM_CHARS`, a conservative per-call payload constant documented in reference §6) computed from each batch's concatenated `nfm` (which F3's batches already carry), with `MAX_BLOCKS` retained as a secondary cap. The oversize-single-block WARN generalizes from tables to ANY single top-level block whose own NFM exceeds the ceiling (a 2 MB code fence stays whole in its own batch + stderr WARN, same posture as the oversize table). Selftests: giant-code-fence fixture → own batch + WARN; 100 one-word paragraphs → NOT split when under the char ceiling.
- Rationale: 100 blocks is the REST request limit; on the MCP path the real constraint is payload size per call. Counting blocks lets a 1-block multi-megabyte fence sail through while needlessly splitting light docs — the char ceiling makes "conservative payload budget" true rather than asserted, and it is cheap since the NFM is already in hand at plan time.
- Blast radius: chunk-blocks.mjs (+selftests) + reference §6 one line (folds into F3's planner rewrite — same code). SKILL.md unchanged.

### to-notion-doc-F19 — Accepted (plugin-scope remedy; not fixable inside this unit's files)
- Fix: place a byte-identical copy of pmos-toolkit's `skills/_shared/non-interactive.md` at `plugins/pmos-utilities/skills/_shared/non-interactive.md` — per the repo's own bootstrap-gap rule (sync-shared.sh is intersection-only and cannot create the file; the first placement is manual, after which it stays in the sync intersection). NO change to SKILL.md's frozen inline block: it is repo-frozen byte-identical (lint-enforced), so editing the cite inside this skill would break byte-identity across every skill and is explicitly off the table.
- Rationale: The frozen block's "Section D of this file (`_shared/non-interactive.md`)" cite dangles within pmos-utilities (verified: `skills/` holds only converter, mac-health, reflect, to-notion-doc). The reviewer's own framing is right that the remedy is plugin-level, not per-skill. Note: this fix benefits all four pmos-utilities skills equally and properly belongs to the `_cross__pmos-utilities` unit's proposal; recorded here as accepted so the finding is not dropped, with ownership flagged for the cross unit.
- Blast radius: one new file under `plugins/pmos-utilities/skills/_shared/`; `tools/lint-non-interactive-inline.sh` (scans all plugins) unaffected — inline blocks unchanged; future `scripts/sync-shared.sh` runs pick the file up (intersection now includes it). No skill body, script, or eval touched.

**Pass 2 author verdict:** 8 accepted / 0 rejected / 0 invalid. F12 amends F6; F13+F18 fold into F3; F15 folds into F4; F14 shares verify-page CLI work with F4; F16 amends F5; F17 folds into F2/F3's Phase-3 paragraph. F19's remedy is plugin-level (flagged to _cross__pmos-utilities).
