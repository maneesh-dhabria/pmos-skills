## Pass 1 — reviewer findings

### interview-feedback-F1 [Blocker] SKILL.md and transcribe.sh contradict each other on whether --force-transcribe destroys the curated transcript
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:39 (vs. scripts/transcribe.sh:99 and its selftest at :264)
- Quote: "the new transcription is written to `transcript.whisper.txt` so the curated file is never clobbered (destructive-opt-in; forwarded verbatim to `scripts/transcribe.sh`"
- Problem: The script does the opposite. `resolve_transcript_target` routes to `transcript.refined.txt` when force=1 (comment at transcribe.sh:94: "it exists and force == \"1\" -> transcript.refined.txt (overwrite)") and the selftest asserts exactly that ("--force-transcribe overwrites refined", transcribe.sh:264). SKILL.md repeats the false claim a second time in Phase Transcribe ("it still routes to `transcript.whisper.txt`, never over the curated file", SKILL.md:125). An operator who hand-curated speaker labels and then follows SKILL.md's stated contract will pass the flag believing it is non-destructive and silently lose the curated file — a data-loss contract violation on the skill's own flag documentation, and the flag's self-description ("destructive-opt-in" in the same sentence as "never clobbered") is internally incoherent. One side must be fixed; the frozen citation rule (§K) says pick one home for the fact.

### interview-feedback-F2 [Should-fix] The "grounding hard gate" passes an output with zero citations — no coverage floor
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:15 (vs. scripts/check-citations.mjs:169)
- Quote: "Grounding is enforced, not asserted. A deterministic gate (`scripts/check-citations.mjs`) refuses any output whose transcript-tier citation is not a verbatim ≥40-char substring"
- Problem: `check-citations.mjs` returns `failed === 0 ? 0 : 1` — a filled scorecard containing no `data-cite-tier` elements at all scores "0 passed, 0 failed" and exits 0, so the blocking STOP-before-done gate (INV-1) green-lights a completely un-grounded artifact. Citation *presence* ("every subjective claim carries a cite") remains a pure model assertion; only citation *fidelity* is enforced. The headline "enforced, not asserted" oversells the gate, and a trivial deterministic floor (e.g. fail or warn when the artifact has filled `notes:` slots but zero citations) is exactly the kind of check §H says should be a script.

### interview-feedback-F3 [Should-fix] The mandated gate invocation hardcodes transcript.refined.txt, which doesn't exist on tier-2/tier-3 runs
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:205 (vs. scripts/check-citations.mjs:157-158)
- Quote: "Run `scripts/check-citations.mjs filled-scorecard.html transcript.refined.txt` — appending the submission file as a **third positional**"
- Problem: Phase Score's hard gate (and Phase Coach's re-run) is unconditional, but on a notes-only round (tier 2 — no recording, no transcript; e.g. a recruiter screen scored from notes) there is no `transcript.refined.txt`, and `runFiles` does `readFileSync(transcriptPath)` with no existence guard — the documented command crashes with ENOENT instead of validating the notes-tier cites. The skill gives no no-transcript invocation form (empty-file stand-in, optional second positional, anything), so the most common degraded path cannot pass its own mandatory gate as written.

### interview-feedback-F4 [Should-fix] SKILL.md promises a degrade:tier2-vs-tier3 signal the script never emits
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:127 (vs. scripts/transcribe.sh:111)
- Quote: "exits non-zero with a one-line install nudge and a `degrade:tier2` (notes present) or `degrade:tier3` (no notes) signal on stdout. Honor it:"
- Problem: `transcribe.sh` has exactly one degrade path — `degrade()` hardcodes `echo "degrade:tier3"` — and the script has no way to know whether notes exist (it never sees them). The tier-2/tier-3 discrimination SKILL.md attributes to the script is fiction; the model must make that call itself. A model that literally "honors" the stdout signal would route a notes-rich round to the tier-3 recall questionnaire (and, non-interactively, to the refusal path) when tier-2 notes grounding was available. The script's own header (transcribe.sh:14) confirms it only ever emits tier3.

### interview-feedback-F5 [Should-fix] storage.sh new-candidate neither copies inputs nor matches the documented folder layout
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:115 (vs. scripts/storage.sh:180 and SKILL.md:262)
- Quote: "`scripts/storage.sh new-candidate <role> <round> <candidate>` creates the folder and copies each raw input into `inputs/` verbatim (never mutate originals)."
- Problem: Two drifts against the script. (1) `cmd_new_candidate` takes no input paths and copies nothing — it only mkdirs `inputs/` (storage.sh:180-182) — yet SKILL.md credits the script with the verbatim copy, so the never-mutate-originals guarantee is actually unowned prose. (2) The § Storage layout documents the candidate dir as `<date>-<round>-<candidate>-<lastco>/` (SKILL.md:259-262) but the script creates `${_nc_round}-${_nc_candidate}` and its selftest pins `r1-jane-doe` (storage.sh:271) — no date, no lastco. `list` and any later run that globs the documented layout won't match what the sanctioned script created. One canonical layout, one owner (§K).

### interview-feedback-F6 [Should-fix] The "referenced in reco?" checklist gate is self-satisfying boilerplate
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/fill-scorecard.mjs:260 (vs. SKILL.md:203)
- Quote: "'\n    <p class=\"notes\" data-submission-ref>Recommendation accounts for the written-submission assessment above.</p>'"
- Problem: SKILL.md's submission checklist requires "referenced in reco? — the recommendation cites the submission assessment" and warns "a submission that is assessed but not reflected in the hire/no-hire call is an incomplete run" (SKILL.md:197). But `fill()` unconditionally injects this fixed sentence whenever any submission is passed, so the check can never fail regardless of whether the actual `notes:reco` content engages with the submission at all. The gate verifies the filler's own boilerplate, not the product requirement — checkbox theater that gives false assurance on exactly the judgment the checklist exists to force.

### interview-feedback-F7 [Should-fix] Submission-block placement keys on a CSS class, not the DOM contract's data-* anchors — silent drop on mismatch
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/fill-scorecard.mjs:254
- Quote: "const recoRe = /<section\\b[^>]*\\bclass\\s*=\\s*\"reco\"[^>]*>/;"
- Problem: The skeleton's stated contract is that fillers discover structure via machine anchors (`data-input="reco"` etc. — scorecard-skeleton.html:9-18 "a filler MUST be able to discover dimensions/scales/flags deterministically"), and every other pass in this file targets data-* attributes. The submission-assessment insertion alone anchors on the presentational `class="reco"`; a scorecard that keeps all data anchors but restyles/renames the class gets `if (rm)` = no match, and the whole submission block plus the reco reference silently vanish — no warning, no error — leaving the Phase Score checklist ("assessed in context?") to fail only if the model happens to notice.

### interview-feedback-F8 [Nit] Undocumented IFB_ROOT precedence level in root resolution
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/storage.sh:147 (vs. SKILL.md:114)
- Quote: "# Precedence: --root → IFB_ROOT → settings → default."
- Problem: SKILL.md Phase Resolve documents the precedence as `--root <path>` → settings → built-in default, but the script inserts an `IFB_ROOT` env override between flag and settings (and asserts it in selftest case 2). An env var that silently outranks the project's settings.yaml is exactly the kind of fact that belongs in the skill's documented precedence (§K) — as shipped, a set-and-forgotten IFB_ROOT redirects confidential candidate data somewhere the operator's settings say it shouldn't go.

### interview-feedback-F9 [Nit] Frozen non-interactive block cites `_shared/non-interactive.md`, which does not exist in this plugin's _shared/
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:78
- Quote: "sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`). Runtime and audit therefore share one decision contract"
- Problem: `plugins/pmos-managerkit/skills/_shared/` contains only `interview-guidelines/` — no `non-interactive.md` — so within this plugin the cite (and the `tools/audit-recommended.sh` path, which is pmos-toolkit-relative) dangles. I recognize the block is intentionally byte-frozen repo-wide, which is why this is a nit rather than worse, but the freeze bakes a toolkit-rooted relative path into every consumer plugin: a reader (or tool) resolving the cite from managerkit finds nothing. Either the canonical block should cite a plugin-neutral location or consumer plugins should carry the file.

### interview-feedback-F10 [Nit] Two phases are both named "Setup", and cross-refs to "Phase Setup" are ambiguous
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:52 (vs. headings at :95 and :222)
- Quote: "the verified-source research in Phase [Setup](#setup) and the rubric authoring run inline; no parallel work to degrade."
- Problem: The file has "Phase 0: Setup {#setup-load}" (pre-verb bootstrap) and "Phase: Setup {#setup}" (the setup verb). Slugs disambiguate for machines, but every prose mention of "Setup" forces the reader to work out which one is meant, and §J's slug discipline exists precisely so names stay unambiguous. Renaming one (e.g. "Phase 0: Load context") costs nothing and removes the collision; also note the phase set is otherwise un-numbered while "Phase 0" and "Phase N: Capture Learnings" imply a numbering that doesn't exist.

**Pass 1 verdict:** 1 blockers / 6 should-fix / 3 nits — material findings

## Pass 1 — author response

### interview-feedback-F1 — Accepted
- Fix: SKILL.md · Flags list (line 39) + Phase Transcribe preserve-guard paragraph (line 125) · before: "--force-transcribe … the new transcription is written to `transcript.whisper.txt` so the curated file is never clobbered" and "(it still routes to `transcript.whisper.txt`, never over the curated file)" → after: state the script's actual (and internally coherent) contract — WITHOUT the flag, a fresh transcription never clobbers an existing curated `transcript.refined.txt` (it routes to `transcript.whisper.txt`, the preserve guard); WITH `--force-transcribe`, the new transcription OVERWRITES `transcript.refined.txt` — that overwrite is exactly what makes it a destructive-opt-in flag per §I. Cite `transcribe.sh::resolve_transcript_target` as the single owner of target selection (§K) and keep the "surface the preserve: line" instruction. The script and its selftest are untouched — they are correct: the preserve guard is the default, the flag is the opt-out.
- Rationale: The script's semantics are the only coherent reading — if `--force-transcribe` also routed to `transcript.whisper.txt` it would be behaviorally identical to the no-flag default (which already writes whisper.txt when refined exists), i.e. a no-op flag; and the flag's own "(destructive-opt-in)" label in the same SKILL.md sentence agrees with the script, not with the "never clobbered" prose. So SKILL.md prose is the drifted side. Fixing prose also removes the data-loss trap in the opposite direction from the one the reviewer described: the current prose invites an operator to pass the flag casually believing it's safe.
- Blast radius: SKILL.md only (2 sentences). transcribe.sh + its selftest unchanged. Re-run `tools/lint-flags-vs-hints.sh` + `tools/lint-phase-refs.sh` after the edit (flag still hinted + handled). No substrate, no eval-rubric change.

### interview-feedback-F2 — Accepted
- Fix: scripts/check-citations.mjs · `runFiles`/`checkCitations` (around line 163) · before: `return failed === 0 ? 0 : 1` passes an artifact with zero `data-cite-tier` elements → after: add a deterministic coverage floor — when the parsed citation count is 0 AND the artifact contains at least one filled `data-input="notes:…"` or `data-input="reco"` slot with non-empty text, print `check-citations: 0 citations found in a filled artifact — grounding floor violated` and exit 1. Add two selftest cases (FAIL: filled notes, zero cites → exit 1; PASS: skeleton/empty-slots artifact with zero cites → exit 0, so the gate stays inert on unfilled sheets). Companion one-line SKILL.md edit at line 15: broaden the headline to "…refuses any output whose transcript-tier citation is not verbatim, or that carries no citations at all despite filled notes".
- Rationale: The reviewer is right that only fidelity, not presence, is enforced — a zero-cite filled scorecard currently green-lights INV-1. The floor is trivially deterministic (§H says script it). Conditioning on filled slots avoids false failures on legitimately empty/skeleton inputs and on the tier-3 path (which never produces a filled scorecard — the run refuses first).
- Blast radius: check-citations.mjs + its selftest (case count grows; run-tests.sh which invokes `--selftest` picks it up automatically), one sentence in SKILL.md. The interviewer-notes output (Phase Coach) also passes through this gate — it always carries cites when filled, so no behavior change there.

### interview-feedback-F3 — Accepted
- Fix: scripts/check-citations.mjs · `runFiles` (lines 156-159) + usage text + SKILL.md Phase Score gate paragraph (line 205) and Phase Coach (line 220) · before: `readFileSync(transcriptPath)` crashes ENOENT on tier-2/tier-3 runs; SKILL.md gives only the transcript-full invocation → after: (a) script accepts a missing transcript symmetrically with the existing submission handling — when the second positional is the literal `-` (or the file does not exist, with a stderr note), treat the transcript as null; any `data-cite-tier="transcript"` citation then FAILS with "transcript-tier citation but no transcript file was provided" (mirrors the existing FAIL-D submission-no-file case at line 117-121); notes/recalled/submission tiers validate as normal. Add selftest cases (PASS: notes-only artifact with `-` transcript; FAIL: transcript-tier cite with `-` transcript). (b) SKILL.md documents the notes-only invocation form: on a round with no transcript, run `scripts/check-citations.mjs filled-scorecard.html - [submission]`.
- Rationale: The gate is unconditional in Phase Score/Coach, so the most common degraded path (recruiter screen scored from notes) must be able to run it; the null-source pattern already exists in the same file for submissions, so this is a symmetric extension, not new machinery.
- Blast radius: check-citations.mjs (arg handling + 2-3 selftest cases), SKILL.md (gate invocation in Phase Score, mirrored mention in Phase Coach). No other skill consumes this script.

### interview-feedback-F4 — Accepted
- Fix: SKILL.md · Phase Transcribe "Graceful degrade" paragraph (line 127) · before: "exits non-zero with a one-line install nudge and a `degrade:tier2` (notes present) or `degrade:tier3` (no notes) signal on stdout. Honor it:" → after: state reality — the script emits exactly `degrade:tier3` (it never sees the notes, so it cannot discriminate); on receiving it, YOU (the model) pick the actual tier: interviewer notes present → ground at tier 2; no notes → tier-3 recall questionnaire (interactive-only, refusal path non-interactively). Cite transcribe.sh's header (§K) as the owner of the signal vocabulary.
- Rationale: The tier-2/tier-3 discrimination genuinely cannot live in the script (it receives only `<recording> <out-dir>`), and moving notes-awareness into it would couple transcription to input plumbing for no gain. The prose is the fiction; the fix makes the model's routing decision explicit instead of attributing it to a signal that never comes. Rejecting the alternative (teach the script about notes) on §K/simplicity grounds.
- Blast radius: SKILL.md only (one paragraph). transcribe.sh + selftest unchanged. Lints unaffected.

### interview-feedback-F5 — Accepted
- Fix: scripts/storage.sh · `cmd_new_candidate` (lines 170-183) + selftest case 4 (lines 267-273); SKILL.md Phase Resolve step 2 (line 115) + § Storage layout (line 262) · before: script builds `<round>-<candidate>` and copies nothing, while SKILL.md documents `<date>-<round>-<candidate>-<lastco>/` and credits the script with the verbatim input copy → after: make the script the single owner of BOTH facts (§K, and copy-verbatim is deterministic so §H says script it). New signature: `new-candidate <role-dir> <round> <candidate> [--lastco <co>] [--date <YYYY-MM-DD>] [input files…]`; it builds `<date>-<round>-<candidate>[-<lastco>]/` (date defaults to today; lastco segment omitted when not supplied), mkdirs `inputs/`, and `cp`s each input file verbatim into `inputs/` (dying on a missing input, never mutating originals). Update selftest case 4 to pin the dated name and assert a planted input file arrives byte-identical in `inputs/`. SKILL.md Phase Resolve step 2 passes the raw inputs through to the script; § Storage layout notes the `-<lastco>` segment is optional-when-unknown so layout and script agree.
- Rationale: Both drifts are real. The never-mutate-originals guarantee is currently unowned prose — exactly the deterministic invariant that belongs in a script with a selftest. Choosing the script (not prose demotion) as the fix direction because the documented dated layout is what `list` and future runs glob, and the date prefix is what keeps two rounds of the same candidate distinct.
- Blast radius: storage.sh (one subcommand + selftest), SKILL.md (2 spots), tests/run-tests.sh (picks up selftest automatically). Phase List's walk expectations already match the documented layout, so fixing the script toward the docs is the compatible direction. No other skill calls storage.sh.

### interview-feedback-F6 — Accepted
- Fix: scripts/fill-scorecard.mjs · submission pass (lines 252-266) + SKILL.md Phase Score step 3 / checklist (lines 197-203) · before: `fill()` unconditionally injects the fixed sentence "Recommendation accounts for the written-submission assessment above." whenever a submission is passed, so the "referenced in reco?" checklist box can never fail → after: the ref line's text comes from a required `values.submission.recoRef` field — the model-authored one-liner stating HOW the submission moved (or didn't move) the hire/no-hire call; when a submission is supplied but `recoRef` is missing/empty, the script exits non-zero with `submission present but values.submission.recoRef is empty — the reco must engage with the submission assessment` (deterministic presence gate, §H). SKILL.md's checklist item is rewritten to match: "referenced in reco? — `notes:reco`/`data-submission-ref` states how the submission influenced the recommendation (the filler refuses a submission fill without it)". Add a selftest case (FAIL: submission without recoRef; PASS: with one, and the authored text — not boilerplate — appears in the `data-submission-ref` element).
- Rationale: The reviewer's diagnosis is exact — the gate verifies the filler's own boilerplate. Presence-of-an-authored-linkage is deterministically checkable; the QUALITY of the linkage stays a model judgment in the checklist, but at least the box can now fail, and the fixed sentence can no longer masquerade as engagement.
- Blast radius: fill-scorecard.mjs (submission pass + selftest), SKILL.md checklist + step 3. values.json shape gains one key — documented at the Phase Score call site; no other consumer of fill-scorecard.mjs exists.

### interview-feedback-F7 — Accepted
- Fix: scripts/fill-scorecard.mjs · submission-block placement (line 254) · before: `const recoRe = /<section\b[^>]*\bclass\s*=\s*"reco"[^>]*>/;` with a silent `if (rm)` drop on no-match → after: anchor on the DOM contract, not presentation — locate the `data-input="reco"` element (the skeleton's machine anchor, scorecard-skeleton.html line 17/193), walk back to its enclosing `<section>` open tag and insert there; fall back to the `class="reco"` regex for legacy sheets; if NEITHER matches, do not drop silently — exit non-zero with `submission supplied but no reco anchor (data-input="reco" or class="reco") found — cannot place the submission assessment`. Add selftest cases: (a) scorecard with data anchors but a renamed class → block + ref land correctly; (b) no reco anchor at all → non-zero exit, not a silent omission.
- Rationale: The skeleton's own header declares data-* anchors as THE discovery contract; every other pass in the file honors it. Keying the one submission pass on a CSS class is drift, and the silent-drop failure mode defeats the very checklist ("assessed in context?") that depends on the block existing.
- Blast radius: fill-scorecard.mjs only (+selftest cases). Bundled scorecards instantiate the skeleton (which carries both `class="reco"` and `data-input="reco"`), so behavior on existing sheets is unchanged; only the mismatch path changes from silent to loud. Interacts with F6's edit in the same function — implement together.

### interview-feedback-F8 — Accepted
- Fix: SKILL.md · Phase Resolve step 1 (line 114) · before: "Precedence: `--root <path>` → `.pmos/settings.yaml :: managerkit.interview_root` → built-in default `./interviews/`" → after: insert the env level the script actually implements: `--root <path>` → `IFB_ROOT` env var → settings → built-in default, with a one-line caution that a set-and-forgotten `IFB_ROOT` silently outranks the project's settings (confidential data routing), so unset it when not deliberately testing/automating.
- Rationale: storage.sh:147 and selftest case 2 make IFB_ROOT load-bearing (used by tests and useful for automation), so removing it from the script would churn the selftest for no benefit; the §K fix is to document the real precedence in the skill's one canonical precedence sentence. Prose-only.
- Blast radius: SKILL.md one sentence. storage.sh untouched. No lint impact.

### interview-feedback-F9 — Accepted
- Fix: plugins/pmos-managerkit/skills/_shared/ · add `non-interactive.md` as a byte-identical copy of pmos-toolkit's `skills/_shared/non-interactive.md` (the canonical owner) · before: the frozen block's cite (`_shared/non-interactive.md`, SKILL.md:78) dangles within managerkit — `_shared/` holds only `interview-guidelines/` → after: the file exists in managerkit's `_shared/`, resolving the cite for all three managerkit skills that carry the frozen block (interview-feedback, interview-guide, one-on-one), and entering `sync-shared.sh`'s intersection so it stays aligned thereafter. The frozen block itself is NOT edited (byte-identity is lint-enforced repo-wide; a per-plugin rewording would break `tools/lint-non-interactive-inline.sh`).
- Rationale: This is precisely the documented bootstrap gap in the repo's CLAUDE.md ("the first time a consumer plugin needs a cross-plugin protocol, place the file there manually — a byte-identical copy from the canonical owner"). The reviewer's alternative (make the canonical block cite a plugin-neutral location) means re-freezing the block across every plugin — far larger blast radius for the same outcome.
- Blast radius: one new file under pmos-managerkit `_shared/`; fixes the same dangling cite in one-on-one and interview-guide as a side effect. sync-shared.sh intersection grows by one file. lint-non-interactive-inline.sh unaffected (block bytes unchanged). Note: the `tools/audit-recommended.sh` path in the block remains toolkit-rooted — accepted as-is because it is inside the frozen block and describes a CI-side tool, not a runtime resolve.

### interview-feedback-F10 — Accepted
- Fix: SKILL.md · heading "## Phase 0: Setup {#setup-load}" (line 95) · before: two phases both named "Setup" (`{#setup-load}` bootstrap vs `{#setup}` the setup verb) → after: rename the bootstrap heading to "## Phase 0: Load context {#setup-load}" — slug UNCHANGED per §J so no cross-ref can ghost — and sweep in-file prose for bare "Setup" mentions to confirm each unambiguously means the verb (line 52's "Phase [Setup](#setup)" already links the verb slug and stays). Decline the secondary numbering suggestion: "Phase 0" (always-first bootstrap) and "Phase N" (always-last learnings) are deliberate ordinal bookends around an otherwise verb-routed (non-sequential) phase set — the named phases run per-verb, so integer numbering between them would be false precision.
- Rationale: The name collision is real and costs a free rename to remove; keeping the slug stable makes the change zero-risk for `lint-phase-refs.sh` and external `interview-feedback/SKILL.md#setup` cites.
- Blast radius: SKILL.md one heading + prose sweep; run `tools/lint-phase-refs.sh` on the skill dir to confirm all `{#setup}` / `{#setup-load}` refs still resolve.

**Pass 1 author verdict:** 10/10 accepted (0 rejected, 0 invalid — every quote grounded and every cross-referenced script behavior reproduced).

## Pass 2 — reviewer findings

### interview-feedback-F11 [Should-fix] F2's coverage-floor trigger condition is self-contradictory — `data-input="reco"` always has non-empty text on a blank skeleton
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/scorecard-skeleton.html:193
- Quote: "<div class=\"reco-opts\" data-input=\"reco\">\n      <span data-reco=\"strong-no\">Strong no-hire</span>"
- Problem: The proposal's F2 fix fires the floor "when parsed citation count is 0 AND the artifact contains at least one filled `data-input=\"notes:…\"` or `data-input=\"reco\"` slot with non-empty text." But `data-input="reco"` is not a text slot — it is the selection CONTROL whose inner text is the four permanent option labels ("Strong no-hire / No-hire / Hire / Strong hire"). Its text is non-empty on every sheet, including the pristine skeleton. Implemented exactly as written, the floor trips on all unfilled scorecards, directly contradicting the proposal's own promised PASS selftest case ("skeleton/empty-slot artifact with zero cites — the gate stays inert on unfilled sheets"), and breaking the tier-3 refusal path the rationale says it protects. The condition must key on genuinely fillable text slots — `data-input="notes:…"` (empty in the skeleton, `:empty::before` placeholder is CSS-only) — or on a `data-selected` marker, never on the reco control's label text. As drafted, an implementer either ships a gate that fails every skeleton or silently rewrites the spec.

### interview-feedback-F12 [Should-fix] The F2 floor is inert on output (b) — a zero-citation interviewer-notes artifact still passes the same blocking gate
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:220
- Quote: "Re-run `check-citations.mjs` over this output too — the **same blocking STOP-before-done gate applies (INV-1)**"
- Problem: Phase Coach runs the identical INV-1 gate over `interviewer-notes.html`, but that artifact's contract (reference/interviewer-notes-skeleton.html) uses `data-output="interviewer-notes"` / `data-interviewer` / `ul.well` / `ul.improve` anchors and contains no `data-input` attributes anywhere. The F2 floor as specified conditions solely on filled `data-input="notes:…"`/`data-input="reco"` slots, so a completely un-cited coach write-up — the exact "0 passed, 0 failed → exit 0" failure F2 diagnoses — still green-lights on one of the two artifacts the gate is mandated to block. The floor needs a second trigger for output (b) (e.g., non-empty `<li>` claims inside a `data-output="interviewer-notes"` root, or presence of any `data-interviewer` section with prose), plus matching selftest cases; otherwise F2 closes only half the hole it names.

### interview-feedback-F13 [Nit] interviewer-notes-skeleton doc comment cites a rubric path that does not exist (`reference/interviewer-effectiveness.html`)
- Where: plugins/pmos-managerkit/skills/interview-feedback/reference/interviewer-notes-skeleton.html:6
- Quote: "each scored against reference/interviewer-effectiveness.html."
- Problem: The rubric lives at `../_shared/interview-guidelines/interviewer-effectiveness.html` (SKILL.md:220 and run-tests.sh:52 both point there); nothing named `interviewer-effectiveness.html` exists under this skill's `reference/`. A stale path from before the 260702-cqf move to the shared substrate — the skeleton is the template the Coach phase instantiates, so its doc comment is exactly the text a model reads while authoring output (b). One-line path fix; not covered by any proposal item.

### interview-feedback-F14 [Nit] transcribe.sh's own usage/header never mentions `--force-transcribe`, and F1 freezes the script — the self-documentation drift survives the fix
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/transcribe.sh:326
- Quote: "echo \"usage: transcribe.sh <recording> <out-dir> [--model <medium|base>]\" >&2"
- Problem: main() parses `--force-transcribe` (line 295) and it is the skill's one destructive-opt-in flag, yet the script's usage error and its header Usage comment (line 6) list only `--model`. F1 fixes SKILL.md's description of the flag but explicitly declares "scripts/transcribe.sh + its selftest are untouched," so after the proposal lands, the script still self-documents a flag surface that omits its most dangerous option. An operator running `transcribe.sh` bare gets a usage line that hides the overwrite flag entirely. One-line addition to both usage strings (no behavior change, selftest unaffected).

### interview-feedback-F15 [Nit] degrade nudge misnames the cause — "whisper model not found" even when ffmpeg or whisper-cli is the missing piece
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/transcribe.sh:127
- Quote: "if [ -z \"$model\" ] || ! command -v whisper-cli >/dev/null 2>&1 || ! command -v ffmpeg >/dev/null 2>&1; then"
- Problem: All three missing-dependency conditions funnel into the single degrade() message "whisper model not found — install whisper.cpp + a ggml model under ~/whisper-models/…" (line 110). An operator with a model and whisper-cli installed but no ffmpeg is told to install a model they already have — the "one-line install nudge" SKILL.md promises points at the wrong install. Cheap fix: branch the stderr line on which check failed (the `degrade:tier3` stdout signal stays identical, so F4's contract is untouched).

### interview-feedback-F16 [Nit] F5's dated-layout switch has no back-compat story for candidate dirs the old signature already created
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/storage.sh:180
- Quote: "_nc_cand_dir=\"${_nc_role_dir}/${_nc_round}-${_nc_candidate}\""
- Problem: Today's script has been shipping `<round>-<candidate>` dirs (selftest pins `r1-jane-doe`) into users' confidential storage roots. F5 moves the script to `<date>-<round>-<candidate>[-<lastco>]/` and updates the docs, but says nothing about existing roots: Phase Resolve step 2 ("resolve the round folder … per the storage layout") and Phase List's prose walk now describe only the dated shape, so previously-created candidate folders no longer match the documented discovery contract. Also unaddressed in the rewrite: the layout assumes kebab names (`jane-doe`) but the script performs no normalization, so `new-candidate … "Jane Doe"` mints a dir with a space that the documented `<candidate>` segment never anticipates. A one-line tolerance note (list/locate must match both generations) plus a slugify pass in the new `cmd_new_candidate` closes both.

**Pass 2 verdict:** 0 blockers / 2 should-fix / 4 nits — material findings

## Pass 2 — author response

### interview-feedback-F11 — Accepted
- Fix: proposal F2 spec (implemented in scripts/check-citations.mjs) · before: floor trigger "at least one filled `data-input=\"notes:…\"` or `data-input=\"reco\"` slot with non-empty text" → after: drop `data-input="reco"` from the trigger entirely — it is the selection control whose inner text is the four permanent option labels (skeleton line 193-198), non-empty on every sheet including the pristine skeleton. The floor keys on genuinely fillable slots only: (i) any `data-input="notes:…"` element with non-empty text content (these are empty in the skeleton; the placeholder is CSS `:empty::before` only), OR (ii) any element carrying a `data-selected` marker (a made recommendation on an otherwise note-less sheet still counts as "filled"). The promised selftest pair stays and now actually holds: PASS on the pristine skeleton (zero cites, no filled notes, no data-selected), FAIL on filled notes with zero cites; add a third case — data-selected reco but empty notes, zero cites → FAIL.
- Rationale: The reviewer caught a genuine self-contradiction in my F2 spec — as written, the floor trips on every unfilled sheet, contradicting the spec's own PASS case, and an implementer would either ship a broken gate or silently rewrite the spec. The label-text observation is exact; conditioning on notes-slot text + data-selected preserves F2's intent (a filled artifact must carry cites) without any skeleton false-positive.
- Blast radius: check-citations.mjs + selftest (same files F2 already touches — this amends F2, no new surface). SKILL.md wording unchanged from the F2 edit.

### interview-feedback-F12 — Accepted
- Fix: proposal F2 spec (scripts/check-citations.mjs) + one clause in SKILL.md Phase Coach (line 220) · before: floor conditions solely on scorecard `data-input` slots, so the identical INV-1 gate run over `interviewer-notes.html` (which has NO data-input anywhere — its anchors are `data-output="interviewer-notes"` / `data-interviewer` / `ul.well` / `ul.improve`) stays inert: a zero-citation coach write-up still exits 0 → after: add a second, output-(b)-shaped trigger — when the artifact's root carries `data-output="interviewer-notes"` AND at least one `data-interviewer` section contains a non-empty `<li>` (a claim), zero parsed citations → floor violation, exit 1. Selftest cases: FAIL (interviewer-notes artifact with populated well/improve lists, zero cites), PASS (interviewer-notes skeleton with empty lists, zero cites). SKILL.md Phase Coach gains half a sentence noting the coverage floor applies to this output too.
- Rationale: Correct — my F2 rationale ("Phase Coach output always carries cites when filled") was an assertion of model behavior, which is exactly the failure mode F2 exists to close; the gate must enforce it. The two artifact shapes are disjoint (data-input vs data-output roots), so the two triggers cannot cross-fire.
- Blast radius: check-citations.mjs + selftest (amends F2; implement F2+F3+F11+F12 as one change set), SKILL.md one clause in Phase Coach. No other consumers.

### interview-feedback-F13 — Accepted
- Fix: reference/interviewer-notes-skeleton.html · doc comment line 6 · before: "each scored against reference/interviewer-effectiveness.html." → after: "each scored against ../_shared/interview-guidelines/interviewer-effectiveness.html." — matching SKILL.md Phase Coach (line 220) and tests/run-tests.sh:52, which both already point at the shared substrate.
- Rationale: Verified: `reference/` contains only interviewer-notes-skeleton.html; the rubric lives in `../_shared/interview-guidelines/`. Stale pre-substrate-move path in exactly the doc comment the model reads while authoring output (b) — a wrong resolve at authoring time. One-line fix, no behavior.
- Blast radius: one comment line in one skeleton. No script, lint, or test touches that string.

### interview-feedback-F14 — Accepted
- Fix: scripts/transcribe.sh · header Usage comment (line 6) + usage error string (line 326) · before: both list only `[--model <medium|base>]` while main() parses `--force-transcribe` (line 295), the skill's one destructive-opt-in flag → after: both strings read `transcribe.sh <recording> <out-dir> [--model <medium|base>] [--force-transcribe]`, with the header comment gaining a half-line "(--force-transcribe OVERWRITES an existing transcript.refined.txt)". String-only edit; no parsing or routing change, so the selftest (which pins routing behavior, not usage text) is unaffected — F1's "script untouched" stance applied to BEHAVIOR, and I amend it to permit this self-documentation string fix.
- Rationale: The reviewer is right that F1 as scoped leaves the script hiding its most dangerous flag from anyone who runs it bare. §I says machine-coupled/destructive flags get first-class documentation; the two usage strings are the script's own doc surface.
- Blast radius: transcribe.sh two strings (+ optional half-line header note). Selftest unchanged. lint-flags-vs-hints operates on SKILL.md, not scripts — unaffected.

### interview-feedback-F15 — Accepted
- Fix: scripts/transcribe.sh · degrade path (degrade() at ~line 108-113 and its call site at line 127) · before: all three missing-dependency conditions (`[ -z "$model" ]`, no whisper-cli, no ffmpeg) funnel into one stderr message "whisper model not found — install whisper.cpp + a ggml model…" → after: the call site determines WHICH check failed and passes a cause string into degrade() (e.g. "no ggml model under ~/whisper-models/", "whisper-cli not on PATH — install whisper.cpp", "ffmpeg not on PATH — install ffmpeg"); degrade() prints the cause-specific one-line nudge on stderr, and the stdout signal stays exactly `degrade:tier3` with exit 3, so F4's signal-vocabulary contract and the header (line 14) are untouched. Optionally extend the selftest degrade case to assert the stderr line names the actually-missing dependency (bash-3.2-safe PATH-shadowing already used by the existing degrade selftest).
- Rationale: An operator with a model installed but no ffmpeg is told to install a model they have — the SKILL.md-promised "one-line install nudge" points at the wrong install. Cheap, behavior-preserving on stdout/exit-code, and it directly serves the degrade UX the skill sells.
- Blast radius: transcribe.sh (degrade + call site, ~6 lines) + optionally one selftest assertion. SKILL.md unchanged (its F4-edited prose says "a one-line install nudge" generically — still true). F4's contract explicitly preserved.

### interview-feedback-F16 — Accepted
- Fix: two amendments to proposal F5 · (a) scripts/storage.sh `cmd_new_candidate` gains a slugify pass — round/candidate/lastco segments are lowercased, whitespace/underscores → hyphens, non-[a-z0-9-] stripped, so `new-candidate … "Jane Doe"` mints `…-jane-doe` and the documented `<candidate>` kebab segment is an enforced invariant, not an assumption; selftest case asserts `"Jane Doe"` → `jane-doe`. (b) SKILL.md § Storage layout + Phase List prose gain a one-line back-compat tolerance: candidate dirs created before the dated layout are `<round>-<candidate>` (no date/lastco prefix) — `list`/locate walks MUST match both generations (match on the `<round>-<candidate>` core, treat the leading `<date>-` and trailing `-<lastco>` as optional), and never rename existing dirs (confidential storage roots are user data, not ours to migrate).
- Rationale: Both sub-points are real gaps in F5 as drafted: the old `r1-jane-doe` shape is already sitting in users' roots, so documenting only the new shape breaks the discovery contract for existing data; and an unslugified multi-word candidate breaks the layout grammar the docs promise. Tolerant-read + strict-write is the standard posture; a migration/rename of confidential dirs is explicitly out of scope.
- Blast radius: storage.sh (slugify inside the F5-rewritten cmd_new_candidate + selftest case), SKILL.md (one tolerance line in § Storage layout, echoed in Phase List). Amends F5 — same files, implement together.

**Pass 2 author verdict:** 6/6 accepted (0 rejected, 0 invalid — every quote verified verbatim against the files; F11/F12 are corrections to my own F2 spec, F16 amends F5, F14 relaxes F1's "script untouched" scope for string-only self-documentation).

## Pass 3 — reviewer findings

### interview-feedback-F17 [Should-fix] F2/F11/F12 floor triggers are token-trap-prone: the pristine skeleton's own CSS and doc-comment contain `data-selected`, and the spec neither mandates tag-scoped detection nor pins the selftest fixture to the shipped skeleton
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/scorecard-skeleton.html:84 (also :14, :102, :109)
- Quote: ".scale [data-v][data-selected]{background:var(--accent);color:#fff;border-color:var(--accent);font-weight:700}"
- Problem: The proposal's Trigger A fires on "any element carrying a `data-selected` marker", and its promised PASS case is "pristine scorecard skeleton, zero cites". But the pristine skeleton contains the literal token `data-selected` in three CSS selectors (lines 84/102/109) and in doc-comment prose (line 14: "selectable score; data-selected on the chosen one."). A grep-shaped implementation trips the floor on every sheet ever emitted (the CSS ships in all of them). This exact trap class already fired once in this very gate — check-citations.mjs:62 records "(Dogfood-caught: the skeleton's doc-comment tripped the gate.)" — yet the proposal's spec says nothing about comment-stripping or opening-tag-attribute scoping for the NEW trigger scan. The only guard is the PASS selftest case, and it only guards if the fixture is the SHIPPED skeleton (a synthesized minimal fixture without the `<style>` block lets the naive scan go green and ship). The spec must either mandate parseCitations-style opening-tag attribute detection after comment-stripping, or pin the PASS fixture to `_shared/interview-guidelines/scorecard-skeleton.html` (as questionnaire.mjs's selftest already does at scripts/questionnaire.mjs:116).

### interview-feedback-F18 [Should-fix] transcribe.sh has two silent-failure modes that contradict its own "never fails hard" contract — untouched by the proposal
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/transcribe.sh:139 (and :166, :171, :15)
- Quote: "ffmpeg -i \"$recording\" -vn -ar 16000 -ac 1 -y \"$wav\" >/dev/null 2>&1"
- Problem: The header (line 15) promises "It never fails hard / never crashes the caller." But under `set -euo pipefail`, an ffmpeg failure on a corrupt/unsupported recording kills the script at line 139 with ffmpeg's exit code, NO `degrade:` signal on stdout, and NO diagnostic (stderr is redirected to /dev/null) — the caller gets an opaque non-zero. Conversely, every `whisper-cli` invocation is `|| true`'d (lines 166, 171), so a whisper crash yields a ZERO-BYTE `transcript.refined.txt` printed as success (exit 0) — a fabricated-empty grounding source presented as clean. The proposal's transcribe.sh items (F14 usage strings, F15 degrade-cause nudge) are string-only and F1/F4 explicitly freeze routing/signals, so this behavior gap survives all sixteen accepted fixes. Minimum fix: check ffmpeg's exit and emit a distinct diagnostic + non-zero contract exit; refuse to print success when the transcript file is empty.

### interview-feedback-F19 [Should-fix] F6 as specced breaks the three existing submission selftest fixtures — none carries `recoRef`, so "exits non-zero when recoRef is missing" makes the current selftest fail, and throw-vs-exit for library-called fill() is unspecified
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/fill-scorecard.mjs:575 (also :596-607, :622-629)
- Quote: "liveContext: 'Used the live discussion to structure the missing parts rather than restating. SUB_CONTEXT_TOKEN',"
- Problem: F6 requires that when a submission is supplied without `values.submission.recoRef`, "the script exits non-zero with a clear message". The existing selftest calls `fill()` directly with three submission fixtures (`filledSub` ~:565, `filledNoBrief` ~:596, `filledPre` ~:622) and NONE has a `recoRef` key — so implemented literally (a `process.exit` inside the fill path) the selftest dies mid-run; implemented as a throw, all three fixtures still abort their assertions. The cross-cutting note's "keep existing selftest cases green and extend, don't rewrite" is therefore unsatisfiable for fill-scorecard without modifying those fixtures, and the proposal never says to. The dangerous wrong implementation this vagueness invites: making recoRef optional-with-warning "to keep the fixtures green", which reinstates exactly the checkbox theater F6 exists to kill. The spec should (a) state that the presence gate lives in the CLI `fill` command (or that `fill()` throws and the CLI maps it to exit≠0), and (b) explicitly amend the three existing fixtures to carry an authored `recoRef`.

### interview-feedback-F20 [Nit] F3's "or a missing file" leniency softens the blocking gate: a typo'd or never-written transcript path degrades to a soft pass instead of a loud ENOENT
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__interview-feedback.md:56
- Quote: "the script accepts the literal `-` as the transcript positional (or a missing file, with a stderr note) and treats the transcript as null"
- Problem: The explicit `-` sentinel is the right design — the caller declares "no transcript" deliberately. The parenthetical missing-file tolerance is a different animal: today `check-citations.mjs filled-scorecard.html transcript.refined.txt` run against a wrong cwd or a transcription that never wrote the file crashes ENOENT (loud, unambiguous); after F3 it proceeds with transcript=null, and if the artifact happens to carry only notes-tier cites the blocking INV-1 gate exits 0 on the back of an operator error. A stderr note is advisory; §H-style hard gates shouldn't auto-null a positional the caller explicitly supplied. Drop the missing-file clause and keep only the literal `-` (missing file stays a hard error).

### interview-feedback-F21 [Nit] F13's replacement path is wrong from the file's own directory — `../_shared/…` from `reference/` resolves to a nonexistent dir; it needs `../../_shared/…`
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__interview-feedback.md:140
- Quote: "\"each scored against ../_shared/interview-guidelines/interviewer-effectiveness.html.\" — matching SKILL.md Phase Coach"
- Problem: The stale-path fix copies the `../_shared/` prefix from SKILL.md and run-tests.sh, but those resolve relative to the SKILL DIRECTORY (SKILL.md sits at the skill root; run-tests.sh computes `$SKILL_DIR/../_shared`). The doc comment being fixed lives one level deeper, in `reference/interviewer-notes-skeleton.html`, so relative to its own location `../_shared/` is `skills/interview-feedback/_shared/` — which does not exist. The corrected comment should read `../../_shared/interview-guidelines/interviewer-effectiveness.html` (or state the path skill-root-relative). As written, the fix replaces one dangling path with another off-by-one-level dangling path in exactly the comment the model reads while authoring output (b).

### interview-feedback-F22 [Nit] F12's promised PASS fixture ("interviewer-notes skeleton with empty lists, zero cites") does not match the shipped skeleton, whose template `<li>`s are placeholder-filled and which carries literal sub-40-char cite elements
- Where: plugins/pmos-managerkit/skills/interview-feedback/reference/interviewer-notes-skeleton.html:62
- Quote: "<cite data-cite-tier=\"transcript\" data-source=\"transcript.refined.txt\">{{verbatim ≥40-char quote}}</cite>"
- Problem: The shipped skeleton is neither empty-list nor zero-cite: its `ul.well`/`ul.improve` template `<li>`s contain `{{strength}}`/`{{improvement}}` placeholder text (non-empty inner text, so they'd read as "filled" to Trigger B), and it carries two REAL (non-commented) `<cite data-cite-tier>` elements whose `{{verbatim ≥40-char quote}}` placeholder is ~28 chars — so running the gate on the actual skeleton exits 1 on the length check, not 0. An implementer who reads "interviewer-notes skeleton" as the bundled file and copies it into the selftest gets a PASS case that fails for a reason unrelated to the floor. Note the asymmetry with F17: for the scorecard the fixture MUST be the shipped skeleton (to catch the CSS token trap), while for the notes skeleton it must NOT be (or the triggers must be `{{…}}`-placeholder-aware). The spec should name the fixture explicitly in both cases.

### interview-feedback-F23 [Nit] Floor Trigger A is blind to work-history-only fills — role:/trajectory: prose slots don't count as "filled", so a role-evidence write-up with zero cites can still pass the floor
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:148
- Quote: "Every subjective per-role/trajectory note carries the same `<cite data-cite-tier=…>` grounding the competency notes do."
- Problem: Trigger A keys on non-empty `data-input="notes:…"` text or a `data-selected` marker. A work-history sheet whose `data-input="role:scope|contribution|result"` and `data-input="trajectory:…"` slots are filled with subjective prose — but whose `data-measured`/`data-verdict`/score selections were omitted (all optional in fill-scorecard.mjs: `if (data.measured != null)`, `if (traj.verdict != null)`) and whose notes: slots are empty — is a filled, claim-bearing artifact that the zero-cite floor waves through, despite SKILL.md line 148 mandating cites on exactly those notes. Low likelihood in practice (a real fill usually selects scores too), but since F12 exists precisely to close the "floor inert on one artifact shape" hole, Trigger A should also count non-empty `data-input="role:…"`/`data-input="trajectory:…"` slots.

**Pass 3 verdict:** 0 blockers / 3 should-fix / 4 nits — material findings

## Pass 3 — author response

### interview-feedback-F17 — Accepted
- Fix: proposal F2/F11/F12 spec (scripts/check-citations.mjs + selftest) · before: Trigger A fires on "any element carrying a `data-selected` marker" with no detection-method mandate and an unpinned PASS fixture → after: (a) the spec MANDATES the new trigger scan reuse the file's existing detection discipline — strip HTML comments first (the same `html.replace(/<!--[\s\S]*?-->/g,'')` pass parseCitations already does) and detect `data-selected` ONLY as an attribute inside an OPENING TAG (parseCitations-style open-tag scan), never as a raw substring — because the pristine skeleton carries the literal token `data-selected` in three CSS selectors (scorecard-skeleton.html:84/:102/:109) and in doc-comment prose (:14), and a grep-shaped scan would trip the floor on every sheet ever emitted; same tag-scoped discipline for the `data-input="notes:…"` non-empty-text check (the CSS also contains `data-input^="notes"` selectors at :85). (b) The PASS selftest fixture is PINNED to the shipped `_shared/interview-guidelines/scorecard-skeleton.html`, loaded by path exactly as questionnaire.mjs's selftest already does (questionnaire.mjs:116) — a synthesized minimal fixture without the `<style>` block would let a naive scan go green.
- Rationale: This exact trap class already fired once in this very gate — check-citations.mjs:62 records "(Dogfood-caught: the skeleton's doc-comment tripped the gate.)" — and my F11-corrected spec still left the detection method unstated. Both halves (mandated method + pinned fixture) are needed: the fixture alone only guards if it contains the trap tokens, and the method alone is unverified without the fixture.
- Blast radius: none beyond F2's files — this amends the F2/F11/F12 change-set spec; check-citations.mjs + selftest, no SKILL.md change.

### interview-feedback-F18 — Accepted
- Fix: scripts/transcribe.sh · wav extraction (line 139), whisper invocations (lines 166, 171), a final transcript guard, header contract line (line 15) + selftest; one clause in SKILL.md Phase Transcribe · before: under `set -euo pipefail` (line 20) an ffmpeg failure on a corrupt/unsupported recording kills the script with ffmpeg's exit code, no `degrade:` signal, no diagnostic (stderr → /dev/null); conversely every whisper-cli call is `|| true`'d so a whisper crash yields a ZERO-BYTE transcript.refined.txt printed as success (exit 0) — a fabricated-empty grounding source → after: (a) wrap the wav extraction in an explicit check (`if ! ffmpeg …`), tee ffmpeg's stderr to `<out-dir>/ffmpeg.log` instead of /dev/null, and on failure print `error: ffmpeg could not decode <recording> — see <out-dir>/ffmpeg.log` on stderr and exit with a NEW distinct code (4) — no `degrade:` signal, because a bad input is not a missing dependency; same guard on per-chunk ffmpeg. (b) keep per-chunk whisper `|| true` (one bad chunk shouldn't kill a long recording) but add a hard final guard: if the transcript target file is empty (`! -s`) after all whisper runs, print `error: whisper produced an empty transcript — see <out-dir>/whisper.log` (whisper stderr now teed to a log, not /dev/null) and exit 4 instead of printing the success lines. (c) header line 15 rescoped: "never fails hard" applies to MISSING DEPENDENCIES (the degrade:tier3 path, exit 3); input/transcription failures exit 4 with a diagnostic and no degrade signal. (d) selftest: PATH-shadow a failing ffmpeg → exit 4 + error line + no degrade signal; PATH-shadow a whisper-cli that writes nothing → exit 4, no success line. (e) SKILL.md Phase Transcribe gains one clause distinguishing exit 3 (`degrade:tier3` — route tiers) from exit 4 (input/transcription error — surface the log, do not degrade).
- Rationale: Both silent modes are real and contract-violating on the script's own header. The empty-transcript-as-success mode is the worse one — it hands the Ground phase a fabricated-empty grounding source with a green exit code, corrupting the skill's core grounding promise. Exit-code separation (3=degrade, 4=error) keeps F4's signal vocabulary and F15's cause-nudge intact — those contracts are explicitly preserved.
- Blast radius: transcribe.sh (~15 lines) + selftest cases; one clause in SKILL.md Phase Transcribe. F1/F4's "routing + degrade:tier3 contract byte-identical" stance is preserved (routing and the degrade path are untouched; this adds a new, previously-undefined error path). F14/F15 same file — implement as one transcribe.sh change set.

### interview-feedback-F19 — Accepted
- Fix: proposal F6 spec (scripts/fill-scorecard.mjs + selftest) · before: F6 says "the script exits non-zero" when recoRef is missing, but the selftest calls fill() directly with three submission fixtures (filledSub ~:565, filledNoBrief ~:596, filledPre ~:622) none of which carries recoRef — implemented literally the selftest dies mid-run, and the vague spec invites the dangerous "optional-with-warning" cop-out → after: the spec states BOTH halves explicitly: (a) the presence gate is `fill()` THROWING (`throw new Error('submission present but values.submission.recoRef is empty — the reco must engage with the submission assessment')`); the CLI `fill` entrypoint catches it and maps to exit 1 with the message on stderr — so library callers (the selftest) get a catchable error and CLI callers get the non-zero exit. recoRef is NEVER optional-with-warning — that would reinstate exactly the checkbox theater F6 kills. (b) All three existing submission fixtures are amended to carry an authored `recoRef` (with sentinel tokens, e.g. `SUB_RECOREF_TOKEN` / `NB_RECOREF_TOKEN` / `PRE_RECOREF_TOKEN`), with an assertion that the token — not boilerplate — lands inside the `data-submission-ref` element; plus one new FAIL case: fill() with a submission lacking recoRef, wrapped in try/catch, asserting the throw. The cross-cutting note's "keep existing selftest cases green and extend, don't rewrite" is amended: fixture INPUTS may be minimally extended to satisfy a newly-gated required field; assertions are extended, not rewritten.
- Rationale: The reviewer is right that F6 as drafted was unimplementable against the shipped selftest and under-specified on throw-vs-exit; both gaps invite the one wrong implementation (optional recoRef) that nullifies the finding F6 fixed. Naming the sentinel-token pattern keeps the fixtures consistent with the file's existing style.
- Blast radius: none beyond F6's files — fill-scorecard.mjs + selftest (same F6+F7 change set). values.json contract unchanged from F6 (recoRef already added there).

### interview-feedback-F20 — Accepted
- Fix: proposal F3 spec (scripts/check-citations.mjs) · before: F3 accepts "the literal `-` (or a missing file, with a stderr note)" as transcript-null → after: DROP the missing-file leniency entirely. Only the explicit literal `-` declares "no transcript"; a supplied path that does not exist remains a hard error (may be upgraded from a raw ENOENT stack to a clean one-line `error: transcript file not found: <path>` + exit ≥1, but never a soft pass). Selftest gains one case: nonexistent transcript path → non-zero exit, not null-transcript behavior.
- Rationale: The reviewer's distinction is exactly right — `-` is a deliberate caller declaration; a missing file is an operator error (typo, wrong cwd, transcription that never ran), and auto-nulling it lets a notes-cite-only artifact pass the blocking INV-1 gate on the back of that error. Hard gates don't auto-degrade an explicitly supplied positional (§H posture).
- Blast radius: none beyond F3's files; removes a clause rather than adding surface.

### interview-feedback-F21 — Accepted
- Fix: proposal F13 spec (reference/interviewer-notes-skeleton.html doc comment, line 6) · before: my replacement path read "../_shared/interview-guidelines/interviewer-effectiveness.html", copied from SKILL.md/run-tests.sh which resolve from the SKILL ROOT — but the doc comment lives one level deeper in `reference/`, from which `../_shared/` resolves to `skills/interview-feedback/_shared/` (nonexistent) → after: the comment reads `../../_shared/interview-guidelines/interviewer-effectiveness.html` (correct from the file's own directory: reference/ → interview-feedback/ → skills/ → _shared/), verified by resolving the path relative to the file before committing.
- Rationale: Reviewer caught my fix replacing one dangling path with another off-by-one-level dangling path — in exactly the comment the model reads while authoring output (b). Verified the directory shapes: `skills/interview-feedback/reference/` and `skills/_shared/interview-guidelines/` — two levels up is required.
- Blast radius: unchanged from F13 — one comment line, nothing executes it.

### interview-feedback-F22 — Accepted
- Fix: proposal F12 spec (check-citations.mjs selftest fixtures) · before: F12 promised a PASS case on "interviewer-notes skeleton with empty lists, zero cites" — but the shipped reference/interviewer-notes-skeleton.html is neither: its template `<li>`s carry `{{strength}}`/`{{improvement}}` placeholder text (non-empty → Trigger B reads "filled") and it contains two live (non-commented) `<cite data-cite-tier="transcript">` elements whose `{{verbatim ≥40-char quote}}` placeholder is ~28 chars — the gate exits 1 on the fidelity length check regardless of the floor → after: the spec names both fixtures explicitly and asymmetrically: (a) scorecard-shape PASS fixture = the SHIPPED `_shared/interview-guidelines/scorecard-skeleton.html`, loaded by path (per F17 — it must contain the CSS token traps); (b) interviewer-notes-shape PASS/FAIL fixtures = SYNTHESIZED minimal documents (PASS: `data-output="interviewer-notes"` root, one `data-interviewer` section, empty `ul.well`/`ul.improve`, zero cites → exit 0; FAIL: same but with a non-empty prose `<li>` and zero cites → exit 1). The shipped notes skeleton is NOT used as a gate fixture — it is a template with live placeholder markup, not a passing artifact — and a one-line comment in the selftest records why.
- Rationale: The reviewer caught that my two promised PASS cases pull in opposite directions (shipped-file-required vs shipped-file-unusable) and the spec never said which file "skeleton" meant in each. Naming both removes the last fixture ambiguity in the F2 change set. I decline the alternative (making Trigger B `{{…}}`-placeholder-aware) — teaching the gate to recognize template mustaches adds scan surface for zero production value, since no terminal artifact legitimately contains them.
- Blast radius: none beyond F2/F12's files — selftest fixture construction only.

### interview-feedback-F23 — Accepted
- Fix: proposal F2/F11 spec (check-citations.mjs Trigger A) + selftest · before: Trigger A keys only on non-empty `data-input="notes:…"` text or a `data-selected` marker, so a work-history sheet with subjective prose in `data-input="role:scope|contribution|result"` / `data-input="trajectory:…"` slots but no score selections and empty notes: slots passes the floor un-cited, despite SKILL.md:148 mandating cites on exactly those notes → after: Trigger A additionally counts non-empty text in the SUBJECTIVE work-history slots — `data-input="role:scope"`, `role:contribution`, `role:result`, and all `data-input="trajectory:…"` slots — but NOT the purely factual `role:company|title|tenure` slots (a sheet with only factual header fields filled carries no subjective claim needing a cite, and SKILL.md:148's mandate is scoped to "subjective per-role/trajectory note"). Verified these slots are empty in the shipped skeleton (scorecard-skeleton.html:161-166 — all `<div data-input="role:…"></div>`), so the pinned-skeleton PASS case (F17) still holds. Same tag-scoped, comment-stripped detection discipline as F17. Selftest adds: role:contribution filled with prose, everything else empty, zero cites → FAIL.
- Rationale: The gap is real and exactly the shape F12 closed for output (b) — an artifact surface the floor is blind to. Scoping to the subjective slots keeps the trigger aligned with the skill's own grounding mandate and avoids a false positive on a factual-fields-only partial sheet.
- Blast radius: none beyond F2's files — one more clause in Trigger A + one selftest case; the disjointness argument vs Trigger B is unaffected (still data-input vs data-output roots).

**Pass 3 author verdict:** 7/7 accepted (0 rejected, 0 invalid — every quote verified against the files; F17/F22/F23 amend the F2 change-set spec, F19 amends F6, F20 amends F3, F21 corrects F13, F18 is the sole new-surface fix).

## Final author closeout

Reviewed the full scratchpad for any reviewer-findings section lacking an author response: there is none. Passes 1, 2, and 3 each carry a complete author-response section; every finding F1–F23 is dispositioned (23 accepted, 0 rejected, 0 invalid), and the cumulative proposal already reflects all of them, including the pass-2/3 corrections to my own pass-1 specs (F11/F12/F17/F22/F23 → F2, F16 → F5, F19 → F6, F20 → F3, F21 → F13, F14 → F1 scope).

Closeout spot-checks re-verified grounding against the current files (all hold verbatim):
- scorecard-skeleton.html:14/:84/:102 — literal `data-selected` tokens in doc-comment + CSS (F17's token trap).
- check-citations.mjs:62 — "(Dogfood-caught: the skeleton's doc-comment tripped the gate.)" (F17's precedent).
- transcribe.sh:139 — unguarded `ffmpeg -i "$recording" … >/dev/null 2>&1` under `set -euo pipefail` (F18).
- fill-scorecard.mjs:575 — `liveContext: 'Used the live discussion to structure the missing parts…'` fixture, and zero occurrences of `recoRef` anywhere in the file (F19).

No new findings to accept or reject in this pass. No material finding remains unaddressed; no disagreements are open (the only declined items are within-finding alternatives, each argued inline: F9 plugin-neutral re-freeze, F10 integer numbering, F22 placeholder-aware Trigger B). The review loop for this unit is CLOSED.

**Final status: CONVERGED** — 23 accepted / 0 rejected across 3 passes; the proposal at docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__interview-feedback.md is the final self-contained deliverable. No changes have been implemented; the proposal describes intended edits only.
