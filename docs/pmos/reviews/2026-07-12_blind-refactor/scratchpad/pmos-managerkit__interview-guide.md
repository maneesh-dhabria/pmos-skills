## Pass 1 — reviewer findings

### interview-guide-F1 [Should-fix] Duration parser silently mis-parses ranges and decimals instead of refusing ambiguity
- Where: plugins/pmos-managerkit/skills/interview-guide/scripts/validate-scorecard-anchors.mjs:504 (parser at :156-167)
- Quote: "integer of minutes on stdout (exit 0), or exit non-zero when it cannot be parsed unambiguously so the"
- Problem: The contract (INV-6/A1, SKILL.md:160 "re-prompt rather than guessing") promises exit-non-zero on ambiguity, but `parseDuration` sums every leftover integer, so ambiguous answers succeed with wrong numbers. Verified live: `--check-duration '90-120 mins'` → 210, exit 0, NO warning (in-band, fully silent); `'1.5 hours'` → 301 (should be 90 or a refusal); `'45 mins each of 2 parts'` → 47. A range is a completely plausible operator answer to "How long is this round scheduled to run?", and the mis-parsed value flows straight into the `data-duration`/`data-budget` machine anchors and the per-area minute plan. The §H gate that exists precisely so the model never guesses is itself guessing. Fix: refuse (exit 1) when more than one unlabeled integer remains after unit consumption unless joined by an explicit `+`, and reject decimals adjacent to a unit; add these as selftest cases.

### interview-guide-F2 [Should-fix] Anchor validator's "per dim" checks are file-global — a duplicate-id or empty dimension section passes the hard gate
- Where: plugins/pmos-managerkit/skills/interview-guide/reference/output-shapes.md:49 (validator at scripts/validate-scorecard-anchors.mjs:60-73)
- Quote: "per dim: `data-weight=\"<n>\"` (integer; **all weights sum to exactly 100**); a `data-scale=\"1-4\"`"
- Problem: output-shapes.md documents scale, notes slot, and both flag containers as per-dimension requirements, but the script checks `data-scale`, `data-v`, `data-flags="green"`, and `data-flags="red"` only once file-wide, and never rejects duplicate `data-dim` ids. Verified live: a sheet with two `data-dim="user-empathy"` sections where the second has NO scale, NO notes, and NO flags prints "✓ scorecard anchors: valid (2 dimensions, weights sum to 100, reco control present)" and exits 0. Duplicate ids and hollow sections are exactly the copy-paste failure modes a model authoring N similar sections produces, and a passing gate here means `/interview-feedback score` later hits a sheet it can't fill per-dimension. The gate is materially weaker than the contract it claims to prove — scope the per-dim checks to each extracted `<section class="dim">` block (the `extractSections` helper already exists) and assert dim-id uniqueness.

### interview-guide-F3 [Should-fix] The 1:1 area↔dim id MUST — a deterministic set-equality check — has no script gate; the interviewer reference has no validator at all
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:176
- Quote: "**Area ids MUST match the scoring-sheet `data-dim` ids 1:1** so the reference and sheet line up"
- Problem: This MUST is the other half of the interop loop (probes lifted per dimension by `/interview-feedback`), and it is pure set equality between `data-area` ids in output (a) and `data-dim` ids in output (b) — a textbook §H deterministic check ("deterministic = hard gate", per the repo's own policy). Yet it is enforced only by the judgment self-review (self-eval-rubric.md Axis 2), which is explicitly non-blocking ("the manager is the gate"). More broadly, output (a)'s required anchors (`data-ref="round"`, `data-signals`, `data-probes`) are never validated by any script, while output (b) gets a blocking gate. Asymmetric rigor on a symmetric contract: extend the validator with a two-file mode (`validate ... --against interviewer-reference.html`) that asserts id-set equality and the reference anchor set, and wire it into the Scoring Sheet or Self-Review phase as a hard gate.

### interview-guide-F4 [Nit] "`--no`-duration" reads as a nonexistent flag
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:184
- Quote: "When no duration was confirmed (deferred, or `--no`-duration), emit **neither** anchor"
- Problem: The backticked token suggests a `--no-duration` flag that doesn't exist anywhere in the skill (the parallel construct that does exist is `--no-case`). Intended meaning is "no `--duration` passed". A model executing the skill could infer a phantom flag; a reader is at minimum confused. Reword to "no `--duration`".

### interview-guide-F5 [Nit] Row/column terminology whiplash in the work-history weight-selection instructions
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:198
- Quote: "Every column is pre-summed to 100 — you read a row, you do not compute one"
- Problem: One sentence calls the same thing a column and a row; SKILL.md:38 says "weight row", :198 says "copy the matching column", and level-ladder.md:33 tries to reconcile with "Each **column** is one level's weight row". For an instruction whose entire point is "transcribe verbatim, don't think", making the reader disambiguate table orientation invites the exact transcription error the §H framing exists to prevent. Pick one term (the table's levels are columns; call them "the level's column") and use it everywhere.

### interview-guide-F6 [Nit] Frozen non-interactive block self-cites `_shared/non-interactive.md`, which does not exist in this plugin's `_shared/`
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:81
- Quote: "sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`)"
- Problem: `plugins/pmos-managerkit/skills/_shared/` contains only `interview-guidelines/` — no `non-interactive.md` — so "this file" is a dangling cite for anyone (or any standalone managerkit install) resolving it relative to the plugin; the referenced `tools/audit-recommended.sh` likewise lives in pmos-toolkit. The block is frozen byte-identical repo policy (lint passes, so no local fix is possible in this SKILL.md), but the plugin-level gap is the documented sync-shared bootstrap gap: place a byte-identical copy of `non-interactive.md` in pmos-managerkit's `_shared/` so the canonical text's self-reference resolves. Flagged at Nit because the fix is plugin-scoped, not skill-scoped.

### interview-guide-F7 [Nit] Cited substrate `reference-resolution.md` carries relative links that resolve to nothing from its actual location
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md:3
- Quote: "Cited from SKILL.md Phase [Resolve](../SKILL.md#resolve) and Phase [Coach](../SKILL.md#coach)"
- Problem: From `_shared/interview-guidelines/`, `../SKILL.md` is `_shared/SKILL.md` — which doesn't exist; the file means `/interview-feedback`'s SKILL.md. Same for `../SKILL.md#transcribe` (:25) and `../SKILL.md#score` (:15). interview-guide cites this file for the `--role-dir` write path (SKILL.md Phase Write), and a reader following the substrate's own links dead-ends. The phase-refs lint doesn't scan `_shared/` files, so nothing catches this. Fix the links to `../../interview-feedback/SKILL.md#...`.

**Pass 1 verdict:** 0 blockers / 3 should-fix / 4 nits — material findings

## Pass 1 — author response

### interview-guide-F1 — Accepted
- Fix: scripts/validate-scorecard-anchors.mjs · `parseDuration` (~:156-167) + its doc-comment (~:150-155) + `--check-duration` selftest fixtures. Before: unit-labeled tokens are consumed, then ALL leftover bare integers are summed — so ranges ('90-120 mins' → 210), decimals adjacent to a unit ('1.5 hours' → 301 via "5 hours"+leftover "1"), and prose counts ('45 mins each of 2 parts' → 47) all exit 0 silently. After: split the input on explicit `+` into addends; each addend must resolve to exactly ONE number (unit-labeled or bare) — two-plus bare integers in one addend (a range like 90-120), or a bare integer coexisting with unit tokens in the same addend without `+`, is a refusal (exit 1, existing "ask the operator" stderr message); a decimal digit-run adjacent to a unit token (`1.5 hours`) is a refusal (whole-minutes contract; re-prompt, never guess). Multiple unit-labeled tokens without `+` remain accepted ('1 hour 30 minutes' → 90, unambiguous). Keep accepted: '90'→90, '90 mins'→90, '2 hrs'→120, '90 + 15 buffer'→105. Add all of the above (accept + refuse) as `--selftest` cases and rewrite the "Handled shapes" doc-comment to state the refusal rule.
- Rationale: SKILL.md:160 and the script's own contract comment (:504) promise exit-non-zero on ambiguity ("re-prompt rather than guessing", INV-6/A1); the current parser guesses. A range is a highly plausible operator answer, and the wrong number flows into `data-duration`/`data-budget` machine anchors consumed by `/interview-feedback`. A §H gate that itself guesses is a contract violation in the gate.
- Blast radius: validate-scorecard-anchors.mjs only (parser + selftest + comment). SKILL.md:160 already states the intended contract — no prose change needed there. SKILL.md:192's selftest-coverage parenthetical gets "duration ambiguity" added to its fixture list (see F2/F3 — same sentence). No other skill calls `--check-duration`. Lints/evals untouched.

### interview-guide-F2 — Accepted
- Fix: scripts/validate-scorecard-anchors.mjs · core validate (:53-73) + `discoverDims` usage + selftest. Before: `data-scale`, `data-v`, `data-flags="green"`, `data-flags="red"` are asserted once file-wide, and duplicate `data-dim` ids are never rejected — a sheet with a second hollow duplicate section passes. After: (1) assert `data-dim` id uniqueness (duplicate → hard failure naming the id); (2) extract each `<section class="dim" data-dim="<id>">…</section>` block (new sibling of the existing `extractSections` helper, keyed on `data-dim` instead of `data-card`; sections are flat, same non-greedy match) and assert PER BLOCK: one `data-weight`, a `data-scale` container with `data-v` options, the `data-input="notes:<id>"` slot, and both `data-flags="green"`/`"red"` containers. Failure messages name the dim. Add selftest fixtures: duplicate-id sheet, hollow-second-section sheet (both must fail), plus the existing good fixtures re-passing.
- Rationale: reference/output-shapes.md:49 documents these as per-dimension requirements; the gate proves a strictly weaker property. Duplicate/hollow sections are exactly the copy-paste failure mode of a model authoring N similar blocks, and a passing gate then breaks `/interview-feedback score` downstream. §H says deterministic = hard gate — the gate should prove what the contract says.
- Blast radius: validate-scorecard-anchors.mjs (core + helper + selftest). The 8 bundled scorecards + skeletons in `_shared/interview-guidelines/` must re-pass the stricter gate (verify each; they are believed conformant — if one is not, that is a real latent defect to fix in the corpus, shared with /interview-feedback). SKILL.md:192 selftest parenthetical updated (with F1/F3). output-shapes.md unchanged (it already states the per-dim contract).

### interview-guide-F3 — Accepted
- Fix: scripts/validate-scorecard-anchors.mjs · new two-file mode `validate-scorecard-anchors.mjs <scoring-sheet.html> --against <interviewer-reference.html>`; SKILL.md Phase Scoring Sheet (§H hard gate block, :186-192) + Phase Self-Review; reference/output-shapes.md; self-eval-rubric.md Axis 2. Before: the SKILL.md:176 MUST ("Area ids MUST match the scoring-sheet `data-dim` ids 1:1") is enforced only by the non-blocking judgment self-review, and output (a)'s anchors (`data-ref="round"`, per-area `data-area`, `data-signals="green"/"red"`, `data-probes`) have no script gate at all. After: the `--against` mode asserts (i) set equality of reference `data-area` ids vs sheet `data-dim` ids (for work-history, scoped to the competency dims — the additive `role-evidence`/`trajectory-synthesis` families are sheet-only by design and excluded from the equality set, stated in the usage/error text); (ii) reference anchor presence: `data-ref="round"` on `<main>`, and per `data-area` block its `data-signals="green"`, `data-signals="red"`, `data-probes="<id>"`. Wire the two-file invocation into the existing §H hard-gate step of Phase Scoring Sheet (both outputs exist by then), blocking on non-zero exit exactly like the current single-file gate. Add selftest fixtures: matching pair (pass), id-set mismatch (fail), reference missing data-probes (fail). Update output-shapes.md to name the two-file gate next to the anchor list, and soften self-eval-rubric Axis 2's 1:1 check to "confirm the script gate ran" rather than re-derive it by judgment.
- Rationale: pure set equality is a textbook §H deterministic check ("deterministic = hard gate" per repo policy); leaving it to a non-blocking judgment pass while the sheet gets a blocking gate is asymmetric rigor on a symmetric interop contract that `/interview-feedback`'s probe-lifting depends on.
- Blast radius: validate-scorecard-anchors.mjs (new mode + selftest count jump — prove the jump, per the disjoint-merge lesson); SKILL.md (gate block + self-review phase + :192 parenthetical); output-shapes.md; self-eval-rubric.md. Bundled corpus pairs in `_shared/interview-guidelines/guidelines/<archetype>/` must pass the new mode (verify all 8; shared with /interview-feedback). tools/lint-phase-refs.sh re-run after SKILL.md edits. No flags added to argument-hint (script-internal invocation, not an operator flag).

### interview-guide-F4 — Accepted
- Fix: SKILL.md:184 · Phase Scoring Sheet, time-budget paragraph. Before: "(deferred, or `--no`-duration)". After: "(deferred, or no `--duration`)" — matching the phrasing already used at :178 ("deferred, or no `--duration`").
- Rationale: the backticked token reads as a phantom `--no-duration` flag (the real parallel is `--no-case`); a model executing the skill could infer a flag that doesn't exist. One-word transposition fix.
- Blast radius: none — prose only; the correct phrasing already exists at :178, so this is convergence, not a new formulation. Re-run tools/lint-flags-vs-hints.sh to confirm no body-defined-flag false positive disappears.

### interview-guide-F5 — Accepted
- Fix: standardize on "the level's **column**" for the per-level weight set, everywhere. SKILL.md:38 ("selects the competency **weight row**" → "weight column"; "reads the selected row's weights" → "selected column's weights"), :198 ("Every column is pre-summed to 100 — you read a row, you do not compute one" → "you copy a column, you do not compute one"), :247 ("selects the pre-summed weight row" → "pre-summed weight column"); level-ladder.md:3 ("per-competency weight row" → "weight column") and :33 ("Each **column** is one level's weight row" → "Each **column** is one level's weight set"). Rows remain competencies, columns remain levels — matching the actual table orientation.
- Rationale: an instruction whose entire point is "transcribe verbatim, don't think" (§H) should not force the reader to disambiguate table orientation mid-sentence; the current text calls the same object row and column in one breath.
- Blast radius: SKILL.md + `_shared/interview-guidelines/guidelines/work-history/level-ladder.md` (substrate shared with /interview-feedback — grep confirms /interview-feedback contains no "weight row" phrasing, so no cross-skill prose drift). The `_shared` file exists only in pmos-managerkit (not in any sync intersection), so no sync-shared ripple.

### interview-guide-F6 — Accepted (plugin-scoped)
- Fix: NOT a SKILL.md edit — the block is frozen byte-identical by repo policy (lint is source of truth). Fix per the documented sync-shared bootstrap procedure (CLAUDE.md "Bootstrap gap"): place a byte-identical copy of pmos-toolkit's `skills/_shared/non-interactive.md` at `plugins/pmos-managerkit/skills/_shared/non-interactive.md`, so the frozen block's self-cite ("Section D of this file (`_shared/non-interactive.md`)") resolves within the plugin. Once present it enters the sync-shared intersection and stays aligned via `scripts/sync-shared.sh --from=pmos-toolkit`.
- Rationale: managerkit's `_shared/` holds only `interview-guidelines/` — the cite is dangling for a standalone managerkit install, and every managerkit skill carrying the frozen block (interview-guide, interview-feedback, one-on-one) has the same gap; one copied file fixes all three. This is exactly the dangling-cite class the CLAUDE.md bootstrap-gap note and the `route:skill` [J] coherence gate exist for.
- Blast radius: one new file in pmos-managerkit `_shared/` (must be byte-identical to the pmos-toolkit canonical); future sync-shared runs now include it; no SKILL.md, lint, or eval changes. The `tools/audit-recommended.sh` cross-plugin reference remains toolkit-hosted by design (it already "scans all plugins").

### interview-guide-F7 — Accepted
- Fix: `plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md` · the four relative phase links. Before: `../SKILL.md#resolve`, `../SKILL.md#coach` (:3), `../SKILL.md#score` (:15), `../SKILL.md#transcribe` (:25) — which resolve to the nonexistent `_shared/SKILL.md`. After: `../../interview-feedback/SKILL.md#resolve` etc. (the file's phase names — Resolve, Coach, Score, Transcribe — all live in /interview-feedback's SKILL.md, confirming the intended target).
- Rationale: interview-guide cites this substrate for the `--role-dir` write path; a reader following its own links dead-ends. The phase-refs lint doesn't scan `_shared/`, so nothing else will ever catch it.
- Blast radius: one `_shared` substrate file, consumed by /interview-feedback and /interview-guide (link-only change, no semantic change). File exists only in pmos-managerkit — no sync-shared ripple. Optionally re-run tools/lint-phase-refs.sh on both consumer skills to confirm the anchors named (`#resolve`, `#coach`, `#score`, `#transcribe`) exist in interview-feedback/SKILL.md.

**Author verdict:** 7/7 accepted (3 should-fix, 4 nits), 0 rejected, 0 invalid. F1/F2/F3 converge on one file (the validator) and one shared SKILL.md sentence (:192's selftest inventory) — implement together, and re-verify the 8 bundled corpus scorecard/reference pairs against the strengthened gates.

## Pass 2 — reviewer findings
### interview-guide-F8 [Blocker] F3's two-file set-equality gate hard-fails the bundled work-history archetype — the proposed carve-out excludes the wrong thing
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__interview-guide.md:41 (F3 "After" spec); plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/interviewer-reference.html:87
- Quote: "excluding the sheet-only additive `role-evidence`/`trajectory-synthesis` families (stated in usage/error text)"
- Problem: I simulated F3's set-equality check over all 8 bundled corpus pairs. Seven pass; **work-history fails with zero overlap**: the sheet's `data-dim` set is the 12 Reforge competencies while the reference's `data-area` set is a six-item METHOD-PHASE taxonomy (`career-arc-scan`, `per-role-deep-dive`, `contribution-attribution`, `results-and-measurement`, `level-and-trajectory`, `management-and-references`). The proposal's carve-out is a no-op — `role-evidence`/`trajectory-synthesis` blocks carry `data-card`, not `data-dim`, so excluding them removes nothing from either id set. And this is not a latent corpus defect: the corpus documents it as deliberate at interviewer-reference.html:87–88 — "Competency scoring maps to scorecard.html's data-dim sections, not 1:1 to these areas." Implemented exactly as described, the new BLOCKING §H step fails every work-history run, and the implementation note ("a corpus failure is a real defect, not gate noise") actively steers the implementer toward 'fixing' a deliberate design. F3 needs either a full work-history exemption from set-equality (keep only the anchor-presence half) or an explicit area→dim mapping contract for that archetype — decided before implementation, not discovered at gate time.

### interview-guide-F9 [Should-fix] The unqualified "1:1 MUST" prose is already false for work-history — in SKILL.md, output-shapes.md, and the self-eval rubric — independent of F3
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:176 (also reference/output-shapes.md:23-24, reference/self-eval-rubric.md:23-24)
- Quote: "**Area ids MUST match the scoring-sheet `data-dim` ids 1:1** so the reference and sheet line up"
- Problem: the bundled work-history pair — shipped by this plugin as canonical corpus — violates this MUST by design (see F8's corpus comment). So even without F3, a work-history run following SKILL.md:176 verbatim would either restructure the reference into 12 competency areas (diverging from the bundled model it's told to tailor) or fail its own self-review: self-eval-rubric Axis 2's first checkbox ("reference `data-area` ids and sheet `data-dim` ids are 1:1 (no area without a dimension; no dimension without an area)") can never be checked for work-history, permanently depressing the alignment score for a conformant artifact. The 1:1 contract needs an explicit work-history qualification in all three homes (one canonical statement, cited from the others — §K), stating what interop `/interview-feedback` actually relies on there (probe lifting by method phase vs scoring by competency dim). Pass 1's F3 asserted the 1:1 MUST is a real interop invariant; the corpus proves it is conditional.

### interview-guide-F10 [Should-fix] Validator never asserts `data-input="notes:reco"` though output-shapes lists it among the anchors the script validates
- Where: plugins/pmos-managerkit/skills/interview-guide/reference/output-shapes.md:53 (script gap at scripts/validate-scorecard-anchors.mjs:76-83)
- Quote: "`data-reco=\"strong-yes|yes|no|strong-no\"`, plus a `data-input=\"notes:reco\"` slot."
- Problem: output-shapes.md:45 heads this list "Machine anchors (required — validated by `scripts/validate-scorecard-anchors.mjs`)", and `/interview-feedback`'s fill-scorecard.mjs writes into this slot (fill-scorecard.mjs:244 `injectInputSlot(out, 'notes:reco', values.recoNotes)`). But `validate()` checks only `data-input="reco"` and the four `data-reco` options — a sheet missing the reco notes slot passes the "hard gate" and later breaks the fill on the consuming side. The BROKEN_FIXTURE even omits the slot and the selftest doesn't expect a failure for it. Not covered by the proposal: F2 scopes per-DIM anchors; this is the overall-reco family. One-line check + a selftest expectation closes it.

### interview-guide-F11 [Nit] F5's row→column sweep misses five residual "row" phrasings, including in the sentences it edits
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/level-ladder.md:4 (also :12-13, :19, :101-102; SKILL.md:198)
- Quote: "`--seniority` selects a row; the model **reads** the row's weights verbatim"
- Problem: F5 enumerates exact before→after pairs (SKILL.md :38/:198/:247; level-ladder.md :3/:33). Implemented exactly as listed, these survive: level-ladder.md:4-5 ("selects a row … reads the row's weights"), :12-13 ("The row totals always equal 100"), :19 ("the shipped default row"), :101-102 ("falls back to the matching row of *this* default ladder"), and SKILL.md:198's trailing sentence "The checked-in `scorecard.html` ships the `senior-pm` row as its default." — the very sentence F5 partially edits. The terminology whiplash F5 exists to remove persists in its own fix surface; the sweep should be a grep for `\brow\b` across both files, not a line list.

### interview-guide-F12 [Nit] F1's refusal rule is self-contradictory as written — "exactly ONE number" vs the multi-unit carve-out — and could be implemented to wrongly refuse "1 hour 30 minutes"
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__interview-guide.md:27
- Quote: "split input on explicit `+` into addends; each addend must resolve to exactly ONE number."
- Problem: two sentences later the same paragraph accepts "Multiple unit-labeled tokens without `+`" (`1 hour 30 minutes` → 90) — an addend resolving to TWO numbers. An implementer following the headline rule refuses the carve-out case (a shape the SKILL.md:148 prompt explicitly invites: "e.g. '90 mins', '1 hour', …"); an implementer following the carve-out must guess where the boundary is (is "1 hour 30" one leftover-bare violation or a third carve-out?). State the rule once, precisely — e.g. "per addend: any number of unit-labeled tokens, at most ZERO bare integers when a unit token is present, at most ONE bare integer otherwise; any decimal refuses" — and make the selftest fixtures the normative enumeration.

**Pass 2 verdict:** 1 blockers / 2 should-fix / 2 nits — material findings

## Pass 2 — author response

### interview-guide-F8 — Accepted
- Fix: amends F3's two-file mode spec. The `--against` mode reads `data-archetype` from the reference's `<main data-ref="round">` (present in the corpus: work-history/interviewer-reference.html:51 `data-archetype="work-history"`). For `data-archetype="work-history"`, SKIP the area↔dim set-equality assertion entirely — the reference's areas are the six method phases by documented design (interviewer-reference.html:87-88 "Competency scoring maps to scorecard.html's data-dim sections, not 1:1 to these areas") — and run only the anchor-presence half (data-ref="round", per-area data-signals green/red, data-probes). Print an explicit note line on pass ("work-history: area↔dim id equality not applicable — method-phase areas by design"). For every other archetype, assert full set equality. DROP the pass-1 role-evidence/trajectory-synthesis carve-out language entirely — the reviewer is right that it was a no-op (those blocks carry `data-card`, not `data-dim`; verified via grep on scorecard.html). Selftest fixtures: a work-history pair with non-matching id sets (must PASS), a work-history reference missing data-probes (must FAIL), plus the non-work-history mismatch fixture (must FAIL). Replace the pass-1 implementation note ("a corpus failure is a real defect") with: "the work-history corpus pair intentionally has non-matching id sets — the strengthened gate MUST pass it unmodified; do not 'fix' the corpus."
- Rationale: the reviewer simulated the gate over all 8 corpus pairs and work-history fails with zero overlap; the corpus documents the divergence as deliberate. A blocking §H gate that fails every run of one bundled archetype — with an implementation note steering toward corpus 'repair' — is worse than no gate. Archetype detection is itself deterministic (the anchor is in the file), so the exemption stays inside the §H frame.
- Blast radius: same files as F3 (validator + selftest, SKILL.md gate block, output-shapes.md, self-eval-rubric.md) — no new surfaces; the fixture set grows by the work-history pair cases. Interlocks with F9 (the prose MUST must be qualified to match the gate's exemption, one canonical home).

### interview-guide-F9 — Accepted
- Fix: qualify the 1:1 contract in one canonical home, cite from the others (§K). Canonical statement goes in reference/output-shapes.md (the anchor-contract doc), replacing the two unqualified lines (:23-24 "**`<id>` MUST match the sheet's `data-dim` id 1:1**" and :47-48 "ids match the reference `data-area` ids"): "Area ids MUST match the sheet's `data-dim` ids 1:1 for every archetype EXCEPT `work-history`, whose reference areas are the six method phases (probe lifting is per phase; competency scoring maps to the sheet's `data-dim` sections — see the corpus comment in guidelines/work-history/interviewer-reference.html). The two-file validator mode enforces exactly this." SKILL.md:176's bolded MUST gains a short parenthetical qualifier citing output-shapes.md ("(all archetypes except work-history — see reference/output-shapes.md)"). self-eval-rubric.md Axis 2: the first checkbox (1:1) and second checkbox (scored-not-probed / probed-not-scored) get the same qualification — for work-history the check becomes "every sheet dim's evidence is probed by at least one method-phase area (coverage, not 1:1)" — folded into F3's already-planned Axis 2 rewrite ("confirm the script gate ran" for the deterministic half; the coverage judgment stays as judgment).
- Rationale: the plugin's own canonical corpus violates the unqualified MUST by design, so a conformant work-history run can never check the rubric box and a literal reading of SKILL.md:176 steers the model to restructure the reference against the bundled archetype it is told to tailor. The contract is conditional; the prose must say so in all three homes, canonically once.
- Blast radius: SKILL.md (:176 one parenthetical), output-shapes.md (two lines), self-eval-rubric.md (Axis 2, shared edit with F3). No script change beyond F8's. tools/lint-phase-refs.sh re-run after SKILL.md edit. No cross-plugin surface.

### interview-guide-F10 — Accepted
- Fix: scripts/validate-scorecard-anchors.mjs · validate() section 4 (the reco block, :75-83) + selftest. Add one check: assert `data-input="notes:reco"` is present, failure message "missing data-input=\"notes:reco\" (the overall-reco notes slot)". BROKEN_FIXTURE already omits the slot (only the good fixtures at :263/:307/:328/:348 carry it) — add the new failure string to the broken-fixture expected-failures list so the selftest proves the check fires. Selftest count bump proven (count-claim lesson).
- Rationale: output-shapes.md:45 heads the list "required — validated by `scripts/validate-scorecard-anchors.mjs`" and :53 names the slot; /interview-feedback's fill-scorecard.mjs writes into it (injectInputSlot(out, 'notes:reco', …)). A sheet missing it passes the hard gate today and breaks the fill downstream — same gate-weaker-than-contract class as F2, different anchor family.
- Blast radius: validator + selftest only. output-shapes.md unchanged (already states the contract). Folds into the F1/F2/F3 combined validator edit and the SKILL.md:192 selftest-inventory sentence.

### interview-guide-F11 — Accepted
- Fix: amends F5 from a line-list to a grep-driven sweep. Rule: run `grep -n '\brow\b' ` over SKILL.md and level-ladder.md; every hit denoting a PER-LEVEL WEIGHT SET becomes "column"; hits genuinely meaning a competency row (the table's actual rows) keep "row". Known residuals to convert beyond F5's list: level-ladder.md:4-5 ("selects a row; the model **reads** the row's weights" → "selects a column; … the column's weights"), :6 ("every row here is pre-summed to 100" → "every column"), :12-13 ("The row totals always equal 100" → "column totals"), :19 ("the shipped default row" → "default column"), :101-102 ("falls back to the matching row of *this* default ladder" → "matching column"); SKILL.md:198 trailing sentence ("ships the `senior-pm` row as its default" → "`senior-pm` column"), :206 ("bundled `<level>` ladder row" → "ladder column"). Acceptance check: post-edit `grep -n '\brow\b'` over both files and confirm every remaining hit means a competency row (or delete the term).
- Rationale: the reviewer is right that fixing five sentences while five more say "row" — including the tail of a sentence F5 itself edits — preserves the exact whiplash the fix exists to remove. A terminology fix must be exhaustive or it is noise.
- Blast radius: same two files as F5 (SKILL.md + the managerkit-only level-ladder.md substrate); still no /interview-feedback or sync-shared ripple (re-grep /interview-feedback for "row" as confirmation).

### interview-guide-F12 — Accepted
- Fix: amends F1's rule statement (proposal + the validator doc-comment it specifies). Replace the contradictory pair ("each addend must resolve to exactly ONE number" vs the multi-unit carve-out) with one precise rule: "Split on explicit `+` into addends. Per addend: any count of UNIT-LABELED numbers is allowed and summed (`1 hour 30 minutes` → 90); a BARE (unitless) integer is allowed only when it is the addend's SOLE number (`90` → 90); a bare integer coexisting with any other number in the same addend refuses (`90-120 mins`: bare 90 + labeled 120 → exit 1; `45 mins each of 2 parts`: labeled 45 + bare 2 → exit 1; `1 hour 30`: labeled 1h + bare 30 → exit 1, re-prompt); any decimal anywhere refuses (`1.5 hours` → exit 1)." The `--selftest` fixtures are the normative enumeration — every accept and refuse case above ships as a fixture, and the doc-comment cites the fixtures as the spec of record.
- Rationale: verified against the reviewer's cases — the restated rule accepts everything SKILL.md:148's prompt invites and refuses every ambiguous shape from F1, with no carve-out needed. An ambiguity-refusing parser specified ambiguously would just relocate the guessing from runtime to implementation time.
- Blast radius: none beyond F1's (validator parser + doc-comment + selftest); the proposal text itself is corrected in this pass's rewrite.

**Author verdict (pass 2):** 5/5 accepted (1 blocker, 2 should-fix, 2 nits), 0 rejected, 0 invalid. F8/F9 interlock: the gate exemption and the prose qualification must land together or the contract and the gate diverge again. F10/F12 fold into the combined validator edit from pass 1.

## Pass 3 — reviewer findings

Reviewer verification notes: simulated the proposal's fixes as implemented. Independently re-proved the two "believed conformant" claims: (1) F2's stricter per-dim gate (per-block weight/scale/data-v/notes:<id>/both flags + dim-id uniqueness) passes all 8 bundled scorecards with zero misses; (2) F3's reference-side extraction assumptions hold — all 8 bundled `interviewer-reference.html` files have flat (non-nested) `data-area` sections, each carrying `data-signals="green"/"red"` and `data-probes`, and every reference `<main>` carries exactly one `data-archetype`. F7's four target anchors (`{#resolve}`, `{#transcribe}`, `{#score}`, `{#coach}`) all exist in `interview-feedback/SKILL.md`. F1/F12's rule preserves every phrasing the SKILL.md prompt itself suggests ("90 mins", "1 hour", "90 + 15 buffer"). The new `--against` script flag appears only in fenced code, which `lint-flags-vs-hints.sh` explicitly skips for BODY-ONLY detection — no lint regression. Remaining gaps below.

### interview-guide-F13 [Should-fix] Two-file gate keys the exemption on the reference's archetype but never asserts sheet↔reference archetype agreement
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__interview-guide.md:48
- Quote: "the mode reads `data-archetype` from the reference `<main data-ref=\"round\">` (present in the corpus at `guidelines/work-history/interviewer-reference.html:51`)"
- Problem: The single-file gate keys work-history family checks on the SHEET's `data-archetype` (`validate-scorecard-anchors.mjs:105` — `if (/data-archetype\s*=\s*"work-history"/.test(html))`), while the proposed two-file mode keys the set-equality exemption on the REFERENCE's `data-archetype`. Nothing in the proposal asserts the two attributes agree. A mistagged pair — e.g. a sheet emitted with `data-archetype="product-sense"` beside a reference tagged `work-history` (or vice-versa) — gets the equality gate silently voided by the exemption on one side and the WH family checks silently skipped on the other, so a completely misaligned pair passes both hard gates. The whole point of F3/F8 was that the exemption stays "inside the §H frame"; a §H exemption keyed on a single unverified attribute in one of two files is spoofable by exactly the model error class the gate exists to catch. One-line fix: the two-file mode first asserts sheet `data-archetype` === reference `data-archetype` (hard failure naming both), THEN branches on it. Add a mismatched-archetype fixture to the selftest.

### interview-guide-F14 [Nit] Dangling cross-plugin cite: `../feature-sdlc/reference/skill-patterns.md` resolves to nothing in this plugin
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:20
- Quote: "This skill follows the SKILLS-standard authoring guide at `../feature-sdlc/reference/skill-patterns.md` (pmos-toolkit)"
- Problem: Relative to this skill, `../feature-sdlc/` is `plugins/pmos-managerkit/skills/feature-sdlc/`, which does not exist (verified — no such dir). Same dangling-cite disease class the proposal fixes in F6 (non-interactive.md) and F7 (reference-resolution.md links), but this instance is uncovered. The sibling skill proves the correct form in-repo: `one-on-one/SKILL.md:23` cites `../../../pmos-toolkit/skills/feature-sdlc/reference/skill-patterns.md`. `interview-feedback/SKILL.md:19` shares the same defect — plugin-scoped fix, like F6.

### interview-guide-F15 [Nit] F9 qualifies the 1:1 contract in three homes but misses the fourth: the reference-skeleton's own doc-comment
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-skeleton.html:12
- Quote: "one per evaluation area (kebab id; SHOULD map 1:1 to a"
- Problem: F9's §K consolidation covers `output-shapes.md`, `SKILL.md:176`, and `self-eval-rubric.md` — but the skeleton the model actually instantiates for custom rounds states the mapping too, and its closing line ("Pairs with scorecard-skeleton.html (same dimension ids)", :18-19) is an unqualified same-ids claim. After F9 lands, the skeleton remains a fourth, unqualified statement of a contract the plugin's own work-history corpus violates by design — the exact divergence-by-duplication F9 exists to end. Cheap fix: one parenthetical in the skeleton doc-comment citing output-shapes.md's canonical qualified statement.

### interview-guide-F16 [Nit] Case reference-solution's `data-maps-dim` ids are never validated against the sheet's dims — judgment-only on a deterministic id check
- Where: plugins/pmos-managerkit/skills/interview-guide/reference/output-shapes.md:86
- Quote: "a `data-maps-dim=\"<id>\"` note per section tying the case back to the scoring-sheet"
- Problem: The F3 principle — symmetric interop id contracts get script gates, not just self-review checkboxes — stops at outputs (a)+(b). Output (c)'s `data-maps-dim` ids referencing the sheet's `data-dim` set is the same deterministic id-subset check, yet it is asserted only by self-eval Axis 3 judgment (`self-eval-rubric.md:37-38`); a reference-solution mapping to a nonexistent or renamed dim passes everything. Mitigating: grep shows no `/interview-feedback` machine consumer of `data-maps-dim`, so stakes are prose-level — hence Nit, not Should-fix. A natural fold-in once the two-file mode exists (a third optional `--case <path>` compare), or an explicit "deliberately ungated — no machine consumer" note in output-shapes.md so the asymmetry reads as chosen, not missed.

### interview-guide-F17 [Nit] "Both outputs exist by then" — the two-file gate's input paths are unstated at the point it runs
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__interview-guide.md:49
- Quote: "Wire the two-file invocation into the existing blocking §H step of Phase Scoring Sheet (both outputs exist by then)"
- Problem: Vague enough to implement wrong. SKILL.md resolves the output location only in Phase Write ("Resolve the output location and write the artifacts", SKILL.md:227-229); the Scoring Sheet §H step already runs the validator on `<scoring-sheet.html>` mid-pipeline without saying where that file lives, and the two-file mode now also needs the interviewer-reference draft on disk at a known path. Authored-in-conversation drafts "exist" only as text; an implementer could reasonably conclude the gate must move to Phase Write — changing the blocking semantics F3 specifies. The SKILL.md wiring should state where both drafts are written before the gate runs (e.g. drafts land at the resolved output paths before the §H step, with Phase Write reduced to announce + role-dir placement).

**Pass 3 verdict:** 0 blockers / 1 should-fix / 4 nits — material findings

## Pass 3 — author response

### interview-guide-F13 — Accepted
- Fix: amends F3/F8's two-file mode spec (validator + selftest). Before: the exemption branch keys on the REFERENCE's `data-archetype` while the existing single-file family checks key on the SHEET's (`validate-scorecard-anchors.mjs:105`), with no agreement assertion — a mistagged pair silently voids the equality gate on one side and skips the WH family checks on the other. After: the `--against` mode's FIRST assertion is sheet `data-archetype` === reference `data-archetype` (both absent counts as agreement for non-work-history pairs; present-vs-absent or differing values is a hard failure naming both values and both files), and only THEN branches on the agreed archetype for the set-equality exemption. Add a mismatched-archetype fixture pair to the selftest (must FAIL) alongside the existing pass/fail fixtures from F8.
- Rationale: a §H exemption keyed on a single unverified attribute in one of two files is spoofable by exactly the model error class the gate exists to catch (a wrongly tagged emit). The agreement check is itself deterministic and one line — it keeps the exemption inside the §H frame, which was F8's whole justification.
- Blast radius: validator two-file mode + one selftest fixture pair; the 8 bundled corpus pairs must still pass (they will — spot-check confirms sheet and reference archetype tags agree in the corpus; work-history carries `data-archetype="work-history"` on both sides). No SKILL.md/doc change beyond what F3/F8 already touch.

### interview-guide-F14 — Accepted (plugin-scoped, same class as F6)
- Fix: SKILL.md:20 · the authoring-guide cite. Before: "`../feature-sdlc/reference/skill-patterns.md` (pmos-toolkit)" — resolves to the nonexistent `plugins/pmos-managerkit/skills/feature-sdlc/`. After: "`../../../pmos-toolkit/skills/feature-sdlc/reference/skill-patterns.md`" — the exact form already proven in-repo by the sibling `one-on-one/SKILL.md:23`. The parenthetical "(pmos-toolkit)" becomes redundant and is dropped (the path names the plugin). Apply the identical one-line fix to `interview-feedback/SKILL.md:19`, which shares the defect verbatim — one edit class, two files, flagged here because this unit surfaced it.
- Rationale: same dangling-cite disease as F6/F7 — a relative path that resolves to nothing from where it is written; the correct cross-plugin form exists three directories over, so this is convergence, not invention. Unlike F6, this text is NOT inside the frozen non-interactive block, so a direct SKILL.md edit is permitted.
- Blast radius: two SKILL.md files (interview-guide, interview-feedback — the latter is technically another unit's surface; coordinate or let that unit's proposal absorb it). Prose-only; re-run tools/lint-phase-refs.sh + lint-flags-vs-hints.sh on both skills as confirmation (neither lint checks file-existence of cites, so no lint delta expected). Note: unlike F6, copying skill-patterns.md into managerkit `_shared/` is NOT the fix — the cite is informational authorship provenance, not a runtime dependency, and the toolkit copy is the canonical single source (§K).

### interview-guide-F15 — Accepted
- Fix: `_shared/interview-guidelines/reference-skeleton.html` · doc-comment (:12 and the closing pairing line :18-19). Before: ":12 "one per evaluation area (kebab id; SHOULD map 1:1 to a scorecard data-dim…)" plus the unqualified ":18-19 "Pairs with scorecard-skeleton.html (same dimension ids)". After: extend the :12 parenthetical with the qualifier and the canonical cite — "(kebab id; maps 1:1 to a scorecard data-dim for every archetype except work-history, whose areas are method phases — see interview-guide/reference/output-shapes.md)" — and soften :18-19 to "Pairs with scorecard-skeleton.html (same dimension ids for 1:1 archetypes)". Two-line doc-comment edit, folded into F9's landing.
- Rationale: F9's §K consolidation is defeated if the skeleton the model actually instantiates for custom rounds remains a fourth unqualified statement of the conditional contract — divergence-by-duplication is exactly what F9 exists to end. The skeleton keeps only a short qualifier + cite, not the full statement (canonical home stays output-shapes.md).
- Blast radius: one `_shared` substrate file (managerkit-only, no sync-shared ripple; also read by /interview-feedback but the edit is doc-comment prose, no anchor/markup change). Lands together with F8/F9 per the existing interlock note.

### interview-guide-F16 — Accepted
- Fix: reference/output-shapes.md · the output (c) anchor bullet (:86) + self-eval-rubric.md:37-38 stays as-is. Before: `data-maps-dim="<id>"` is asserted only by Axis 3 judgment; nothing states whether the missing script gate is chosen or missed. After: add one sentence to output-shapes.md beside the `data-maps-dim` bullet: "Deliberately ungated — no machine consumer reads `data-maps-dim` (it is interviewer-facing prose linkage); the self-review checks it by judgment. If a machine consumer appears, fold a `--case <path>` compare into the two-file validator mode." The optional third compare mode is recorded as a future fold-in, NOT implemented now.
- Rationale: the reviewer's own mitigation is right — grep shows no `/interview-feedback` consumer, so a blocking gate here would be gold-plating (§H hard gates are for contracts something depends on). But the asymmetry must read as chosen, not missed, or the next reviewer re-files this finding; one sentence buys that.
- Blast radius: output-shapes.md only (one sentence). No script, SKILL.md, or rubric change. No selftest delta.

### interview-guide-F17 — Accepted
- Fix: SKILL.md · Phase Scoring Sheet §H gate block (:186-192) + Phase Write (:224-229), amending F3/F8's wiring sentence. Before: the gate says "After writing the sheet, run …" but output-location resolution happens only in Phase Write, and the two-file mode now also needs the reference draft on disk — an implementer could conclude the gate must move to Phase Write, changing F3's blocking semantics. After: (1) move the output-location resolution (default path / `--out` / `--role-dir` per reference-resolution.md) to the top of Phase Interviewer Reference, stated once: "Resolve the output location NOW (see Phase Write for the rules); write each artifact to its resolved path as it is authored." (2) The Scoring Sheet §H block then names both concrete inputs: `node scripts/validate-scorecard-anchors.mjs <resolved>/scoring-sheet.html --against <resolved>/interviewer-reference.html` — both files exist on disk because each phase wrote its draft on completion. (3) Phase Write reduces to: confirm/announce the written paths, apply the role-dir gitignore guard, report self-review scores — no writing moves there. §K: the resolution RULES stay canonically in Phase Write; the reference phase cites them.
- Rationale: "both outputs exist by then" was an assertion about conversation state, not disk state — and the gate is a script that reads files. Stating the write-before-gate ordering and the exact paths removes the only reasonable wrong implementation (relocating the blocking gate to Phase Write).
- Blast radius: SKILL.md two phases (folds into F3/F8's already-planned gate-block edit); tools/lint-phase-refs.sh re-run (the new "see Phase Write" cite must use the `{#write}` slug per §J). No script change beyond F3/F8/F13's. No doc-file change.

**Author verdict (pass 3):** 5/5 accepted (1 should-fix, 4 nits), 0 rejected, 0 invalid. F13 folds into the two-file mode (with F3/F8); F15 folds into F9's landing; F17 folds into F3/F8's SKILL.md wiring. F14 is a two-file one-liner shared with the interview-feedback unit. Cumulative: 17/17 accepted across 3 passes, 0 rejected.

## Author closeout — final pass (review loop ended)

Audit of the ledger before closing:
- **Latest reviewer-findings section:** Pass 3 (F13–F17). It HAS a matching author-response section ("Pass 3 — author response", 5/5 accepted) — no orphaned findings section exists.
- **Earlier findings unaddressed in the proposal:** none. All 17 findings (F1–F17) appear in the cumulative proposal's ledger with full accepted-change detail; every pass-2/pass-3 amendment (F8→F3, F11→F5, F12→F1, F13/F17→F3, F15→F9) is folded into the corresponding combined section.
- **Grounding spot-check (closeout re-verification of the latest pass):** F14's quote matches `interview-guide/SKILL.md:20` and the identical defect at `interview-feedback/SKILL.md:19`, with the correct cross-plugin form confirmed at `one-on-one/SKILL.md:23`; F15's "SHOULD map 1:1 to a" quote matches `reference-skeleton.html:12`; F16's `data-maps-dim` quote matches `output-shapes.md:86`; F17's "both outputs exist by then" quote matches the proposal text it critiqued. All grounded — nothing to reclassify as invalid.

**Dispositions this closeout:** none new — 0 accepted, 0 rejected in this pass (all 17 were dispositioned in passes 1–3; nothing outstanding).

**Convergence determination:** Pass 3 introduced 0 blockers, 1 should-fix (F13), 4 nits — every one accepted with a concrete amended spec, and no reviewer pass followed with objections to those responses. No unresolved disagreements remain; no finding was rejected, so there is no contested ground. The trajectory (pass 1: 3 SF / pass 2: 1 B — against the pass-1 spec, not the shipped skill / pass 3: 1 SF + 4 nits, all spec-tightening amendments) shows the material surface exhausted.

**Final status: CONVERGED.** Cumulative: 17/17 accepted (1 blocker, 6 should-fix, 10 nits), 0 rejected, 0 invalid, 0 open questions. The cumulative proposal is rewritten as the final self-contained record; implementation notes there are the execution contract.
