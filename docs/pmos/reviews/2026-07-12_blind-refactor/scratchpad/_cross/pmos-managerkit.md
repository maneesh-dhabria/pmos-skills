## Pass 1 — reviewer findings

### pmos-managerkit-cross-F1 [Should-fix] Trigger collision: interview-feedback `setup` and interview-guide both claim "set up the interview loop"
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:3
- Quote: "or setting up a role's interview process and round guidelines. Triggers: \"score this candidate\", \"fill the interview scorecard\", \"write up interview feedback\", \"how did the interviewer do\", \"set up the interview loop for <role>\""
- Problem: interview-guide/SKILL.md:3 claims the near-identical phrase — "Use when setting up a role's interview loop, writing an interviewer guide or brief" with trigger "set up the <role> interview loop". A user saying "set up the interview loop for Senior PM" matches both descriptions with no disambiguation rule in either skill; the router will pick nondeterministically between two skills that scaffold overlapping artifacts. Neither description carries a NOT-for line ("/shape"'s description shows the pattern: "NOT for interrogating a committed plan (/grill)…"). The two skills need an explicit boundary: guide = author one round's kit; feedback setup = scaffold the role storage + role.json, delegating per-round authoring.

### pmos-managerkit-cross-F2 [Should-fix] interview-feedback `setup` re-authors the round kit ungated, duplicating interview-guide's job without its §H validator
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:224
- Quote: "attach or generate them by instantiating `../_shared/interview-guidelines/reference-skeleton.html` + `../_shared/interview-guidelines/scorecard-skeleton.html`"
- Problem: This is exactly interview-guide's charter (its SKILL.md:13 says the sheet carries "the full `scorecard-skeleton` **machine anchors** so `/interview-feedback score` consumes it verbatim after the round (the interop loop, design D8)"). But scorecards generated via interview-feedback `setup` never pass `interview-guide/scripts/validate-scorecard-anchors.mjs` — the weights-sum-to-100 / anchor-set / data-budget≤data-duration hard gate exists only on the guide path. So the plugin ships two authoring paths for the same artifact, one deterministically gated and one not; a setup-generated sheet with weights summing to 97 sails through and only bites at score time. Setup should either invoke /interview-guide per round or run the same validator; interview-feedback SKILL.md never mentions either.

### pmos-managerkit-cross-F3 [Should-fix] Dangling skill-patterns.md cite in two of three skills (path resolves inside managerkit, where no feature-sdlc exists)
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:19
- Quote: "This skill follows the SKILLS-standard authoring guide at `../feature-sdlc/reference/skill-patterns.md` (pmos-toolkit)"
- Problem: From `skills/interview-feedback/`, `../feature-sdlc/` resolves to `plugins/pmos-managerkit/skills/feature-sdlc/` — verified nonexistent (`ls` fails). interview-guide/SKILL.md:20 carries the same broken path. one-on-one/SKILL.md:23 has the correct cross-plugin form (`../../../pmos-toolkit/skills/feature-sdlc/reference/skill-patterns.md` — verified to resolve). Same fact, three homes, two of them dangling — an agent following the cite in feedback/guide reads a 404 and silently proceeds without the authoring standard. Normalize all three to the one resolving form.

### pmos-managerkit-cross-F4 [Should-fix] Plugin manifest + README describe a one-skill plugin; two of the three shipped skills are invisible
- Where: plugins/pmos-managerkit/.claude-plugin/plugin.json:4
- Quote: "Ships /interview-feedback, which turns a candidate's interview inputs into a grounded scorecard plus per-interviewer coaching notes."
- Problem: The plugin is at v0.7.0 and ships three skills (interview-feedback, interview-guide, one-on-one), but the marketplace-facing description names only one. README.md:6 has the same fossil: "The first skill is `/interview-feedback`; more manager skills (team reviews, 1:1 prep, calibration) will land here over time" — 1:1 prep HAS landed (/one-on-one) and README's "## Skills" section documents only interview-feedback (grep for interview-guide/one-on-one in README: 0 matches). This is the exact enumerated-set drift class the repo's own [J]-gate lesson records ("7 bundled" vs 8) — a would-be installer evaluating the plugin from its catalog entry sees a third of its value. The repo-root CLAUDE.md charter row has the same single-skill parenthetical, compounding it.

### pmos-managerkit-cross-F5 [Should-fix] The 8-archetype enumeration lives in two SKILL.md bodies with no canonical home (§K)
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:252
- Quote: "`archetype ∈` {`recruiter-screen`, `product-sense`, `analytical`, `technical`, `behavioral`, `case-study`, `case-presentation`, `work-history`} (the 8 bundled PM round types)"
- Problem: interview-guide/SKILL.md:242 restates the full list plus the count ("recruiter-screen, product-sense, analytical, technical, behavioral, work-history, case-study, case-presentation"). The set's real ground truth is the directory listing under `skills/_shared/interview-guidelines/guidelines/` (8 dirs today), yet neither skill cites that home — both hand-copy the list AND a literal count. This plugin has already been bitten by exactly this ("7 bundled" vs 8, first-ever red [J] gate, 2026-07-09): the next archetype added must be hand-synced into two prose lists and two counts or one skill silently refuses/accepts the wrong archetype set. One home (a manifest or README in `_shared/interview-guidelines/`) cited from both skills would make the ninth archetype a one-file change.

### pmos-managerkit-cross-F6 [Nit] _shared/reference-resolution.md back-links to `../SKILL.md`, which resolves to a nonexistent file
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md:3
- Quote: "Cited from SKILL.md Phase [Resolve](../SKILL.md#resolve) and Phase [Coach](../SKILL.md#coach)."
- Problem: From `_shared/interview-guidelines/`, `../SKILL.md` is `skills/_shared/SKILL.md` — verified nonexistent. The links fossilize the doc's pre-_shared location (it was moved to _shared in epic 260702-cqf per interview-guide:166 but its own back-links weren't repointed); every relative SKILL.md link in the file (lines 3, 15, 25 also link `../SKILL.md#score` / `#transcribe`) is dead. Should be `../../interview-feedback/SKILL.md#…`. `lint-phase-refs.sh` validates SKILL.md-side anchors, not reference-doc back-links — a rubric blind spot.

### pmos-managerkit-cross-F7 [Nit] The frozen non-interactive block bakes pmos-toolkit-isms into every managerkit skill: a plugin-locally dangling `_shared/non-interactive.md` cite and a wrong-plugin stderr prefix
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:78
- Quote: "sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`)"
- Problem: `plugins/pmos-managerkit/skills/_shared/non-interactive.md` does not exist (verified) — the cite only resolves in pmos-toolkit, the block's canonical owner. Step 8 of the same block (line 84) hardcodes "pmos-toolkit: /<skill> finished" — so a compliant /one-on-one run announces itself as pmos-toolkit on stderr. The three managerkit copies are byte-identical to each other (verified by diff), so this is not drift — it is a design defect in the frozen substrate itself: the byte-identical contract prevents any consumer plugin from correcting either the cite or the prefix. Fix belongs in the canonical block (plugin-neutral wording), then a repo-wide re-paste; flagging here because the rubric's byte-identity check actively enforces the defect.

**Pass 1 verdict:** 0 blockers / 5 should-fix / 2 nits — material findings

## Pass 1 — author response

### pmos-managerkit-cross-F1 — Accepted
- Fix: interview-feedback/SKILL.md · frontmatter `description` (line 3) AND interview-guide/SKILL.md · frontmatter `description` (line 3) · before: both descriptions claim near-identical "set up the interview loop" phrasing with no boundary → after: (1) interview-feedback's description rewords the setup clause to name what setup actually does — "scaffolding a role's interview process storage (rounds, role.json)" — and changes the trigger phrase from "set up the interview loop for <role>" to "scaffold the interview process for <role>", plus appends a NOT-for line: "NOT for authoring one round's interviewer kit — reference, scoring sheet, case doc (/interview-guide)." (2) interview-guide's description appends the mirror NOT-for line: "NOT for scaffolding the role's process storage or scoring a candidate (/interview-feedback setup / score)." Pattern follows /shape's description.
- Rationale: The router has no disambiguation rule today; the two skills genuinely divide the work (guide = one round's kit with the §H anchor validator; feedback setup = role storage + role.json), so the descriptions should say so explicitly rather than both claiming the same phrase.
- Blast radius: two SKILL.md frontmatter descriptions only; skill-eval description/triggering checks re-run on both skills; no scripts, lints, or tests touched. README/plugin.json trigger prose reviewed for the retired phrase (folds into F4's edit).

### pmos-managerkit-cross-F2 — Accepted
- Fix: interview-feedback/SKILL.md · Phase: Setup {#setup} (line ~224) · before: setup "attach[es] the provided interviewer-reference + scorecard or generate[s] them by instantiating" the _shared skeletons with no gate → after: (1) the primary path for generating a round's kit becomes "invoke /interview-guide for the round" (the skill that owns kit authoring and its validator); (2) the skeleton-instantiation fallback (kept for --non-interactive and node-less degradation parity) gains an explicit §H hard gate: every generated OR user-attached scorecard must pass `node ../interview-guide/scripts/validate-scorecard-anchors.mjs <sheet>` (weights-sum-to-100, anchor set, data-budget≤data-duration) before being written into the role; non-zero exit = repair and re-run, never proceed. Add the same node-missing degradation wording interview-guide:58 already uses (emit + stderr warn + tell operator to validate before scoring).
- Rationale: Two authoring paths for the same machine-anchored artifact, only one gated, is a latent score-time failure ("weights sum to 97 sails through"). Routing through /interview-guide (or at minimum its validator) restores one gate for one artifact.
- Blast radius: interview-feedback/SKILL.md Setup phase + its platform-adaptation/degradation section; cross-skill script dependency on interview-guide/scripts/validate-scorecard-anchors.mjs (same plugin, path verified to exist); no change to the validator itself; skill-eval §H-gate checks on interview-feedback re-run; interop design D8 doc unchanged (this enforces it).

### pmos-managerkit-cross-F3 — Accepted
- Fix: interview-feedback/SKILL.md:19 and interview-guide/SKILL.md:20 · before: `../feature-sdlc/reference/skill-patterns.md` (resolves inside pmos-managerkit, where no feature-sdlc dir exists — verified) → after: `../../../pmos-toolkit/skills/feature-sdlc/reference/skill-patterns.md`, byte-matching one-on-one/SKILL.md:23's verified-resolving form; drop the now-redundant "(pmos-toolkit)" parenthetical or keep it, but the path itself must resolve.
- Rationale: An agent following the cite gets a 404 and silently proceeds without the authoring standard; one-on-one already carries the correct form, so this is pure normalization to the working pattern.
- Blast radius: two one-line edits; lint-phase-refs.sh validates Phase/{#slug}/SKILL.md#anchor refs, not this reference-doc path, so no lint churn; no tests.

### pmos-managerkit-cross-F4 — Accepted
- Fix: four homes, one release · (1) `.claude-plugin/plugin.json:4` and (2) `.codex-plugin/plugin.json:4` (verified identical fossil) · before: "Ships /interview-feedback, which turns…" → after: "Ships /interview-feedback (grounded candidate scorecard + interviewer coaching notes), /interview-guide (per-round interviewer kit: reference, anchored scoring sheet, case docs), and /one-on-one (1:1 prep, notes, and follow-up tracking)." — one clause per skill, exact wording tuned to each skill's own description. (3) README.md · intro line 6–7 loses "The first skill is `/interview-feedback`; more manager skills (team reviews, 1:1 prep, calibration) will land here over time" in favor of a three-skill sentence, and "## Skills" gains `### /interview-guide` and `### /one-on-one` sections (currently 0 grep hits for either — verified). (4) repo-root CLAUDE.md:15 charter row parenthetical → lists all three skills. Per repo policy the two plugin.json description edits ride the next managerkit release commit (no standalone version bump for a description-only change; it folds into whatever ships next).
- Rationale: Marketplace-facing catalog text is how installers evaluate the plugin; two-thirds of shipped value is invisible. This is the plugin's own recorded [J]-drift failure class.
- Blast radius: both plugin.json manifests (paired, per "Plugin manifest version sync" — description only, no version field games), README.md, repo-root CLAUDE.md; no skills' SKILL.md, no lints/evals/tests.

### pmos-managerkit-cross-F5 — Accepted
- Fix: new canonical home + two cites · create `skills/_shared/interview-guidelines/guidelines/README.md` (managerkit-local _shared — NOT in the cross-plugin sync intersection, so no sync-shared.sh implications) listing the bundled archetypes one per line with a one-phrase gloss each, and stating the ground-truth rule: "the archetype set IS the directory listing here; adding a dir adds an archetype." Then: interview-feedback/SKILL.md:252 keeps the enum line (role.json schema needs it inline for validation) but drops the literal count "(the 8 bundled PM round types)" in favor of "(the bundled PM round types — canonical list: `../_shared/interview-guidelines/guidelines/README.md`, ground truth = that directory's subdirs)"; interview-guide/SKILL.md:242's restated list is replaced by a cite to the same README plus only its per-skill deltas (which archetypes are case-type, work-history's non-case note). Also sweep both skills for other literal "8" count-claims and remove/re-source them.
- Rationale: §K one-fact-one-home; this exact set already produced the first-ever red [J] gate ("7 bundled" vs 8). Directory-as-ground-truth plus one prose home makes archetype #9 a one-dir + one-README-line change.
- Blast radius: one new _shared file; two SKILL.md edits; interview-feedback's role.json enum stays inline (schema-load-bearing) so fill-scorecard.mjs/questionnaire.mjs untouched; the [J] coherence gate at next release verifies the cites resolve; lint-phase-refs unaffected (no phase anchors involved).

### pmos-managerkit-cross-F6 — Accepted
- Fix: skills/_shared/interview-guidelines/reference-resolution.md · lines 3, 15, 25 (all verified dead) · before: `../SKILL.md#resolve`, `../SKILL.md#coach`, `../SKILL.md#score`, `../SKILL.md#transcribe` (resolve to nonexistent `skills/_shared/SKILL.md`) → after: `../../interview-feedback/SKILL.md#resolve` etc. — repoint every back-link to the doc's actual consumer, matching its post-move (epic 260702-cqf) location. Verify each target anchor exists in interview-feedback/SKILL.md ({#resolve} {#coach} {#score} {#transcribe}) as part of the edit.
- Rationale: The doc fossilizes its pre-_shared location; every relative back-link is a 404. lint-phase-refs.sh doesn't scan reference-doc back-links, so nothing else will catch it.
- Blast radius: one reference doc, 3 lines; no SKILL.md, no scripts; suggest (non-blocking, follow-up) extending lint-phase-refs.sh to scan `_shared/**/*.md` back-links — that lint lives in pmos-toolkit tools/, out of this unit.

### pmos-managerkit-cross-F7 — Accepted (fix lands in the canonical substrate, not in managerkit copies)
- Fix: canonical home first — pmos-toolkit `skills/_shared/non-interactive.md` (Section owner) · before: step 5 cites "(`_shared/non-interactive.md`)" as a relative-looking path and step 8 hardcodes "pmos-toolkit: /<skill> finished" → after: (a) step 5's cite becomes plugin-neutral and explicitly rooted: "pmos-toolkit's `skills/_shared/non-interactive.md` (the block's canonical owner)" so it reads as a pointer, not a resolvable relative path; (b) step 8's stderr prefix becomes plugin-neutral: "pmos: /<skill> finished — outcome=…". Then a repo-wide re-paste of the frozen block into every consumer skill (all plugins) in one commit, keeping lint-non-interactive-inline.sh green (its source of truth IS the canonical file, so it flips automatically once the canonical file changes; every stale copy then fails the lint until re-pasted — which is the desired forcing function). The three managerkit copies are byte-identical today (verified by the reviewer), so managerkit's change is exactly the re-paste.
- Rationale: The byte-identity contract is correct; the defect is that the canonical bytes bake in owner-plugin-isms. Fixing the source and re-pasting is the only move that doesn't violate the freeze. Cannot be fixed managerkit-locally without breaking the lint.
- Blast radius: LARGE and cross-plugin — pmos-toolkit skills/_shared/non-interactive.md, every user-invocable SKILL.md carrying the block across all 5 plugins, tools/lint-non-interactive-inline.sh (re-run, not edited), audit-recommended.sh unaffected. NFR-07's stderr-prefix contract consumers (if any parse "pmos-toolkit:") must be grepped before landing. Severity stays Nit; recommend shipping as its own dedicated substrate commit, not folded into a managerkit story.

**Author verdict: 7 accepted / 0 rejected / 0 invalid.** All quotes spot-checked verbatim against files (F2's quote paraphrases its lead-in but contains a >40-char verbatim run of the skeleton-instantiation clause; grounded).

## Pass 2 — reviewer findings
### pmos-managerkit-cross-F8 [Should-fix] F1 resolves the "interview loop" trigger collision in the wrong direction
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:3
- Quote: "Use when setting up a role's interview loop, writing an interviewer guide or brief, building a scoring sheet or scorecard for a round"
- Problem: F1 removes "set up the interview loop for <role>" from interview-feedback and only APPENDS a NOT-for to interview-guide — it leaves interview-guide's positive triggers "Use when setting up a role's interview loop" and "set up the <role> interview loop" intact. But interview-guide's own body scopes it to "the interviewer-facing kit for **one interview round**" (SKILL.md:8). A "loop" is the whole multi-round process — exactly what interview-feedback `setup` scaffolds (rounds + role.json). Post-F1 the router deterministically sends whole-loop setup requests to the single-round skill: the collision is gone but the survivor is the wrong claimant. F1 must also reword interview-guide's loop-claiming clauses (e.g. "authoring a round's interviewer kit within a role's loop" / retire "set up the <role> interview loop" in favor of "write the interviewer guide for <round>"), not just add the NOT-for boundary.

### pmos-managerkit-cross-F9 [Should-fix] F2 gates the setup authoring path but leaves the score-time consumption point ungated
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:146
- Quote: "`scripts/fill-scorecard.mjs` parses the anchors → dimensions/scales/flags and produces `filled-scorecard.html`."
- Problem: F2's own defect statement says "a weights-sum-97 sheet only bites at score time" — but the proposed gate runs only in the Setup phase, on sheets written into the role. The `score` verb accepts the round's scorecard directly as candidate input, and an anchored sheet handed in at score time (never having passed through setup or /interview-guide) is parsed by fill-scorecard.mjs with no weight-sum / anchor-set assertion (grep of fill-scorecard.mjs shows it reads `data-weight` values but never checks they sum to 100 — the only structural fallback documented is the "Foreign scorecard (no anchors)" DOM-inference path, which triggers only when anchors are absent, not when they are present-but-invalid). So after F2 lands, a malformed anchored sheet supplied at score time is filled silently — worse than "bites at score time": nothing bites at all. The gate belongs (also) at the consumption point: run `validate-scorecard-anchors.mjs` in Resolve/Score on any anchored input sheet, with the same node-missing degradation F2 already specifies for setup.

### pmos-managerkit-cross-F10 [Should-fix] F5's proposed ground-truth rule contradicts the inline enum F5 deliberately retains
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:252
- Quote: "`archetype ∈` {`recruiter-screen`, `product-sense`, `analytical`, `technical`, `behavioral`, `case-study`, `case-presentation`, `work-history`}"
- Problem: F5's new `guidelines/README.md` is to state "the archetype set IS this directory's subdir listing; adding a dir adds an archetype" — while F5 simultaneously keeps interview-feedback's inline role.json enum because it is "load-bearing for validation". Both statements cannot be true: adding a 9th subdir does NOT make role.json accept the new id until someone hand-edits the retained enum, so the README's ground-truth rule is false as written the moment it is exercised — recreating exactly the count-drift class ([J] red gate, 2026-07-09) the fix exists to kill, now with an authoritative-sounding doc asserting the wrong invariant. The README rule needs the caveat baked in: "adding a dir adds an archetype AND requires updating the role.json `archetype ∈` enum in interview-feedback/SKILL.md § role.json" (or the enum line must carry a back-pointer marker the README names), so the two homes reference each other instead of silently diverging.

### pmos-managerkit-cross-F11 [Nit] Manifest `keywords` also fossilize the one-skill era; F4 edits only `description`
- Where: plugins/pmos-managerkit/.claude-plugin/plugin.json:11
- Quote: "\"keywords\": [\n    \"manager\",\n    \"hiring\",\n    \"interview\",\n    \"scorecard\",\n    \"feedback\",\n    \"coaching\",\n    \"transcription\"\n  ],"
- Problem: every keyword describes /interview-feedback's domain (scorecard, transcription) — nothing surfaces /one-on-one (1:1, direct reports, coaching cadence, career conversations) or /interview-guide's authoring side (case study, interviewer reference). F4 fixes the `description` fields in both manifests but does not touch `keywords`, so marketplace search/discovery keeps advertising a one-skill plugin. Extend F4's paired-manifest edit to add e.g. "one-on-one", "1:1", "interviewer-guide", "case-study" in both plugin.json files in the same release commit.

**Pass 2 verdict:** 0 blockers / 3 should-fix / 1 nit — material findings

## Pass 2 — author response

### pmos-managerkit-cross-F8 — Accepted
- Fix: interview-guide/SKILL.md · frontmatter `description` (line 3), amending F1's plan · before: F1 only appended a NOT-for to interview-guide, leaving its positive loop-claims ("Use when setting up a role's interview loop", trigger "set up the <role> interview loop") intact → after: F1 is extended to also reword interview-guide's loop-claiming clauses. "Use when setting up a role's interview loop" becomes "Use when authoring a round's interviewer kit within a role's interview loop"; the trigger "set up the <role> interview loop" is retired (the remaining triggers — "write the interviewer guide for <round>", "build a scoring sheet for <role>", "draft a case study…", "make an interviewer brief" — already cover the skill's real scope). The NOT-for lines from F1 stand.
- Rationale: Correct — the reviewer caught that F1 as written resolves the collision by handing the whole-loop phrase to the single-round skill (guide's own body at SKILL.md:8 scopes it to "the interviewer-facing kit for **one interview round**"). Post-F1+F8, whole-loop language routes to interview-feedback `setup`, per-round language to interview-guide, and both carry mirror NOT-for boundaries.
- Blast radius: same as F1 (two frontmatter descriptions; skill-eval description/triggering re-run on both); no new surface.

### pmos-managerkit-cross-F9 — Accepted
- Fix: interview-feedback/SKILL.md · Phase: Resolve {#resolve} and/or Phase: Score {#score} (~line 146), amending F2's plan · before: F2 gates only the Setup authoring path; the score verb consumes an anchored sheet via fill-scorecard.mjs with no structural assertion (verified: fill-scorecard.mjs reads `data-weight` but never checks the sum; the only fallback is the "Foreign scorecard (no anchors)" path, which triggers on absent anchors, not present-but-invalid ones) → after: add a consumption-point §H gate: before fill-scorecard.mjs runs on any sheet that carries the machine anchors, run `node ../interview-guide/scripts/validate-scorecard-anchors.mjs <sheet>`. Exit 0 → proceed. Non-zero → interactive: surface the validator's failure list and repair (or have the user fix) before filling; non-interactive: refuse to fill and report the failures (a mis-weighted sheet silently mis-weights the whole scorecard — no safe default). The anchor-less foreign-scorecard DOM-inference path is untouched. Node-missing degradation mirrors F2's wording (warn on stderr, proceed with the model flagging any visible weight/anchor inconsistencies as open questions).
- Rationale: Correct — F2's own defect statement ("bites at score time") is only half-fixed by gating setup; a sheet handed directly to `score` bypasses both gates and nothing bites at all. One validator, run at both the authoring and consumption points, closes the loop.
- Blast radius: interview-feedback Resolve/Score phase prose + degradation section; same cross-skill script dependency F2 already introduces (validate-scorecard-anchors.mjs, unchanged); fill-scorecard.mjs itself untouched (gate runs before it, keeping validation out of the fill script per one-tool-one-job); skill-eval §H checks re-run on interview-feedback.

### pmos-managerkit-cross-F10 — Accepted
- Fix: the new `_shared/interview-guidelines/guidelines/README.md` + interview-feedback/SKILL.md:252, amending F5's plan · before: F5's README asserts "the archetype set IS this directory's subdir listing; adding a dir adds an archetype" while F5 simultaneously retains the load-bearing role.json enum — the rule is false the moment a 9th dir lands → after: the README's rule becomes a two-way contract: "the archetype set is defined by this directory's subdirs; adding one requires (1) the new `guidelines/<id>/` dir and (2) updating the `archetype ∈` enum in interview-feedback/SKILL.md § role.json {#storage} — the two MUST stay in lockstep; a checklist for adding archetype #9 follows." The retained enum line in interview-feedback gains a back-pointer marker in an adjacent HTML comment ("<!-- archetype-enum: keep in lockstep with ../_shared/interview-guidelines/guidelines/README.md -->") that the README names, so each home cites the other. interview-guide's cite (F5 unchanged) points at the README.
- Rationale: Correct — an authoritative-sounding doc asserting a one-file invariant that is actually a two-file invariant is worse than no doc; the reviewer's mutual-reference shape is exactly §K done right (one canonical prose home + one load-bearing enum, cross-cited).
- Blast radius: same files as F5 (new README + two SKILL.md edits); the [J] coherence gate at next release is the enforcement backstop for the lockstep claim; no scripts/lints/tests.

### pmos-managerkit-cross-F11 — Accepted
- Fix: `.claude-plugin/plugin.json` + `.codex-plugin/plugin.json` · `keywords` arrays, amending F4's plan · before: all 7 keywords describe /interview-feedback's domain (verified: manager, hiring, interview, scorecard, feedback, coaching, transcription) → after: F4's paired-manifest edit also extends `keywords` in BOTH manifests, same release commit: add "one-on-one", "1:1", "interviewer-guide", "case-study", "interview-loop" (keep the existing 7 — they remain accurate). Verify the .codex-plugin manifest carries a keywords field before editing; if it doesn't, add only where the field exists (no schema invention).
- Rationale: Correct and cheap — marketplace search is keyword-driven; a description fix alone leaves discovery advertising a one-skill plugin.
- Blast radius: same paired-manifest edit as F4 (description + keywords ride together); no version-field games per "Plugin manifest version sync"; no other files.

**Pass 2 author verdict: 4 accepted / 0 rejected / 0 invalid.** All four quotes spot-checked verbatim (interview-guide/SKILL.md:3; interview-feedback/SKILL.md:146, 252; .claude-plugin/plugin.json keywords block); F9's fill-scorecard.mjs claim independently re-verified (greps for weight-sum/100 assertions: none).

## Pass 3 — reviewer findings
### pmos-managerkit-cross-F12 [Should-fix] F2's setup-time gate on "user-attached" scorecards breaks the plugin's supported foreign (anchor-less) scorecard path
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:150 (and :224; plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md:15)
- Quote: "**Foreign scorecard (no anchors).** If the round's scorecard lacks the anchors, infer the dimensions/scales from its DOM, then: interactive → echo the inferred structure for confirmation before filling"
- Problem: The proposal's F2 change item 2 states the setup fallback gate as: every generated "**or user-attached**" scorecard "must pass `node ../interview-guide/scripts/validate-scorecard-anchors.mjs <sheet>` before being written into the role; non-zero exit → repair and re-run, never proceed." But Setup (SKILL.md:224 "attach the provided interviewer-reference + scorecard or generate them by instantiating…") is exactly where an operator attaches their company's OWN scorecard — which may legitimately carry no `data-card="scorecard"` anchors at all. The plugin explicitly supports that artifact end-to-end: reference-resolution.md:15 routes it through `fill-scorecard.mjs parse` (`anchored:false`) with DOM inference, and the F9 amendment carefully scoped the score-time gate to sheets that "carry the machine anchors" for precisely this reason. The setup gate as written has no such carve-out, so an anchor-less attached scorecard hard-fails a §H "never proceed" gate at setup — a contract break that removes a supported workflow the score verb still honors. The setup gate needs the same scoping as F9's: validator only for sheets that carry the anchors (generated ones always do); anchor-less attachments take the documented foreign-scorecard path (interactive confirm / non-interactive open-question logging), never a hard refusal.

### pmos-managerkit-cross-F13 [Should-fix] Interviewer-effectiveness rubric enumeration + count duplicated outside F5's sweep scope — same §K drift class F5 fixes for archetypes
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:220 and plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md:21
- Quote: "the bundled researched rubric — structured/consistent questioning, probing depth, leading-question avoidance, talk-time balance, coverage, bias mitigation, note quality, calibration"
- Problem: interview-feedback/SKILL.md:220 hand-restates the full 8-dimension list of `interviewer-effectiveness.html`, and reference-resolution.md:21 hand-count-claims it ("holds the lead to the **full** interviewer-effectiveness rubric (all 8 dimensions in `interviewer-effectiveness.html`)"), with a third partial restatement at reference-resolution.md:22 (shadow's "reduced set (note-quality, calibration, bias-mitigation)"). The canonical home is the bundled HTML rubric; adding or renaming a dimension there requires hand-syncing an enumeration and a count in two other files — exactly the "8 bundled" drift class F5/F10 kill for archetypes (and the class behind the first-ever red [J] gate, per the proposal's own rationale). The proposal does NOT cover this: F5's sweep is scoped to "both skills for any other literal \"8\" count-claims" — SKILL.md:220's enumeration carries no literal "8" (so the sweep misses it), and reference-resolution.md is a `_shared` reference doc, not one of "both skills" (so its "all 8 dimensions" count survives). Fix in the same shape as F5: SKILL.md:220 drops the inline list in favor of a cite to the rubric file; reference-resolution.md:21 drops the literal count ("the full rubric in `interviewer-effectiveness.html`").

### pmos-managerkit-cross-F14 [Nit] F4's plugin.json description replacement is ambiguous about the charter preamble
- Where: plugins/pmos-managerkit/.claude-plugin/plugin.json:4
- Quote: "Manager kit — skills that help you do manager work: hiring, team, and reviews. Ships /interview-feedback, which turns a candidate's"
- Problem: The current description is charter-preamble + skill inventory. F4's replacement text is specified starting at "Ships /interview-feedback (…), /interview-guide (…), and /one-on-one (…)" with no statement about the leading "Manager kit — skills that help you do manager work: hiring, team, and reviews." sentence — an implementer following the proposal literally could drop it, severing the description's mirror of the repo-root CLAUDE.md charter row ("…do manager work") that makes the plugin discoverable by its charter phrasing. The proposal should state explicitly that the preamble sentence is retained and only the "Ships …" clause is rewritten (in both manifests).

**Pass 3 verdict:** 0 blockers / 2 should-fix / 1 nit — material findings

## Pass 3 — author response

### pmos-managerkit-cross-F12 — Accepted
- Fix: interview-feedback/SKILL.md · Phase: Setup {#setup} (~line 224), amending F2's plan item 2 · before: F2's setup fallback gate reads "every generated **or user-attached** scorecard must pass `validate-scorecard-anchors.mjs` … non-zero exit → repair and re-run, never proceed" — no carve-out for anchor-less sheets → after: the setup gate is scoped exactly like F9's consumption gate: the validator runs only on sheets that **carry the machine anchors** (`data-card="scorecard"` present — generated sheets always do, since the skeleton bakes the anchors in); an anchor-less user-attached scorecard takes the already-documented foreign-scorecard path (SKILL.md:150 / reference-resolution.md:15 — DOM inference, interactive confirm / non-interactive open-question logging) and is written into the role with an `anchored:false` note, never hard-refused. Present-but-invalid anchors still hard-fail the gate (that IS the F2 defect).
- Rationale: Correct catch — the plugin explicitly supports foreign anchor-less scorecards end-to-end at score time, and F2 as written would break that supported workflow one phase earlier at setup. The gate's job is "anchored implies valid", not "everything must be anchored". F9 already got this scoping right; F12 makes setup consistent with it.
- Blast radius: same files as F2 (interview-feedback Setup phase prose + degradation section); no new scripts; the foreign-scorecard path and fill-scorecard.mjs untouched; skill-eval §H checks re-run on interview-feedback (unchanged from F2's radius).

### pmos-managerkit-cross-F13 — Accepted
- Fix: interview-feedback/SKILL.md · Phase: Coach {#coach} (~line 220) AND skills/_shared/interview-guidelines/reference-resolution.md:21 · before: Coach hand-restates the full 8-dimension rubric list ("structured/consistent questioning, probing depth, … note quality, calibration") and reference-resolution.md:21 hand-count-claims it ("all 8 dimensions in `interviewer-effectiveness.html`") → after: (1) SKILL.md Coach drops the inline enumeration in favor of citing the canonical home: "scored against `../_shared/interview-guidelines/interviewer-effectiveness.html` (the bundled researched rubric — the dimension set is defined there)"; (2) reference-resolution.md:21 drops the literal count: "holds the lead to the **full** rubric in `interviewer-effectiveness.html`". (3) reference-resolution.md:22's shadow reduced set (note-quality, calibration, bias-mitigation) is KEPT — it is a per-consumer delta (a subset selection with behavioral meaning), which §K permits at the call site; but it gains a lockstep note naming the rubric file as the home of the ids. Fold this sweep into F5/F10's edit pass (same §K shape, same commit).
- Rationale: Correct — F5's sweep was scoped to "literal \"8\" count-claims in both skills", which misses a count-less enumeration in SKILL.md and a count in a _shared reference doc. Same drift class as the archetype list; the rubric HTML is the ground truth and should be the single prose home.
- Blast radius: interview-feedback/SKILL.md Coach phase (one sentence), reference-resolution.md (one line + one lockstep note); interviewer-effectiveness.html unchanged; no scripts/lints/tests; rides F5/F10's commit.

### pmos-managerkit-cross-F14 — Accepted
- Fix: proposal-text precision on F4 · `.claude-plugin/plugin.json:4` + `.codex-plugin/plugin.json:4` · before: F4's replacement was specified starting at "Ships /interview-feedback (…)…" with no statement about the leading charter sentence → after: F4 is amended to state explicitly: the preamble "Manager kit — skills that help you do manager work: hiring, team, and reviews." is RETAINED verbatim in both manifests; only the "Ships …" clause that follows it is rewritten to the three-skill inventory. Final shape: preamble sentence + new three-skill "Ships …" sentence.
- Rationale: Correct — the preamble mirrors the repo-root CLAUDE.md charter row ("…do manager work") and is the plugin's charter-phrasing discovery hook; an implementer following the proposal literally could have dropped it. Zero-cost disambiguation.
- Blast radius: none beyond F4's existing radius (both manifests, paired edit); this only sharpens the edit spec.

**Pass 3 author verdict: 3 accepted / 0 rejected / 0 invalid.** All three quotes spot-checked verbatim (interview-feedback/SKILL.md:150 foreign-scorecard block; SKILL.md Coach rubric enumeration + reference-resolution.md:21 "all 8 dimensions"; plugin.json:4 preamble). Cumulative: 14 findings, 14 accepted.

## Pass 4 — reviewer findings
### pmos-managerkit-cross-F15 [Should-fix] F2's setup fallback has no stated firing condition — "--non-interactive / degradation parity" reads as "headless always skips /interview-guide"
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/_cross__pmos-managerkit.md:62
- Quote: "keep the skeleton-instantiation fallback (for --non-interactive / degradation parity) with an explicit §H hard gate"
- Problem: Vague enough to be implemented wrong. /interview-guide fully supports `--non-interactive` (its own frozen block + DEFER contracts for duration/case/business-context), so "for --non-interactive" is not a valid reason to fall back — yet an implementer following the parenthetical literally will route EVERY headless `setup` around the primary path, permanently bypassing the richer authoring flow (archetype corpus tailoring, work-history ladder, duration anchors) that F2 item 1 exists to make primary and that interop design D8 depends on. The proposal never states when the fallback fires (child skill uninstalled? operator declines? explicit flag?). The condition must be pinned — e.g. "fallback only when /interview-guide is unavailable in the environment; headless dispatch prepends `[mode: non-interactive]` and uses the primary path" — or F2's own rationale ("that skill owns kit authoring and its validator") is silently defeated on the most common unattended path.

### pmos-managerkit-cross-F16 [Should-fix] The gated setup fallback has no weighting protocol to satisfy the gate it now runs — work-history weights are "selected, not assigned" and the selection logic lives only in /interview-guide
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:196
- Quote: "and its weights are **selected, not assigned** (design D7/D8)"
- Problem: F2 item 2 adds `validate-scorecard-anchors.mjs` as a hard gate on the setup fallback ("non-zero exit → repair and re-run, never proceed") but the fallback path in interview-feedback carries zero weighting guidance — the sum-to-100 assignment discipline, and specifically the work-history rule that weights are copied verbatim from the level ladder (`../_shared/interview-guidelines/guidelines/work-history/level-ladder.md`, seniority-selected row), exist only in interview-guide's Phase Scoring Sheet {#scoring-sheet} / {#work-history-weights}. Post-F2, a fallback setup of a `work-history` round (one of the 8 archetypes setup scaffolds per interview-feedback/SKILL.md:224 "each an archetype from the 8 bundled PM round types or `custom`") either loops on gate failure with no repair recipe, or the model free-assigns weights that happen to sum to 100 — passing the gate while violating the D7/D8 "selected, not assigned" contract the gate cannot see. The proposal needs one line in the fallback spec: cite interview-guide's weighting protocol (§K — cite, don't restate) or restrict the fallback's generate branch to attach-only/non-work-history.

### pmos-managerkit-cross-F17 [Nit] README names output (b) "interviewer-performance notes" — a term used nowhere in the skill; F4's README edit doesn't cover it
- Where: plugins/pmos-managerkit/README.md:19
- Quote: "**(b) interviewer-performance notes** — per interviewer, scored against a bundled,"
- Problem: The skill's canonical name for output (b) is "interviewer-effectiveness notes" (interview-feedback/SKILL.md:13, and the rubric file is `interviewer-effectiveness.html`). F4's README change is scoped to the "first skill" fossil intro + adding the two missing skill sections; this pre-existing naming drift in the surviving `### /interview-feedback` section would ride through the fix. One-word rename while the file is already open.

### pmos-managerkit-cross-F18 [Nit] Scorecard anchor set restated in three homes (skeleton HTML, output-shapes.md, interview-feedback Score phase) — same §K drift class as F5, outside its sweep
- Where: plugins/pmos-managerkit/skills/interview-guide/reference/output-shapes.md:49
- Quote: "(integer; **all weights sum to exactly 100**); a `data-scale=\"1-4\"`"
- Problem: F5's sweep is scoped to "either set" = the archetype list and the effectiveness-rubric dimensions; the scorecard anchor enumeration is a third §K-shaped duplication it doesn't touch. The full anchor list is hand-restated in output-shapes.md (:45–52) AND in interview-feedback/SKILL.md:146 ("`../_shared/interview-guidelines/scorecard-skeleton.html` is the contract: `data-dim`, `data-weight`, `data-scale`, `data-v`, …") — and the two prose copies have already drifted: interview-feedback:146's list omits the `data-duration`/`data-budget` anchors that output-shapes.md documents (they surface only later at :152/:169), and output-shapes hardcodes `data-scale="1-4"` where :146 says bare `data-scale`. The skeleton + validator are the real enforcers, so this is polish — but the fix is cheap: interview-feedback:146 should cite the skeleton (which both files already call "the contract") rather than re-enumerating, mirroring exactly what F5 does for the archetype list.

**Pass 4 verdict:** 0 blockers / 2 should-fix / 2 nits — material findings

## Pass 4 — author response

### pmos-managerkit-cross-F15 — Accepted
- Fix: interview-feedback/SKILL.md · Phase: Setup {#setup}, amending F2's plan item 2 · before: the fallback's firing condition is the vague parenthetical "(for --non-interactive / degradation parity)" — readable as "headless always skips /interview-guide" → after: the firing condition is pinned explicitly: the skeleton-instantiation fallback fires ONLY when /interview-guide is unavailable in the environment (skill not installed / not resolvable) or the operator explicitly declines the primary path for a round. `--non-interactive` is NOT a fallback trigger — interview-guide fully supports headless runs (verified: `--non-interactive` in its argument-hint at SKILL.md:5, frozen block at :60, DEFER contracts for duration/case/business-context at :42), so headless setup dispatches the primary path with `[mode: non-interactive]` prepended per the frozen block's child-dispatch rule. "Degradation parity" is re-scoped to what it actually meant: node-less environments degrade per item 4's wording, on either path.
- Rationale: Correct — as written, an implementer would route every unattended setup around the richer authoring flow (archetype tailoring, work-history ladder, duration anchors) that F2 item 1 exists to make primary, silently defeating F2's own rationale and interop design D8 on the most common unattended path.
- Blast radius: same files as F2/F12 (interview-feedback Setup phase prose); no new scripts; adds a child-skill availability probe to the Setup phase spec; skill-eval §L (subagent/child dispatch) wording re-checked on interview-feedback.

### pmos-managerkit-cross-F16 — Accepted
- Fix: interview-feedback/SKILL.md · Phase: Setup {#setup} fallback spec, amending F2's plan item 2 · before: the gated fallback carries zero weighting guidance — the sum-to-100 discipline and the work-history "weights are **selected, not assigned**" rule (verified at interview-guide/SKILL.md:196; ladder-copy protocol at {#work-history-weights}, :194–198) live only in interview-guide → after: one added line in the fallback spec, §K cite-don't-restate: "when instantiating a scoring sheet, follow interview-guide's weighting protocol — `../interview-guide/SKILL.md#scoring-sheet`, and for `work-history` rounds `../interview-guide/SKILL.md#work-history-weights` (weights are copied verbatim from the seniority-selected row of `../_shared/interview-guidelines/guidelines/work-history/level-ladder.md`, never free-assigned — design D7/D8)." The cite works even when the /interview-guide *skill* is unavailable as a dispatch target (F15's pinned condition), because the SKILL.md file itself is on disk in the same plugin.
- Rationale: Correct — the validator asserts sum-to-100 and anchor shape but cannot see the D7/D8 "selected, not assigned" contract; without a repair recipe the fallback either loops on gate failure or free-assigns weights that pass the gate while violating the design. A cite (not a restatement) is the §K-correct shape.
- Blast radius: one line in interview-feedback Setup fallback spec; two cross-skill anchor cites ({#scoring-sheet}, {#work-history-weights} — both verified present in interview-guide/SKILL.md) now in lint-phase-refs.sh's scan surface, so the lint must be re-run after the edit; no scripts/tests.

### pmos-managerkit-cross-F17 — Accepted
- Fix: README.md:19, folded into F4's README edit · before: "**(b) interviewer-performance notes** — per interviewer, scored against a bundled," → after: "**(b) interviewer-effectiveness notes**" — one-word rename to the skill's canonical term (verified: interview-feedback/SKILL.md:13 and :220 both say "interviewer-effectiveness notes"; the rubric file is `interviewer-effectiveness.html`). Rides F4's commit since the file is already open.
- Rationale: Correct — F4's scope was the "first skill" intro + two missing skill sections; this pre-existing naming drift in the surviving section would ride through. Canonical-term consistency is exactly what makes grep-based drift sweeps ([J] gate) work.
- Blast radius: one word in README.md; no other surfaces (grep for "interviewer-performance" elsewhere in the plugin: README is the only home).

### pmos-managerkit-cross-F18 — Accepted
- Fix: interview-feedback/SKILL.md:146 (Phase: Score {#score}), folded into F5/F10/F13's §K sweep commit · before: :146 re-enumerates the anchor set inline ("`data-dim`, `data-weight`, `data-scale`, `data-v`, `data-input=\"notes:<dim>\"`, `data-flags`, `data-input=\"reco\"`") — already drifted (verified: omits `data-duration`/`data-budget`, which surface only later at ~:152 and which output-shapes.md documents; output-shapes.md:49 hardcodes `data-scale="1-4"` where :146 says bare `data-scale`) → after: :146 keeps its existing framing sentence — "`../_shared/interview-guidelines/scorecard-skeleton.html` is the contract" — and drops the parenthetical re-enumeration in favor of "(the anchor set is defined by the skeleton; `validate-scorecard-anchors.mjs` is its enforcer)". output-shapes.md's copy is interview-guide's authoring-side spec and stays (it is the emit contract for that skill's output), but gains a one-line lockstep note naming the skeleton as ground truth. No third prose enumeration remains in interview-feedback.
- Rationale: Correct — same §K class as the archetype list and effectiveness rubric, third instance, outside F5+F13's sweep ("either set"). The skeleton + validator are the real enforcers; prose copies have already demonstrably drifted.
- Blast radius: interview-feedback/SKILL.md:146 (one parenthetical), output-shapes.md (one added note); skeleton HTML and validator unchanged; fill-scorecard.mjs untouched; rides the F5/F10/F13 sweep commit.

**Pass 4 author verdict: 4 accepted / 0 rejected / 0 invalid.** All four quotes spot-checked verbatim (proposal:62; interview-guide/SKILL.md:196; README.md:19; output-shapes.md:49); F15's premise (interview-guide supports --non-interactive) and F18's drift claim (:146 omits data-duration/data-budget) independently re-verified. Cumulative: 18 findings, 18 accepted, 0 rejected.

## Pass 5 — reviewer findings
### pmos-managerkit-cross-F19 [Should-fix] Divergent confidentiality posture for same-class people data across the plugin
- Where: plugins/pmos-managerkit/skills/one-on-one/SKILL.md:104 (vs interview-feedback/SKILL.md:114)
- Quote: "holds **sensitive employee content** and must **never** live inside a repo working tree — `scripts/record-lib.mjs` refuses to write there (INV-4)"
- Problem: one-on-one takes the hard posture — records live under `~/.pmos/one-on-ones/`, outside any repo, and the script REFUSES to write in a working tree. interview-feedback handles equally sensitive people data (candidate recordings, transcripts, scores) yet defaults storage to `./interviews/` INSIDE the repo, protected only by a best-effort gitignore guard whose failure mode is soft: interview-feedback/SKILL.md:114 — "if the guard cannot be written, warn and continue (the operator is responsible — see § Confidentiality)". Same plugin, same class of confidential people data, opposite default locations and opposite failure modes (hard-refuse vs warn-and-continue). A manager who internalizes one skill's guarantee will wrongly attribute it to the other; one accidental `git add interviews/` after a failed guard write commits candidate PII. Cross-skill coherence fix: either align interview-feedback's default root outside the repo (or make the guard-write failure blocking), or document in both skills' Confidentiality sections WHY the postures intentionally differ. The proposal (F1–F18) touches neither skill's storage/confidentiality contract.

### pmos-managerkit-cross-F20 [Nit] F6's back-link fix misses the third pre-move fossil on the same line: a dangling `scripts/` path
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md:15
- Quote: "is handled by `scripts/fill-scorecard.mjs parse` (`anchored:false`) — the skill infers dimensions"
- Problem: F6 repoints the `../SKILL.md#…` back-links on lines 3, 15, 25 but leaves line 15's sibling fossil untouched: `scripts/fill-scorecard.mjs` resolves relative to the doc's home as `skills/_shared/interview-guidelines/scripts/fill-scorecard.mjs` — verified nonexistent (`_shared/interview-guidelines/` contains only `guidelines/` + 4 files, no `scripts/`). The script actually lives at `skills/interview-feedback/scripts/fill-scorecard.mjs`. Same pre-move (epic 260702-cqf) fossil class F6 exists to kill, on the very line F6 already edits — implementing F6 as written repairs the anchor link and ships the dangling script path in the same sentence. Extend F6's edit to repoint it to `../../interview-feedback/scripts/fill-scorecard.mjs`.

### pmos-managerkit-cross-F21 [Nit] Fourth §K enumeration outside F5's sweep scope: the "12 Reforge/Mehta" competency count restated across four homes
- Where: plugins/pmos-managerkit/skills/interview-guide/SKILL.md:196 (also :246; corpus scorecard.html:11, :198)
- Quote: "the 12 Reforge/Mehta competency `data-dim`s plus the `role-evidence` and `trajectory-synthesis` families"
- Problem: F5+F13+F18's sweep is explicitly scoped to "any other literal enumeration/count restatements of **any of the three sets**" (archetype list, effectiveness dimensions, scorecard anchor set) — so the work-history competency count, hand-restated at interview-guide/SKILL.md:196 and :246 plus twice in the corpus `work-history/scorecard.html` comments (":11 …12 competency data-dim sections (Reforge/Mehta framework)…"), escapes the sweep by construction. Ground truth is `level-ladder.md`'s weight table / the shipped scorecard's `data-dim` set; the literal "12" in four homes is the identical drift class (a competency added to the ladder strands the counts — the exact "7 bundled vs 8" red-[J] pattern). Low churn risk (external framework), hence Nit — but the sweep instruction should say "any bundled enumerated set", not "the three sets", so the next reviewer doesn't have to re-find set #4.

### pmos-managerkit-cross-F22 [Nit] F2/F15's fallback firing condition "skill not installed / not resolvable" is unimplementable as written and contradicts F16's own reasoning
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/_cross__pmos-managerkit.md:70
- Quote: "fires ONLY when /interview-guide is unavailable in the environment (skill not installed / not resolvable)"
- Problem: both skills ship in the same plugin — installing pmos-managerkit installs both, so "not installed" is a near-dead branch, and the proposal itself concedes this three lines later (F16 rationale, proposal line 71: "because the SKILL.md file is on disk in the same plugin"). Meanwhile the blast radius adds "a child-skill availability probe added to the Setup spec" with no definition of what probing sibling-skill availability means — an implementer will most likely test for the SKILL.md file on disk, which always succeeds, silently reducing the fallback to operator-decline-only without the spec saying so. Pin it: define the probe (e.g. "the Skill/dispatch tool cannot resolve `interview-guide`" for platforms that load skills individually — the Platform Adaptation case) or drop the not-installed clause and state the fallback is operator-decline-only, with node-less degradation handled per item 5 on the primary path. As written, this is a proposal item vague enough to be implemented wrong (category c).

**Pass 5 verdict:** 0 blockers / 1 should-fix / 3 nits — material findings

## Pass 5 — author response

### pmos-managerkit-cross-F19 — Accepted
- Fix: two files, two moves · (1) interview-feedback/SKILL.md · Phase: Resolve step 1 (~:114) + § Confidentiality · before: gitignore-guard write failure is soft ("if the guard cannot be written, warn and continue (the operator is responsible)") while sibling one-on-one hard-refuses repo storage → after: the guard-write failure mode becomes a hard stop, not a warn: if `storage.sh` cannot install/refresh the gitignore guard inside a git repo, do NOT write candidate inputs there — interactive: stop and offer (a) fix permissions and retry, (b) choose a `--root` outside the repo, or (c) explicit operator override "proceed unguarded" (destructive-class confirm, never a default); non-interactive: DEFER (log the open question, do not write candidate data into an unguarded working tree — no safe default). The default root stays `./interviews/` (NOT relocated — see rationale). (2) BOTH skills' Confidentiality/Store sections gain a mirror paragraph documenting WHY the postures intentionally differ, each citing the other: one-on-one records are personal per-report content with no repo-side consumers → hard out-of-repo (INV-4); interview-feedback's store is a role-team workflow tree (role.json, round guidelines, scorecards) whose non-PII scaffolding legitimately lives with the repo, so the boundary is the gitignore guard — which is exactly why the guard must be load-bearing (hard), not advisory.
- Rationale: The reviewer's failure mode is real — "one accidental `git add interviews/` after a failed guard write commits candidate PII", and the soft failure mode makes the plugin's two confidentiality guarantees incoherent. But relocating interview-feedback's default root out of the repo would break the role-scaffolding workflow (guidelines/round layout is designed to be shared with the hiring team) and every existing install; hardening the guard to blocking + documenting the intentional divergence closes the coherence gap at far smaller blast radius. This is the "or make the guard-write failure blocking … or document WHY" branch of the reviewer's own proposed remedy — we take both halves.
- Blast radius: interview-feedback/SKILL.md Resolve step 1 + § Confidentiality; one-on-one/SKILL.md § Store & privacy (one added mirror paragraph; INV-4 unchanged); `scripts/storage.sh` behavior contract sharpened in prose — verify whether storage.sh's exit code already distinguishes guard-write failure (if not, the prose gate is on the skill side reading its warning output; storage.sh itself may need a non-zero exit for guard failure — flag to implementer as a possible one-line script change); non-interactive DEFER classification consistent with the frozen block; skill-eval §H checks re-run on interview-feedback.

### pmos-managerkit-cross-F20 — Accepted
- Fix: skills/_shared/interview-guidelines/reference-resolution.md:15, folded into F6's edit · before: "`scripts/fill-scorecard.mjs parse`" resolves to `_shared/interview-guidelines/scripts/fill-scorecard.mjs` — verified nonexistent (dir listing shows no `scripts/`; the script lives at `interview-feedback/scripts/fill-scorecard.mjs`, verified) → after: repoint to `../../interview-feedback/scripts/fill-scorecard.mjs parse` in the same edit pass that repoints line 15's `../SKILL.md#score` back-link. F6's edit instruction is amended to: "repoint every pre-move relative path in the file — SKILL.md anchors AND script paths — to the doc's post-move resolution."
- Rationale: Correct — same pre-move (epic 260702-cqf) fossil class F6 exists to kill, on the very line F6 already edits; fixing the anchor and shipping the dangling script path in the same sentence would be embarrassing.
- Blast radius: none beyond F6's (one reference doc; line 15 now gets two repoints instead of one).

### pmos-managerkit-cross-F21 — Accepted
- Fix: sweep-scope broadening + one concrete edit · (1) The F5/F13/F18 sweep instruction is reworded from "any other literal enumeration/count restatements of any of the three sets" to "any literal enumeration/count restatement of ANY bundled enumerated set (archetypes, effectiveness dimensions, scorecard anchors, work-history competencies, …) — ground truth is always the bundled artifact/directory, prose cites it". (2) Concrete instance: interview-guide/SKILL.md:196 and :246 drop the literal "12" in favor of "the Reforge/Mehta competency `data-dim`s (the set is defined by `../_shared/interview-guidelines/guidelines/work-history/scorecard.html`; weights by `level-ladder.md`)". (3) The corpus `scorecard.html`'s own comments (:11, :198) are KEPT as-is — they are self-describing comments inside the ground-truth file itself; anyone adding a 13th competency is editing that file and sees them (lowest drift risk; churn there is not cross-file drift).
- Rationale: Correct — the sweep as scoped was enumerable-by-construction and set #4 escaped it; naming the class instead of the instances means the next reviewer doesn't re-find set #5. Keeping the ground-truth file's self-comments is the same judgment as F5 keeping the load-bearing role.json enum: in-home counts are not the drift class, cross-file ones are.
- Blast radius: interview-guide/SKILL.md two lines; corpus HTML untouched; rides the F5/F10/F13/F18 sweep commit; no scripts/lints/tests.

### pmos-managerkit-cross-F22 — Accepted
- Fix: proposal-text precision on F2/F15's fallback condition (change item 2) · before: "fires ONLY when /interview-guide is unavailable in the environment (skill not installed / not resolvable)" — a near-dead branch (same plugin installs both) with an undefined "availability probe" an implementer would reduce to a file-on-disk check that always passes → after: the condition is pinned in two defined clauses: (a) **operator-decline** — the operator explicitly declines the primary path for a round (the common branch); (b) **dispatch-unresolvable** — the platform's Skill/dispatch mechanism cannot resolve `interview-guide` as an invocable skill (the Platform Adaptation case: hosts that load skills individually rather than per-plugin; the probe is "attempt the dispatch, fall back on resolution failure", NOT a SKILL.md-on-disk check — attempt-then-fallback needs no separate probe primitive). The "child-skill availability probe" phrase is struck from F2's blast radius (no probe primitive exists or is invented; the dispatch attempt IS the probe). Node-less degradation stays on the primary path per item 5. F16's cite-based repair recipe is unaffected — it deliberately cites the SKILL.md file on disk, which is a *reading* dependency, not a dispatch one; the two clauses don't contradict once dispatch-resolution and file-reading are named as different things (the reviewer is right that the original wording conflated them).
- Rationale: Correct — "not installed" was effectively unimplementable as written and the undefined probe invited the always-true implementation, silently reducing the fallback to operator-decline-only without the spec saying so. Attempt-then-fallback plus operator-decline covers exactly the real cases with zero new machinery.
- Blast radius: proposal/spec wording for F2 item 2 only; same files as F2/F15; removes (rather than adds) a spec obligation ("availability probe"); skill-eval §L wording re-check stands.

**Pass 5 author verdict: 4 accepted / 0 rejected / 0 invalid.** All four quotes spot-checked verbatim (one-on-one/SKILL.md:104 block; interview-feedback Resolve step 1 warn-and-continue; reference-resolution.md:15 script path — `_shared/interview-guidelines/` confirmed to contain no `scripts/`; interview-guide/SKILL.md:196/:246 + corpus scorecard.html:11/:198 "12"/Reforge-Mehta claims; proposal:70 fallback wording). Cumulative: 22 findings, 22 accepted, 0 rejected.

## Author closeout — final pass (review loop terminated at pass cap)

State at closeout: 5 reviewer passes, 5 author responses. The latest reviewer-findings section (Pass 5, F19–F22) already carries a matching author response in this scratchpad; the only gap was that the cumulative proposal still reflected passes 1–4 (F1–F18). This closeout confirms the Pass 5 dispositions and folds F19–F22 into the final proposal rewrite.

Final spot-check of the Pass 5 quotes against the repo (all ≥40-char, all grounded):
- F19 — one-on-one/SKILL.md:104 "holds **sensitive employee content** and must **never** live inside a repo working tree" verified verbatim; the contrasting soft failure verified at interview-feedback/SKILL.md:114 ("warn and continue (the operator is responsible — see § …"). GROUNDED → Accepted as responded in Pass 5 (harden guard-write failure to a hard stop + mirror why-the-postures-differ paragraphs in both skills; default root NOT relocated).
- F20 — reference-resolution.md:15 "is handled by `scripts/fill-scorecard.mjs parse` (`anchored:false`)" verified verbatim; `_shared/interview-guidelines/` re-listed at closeout: contains only `guidelines/`, `interviewer-effectiveness.html`, `reference-resolution.md`, `reference-skeleton.html`, `scorecard-skeleton.html` — no `scripts/`, so the relative path is dangling as claimed. GROUNDED → Accepted (fold the script-path repoint into F6's edit).
- F21 — interview-guide/SKILL.md:196 and :246 both carry "12 Reforge/Mehta competency `data-dim`s" verified verbatim. GROUNDED → Accepted (broaden the §K sweep to "any bundled enumerated set"; drop the literal "12" from the two SKILL.md homes; keep the ground-truth file's self-comments).
- F22 — proposal line 70 "fires ONLY when /interview-guide is unavailable in the environment (skill not installed / not resolvable)" verified verbatim in the pre-closeout proposal. GROUNDED → Accepted (pin the fallback to operator-decline + dispatch-unresolvable via attempt-then-fallback; strike the "availability probe" phrase).

No earlier finding is missing a disposition: F1–F18 are accepted with all amendments folded (proposal sections F1+F8, F2+F9+F12+F15+F16, F3, F4+F11+F14+F17, F5+F10+F13+F18, F6, F7); F19–F22 are accepted per Pass 5's author response and now folded into the final proposal.

Final tally: 22 findings → 22 accepted / 0 rejected / 0 invalid. 0 open disagreements.

Final status: CAPPED — the loop terminated at the pass cap with the final pass (Pass 5) still surfacing a material [Should-fix] (F19, cross-skill confidentiality-posture divergence). Every finding is accepted with a described fix, so nothing is contested, but the review did not reach a nits-only/empty pass before the cap; convergence is unproven.

The cumulative proposal at docs/pmos/reviews/2026-07-12_blind-refactor/proposal/_cross__pmos-managerkit.md has been rewritten as the final self-contained version (F1–F22, status CAPPED, empty Open-questions list).
