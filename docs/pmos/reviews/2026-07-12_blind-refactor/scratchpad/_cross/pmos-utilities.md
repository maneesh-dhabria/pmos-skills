## Pass 1 — reviewer findings

### pmos-utilities-cross-F1 [Should-fix] Charter and manifest describe a 2-skill plugin; 4 skills exist, and 2 of them fail the membership test
- Where: CLAUDE.md:13 (repo root) and plugins/pmos-utilities/.claude-plugin/plugin.json:4
- Quote: "standalone diagnostics, cleanup, and meta-tooling that aren't part of a feature pipeline or a learning artifact (mac-health, reflect — a cross-plugin session retrospective)."
- Problem: The charter's Holds column — which CLAUDE.md itself says is "the membership test, not just a description" — names only mac-health and reflect, but the plugin ships four skills (converter, mac-health, reflect, to-notion-doc). The manifest has the same gap: `"description": "Environment utilities — keep your machine and working setup healthy. Standalone diagnostics and cleanup (e.g. Mac health)…"`. Neither /converter (convert a user's data file between formats) nor /to-notion-doc (publish a local document to Notion) maintains the user's environment — they operate on the user's *content*, which is much closer to pmos-toolkit's authoring/artifact charter (to-notion-doc is effectively a publish-an-artifact skill). Either the charter/manifest must be amended to honestly claim a broader "standalone utilities" scope, or the two content skills belong elsewhere. As it stands, the stated membership test would reject half the plugin's own contents, which makes the next skill-placement decision arbitrary.

### pmos-utilities-cross-F2 [Should-fix] /converter's prose and report line claim 2 conversion pairs while 4 are shipped and auto-registered
- Where: plugins/pmos-utilities/skills/converter/SKILL.md:18 (also :3, :93)
- Quote: "v1 ships two bidirectional pure pairs — **JSON ↔ YAML** and **CSV ↔ JSON** — built on a"
- Problem: `lib/converters/` contains four descriptor modules (`json-yaml.js`, `csv-json.js`, `html-md.js`, `pdf-md.js`), and `registry.js` auto-discovers all of them ("discover(dir): require every lib/converters/*.js so each module self-registers", lib/registry.js:75). `html-md.js` even documents itself as live ("with NO edits to server.js or ui/converter.html — the proof of the registry", html-md.js:5). Yet the frontmatter description defers those pairs to the future ("and, as the skill grows, HTML↔Markdown, PDF↔Markdown", SKILL.md:3) and Phase 0 step 4 instructs the model to "Report to the user: the URL, the supported conversions (JSON↔YAML, CSV↔JSON)" (SKILL.md:93) — a hard-coded count-claim that makes the skill misreport its own capabilities at runtime, and weakens triggering ("convert this HTML to Markdown" is advertised only as a future feature). Classic enumerated-set drift: the inventory grew, the prose didn't.

### pmos-utilities-cross-F3 [Should-fix] /reflect has duplicate phase numbers (two Phase 1s, 2s, 4s, 5s) and zero anchors — direct §J violation
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:82 vs 124 (also 109 vs 134, 159 vs 196, 208 vs 255)
- Quote: "## Phase 1 (multi-session prelude): Enumerate transcript candidates + D18 cap"
- Problem: The body interleaves a multi-session phase spine with the single-session spine using the *same* integer numbers: "## Phase 1 (multi-session prelude)…" (l.82) and "## Phase 1: Locate the Session Transcript" (l.124); likewise Phase 2 (l.109/134), Phase 4 (l.159/196), Phase 5 (l.208/255). Not one heading carries a `{#kebab-slug}` anchor (grep count: 0), so every cross-reference is a bare ambiguous number — e.g. "this seeds Phase 2 dispatch" (l.107) and "Single-session path goes directly to Phase 1 below" (l.84, which points *backwards-numbered but forwards-positioned*). §J exists precisely to prevent this: an executor resolving "Phase 2" has a coin-flip between two different procedures. Renumber one spine (e.g. Phases 1a–5a or M1–M5) and add anchors.

### pmos-utilities-cross-F4 [Should-fix] /reflect names two different transcript roots for the same fact — and they've drifted
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:88 vs 129
- Quote: "**Enumerate candidates** by globbing `~/.claude-personal/projects/<project>/*.jsonl`"
- Problem: The multi-session path globs `~/.claude-personal/projects/…` (l.88, repeated in the `--project` flag row at l.38), while the single-session path lists "`~/.claude/projects/<slug>/*.jsonl` (sorted by mtime, newest first)" (l.129). This is one fact — "where do session transcripts live" — stated in two homes with different values (§K violation), and on a machine with per-alias `CLAUDE_CONFIG_DIR` trees these are genuinely different directories: single-session and multi-session runs of the same skill would scan different session corpora, silently. The transcript-root resolution (ideally derived from the active config dir, not hard-coded) needs one canonical home cited from both phases.

### pmos-utilities-cross-F5 [Should-fix] /mac-health locates its bundled script by `find .` from the caller's cwd — dead on arrival for installed plugins
- Where: plugins/pmos-utilities/skills/mac-health/SKILL.md:64
- Quote: "bash \"$(dirname \"$(find . -path '*/mac-health/scripts/baseline_snapshot.sh' -print -quit 2>/dev/null)\")/baseline_snapshot.sh\" 2>/dev/null || echo \"Script not found — use manual commands below\""
- Problem: The skill's only script is resolved by searching *downward from the current working directory*. When the plugin is installed via the marketplace (its normal deployment), the skill files live in the plugin cache, not under the user's project cwd — so the find misses, the `|| echo` fallback fires on essentially every real run, and the bundled script is dead weight the model must recover from with the "manual commands" list. Sibling /converter shows the right pattern in the same plugin: "**Resolve paths** relative to this skill's directory (`<skill-dir>`)" (converter/SKILL.md:79). Same fix applies here; as written this also silently double-runs `ps`/`top` because the manual fallback duplicates the script's job (a §H arithmetic/script split that never actually engages).

### pmos-utilities-cross-F6 [Nit] Frozen non-interactive block misbrands every pmos-utilities skill as pmos-toolkit and carries a dangling `_shared/` cite
- Where: plugins/pmos-utilities/skills/mac-health/SKILL.md:43 (identical in all 4 skills)
- Quote: "Print to stderr at exit: `pmos-toolkit: /<skill> finished — outcome=<clean|deferred|error>, open_questions=<N>` (NFR-07)."
- Problem: The byte-frozen W14 block hard-codes the `pmos-toolkit:` prefix in its end-of-skill summary, so /mac-health, /reflect, /converter and /to-notion-doc all announce themselves as another plugin's skills. The same block also cites "`tools/audit-recommended.sh`, which sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`)" (mac-health:37) — but pmos-utilities has **no** `skills/_shared/` directory at all, so a standalone install of this plugin ships a dangling substrate cite. Both defects live in the canonical block (one home, in pmos-toolkit), which is exactly why the byte-identical lint can never surface them — the rubric enforces faithful replication of a block that is wrong outside its home plugin. Fix in the canonical file (parameterize the prefix to `pmos-skills:` or `/<plugin>:`; scope the Section-D sentence to the auditing repo) and re-paste.

### pmos-utilities-cross-F7 [Nit] W14 lint has no "prompt-free" exemption, so /converter carries ~28 lines of self-admittedly dead contract
- Where: plugins/pmos-utilities/skills/converter/SKILL.md:31-32
- Quote: "The non-interactive contract block below is inlined only to satisfy the repo-wide W14 lint; no checkpoint ever fires."
- Problem: The skill states outright that its largest single block is inert scaffolding. The exemption vocabulary covers `refused` and `delegated` but not "prompt-free" — a skill that issues zero prompts has no honest marker available, so it pays the full ~28-line context tax on every load (in a 104-line skill, roughly a quarter of the body) purely to keep a lint green. This is a gap in the lint/rubric contract, not in the skill: add a third self-documenting marker (e.g. `<!-- non-interactive: prompt-free … -->`) to `_shared/non-interactive.md` §exemptions and the lint.

### pmos-utilities-cross-F8 [Nit] /reflect's argument-hint offers two spellings of the same skill filter
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:5
- Quote: "argument-hint: \"[skill-name to filter, optional] [--last N] [--days N] [--since YYYY-MM-DD] [--project current|all] [--skill <name>]\""
- Problem: The positional `[skill-name to filter, optional]` and the typed `--skill <name>` flag are the same contract twice in one hint, and the body's flag table defines only `--skill` while Phase 2 honors "the skill name argument" (l.144) — so the positional form is a de-facto nl-sugar alias advertised as a contract surface. §I says the hint lists contract flags only; keep `--skill`, drop the positional from the hint (still parse it, marked `<!-- nl-sugar -->`).

### pmos-utilities-cross-F9 [Nit] /converter cites a repo-only design doc that doesn't ship with the plugin
- Where: plugins/pmos-utilities/skills/converter/SKILL.md:22-23
- Quote: "The cross-skill design contract lives in `docs/pmos/features/2026-06-17_converter/02_design.html`"
- Problem: `docs/pmos/features/` is repo history, not plugin payload — an installed copy of pmos-utilities has no such path, so the skill's one pointer to "the registry shape, the text/binary descriptor modes, the pure-vs-llm backend split" dangles everywhere except this development repo. The load-bearing facts an extender needs should live in a shipped home (e.g. the descriptor-shape comment already at the top of `lib/registry.js`) and be cited from there; keep the design-doc mention, if at all, as explicitly repo-only provenance.

**Pass 1 verdict:** 0 blockers / 5 should-fix / 4 nits — material findings

## Pass 1 — author response

### pmos-utilities-cross-F1 — Accepted
- Fix: CLAUDE.md (repo root) · "## Plugin charters" table, pmos-utilities row · broaden the charter from "…maintain my environment" / "standalone diagnostics, cleanup, and meta-tooling" to an honest "…run a standalone local utility" scope: environment diagnostics + meta-tooling (mac-health, reflect) PLUS local single-purpose content tools that fit no pipeline (converter — local format conversion; to-notion-doc — publish a local doc to Notion). Mirror the same broadened description into `plugins/pmos-utilities/.claude-plugin/plugin.json` `.description`, `plugins/pmos-utilities/.codex-plugin/plugin.json` `.description` (currently identical text), and both marketplace.json entries' descriptions. Explicitly name all 4 skills in the charter's Holds column so the membership test matches the inventory.
- Rationale: I choose the amend-the-charter branch over relocating converter/to-notion-doc. Both skills were deliberately placed here (converter's design contract at docs/pmos/features/2026-06-17_converter/ shipped into utilities; they are standalone launch/publish tools, not stages of the delivery pipeline). Moving them would churn canonical skill paths, marketplace entries, learnings headers (`## /converter`), and the reflect plugin-namespace matchers, for zero user benefit. The defect is that the stated membership test rejects half the plugin — the cheapest correct fix is to state the real membership rule.
- Blast radius: CLAUDE.md charter table (repo doc, no loader impact); 2 plugin.json descriptions (description-only — no version bump semantics, but per release policy the edit rides the next pmos-utilities release); 2 marketplace.json description fields. No skill files, lints, evals, or tests touched.

### pmos-utilities-cross-F2 — Accepted
- Fix: plugins/pmos-utilities/skills/converter/SKILL.md · three sites. (a) Frontmatter description (l.3): replace "JSON↔YAML, CSV↔JSON (and, as the skill grows, HTML↔Markdown, PDF↔Markdown)" with the four shipped pairs as current capability ("JSON↔YAML, CSV↔JSON, HTML→Markdown, PDF→Markdown" — direction per each descriptor's actual registration) plus trigger phrases "convert this HTML to Markdown", "extract this PDF as Markdown". (b) Body intro (l.18): replace the hard count-claim "v1 ships two bidirectional pure pairs" with a registry-derived statement — "the shipped conversions are whatever descriptor modules exist under `lib/converters/` (currently json-yaml, csv-json, html-md, pdf-md); the server prints the authoritative list at launch". (c) Phase 0 step 4 (l.93): change "the supported conversions (JSON↔YAML, CSV↔JSON)" to "the supported conversions **as printed by the server at startup** (the registry is the source of truth — never restate a hard-coded list)". Before finalizing wording, read each of the 4 descriptor modules to confirm directionality (html-md/pdf-md may be one-way) so the prose doesn't over-claim "bidirectional".
- Rationale: Classic enumerated-set drift (the exact failure mode in the repo's durable lessons). Sites (b)/(c) are fixed structurally — deriving the claim from the registry/server output — so the next added converter can't re-drift them; only the frontmatter description must stay a literal list (it's the trigger surface) and gets the four current pairs.
- Blast radius: converter/SKILL.md only. Registry/server/UI code untouched (they're already correct). No lint/eval config changes; re-run tools/lint-flags-vs-hints.sh + lint-phase-refs.sh on the skill dir after edit as standard hygiene.

### pmos-utilities-cross-F3 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · all `## Phase N` headings · renumber the multi-session spine to its own namespace and add `{#kebab-slug}` anchors to every phase heading in both spines. Concretely: "Phase 1 (multi-session prelude)" → "Phase M1: Enumerate transcript candidates {#m1-enumerate-candidates}"; "Phase 2 (multi-session dispatch)" → "Phase M2 {#m2-dispatch}"; "Phase 4 (multi-session aggregation)" → "Phase M3 {#m3-aggregate}"; "Phase 5 (multi-session emission)" → "Phase M4 {#m4-emit}". Single-session spine keeps integers 0–6 with anchors ({#locate-transcript}, {#detect-invocations}, {#peek-frontmatter}, {#analyze}, {#emit-retro}, {#capture-learnings}). Then sweep every bare cross-reference and re-point it at the slug: l.84 "Single-session path goes directly to Phase 1 below" → cite {#locate-transcript}; l.107 "seeds Phase 2 dispatch" → {#m2-dispatch}; l.111 "Skip if not in multi-session mode"; l.120 "Phase 5 emission" → {#m4-emit}; l.161 "Phase 2 multi-session dispatch" → {#m2-dispatch}; l.163 "After Phase 2 dispatch" → {#m2-dispatch}; l.212 "After Phase 4 aggregation" → {#m3-aggregate}; l.115 "Phase 4 standalone rubric" → {#analyze}; l.243 "During Phase 2 dispatch" → {#m2-dispatch}. Also fix the Phase 3 prose and Phase-5-standalone references that mention phases by bare number.
- Rationale: Direct §J violation with real ambiguity — an executor resolving "Phase 2" has two distinct procedures with the same number. The M-prefix keeps the multi-session prelude visibly a separate spine (it already reads as one) without renumbering the single-session path that external docs may reference.
- Blast radius: reflect/SKILL.md only, but the reference sweep is the risk surface — tools/lint-phase-refs.sh must pass on the skill dir afterwards (it validates `Phase <N>` and `{#slug}` refs and is exactly the backstop for this edit). Check whether any other skill or doc cites reflect's phases by number (grep `reflect/SKILL.md#` and "reflect.*Phase" repo-wide) before renumbering; feature-sdlc's `--from-reflect` path references /reflect output shape, not phase numbers, so expected clean.

### pmos-utilities-cross-F4 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · one canonical "Transcript root resolution" definition placed once (in the flag-parser/Phase 0 area, with a {#transcript-root} anchor): resolve the transcript root from the active config dir — `${CLAUDE_CONFIG_DIR:-~/.claude}/projects/` — never a hard-coded alias-specific path. Then: multi-session Phase M1 step 1 (l.88) globs `<transcript-root>/<project>/*.jsonl` citing {#transcript-root}; the `--project` flag row (l.38) drops the literal `~/.claude-personal/projects/` and says "every project under the transcript root ({#transcript-root})"; single-session Phase 1 step 2 (l.129) lists `<transcript-root>/<slug>/*.jsonl` citing the same anchor.
- Rationale: §K violation with a live behavioral bug on exactly this user's machine — per-alias `CLAUDE_CONFIG_DIR` trees mean `~/.claude/projects` and `~/.claude-personal/projects` are different corpora, so single- vs multi-session runs would silently scan different sessions. Deriving from `CLAUDE_CONFIG_DIR` (with `~/.claude` fallback) is the only value that is correct for all aliases.
- Blast radius: reflect/SKILL.md only. Folds naturally into the F3 edit (same headings/anchors touched — do them in one pass). lint-phase-refs.sh re-run covers the new anchor.

### pmos-utilities-cross-F5 — Accepted
- Fix: plugins/pmos-utilities/skills/mac-health/SKILL.md · "### 1. Capture a baseline" (l.59–76) · replace the `find .`-from-cwd resolution with the sibling /converter pattern: a step-0 "Resolve paths relative to this skill's directory (`<skill-dir>`)" instruction, then `bash <skill-dir>/scripts/baseline_snapshot.sh`. Keep the manual command list, but reframe it as the explicit fallback branch ("if the script is missing or errors, run these manually") rather than an "Or run manually" peer option, so the model doesn't double-run ps/top after a successful script run.
- Rationale: The skill's normal deployment is the marketplace plugin cache — the downward `find .` from the user's project cwd can never see the cache, so the `|| echo` fallback fires on essentially every real install and the bundled script is dead weight. The skill-dir-relative pattern is already proven in the same plugin (converter/SKILL.md:79) and is how the loader presents skills.
- Blast radius: mac-health/SKILL.md only; scripts/baseline_snapshot.sh unchanged. No lints/evals affected.

### pmos-utilities-cross-F6 — Accepted (canonical-home fix; cross-unit ownership)
- Fix: canonical file `plugins/pmos-toolkit/skills/_shared/non-interactive.md` (the frozen block's one home — NOT any pmos-utilities file directly) · two edits inside the frozen block: (a) step 8's summary prefix `pmos-toolkit: /<skill> finished` → plugin-neutral `pmos-skills: /<skill> finished`; (b) step 5's parenthetical "(`_shared/non-interactive.md`)" reworded to name the auditing repo's canonical home explicitly — "Section D of the canonical contract file (pmos-toolkit `skills/_shared/non-interactive.md` in the pmos-skills repo)" — so a standalone pmos-utilities install no longer implies a local `_shared/` that doesn't exist (reflect/SKILL.md:288 itself states pmos-utilities "is self-contained (no `_shared/` substrate)"). Then re-paste the updated block byte-identically into every user-invocable skill across all 5 plugins (including the 4 pmos-utilities skills) and re-run `plugins/pmos-toolkit/tools/lint-non-interactive-inline.sh`.
- Rationale: Both defects live in the canonical block, which the byte-identical lint faithfully replicates everywhere — the lint can never surface them; only a canonical-home edit can. The `pmos-skills:` prefix is stable, greppable, and correct for every plugin; a fully parameterized `/<plugin>:` would complicate the frozen-literal contract for no gain.
- Blast radius: LARGE and repo-wide — the canonical `_shared/non-interactive.md`, a byte-identical re-paste into every W14-inlined SKILL.md in all 5 plugins, `lint-non-interactive-inline.sh` green run, and any test/grep that asserts the exact `pmos-toolkit: /<skill> finished` NFR-07 string (grep for it repo-wide, including specs/evals, before landing). This exceeds the pmos-utilities unit — must be coordinated as a repo-level change (candidate: its own story), not smuggled into a utilities-only patch. Severity stays Nit; the fix is deliberate and batched.

### pmos-utilities-cross-F7 — Accepted (contract change, canonical-home scope)
- Fix: `plugins/pmos-toolkit/skills/_shared/non-interactive.md` · exemption vocabulary (Section with `refused` / `delegated` markers) · add a third self-documenting marker `<!-- non-interactive: prompt-free; <justification> -->` meaning: the skill issues zero `AskUserQuestion` calls by design, omits the inlined block entirely, and relies on FR-08's pre-rollout warning path for a stray `--non-interactive` flag (graceful interactive fallback, which for a prompt-free skill is behaviorally identical). Update `plugins/pmos-toolkit/tools/lint-non-interactive-inline.sh` to accept the marker in place of the block, and `tools/audit-recommended.sh` to hard-fail if a `prompt-free`-marked skill actually contains an `AskUserQuestion` call site (the marker's honesty gate). Then converter/SKILL.md swaps its ~28-line inert block + the "inlined only to satisfy the repo-wide W14 lint" apology (l.31–32) for the one-line marker.
- Rationale: The skill self-documents that a quarter of its body is dead scaffolding kept solely for a lint. The gap is in the exemption vocabulary, not the skill; the audit cross-check makes the new marker as safe as `refused`/`delegated` (a later-added prompt can't hide behind it).
- Blast radius: canonical non-interactive.md, lint-non-interactive-inline.sh, audit-recommended.sh (and its extractor selftest if any), converter/SKILL.md, plus skill-eval.md/skill-patterns.md if they restate the exemption list (grep "refused" / "delegated" across feature-sdlc reference docs — §K says the list has one home, verify before editing). Cross-unit: coordinate with the pmos-toolkit owner; candidate same story as F6.

### pmos-utilities-cross-F8 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · frontmatter argument-hint (l.5) · drop `[skill-name to filter, optional]` from the hint, keeping `--skill <name>` as the sole contract surface. In the body, keep accepting a bare positional skill name (Phase 2, l.144 already honors it) but document it as a silent alias with the `<!-- nl-sugar -->` marker adjacent to the `--skill` flag-table row, per §I.
- Rationale: §I — the hint lists contract flags only; two spellings of one filter in the hint is exactly the redundancy the policy exists to remove, and the positional form is already a de-facto nl-sugar alias (only `--skill` is defined in the flag table).
- Blast radius: reflect/SKILL.md frontmatter + one body marker. tools/lint-flags-vs-hints.sh re-run (the nl-sugar marker is what keeps the body-defined positional handling green).

### pmos-utilities-cross-F9 — Accepted
- Fix: plugins/pmos-utilities/skills/converter/SKILL.md · body intro (l.21–23) · re-point the load-bearing extension facts at the shipped home: "the registry shape, text/binary descriptor modes, and pure-vs-llm backend split are documented in the header comment of `<skill-dir>/lib/registry.js` (the descriptor contract every `lib/converters/*.js` module satisfies)". Verify/extend that header comment so it actually carries those three facts (it already documents discover() and the descriptor shape; add the text/binary-mode and pure-vs-llm notes if missing). Keep the `docs/pmos/features/2026-06-17_converter/02_design.html` mention as a trailing parenthetical explicitly labeled "design provenance — exists only in the pmos-skills development repo, not in installed copies".
- Rationale: docs/pmos/features/ is repo history, not plugin payload — an installed plugin's only pointer to its own extension contract dangles. The registry header is the natural shipped home (§K: one canonical home, cited from the skill).
- Blast radius: converter/SKILL.md + possibly a comment-only extension to lib/registry.js header (no code change). No lints/evals/tests affected.

## Pass 2 — reviewer findings

### pmos-utilities-cross-F10 [Should-fix] F3's "Phase M1–M4" scheme fails the gated j-phases-integer rubric check it is meant to satisfy
- Where: plugins/pmos-toolkit/skills/feature-sdlc/reference/skill-eval.md:241 (vs proposal F3, "multi-session spine renumbered to its own namespace with anchors — Phase M1 {#m1-enumerate-candidates}")
- Quote: "Top-level phases are integers only — no fractional (7.5-style) or lettered (0c-style) top-level phases; sub-structure inside a phase is \"step N\" prose, never a pseudo-phase label; pass iff every top-level phase label is an integer."
- Problem: F3 fixes the duplicate-integer §J violation by introducing lettered top-level phases (`Phase M1`…`Phase M4`) — exactly the "lettered (0c-style) top-level phases" shape the gated `j-phases-integer` [J] check names as a fail. Implemented as described, /reflect trades one §J defect for another and fails skill-eval the next time it passes through the pipeline. The M-labels also make every heading a new "Phase <label>" definition the phase-ref lint must resolve, for no gain over simply dropping the `Phase` keyword: rename the multi-session spine to non-phase headings (e.g. `## Multi-session prelude: Enumerate candidates {#ms-enumerate}`), keep the single-session spine as the sole integer phase namespace, and let the slugs do the addressing — `j-phases-integer` only judges phase headings, and F3's slug-sweep still lands intact.

### pmos-utilities-cross-F11 [Should-fix] /reflect ships a dead contract flag — `--msf-auto-apply-threshold` has no consuming phase
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:41
- Quote: "| `--msf-auto-apply-threshold N` | int (0-100) | 80 | Confidence threshold for any folded MSF apply-loops invoked by /reflect (per FR-RETRO-MSF integration). |"
- Problem: Remaining gap the proposal doesn't touch (F8 covers only the positional/`--skill` duplication). The flag is hinted (SKILL.md:5) and defined in the flag table, but no phase in the body ever invokes an MSF apply-loop — "msf" appears exactly twice in the file, both in the flag surfaces, never in Phases 0–6. This is precisely the "dead flags" failure mode k-one-fact-one-home cites as a design-review root cause: the flag table passes `lint-flags-vs-hints.sh` (it counts as body handling) while promising behavior the skill cannot deliver. Either the folded-MSF integration phase was never written (then the flag should be deleted along with the FR-RETRO-MSF cite) or it lives elsewhere and the table row should point at the real consumer. Product-sense: a user tuning this threshold gets silent no-op.

### pmos-utilities-cross-F12 [Should-fix] F1's replacement charter is mechanism-phrased, breaking the purpose-question pattern that makes the table a membership test
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/_cross__pmos-utilities.md:32 (vs CLAUDE.md ## Plugin charters)
- Quote: "broaden the charter question to an honest \"help me… run a standalone local utility\" scope covering (a) environment diagnostics + meta-tooling (mac-health, reflect) and (b) local single-purpose content tools"
- Problem: Every other charter is a user-need question — "ship a feature", "learn a topic", "play a casual game", "do manager work" — and CLAUDE.md declares "The charters are the membership test, not just a description." "Run a standalone local utility" describes form, not need: by mechanism, every pmos-gamekit skill is also "a standalone local utility" ("a self-contained HTML file launched from a skill via a zero-dependency local server" — the same launch pattern converter's own body describes). A form-phrased charter turns the next placement decision back into the arbitrary call F1 set out to eliminate — any local tool "fits". The amended row needs a purpose-phrased question (e.g. "help me… get a one-off local job done — on my machine or my content — that ships nothing, teaches nothing, and plays nothing") or explicit exclusions mirroring the existing "aren't part of a feature pipeline or a learning artifact" clause, extended to games. As written, F1 is vague enough to be implemented into a catch-all.

### pmos-utilities-cross-F13 [Nit] F7's `prompt-free` fallback story is self-referential and "call site" is undefined for the honesty gate
- Where: plugins/pmos-utilities/skills/converter/SKILL.md:65 (vs proposal F7, "a stray `--non-interactive` flag is handled by the existing FR-08 pre-rollout warning path")
- Quote: "**Pre-rollout BC.** If the `--non-interactive` argument is present BUT this SKILL.md does NOT contain the `<!-- non-interactive-block:start -->` marker"
- Problem: Two implement-it-wrong hazards. (1) FR-08 is defined only *inside* the inlined block — the very block F7 removes from converter. Once removed, nothing in converter's loaded context instructs the model to emit the FR-08 warning; the rule as written is self-referential (it can only execute where the block exists, where its condition is false). Harmless for a genuinely prompt-free skill, but the proposal's claim that the warning path "handles" the stray flag is hollow — the marker's spec text should state the actual behavior (flag is inert; skill runs identically) rather than cite FR-08. (2) "audit-recommended.sh hard-fails if a `prompt-free`-marked skill actually contains an AskUserQuestion call site" leaves "call site" undefined: converter's own Platform Adaptation bullet contains the literal string `AskUserQuestion` (SKILL.md:29, a non-canonical wording of the degradation bullet), so a naive grep implementation hard-fails the first marker user on itself. The spec must bind the gate to the existing shared call-site extractor (which already skips never-a-prompt line shapes), not to raw token presence.

### pmos-utilities-cross-F14 [Nit] /reflect's multi-session candidate rows compute a field nothing consumes, at full-corpus scan cost
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:88
- Quote: "Build candidate rows: `(date, size, skill-invocation-count, project-slug)`. Sort by date descending."
- Problem: Not covered by F3/F4 (which touch the same lines for anchors/roots only). `skill-invocation-count` requires opening and scanning every candidate transcript's content *before* the D18 cap trims the set — with `--project all` that can be hundreds of jsonl files — yet no downstream step reads it: the selectors (step 2) filter on date only, the cap (step 4) counts rows, and Phase 2 dispatch takes the trimmed list. A dead tuple field that mandates the exact up-front corpus scan the 20-transcript cap exists to bound. Drop it from the row shape (or make it lazy, computed only for the trimmed set if it's ever surfaced to the user).

**Pass 2 verdict:** 0 blockers / 3 should-fix / 2 nits — material findings

## Pass 2 — author response

### pmos-utilities-cross-F10 — Accepted
- Fix: amends F3's design (plugins/pmos-utilities/skills/reflect/SKILL.md · multi-session headings). Drop the "Phase M1–M4" scheme entirely. The multi-session spine loses the `Phase` keyword: headings become non-phase sections — "## Multi-session prelude: enumerate candidates + D18 cap {#ms-enumerate}", "## Multi-session dispatch {#ms-dispatch}", "## Multi-session aggregation {#ms-aggregate}", "## Multi-session emission {#ms-emit}". The single-session spine remains the sole integer `Phase 0–6` namespace, each heading gaining its `{#slug}` anchor per the original F3 plan. The F3 cross-reference sweep lands unchanged except the targets: l.107/161/163/243 → {#ms-dispatch}, l.120 → {#ms-emit}, l.212 → {#ms-aggregate}, and any prose saying "Phase 2 (multi-session)" becomes "the multi-session dispatch ({#ms-dispatch})".
- Rationale: The reviewer is right — skill-eval.md:241's gated `j-phases-integer` check names "lettered (0c-style) top-level phases" as a fail, and `Phase M1` is exactly that shape; my F3 design traded one §J defect for a rubric-failing one. Slugs are the address anyway (§J: "the number is ordering sugar; the slug is the address"); removing the `Phase` keyword from the secondary spine exits `j-phases-integer`'s jurisdiction (it judges phase headings only) while keeping every heading addressable.
- Blast radius: same as F3 — reflect/SKILL.md only; tools/lint-phase-refs.sh re-run is the backstop (fewer `Phase <label>` definitions to resolve than the M-scheme, strictly simpler). Supersedes F3's heading naming; F3's anchor+sweep mechanics stand.

### pmos-utilities-cross-F11 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · frontmatter argument-hint (l.5) + flag table (l.41) · delete `--msf-auto-apply-threshold N` from both surfaces, including the flag-table row's "per FR-RETRO-MSF integration" cite. No replacement phase is written: /reflect is recommendations-only by design (it emits paste-back retro blocks; it never applies edits), so a folded-MSF apply-loop has no place in its contract — the flag was aspirational scaffolding that never got a consumer.
- Rationale: Verified — "msf" appears in the file only at l.5 and l.41 (l.177's "An MSF ..." is an unrelated article-stripping example); no phase in the body invokes any apply-loop, so a user tuning the threshold gets a silent no-op. The flag name is machine-coupled where it is real (spec/requirements/wireframes → _shared/folded-phase.md, all in pmos-toolkit); deleting an inert copy from reflect renames nothing and breaks no consumer — reflect was never wired into that mechanism. Delete beats point-at-real-consumer because reflect has no folded phase for the row to describe.
- Blast radius: reflect/SKILL.md only (hint + one table row). tools/lint-flags-vs-hints.sh re-run (removing hint+body together keeps it green). Grep repo-wide for "FR-RETRO-MSF" before landing to confirm no doc promises the integration (only hit today is this flag row).

### pmos-utilities-cross-F12 — Accepted
- Fix: amends F1's design (repo-root CLAUDE.md · pmos-utilities charter row). Replace my form-phrased "help me… run a standalone local utility" with a purpose-phrased question plus explicit exclusions mirroring the row's existing style: charter question "help me… **get a one-off job done — on my machine or my content**", Holds column "standalone utilities that ship no feature, teach no topic, and play no game: environment diagnostics and meta-tooling (mac-health, reflect — a cross-plugin session retrospective) plus local content tools (converter — file-format conversion; to-notion-doc — publish a local doc to Notion)". Manifest/marketplace descriptions mirror the same purpose+exclusions wording. All other F1 mechanics (4 files, all 4 skills named) unchanged.
- Rationale: The reviewer's mechanism test is decisive — every gamekit skill is also "a standalone local utility" by form (same zero-dependency local-server launch pattern converter itself uses), so my wording would have re-created the arbitrary-placement problem F1 set out to fix. Purpose-phrasing plus the ships-nothing/teaches-nothing/plays-nothing exclusion triple keeps the table a genuine membership test consistent with its sibling rows.
- Blast radius: identical to F1 (CLAUDE.md row, 2 plugin.json descriptions, 2 marketplace.json descriptions). No skill bodies, lints, evals, or tests.

### pmos-utilities-cross-F13 — Accepted
- Fix: amends F7's design (canonical plugins/pmos-toolkit/skills/_shared/non-interactive.md + the two pmos-toolkit tool scripts). Two spec corrections: (1) the `prompt-free` marker's spec text states the actual behavior directly — "a stray `--non-interactive` flag is inert: the skill issues no prompts, so it runs identically with or without it; no warning is emitted" — and drops the FR-08 cite entirely (FR-08 lives inside the block the marker replaces; citing it from a skill that omits the block is self-referential dead text, as the reviewer shows via converter:65's marker-conditional wording). (2) The honesty gate in audit-recommended.sh is specified to bind to the existing shared call-site extractor (the one that already skips the two provably-never-a-prompt line shapes — the canonical platform-adaptation degradation bullet and negative prose), NOT raw `AskUserQuestion` token grep: hard-fail iff the extractor finds ≥1 real call site in a `prompt-free`-marked skill. Acceptance test named in the story: converter itself (whose l.29 Platform Adaptation bullet contains the literal token) must pass the gate.
- Rationale: Both hazards are real implement-it-wrong traps: the FR-08 story could only execute where its condition is false, and a naive grep gate would hard-fail the first marker user on its own degradation bullet — the same token-vs-call-site distinction the repo's zero-match-gate lesson already codifies.
- Blast radius: same files as F7 (canonical non-interactive.md, lint-non-interactive-inline.sh, audit-recommended.sh + extractor selftest, converter/SKILL.md); the extractor-binding requirement adds converter-passes-the-gate as an explicit test. Still cross-unit, still the F6/F7 companion story.

### pmos-utilities-cross-F14 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · multi-session prelude step 1 (l.88) · row shape becomes `(date, size, project-slug)` — drop `skill-invocation-count` from enumeration. If a per-transcript invocation count is ever wanted for user-facing display, compute it lazily for the trimmed post-cap set only (≤20 files) — but nothing consumes it today (verified: step 2 filters on date, step 4 counts rows, Phase 2 dispatch takes the trimmed list and does its own in-transcript detection), so the default fix is plain deletion.
- Rationale: The dead field mandates opening and content-scanning every candidate jsonl before the D18 cap trims the set — with `--project all` that is exactly the unbounded up-front corpus scan the 20-transcript cap and the NFR-02 wall-clock budget exist to prevent. Enumeration should touch only filesystem metadata (mtime/size/path), all obtainable without opening files.
- Blast radius: reflect/SKILL.md one line; folds into the same F3/F4/F10 edit pass over the multi-session section. No lints/evals/tests affected.
