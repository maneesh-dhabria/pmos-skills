## pmos-managerkit _shared — Pass 1 — reviewer findings

Unit: `plugins/pmos-managerkit/skills/_shared/` (sole subtree: `interview-guidelines/` — 2 skeletons, 1 effectiveness rubric, 1 resolution protocol, 1 level ladder, 8 archetype reference+scorecard pairs, 1 additional/README). Consumption map established by grep: skeletons → interview-guide SKILL.md + output-shapes.md, interview-feedback SKILL.md + questionnaire.mjs + fill-scorecard.mjs + run-tests.sh; archetype corpus → interview-guide (authoring ground) + interview-feedback run-tests.sh (weight gates); interviewer-effectiveness.html + reference-resolution.md → interview-feedback only; level-ladder.md → interview-guide. No dead files. Cross-plugin drift: no same-named files exist in any other plugin's `_shared/` (interview-guidelines is unique to managerkit), so zero drifted duplicates. All 9 scorecards' weights sum to 100 and are gated by run-tests.sh; the effectiveness rubric's provenance block is honest about secondhand coefficients — genuinely good craft. Findings below are the gaps.

### pmos-managerkit-shared-F1 [Should-fix] Bundled archetype corpus is unreachable from /interview-feedback's own resolution chain
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md:10
- Quote: "**Round-level guideline** — `<role-dir>/guidelines/<round>/{interviewer-reference.html, scorecard.html}`. This is the default for a configured role."
- Problem: The precedence chain (`--reference` → role-dir round guideline → role-level default) never falls back to the bundled archetype pair, even though every round's `role.json` carries an `archetype` naming one of the 8 bundled types. And interview-feedback's setup phase (SKILL.md:224) says "attach the provided interviewer-reference + scorecard or generate them by instantiating `../_shared/interview-guidelines/reference-skeleton.html`" — i.e. a product-sense round scaffolded without operator-provided files gets the BLANK two-example-dimension skeleton while a fully researched product-sense reference+scorecard pair sits in the same substrate. The substrate's largest asset (~16 files, the researched corpus) is consumed only via the sibling /interview-guide; the scoring skill that ships the corpus leaves its value on the floor unless the user happens to run the other skill first. Either setup should copy the archetype pair, or resolution should fall back corpus-last — today neither is specified.

### pmos-managerkit-shared-F2 [Should-fix] reference-resolution.md's three SKILL.md links are broken relative paths
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md:3
- Quote: "Cited from SKILL.md Phase [Resolve](../SKILL.md#resolve) and Phase [Coach](../SKILL.md#coach)."
- Problem: From this file's home, `../SKILL.md` resolves to `skills/_shared/SKILL.md`, which does not exist — the intended target is `../../interview-feedback/SKILL.md`. Same broken shape at line 15 (`[Score](../SKILL.md#score)`) and line 25 (`[Transcribe](../SKILL.md#transcribe)`). The links were evidently written when this doc lived inside interview-feedback and were not re-pointed on the move to `_shared/`. Rubric gap worth noting: `tools/lint-phase-refs.sh` validates `Phase <N>`, `{#slug}`, and `<skill>/SKILL.md#anchor` forms — a bare `../SKILL.md#anchor` matches none of them, so this class of broken substrate link is lint-invisible.

### pmos-managerkit-shared-F3 [Should-fix] reference-skeleton.html omits the `.calib` element its own authoring contract requires
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-skeleton.html:11
- Quote: "THE single DOM contract for a round's interviewer guideline. Every bundled round reference"
- Problem: The consumer contract demands a calibration line per area — interview-guide/SKILL.md:176 requires "a `.calib` good/average/poor line + the common interviewer mistake" and output-shapes.md:32 checklists "a `.calib` line: good / average / poor" — and all 8 bundled archetype references carry `.calib` markup + CSS. The skeleton has zero occurrences of `calib` (no element, no style rule). So "THE single DOM contract" is missing a required member of the contract: every skill-authored reference instantiated from the skeleton must hand-invent the `.calib` markup and styling, which is exactly the drift a canonical skeleton exists to prevent.

### pmos-managerkit-shared-F4 [Should-fix] `_shared/non-interactive.md` is cited by all three skills but absent from this plugin's substrate
- Where: plugins/pmos-managerkit/skills/interview-feedback/SKILL.md:78
- Quote: "sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`)"
- Problem: The frozen non-interactive block in interview-feedback (SKILL.md:78), interview-guide (SKILL.md:81), and one-on-one (SKILL.md:92) names `_shared/non-interactive.md` as "this file" — the canonical home of the contract it inlines — yet `plugins/pmos-managerkit/skills/_shared/` contains only `interview-guidelines/`; the file exists in pmos-toolkit's and pmos-learnkit's `_shared/` but not here. Within this plugin the cite is dangling: a reader (or the sourcing described) cannot resolve it. This is the documented intersection-only-sync bootstrap gap — learnkit got its byte-identical copy placed manually; managerkit never did. Fix is one manual byte-identical copy from the canonical owner, after which sync-shared.sh keeps it aligned.

### pmos-managerkit-shared-F5 [Should-fix] Work-history exemplar breaks the area↔dim 1:1 contract its consumer states as MUST, silently zeroing questionnaire probes
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/interviewer-reference.html:7
- Quote: "Area ids here are the METHOD phases; competency scoring lives in the scorecard + level ladder."
- Problem: The reference carries 6 method-phase area ids (career-arc-scan, per-role-deep-dive, …) while the paired scorecard carries 12 Reforge/Mehta competency `data-dim` ids — the only archetype where the sets are disjoint. interview-guide/SKILL.md:176 states "Area ids MUST match the scoring-sheet `data-dim` ids 1:1 … (and so `/interview-feedback`'s questionnaire can lift probes per dimension)" with no carve-out for work-history (the Archetypes section documents the round's two other distinctions but not this one). The functional cost is real: `questionnaire.mjs` keys probes by area id and emits one section per `data-dim` (`probes[d.id] || []`), so a tier-3 recall questionnaire for a work-history round ships all 12 dimensions with ZERO suggested probes — the exemplar's deviation degrades a consumer silently. Either the MUST needs a documented method-archetype exception plus a questionnaire fallback (e.g. lift all probes when ids are disjoint), or the exemplar needs per-dim probe mapping.

### pmos-managerkit-shared-F6 [Should-fix] additional/README.md instructs writing role-specific inputs INTO the installed shared corpus
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/case-study/additional/README.md:3
- Quote: "Drop the role's actual case prompt or take-home brief into this folder per candidate role —"
- Problem: "this folder" is inside the plugin-installed shared corpus, which is a read-only template shared across every role — but the instruction says to drop per-role artifacts there. reference-resolution.md:29 gives the correct home: "Copy them under `guidelines/<round>/additional/`" in the ROLE directory (under the gitignore-guarded storage root). A user following the README as written mutates the plugin install and shares one role's case prompt with every role. The README belongs in the scaffolded role dir (emitted by setup), not the corpus — or must say "in your role directory's guidelines/<round>/additional/". Also asymmetric: only case-study has an `additional/` scaffold although the README itself names take-home briefs (a technical/case-presentation concern) and reference-resolution.md defines `additional_docs[]` for every round.

### pmos-managerkit-shared-F7 [Nit] All 19 corpus HTML files claim `pmos:skill` = interview-feedback, including files whose own headers attribute /interview-guide
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/interviewer-reference.html:3
- Quote: "Work-history round interviewer reference — /interview-guide (pmos-managerkit), design §16.1 + §16.7."
- Problem: The same file's line 15 carries `<meta name="pmos:skill" content="interview-feedback">` — every one of the 19 corpus HTML files does, a leftover from when the corpus lived under interview-feedback. `pmos:skill` is the comments-overlay's routing key to the originating skill's apply-edit shim; skeletons instantiated by /interview-guide propagate the wrong attribution into guide-authored artifacts. Harm is latent today (managerkit ships no apply-edit shims), but the metadata is wrong-by-inheritance and will misroute the moment shims land.

### pmos-managerkit-shared-F8 [Nit] Substrate files cite consumer scripts by bare `scripts/…` paths that resolve to nothing — and to different skills
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/scorecard-skeleton.html:49
- Quote: "enforced by scripts/validate-scorecard-anchors.mjs, never by the model."
- Problem: From the skeleton's home there is no `scripts/` dir; the named validator actually lives in `interview-guide/scripts/` — while the same skeleton's header (line 7) points at "the filler (scripts/fill-scorecard.mjs)", which lives in `interview-feedback/scripts/`. level-ladder.md:7 repeats the unanchored validator cite, and reference-resolution.md:15 the unanchored filler cite. A shared file citing two different skills' internals with identical relative notation forces the reader to grep for every script's true home; each cite should name the owning skill (e.g. `interview-guide/scripts/validate-scorecard-anchors.mjs`).

**Pass 1 verdict:** 0 blockers / 6 should-fix / 2 nits — material findings

## pmos-managerkit _shared — Pass 1 — author response

All 8 findings spot-checked against the files: every quote exists verbatim at the cited file:line, and the two functional claims were re-verified (F5: work-history's 6 `data-area` ids and 12 scorecard `data-dim` ids are fully disjoint, and `questionnaire.mjs:62,100` keys `probes[d.id]` so every dimension section gets zero probes; F8: the unanchored validator cite is at `guidelines/work-history/level-ladder.md:7`). 8 accepted, 0 rejected.

### pmos-managerkit-shared-F1 — Accepted
- Fix: two coordinated edits. (1) `interview-feedback/SKILL.md` Phase Setup {#setup} — before: "attach the provided interviewer-reference + scorecard or generate them by instantiating `../_shared/interview-guidelines/reference-skeleton.html` + `scorecard-skeleton.html`"; after: for a round whose archetype is one of the 8 bundled types and no operator files are provided, COPY the bundled pair from `../_shared/interview-guidelines/guidelines/<archetype>/` into the role dir (recording provenance in `role.json`); the blank skeletons become the path for `archetype: custom` only. (2) `_shared/interview-guidelines/reference-resolution.md` §Reference resolution — append precedence step 4: "**Bundled archetype pair** — `_shared/interview-guidelines/guidelines/<archetype>/` for the round's `role.json` archetype. Record that the bundled corpus was used." so a role scaffolded outside setup (or with deleted guideline files) still resolves corpus-last instead of erroring or getting a blank skeleton.
- Rationale: the researched corpus is the substrate's largest asset and today reaches /interview-feedback users only if they happen to run /interview-guide first; setup-copies + corpus-last fallback closes both entry points without changing behavior for operator-provided or guide-authored files (they still win at steps 1–3).
- Blast radius: interview-feedback SKILL.md (setup phase prose + role.json section provenance note), reference-resolution.md; work-history rounds also copy `level-ladder.md` alongside the pair (its scorecard depends on it). Re-run `tools/lint-phase-refs.sh` + skill-eval on interview-feedback. No script changes — resolution is model-performed, not scripted.

### pmos-managerkit-shared-F2 — Accepted
- Fix: `_shared/interview-guidelines/reference-resolution.md` lines 3, 15, 25 — before: `(../SKILL.md#resolve)`, `(../SKILL.md#coach)`, `(../SKILL.md#score)`, `(../SKILL.md#transcribe)`; after: re-point all four link targets to `../../interview-feedback/SKILL.md#<same-anchor>` (the file's true home relative to `_shared/interview-guidelines/`).
- Rationale: `../SKILL.md` resolves to a nonexistent `skills/_shared/SKILL.md` — stale paths from before the doc moved into `_shared/`. Anchors themselves (`#resolve`, `#coach`, `#score`, `#transcribe`) are valid headings in interview-feedback/SKILL.md, so only the path segment changes.
- Blast radius: docs-only. Note for the lint backlog (out of this unit's scope): `tools/lint-phase-refs.sh` does not validate bare `../SKILL.md#anchor` links, so this class stays lint-invisible — candidate follow-up.

### pmos-managerkit-shared-F3 — Accepted
- Fix: `_shared/interview-guidelines/reference-skeleton.html` — add to each example area section a `.calib` element (one-line good / average / poor calibration + common interviewer mistake, matching the markup shape the 8 bundled references already use) plus the corresponding CSS rule in the inline `<style>`; extend the header doc-comment's anchor/structure list to name `.calib` as a required per-area member.
- Rationale: interview-guide/SKILL.md:176 and output-shapes.md both make `.calib` a required per-area element, and all 8 bundled references carry it — the skeleton claiming to be "THE single DOM contract" while omitting it forces every skeleton-instantiated reference to hand-invent the markup, the exact drift the skeleton exists to prevent.
- Blast radius: skeleton only; no script parses `.calib` (verified: questionnaire.mjs lifts `data-probes`, validate-scorecard-anchors.mjs targets the scorecard). Bundled references already conform, so no corpus churn. interview-feedback run-tests.sh skeleton assertions may need one new expectation line if they enumerate skeleton structure.

### pmos-managerkit-shared-F4 — Accepted
- Fix: place a byte-identical copy of pmos-toolkit's `skills/_shared/non-interactive.md` at `plugins/pmos-managerkit/skills/_shared/non-interactive.md` (manual one-time copy from the canonical owner, per the documented intersection-only-sync bootstrap procedure in repo CLAUDE.md).
- Rationale: all three managerkit skills' frozen blocks cite `_shared/non-interactive.md` as "this file" (the canonical home + Section D extractor they claim to source); within this plugin the cite is dangling. After the manual bootstrap, `scripts/sync-shared.sh` keeps it in the intersection and aligned.
- Blast radius: pmos-toolkit's `tools/lint-non-interactive-inline.sh` (must stay green — the inline blocks are already byte-identical to the canonical file, so adding the file only makes the local cite resolvable); sync-shared.sh intersection grows by one file for managerkit. No skill-body edits.

### pmos-managerkit-shared-F5 — Accepted
- Fix: three coordinated edits, keeping the method-phase design (it is intentional Topgrading structure, per the exemplar's own header). (1) `interview-guide/SKILL.md:176` — before: unconditional "Area ids MUST match the scoring-sheet `data-dim` ids 1:1"; after: add an explicit work-history carve-out sentence pointing at the §work-history-weights section ("except archetype `work-history`, whose reference areas are METHOD phases — see {#work-history-weights}"), and fold the same one-line exception into the Archetypes section's work-history entry. (2) `interview-feedback/scripts/questionnaire.mjs` — when the reference's area-id set and the scorecard's `data-dim` set have an empty intersection, fall back to emitting all lifted probes in a single "method probes" section (grouped by area) instead of 12 probe-less dimension sections; add one selftest case for the disjoint-ids input. (3) `_shared/interview-guidelines/guidelines/work-history/interviewer-reference.html` header — extend the existing "Area ids here are the METHOD phases" comment to state that this is the sanctioned exception to the 1:1 rule and that questionnaire emission uses the disjoint-ids fallback.
- Rationale: the exemplar's deviation is a deliberate design (method vs. competency), but it is undocumented against a MUST and silently zeroes a consumer — a tier-3 work-history questionnaire ships 12 dimensions with no probes. Documenting the exception plus a deterministic fallback repairs the consumer without rewriting a researched exemplar.
- Blast radius: interview-guide SKILL.md (2 sections), questionnaire.mjs + interview-feedback run-tests.sh selftest count (count-claim greps per the coherence-gate lesson), work-history reference header comment. Skill-eval + 4 lints re-run on both skills. Note the skeleton header says "SHOULD map 1:1" while the guide says MUST — the carve-out edit reconciles the two (one fact, one home: the guide owns the rule; the skeleton comment stays SHOULD and cites it).

### pmos-managerkit-shared-F6 — Accepted
- Fix: `_shared/interview-guidelines/guidelines/case-study/additional/README.md` — before: "Drop the role's actual case prompt or take-home brief into this folder per candidate role"; after: rewrite to state the drop target is the ROLE directory under the storage root — `<role-dir>/guidelines/<round>/additional/` (cite reference-resolution.md §additional_docs) — and that THIS bundled folder is the read-only template setup copies from; keep the role.json `additional_docs[]` registration instruction. Secondary: `interview-feedback/SKILL.md` Phase Setup — scaffold an `additional/` dir (with the corrected README) for every round, not only case-study, matching the SKILL.md:261 layout line `guidelines/<round>/{ …, additional/ }` and the round-agnostic `additional_docs[]` contract.
- Rationale: as written the README directs users to mutate the plugin install and cross-contaminate roles; reference-resolution.md already names the correct home, so this is a contradiction between substrate files with a destructive reading.
- Blast radius: README, interview-feedback SKILL.md setup phase. No scripts or tests reference the README contents.

### pmos-managerkit-shared-F7 — Accepted
- Fix: correct `<meta name="pmos:skill">` per file by owning skill: the 17 archetype corpus files whose header comments attribute /interview-guide (8 reference+scorecard pairs + work-history level-ladder is .md so N/A — the HTML pairs) get `content="interview-guide"`; the two skeletons and `interviewer-effectiveness.html`, owned by /interview-feedback per their headers, keep `content="interview-feedback"`. One pass over all 19 corpus HTML files, aligning meta with each file's own header attribution.
- Rationale: `pmos:skill` is the comments-overlay routing key to the originating skill's apply-edit shim; wrong-by-inheritance metadata will misroute the moment managerkit ships shims, and skeleton instantiation propagates the wrong attribution into guide-authored artifacts today.
- Blast radius: metadata-only edits across 19 HTML files; no script or test reads `pmos:skill` in this plugin currently (latent consumer is the comments overlay).

### pmos-managerkit-shared-F8 — Accepted
- Fix: qualify every consumer-script cite in substrate files with the owning skill: `scorecard-skeleton.html:7` "the filler (scripts/fill-scorecard.mjs)" → "the filler (interview-feedback/scripts/fill-scorecard.mjs)"; `scorecard-skeleton.html:49` and `guidelines/work-history/level-ladder.md:7` "scripts/validate-scorecard-anchors.mjs" → "interview-guide/scripts/validate-scorecard-anchors.mjs"; `reference-resolution.md:15` "scripts/fill-scorecard.mjs parse" → "interview-feedback/scripts/fill-scorecard.mjs parse".
- Rationale: a shared file citing two different skills' internals with identical bare `scripts/…` notation resolves to nothing from the substrate's own directory and forces the reader to grep for each script's true home.
- Blast radius: docs/comment text only; no anchors, lints, or tests parse these cites.

**Author verdict:** 8/8 accepted (6 should-fix, 2 nits), 0 rejected, 0 invalid.

## pmos-managerkit _shared — Pass 2 — reviewer findings

Simulated the proposal (F1–F8) as implemented exactly as written, then re-audited the substrate and the consumer surfaces the proposal touches. Verified clean: F2's re-pointed anchors all exist (`{#resolve}` :99, `{#transcribe}` :119, `{#score}` :144, `{#coach}` :218 in interview-feedback/SKILL.md; `{#scoring-sheet}` :180 in interview-guide/SKILL.md); F4 is safe (toolkit and learnkit `non-interactive.md` are byte-identical, and `lint-non-interactive-inline.sh:36` pins `CANONICAL_FILE="${PLUGIN_ROOT}/skills/_shared/non-interactive.md"` under pmos-toolkit only, so a managerkit copy cannot perturb the lint); no dead substrate files remain post-proposal; no same-named cross-plugin duplicate exists for anything under `interview-guidelines/`. New findings below.

### pmos-managerkit-shared-F9 [Should-fix] F3's premise is false — the `.calib` contract is broken across the corpus itself, not just the skeleton
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-skeleton.html:5
- Quote: "THE single DOM contract for a round's interviewer guideline. Every bundled round reference"
- Problem: The accepted F3 rests on the claim that "all 8 bundled references carry `.calib` markup + CSS" and directs the implementer to match "the bundled references' markup shape". Neither is true. Per-area `class="calib"` element counts vs `data-area` counts: product-sense 5/5 and work-history 6/6 conform; analytical 2/5, behavioral 2/5, case-study 2/5, technical 1/5, case-presentation 0/5, recruiter-screen 0/5. Worse, there is no single "bundled markup shape" to copy: analytical uses one `<section class="calib">` with a `<dl>` (line 179–182), case-presentation uses a differently-named `<section class="calibration">` (line 203), and recruiter-screen has no calib DOM at all — only inline prose ("the panel's job. <strong>Common mistakes / calibration:</strong> over-weighting polish and", line 48). So implementing F3 exactly as written (a) forces an arbitrary pick among three incompatible shapes, and (b) still leaves 6 of 8 bundled exemplars violating both interview-guide/SKILL.md:176's per-area `.calib` requirement and this skeleton's own "Every bundled round reference … instantiate THIS structure" claim. The fix must either normalize the corpus to one per-area `.calib` shape or downgrade the guide's per-area requirement to match reality — F3 alone papers over corpus-internal drift the skeleton exists to prevent.

### pmos-managerkit-shared-F10 [Should-fix] F1 + F7 combined misattribute /interview-feedback's primary output to /interview-guide
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/scorecard.html:24
- Quote: "<meta name=\"pmos:skill\" content=\"interview-feedback\">"
- Problem: Two accepted fixes compose into a new defect neither proposal item considers. F7 flips the 16 archetype-pair metas to `content="interview-guide"`; F1 makes interview-feedback's Setup COPY those same bundled pairs into the role dir as the round's working reference + scorecard. fill-scorecard.mjs then fills the copied scorecard with explicit "byte-for-byte preservation of the <head> region (untouched)" (fill-scorecard.mjs:553) — so every `filled-scorecard.html` produced through the new F1 path ships `pmos:skill=interview-guide`, i.e. /interview-feedback's flagship candidate-scoring artifact is attributed to the wrong skill. This is exactly the misrouting F7's own rationale warns about ("the comments-overlay routing key to the originating skill's apply-edit shim"), now injected by the fix pair itself. F1's copy step needs to rewrite the meta (and title placeholders) on copy, or F7 must exclude the two files-as-templates role the corpus now plays post-F1.

### pmos-managerkit-shared-F11 [Should-fix] F1's bundled-pair copy silently bakes senior-pm weights into every work-history round
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/level-ladder.md:19
- Quote: "when neither resolves, default to **`senior-pm`** (the shipped default row, also used by the checked-in"
- Problem: F1 says work-history rounds "also copy `level-ladder.md`" alongside the bundled pair, but the bundled `scorecard.html` hard-codes the senior-pm weight column, and the entire seniority-resolution machinery (`--seniority` aliases, title inference, `--level-rubric` override, the validate-scorecard-anchors sum-gate) is wired ONLY into /interview-guide (level-ladder.md:94–104 routes the override to "/interview-guide SKILL.md § Scoring Sheet"). /interview-feedback's Setup has no seniority step, and the proposal adds none — so F1 as written scaffolds an APM or Director role with senior-pm weights (e.g. team-leadership 7 instead of 3 or 16), silently skewing the weighted hire signal on a real hiring decision. Copying level-ladder.md into the role dir doesn't help: nothing in interview-feedback reads it. F1 needs either a seniority resolution step in Setup for work-history rounds (reusing the ladder's alias table + title inference) or an explicit provenance warning that the copied sheet carries the default senior-pm row and should be re-weighted via /interview-guide.

### pmos-managerkit-shared-F12 [Nit] Skeleton contradicts itself (and the hard gate) on weight-sum: "should" at the contract line, "must" at the instantiation comment
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/scorecard-skeleton.html:12
- Quote: "integer weight; the weights across dims should sum to 100."
- Problem: The same file's body comment (line 124) says "data-weight values must sum to 100", interview-guide/SKILL.md:182 says weights "**must sum to 100**", and the sum is deterministically enforced by validate-scorecard-anchors.mjs (a §H hard gate per level-ladder.md:99–100). "should" at the machine-anchor contract line understates a refusal-backed invariant. F5's reconciliation note fixes the parallel SHOULD/MUST tension for area-id mapping but not this one. One-word fix: "must".

### pmos-managerkit-shared-F13 [Nit] F5's disjoint-only fallback still leaves partially-drifted pairs emitting silent probe-less sections
- Where: plugins/pmos-managerkit/skills/interview-feedback/scripts/questionnaire.mjs:62
- Quote: "const ps = (probes[d.id] || []).map(p => `        <li class=\"probe\">${p}</li>`).join('\\n');"
- Problem: F5's trigger is "when the reference area-id set and the scorecard `data-dim` set have an EMPTY intersection". A hand-edited or `custom`-archetype pair whose ids drift partially (say 3 of 5 match) never trips it, so the unmatched dims still render zero-probe sections with no signal — the original silent-degradation mode survives in reduced form. Cheap closure: alongside the disjoint fallback, emit one stderr warning per dim id with no probe match (deterministic, scriptable, no behavior change on conforming pairs), so drift is visible instead of silent.

### pmos-managerkit-shared-F14 [Nit] F6's corrected README self-describes as "this bundled folder" yet F6 also copies it into every role dir
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/case-study/additional/README.md:3
- Quote: "Drop the role's actual case prompt or take-home brief into this folder per candidate role —"
- Problem: F6 item 1 rewrites this README to say "this bundled folder is the read-only template setup copies from", and F6 item 2 has Setup scaffold `additional/` "(with the corrected README)" into every round of every role dir. The copied instance then falsely describes itself as the read-only bundled template while sitting in the exact writable role dir it tells the reader to go find. One README cannot serve both homes; the rewrite needs two texts (bundled: "do not write here; setup copies this scaffold" / role-dir: "drop the round's docs here and register them in `additional_docs[]`") or a location-neutral wording. As specified, the fix re-creates a location-confusion bug one directory over.

**Pass 2 verdict:** 0 blockers / 3 should-fix / 3 nits — material findings

## pmos-managerkit _shared — Pass 2 — author response

All 6 findings spot-checked: every quote verbatim at the cited file:line. Functional claims re-verified — F9's per-archetype counts confirmed by grep (`class="calib"` per-area coverage: product-sense 5/5-ish and work-history conform; analytical 2, behavioral 2, case-study 2, technical 1, case-presentation 0 (uses `class="calibration"`), recruiter-screen 0 (inline prose only)); F10's head-preservation confirmed at fill-scorecard.mjs:553 ("byte-for-byte preservation of the <head> region"); F11's senior-pm default + interview-guide-only override routing confirmed at level-ladder.md:19 and :94–104. 6 accepted, 0 rejected. Findings F9–F11 are composition defects in my own Pass-1 proposal — accepted as amendments to F3, F1+F7, and F1 respectively.

### pmos-managerkit-shared-F9 — Accepted
- Fix: supersedes F3's spec. (1) Declare ONE canonical per-area calib shape — the product-sense/work-history form: a per-area `.calib` element carrying a one-line good/average/poor calibration + common interviewer mistake — and add it (element + CSS + header structure-list entry) to `reference-skeleton.html` as F3 intended. (2) Normalize the 6 non-conforming bundled references to that shape: analytical/behavioral/case-study/technical gain per-area `.calib` lines for their uncovered areas (redistributing the existing section-level `<dl>` calibration content per area, authoring the missing lines from each reference's own signals prose); case-presentation's `<section class="calibration">` is converted to per-area `.calib`; recruiter-screen's inline "Common mistakes / calibration:" prose is lifted into per-area `.calib` elements. (3) The skeleton header's "Every bundled round reference … instantiate THIS structure" claim becomes true rather than being softened.
- Rationale: my F3 rationale ("all 8 bundled references carry `.calib`") was factually wrong — the reviewer's counts are correct. interview-guide/SKILL.md:176's per-area requirement is the right bar (2 exemplars already meet it); downgrading the guide would codify corpus drift instead of fixing it. Normalization is authoring work grounded in calibration content the references mostly already contain.
- Blast radius: reference-skeleton.html + 6 archetype interviewer-reference.html files (content edits inside `<main>`, no anchor changes — `data-area`/`data-probes`/`data-signals` untouched, so questionnaire.mjs unaffected); interview-guide output-shapes.md checklist already matches; re-run interview-feedback run-tests.sh + skill-eval on both skills.

### pmos-managerkit-shared-F10 — Accepted
- Fix: amend F1's copy step: when Setup copies a bundled archetype pair into the role dir, it REWRITES `<meta name="pmos:skill">` to `interview-feedback` (the working copy is an interview-feedback artifact) and updates the header comment to a provenance line ("copied from bundled `<archetype>` corpus by /interview-feedback setup, <date>"). F7's meta flip to `interview-guide` applies only to the bundled corpus files in `_shared/` (their at-rest attribution); role-dir copies are re-stamped at copy time.
- Rationale: fill-scorecard.mjs preserves the `<head>` byte-for-byte, so without a copy-time rewrite every filled scorecard produced through the F1 path would ship `pmos:skill=interview-guide` — the exact misrouting F7 exists to prevent. Copy-time re-stamping keeps both fixes coherent: corpus files attribute their author, working copies attribute their operating skill.
- Blast radius: interview-feedback SKILL.md setup phase (one added sentence to F1's copy spec); no script changes (copy is model-performed).

### pmos-managerkit-shared-F11 — Accepted
- Fix: amend F1 for work-history rounds. Setup's bundled-copy step gains a seniority-resolution sub-step: resolve the candidate level via level-ladder.md's alias table + title inference (the ladder is already copied per F1). If the resolved level is `senior-pm` (or unresolvable → ladder default), proceed and record `weights: senior-pm (ladder default)` in `role.json`. If it resolves to a DIFFERENT level, swap the copied scorecard's `data-weight` column to the matching ladder row (mechanical table lookup — no model arithmetic) and gate the result with `interview-guide/scripts/validate-scorecard-anchors.mjs`'s sum-gate before accepting the copy; on gate failure, fall back to the senior-pm sheet + a stderr warning and a `role.json` provenance flag directing re-weighting via /interview-guide § Scoring Sheet. Non-interactive runs never prompt: they take the resolved-or-default row and always record provenance.
- Rationale: silently scoring an APM or Director against senior-pm weights skews a real hiring signal — the worst kind of quiet wrong. The ladder's alias/inference rules and the sum-gate already exist; Setup only needs to use them. The cross-skill script invocation is sanctioned precedent: level-ladder.md:99 already routes the override sum-gate to that validator.
- Blast radius: interview-feedback SKILL.md setup phase + role.json schema (provenance field); reuses interview-guide's validator by path (no new script); one run-tests.sh case if setup behavior is test-enumerated; re-run skill-eval + audit-recommended (any new prompt needs a Recommended option) on interview-feedback.

### pmos-managerkit-shared-F12 — Accepted
- Fix: `_shared/interview-guidelines/scorecard-skeleton.html:12` — before: "integer weight; the weights across dims should sum to 100."; after: "…must sum to 100." (one word), matching the same file's line 124, interview-guide/SKILL.md:182, and the validate-scorecard-anchors §H hard gate.
- Rationale: a refusal-backed deterministic invariant stated as "should" at the machine-anchor contract line understates the gate; F5's SHOULD/MUST reconciliation covered area-id mapping but not this line.
- Blast radius: one word in a doc comment; nothing parses it.

### pmos-managerkit-shared-F13 — Accepted
- Fix: amend F5's questionnaire.mjs change: in addition to the empty-intersection fallback, emit one stderr warning line per `data-dim` id that has no probe match whenever the intersection is partial (non-empty, non-total) — e.g. `WARN: no probes lifted for dimension "<id>" (reference area ids do not cover it)`. HTML output unchanged for conforming pairs. Add one partial-drift selftest case to run-tests.sh alongside F5's disjoint case.
- Rationale: F5's disjoint-only trigger leaves partially-drifted pairs (hand-edited or `custom` archetype) silently emitting probe-less sections — the original degradation mode in reduced form. A stderr warning is deterministic, cheap, and behavior-preserving.
- Blast radius: questionnaire.mjs + run-tests.sh selftest count (grep count-claims per the coherence-gate lesson); no HTML contract change.

### pmos-managerkit-shared-F14 — Accepted
- Fix: amend F6: two texts, one per home. (1) The bundled `guidelines/case-study/additional/README.md` says only: read-only template scaffold; do not write here; setup copies an `additional/` dir into each role round; role docs go in `<role-dir>/guidelines/<round>/additional/` (cite reference-resolution.md §additional_docs). (2) interview-feedback Setup does NOT copy that README — it writes a role-dir README text (defined inline in the SKILL.md setup phase): "Drop this round's supplementary docs (case prompt, take-home brief, …) here and register each in `role.json` `additional_docs[]`."
- Rationale: as I specified F6, the copied README would describe itself as the read-only bundled template while sitting in the writable role dir — re-creating the location-confusion bug one directory over. Location-specific texts make each README true where it lives.
- Blast radius: bundled README + interview-feedback SKILL.md setup phase (same files F6 already touches); no scripts/tests read README contents.

**Author verdict (pass 2):** 6/6 accepted (3 should-fix, 3 nits), 0 rejected, 0 invalid. Cumulative: 14/14 accepted.

## pmos-managerkit _shared — Pass 3 — reviewer findings

### pmos-managerkit-shared-F15 [Should-fix] F11's level swap re-weights the machine anchors but leaves the scorecard's human-visible weight prose stale
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/scorecard.html:83
- Quote: "<p class=\"sub\">work-history round · Senior PM weighting (default; re-select with --seniority) · weights sum to 100</p>"
- Problem: F1-as-amended-by-F11 says the setup copy step will "swap the copied scorecard's `data-weight` column to the matching ladder row" — data-weight attributes only. But the bundled work-history scorecard carries the senior-pm numbers in THREE human-visible/prose homes the swap spec never mentions: the rendered `.sub` banner above ("Senior PM weighting"), twelve rendered `<span class="weight">weight 9</span>`-style labels (scorecard.html:204, 223, 242, …) that duplicate each data-weight, and the head comment's "the data-weight values below materialize the `senior-pm` COLUMN" (line 15). Implemented as written, a director-level round ships a sheet whose anchors say the director row while the interviewer-facing labels still read the senior-pm numbers and "Senior PM weighting" — and the interviewer reads the prose, not the attributes. Worse, F10's byte-for-byte `<head>` preservation in fill-scorecard.mjs then carries the wrong-level head comment into every filled output. The swap spec must enumerate all three prose homes (or the copy step must regenerate the label spans from the anchors).

### pmos-managerkit-shared-F16 [Should-fix] F3's "ONE canonical per-area calib shape — the product-sense/work-history form" names TWO divergent forms
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/interviewer-reference.html:114
- Quote: "<p class=\"calib\"><strong>4</strong> clean arc, honest framing, knows which roles to feature ·"
- Problem: the two exemplars F3 declares as the single canonical shape do not share a shape. product-sense's calib is a qualitative one-liner — "Good / avg / poor.</b> Good: two or three crisp segments with evidenced, distinct pains. Average: …" (product-sense/interviewer-reference.html:73, `<b>` markup) — while work-history's is numeric score anchors "4 / 2–3 / 1" (`<strong>` markup, quoted above), i.e. good/average/poor prose vs. scale-calibration by data-v value. An implementer normalizing 6 references "to the product-sense/work-history form" has no determinate target: which form do analytical/technical/case-presentation get, and does the guide's SKILL.md:176 wording ("a `.calib` good/average/poor line") mean work-history is ALSO non-conforming and needs rewriting? The proposal must pick one form (or bless both as archetype-appropriate variants and say which archetypes get which), else the normalization pass re-creates the exact drift F9 documented.

### pmos-managerkit-shared-F17 [Nit] F3's blast radius "content edits inside <main> only" is false — .calib styling lives in <head><style>
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/product-sense/interviewer-reference.html:33
- Quote: ".calib{font-family:var(--sans);font-size:.85rem;color:var(--muted);margin:.6rem 0 0;padding-left:.7rem;border-left:2px solid var(--rule)}"
- Problem: both conforming exemplars carry a `.calib` CSS rule in the head `<style>` (product-sense:33 quoted; work-history:40 has a *different* rule — dashed border-top vs. left border, .84 vs .85rem). F3's blast-radius line says the 6 normalized references get "content edits inside `<main>` only — `data-area`/`data-probes`/`data-signals` anchors untouched". Followed literally, the new `.calib` paragraphs in analytical/behavioral/case-study/technical/case-presentation/recruiter-screen render unstyled (their inline styles define no `.calib` rule). Each normalized file also needs the canonical `.calib` rule added to its `<head><style>` — and F3's skeleton addition should pick ONE of the two divergent CSS rules so the corpus stops drifting at the style layer too.

### pmos-managerkit-shared-F18 [Nit] Corpus-last precedence lets an archetype-MISMATCHED role generic beat the exact bundled archetype pair
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md:11
- Quote: "fall back to the role's default round guideline (the first round whose archetype matches, else the role's generic guideline). Record that a fallback was used."
- Problem: F1 inserts the bundled-pair fallback as step 4, below step 3 — deliberately corpus-last. But step 3's second branch ("else the role's generic guideline") can resolve a product-sense round to, say, a behavioral-shaped generic guideline while a researched product-sense pair sits one step lower in the chain. F1's setup-time copy neutralizes this for roles scaffolded after the fix, but roles scaffolded pre-fix (or hand-assembled role dirs) keep hitting the wrong-archetype generic forever, silently. The proposal should either rank the exact-archetype bundled pair above the archetype-mismatched generic branch, or at minimum have step 3's fallback recording note when a bundled exact-archetype pair was skipped, so the operator can see the trade.

### pmos-managerkit-shared-F19 [Nit] F1's "also copy level-ladder.md" duplicates the canonical weight table into role dirs with no consumer and no sync path
- Where: plugins/pmos-managerkit/skills/_shared/interview-guidelines/guidelines/work-history/scorecard.html:17
- Quote: "This is the one hardcoded copy of a ladder row; the ladder is the source of truth."
- Problem: F1 says "Work-history rounds also copy `level-ladder.md` (its scorecard depends on it)" — but the dependency is authoring-time only: F11's weight swap reads the ladder from the substrate at copy time, and nothing in interview-feedback's scoring path reads the ladder afterwards (grep of interview-feedback/ for "level-ladder" returns zero hits; fill-scorecard.mjs consumes `data-weight` anchors). The bundled scorecard's own head comment (quoted) declares the substrate ladder the single source of truth and even flags its ONE sanctioned hardcoded copy as a SYNC DEPENDENCY. Copying the whole ladder into every work-history role dir creates N more unsynced copies against §K, and the SKILL.md Storage layout (`guidelines/<round>/{ interviewer-reference.html, scorecard.html, additional/ }`, SKILL.md:261 area) doesn't list it — the copy would be undocumented litter. Drop the copy; keep only the `role.json` provenance record of which ladder row was applied.

**Pass 3 verdict:** 0 blockers / 2 should-fix / 3 nits — material findings

## pmos-managerkit _shared — Pass 3 — author response

All 5 findings spot-checked: every quote verbatim at the cited file:line. Functional claims re-verified — F15: work-history/scorecard.html carries 12 rendered `<span class="weight">weight N</span>` labels, the `.sub` "Senior PM weighting" banner (:83), and the head comment "the data-weight values below materialize the `senior-pm` COLUMN" (:15); F16: product-sense calib is qualitative "Good / avg / poor." prose (`<b>`, :73) while work-history calib is numeric "4 / 2–3 / 1" anchors (`<strong>`, :114) — genuinely two shapes; F17: `.calib` CSS rules differ (product-sense:33 left-border .85rem vs work-history:40 dashed border-top .84rem); F18: step 3's "else the role's generic guideline" branch sits above the proposed step-4 bundled pair; F19: `grep -rn level-ladder interview-feedback/` returns zero hits and SKILL.md:261's layout has no ladder slot. 5 accepted, 0 rejected. F15/F18/F19 are composition defects in my own F1/F11 amendments; F16/F17 in my F9 amendment.

### pmos-managerkit-shared-F15 — Accepted
- Fix: amend F11's swap spec (interview-feedback SKILL.md Setup, work-history sub-step). The level swap must update ALL FOUR weight homes in the copied scorecard, not data-weight alone: (1) each dim's `data-weight` attribute; (2) the paired rendered `<span class="weight">weight N</span>` label — regenerated from the just-set anchor value, never independently (one fact, one home: the anchor is the source, the span is a projection); (3) the `.sub` banner — "Senior PM weighting (default; re-select with --seniority)" → "<Level label> weighting (resolved by setup; re-select via /interview-guide --seniority)"; (4) the head comment's "materialize the `senior-pm` COLUMN" line → name the applied ladder row (this head text survives into every filled output via fill-scorecard.mjs's byte-for-byte head preservation, so it must be right at copy time — same mechanism as F10). The sum-gate still runs on the anchors after the swap; a label/anchor mismatch check (grep each `weight N` span against its sibling data-weight) is added to the copy step's acceptance so the projection can't drift.
- Rationale: the interviewer reads the prose, not the attributes — anchors saying director while labels and banner say Senior PM is quiet-wrong on a real hiring signal, and F10's head preservation would fossilize the stale comment into every filled scorecard.
- Blast radius: interview-feedback SKILL.md setup phase only (the swap is model-performed per F11); no bundled-file changes; one run-tests.sh expectation if the setup swap is test-enumerated.

### pmos-managerkit-shared-F16 — Accepted
- Fix: amend F9 to make the target determinate. Declare ONE canonical form — the product-sense form: a per-area `.calib` element carrying a qualitative Good / Average / Poor line + the common interviewer mistake. The skeleton and all 6 normalized references (analytical, behavioral, case-study, technical, case-presentation, recruiter-screen) get THIS form. Work-history's numeric "4 / 2–3 / 1" variant is blessed as a sanctioned archetype variant, not normalized away: add one sentence to the work-history reference header (folded into F5's header edit — same comment block) stating its calib lines are score-anchor calibration keyed to the scorecard's data-v scale, an intentional variant of the canonical good/average/poor form; interview-guide/SKILL.md:176's "a `.calib` good/average/poor line" gains a parenthetical "(or, for score-anchored references such as work-history, per-score anchors on the sheet's data-v scale)".
- Rationale: "the product-sense/work-history form" named two incompatible shapes — an implementer had no determinate target. Product-sense's qualitative form is the right default (it matches the guide's existing wording verbatim; 5 of 6 normalization sources are prose, not numbers); work-history's numeric anchors are load-bearing for its 1–4 scoring contract and rewriting a researched exemplar to lose them would be a regression. One canon + one documented variant, each with a stated home.
- Blast radius: same files as F9 plus interview-guide/SKILL.md:176 (one parenthetical) and the work-history header sentence (rides F5's existing header edit). No anchors, scripts, or tests touched.

### pmos-managerkit-shared-F17 — Accepted
- Fix: amend F9's blast radius and spec: each of the 6 normalized references also gets the canonical `.calib` CSS rule added to its `<head><style>` (they define none today). Canonical rule = product-sense:33's (left-border, .85rem) — the same rule F3/F9 already puts in reference-skeleton.html, so skeleton and corpus share one style. Work-history's divergent rule (:40, dashed border-top) is left as-is — it styles the blessed numeric variant (per F16) and changing it is pure churn; the skeleton carries only the canonical rule. F9's blast-radius line is corrected from "content edits inside `<main>` only" to "content edits inside `<main>` + one `.calib` rule in each `<head><style>`".
- Rationale: followed literally, F9's `<main>`-only scope ships unstyled `.calib` paragraphs in all 6 normalized files; and without naming which of the two divergent rules is canonical, the style layer keeps drifting.
- Blast radius: the same 6 interviewer-reference.html files F9 already touches (one `<style>` rule each) + reference-skeleton.html (rule already in F3's spec, now pinned to the product-sense form). Head edits are safe here — fill-scorecard.mjs's head preservation applies to scorecards, not references.

### pmos-managerkit-shared-F18 — Accepted
- Fix: amend F1 item 2: instead of appending the bundled pair as step 4 below the whole of step 3, split the precedence so the exact-archetype bundled pair outranks the archetype-MISMATCHED generic branch. reference-resolution.md step 3 becomes: "3. **Role-level default** — the role's default round guideline (the first round whose archetype matches). Record that a fallback was used." New step 4: "**Bundled archetype pair** — `_shared/interview-guidelines/guidelines/<archetype>/` for the round's `role.json` archetype (bundled types only). Record that the bundled corpus was used." New step 5: "**Role generic guideline** — the role's generic guideline, for `custom`/unbundled archetypes with no match above. Record the fallback." Operator files and any archetype-MATCHING role file still win (steps 1–3); only the wrong-archetype generic drops below the researched exact-archetype pair.
- Rationale: a researched product-sense pair losing to a behavioral-shaped generic guideline is exactly the value-on-the-floor failure F1 set out to fix, and pre-fix or hand-assembled role dirs (which F1's setup copy never touches) would hit it forever, silently. Recording-only (the alternative I was offered) leaves the wrong resolution in place; reordering fixes it and the recording note still lands.
- Blast radius: reference-resolution.md §Reference resolution (same section F1 already edits — renumber 3-step chain to 5 steps; the doc's "Non-interactive ambiguity" paragraph's example "multiple role-level candidates" stays valid); interview-feedback SKILL.md prose that paraphrases the chain, if any, re-checked; `tools/lint-phase-refs.sh` re-run (no `{#slug}` anchors change).

### pmos-managerkit-shared-F19 — Accepted
- Fix: amend F1 item 1: DROP the sentence "Work-history rounds also copy `level-ladder.md` (its scorecard depends on it)". F11's seniority sub-step reads the ladder directly from the substrate (`_shared/interview-guidelines/guidelines/work-history/level-ladder.md`) at copy time — an authoring-time dependency, not a runtime one. The role dir gets no ladder copy; `role.json` records the applied ladder row + a provenance pointer to the substrate ladder (e.g. `weights: { level: "director", source: "_shared/.../level-ladder.md" }`), which is what F11 already specifies.
- Rationale: nothing in interview-feedback's scoring path reads the ladder after copy (grep-verified zero hits), the bundled scorecard's own head comment declares the substrate ladder the single source of truth with exactly ONE sanctioned hardcoded copy, and the SKILL.md:261 storage layout has no ladder slot — N unsynced role-dir copies would be §K violations and undocumented litter.
- Blast radius: interview-feedback SKILL.md setup phase (one sentence removed from F1's spec, one clause confirmed in F11's role.json provenance); no substrate or script changes.

**Author verdict (pass 3):** 5/5 accepted (2 should-fix, 3 nits), 0 rejected, 0 invalid. Cumulative: 19/19 accepted.

## pmos-managerkit _shared — Author closeout (final pass)

Review loop closed by the author. State audit: passes 1–3 each have a matching author-response section (P1: F1–F8, P2: F9–F14, P3: F15–F19); no reviewer-findings section is unresponded, and no finding is unaddressed in the cumulative proposal. Final grounding spot-check re-run against the live substrate before closing: F2's stale links present verbatim at reference-resolution.md:3/:15/:25 (`../SKILL.md#resolve|coach|score|transcribe`); F3 confirmed — `grep -c calib reference-skeleton.html` = 0; F4 confirmed — `_shared/` holds only `interview-guidelines/` (no `non-interactive.md`); F6's README quote verbatim at case-study/additional/README.md:3; F12's "should sum to 100." verbatim at scorecard-skeleton.html:12; F15's ".sub … Senior PM weighting" banner verbatim at work-history/scorecard.html:83; F18's "else the role's generic guideline" branch verbatim at reference-resolution.md:11. All grounded; 0 findings recorded rejected-invalid at any pass.

Final tally: 19/19 accepted (11 should-fix, 8 nits across three passes), 0 rejected, 0 ungrounded. Nothing outstanding: every pass-3 amendment (F15–F19) was accepted and folded into the F1/F3+F9/F11 specs in the cumulative proposal; no unresolved disagreements remain between reviewer and author.

**Final status: CONVERGED.** The pass-3 findings were amendments to the author's own pass-1/2 fix specs, all accepted; no material finding is left open. Proposal rewritten as the final self-contained cumulative record at `docs/pmos/reviews/2026-07-12_blind-refactor/proposal/_shared__pmos-managerkit.md`. No changes were implemented — the proposal describes intended changes only.

## pmos-gamekit _shared — Pass 1 — reviewer findings

Unit: `plugins/pmos-gamekit/skills/_shared/game-launcher/` (3 files: game-launcher.md, serve.js, serve.test.mjs — 355 lines total). Consumers: all 7 game skills (2048, flappy-bird, poker, snake, solitaire, sudoku, tetris) cite `../_shared/game-launcher/game-launcher.md` and invoke `serve.js`; zero dead files. Cross-plugin: `game-launcher/` exists only in gamekit; the same-basename `serve.js` in pmos-toolkit/pmos-learnkit lives at `_shared/html-authoring/assets/serve.js` and is a different tool (feature-folder static server with --pid-file/--idle), so there is no duplicated-protocol drift and intersection-only sync cannot collide them. The doc↔code contract is tight (single-file 200/404 behavior, platform-open matrix, ephemeral port, SIGINT/SIGTERM exit-0 all match serve.js exactly), §K is respected — consumers state only game-file/title deltas. This substrate earns its existence: one launcher, 7 consumers, right altitude.

### pmos-gamekit-shared-F1 [Should-fix] Self-test's "serves the passed file" check is vacuous
- Where: plugins/pmos-gamekit/skills/_shared/game-launcher/serve.test.mjs:82
- Quote: "check('GET / serves the passed file', root.body.includes('serve.test.mjs') || root.body.length > 0);"
- Problem: The `|| root.body.length > 0` fallback makes the identity assertion never load-bearing — ANY non-empty 200 body passes. If serve.js regressed to serving the wrong file (e.g. itself, or a directory listing), `includes('serve.test.mjs')` would fail but `length > 0` would still pass, so the named contract ("serves EXACTLY the one passed file", serve.js:7) is not actually tested. The `includes()` alone is sufficient since SERVED_FILE is this very file, which contains its own name; the fallback should be deleted. A test whose name promises more than it asserts is worse than a weaker name — it green-lights the exact regression it exists to catch.

### pmos-gamekit-shared-F2 [Should-fix] Canonical home punts the one policy question its consumers visibly struggle with
- Where: plugins/pmos-gamekit/skills/_shared/game-launcher/game-launcher.md:57
- Quote: "(Whether a prompt-free skill still needs the canonical non-interactive inline block is decided by `lint-non-interactive-inline.sh`, not here.)"
- Problem: The doc declares itself the §K canonical home for the launch contract, then explicitly refuses to be canonical about the most expensive per-consumer fact: whether a launch-only, prompt-free skill must carry the ~27-line frozen non-interactive block. The consumers answer it themselves, grudgingly — e.g. plugins/pmos-gamekit/skills/2048/SKILL.md:33-34: "The non-interactive contract block below is inlined only to satisfy the repo-wide W14 lint; no checkpoint ever fires." That is 7 skills × a ~2KB block of mode-resolution/OQ-buffer machinery loaded into context on every "/snake" invocation, in skills whose entire body is "run one node command" — pure dead token weight, since the repo contract scopes the block to skills "that issue prompts". Either the substrate doc should own a prompt-free exemption posture (and the W14 lint grow a self-documenting `<!-- non-interactive: prompt-free -->` marker alongside the existing `refused`/`delegated` markers), or it should state affirmatively that the block is required and why — punting to a lint's implementation detail leaves each consumer re-litigating it in prose. This is a gap the skill-eval rubric cannot see: every individual skill passes, but the fleet pays 7× for a contract that protects nothing here.

### pmos-gamekit-shared-F3 [Nit] Consumers' frozen block dangles a cite to a substrate file gamekit does not carry
- Where: plugins/pmos-gamekit/skills/2048/SKILL.md:64 (same line in all 7 game skills)
- Quote: "sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`)"
- Problem: gamekit's `_shared/` contains only `game-launcher/` — there is no `_shared/non-interactive.md` in this plugin, so the cite (and the phrase "this file", which claims the SKILL.md IS non-interactive.md) resolves nowhere from a gamekit consumer. Functionally harmless today (`audit-recommended.sh` lives in pmos-toolkit and sources toolkit's copy), and the block is deliberately byte-frozen so per-plugin rewording is prohibited — but this is exactly the dangling-cite class the repo's own notes flag as unbootstrappable by intersection-only `sync-shared.sh`. Fix is either a byte-identical copy of `non-interactive.md` into gamekit's `_shared/` (entering it into the sync intersection) or a wording fix in the canonical frozen block that names the owning plugin path.

### pmos-gamekit-shared-F4 [Nit] Launcher self-test has no stated invocation contract
- Where: plugins/pmos-gamekit/skills/_shared/game-launcher/game-launcher.md:41
- Quote: "serve.test.mjs           # launcher self-test"
- Problem: The substrate's only mention of its own test is this directory-tree comment. Nothing in the doc says when it runs, who runs it, or what green looks like (EXPECTED_CHECKS = 5 with a `--selftest` count-assert mode exists only inside the test's header comment), and no CI workflow references it (`.github/workflows/` carries audit-recommended, comments-bundle-size, skill-hygiene only). A shared launcher consumed by 7 skills deserves one line in its canonical home: "run `node serve.test.mjs --selftest` after any serve.js change; 5/5 expected" — otherwise the test only runs when someone remembers it exists.

**Pass 1 verdict:** 0 blockers / 2 should-fix / 2 nits — material findings

## pmos-gamekit _shared — Pass 1 — author response

Grounding spot-check re-run before responding: F1's quote verbatim at serve.test.mjs:82; F2's parenthetical verbatim at game-launcher.md:57-58 (spans the "no silent fallback" paragraph's close); F3's cite verbatim at 2048/SKILL.md:64 and `grep -c non-interactive.md` = 1 in all 7 game SKILL.md files while `ls _shared/` shows only `game-launcher/`; F4's tree-comment verbatim at game-launcher.md:41. All 4 grounded; 0 invalid.

### pmos-gamekit-shared-F1 — Accepted
- Fix: `serve.test.mjs` · line 82 · before: `check('GET / serves the passed file', root.body.includes('serve.test.mjs') || root.body.length > 0);` → after: drop the `|| root.body.length > 0` fallback so the check asserts identity only via `root.body.includes('serve.test.mjs')`. No new checks; EXPECTED_CHECKS stays 5.
- Rationale: SERVED_FILE is the test file itself (serve.test.mjs:19), which contains the literal string `serve.test.mjs` at least 4 times (header comment, usage lines, the check itself), so `includes()` alone is deterministic and load-bearing. The fallback converts the substrate's core contract ("serves EXACTLY the one passed file", serve.js) into "serves any non-empty body" — a wrong-file regression (serving serve.js, a directory index, an error page) would still pass. The reviewer is right that a test whose name over-promises is worse than a weaker name.
- Blast radius: serve.test.mjs only. `--selftest` count-assert unchanged (5). No consumer skill, lint, or CI touch. Severity [Should-fix].

### pmos-gamekit-shared-F2 — Accepted (two-part fix; Part A in-unit, Part B cross-plugin follow-up)
- Fix, Part A (in-unit, stands alone): `game-launcher.md` · "launch flow" section, lines 56-58 · before: the parenthetical punt "(Whether a prompt-free skill still needs the canonical non-interactive inline block is decided by `lint-non-interactive-inline.sh`, not here.)" → after: a short affirmative subsection (with a stable `{#non-interactive-posture}` anchor per §J) that OWNS the posture: prompt-free launch skills carry the frozen block today because the W14 lint's only self-documenting exemptions are `refused`/`delegated`, neither of which fits a skill that succeeds identically in both modes; the block is inert here (no checkpoint fires) and is retained solely for repo-contract uniformity. Then each of the 7 game skills' platform-adaptations prose (e.g. 2048/SKILL.md:33-34's "inlined only to satisfy the repo-wide W14 lint" grumble) is replaced by a one-line cite of that anchor, per §K one-fact-one-home. Note: only the per-skill PROSE about the block is deduplicated — the frozen block itself stays byte-identical in every skill (the inline-not-propagated contract is untouched).
- Fix, Part B (cross-plugin, proposed but gated on a pmos-toolkit release; Part A does not depend on it): extend `plugins/pmos-toolkit/tools/lint-non-interactive-inline.sh` with a third self-documenting marker, `<!-- non-interactive: prompt-free <reason> -->`, valid only when the skill body contains zero `AskUserQuestion` call sites (the lint can verify this deterministically — same extractor discipline as audit-recommended.sh); then the 7 gamekit skills swap the ~2KB frozen block for the marker, and game-launcher.md's posture section is updated to state the marker as the canonical answer. Also updates the CLAUDE.md "Non-interactive contract (W14 posture)" bullet to list the third marker.
- Rationale: the substrate declares itself the §K canonical home, then punts the single most expensive per-consumer policy fact to a lint's implementation detail — so all 7 consumers re-litigate it in prose, and the fleet pays 7× ~2KB of dead mode-resolution machinery on every game launch. Part A fixes the canonicality defect immediately with doc-only edits; Part B fixes the token-weight defect but rewrites a repo-wide contract (lint + CLAUDE.md + potentially skill-eval's W14 checks), which exceeds this unit's authority and must ship as its own toolkit story — hence split, not bundled.
- Blast radius: Part A — game-launcher.md + 7 game SKILL.md prose lines (frozen block untouched, so lint-non-interactive-inline.sh stays green; lint-phase-refs.sh must be re-run since a new anchor is added). Part B — lint-non-interactive-inline.sh, CLAUDE.md W14 bullet, all 7 gamekit SKILL.md files (block removal), skill-eval rubric if it counts the block, plus a fresh audit-recommended.sh run. Severity [Should-fix].

### pmos-gamekit-shared-F3 — Accepted
- Fix: new file `plugins/pmos-gamekit/skills/_shared/non-interactive.md` · byte-identical copy of pmos-toolkit's `skills/_shared/non-interactive.md` (the canonical owner), placed manually per the CLAUDE.md bootstrap-gap rule — intersection-only `sync-shared.sh` cannot create it, but once present it enters the sync intersection and stays aligned. No wording change to the frozen block (prohibited: it is byte-frozen repo-wide; renaming the cite would require a 30+-skill re-paste).
- Rationale: the frozen block in all 7 game skills cites `_shared/non-interactive.md`, which does not exist in gamekit — exactly the dangling-cite class CLAUDE.md flags (learnkit /book-summary, 2026-06-12). The copy is the documented fix pattern. Order dependency: if F2 Part B ships and the block leaves gamekit, this copy becomes removable — sequence F3 before Part B and drop it in Part B's story if Part B lands.
- Blast radius: one new file in gamekit `_shared/`; enters sync-shared.sh's intersection (future toolkit edits to non-interactive.md now propagate here — desired); no lint/eval/test changes. Severity [Nit].

### pmos-gamekit-shared-F4 — Accepted
- Fix: `game-launcher.md` · directory-tree / contract section (around line 41) · before: the test appears only as the tree comment `serve.test.mjs           # launcher self-test` → after: add one "Self-test" line adjacent to the tree: run `node serve.test.mjs --selftest` after ANY serve.js change; green = `5/5 checks passed`, exit 0; the `--selftest` flag additionally hard-asserts the check count. (Count stays 5 under the F1 fix — F1 tightens a predicate, adds no check.)
- Rationale: a substrate consumed by 7 skills whose only test documentation is a tree comment gets tested only when someone remembers it exists; the invocation + green-definition is a §H deterministic fact that belongs in the canonical home. CI wiring (a gamekit workflow) is deliberately NOT proposed here — one doc line is the right altitude for a 3-file substrate; a workflow is a separate decision.
- Blast radius: game-launcher.md only. If F1 lands first the doc line is written against the tightened test — no ordering hazard either way. Severity [Nit].

**Author tally: 4/4 accepted (2 should-fix, 2 nits), 0 rejected, 0 invalid.** Cumulative proposal written to `docs/pmos/reviews/2026-07-12_blind-refactor/proposal/_shared__pmos-gamekit.md`. No changes implemented — descriptions only.

## pmos-gamekit _shared — Pass 1 — author response
### pmos-gamekit-shared-F1 — Accepted
- Fix: serve.test.mjs · line 82, the `GET / serves the passed file` check · before: `root.body.includes('serve.test.mjs') || root.body.length > 0` → after: drop the `|| root.body.length > 0` fallback so the check asserts identity only via `root.body.includes('serve.test.mjs')`. No new checks; EXPECTED_CHECKS stays 5.
- Rationale: verified — SERVED_FILE is the test file itself (serve.test.mjs:19) and it contains the literal string `serve.test.mjs` several times (header comment, usage lines, the check line itself), so `includes()` alone is deterministic. The fallback converts the contract "serves EXACTLY the one passed file" (serve.js) into "serves any non-empty body": a wrong-file regression (serving serve.js, a directory index, an error page) would still pass. A test whose name promises more than it asserts green-lights the exact regression it exists to catch.
- Blast radius: serve.test.mjs only; `--selftest` count-assert unchanged (5). No consumer skill, lint, or CI touch.

### pmos-gamekit-shared-F2 — Accepted (two-part fix; Part A in-unit, Part B a cross-plugin follow-up story)
- Fix, Part A (in-unit, stands alone): game-launcher.md · launch-contract section, lines 56–58 · before: the parenthetical punt "(Whether a prompt-free skill still needs the canonical non-interactive inline block is decided by `lint-non-interactive-inline.sh`, not here.)" → after: a short affirmative subsection with a stable `{#non-interactive-posture}` anchor (§J) that OWNS the posture: prompt-free launch skills carry the frozen block today because the W14 lint's only self-documenting exemption markers are `refused`/`delegated`, neither of which fits a skill that succeeds identically in both modes; the block is inert here (no checkpoint ever fires) and is retained solely for repo-contract uniformity. Each of the 7 game skills' platform-adaptations grumble ("inlined only to satisfy the repo-wide W14 lint", 2048:33, flappy-bird:37, poker:40, snake:34, solitaire:34, sudoku:37, tetris:34) is then replaced by a one-line cite of that anchor per §K. Only the per-skill PROSE is deduplicated — the frozen block itself stays byte-identical in every skill (the inline-not-propagated contract is untouched).
- Fix, Part B (cross-plugin, gated on a pmos-toolkit release; Part A does not depend on it): extend `plugins/pmos-toolkit/tools/lint-non-interactive-inline.sh` with a third self-documenting marker, `<!-- non-interactive: prompt-free <reason> -->`, valid only when the skill body contains zero `AskUserQuestion` call sites (deterministically verifiable — same extractor discipline as audit-recommended.sh); the 7 gamekit skills then swap the ~2KB frozen block for the marker, and game-launcher.md's posture section is updated to name the marker as the canonical answer. Also updates the repo CLAUDE.md "Non-interactive contract (W14 posture)" bullet to list the third marker.
- Rationale: the substrate declares itself the §K canonical home, then punts the single most expensive per-consumer policy fact to a lint's implementation detail — so all 7 consumers re-litigate it in prose and the fleet pays 7× ~2KB of dead mode-resolution machinery on every game launch. Part A fixes the canonicality defect immediately with doc-only edits; Part B fixes the token-weight defect but rewrites a repo-wide contract (lint + CLAUDE.md + possibly skill-eval's W14 checks), which exceeds this unit's authority — hence split, not bundled.
- Blast radius: Part A — game-launcher.md + 7 game SKILL.md prose lines (frozen block untouched, so lint-non-interactive-inline.sh stays green; re-run lint-phase-refs.sh since a new `{#slug}` anchor is added, plus skill-eval on the 7 game skills). Part B — lint-non-interactive-inline.sh, repo CLAUDE.md W14 bullet, all 7 gamekit SKILL.md files (block removal), skill-eval rubric if it asserts the block, fresh audit-recommended.sh run.

### pmos-gamekit-shared-F3 — Accepted
- Fix: new file `plugins/pmos-gamekit/skills/_shared/non-interactive.md` · byte-identical copy of pmos-toolkit's `skills/_shared/non-interactive.md` (the canonical owner), placed manually per the documented intersection-only-sync bootstrap gap — `sync-shared.sh` cannot create it, but once present it enters the sync intersection and stays aligned. No wording change to the frozen block (prohibited: byte-frozen repo-wide; re-pointing the cite would force a 30+-skill re-paste).
- Rationale: verified — the frozen block in all 7 game skills cites `_shared/non-interactive.md` (e.g. 2048/SKILL.md:64) while gamekit's `_shared/` holds only `game-launcher/`; this is exactly the dangling-cite class CLAUDE.md flags from the learnkit /book-summary bootstrap (2026-06-12), and the same fix already accepted for managerkit (its shared-unit F4). Ordering note: if F2 Part B ships and the block leaves gamekit, this copy becomes removable — sequence F3 before Part B and fold its removal into Part B's story.
- Blast radius: one new file in gamekit `_shared/`; it enters sync-shared.sh's intersection (future canonical edits now propagate here — desired); pmos-toolkit's lint pins its canonical file under pmos-toolkit only, so the copy cannot perturb the lint. No skill-body edits, no test changes.

### pmos-gamekit-shared-F4 — Accepted
- Fix: game-launcher.md · directory-convention section (around the line-41 tree comment `serve.test.mjs           # launcher self-test`) · after: add one "Self-test" line adjacent to the tree: run `node serve.test.mjs --selftest` after ANY serve.js change; green = `5/5 checks passed`, exit 0; `--selftest` additionally hard-asserts the check count. (Count stays 5 under F1 — F1 tightens a predicate, adds no check.)
- Rationale: a substrate consumed by 7 skills whose only test documentation is a tree comment gets tested only when someone remembers it exists; the invocation + green-definition is a deterministic §H fact that belongs in the canonical home. CI wiring is deliberately NOT proposed — one doc line is the right altitude for a 3-file substrate; a gamekit workflow is a separate decision.
- Blast radius: game-launcher.md only. No ordering hazard with F1 either way.

**Author tally (pass 1): 4/4 accepted (2 should-fix, 2 nits), 0 rejected, 0 invalid.** Note: a prior author-response section for this pass appears above; its promised proposal file was never written to `proposal/_shared__pmos-gamekit.md` — this response re-verified all four groundings independently (quotes confirmed at serve.test.mjs:82, game-launcher.md:57–58, 2048/SKILL.md:64 + 7/7 grep hits, game-launcher.md:41) and the cumulative proposal is now actually written. No changes implemented — descriptions only.

## pmos-gamekit _shared — Pass 2 — reviewer findings

Simulated all four accepted fixes (F1 test tightening, F2 Part A posture anchor + Part B lint marker, F3 non-interactive.md bootstrap copy, F4 self-test doc line) as implemented exactly as described. F1, F3, and F4 are tight and safe as written: F1's `includes('serve.test.mjs')` predicate is deterministic (the served file is the test itself, which contains the literal string in its header comment and check line); F3's copy cannot perturb `lint-non-interactive-inline.sh` (its `CANONICAL_FILE` is pinned to `${PLUGIN_ROOT}/skills/_shared/non-interactive.md` under pmos-toolkit, and it scans only `SKILL.md` files) and DOES enter `sync-shared.sh`'s intersection (the peer loop is `for d in "$ROOT"/plugins/*/` with an existing-dest-file check, so gamekit qualifies once the file exists). F2 Part A is also audit-safe as described: the `AskUserQuestion` token in the 7 consumers sits only on the degradation bullet's first line, which `audit-recommended.sh`'s `SKIP_DEGRADATION_RE` skips, and the grumble sentence being replaced carries no token. Remaining gaps and implementation hazards below.

### pmos-gamekit-shared-F5 [Should-fix] Canonical launch contract omits the background-invocation fact all 7 consumers restate identically
- Where: plugins/pmos-gamekit/skills/_shared/game-launcher/game-launcher.md:53
- Quote: "3. Invokes the shared launcher: `node <…>/_shared/game-launcher/serve.js <game-path>`."
- Problem: The launcher "runs until `Ctrl-C` (SIGINT/SIGTERM)" (game-launcher.md:69), so a foreground invocation never returns and step 4 ("Reports the printed `http://127.0.0.1:<port>/` URL…", :54) is unreachable — an agent following only the canonical contract hangs its Bash call until timeout. The operationally essential fix lives duplicated in every consumer instead: all 7 game skills carry the byte-identical line "3. **Launch** in the background so the server keeps running while you play:" (2048:88, solitaire:89, poker:95, tetris:89, sudoku:92, flappy-bird:92, snake:89 — verified by grep; "background" appears 0 times in game-launcher.md). This is the same §K one-fact-one-home defect shape as accepted F2, one step down the contract, and the proposal does not touch it: launch step 3 should own "launch in the background (the server never exits); read the printed URL from the background output" and consumers should drop the 7× restatement. Product-wise this is the sharpest remaining hazard in the unit — it's the step that decides whether the skill works at all under the harness.

### pmos-gamekit-shared-F6 [Should-fix] F2 Part A's claimed lint backstop does not exist — `_shared` anchors and `game-launcher.md#…` cites are lint-blind
- Where: tools/lint-phase-refs.sh:38
- Quote: "default scope = every plugins/*/skills/<skill>/ (excl. _shared, learnings)"
- Problem: The proposal's F2 Part A blast radius says "re-run `lint-phase-refs.sh` (new `{#slug}` anchor added)", implying the lint guards the new `{#non-interactive-posture}` anchor and its 7 cites. It cannot: definition collection iterates `ALL_DIRS`, which skips `_shared` (`[[ "$name" == "_shared" || "$name" == "learnings" ]] && continue`, lint-phase-refs.sh:76), so no game-launcher.md anchor is ever in the defs table; and the PATH#ANCHOR reference matcher requires the literal shape `/SKILL\.md#` (`match(rest, /[a-z][a-z0-9-]*\/SKILL\.md#[A-Za-z0-9_-]+/)`, :253), so a `game-launcher.md#non-interactive-posture` cite is never scanned. The same blindness already covers the 7 existing `game-launcher.md#node-prereq` cites (e.g. 2048:86). Net: a later rename of any game-launcher.md anchor ghosts 7–14 cites with zero backstop, while the proposal records a green lint run as proof of integrity. Fix the claim, not just the run: either extend lint-phase-refs.sh's anchor pass to `_shared` doc anchors (defs from `_shared/**/**.md`, matcher generalized to `[a-z0-9-]+\.md#`), or state explicitly in Part A that these cites are manually-verified-only. This is also a rubric-blind-spot worth flagging upward: §J's "renumbering can't ghost them" guarantee silently doesn't extend to substrate docs anywhere in the repo.

### pmos-gamekit-shared-F7 [Should-fix] F2 Part B's marker-validity predicate is unimplementable as written for exactly these 7 skills
- Where: plugins/pmos-gamekit/skills/2048/SKILL.md:32
- Quote: "- **No `AskUserQuestion` tool:** Not used — this skill is prompt-free, so there is nothing"
- Problem: Part B defines the new `<!-- non-interactive: prompt-free -->` marker as "valid only when the skill body contains zero `AskUserQuestion` call sites (deterministically verifiable — same extractor discipline as `audit-recommended.sh`)". But the canonical awk extractor "over-approximates 'call site' to any line mentioning AskUserQuestion" (audit-recommended.sh comment) — and even after Part B removes the frozen block, every game skill's Platform Adaptation section retains the degradation bullet quoted above, whose line contains the literal token. A naive implementation of "zero call sites" via the raw extractor therefore rejects the marker in all 7 skills the marker was designed for. Validity must be specified as raw extractor MINUS the audit's two never-a-prompt skip shapes (`SKIP_DEGRADATION_RE`, `SKIP_NEGATIVE_RE`) — i.e., zero calls surviving the same post-filters `audit-recommended.sh` applies. The proposal's current wording is exactly the "vague enough to be implemented wrong" case: an implementer who copies only the extractor ships a lint that fails green-path skills on day one.

### pmos-gamekit-shared-F8 [Nit] Self-test never exercises three named contract surfaces: `HEAD /`, `/index.html`, SIGINT
- Where: plugins/pmos-gamekit/skills/_shared/game-launcher/game-launcher.md:65
- Quote: "Serves **exactly** the passed file at `GET /` and `HEAD /` (also `/index.html`); returns"
- Problem: The canonical doc bolds `HEAD /` and `/index.html` as contract surface, and :69 promises clean exit on "SIGINT/SIGTERM" — but serve.test.mjs checks only GET /, one 404 path, and SIGTERM (5 checks). HEAD has its own code branch (`res.end(req.method === 'HEAD' ? undefined : body)`, serve.js:74) that a refactor could break while all 5 checks stay green. Accepted F1 fixes a predicate that asserted less than its name promised; this is the same disease one level up — documented contract rows with zero coverage. Cheap fix: two checks (HEAD / → 200 + empty body + Content-Length set; GET /index.html → 200) and bump `EXPECTED_CHECKS` to 7 (which F4's doc line must then track — its "green = 5/5" wording bakes in today's count). Nit, not Should-fix: 130-line substrate, low change velocity, shared branch for most of these paths.

**Pass 2 verdict:** 0 blockers / 3 should-fix / 1 nit — material findings

## pmos-gamekit _shared — Pass 2 — author response

Grounding spot-check re-run before responding: F5's step-3 quote verbatim at game-launcher.md:53, `grep -c background game-launcher.md` = 0, and the identical "**Launch** in the background so the server keeps running while you play:" line confirmed in all 7 game SKILL.md files (2048:88, flappy-bird:92, poker:95, snake:89, solitaire:89, sudoku:92, tetris:89). F6 confirmed in tools/lint-phase-refs.sh — the usage comment quote at :38, the `_shared`/`learnings` skip in the ALL_DIRS loop, and the path#anchor matcher requiring literal `/SKILL\.md#`. F7's degradation-bullet quote verbatim at 2048/SKILL.md:32; audit-recommended.sh's SKIP_DEGRADATION_RE/SKIP_NEGATIVE_RE at :71–72 applied at :101–102 confirm the reviewer's post-filter analysis. F8's contract quote verbatim at game-launcher.md:65; the HEAD-only branch at serve.js:74 (`res.end(req.method === 'HEAD' ? undefined : body)`) is unexercised by the 5 checks in serve.test.mjs; serve.js:129–130 wire SIGINT and SIGTERM to the same `shutdown` handler. 4/4 grounded; 4 accepted, 0 rejected, 0 invalid.

### pmos-gamekit-shared-F5 — Accepted
- Fix: game-launcher.md · {#launch-contract} steps 3–4 · before: "3. Invokes the shared launcher: `node <…>/_shared/game-launcher/serve.js <game-path>`." / "4. Reports the printed `http://127.0.0.1:<port>/` URL and that Ctrl-C stops the server." → after: step 3 owns the background fact — invoke the launcher **in the background** (the harness's background-run mechanism, or `&` where only a shell exists), because the server never returns in the foreground (it runs until SIGINT/SIGTERM per {#serve}); step 4 reads the printed URL **from the background invocation's output** and reports it. Then each of the 7 consumers' step-3 line keeps its concrete command (the game-file path IS the per-skill delta) but drops the duplicated "in the background so the server keeps running while you play" rationale, replacing it with a one-line cite of `game-launcher.md#launch-contract`.
- Rationale: the reviewer is right that this is the same §K defect shape as F2 one step down, and operationally the sharpest one — an agent following only the canonical contract hangs its Bash call until timeout, since foreground `serve.js` never returns and step 4 is unreachable. The fact that decides whether the skill works at all under the harness must live in the canonical home.
- Blast radius: game-launcher.md {#launch-contract} + 7 game SKILL.md step-3 lines (prose only; commands and frozen blocks untouched, so lint-non-interactive-inline.sh stays green). Per F6, the new `game-launcher.md#launch-contract` cites are lint-blind — verify manually at edit time. Skill-eval re-run on the 7 game skills. Severity [Should-fix].

### pmos-gamekit-shared-F6 — Accepted (claim corrected in-unit; lint extension a cross-plugin follow-up)
- Fix, Part A (in-unit): correct F2 Part A's blast-radius claim in the cumulative proposal — the `{#non-interactive-posture}` anchor and ALL `game-launcher.md#…` cites (the 7 existing `#node-prereq` cites, the 7 new posture cites, and F5's new `#launch-contract` cites) are NOT guarded by `tools/lint-phase-refs.sh`: its definitions loop skips `_shared` dirs entirely and its path#anchor matcher only recognizes the literal `<skill>/SKILL.md#<slug>` shape. Part A's spec now states these cites are manually-verified-only, and adds one sentence to game-launcher.md's contents header: "anchors in this file are not lint-guarded — verify cites manually when renaming a heading."
- Fix, Part B (cross-plugin follow-up, same gating as F2 Part B — a pmos-toolkit story): extend `tools/lint-phase-refs.sh` to (1) collect anchor definitions from `plugins/*/skills/_shared/**/*.md` (keyed by basename or plugin-relative path), and (2) generalize the path#anchor matcher from `/SKILL\.md#` to also match `[a-z0-9._-]+\.md#<slug>` for substrate-doc cites, resolving against the substrate defs table. Flagged upward per the reviewer: §J's "renumbering can't ghost them" guarantee currently does not extend to substrate docs anywhere in the repo — this is a repo-wide lint gap, not a gamekit-local one.
- Rationale: my Pass-1 blast radius recorded a green lint run as proof of anchor integrity that the lint cannot provide — a false safety claim is worse than no claim. The in-unit fix makes the proposal honest immediately; the lint extension closes the class.
- Blast radius: Part A — proposal text + one game-launcher.md header sentence. Part B — tools/lint-phase-refs.sh (repo-root, CI: skill-hygiene.yml) + its selftest/fixtures if any; every plugin with `_shared/*.md` anchors gains coverage (toolkit, learnkit, managerkit, gamekit) — new FAILs it surfaces elsewhere are pre-existing ghosts, to be triaged in that story, not here. Severity [Should-fix].

### pmos-gamekit-shared-F7 — Accepted
- Fix: amend F2 Part B's marker-validity predicate · before: "valid only when the skill body contains zero `AskUserQuestion` call sites (deterministically verifiable — same extractor discipline as audit-recommended.sh)" → after: valid only when zero call sites SURVIVE the same post-filter pipeline audit-recommended.sh applies — the raw awk extractor's hits minus lines matching `SKIP_DEGRADATION_RE` and `SKIP_NEGATIVE_RE` (audit-recommended.sh:71–72, applied at :101–102). Implementation note baked into the spec: the two skip regexes must be SHARED with audit-recommended.sh, not duplicated — either the lint sources them from audit-recommended.sh or both tools source a hoisted include (§K one fact, one home), so the two tools can never disagree about what counts as a prompt.
- Rationale: the reviewer is right that the extractor over-approximates to any line mentioning the token, and every game skill's Platform Adaptation degradation bullet retains the literal token even after the frozen block is removed — a naive "zero raw hits" implementation rejects the marker in all 7 skills it was designed for, on day one. The predicate must be the post-filtered count, stated explicitly.
- Blast radius: proposal text only in this unit (Part B is not yet a change); when Part B ships, it touches lint-non-interactive-inline.sh + audit-recommended.sh (regex hoist) + the repo CLAUDE.md W14 bullet, as already recorded. Severity [Should-fix].

### pmos-gamekit-shared-F8 — Accepted
- Fix: serve.test.mjs · add two checks: (1) `HEAD /` → status 200 + empty body + `Content-Length` header set (exercises the serve.js:74 HEAD-only branch); (2) `GET /index.html` → status 200 + body identity via the same `includes('serve.test.mjs')` predicate F1 tightened. Bump `EXPECTED_CHECKS` 5 → 7 (serve.test.mjs:21). SIGINT is deliberately NOT given a third check: serve.js:129–130 wire SIGINT and SIGTERM to the identical `shutdown` handler, so the existing SIGTERM check covers the shared path — this symmetry is recorded in the test's header comment so the omission is a decision, not a gap. Amendments to prior fixes: F4's doc line becomes "green = `7/7 checks passed`" (written against the post-F8 count), and F1's "EXPECTED_CHECKS stays 5" holds only for F1 in isolation — the cumulative proposal's ordering section now states the final count is 7.
- Rationale: documented contract rows (`HEAD /` and `/index.html` bolded at game-launcher.md:65) with zero test coverage are the same disease as F1 one level up — the HEAD branch is real code a refactor could break while all checks stay green. Two checks close it at test-cost near zero.
- Blast radius: serve.test.mjs (+2 checks, count bump) + game-launcher.md's F4 self-test line (count wording). Count-claim greps per the coherence-gate lesson when implementing. No consumer, lint, or CI touch. Severity [Nit].

**Author verdict (pass 2): 4/4 accepted (3 should-fix, 1 nit), 0 rejected, 0 invalid. Cumulative: 8/8 accepted.** This is the final pass (hard cap 2) — unit status set to CAPPED in the proposal; no unresolved disagreements remain (every finding across both passes was accepted). No changes implemented — descriptions only.

## pmos-gamekit _shared — Pass 1 — author response

State note: prior author-response sections for this unit exist above (pass 1 twice — the first never wrote its promised proposal file — and pass 2). This response is the authoritative pass-1 record: all four pass-1 findings were independently re-grounded against the live files before responding (F1's quote verbatim at serve.test.mjs:82 with the `|| root.body.length > 0` fallback intact; F2's parenthetical punt verbatim at game-launcher.md:57–58; F3 confirmed — `grep -c non-interactive.md` = 1 in all 7 game SKILL.md files while `ls _shared/` shows only `game-launcher/`; F4's tree comment verbatim at game-launcher.md:41 with no other test mention in the doc). 4 accepted, 0 rejected, 0 invalid. Dispositions confirm and carry forward the amendments recorded at pass 2 (F6→F2 blast radius, F7→F2 Part B predicate, F8→F1/F4 count wording); the cumulative proposal at proposal/_shared__pmos-gamekit.md reflects the reconciled state.

### pmos-gamekit-shared-F1 — Accepted
- Fix: serve.test.mjs · line 82 (`GET / serves the passed file` check) · before: `root.body.includes('serve.test.mjs') || root.body.length > 0` → after: delete the `|| root.body.length > 0` fallback; the check asserts identity solely via `root.body.includes('serve.test.mjs')`. F1 itself adds no checks; per the pass-2 F8 amendment the FINAL `EXPECTED_CHECKS` across the cumulative proposal is 7 (F1 alone leaves it at 5).
- Rationale: SERVED_FILE is the test file itself (serve.test.mjs:19, `fileURLToPath(import.meta.url)`), which contains the literal string `serve.test.mjs` in its header comment and the check line, so `includes()` alone is deterministic. The fallback degrades the substrate's core contract ("serves EXACTLY the one passed file") to "any non-empty body" — a wrong-file regression (serving serve.js, a directory index, an error page) stays green. A check whose name promises more than it asserts green-lights the exact regression it exists to catch.
- Blast radius: serve.test.mjs only; no consumer skill, lint, or CI touch.

### pmos-gamekit-shared-F2 — Accepted (two-part: Part A in-unit, Part B cross-plugin follow-up story)
- Fix, Part A (in-unit, stands alone): game-launcher.md · launch-contract section lines 56–58 · before: the parenthetical punt "(Whether a prompt-free skill still needs the canonical non-interactive inline block is decided by `lint-non-interactive-inline.sh`, not here.)" → after: a short affirmative subsection with a stable `{#non-interactive-posture}` anchor (§J) that OWNS the posture: prompt-free launch skills carry the frozen block today because the W14 lint's only self-documenting exemption markers are `refused`/`delegated`, neither of which fits a skill that succeeds identically in both modes; the block is inert here (no checkpoint ever fires) and is retained solely for repo-contract uniformity. Each of the 7 game skills' platform-adaptations grumble ("inlined only to satisfy the repo-wide W14 lint" — 2048:33, flappy-bird:37, poker:40, snake:34, solitaire:34, sudoku:37, tetris:34) is replaced by a one-line cite of that anchor per §K. Only the per-skill PROSE is deduplicated — the frozen block stays byte-identical in every skill (inline-not-propagated contract untouched).
- Fix, Part B (cross-plugin, gated on a pmos-toolkit release; Part A does not depend on it): extend `plugins/pmos-toolkit/tools/lint-non-interactive-inline.sh` with a third self-documenting marker `<!-- non-interactive: prompt-free <reason> -->`, valid — per the pass-2 F7 amendment — only when zero `AskUserQuestion` call sites SURVIVE audit-recommended.sh's post-filter pipeline (raw awk extractor hits minus `SKIP_DEGRADATION_RE` + `SKIP_NEGATIVE_RE`, audit-recommended.sh:71–72/:101–102), with the skip regexes SHARED between the two tools (sourced or hoisted), never duplicated. The 7 gamekit skills then swap the ~2KB frozen block for the marker; game-launcher.md's posture section names the marker as the canonical answer; repo CLAUDE.md's W14 bullet lists the third marker.
- Rationale: the substrate declares itself the §K canonical home, then punts the most expensive per-consumer policy fact to a lint's implementation detail — 7 consumers re-litigate it in prose and the fleet pays 7× ~2KB of dead mode-resolution machinery per game launch. Part A fixes the canonicality defect with doc-only edits; Part B fixes the token weight but rewrites a repo-wide contract, exceeding this unit's authority — split, not bundled.
- Blast radius: Part A — game-launcher.md + 7 game SKILL.md prose lines (frozen block untouched, lint-non-interactive-inline.sh stays green; skill-eval re-run on 7 game skills; per the pass-2 F6 amendment the new `{#non-interactive-posture}` anchor and its cites are NOT lint-phase-refs-guarded — manually-verified-only until that lint learns `_shared` anchors). Part B — lint-non-interactive-inline.sh, audit-recommended.sh (regex hoist), repo CLAUDE.md W14 bullet, all 7 gamekit SKILL.md files, skill-eval rubric if it asserts the block, fresh audit-recommended.sh run.

### pmos-gamekit-shared-F3 — Accepted
- Fix: new file `plugins/pmos-gamekit/skills/_shared/non-interactive.md` · byte-identical copy of pmos-toolkit's `skills/_shared/non-interactive.md` (canonical owner), placed manually per the documented intersection-only-sync bootstrap gap — sync-shared.sh cannot create it, but once present it enters the sync intersection and stays aligned. No wording change to the frozen block (byte-frozen repo-wide; re-pointing the cite would force a 30+-skill re-paste). Ordering: sequence F3 before F2 Part B; if Part B ships and the block leaves gamekit, Part B's story removes this copy.
- Rationale: the frozen block in all 7 game skills cites `_shared/non-interactive.md` (e.g. 2048/SKILL.md:64) which does not exist in gamekit — exactly the dangling-cite class CLAUDE.md flags (learnkit /book-summary bootstrap, 2026-06-12) and the same fix accepted for managerkit's shared unit (its F4).
- Blast radius: one new file in gamekit `_shared/`; enters sync-shared.sh's intersection (desired). Toolkit's lint pins its CANONICAL_FILE under pmos-toolkit and scans only SKILL.md files, so the copy cannot perturb it. No skill-body edits, no tests.

### pmos-gamekit-shared-F4 — Accepted
- Fix: game-launcher.md · directory-convention section (adjacent to the line-41 tree comment) · add one "Self-test" line: run `node serve.test.mjs --selftest` after ANY serve.js change; green = all checks passed, exit 0; `--selftest` additionally hard-asserts the check count. Per the pass-2 F8 amendment the cumulative spec writes the count as `7/7 checks passed` — land F4 with or after F8, or write "5/5" and take a two-step edit.
- Rationale: a substrate consumed by 7 skills whose only test documentation is a tree comment gets tested only when someone remembers it exists; invocation + green-definition is a deterministic §H fact belonging in the canonical home. CI wiring deliberately NOT proposed — one doc line is the right altitude for a 3-file substrate.
- Blast radius: game-launcher.md only.

**Author verdict (pass 1, authoritative): 4/4 accepted (2 should-fix, 2 nits), 0 rejected, 0 invalid.** Cumulative across both passes: 8/8 accepted. Proposal at proposal/_shared__pmos-gamekit.md rewritten as the self-contained cumulative record. No changes implemented — descriptions only.
