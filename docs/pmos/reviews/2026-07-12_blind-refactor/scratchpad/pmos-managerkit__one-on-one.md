## Pass 1 — reviewer findings

### one-on-one-F1 [Should-fix] No verb can ever close an open action item — the stale-action loop is a one-way ratchet
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/log.mjs:48
- Quote: "// Mirror still-open actions into Open action items (dated for stale tracking)."
- Problem: `log` mirrors every still-open action into the header's "Open action items" section, but no verb ever removes or completes one. `set.mjs`'s FIELD_MAP has no field for that section, `log --clear` only filters the Inbox, and logging the same action later as `done` adds a done line to the session without touching the mirrored open item. Meanwhile SKILL.md:106 forbids the only workaround ("Do not hand-edit records or echo their raw contents into a repo"). Result: after normal use, the stale-action flag — a headline feature in the skill description — fires forever and the prep artifact accumulates dead "open loops" with no sanctioned way to clear them. The core hygiene loop the skill sells is unclosable through its own verb surface.

### one-on-one-F2 [Should-fix] Any write silently destroys unknown sections in a sensitive record — contradicting the code's own "nothing is silently dropped" claim
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/record-lib.mjs:83
- Quote: "sections on read (unknown headings are kept under their own key so nothing is silently dropped),"
- Problem: parse is tolerant, but `serializeRecord` "always re-emits the canonical fixed set" (line 84) — so `writeRecord(readRecord(...))` deletes any non-canonical section. Verified empirically: appended a `## My custom notes` section to a record, ran a single `note.mjs` write, and the section was gone (grep count 0). These are hand-ownable Markdown files in `~/.pmos/` holding sensitive employee content; a manager who adds a personal section (a natural thing to do with a Markdown file in their home dir) loses it on the next quick-capture, with no warning. The comment's claim of no silent drops is true only on read and false across the read→write round-trip. Either preserve unknown sections through serialize, or refuse/warn on encountering them.

### one-on-one-F3 [Should-fix] "Commentable HTML prep artifact" is a false claim — the artifact ships no commenting machinery
- Where: plugins/pmos-managerkit/skills/one-on-one/SKILL.md:151
- Quote: "The script emits a terminal summary and writes a self-contained, commentable HTML prep artifact to"
- Problem: In this repo "commentable" is a specific contract (inline `pmos-comments` JSON block + overlay JS + launcher write path), and SKILL.md tells the user to "annotate it in the browser before the meeting" (line 152). But `reference/prep-skeleton.html` contains no `pmos-comments` block, no overlay script, and no editing affordance of any kind — its own footer says "edit freely; this is a prep aid, not a record", and the claim is repeated in the Verbs table (SKILL.md:32 "emit a commentable HTML prep artifact"). A user who opens the artifact to annotate finds a static read-only page. Either wire the comments overlay in (mindful of the INV-4 store location) or drop "commentable"/"annotate it in the browser" from the contract prose.

### one-on-one-F4 [Nit] Bare `<report>` argument is accepted but silently ignored — there is no per-report overview
- Where: plugins/pmos-managerkit/skills/one-on-one/SKILL.md:36
- Quote: "bare (`<report>` or nothing)** — the who's-due roster across all reports. → Phase [Overview](#overview)."
- Problem: The argument-hint advertises "`<report>` (bare = overview)", which reads as a report-scoped overview, but `overview.mjs` accepts no handle — verified: `overview.mjs --today 2026-07-12 t-x` prints the full roster and drops the positional arg on the floor. `/one-on-one sarah` returning everyone (not Sarah's record snapshot) is a surprising product surface; either scope the roster when a handle is supplied, or make the hint say the bare form is always the full roster.

### one-on-one-F5 [Nit] status-creep is a §H "deterministic" gate built on a fragile keyword regex
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/record-lib.mjs:178
- Quote: "const HUMAN_RE = /\b(growth|career|human|check-?in|recognition|feeling|morale|well-?being|coaching|feedback|goal|develop)\b/i;"
- Problem: A session logged as "Topics: feedback on the API spec" or "Decisions: career-page redesign shipped" matches `feedback`/`career` and clears status-creep even though the session was pure status. The classification "topic-only vs human content" is a judgment call being hard-gated by substring matching — the flag is deterministic in the §H sense but systematically wrong on common status vocabulary, and `log` offers no structured way (e.g. a `--growth`/`--human` field) to record the signal explicitly. Worth either a structured log field or an honest "advisory heuristic" framing in the plan output.

### one-on-one-F6 [Nit] Dead code in parseRecord — a computed `lines` value with an always-true filter is discarded
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/record-lib.mjs:105
- Quote: "const lines = rest.replace(/\n+$/, '').split('\n').filter((l, i, a) => !(l === '' && i === a.length));"
- Problem: `lines` is never used — line 106 recomputes `sections[name]` from `rest` with different trimming — and the filter predicate can never be false (`i` is always < `a.length`). Confusing leftover in the most safety-critical file of the skill (the byte-stability lib); delete it.

### one-on-one-F7 [Nit] SKILL.md cites a design doc (`02_design.html §5`) that ships nowhere with the skill
- Where: plugins/pmos-managerkit/skills/one-on-one/SKILL.md:153
- Quote: "Its sections are in **human-first order** (02_design.html §5): **Human first** (opener) → **Last time's open"
- Problem: `02_design.html` is a feature-pipeline artifact, cited with no path and not bundled under the skill (the same bare cite appears in record-lib.mjs, plan.mjs and prep-skeleton.html comments). For a consumer loading the SKILL.md, it's an unresolvable reference that adds tokens without information — the section list that follows is the actual content. State the ordering as the skill's own contract and drop the doc pointer (or make it a real repo path in a code comment only).

### one-on-one-F8 [Nit] Flag thresholds (21d stale, 90d career-due, 3-session creep) are invisible and untunable
- Where: plugins/pmos-managerkit/skills/one-on-one/SKILL.md:155
- Quote: "**stale-action** (an open item older than the threshold), **career-due** (no recent career conversation)"
- Problem: The manager-facing doc never says what "the threshold" or "recently" mean — the numbers live only as script defaults (record-lib.mjs 21/90/n=3) with no CLI flag or record-level override (a monthly-cadence report arguably deserves a longer stale window than a weekly one). Deterministic-by-script is right per §H, but the values are part of the user contract and should be stated in the Plan phase prose, and ideally overridable per report.

**Pass 1 verdict:** 0 blockers / 3 should-fix / 5 nits — material findings

## Pass 1 — author response

### one-on-one-F1 — Accepted
- Fix: scripts/log.mjs (mirror/clear block, ~lines 48–62) + SKILL.md Phase 4 {#log}. Before: still-open actions are appended to "Open action items" and nothing ever removes them. After: (a) add a repeatable `--close "<substr>"` flag to log.mjs that removes matching `- [ ]` lines from "Open action items" (case-insensitive substring, same contract as `--clear` for the Inbox) and reports the count; (b) auto-close: when an `--action "owner|done|text"` is logged, remove any open-item line whose text substring-matches that action's text — logging a completion closes the mirrored loop without a separate flag. SKILL.md Phase 4 step 2 documents both ("done actions auto-close their mirrored open item; `--close` closes by substring"). `--close` stays out of argument-hint (script-internal plumbing per §I, same as `--clear`).
- Rationale: the stale-action flag is a headline feature; a one-way ratchet with hand-editing forbidden (SKILL.md:106) makes it permanently red after normal use. Closure must go through the sanctioned verb surface, and `log` is the natural home (closure is a session outcome).
- Blast radius: log.mjs; SKILL.md Phase 4; tests/run-tests.sh (new close/auto-close cases); record-lib selftest untouched (staleActionFlags unchanged). No other skills/substrates.

### one-on-one-F2 — Accepted
- Fix: scripts/record-lib.mjs `serializeRecord` (line 114) + the comment at lines 81–83. Before: serialize "always re-emits the canonical fixed set", so writeRecord(readRecord(...)) deletes any non-canonical section. After: serialize emits the canonical SECTIONS in order, then appends every unknown section key (in parse insertion order) after `Sessions`, so hand-added sections survive every write round-trip. Update the comment to claim preservation across read AND write. Add selftest assertions: (a) a record with `## My custom notes` round-trips byte-stably through parse→serialize, (b) canonical records (no unknown sections) serialize identically to today (byte-stability AC1 unchanged).
- Rationale: these are hand-ownable Markdown files holding sensitive employee content in the user's home dir; silently destroying a manager's own section on the next quick-capture is data loss, and the code's own comment promises the opposite. Preserving (vs refusing) is strictly safer and keeps `note` unattended-safe (no new prompt surface).
- Blast radius: record-lib.mjs only (all verbs inherit the fix through writeRecord); selftest count grows; tests/run-tests.sh may add an integration case. Canonical-record bytes unchanged, so no downstream reader breaks.

### one-on-one-F3 — Accepted
- Fix: drop the false claim rather than wire the comments machinery. SKILL.md:32 "emit a commentable HTML prep artifact" → "emit a self-contained HTML prep artifact"; SKILL.md:151–152 "self-contained, commentable HTML prep artifact … annotate it in the browser before the meeting" → "self-contained HTML prep artifact … open it in the browser before the meeting"; scripts/plan.mjs:3 comment drops "commentable"; plan.mjs:55 terminal line "open it to annotate before the meeting" → "open it before the meeting".
- Rationale: in this repo "commentable" is a specific contract (inline pmos-comments block + overlay JS + localhost launcher write path, per the repo CLAUDE.md inline-doc-comments section). Wiring it in would drag the 14-surface pmos-toolkit fanout contract, the launcher trio, and the coverage gate into managerkit, and the artifact lives under the INV-4 store (`~/.pmos/one-on-ones/prep/`), outside the docs/pmos artifact world the comments flow assumes. The prep page is a throwaway prep aid ("not a record", its own footer says) — honest prose is the right-sized fix.
- Blast radius: SKILL.md (2 spots), plan.mjs (2 spots). No change to prep-skeleton.html markup. No tests assert "commentable" (verify with a grep before shipping).

### one-on-one-F4 — Accepted
- Fix: scripts/overview.mjs + SKILL.md Phase 7 {#overview}. Before: `overview.mjs` ignores any handle; `/one-on-one sarah` prints the full roster. After: overview.mjs accepts an optional `--handle <h>` (the skill body passes the resolved handle from Phase 0 when the bare invocation named a report); when present, print that single report's row plus its expanded flag details (stale items with ages, career-due date) instead of the whole roster; unknown handle → the existing "no 1:1 record" error. SKILL.md Phase 7 documents both forms: bare = full roster, `<report>` = that report's due-status snapshot.
- Rationale: the argument-hint advertises `<report>` as a bare form; accepting and silently discarding it is a surprising surface. Scoping (rather than rewording the hint) matches the natural reading of "/one-on-one sarah" and costs one small branch.
- Blast radius: overview.mjs; SKILL.md Phase 7; tests/run-tests.sh (scoped-overview case). argument-hint unchanged (it already says `<report>`).

### one-on-one-F5 — Accepted
- Fix: structured signal first, honest heuristic second. (a) scripts/log.mjs gains a repeatable `--growth "<text>"` field emitting a `**Growth:** …` session line; SKILL.md Phase 4 step 1 lists it ("growth/human moments" alongside topics/decisions). (b) scripts/record-lib.mjs `statusCreepFlag`/HUMAN_RE (lines 178–185): a session counts as human if it has a `**Growth:**` line, OR if HUMAN_RE matches on lines NOT starting with `**Topics:**`/`**Decisions:**` — so "Topics: feedback on the API spec" no longer clears the flag. (c) plan.mjs's flag narration adds one clause framing status-creep as keyword-based ("based on logged session fields"). Selftest gains the false-positive case from the finding (a Topics line containing "feedback" must NOT clear creep) plus a `**Growth:**` clears-it case.
- Rationale: the classification is genuinely deterministic (§H) but was systematically wrong on common status vocabulary; excluding the two status-bearing structured lines from the regex plus giving `log` an explicit growth field fixes the failure mode without turning a hard-gated flag into model judgment.
- Blast radius: record-lib.mjs (statusCreepFlag + selftest), log.mjs, SKILL.md Phase 4 and the Phase 3 flag prose, plan.mjs narration, tests/run-tests.sh.

### one-on-one-F6 — Accepted
- Fix: scripts/record-lib.mjs:105 — delete the unused `const lines = …` statement (its filter predicate `!(l === '' && i === a.length)` is vacuously true and the value is never read; line 106 recomputes from `rest`). No behavior change.
- Rationale: dead code in the byte-stability-critical parser invites misreading during future edits.
- Blast radius: none — selftest must still pass byte-identically.

### one-on-one-F7 — Accepted
- Fix: SKILL.md:153 — drop the `(02_design.html §5)` cite and state the human-first ordering as the skill's own contract (the section list that follows is the content). In code comments only (record-lib.mjs:4,22; prep-skeleton.html:8; plan.mjs header), replace the bare `02_design.html` cites with the full repo path `docs/pmos/features/2026-07-02_one-on-ones/02_design.html` (the doc exists there), keeping the §N anchors.
- Rationale: a consumer loading SKILL.md gets an unresolvable pointer that adds tokens without information; developers reading the scripts benefit from a resolvable path. This matches §K — the ordering's canonical home becomes the SKILL.md contract prose, and code comments cite provenance, not authority.
- Blast radius: SKILL.md, record-lib.mjs, plan.mjs, prep-skeleton.html (comments only — no rendered output changes). lint-phase-refs unaffected (no phase anchors touched).

### one-on-one-F8 — Accepted
- Fix: (a) SKILL.md Phase 3 {#plan} flag bullet states the defaults inline: stale-action = open item older than **21 days**, career-due = no career conversation in **90 days**, status-creep = the last **3** sessions all topic-only — cited as "script defaults". (b) scripts/plan.mjs and overview.mjs gain internal `--stale-days <n>` / `--career-days <n>` / `--creep-n <n>` flags plumbed into the existing record-lib function params (which already take thresholds), so a monthly-cadence report can be planned with a longer window via natural language; these stay out of argument-hint per §I (internal plumbing), with SKILL.md noting NL forms ("use a 30-day stale window for X") map to them.
- Rationale: the thresholds are part of the user contract (they decide when a red flag fires) and must be visible in the manager-facing doc; the record-lib functions already parameterize them, so exposure is cheap. Per-record frontmatter overrides are deliberately NOT added (schema change + migration for marginal gain — the per-invocation flag covers the stated need).
- Blast radius: SKILL.md Phase 3 + Flags section note, plan.mjs, overview.mjs; tests/run-tests.sh (threshold-override case); record-lib.mjs unchanged (params exist). lint-flags-vs-hints: new flags must carry the `<!-- nl-sugar -->`/internal-plumbing treatment already used for `--date` etc.

**Pass 1 author verdict:** 8/8 accepted (0 rejected, 0 invalid). All quotes verified against current files.

## Pass 2 — reviewer findings

### one-on-one-F9 [Should-fix] F1's `--close` is dead-on-arrival for close-only invocations — the empty-body guard fires first
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/log.mjs:22
- Quote: "die('nothing to log — supply at least one --topic/--decision/--action/--question (an empty session DEFERs under --non-interactive)', 64);"
- Problem: F1 adds `--close "<substr>"` to `log` as the sanctioned way to complete a stale open item, but the primary use case — "close that intro action" with no new session to write — is `log --handle X --close "intro"` with zero topics/decisions/actions/questions, which dies at this guard before any close processing. The proposal never amends the guard (its blast radius lists only the new flag + auto-close). Implemented exactly as described, closure remains unreachable outside a full session write, so the one-way-ratchet defect F1 set out to fix survives for the between-sessions case. The guard needs an explicit carve-out (close-only invocations are a valid record mutation, not "nothing to log") — and the DEFER prose in SKILL.md Phase 4 step 1 ("an empty session body DEFERs") must be reconciled with it so a close-only non-interactive run isn't spuriously deferred.

### one-on-one-F10 [Should-fix] F5's Topics/Decisions exclusion regresses career-conversation sessions into "topic-only"
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/career.mjs:36
- Quote: "`**Topics:** Laraway career conversation — vision + short-term plan recorded in the header.`"
- Problem: the Sessions marker `career` writes consists of exactly one body line, and it is a `**Topics:**` line — its only human signal ("career") lives inside the very line class F5 excludes from HUMAN_RE matching. Today that marker clears status-creep (HUMAN_RE hits "career" anywhere); under F5 as specced ("a session is human if it has a `**Growth:**` line, OR HUMAN_RE matches on lines NOT starting with `**Topics:**` / `**Decisions:**`") the career session has neither, so it counts toward the creep window — a manager who just ran the most human conversation the skill supports can be flagged status-creep on their next `plan`. The fix that kills one false-negative introduces a sharper false-positive. F5's implementation must either make `career` emit a non-Topics human line (e.g. a `**Growth:**` line) or treat the `(career conversation)` heading suffix as a human signal, and add a selftest case for it.

### one-on-one-F11 [Should-fix] F1's auto-close matching is under-specified against the mirrored line format — wrong-item and no-op closes are both easy
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/log.mjs:50
- Quote: "appendLine(rec, 'Open action items', `- [ ]${a.owner ? ` (${a.owner})` : ''} ${a.text} — since ${date}`.replace('  ', ' '));"
- Problem: the mirrored open-item line auto-close must match against carries owner + `— since <date>` decoration around the original text, and F1 specifies only "text substring-matches the done action's text" — no match direction, no length floor, no owner check. Two failure modes: (a) over-close — a short done text ("sync", "RFC") substring-hits multiple unrelated open items and silently deletes them all, with no flag opt-in because auto-close rides every `--action "owner|done|…"`; the repo's own anchor-resolver precedent imposes a ≥40-char floor on substring-contains precisely to avoid this, and F1 adopts none of it; (b) under-close — the done action is naturally rephrased ("wrote the RFC" vs mirrored "file the RFC"), matches nothing, and silently leaves the loop open while the manager believes logging `done` closed it. The spec needs: direction (open-line CONTAINS done-text), a minimum-length or confirm-on-multi-match rule, and a reported closed-count of 0 surfacing as a visible warning rather than silence.

### one-on-one-F12 [Should-fix] F2 closes only one of three silent-loss paths — preamble text and duplicate headings still vanish on write
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/record-lib.mjs:100
- Quote: "    if (!part.startsWith('## ')) continue;"
- Problem: F2 preserves unknown `## ` sections, but parseRecord still discards (a) any body text between the frontmatter and the first `## ` heading — this `continue` drops the preamble part entirely, so a manager's hand-added note above "Goals & growth focus" is deleted by the next `note`/`set`/`log`; and (b) the earlier of two duplicate headings — `sections[name] =` (record-lib.mjs:106) overwrites, so a hand-edited record with a second `## Inbox` loses the first block's lines. Both are the same defect class F2 was accepted for (silent data loss in a sensitive, hand-ownable record) and F2's own rationale legitimizes hand edits, so its implementation should either extend preservation to these paths (keep a synthetic preamble key; merge duplicate-heading lines) or make parseRecord refuse/warn on them — plus selftest cases. As proposed, F2's updated comment would claim "preservation across the full read→write round-trip" while these two paths still lose bytes, making the comment newly false.

### one-on-one-F13 [Nit] Post-F1/F2 the skill's hand-edit posture is self-contradictory — SKILL.md needs one ruling
- Where: plugins/pmos-managerkit/skills/one-on-one/SKILL.md:105
- Quote: "refuses to write there (INV-4). Do not hand-edit records or echo their raw contents into a repo, PR, or artifact"
- Problem: F1's rationale leans on this sentence as a prohibition ("SKILL.md:106 forbids hand-editing records"), while F2's rationale treats hand-added sections as sanctioned content worth preserving ("a manager's hand-added section from a sensitive record"). Implemented together they leave the contract incoherent: the prose forbids what the serializer is being hardened to protect. The sentence also reads ambiguously — "hand-edit records or echo their raw contents *into a repo…*" may scope the prohibition to repo leakage only. Whichever posture is intended (records are hand-ownable and scripts must tolerate edits, vs. scripts are the only mutation path), state it once, unambiguously, in Store & privacy, and align F1/F2's framing to it.

### one-on-one-F14 [Nit] F5's exclusion doesn't cover action bullets — status-y `- [ ]` lines still keyword-clear creep
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/log.mjs:41
- Quote: "for (const a of acts) block.push(`- [${a.status === 'done' ? 'x' : ' '}]${a.owner ? ` (${a.owner})` : ''} ${a.text}`.trimEnd());"
- Problem: session action items are emitted as plain `- [ ]` bullets, not `**`-prefixed structured lines, so they fall outside F5's `**Topics:**`/`**Decisions:**` exclusion and remain HUMAN_RE-matchable. A purely transactional action like "(me) give feedback on the API doc" or "draft Q3 goals deck" still clears status-creep on an otherwise status-only session — the exact false-negative F5 was accepted to eliminate, surviving through a different line class. If F5 ships, either extend the exclusion to `**Actions:**`-block bullets or acknowledge the residual in the flag's SKILL.md prose so the contract isn't oversold.

### one-on-one-F15 [Nit] F4's "existing 'no 1:1 record' error path" doesn't exist in overview.mjs — an unknown --handle would stack-trace
- Where: plugins/pmos-managerkit/skills/one-on-one/scripts/record-lib.mjs:133
- Quote: "if (!existsSync(p)) throw new Error(`no 1:1 record for '${handle}' at ${p} — run /one-on-one add ${handle} first`);"
- Problem: F4 routes an unknown handle to "the existing 'no 1:1 record' error path", but overview.mjs has no such path — it only maps over listHandles() (which are all existing records) and never calls die() for a missing record. The message above is a thrown Error from record-lib; called directly for a user-supplied `--handle`, it surfaces as an uncaught-exception stack trace, unlike every other verb, which wraps the check in `die(…, 65)`. A literal implementer will ship the stack trace. F4 should specify: recordExists() check + die() with exit 65, matching the sibling verbs' contract.

**Pass 2 verdict:** 0 blockers / 4 should-fix / 3 nits — material findings

## Pass 2 — author response

### one-on-one-F9 — Accepted
- Fix: scripts/log.mjs (empty-body guard, line 21–23) + SKILL.md Phase 4 step 1. Before: guard dies when topics/decisions/actions/questions are all empty, making a close-only `log --handle X --close "intro"` unreachable. After: the guard's emptiness test also counts `--close` (and F5's `--growth`): die only when topics+decisions+actions+questions+close+growth are ALL empty; a close-only invocation skips the session-block build entirely (no `### <date>` heading is written — closing an item between sessions is a record mutation, not a session) and the summary line reports "closed N open item(s)". SKILL.md Phase 4 DEFER prose amended: "an empty session body with no --close DEFERs under --non-interactive; a close-only invocation is valid and writes no session entry."
- Rationale: F1's headline use case is "close that stale item" with nothing new to log; as specced it died at the guard. Not writing a dateless/empty session block also keeps parseSessions and status-creep counts unpolluted.
- Blast radius: log.mjs (guard + conditional block build), SKILL.md Phase 4 (step 1 DEFER prose + step 2), tests/run-tests.sh (close-only case: exits 0, closes item, adds no session heading).

### one-on-one-F10 — Accepted
- Fix: scripts/career.mjs (Sessions marker, line 36) + record-lib.mjs statusCreepFlag + selftest. Before: the career marker's only body line is a `**Topics:**` line, which F5's exclusion would stop matching — a just-recorded career conversation would count toward status-creep. After: career.mjs emits a `**Growth:** Laraway career conversation — vision + short-term plan recorded in the header.` line instead of the `**Topics:**` line (the marker IS the human signal; Growth is the semantically honest field and F5 already defines `**Growth:**` as an unconditional human marker). Belt-and-braces: statusCreepFlag additionally treats a session whose heading carries the `(career conversation)` suffix as human, so pre-existing records written by today's career.mjs stay clear without migration. Selftest gains both cases: a `### <date> (career conversation)` + `**Topics:** Laraway…` session (old format) clears creep; the new `**Growth:**` marker clears creep.
- Rationale: the fix that killed the Topics false-negative must not flag the single most human conversation the skill supports; covering the legacy heading suffix avoids a data migration for existing stores.
- Blast radius: career.mjs (one line), record-lib.mjs (statusCreepFlag + 2 selftest cases), tests/run-tests.sh (career-then-plan integration case). No SKILL.md change beyond F5's already-planned Phase 3 prose.

### one-on-one-F11 — Accepted
- Fix: tighten F1's auto-close/--close matching spec in scripts/log.mjs + SKILL.md Phase 4 step 2. Before: "text substring-matches" with no direction, floor, or feedback. After, specified exactly: (a) direction — an open-item line closes when the line's TEXT PORTION (owner prefix and `— since <date>` suffix stripped before comparison) case-insensitively CONTAINS the done-action's text (for auto-close) or the `--close` argument; (b) floor — auto-close requires the done text to be ≥12 chars after trim; shorter done texts skip auto-close with a stderr note ("done action '<t>' too short to auto-close — use --close"); explicit `--close` has no floor (deliberate user intent, same as --clear); (c) multi-match — auto-close closes at most ONE item; if ≥2 open items match, close none and print a stderr warning listing the candidates ("ambiguous — close explicitly with --close"); explicit `--close` closes all matches (mirrors --clear semantics); (d) zero-match — when a done action or a --close arg matches nothing, print a visible `WARN: no open item matched '<t>' — still open` line, never silence; owner is NOT part of matching (mirrored lines may lack owners).
- Rationale: unconfirmed deletion riding every `done` action needs a conservative bias (close at most one, warn on ambiguity/zero) — the repo's anchor-resolver ≥40-char precedent is the right instinct but too strict for short action texts, so a 12-char floor + single-match rule + loud misses gets the same safety with usable recall. `--close` stays permissive because it is explicit.
- Blast radius: log.mjs only (matching helper + warnings), SKILL.md Phase 4 step 2 (documents the four rules in two sentences), tests/run-tests.sh (over-close-blocked, under-close-warned, strip-decoration-match cases).

### one-on-one-F12 — Accepted
- Fix: extend F2's preservation to the two remaining loss paths in scripts/record-lib.mjs parseRecord/serializeRecord. Before: (a) preamble text between frontmatter and the first `## ` heading is dropped by the `continue` at line 100; (b) a duplicate `## ` heading overwrites the earlier block at line 106. After: (a) parseRecord captures any non-empty pre-heading body part under a reserved synthetic key (e.g. `__preamble`, impossible as a heading name), and serializeRecord re-emits it verbatim between the frontmatter and the first section; (b) duplicate headings MERGE — `sections[name]` becomes the concatenation (first block's lines, then subsequent blocks' lines, one per parse order) so no bytes vanish; serialize naturally emits one merged section (a normalization, not a loss — documented in the comment). The F2 comment rewrite claims preservation across the full round-trip including preamble and duplicate-heading content. Selftest gains: preamble round-trips; duplicate `## Inbox` merges with zero line loss; canonical records (no preamble, no dups) still byte-stable (AC1).
- Rationale: same defect class F2 was accepted for; shipping F2's comment without these paths would make the comment newly false. Merge (vs preserve-as-duplicate) is chosen because the section map keyed by name cannot represent duplicates and every helper (appendLine/prependLine) assumes one block per name.
- Blast radius: record-lib.mjs only (parse, serialize, comment, +3 selftest cases); all verbs inherit via writeRecord. Canonical bytes unchanged.

### one-on-one-F13 — Accepted
- Fix: SKILL.md Store & privacy (lines 103–107) — one explicit ruling. Before: "Do not hand-edit records or echo their raw contents into a repo, PR, or artifact that could be committed" reads as a hand-edit ban and contradicts F2/F12's hardening. After: split into two sentences with distinct scopes — (1) leakage (hard rule): "Never echo a record's raw contents into a repo, PR, or committable artifact; the prep artifact is written under the store's prep/ dir for the same reason." (2) mutation posture (soft rule): "The scripts are the sanctioned mutation path — they keep the record canonical and the flags accurate. Hand edits are tolerated (the parser and serializer preserve unfamiliar sections, preamble text, and merge duplicate headings) but not the primary interface; prefer the verbs." F1's rationale framing in this proposal is restated against the new wording (closure must exist in the verb surface because verbs are primary, not because hand-editing is forbidden).
- Rationale: the two concerns (privacy leakage vs mutation discipline) were fused into one ambiguous sentence; F1 and F2 each leaned on a different reading. One ruling, stated once in Store & privacy (§K), aligns both fixes.
- Blast radius: SKILL.md Store & privacy only; proposal-internal rationale text for F1/F2. No scripts, lints, or tests.

### one-on-one-F14 — Accepted
- Fix: extend F5's exclusion in record-lib.mjs statusCreepFlag. Before (as F5 specced): only `**Topics:**`/`**Decisions:**` lines excluded from HUMAN_RE, so `- [ ] (me) give feedback on the API doc` action bullets still keyword-clear creep. After: HUMAN_RE is evaluated only on lines that are NOT `**Topics:**`, NOT `**Decisions:**`, NOT the bare `**Actions:**` header, and NOT checkbox bullets (`/^- \[[ x]\]/`) — i.e. keyword matching applies to `**Questions:**` lines and free-form lines only, while `**Growth:**` (and F10's career-heading suffix) remain unconditional human signals. SKILL.md Phase 3 flag prose states the contract honestly: "status-creep looks at Growth lines and human keywords in questions/free notes; topics, decisions, and action bullets never count." Selftest gains the action-bullet false-negative case from the finding.
- Rationale: the false-negative F5 was accepted to eliminate survives verbatim through the Actions line class; excluding all structured status carriers makes the heuristic's boundary describable in one sentence.
- Blast radius: record-lib.mjs (statusCreepFlag + 1 selftest case), SKILL.md Phase 3 prose (already being edited under F5/F8). No new files.

### one-on-one-F15 — Accepted
- Fix: make F4's error path explicit in scripts/overview.mjs. Before (as F4 specced): "unknown handle → the existing 'no 1:1 record' error path" — but overview.mjs has no such path; readRecord's thrown Error would surface as an uncaught stack trace. After, specified exactly: when `--handle <h>` is supplied, guard with `recordExists(h)` and on failure call `die(\`no 1:1 record for '${h}' — run /one-on-one add ${h} first\`, 65)` — the same message shape and exit code 65 used by log.mjs/career.mjs/note.mjs/set.mjs (overview.mjs imports die from cli-lib, which it currently does not). tests/run-tests.sh gains an unknown-handle case asserting exit 65 and no stack trace on stderr.
- Rationale: a literal implementer of F4 would ship an uncaught exception; sibling verbs establish the die(…, 65) contract and overview must match it.
- Blast radius: overview.mjs (import + guard), tests/run-tests.sh. F4's SKILL.md Phase 7 text unchanged.

**Pass 2 author verdict:** 7/7 accepted (0 rejected, 0 invalid). All pass-2 quotes verified verbatim against current files (log.mjs:22,50,41; career.mjs:36; record-lib.mjs:100,133; SKILL.md:105).

## Pass 3 — reviewer findings
### one-on-one-F16 [Should-fix] Auto-close (F1) never specifies evaluation order vs same-invocation mirroring — a just-mirrored open item can be wrongly closed or spuriously flagged ambiguous
- Where: scripts/log.mjs:50 (and proposal §F1+F9+F11)
- Quote: "appendLine(rec, 'Open action items', `- [ ]${a.owner ? ` (${a.owner})` : ''} ${a.text} — since ${date}`.replace('  ', ' '));"
- Problem: One `log` invocation both mirrors new still-open actions into "Open action items" and (post-F1) auto-closes on every `done` action. The proposal's matching rules (direction, floor, multi-match, zero-match) are precise, but it never says whether auto-close scans the open-items list BEFORE or AFTER this invocation's own mirrors land. If mirroring runs first (the natural place, given it's already at log.mjs:49–51 and closes would be appended logic), then `--action "sarah|done|file the RFC" --action "sarah|open|file the RFC follow-up"` in one session makes the just-added mirror's text portion contain the done text → per F11's multi-match rule the pre-existing genuinely-stale "file the RFC" item plus the brand-new one are 2 candidates → close NONE and warn "ambiguous", or with no pre-existing match the new item is closed the instant it was created. Both outcomes are wrong and both are conforming implementations of the proposal as written. The spec needs one sentence — auto-close matches only against the open-items list as it stood at invocation start (pre-mirror snapshot) — plus a test case pairing a done action with a same-invocation open action whose text contains it.

### one-on-one-F17 [Should-fix] Destructive mutation paths (`add --force`, `set --replace`) have no destructive-confirm posture or backup on an unversioned sensitive store — untouched by the proposal
- Where: scripts/add.mjs:18
- Quote: "a 1:1 record already exists at ${recordPath(f.handle)} — use a different verb (note/set/log) or --force to re-scaffold"
- Problem: `add --force` rebuilds the record from `emptyRecord(...)` — every session, all performance/coaching feedback, the career plan, gone in one write. `set --replace` (SKILL.md:179 "pass a replace intent to swap a whole section") wipes a whole accreted feedback history to one line. The store is DELIBERATELY outside any git tree (INV-4), so there is no history, no reflog, no undo — destruction is final. Yet the skill body carries no `<!-- defer-only: destructive -->` confirm anywhere (the only defer-only tag is the Phase 1 free-form create-person prompt at SKILL.md:122), no "confirm before re-scaffold/replace" step, and no `.bak` write-aside in the scripts. The entire F2/F12/F13 arc is "never silently lose bytes from this sensitive record," and it hardens the parser while leaving two sanctioned verbs that vaporize the same record wholesale with zero friction. Minimum fix: an interactive confirm (tagged destructive → DEFERs under `--non-interactive`) in the skill body before passing `--force` or `--replace`, and/or a one-file timestamped backup in the store before either destructive write.

### one-on-one-F18 [Nit] F9's guard carve-out covers `--close` but not `--clear` — the "between-sessions record mutation" rationale applies identically to inbox clearing, leaving an arbitrary asymmetry
- Where: scripts/log.mjs:54 (and proposal line 38)
- Quote: "const clears = asArray(f.clear).map((c) => String(c).toLowerCase());"
- Problem: The proposal legitimizes close-only invocations on the rationale that "closing between sessions is a record mutation, not a session," and enumerates the empty-body guard as topics+decisions+actions+questions+close+growth. `--clear` is the exact same category — an inbox item resolved asynchronously between 1:1s ("she got the legal review unblocked over Slack") — yet a `--clear`-only invocation still dies at the "nothing to log" guard because clear processing (log.mjs:54–62) sits below the die and isn't in the F9 list. Either add `clear` to the carve-out (a clear-only run mutates the Inbox, writes no session heading, reports "cleared N") or state why closing an action is between-sessions-legal but clearing an inbox line is not; silence here just recreates the F1 one-way-friction complaint one section lower.

### one-on-one-F19 [Nit] F10's heading-suffix rule is checked against data parseSessions throws away — blast radius omits the required parseSessions change
- Where: scripts/record-lib.mjs:172
- Quote: "if (m) { cur = { date: m[1], lines: [] }; out.push(cur); }"
- Problem: The F5+F10 statusCreepFlag rewrite keys one clause on "its heading carries the `(career conversation)` suffix," but parseSessions captures only `m[1]` (the date) and never retains the heading line or its suffix — an implementer who writes `s.lines.some(l => /\(career conversation\)/.test(l))` matches nothing, silently, because headings never enter `lines`. The proposal's own legacy-heading selftest case would catch the miss (good), but the blast radius says only "record-lib.mjs (statusCreepFlag + ~5 selftest cases)" and never names parseSessions, whose return shape must grow a heading/suffix field (an additive change — no downstream consumer of `{date, lines}` breaks). One line in the change description ("parseSessions additionally returns the heading text / a career marker boolean") closes the gap.

**Pass 3 verdict:** 0 blockers / 2 should-fix / 2 nits — material findings

## Pass 3 — author response

### one-on-one-F16 — Accepted
- Fix: scripts/log.mjs (F1's auto-close spec) + SKILL.md Phase 4 step 2 + tests. Before: the F1+F11 rules never say whether auto-close scans "Open action items" before or after this invocation's own mirrors land, so a same-invocation done+open pair ("file the RFC" done + "file the RFC follow-up" open) either spuriously trips the multi-match ambiguity warning or closes the just-created mirror. After, one added sentence to the spec: auto-close (and explicit `--close`) match ONLY against the open-items list as it stood at invocation start — take a snapshot of the section's lines before any mirroring, run all close matching against that snapshot, then apply removals + this invocation's new mirrors in one write. A just-mirrored item is never a close candidate. tests/run-tests.sh gains the finding's exact case: one invocation with `--action "sarah|done|file the RFC"` + `--action "sarah|open|file the RFC follow-up"` against a record whose open items contain a stale "file the RFC" → the stale item closes, the new follow-up mirror survives, no ambiguity warning.
- Rationale: both wrong outcomes are conforming implementations of the pass-2 spec as written; a pre-mirror snapshot is the only ordering under which "close what was stale when you invoked" is unambiguous.
- Blast radius: log.mjs (ordering of the close/mirror blocks — pure spec tightening, no new surface), SKILL.md Phase 4 step 2 (one clause: "done actions close only items that existed before this log"), tests/run-tests.sh (+1 case). No record-lib change.

### one-on-one-F17 — Accepted
- Fix: two layers, skill body + scripts. (a) SKILL.md: before the skill ever passes `--force` (Phase 1 add) or `--replace` (the set phase), it asks an AskUserQuestion confirm quoting what will be destroyed ("re-scaffolding erases N sessions / replacing wipes the current <section> content — proceed?"), tagged `<!-- defer-only: destructive -->` as the literal previous non-empty line (per SKILL.md:80's own tag contract), so a `--non-interactive` run DEFERs instead of destroying. (b) scripts/add.mjs and scripts/set.mjs: immediately before a `--force` re-scaffold or `--replace` section swap, copy the current record to `<store>/backups/<handle>-<YYYYMMDD-HHMMSS>.md` (inside the INV-4 store, so no repo leakage; mkdir -p on first use) and print the backup path in the summary line. Non-destructive paths (plain add, append-mode set) write no backup. SKILL.md Store & privacy gains one sentence noting destructive verbs write a timestamped backup under `backups/` and that the user owns pruning it.
- Rationale: the store is deliberately un-versioned (INV-4) — no git history, no undo; the whole F2/F12/F13 arc hardens against silent byte loss while two sanctioned verbs vaporize the record wholesale with zero friction. Confirm-in-body covers the skill surface; the script-side backup covers direct CLI use and makes even a confirmed mistake recoverable.
- Blast radius: add.mjs, set.mjs, SKILL.md (Phase 1, set phase, Store & privacy), tests/run-tests.sh (backup-written-on-force, backup-written-on-replace, no-backup-on-append cases), tools/audit-recommended.sh must stay green (new defer-only tag follows the literal-previous-line shape). record-lib.mjs unchanged.

### one-on-one-F18 — Accepted
- Fix: extend F9's guard carve-out in scripts/log.mjs + SKILL.md Phase 4. Before: the F9 emptiness list is topics+decisions+actions+questions+close+growth, so a `--clear`-only invocation ("she unblocked the legal review over Slack") still dies at the "nothing to log" guard even though clear processing sits just below it. After: `clear` joins the list — die only when ALL of topics/decisions/actions/questions/close/growth/clear are empty; a clear-only (or clear+close-only) invocation is a valid between-sessions record mutation: it writes no `### <date>` session heading and the summary reports "cleared N inbox item(s)". SKILL.md Phase 4 DEFER prose becomes: "an empty session body with no --close and no --clear DEFERs under --non-interactive; close-only and clear-only invocations are valid and write no session entry."
- Rationale: the between-sessions rationale F9 established applies identically to the Inbox; leaving `--clear` out is an arbitrary asymmetry that recreates F1's one-way-friction complaint one section lower.
- Blast radius: log.mjs (one term in the guard predicate + conditional block build already required by F9), SKILL.md Phase 4 step 1 prose, tests/run-tests.sh (clear-only case: exits 0, clears item, adds no session heading).

### one-on-one-F19 — Accepted
- Fix: name the parseSessions change explicitly in the F5+F10 change description. Before: F10's legacy clause keys on "heading carries the `(career conversation)` suffix," but parseSessions (record-lib.mjs:172) captures only `m[1]` — the heading line never enters `lines`, so a literal implementer's suffix test matches nothing, silently. After: parseSessions' return shape grows additively — each entry becomes `{date, heading, lines}` where `heading` is the full `### …` line (equivalently a derived `career: /\(career conversation\)/.test(heading)` boolean; the heading field is the more general choice) — and statusCreepFlag's legacy clause tests `s.heading`. No downstream consumer of `{date, lines}` breaks (additive field). The record-lib.mjs:165 comment ("Returns [{date, lines}]") is updated to the new shape. F5/F10's blast radius now names parseSessions + its comment; the already-planned legacy-heading selftest case remains the enforcement.
- Rationale: the pass-2 spec was unimplementable as written against data the parser throws away; one sentence in the change description closes the gap before an implementer ships a silent no-op clause.
- Blast radius: record-lib.mjs (parseSessions + shape comment + statusCreepFlag — same file/functions F5/F10 already touch), no new files or tests beyond the selftest cases already specced.

**Pass 3 author verdict:** 4/4 accepted (0 rejected, 0 invalid). All pass-3 quotes verified verbatim against current files (log.mjs:50,54; add.mjs:18; record-lib.mjs:172); supporting claims spot-checked (set.mjs:32 --replace overwrite, emptyRecord re-scaffold in add.mjs, single defer-only tag at SKILL.md:122, parseSessions dropping the heading line).

## Pass 4 — reviewer findings

Method note: simulated the cumulative proposal (F1–F19) as implemented exactly as written, then hunted for remaining gaps, fix-introduced regressions, and vague items. All files read in full; empirical checks run read-only against the live scripts.

### one-on-one-F20 [Should-fix] Unknown frontmatter keys are still silently dropped — the preservation arc (F2/F12/F13) doesn't cover the frontmatter at all
- Where: scripts/record-lib.mjs:121 (loop at 118–123)
- Quote: "if (has && fm[k] !== undefined && fm[k] !== '') fmLines.push(`${k}: ${fm[k]}`);"
- Problem: `parseRecord` captures EVERY `key: value` frontmatter line into `fm` (line 91's regex is open-ended), but `serializeRecord` iterates only the fixed `FM_KEYS` array — so a hand-added frontmatter key (e.g. `pronouns: she/her`, `timezone: PST`) is silently deleted on the next `note`/`set`/`log` write. Verified empirically: a record parsed with `pronouns` in `fm` serializes without it. This is the exact defect class F2/F12 fixed for the body (silent loss of hand-added content in a sensitive, un-versioned store), but the accepted changes enumerate only "unknown sections, preamble, duplicate headings" — the frontmatter path is untouched. Worse, F13's new tolerance sentence ("the parser and serializer preserve unfamiliar sections, preamble text, and merge duplicate headings… hand edits are tolerated") would ship as a partial overclaim: a hand edit to the frontmatter is still eaten. Also in the same class: frontmatter lines that don't match the `^([A-Za-z0-9_]+):` shape (comments, continuation lines) are dropped at parse with no synthetic capture. Fix belongs in the same F2/F12 commit: emit unknown fm keys after the canonical FM_KEYS (insertion order), add a selftest, and only then is the F13 sentence true.

### one-on-one-F21 [Should-fix] The auto-close zero-match WARN fires (with a false claim) on every done action that was never mirrored — a completely normal flow
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__one-on-one.md:47
- Quote: "a done action or `--close` arg that matches nothing prints a visible `WARN: no open item matched '<t>' — still open` line, never silence"
- Problem: the rule treats every zero-match as a suspected miss, but the most common zero-match is benign: a `--action "owner|done|text"` for work that was agreed and completed within the same week (or logged in the very first session) — it never existed in "Open action items", so there is nothing to close. Under the spec as written, every such log emits `WARN: … — still open`, which is (a) factually wrong — the item is not open anywhere, it was just logged as done — and (b) chronic noise that trains the manager to ignore WARNs, defeating the conservative-bias rationale the same F11 block argues for. The wrong-wording half is certain; the noise half hits any team that completes actions between 1:1s. Fix: keep the loud WARN for explicit `--close` (user asserted a match exists), but soften auto-close zero-match to an informational line ("no prior open item matched '<t>' — nothing auto-closed") or drop the "still open" clause entirely.

### one-on-one-F22 [Nit] "`__preamble`, impossible as a heading name" is false — `## __preamble` is a legal heading that parses to exactly that key
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__one-on-one.md:63
- Quote: "capture any non-empty pre-heading body part under a reserved synthetic key (e.g. `__preamble`, impossible as a heading name)"
- Problem: `parseRecord` derives section names via `part.slice(3, nl).trim()`, so a hand-written `## __preamble` heading yields the key `__preamble` and collides with the synthetic slot — the F12 merge rule would then concatenate real preamble text with the heading's block, and serialize would re-emit the union as preamble, silently deleting the `## __preamble` heading line itself (bytes vanish, contradicting the fix's own goal). The spec is also vague enough to implement double-emission (once as preamble, once in the "every unknown section key" loop after Sessions) unless the implementer remembers to exclude the synthetic key. Two one-line hardenings: use a key no `.trim()`ed heading can produce (e.g. one containing a newline or a leading space, `'\n__preamble'`), and state explicitly that the unknown-section emission loop skips the synthetic key. Add a selftest with a literal `## __preamble` heading.

### one-on-one-F23 [Nit] Explicit `--close` has no floor and closes ALL matches on an un-versioned store, and log's line-removals sit outside F17's backup layer
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-managerkit__one-on-one.md:45
- Quote: "Explicit `--close` has no floor (deliberate user intent, same as `--clear`)."
- Problem: combined with the multi-match rule one line later ("Explicit `--close` closes all matches (mirrors `--clear` semantics)"), a one-character `--close e` deletes essentially every open item in one write — including hand-added open items that exist nowhere else in the record (mirrored items at least survive in the session log; hand-added ones don't). F17 argues "destruction on an un-versioned sensitive store is final" and adds backups for `add --force` / `set --replace`, yet the new bulk-deletion surface introduced by the same proposal gets neither a confirm nor a backup. I'm not re-litigating the no-floor decision (it's explicit and reasoned); the cheap harmonization is to extend F17's backup trigger to any log invocation whose close/clear matching removes ≥2 lines, or to have `--close` echo the removed lines to stdout so they're recoverable from the transcript. Costs one condition in the F17 code path already being written.

### one-on-one-F24 [Nit] parseArgs silently corrupts any value that begins with `--` — `note` writes the literal line "- true" and loses the user's text
- Where: scripts/cli-lib.mjs:15
- Quote: "      if (next === undefined || next.startsWith('--')) {"
- Problem: a value token starting with `--` is treated as the next flag, so the preceding flag becomes boolean `true`. Concretely: `note.mjs --handle sarah --text "--urgent: legal blocker"` passes the `!f.text` guard (true is truthy) and appends the line `- true` to the inbox — the sensitive item's content is silently discarded and replaced with garbage, and the trailing words are re-parsed as bogus flags. Today the skill body constructs the args so exposure is low, but the proposal widens the free-text flag surface (`--close "<substr>"`, `--growth "<text>"`), and dashes are natural in coaching prose. A standard `--` end-of-flags marker or a per-flag "expects a value" set in `parseArgs` (value-typed flags consume the next token unconditionally) closes it in ~3 lines; at minimum the string-typed verbs should reject a boolean-true value instead of serializing it.

### one-on-one-F25 [Nit] The frozen block's `_shared/non-interactive.md` cite dangles in this plugin — bootstrap-copy the file, never edit the block
- Where: SKILL.md:92
- Quote: "sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`)"
- Problem: `plugins/pmos-managerkit/skills/_shared/` contains only `interview-guidelines/` — the cited `non-interactive.md` does not exist in this plugin, so for a managerkit reader "Section D of this file" points at a file that isn't there (the canonical copy lives only in pmos-toolkit's `_shared/`). Nothing breaks functionally (the audit tool runs from pmos-toolkit), and the block itself is byte-frozen — so the fix is emphatically NOT a text edit (that would break the repo-wide byte-identity contract enforced by lint-non-interactive-inline.sh). The repo's own doctrine for consumer plugins citing a canonical cross-plugin protocol is to place a byte-identical copy in the consumer's `_shared/` manually, after which sync-shared.sh keeps it aligned. One-time file copy; flagging it here because the release-time coherence gate is the only other thing that would ever surface it.

**Pass 4 verdict:** 0 blockers / 2 should-fix / 4 nits — material findings

## Pass 4 — author response (FINAL — review loop closed)

### one-on-one-F20 — Accepted
- Fix: scripts/record-lib.mjs, same commit as F2/F12. Before: parseRecord's frontmatter regex (line 91) captures EVERY `key: value` line into `fm`, but serializeRecord iterates only FM_KEYS (line 118), so a hand-added `pronouns: she/her` is silently deleted on the next write; frontmatter lines not matching the `^([A-Za-z0-9_]+):` shape are dropped at parse with no capture at all. After: (a) serializeRecord, after the FM_KEYS loop, emits every fm key NOT in FM_KEYS in parse insertion order (`key: value`, same formatting as canonical keys); (b) parseRecord captures non-matching frontmatter lines verbatim under a reserved synthetic key (same unproducible-key convention as F22 fixes for the body, e.g. `'\n__fm_raw'`) and serializeRecord re-emits them after the unknown keys, so comments/continuation lines survive too; (c) F13's tolerance sentence gains "frontmatter keys" in its preserved-content list — it ships only when this lands, keeping the sentence true. Selftest: a record with `pronouns: she/her` plus a non-key-shaped fm line round-trips byte-stably; canonical records (no unknown fm content) serialize byte-identically to today (AC1).
- Rationale: exact defect class F2/F12 were accepted for — silent loss of hand-added content in a sensitive un-versioned store — and shipping F13's "hand edits are tolerated" sentence without this makes it a partial overclaim.
- Blast radius: record-lib.mjs only (serializeRecord fm loop + parseRecord fm capture + comment + 2 selftest cases); all verbs inherit via writeRecord. Canonical bytes unchanged.

### one-on-one-F21 — Accepted
- Fix: amend F11's zero-match rule in the F1 spec (log.mjs + SKILL.md Phase 4 step 2). Before: every zero-match — including the completely normal "agreed and completed within the same week, never mirrored" done action — prints `WARN: no open item matched '<t>' — still open`, which is factually false (the item is not open anywhere) and chronic noise that trains WARN-blindness. After, split by intent: explicit `--close` that matches nothing keeps the loud `WARN: no open item matched '<t>'` (the user asserted a match exists) but drops the false "— still open" clause; auto-close zero-match prints an informational stderr line `note: no prior open item matched '<t>' — nothing auto-closed` (not WARN). tests/run-tests.sh: never-mirrored done action → exit 0, "note:" line, no "WARN"; --close miss → "WARN" line.
- Rationale: the conservative-bias design F11 argues for depends on WARN meaning something; the wording fix is unambiguous (the "still open" claim is simply false in the benign case) and the noise fix preserves the never-silent guarantee at the right volume.
- Blast radius: log.mjs (two message sites in the close helper), SKILL.md Phase 4 step 2 (one clause), tests/run-tests.sh (message-tier assertions on the two zero-match cases already specced).

### one-on-one-F22 — Accepted
- Fix: harden F12's synthetic-preamble spec in scripts/record-lib.mjs. Before: the proposal claimed `__preamble` is "impossible as a heading name" — false: `part.slice(3, nl).trim()` (line 102) parses a hand-written `## __preamble` heading to exactly that key, colliding with the synthetic slot (merge would fuse real preamble with the heading's block and serialize would delete the heading line — bytes vanish); the spec also permits double-emission unless the unknown-section loop excludes the synthetic key. After: (a) the synthetic key is one no trimmed heading can produce — `'\n__preamble'` (contains a newline); (b) stated explicitly: the unknown-section emission loop in serializeRecord SKIPS the synthetic preamble key (it is emitted once, between frontmatter and the first section, never again after Sessions); (c) F20's raw-frontmatter capture uses the same convention (`'\n__fm_raw'`). Selftest: a record containing a literal `## __preamble` heading round-trips with the heading preserved as an ordinary unknown section AND distinct from real preamble text above it.
- Rationale: the fix's own goal is zero byte loss; a colliding key re-introduces loss through the hardening itself. One-character-class change plus one explicit sentence closes both implementable-wrong readings.
- Blast radius: record-lib.mjs (key constant + one loop-exclusion clause + 1 selftest case) — same F2/F12 commit, no new surface.

### one-on-one-F23 — Accepted
- Fix: harmonize F17's backup layer with the new bulk-deletion surface in log.mjs. Before: a one-character `--close e` closes ALL matches with no floor (explicit-intent rule, not re-litigated), and hand-added open items removed this way exist nowhere else in the record — yet F17's backup covers only add --force / set --replace. After: (a) any log invocation whose close/clear matching removes ≥2 lines in total writes the same `<store>/backups/<handle>-<YYYYMMDD-HHMMSS>.md` backup before the write, printing the path (one condition reusing the F17 helper); (b) regardless of count, every line removed by --close/auto-close/--clear is echoed to stdout under a "closed:"/"cleared:" prefix, so even a single-line removal is recoverable from the transcript. Non-removing log invocations write no backup. SKILL.md Store & privacy's F17 sentence generalizes to "destructive operations (re-scaffold, section replace, multi-line close/clear) write a timestamped backup under backups/".
- Rationale: F17's own argument — destruction on an un-versioned store is final — applies verbatim to bulk close; the fix costs one condition in a code path F17 already builds and keeps `--close`'s permissive semantics intact.
- Blast radius: log.mjs (backup condition + echo lines — helper shared with F17), SKILL.md Store & privacy (one generalized sentence), tests/run-tests.sh (backup-on-multi-close, no-backup-on-single-close-but-echoed cases).

### one-on-one-F24 — Accepted
- Fix: scripts/cli-lib.mjs parseArgs + the string-typed verbs. Before: a value token starting with `--` is treated as the next flag (line 15), so `note --text "--urgent: legal blocker"` sets `text = true`, passes the `!f.text` guard, and appends the literal line `- true` — sensitive content silently discarded, trailing words re-parsed as bogus flags. After: (a) parseArgs gains an opts.valued Set (mirroring opts.multi); a flag in that set consumes the next token unconditionally — each verb declares its value-typed flags (text, close, growth, topic, decision, action, question, handle, name, role, cadence, date, field, value, …); (b) belt-and-braces: string-consuming verbs reject a boolean `true` where a string is required, with die(…, 64) naming the flag ("--text requires a value; values beginning with -- must be quoted and declared") instead of serializing garbage. tests/run-tests.sh: the finding's exact case — note --text "--urgent: legal blocker" round-trips the full string into the Inbox.
- Rationale: the proposal itself widens the free-text flag surface (--close, --growth) and dashes are natural in coaching prose; silently replacing a sensitive note's content with "true" is the same silent-loss class as F2/F12/F20. The valued-set is ~3 lines and preserves existing bare-boolean flags (--force, --replace, --selftest) unchanged.
- Blast radius: cli-lib.mjs (valued-set branch), each verb's parseArgs call site (declare valued flags — mechanical), tests/run-tests.sh (+1 case). No record-format change.

### one-on-one-F25 — Accepted
- Fix: one-time bootstrap file copy, NO text edits. Create `plugins/pmos-managerkit/skills/_shared/non-interactive.md` as a byte-identical copy of pmos-toolkit's canonical `skills/_shared/non-interactive.md`, so SKILL.md:92's frozen-block cite ("Section D of this file (`_shared/non-interactive.md`)") resolves within this plugin. The frozen inline block in SKILL.md is NOT touched (byte-identity enforced by lint-non-interactive-inline.sh). After the copy, the file is in sync-shared.sh's intersection and stays aligned automatically.
- Rationale: this is exactly the documented bootstrap gap (sync-shared.sh is intersection-only and cannot create files in a consumer plugin; repo CLAUDE.md prescribes a manual byte-identical copy the first time). The release-time [J] coherence gate is the only other surface that would catch it.
- Blast radius: one new file in managerkit's `_shared/`; zero edits to existing files; lint-non-interactive-inline.sh unaffected (block untouched).

**Pass 4 author verdict:** 6/6 accepted (0 rejected, 0 invalid). Quotes verified: record-lib.mjs:121 (F20), cli-lib.mjs:15 (F24), SKILL.md:92 + `_shared/` containing only interview-guidelines/ (F25); F21/F22/F23 quote the cumulative proposal itself — all three lines matched verbatim. Substantive spot-checks: fm regex at record-lib.mjs:91 is open-ended while serialize iterates FM_KEYS only (F20 confirmed); `part.slice(3, nl).trim()` at record-lib.mjs:102 parses `## __preamble` to the colliding key (F22 confirmed); cli-lib.mjs:15–16 sets boolean true on `--`-leading values (F24 confirmed).

**CLOSEOUT:** Review loop terminated at the pass cap after Pass 4. Pass 4 contained 2 material (Should-fix) findings — F20, F21 — so the unit closes **CAPPED** per the convergence rule (material findings still arriving when the cap hit). Cumulative: 25 findings, 25 accepted, 0 rejected, 0 invalid. Nothing implemented; all changes remain proposals in the cumulative proposal document.
