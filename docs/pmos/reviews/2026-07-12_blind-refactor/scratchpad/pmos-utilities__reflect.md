## Pass 1 — reviewer findings

### reflect-F1 [Blocker] Multi-session and single-session paths read different transcript roots
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:88 (vs :129)
- Quote: "**Enumerate candidates** by globbing `~/.claude-personal/projects/<project>/*.jsonl` (or all projects if `--project all`)"
- Problem: Multi-session enumeration hardcodes `~/.claude-personal/projects/`, while single-session Phase 1 (line 129) says "List `~/.claude/projects/<slug>/*.jsonl`". These are different config roots — on any given machine at most one is correct, so either `--last N` silently returns "no transcripts found" (E16 exits 0, masking the bug) or the single-session path misses the live transcript. Neither respects `CLAUDE_CONFIG_DIR`, which this user's own setup varies per alias. One fact (where transcripts live), two contradictory homes — a §K violation that is actively broken as written.

### reflect-F2 [Should-fix] Duplicate integer phase numbers with no slug anchors — two interleaved pipelines share Phase 1/2/4/5
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:84
- Quote: "**Skip if no multi-session flag was set** (no `--last`, `--days`, or `--since`). Single-session path goes directly to Phase 1 below."
- Problem: The file contains two "Phase 1", two "Phase 2", two "Phase 4", and two "Phase 5" headings (multi-session variants interleaved with single-session ones, and multi-session Phase 4 is physically placed between single-session Phases 3 and 4). Repo policy §J requires unique integer phases with stable `{#kebab-slug}` anchors and slug-cited cross-refs; here every cross-ref is a bare number into an ambiguous namespace ("goes directly to Phase 1 below", "Phase 2 multi-session dispatch was not run", line 107's "this seeds Phase 2 dispatch"). Compounding it, the single-session phases carry no reciprocal "skip if in multi-session mode" guards — whether multi-session runs also execute single-session Phases 1–4 (e.g., the Phase 3 frontmatter peek) is left to inference.

### reflect-F3 [Should-fix] Deterministic mechanics (exit codes, regex hashing, 60s timeouts) specified as prose with zero shipped scripts
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:171
- Quote: "Apply this regex to each finding text BEFORE computing the first-100-chars hash component"
- Problem: The skill ships no scripts/ at all, yet specifies flag validation with process exit codes ("exit 64 with usage hint on violation", line 43), a regex boilerplate-strip plus first-100-chars hash aggregation, and "60s wall-clock per subagent" timeouts (line 117). Per §H, deterministic checks and arithmetic belong in scripts — an LLM cannot reliably compute string hashes in-head, cannot `exit 64` (there is no process), and the Task tool exposes no enforceable per-subagent wall-clock timeout. This is pseudo-determinism: precise-looking contracts (FR-42, FR-45) that no mechanism actually enforces, giving false confidence in dedup correctness and bounded runtime.

### reflect-F4 [Should-fix] Dead flag: --msf-auto-apply-threshold has no consumer anywhere in the body
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:41
- Quote: "| `--msf-auto-apply-threshold N` | int (0-100) | 80 | Confidence threshold for any folded MSF apply-loops invoked by /reflect"
- Problem: The flag is defined in the parser table and advertised in the frontmatter argument-hint, but no phase of /reflect ever invokes an MSF apply-loop or reads this threshold — the only justification is a cite to "FR-RETRO-MSF integration", a spec ID unresolvable from the shipped skill. A contract flag with no behavior behind it inflates the argument-hint (§I says hints list real contract flags), confuses users, and will silently no-op.

### reflect-F5 [Should-fix] Phase 3 frontmatter lookup assumes repo-relative plugins/ layout that doesn't exist where /reflect actually runs
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:150
- Quote: "otherwise try `plugins/pmos-toolkit/skills/<name>/SKILL.md`, `plugins/pmos-learnkit/skills/<name>/SKILL.md`"
- Problem: /reflect's primary use case is running inside an arbitrary project after a pmos session — where `plugins/pmos-toolkit/...` is not a path that exists (installed skills live in the plugin cache under the config dir). The concrete paths given only resolve inside the agent-skills repo itself; every other project falls to the hand-wave "(or wherever the plugins are installed)". The one lookup that matters most in practice is the one left unspecified, so Phase 3 will routinely degrade to "frontmatter cannot be located" and the "claimed contract" line in every retro block goes missing.

### reflect-F6 [Should-fix] Phase 6 approval is a free-form y/n/edit prompt — invisible to the non-interactive classifier
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:295
- Quote: "never auto-write — always show the user the exact bullets and get approval (`y/n/edit`)"
- Problem: The inlined non-interactive contract classifies `AskUserQuestion` calls only. Phase 6's learnings-approval gate is specified as a prose y/n/edit prompt, so under `--non-interactive` its behavior is undefined — it neither AUTO-PICKs (no Recommended option exists) nor DEFERs via a `defer-only` tag; the model must improvise between hanging on a question and auto-writing to `~/.pmos/learnings.md`/CLAUDE.md, the exact thing "never auto-write" forbids. The prompt should be an AskUserQuestion with a defer-only tag (free-form) so NI runs deterministically defer the write.

### reflect-F7 [Should-fix] Unresolvable spec-ID jargon baked into runtime instructions
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:29
- Quote: "### Multi-session flag parser (T14, new in v2.34.0 per W8)"
- Problem: The body is threaded with private spec identifiers — T14/T17/T18, W8, D7/D10/D18/D32, FR-40/42/44/45/46/49, FR-RETRO-MSF, NFR-02, E13/E16 — with no shipped reference file resolving any of them. For the executing model they are noise it cannot act on; for a reader they are dead links; as token economy they are pure cost. Per §K these belong in the feature spec (their one home), with the skill stating behavior in its own words. ("new in v2.34.0" is also stale provenance in a plugin now versioned independently of toolkit's 2.x track.)

### reflect-F8 [Nit] Frozen NI block cites `_shared/non-interactive.md`, which does not exist in pmos-utilities
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:73
- Quote: "sources the shared call-site extractor from Section D of this file (`_shared/non-interactive.md`)"
- Problem: pmos-utilities ships no `skills/_shared/` at all — line 288 even says so ("self-contained (no `_shared/` substrate)") — so the cite dangles within this plugin. Nit rather than higher because the block is byte-frozen by lint (editing the path in place would break the drift check) and the reference is CI-context-only; the clean fix is bootstrapping a byte-identical `_shared/non-interactive.md` copy into pmos-utilities per the repo's documented intersection-only-sync bootstrap procedure.

### reflect-F9 [Nit] Wave semantics contradict the rolling-pool dispatch, and the worst-case arithmetic is wrong
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:122
- Quote: "**NFR-02 wall-clock budget**: 5×60s = 300s worst-case for 20 candidates with one wave"
- Problem: Dispatch is defined as a rolling pool ("As one returns, dispatch the next"), so discrete "waves" don't exist, yet progress emits are specified as `Wave i/N` with N undefined. And 20 candidates at 5 in-flight with a 60s cap is worst-case ~4×60s = 240s, not "5×60s = 300s ... with one wave" — an arithmetic claim of exactly the kind §H says never to leave to prose.

### reflect-F10 [Nit] Redundant positional skill filter duplicates --skill without an nl-sugar marker
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:5
- Quote: "[skill-name to filter, optional] [--last N] [--days N] [--since YYYY-MM-DD]"
- Problem: The argument-hint offers both a positional "skill-name to filter" and `--skill <name>` for the same behavior; the body (Phase 2) handles only "a skill name argument" generically. Per §I one should be the contract form and the other a marked `<!-- nl-sugar -->` alias; as shipped, precedence when both are supplied is unspecified.

### reflect-F11 [Nit] The multi-session flag parser is filed under "Phase 0: Load Learnings"
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:31
- Quote: "Parse the following flags from the argument string before any other processing."
- Problem: A subsection that gates the entire run's shape (single vs multi-session) is nested under a phase whose declared job is reading `~/.pmos/learnings.md`. Progressive disclosure suffers: a reader scanning phase headings for "where are flags parsed" finds nothing; it deserves its own top-level phase (with a slug anchor, per F2).

**Pass 1 verdict:** 1 blockers / 6 should-fix / 4 nits — material findings

## Pass 1 — author response

### reflect-F1 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · Phase 1 (multi-session prelude) step 1 + single-session Phase 1 step 2 · before: two contradictory hardcoded roots (`~/.claude-personal/projects/` at :88 vs `~/.claude/projects/` at :129) → after: introduce ONE canonical "Transcript root resolution" subsection (in the flag-parser/setup phase, with a `{#transcript-root}` slug anchor) that resolves the root as `${CLAUDE_CONFIG_DIR:-~/.claude}/projects/`, and — when `CLAUDE_CONFIG_DIR` is unset and `~/.claude/projects/<slug>/` yields no jsonl — falls back to probing sibling config dirs matching `~/.claude*/projects/<slug>/` (newest-mtime dir wins), noting the chosen root in the output header. Both the multi-session enumeration step and the single-session locate step are rewritten to cite `{#transcript-root}` instead of naming a path.
- Rationale: One fact (where transcripts live), one home (§K). The reviewer is right that at most one hardcoded root is correct per machine, and this user's own aliases vary `CLAUDE_CONFIG_DIR` per session — the env var is the only authoritative signal, with a probe fallback because the var may not be exported into the transcript-reading context. E16's clean exit-0 on zero candidates would otherwise mask the bug forever.
- Blast radius: none outside this file (no other skill cites reflect's phases); tools/lint-phase-refs.sh must re-pass after the new anchor is added.

### reflect-F2 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · all phase headings · before: two interleaved pipelines sharing integers (two Phase 1/2/4/5, multi-session Phase 4 physically between single-session 3 and 4) with bare-number cross-refs → after: renumber into ONE monotonically increasing integer sequence in file order, each heading gaining a stable `{#kebab-slug}` anchor (e.g. `{#parse-flags}`, `{#enumerate-candidates}`, `{#dispatch-subagents}`, `{#locate-transcript}`, `{#detect-invocations}`, `{#peek-frontmatter}`, `{#aggregate-findings}`, `{#analyze-invocations}`, `{#emit-multi-session}`, `{#emit-retro-blocks}`, `{#capture-learnings}`); every cross-ref rewritten to cite the slug, never a bare number. Additionally add explicit reciprocal guards: each single-session-only phase opens with "Skip if in multi-session mode — subagents apply the {#analyze-invocations} rubric per transcript; the parent runs only {#aggregate-findings} and {#emit-multi-session}", resolving the currently-inferred question of which phases run in which mode. {#peek-frontmatter} and {#capture-learnings} run in BOTH modes and say so.
- Rationale: Direct §J violation; the ambiguity is not cosmetic — "goes directly to Phase 1 below" and "Phase 2 multi-session dispatch" are genuinely ambiguous references today, and the missing skip-guards leave mode routing to model inference.
- Blast radius: tools/lint-phase-refs.sh (must pass on the renumbered file — it is also the regression check that no ghost refs survive); the frozen non-interactive block contains no phase refs into this file, so it is untouched. No external skill references reflect phase numbers (verified by grep: no `reflect/SKILL.md#` cites elsewhere).

### reflect-F3 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md + NEW plugins/pmos-utilities/skills/reflect/scripts/aggregate-findings.mjs · three sub-fixes: (1) ship a small node script that takes the per-transcript YAML findings on stdin and performs the FR-45 boilerplate-strip regex + first-100-chars hash aggregation deterministically, emitting the aggregated rows + constituents as JSON; the {#aggregate-findings} phase is rewritten to "pipe findings through scripts/aggregate-findings.mjs" instead of asking the model to compute string hashes in-head (§H: arithmetic/hashing = script). (2) Rewrite the skill-authored "exit 64 with usage hint on violation" validation prose to "report the usage error and stop the skill run (record `Run Outcome: error` per the non-interactive contract)" — the model has no process to exit; the frozen NI block's own exit-64 language is byte-frozen and untouched. (3) Reword the 60s timeout as an explicitly advisory judgment cutoff: "if a subagent has not returned after roughly 60s of wall clock, treat that transcript as scanned-failed and move on — the Task tool has no enforceable timeout, so this is a monitoring heuristic, not a guarantee."
- Rationale: The reviewer's "pseudo-determinism" diagnosis is correct: precise-looking contracts with no enforcing mechanism are worse than honest heuristics because they claim dedup correctness the model cannot deliver. The hash aggregation is the one piece that genuinely needs a script (identical inputs must aggregate identically across runs for the recurring-pattern report to be trustworthy).
- Blast radius: new scripts/ dir under the skill (canonical location); the script must be bash-3.2-safe-invocable (plain `node scripts/aggregate-findings.mjs`); no lint gates scripts existence, but skill-eval §H checks will now see a real script instead of prose arithmetic. F9's arithmetic fix folds into sub-fix (3)'s honesty rewrite.

### reflect-F4 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · frontmatter argument-hint (line 5) + flag-parser table (line 41) · before: `--msf-auto-apply-threshold N` defined and hinted with zero consumers → after: delete the flag from both the table row and the argument-hint. No deprecation alias needed — a flag that never had behavior has no users to break (§I's "never rename machine-coupled flags" protects flags with consumers; this one is dead on arrival).
- Rationale: /reflect invokes no MSF apply-loop anywhere in its body; the sole justification is an unresolvable spec ID. A hinted contract flag that silently no-ops is a §I violation and user-facing misinformation.
- Blast radius: tools/lint-flags-vs-hints.sh (removing from BOTH hint and body keeps it green — removing from only one would fail it); no other skill passes this flag to /reflect (grep confirms no external `--msf-auto-apply-threshold` call sites).

### reflect-F5 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · Phase 3 (frontmatter peek), line 150 · before: repo-relative `plugins/pmos-*/skills/<name>/SKILL.md` paths that only resolve inside the agent-skills repo, with a "(or wherever the plugins are installed)" hand-wave → after: specify an ordered lookup: (1) the plugin cache under the active config dir — glob `${CLAUDE_CONFIG_DIR:-~/.claude}/plugins/cache/**/pmos-*/skills/<name>/SKILL.md` and the marketplaces mirror `.../plugins/marketplaces/**/plugins/pmos-*/skills/<name>/SKILL.md`; (2) repo-relative `plugins/<plugin>/skills/<name>/SKILL.md` when the cwd is the agent-skills repo itself (detect via the sentinel `.claude-plugin/marketplace.json` at repo root); (3) if neither resolves, keep the existing graceful degradation ("note the skill name and proceed without the contract reference") but ALSO emit one stderr line naming the dirs probed, so a wrong-root failure is diagnosable instead of silent.
- Rationale: The primary runtime context (arbitrary project post-session) is exactly the one the current text leaves unspecified; the concrete-paths-first ordering as shipped optimizes for the rare case. Uses the same `CLAUDE_CONFIG_DIR` resolution home introduced by F1 (§K: cite {#transcript-root}'s config-dir resolution, don't restate it).
- Blast radius: none — Phase 3's frontmatter-only contract and the anti-pattern ("do not read the body") are unchanged; the fix depends on F1's config-dir resolution subsection landing first.

### reflect-F6 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · Phase 6 Capture Learnings, line 295 · before: prose `y/n/edit` approval gate invisible to the NI classifier → after: replace with a structured `AskUserQuestion` presenting the exact proposed bullets, options "Write as shown / Edit before writing / Skip (write nothing)", preceded by the literal tag line `<!-- defer-only: free-form -->` — so in non-interactive mode the classifier deterministically DEFERs (bullets land in the Open Questions buffer, nothing is written to `~/.pmos/learnings.md` or CLAUDE.md), and interactive mode gets the same three-way choice it has today. The "never auto-write" rule sentence is kept and now provably holds under NI.
- Rationale: The frozen NI contract classifies only AskUserQuestion calls; a prose prompt forces the model to improvise between hanging and auto-writing — the latter being the exact forbidden action. defer-only:free-form is the correct class (the edit path is inherently free-form; no Recommended option can stand in for user-authored bullet edits).
- Blast radius: plugins/pmos-toolkit/tools/audit-recommended.sh (the new call site must carry the adjacent defer-only tag to stay green — it will); no Recommended option is added, intentionally.

### reflect-F7 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · body-wide (headings at lines 29/82/109/117/159/169/208/236/241/251; inline cites T14/T17/T18, W8, D7/D10/D18/D32, FR-40/42/44/45/46/49, FR-RETRO-MSF, NFR-02, E16; "new in v2.34.0") · before: private spec IDs threaded through runtime instructions with no shipped resolver → after: delete every spec ID and version-provenance token from skill-authored prose and headings, restating each obligation in plain words at its point of use (e.g. "Cap-confirmation (D18, FR-40)" → "Cap-confirmation"; "per D32" → "to keep wall-clock bounded"; "(T14, new in v2.34.0 per W8)" → dropped from the heading). Two exemptions: (a) the frozen non-interactive block keeps its FR-*/E13/NFR-07 cites byte-identical (lint-frozen); (b) E16's zero-candidate behavior and D18's cap behavior are kept as behavior, just without the ids. The design rationale lives in the feature spec under docs/pmos/features/ — its one home per §K.
- Rationale: For the executing model the IDs are unactionable noise; for readers they are dead links; they also make future edits scarier than they are ("does removing FR-45 break a contract?" — no, the regex is the contract). FR-RETRO-MSF disappears entirely with F4.
- Blast radius: none mechanical — no lint parses these IDs; the frozen-block exemption keeps tools/lint-non-interactive-inline.sh green. Interacts with F2 (heading rewrite) and F9 (NFR-02 prose) — one combined heading/prose pass.

### reflect-F8 — Accepted
- Fix: NEW plugins/pmos-utilities/skills/_shared/non-interactive.md (byte-identical copy from pmos-toolkit's skills/_shared/non-interactive.md) · before: frozen block's Section-D cite dangles within pmos-utilities (plugin ships no _shared/) → after: bootstrap the file manually per the documented intersection-only-sync procedure in CLAUDE.md ("the first time a consumer plugin needs a cross-plugin protocol, place the file there manually — after that it's in the sync intersection and stays aligned"). SKILL.md itself is untouched (the block stays byte-frozen); line 288's "self-contained (no `_shared/` substrate)" sentence is updated to reflect that _shared/ now carries exactly the NI contract file.
- Rationale: Exactly the dangling-cite class the repo's CLAUDE.md documents (book-summary/learnkit precedent, 2026-06-12); the fix path is prescribed there. Editing the path inside the frozen block is the one thing we must NOT do.
- Blast radius: plugins/pmos-toolkit/tools/lint-non-interactive-inline.sh (stays green — block untouched); scripts/sync-shared.sh (the file enters the sync intersection for pmos-utilities from now on); the Phase 6 prose sentence edit is a one-clause touch.

### reflect-F9 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · multi-session dispatch (lines 118/122) + emission's duplicate progress spec (lines 241–249) · before: `Wave i/N` progress format with N undefined atop a rolling pool, plus wrong arithmetic ("5×60s = 300s worst-case for 20 candidates with one wave") stated twice → after: (1) drop wave terminology; progress line becomes `progress: <complete>/<total> done, <in-flight> in flight (T+<sec>s)`, emitted on each return and each dispatch; (2) correct the budget claim to "20 candidates at 5 in flight with the ~60s advisory cutoff → worst-case ≈ 4×60s = 240s" and, per F3, frame it as a heuristic budget rather than an enforced bound; (3) delete the duplicated progress-emit block in the emission phase and replace with a cite to the dispatch phase's slug anchor (§K: one fact, one home — the format currently lives in two places and they would drift).
- Rationale: Waves don't exist under rolling dispatch, so `i/N` is unemittable as specified; and 300s is simply wrong (ceil(20/5)=4 sequential 60s generations in the true worst case). The §K dedup prevents the two copies of the format diverging again.
- Blast radius: folds into F3(3)'s advisory-timeout rewrite and F2's slug anchors; tools/lint-phase-refs.sh validates the new cross-cite.

### reflect-F10 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · argument-hint (line 5) + Phase 2 filter sentence (line 144) · before: positional "skill-name to filter" hinted alongside `--skill <name>` with unspecified precedence → after: `--skill <name>` is the contract form and stays in the hint; the positional is removed from the argument-hint and kept in the body as a parsed alias marked `<!-- nl-sugar -->`, with one precedence sentence: "if both a positional name and `--skill` are supplied and they differ, the explicit flag wins and a stderr warning names the ignored positional."
- Rationale: §I hybrid-NL-first pattern verbatim: every option keeps a natural-language form, the hint lists only the contract flag, silent aliases carry the nl-sugar marker. Deterministic precedence removes the improvised-behavior gap.
- Blast radius: tools/lint-flags-vs-hints.sh (positional leaving the hint + nl-sugar marker on the body alias is exactly the shape it accepts).

### reflect-F11 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · "Multi-session flag parser" subsection (line 29) · before: run-shaping flag parser nested under "Phase 0: Load Learnings" → after: promote it to its own top-level phase — "Phase 0: Parse Flags & Resolve Mode" `{#parse-flags}` (flag table, validation, then the frozen NI block, then the F1 transcript-root resolution) — with learnings-loading becoming its own subsequent phase `{#load-learnings}`. Implemented as part of F2's single renumbering pass, not a second restructure.
- Rationale: The parser gates the entire run's shape; hiding it under a learnings-read phase defeats heading-scan discoverability. The NI block's placement contract ("mode resolution ... run AFTER flag parsing") is preserved and becomes structurally obvious.
- Blast radius: same as F2 (one combined renumber + anchor pass; lint-phase-refs.sh re-run); frozen block moves position within the file but is byte-unchanged, so lint-non-interactive-inline.sh stays green.

## Pass 2 — reviewer findings

### reflect-F12 [Should-fix] C1 misses a third hardcoded transcript-root site — the flag table
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:38
- Quote: "`all` = every project under `~/.claude-personal/projects/`"
- Problem: C1 names exactly two sections to rewrite ("multi-session enumeration step (currently line 88)" and "single-session locate step (currently line 129)"), but the `--project` row of the flag parser table at line 38 ALSO hardcodes `~/.claude-personal/projects/` as the definition of `all` scope. Implemented as described, C1 leaves this row contradicting the new `{#transcript-root}` resolution — a user on a bare-`claude` setup reading the flag contract is told `--project all` scans a config root that may not exist, and the model parsing the table may follow it over the resolution subsection. The fix should cite `{#transcript-root}` from this row too (§K: the table currently becomes a second home for the fact C1 just canonicalized).

### reflect-F13 [Should-fix] C2's mode guards make the advertised `--skill` × multi-session combo structurally dead
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:39
- Quote: "Combine with `--last` for \"recurring spec issues across sessions\""
- Problem: The only place the skill-filter is applied is the single-session detect phase (line 144: "If the user passed a skill name argument, filter to that skill only."). Today that ambiguity is papered over by model inference; after C2 adds explicit reciprocal skip-guards ("Skip if in multi-session mode…"), the filter's sole home is a phase the multi-session path is now INSTRUCTED to skip — so the flag table's headline use case (`--skill spec --last N`) has no phase that executes it. Neither the subagent brief (line 115) nor the aggregation phase mentions the filter. C2/C9 need to give the filter a multi-session home (pass it into the subagent brief, or filter at aggregation) or the fix regresses an advertised contract from "vaguely honored" to "explicitly skipped".

### reflect-F14 [Should-fix] Stale three-plugin enumeration — pmos-gamekit and pmos-managerkit invocations are outside the stated scan set
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:136
- Quote: "across **every installed pmos plugin** — `pmos-toolkit`, `pmos-learnkit`, and `pmos-utilities`"
- Problem: The repo ships FIVE pmos plugins (`ls plugins/` → pmos-gamekit, pmos-managerkit alongside the three named). The description (line 3), the intro (line 10), the subagent brief (line 113 "toolkit, learnkit, utilities"), and the Phase 2 signal list (lines 136–138 enumerate exactly three `skill:` namespaces) all pin the scan set to three. Line 137's generic "match any `pmos-*:*` namespace" partially mitigates, but the four explicit enumerations bias detection and the description — the triggering surface — affirmatively claims a smaller scope, so a session full of /interview-feedback or /one-on-one friction produces a retro that silently omits them. This is exactly the enumerated-set staleness class the repo's [J] coherence gate exists for, and no proposal item touches it. Fix: state the set as "every installed `pmos-*` plugin" everywhere and drop the hardcoded lists (one generic matcher, §K).

### reflect-F15 [Should-fix] Description claims current-session-only, contradicting ~40% of the skill's surface
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:3
- Quote: "invoked in the current session. Reads the session transcript (not the skill's source)"
- Problem: The multi-session pipeline (`--last/--days/--since/--project all`, subagent fan-out, recurring-pattern aggregation) is entirely absent from the description and trigger phrases — a user asking "what keeps going wrong with /spec across my sessions" gets no trigger match, and a user who reads the description is told the skill cannot do what its argument-hint advertises. The proposal restructures the body thoroughly (C1–C9) but never touches the description, so the triggering contract stays wrong after every accepted fix lands. One clause ("…or across recent sessions via --last/--days/--since") plus one trigger phrase closes it.

### reflect-F16 [Should-fix] A second NI-invisible interactive gate survives C6 — the 300-line consolidation prompt
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:292
- Quote: "If it exceeds 300 lines, propose a per-section consolidation to the user before appending."
- Problem: C6 converts the y/n/edit approval gate into a tagged AskUserQuestion, but this SECOND prose-only prompt in the same phase — triggered whenever `~/.pmos/learnings.md` exceeds 300 lines — is left as free prose. It has the identical defect class as accepted F6: the NI classifier only sees AskUserQuestion calls, so under `--non-interactive` the model must improvise between hanging, silently skipping consolidation, or auto-rewriting a user file (a destructive summarization of accumulated learnings — worse than the append F6 guarded). Needs the same treatment: a structured ask with `<!-- defer-only: destructive -->`, deferring consolidation to the OQ buffer in NI runs.

### reflect-F17 [Nit] C3's script stops one step short — the frequency×severity scoring and two-tier partition stay model-side arithmetic
- Where: plugins/pmos-utilities/skills/reflect/SKILL.md:221
- Quote: "> Findings seen in ≥2 sessions, sorted by frequency × severity (blocker=3, friction=2, nit=1)."
- Problem: C3 scripts the hash/strip/aggregate step (§H) but the emission phase still asks the model to compute a weighted sort (frequency × a 3/2/1 severity weight) and partition rows into "≥2 sessions" vs "Unique but Notable" in its head. That is exactly the arithmetic-in-prose class §H forbids and F9 already caught once in this file (the 300s miscalculation). Since `aggregate-findings.mjs` already holds per-hash counts and severities, emitting `score` and `tier` fields from the script is a few lines and removes the last in-head arithmetic.

### reflect-F18 [Nit] C2 contradicts itself on which phases the multi-session parent runs — vague enough to implement wrong
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-utilities__reflect.md:39
- Quote: "the parent runs only `{#aggregate-findings}` and `{#emit-multi-session}`"
- Problem: The same proposal sentence then states "`{#peek-frontmatter}` and `{#capture-learnings}` run in both modes and say so explicitly" — so the parent runs at least four phases in multi-session mode, and the skip-guard text C2 dictates verbatim ("the parent runs only…") is false on its face. An implementer copying the guard sentence literally into every single-session phase produces a file whose guards tell the multi-session parent to skip frontmatter-peek and capture-learnings while two other headings claim both-modes execution — reintroducing the exact mode-routing-by-inference defect F2 was accepted to fix. The guard wording needs the both-modes phases enumerated.

### reflect-F19 [Nit] C1's sibling-probe fallback is defined only in slug terms — undefined for `--project all`, and "newest-mtime" is ambiguous
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-utilities__reflect.md:32
- Quote: "probe sibling dirs matching `~/.claude*/projects/<slug>/` (newest-mtime wins)"
- Problem: Two implementation traps: (1) the probe is keyed to `<slug>`, but `--project all` enumerates every project with no slug — the resolution rule for that mode is unspecified (probe roots whose `projects/` contains ANY jsonl? union across roots?); (2) "newest-mtime wins" doesn't say mtime of what — the slug dir, or the newest jsonl inside it. A dir's mtime updates on file creation/deletion, not on appends to an existing session file, so the two readings pick different roots on real multi-alias machines. C1 should specify: newest jsonl mtime within the candidate dir, and the `--project all` behavior explicitly.

### reflect-F20 [Nit] C6's single ask conflates two write destinations with different rules
- Where: docs/pmos/reviews/2026-07-12_blind-refactor/proposal/pmos-utilities__reflect.md:70
- Quote: "Replace with an `AskUserQuestion` presenting the exact proposed bullets with options"
- Problem: Phase 6 proposes bullets for TWO destinations with distinct contracts — `~/.pmos/learnings.md` (may be created if missing) and repo `CLAUDE.md`/`AGENTS.md` (never created, only-if-exists, line 293). C6 describes one ask with one "Write as shown / Edit / Skip" trichotomy; implemented literally, a user who wants the global bullets but not the repo bullets (the common case — repo files are shared/committed) has no path except free-form Edit. Either two asks (one per destination) or a multiSelect over per-destination options; the proposal should say which, or the implementer will guess.

**Pass 2 verdict:** 0 blockers / 5 should-fix / 4 nits — material findings

## Pass 2 — author response

### reflect-F12 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · flag-parser table `--project` row (line 38) · before: row defines `all` as "every project under `~/.claude-personal/projects/`" — a third hardcoded root C1 missed → after: the row's scope definitions name no literal path; `current` = "this project's dir under the resolved transcript root", `all` = "every project dir under the resolved transcript root — see `{#transcript-root}`". C1's scope is amended to enumerate all three hardcoded-root sites (line 38 table row, line 88 enumeration, line 129 locate step).
- Rationale: Exactly the §K regression C1 was written to prevent — canonicalizing the fact in one home while leaving a second home contradicting it. The table is arguably the highest-authority surface for a model parsing flag semantics.
- Blast radius: same as C1 (this file only; lint-phase-refs.sh for the new slug cite from the table row).

### reflect-F13 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · subagent brief (line 115) + aggregation phase · before: the only application of the skill filter is the single-session detect phase ("If the user passed a skill name argument, filter to that skill only." — line 144), which C2's new guards instruct the multi-session path to SKIP, structurally killing the advertised `--skill X --last N` combo → after: give the filter a multi-session home: (1) the subagent brief gains one clause — "if a `--skill <name>` filter is active, extract findings for that skill only and return an empty list when it never ran"; (2) the aggregation phase states it inherits the already-filtered inputs (no re-filter — one enforcement point, §K); (3) the single-session filter sentence and the brief clause both cite one canonical "skill filter" definition placed in the flag-parser phase next to the `--skill` row, so precedence rules from C9 (positional vs flag) apply uniformly to both modes.
- Rationale: The reviewer caught a genuine iatrogenic regression: pass-1's C2 skip-guards convert "vaguely honored by inference" into "explicitly skipped". Filtering in the subagent (not at aggregation) is correct — it shrinks per-transcript output and keeps haiku extraction cheap; aggregation-side filtering would pay full scan cost to discard.
- Blast radius: C2 and C9 wording amended; no lint impact beyond the slug cites already budgeted.

### reflect-F14 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · description (line 3), intro (line 10), subagent brief (line 115 "toolkit, learnkit, utilities"), Phase 2 signal list (lines 136–138), Phase 3 lookup path list (line 150) · before: four-plus surfaces enumerate exactly three plugins while the repo ships five (pmos-gamekit, pmos-managerkit missing everywhere) → after: every surface states the set generically — "every installed `pmos-*` plugin" — with ONE generic matcher (`pmos-*:*` namespaces / `skill: "pmos-*:*"` / glob `pmos-*` in the frontmatter lookup, which C5's cache glob already uses) and ZERO hardcoded plugin lists anywhere in the file. The description drops the parenthetical enumeration entirely.
- Rationale: Classic enumerated-set staleness — the [J]-gate class from the repo's own durable lessons ("grep count-claims whenever an epic adds to an enumerated set"). A session of /interview-feedback or /solitaire friction currently gets silently omitted from the retro. Generic matcher also future-proofs plugin #6.
- Blast radius: description edit touches the triggering surface (re-check skill-eval description checks); folds into C7/C12's combined prose pass; no lint parses plugin names.

### reflect-F15 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · frontmatter description (line 3) · before: description claims "invoked in the current session" only — the entire multi-session pipeline is absent from both the capability claim and the trigger phrases → after: add one clause "…invoked in the current session — or across recent sessions via `--last N` / `--days N` / `--since DATE`, aggregating recurring patterns" and one trigger phrase, e.g. "what keeps going wrong with <skill> across my sessions". Combined with F14's de-enumeration into a single description rewrite.
- Rationale: The description is the routing contract; as shipped it affirmatively denies ~40% of the skill's surface, so multi-session asks never trigger and users are misinformed. Pass-1 restructured the body but never touched the one surface the router reads.
- Blast radius: triggering behavior (intended improvement); skill-eval description/trigger checks re-run; one file.

### reflect-F16 — Accepted
- Fix: plugins/pmos-utilities/skills/reflect/SKILL.md · Capture Learnings, 300-line consolidation sentence (line 292) · before: "If it exceeds 300 lines, propose a per-section consolidation to the user before appending." — a second prose-only prompt in the same phase C6 fixed, NI-invisible and guarding a DESTRUCTIVE rewrite of accumulated learnings → after: replace with a structured `AskUserQuestion` ("learnings.md is at N lines (>300). Consolidate before appending?" · options "Consolidate as proposed / Append without consolidating / Skip the append") preceded by the literal tag `<!-- defer-only: destructive -->`. Under NI the classifier DEFERs: the consolidation proposal lands in the OQ buffer and no rewrite occurs (the append itself is already deferred by C6's gate, so NI writes nothing at all — stated explicitly).
- Rationale: Identical defect class to accepted F6, and strictly worse stakes (summarizing-away user history vs appending). `destructive` is the right reason code, unlike C6's `free-form` — the hazard here is the rewrite, not the edit path.
- Blast radius: audit-recommended.sh sees a second tagged call site in this file (green by construction); C6's phase text gains one sentence tying the two gates' NI behavior together.

### reflect-F17 — Accepted
- Fix: NEW plugins/pmos-utilities/skills/reflect/scripts/aggregate-findings.mjs (C3 amendment) + emission phase (line 221) · before: the script stops at hash/strip/aggregate while the emission phase still asks the model to compute frequency × severity (3/2/1) weighted sort and partition rows into "≥2 sessions" vs "Unique but Notable" in-head → after: the script also emits per-row `score` (session_count × severity_weight) and `tier` (`recurring` when session_count ≥ 2, else `unique-notable` when severity is blocker or friction, else `dropped`), rows pre-sorted by score descending. The emission phase is rewritten to render the script's rows in order — zero model-side arithmetic.
- Rationale: §H says never leave to prose arithmetic a script can do, and this very file already produced one wrong prose calculation (F9's 300s). The counts and severities are already in the script's hands; score/tier is a few lines.
- Blast radius: contained to the new script + emission phase wording; C3's spec amended before any implementation exists, so no rework.

### reflect-F18 — Accepted
- Fix: proposal C2 wording (and hence the eventual SKILL.md guard text) · before: guard sentence dictates "the parent runs only `{#aggregate-findings}` and `{#emit-multi-session}`" while the same paragraph declares `{#peek-frontmatter}` and `{#capture-learnings}` run in both modes — false on its face, and copyable into a self-contradicting file → after: C2 adds ONE canonical mode-routing table `{#mode-routing}` in the flag-parser phase listing every phase × (single / multi / both); each phase's guard is a single line citing it ("Runs in single-session mode only — see `{#mode-routing}`"), never a re-enumeration. The table states the multi-session parent runs: parse-flags, load-learnings, enumerate-candidates, dispatch-subagents, peek-frontmatter, aggregate-findings, emit-multi-session, capture-learnings; subagents run the analyze-invocations rubric per transcript.
- Rationale: My own pass-1 wording was internally inconsistent — "only two phases" contradicted the both-modes sentence one line later. A single routing table is also the §K-correct shape: mode membership is one fact per phase, one home, cited from each heading.
- Blast radius: C2 amendment only; lint-phase-refs.sh validates the extra `{#mode-routing}` cites.

### reflect-F19 — Accepted
- Fix: proposal C1 wording (and hence the transcript-root subsection) · before: "probe sibling dirs matching `~/.claude*/projects/<slug>/` (newest-mtime wins)" — mtime-of-what unspecified (dir mtime doesn't update on appends to an existing jsonl) and undefined for `--project all` (no slug exists) → after: C1 specifies (1) the tiebreak is the newest **jsonl file mtime within** each candidate dir, never the dir's own mtime; (2) for `--project all`, the probe key becomes "any `projects/*/*.jsonl` under the root" and the root whose newest jsonl is most recent wins — exactly ONE root is chosen per run (no cross-root union; stated explicitly), and the chosen root is named in the output header as already specified.
- Rationale: Both traps are real: on this very machine (`~/.claude`, `~/.claude-personal`, `~/.claude-work`, `~/.claude-workmax`) dir-mtime vs file-mtime pick different roots, and `--project all` is precisely the multi-alias power-user mode where the ambiguity bites. Single-root keeps the run's provenance auditable; union would silently mix alias histories.
- Blast radius: C1 amendment only; no new anchors or lint surface.

### reflect-F20 — Accepted
- Fix: proposal C6 wording (and hence the Capture Learnings gate) · before: one AskUserQuestion covering bullets bound for two destinations with different contracts (`~/.pmos/learnings.md` create-if-missing vs repo `CLAUDE.md`/`AGENTS.md` never-create), forcing a "global yes, repo no" user through free-form Edit → after: C6 specifies TWO sequential asks, each tagged `<!-- defer-only: free-form -->`: ask 1 for the global-learnings bullets (always issued when ≥1 bullet is proposed), ask 2 for the repo-file bullets (issued only when an eligible repo file exists AND ≥1 repo bullet is proposed; skipped entirely otherwise). Each carries the same "Write as shown / Edit before writing / Skip (write nothing)" options scoped to its destination. Zero-bullet destinations issue no ask.
- Rationale: The common case is exactly the asymmetric one — private global notes yes, shared committed repo files no — and a trichotomy spanning both destinations cannot express it. Two asks beat multiSelect here: the destinations' bullets are disjoint proposals, not options in one decision.
- Blast radius: audit-recommended.sh sees two tagged call sites instead of one (green); interacts with F16's third ask in the same phase — phase text gains a short "gates in this phase" ordering note.
