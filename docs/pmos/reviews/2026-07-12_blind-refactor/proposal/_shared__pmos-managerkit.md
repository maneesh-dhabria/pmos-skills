# Proposal — pmos-managerkit__shared (FINAL)

**Unit:** `plugins/pmos-managerkit/skills/_shared/` (sole subtree `interview-guidelines/`: 2 skeletons, `interviewer-effectiveness.html`, `reference-resolution.md`, 8 archetype reference+scorecard pairs, `guidelines/work-history/level-ladder.md`, `guidelines/case-study/additional/README.md`).
**Status:** **CONVERGED** — review loop closed after 3 complete passes (pass 1: F1–F8; pass 2: F9–F14; pass 3: F15–F19). All 19 findings accepted (0 rejected, 0 ungrounded); the pass-3 findings were amendments to the author's own earlier fix specs and are fully folded in below. Nothing material remained open at closeout. **No changes have been implemented** — this document describes intended changes only.

## Disposition table

| Finding | Severity | Disposition |
|---|---|---|
| pmos-managerkit-shared-F1 | Should-fix | Accepted (amended by F10, F11, F15, F18, F19) |
| pmos-managerkit-shared-F2 | Should-fix | Accepted |
| pmos-managerkit-shared-F3 | Should-fix | Accepted (superseded/extended by F9) |
| pmos-managerkit-shared-F4 | Should-fix | Accepted |
| pmos-managerkit-shared-F5 | Should-fix | Accepted (extended by F13) |
| pmos-managerkit-shared-F6 | Should-fix | Accepted (amended by F14) |
| pmos-managerkit-shared-F7 | Nit | Accepted (scoped by F10) |
| pmos-managerkit-shared-F8 | Nit | Accepted |
| pmos-managerkit-shared-F9 | Should-fix | Accepted (made determinate by F16, F17) |
| pmos-managerkit-shared-F10 | Should-fix | Accepted |
| pmos-managerkit-shared-F11 | Should-fix | Accepted (amended by F15, F19) |
| pmos-managerkit-shared-F12 | Nit | Accepted |
| pmos-managerkit-shared-F13 | Nit | Accepted |
| pmos-managerkit-shared-F14 | Nit | Accepted |
| pmos-managerkit-shared-F15 | Should-fix | Accepted |
| pmos-managerkit-shared-F16 | Should-fix | Accepted |
| pmos-managerkit-shared-F17 | Nit | Accepted |
| pmos-managerkit-shared-F18 | Nit | Accepted |
| pmos-managerkit-shared-F19 | Nit | Accepted |

**Totals:** 19 accepted (11 should-fix, 8 nits), 0 rejected, 0 blockers at any pass.

## Grounding record

All 19 findings were spot-checked by the author at each pass: every ≥40-char quote exists verbatim at the cited file:line. A final closeout re-check confirmed the load-bearing quotes against the live substrate (F2's `../SKILL.md#…` links at reference-resolution.md:3/:15/:25; F3's zero `calib` occurrences in reference-skeleton.html; F4's `_shared/` holding only `interview-guidelines/`; F6's README:3 quote; F12's "should sum to 100." at scorecard-skeleton.html:12; F15's Senior-PM `.sub` banner at work-history/scorecard.html:83; F18's generic-guideline branch at reference-resolution.md:11). Key functional re-verifications: (F5) work-history's 6 `data-area` ids are fully disjoint from its scorecard's 12 `data-dim` ids and `questionnaire.mjs` keys `probes[d.id]` (lines 62, 100); (F9) per-archetype `class="calib"` coverage confirmed by grep — only product-sense and work-history conform per-area; (F10) `fill-scorecard.mjs:553` asserts "byte-for-byte preservation of the `<head>` region"; (F11) `level-ladder.md:19` defaults to senior-pm and :94–104 routes the weight-override machinery only to /interview-guide; (F15) work-history/scorecard.html carries 12 rendered `<span class="weight">weight N</span>` labels, the `.sub` "Senior PM weighting" banner (:83), and the head comment "materialize the `senior-pm` COLUMN" (:15); (F16) product-sense calib is qualitative "Good / avg / poor." prose (:73) vs work-history's numeric "4 / 2–3 / 1" anchors (:114) — two genuinely different shapes; (F17) `.calib` CSS rules differ between the two conforming exemplars (product-sense:33 vs work-history:40); (F19) `grep -rn level-ladder` over interview-feedback/ returns zero hits and the SKILL.md:261 storage layout has no ladder slot. Rejected/invalid: none. F9–F11 correct the pass-1 proposal; F15–F19 correct pass-1/2 amendments (F15/F18/F19→F1/F11; F16/F17→F9).

## Accepted changes (full detail)

### F1 [Should-fix] — Make the bundled archetype corpus reachable from /interview-feedback's own resolution chain — AS AMENDED BY F10, F11, F15, F18, F19
- **Finding:** `reference-resolution.md`'s precedence chain (`--reference` → role-dir round guideline → role-level default) never falls back to the bundled archetype pair, and interview-feedback's Setup phase (`SKILL.md` `{#setup}`, ~line 224) instantiates the BLANK skeletons for rounds without operator-provided files — while a fully researched pair for the same archetype sits in the substrate. The corpus (the substrate's largest asset) is consumed only via /interview-guide.
- **Files/sections:** `plugins/pmos-managerkit/skills/interview-feedback/SKILL.md` Phase Setup `{#setup}` (+ role.json section); `plugins/pmos-managerkit/skills/_shared/interview-guidelines/reference-resolution.md` §Reference resolution.
- **Before → after:**
  1. Setup prose currently says "attach the provided interviewer-reference + scorecard or generate them by instantiating `../_shared/interview-guidelines/reference-skeleton.html` + `.../scorecard-skeleton.html`". After: for a round whose archetype is one of the 8 bundled types and no operator files are provided, COPY the bundled pair from `../_shared/interview-guidelines/guidelines/<archetype>/` into the role dir (provenance note in `role.json`); blank-skeleton instantiation remains only for `archetype: custom`. **[F19 amendment]** No `level-ladder.md` copy into the role dir (the pass-2 spec's "work-history rounds also copy level-ladder.md" is DROPPED): the ladder is read from the substrate at copy time only (authoring-time dependency); nothing in interview-feedback's scoring path reads it afterwards (grep-verified zero hits), the bundled scorecard's head comment declares the substrate ladder the single source of truth with exactly ONE sanctioned hardcoded copy, and the SKILL.md:261 layout has no ladder slot — role-dir copies would be unsynced §K litter. `role.json` records the applied ladder row + a provenance pointer to the substrate ladder instead.
  2. **[F18 amendment]** `reference-resolution.md`'s chain is restructured so the exact-archetype bundled pair outranks an archetype-MISMATCHED generic: step 3 becomes "**Role-level default** — the role's default round guideline (the first round whose archetype matches). Record that a fallback was used."; new step 4: "**Bundled archetype pair** — `_shared/interview-guidelines/guidelines/<archetype>/` for the round's `role.json` archetype (bundled types only). Record that the bundled corpus was used."; new step 5: "**Role generic guideline** — the role's generic guideline, for `custom`/unbundled archetypes with no match above. Record the fallback." Operator files and archetype-MATCHING role files still win (steps 1–3); only the wrong-archetype generic drops below the researched exact-archetype pair — otherwise pre-fix or hand-assembled role dirs (which the setup copy never touches) would resolve a product-sense round to a behavioral-shaped generic forever, silently.
  3. **[F10 amendment]** The copy step REWRITES `<meta name="pmos:skill">` to `interview-feedback` on copy (the working copy is an interview-feedback artifact) and replaces the header comment with a provenance line ("copied from bundled `<archetype>` corpus by /interview-feedback setup, <date>"). Without this, fill-scorecard.mjs's byte-for-byte `<head>` preservation would ship every filled scorecard with the post-F7 `pmos:skill=interview-guide` attribution — misrouting /interview-feedback's flagship artifact.
  4. **[F11 amendment]** For work-history rounds the copy step gains a seniority-resolution sub-step: resolve the candidate level via level-ladder.md's alias table + title inference (reading the SUBSTRATE ladder — see item 1/F19). If it resolves to `senior-pm` (or is unresolvable → ladder default), proceed and record `weights: senior-pm (ladder default)` in `role.json`. If it resolves to a DIFFERENT level, swap the copied scorecard to the matching ladder row (mechanical table lookup — no model arithmetic) and gate the result with `interview-guide/scripts/validate-scorecard-anchors.mjs`'s sum-gate before accepting; on gate failure, fall back to the senior-pm sheet with a stderr warning plus a `role.json` provenance flag directing re-weighting via /interview-guide § Scoring Sheet. Non-interactive runs never prompt: resolved-or-default row, always with provenance. (Cross-skill validator invocation is sanctioned precedent — level-ladder.md:99 already routes the override sum-gate there.)
  5. **[F15 amendment]** The level swap in item 4 must update ALL FOUR weight homes in the copied scorecard, not `data-weight` alone: (a) each dim's `data-weight` attribute; (b) the paired rendered `<span class="weight">weight N</span>` label (12 of them) — regenerated from the just-set anchor value, never independently (the anchor is the source, the span a projection); (c) the `.sub` banner "work-history round · Senior PM weighting (default; re-select with --seniority) · weights sum to 100" → "<Level label> weighting (resolved by setup; re-select via /interview-guide --seniority)"; (d) the head comment's "the data-weight values below materialize the `senior-pm` COLUMN" line → name the applied ladder row. (c)/(d) matter because the interviewer reads the prose, not the attributes, and fill-scorecard.mjs's byte-for-byte `<head>` preservation would fossilize a stale head comment into every filled output. The copy step's acceptance adds a label/anchor mismatch check (each `weight N` span greps equal to its sibling `data-weight`), and the sum-gate still runs on the anchors after the swap.
- **Rationale:** closes both entry points (setup-time and resolve-time) so /interview-feedback users get the researched corpus without running /interview-guide first; no behavior change when higher-precedence files exist; the amendments keep attribution, weighting, human-visible prose, precedence order, and §K single-source-of-truth correct through the new path.
- **Blast radius:** interview-feedback SKILL.md (setup phase + role.json schema/provenance), reference-resolution.md (§Reference resolution restructured 3→5 steps; its "Non-interactive ambiguity" paragraph stays valid; any SKILL.md prose paraphrasing the chain re-checked). Re-run `tools/lint-phase-refs.sh`, skill-eval, and `audit-recommended.sh` (any new setup prompt needs a `(Recommended)` option) on interview-feedback; one run-tests.sh case if setup/swap behavior is test-enumerated. Reuses interview-guide's validator by path — no new scripts.

### F2 [Should-fix] — Fix reference-resolution.md's four broken `../SKILL.md#…` links
- **Finding:** From `_shared/interview-guidelines/`, `../SKILL.md` resolves to nonexistent `skills/_shared/SKILL.md`. Grounded quote (line 3): "Cited from SKILL.md Phase [Resolve](../SKILL.md#resolve) and Phase [Coach](../SKILL.md#coach)." Same shape at line 15 (`[Score](../SKILL.md#score)`) and line 25 (`[Transcribe](../SKILL.md#transcribe)`) — stale paths from before the doc moved to `_shared/`.
- **File/section:** `_shared/interview-guidelines/reference-resolution.md` lines 3, 15, 25.
- **Before → after:** re-point all four link targets from `../SKILL.md#<anchor>` to `../../interview-feedback/SKILL.md#<anchor>`. Anchors (`#resolve` :99, `#transcribe` :119, `#score` :144, `#coach` :218) verified present in interview-feedback/SKILL.md; only the path segment changes.
- **Rationale:** dangling links in a shared substrate doc that presents itself as the resolution contract.
- **Blast radius:** docs-only. Follow-up candidate outside this unit: `tools/lint-phase-refs.sh` validates `Phase <N>` / `{#slug}` / `<skill>/SKILL.md#anchor` forms but not bare `../SKILL.md#anchor` — this defect class is lint-invisible.

### F3 + F9 [Should-fix] — Establish ONE per-area `.calib` shape in the skeleton AND normalize the drifted corpus to it — MADE DETERMINATE BY F16 + F17
- **Finding (F3):** The skeleton's header claims "THE single DOM contract for a round's interviewer guideline…" yet contains zero occurrences of `calib`, while interview-guide/SKILL.md:176 requires "a `.calib` good/average/poor line + the common interviewer mistake" per area and output-shapes.md checklists it.
- **Finding (F9, corrects F3's premise):** the pass-1 claim that "all 8 bundled references carry `.calib`" was FALSE. Verified per-area coverage: product-sense and work-history conform; analytical/behavioral/case-study have 2 `.calib` elements each, technical 1; case-presentation uses a differently-named `<section class="calibration">`; recruiter-screen has no calib DOM at all (inline prose: "Common mistakes / calibration:"). 6 of 8 exemplars violate the guide's per-area requirement and the skeleton's own claim.
- **Finding (F16, corrects F9's target):** "the product-sense/work-history form" named TWO divergent forms — product-sense's is a qualitative "Good / avg / poor." one-liner (`<b>` markup, :73) while work-history's is numeric score anchors "**4** … **2–3** … **1**" keyed to the scorecard's data-v scale (`<strong>`, :114). An implementer had no determinate normalization target.
- **Finding (F17, corrects F9's blast radius):** `.calib` styling lives in `<head><style>`, and the two conforming exemplars carry DIFFERENT rules (product-sense:33 left-border .85rem vs work-history:40 dashed border-top .84rem); F9's "content edits inside `<main>` only" scope would ship unstyled `.calib` paragraphs in all 6 normalized files.
- **Files & before → after:**
  1. **Canonical form [F16]:** the product-sense form — a per-area `.calib` element carrying a qualitative Good / Average / Poor line + the common interviewer mistake. The skeleton and all 6 normalized references get THIS form. Work-history's numeric "4 / 2–3 / 1" variant is BLESSED as a sanctioned archetype variant, not normalized away: one sentence added to the work-history reference header (rides F5's header edit — same comment block) stating its calib lines are score-anchor calibration keyed to the scorecard's data-v scale, an intentional variant of the canonical form; interview-guide/SKILL.md:176 gains a parenthetical "(or, for score-anchored references such as work-history, per-score anchors on the sheet's data-v scale)".
  2. `reference-skeleton.html`: add an example `.calib` element per area section (canonical form), the canonical `.calib` CSS rule in the inline `<style>`, and a `.calib` entry in the header's structure/anchor list.
  3. Normalize the 6 non-conforming bundled references to the canonical form: analytical, behavioral, case-study, technical gain per-area `.calib` lines for uncovered areas (redistributing existing section-level `<dl>` calibration content per area; authoring missing lines from each reference's own signals prose); case-presentation's `<section class="calibration">` converts to per-area `.calib`; recruiter-screen's inline calibration prose is lifted into per-area `.calib` elements.
  4. **CSS [F17]:** each of the 6 normalized references also gets the canonical `.calib` CSS rule added to its `<head><style>` (they define none today). Canonical rule = product-sense:33's (left-border, .85rem) — the same rule the skeleton gets, so skeleton and corpus share one style. Work-history's divergent rule is left as-is (it styles the blessed numeric variant; changing it is churn). Head edits are safe here — fill-scorecard.mjs's head preservation applies to scorecards, not references.
- **Rationale:** the guide's per-area requirement is the right bar; downgrading it would codify corpus drift. Product-sense's qualitative form is the right canon (it matches the guide's existing wording verbatim; 5 of 6 normalization sources are prose); work-history's numeric anchors are load-bearing for its 1–4 scoring contract. One canon + one documented variant, each with a stated home. Afterwards the skeleton's "single DOM contract" claim is true.
- **Blast radius:** reference-skeleton.html + 6 archetype interviewer-reference.html files (content edits inside `<main>` + one `.calib` rule in each `<head><style>`; `data-area`/`data-probes`/`data-signals` anchors untouched, so questionnaire.mjs is unaffected) + interview-guide/SKILL.md:176 (one parenthetical) + work-history reference header sentence (rides F5's edit). output-shapes.md checklist already matches; interview-feedback `run-tests.sh` may need one new expectation if it enumerates skeleton structure; re-run skill-eval on both skills.

### F4 [Should-fix] — Bootstrap `_shared/non-interactive.md` into this plugin
- **Finding:** The frozen non-interactive block in interview-feedback (SKILL.md:78), interview-guide (SKILL.md:81), and one-on-one (SKILL.md:92) cites "Section D of this file (`_shared/non-interactive.md`)", but `plugins/pmos-managerkit/skills/_shared/` contains only `interview-guidelines/` — the file exists in pmos-toolkit's and pmos-learnkit's `_shared/` (verified byte-identical to each other) but not here. Documented intersection-only-sync bootstrap gap: `sync-shared.sh` never creates files in a destination.
- **File:** create `plugins/pmos-managerkit/skills/_shared/non-interactive.md` as a byte-identical copy of canonical pmos-toolkit `skills/_shared/non-interactive.md` (manual one-time bootstrap per repo CLAUDE.md's documented procedure).
- **Rationale:** within this plugin the cite is dangling — a reader (or the sourcing the block describes) cannot resolve it. After bootstrap, sync-shared.sh keeps it in the intersection.
- **Blast radius:** none to lints — `lint-non-interactive-inline.sh:36` pins `CANONICAL_FILE` under pmos-toolkit only, so a managerkit copy cannot perturb it (verified pass 2); the three inline blocks are already byte-identical, so adding the local copy only makes the cite resolvable. sync-shared.sh intersection grows by one file. No skill-body edits.

### F5 + F13 — Document the work-history method-archetype exception and fix silent zero-/partial-probe questionnaires
- **Finding (F5 [Should-fix]):** The work-history reference states "Area ids here are the METHOD phases; competency scoring lives in the scorecard + level ladder." — 6 method-phase area ids vs. 12 competency `data-dim` ids, the only archetype where the sets are disjoint. interview-guide/SKILL.md:176 states "Area ids MUST match the scoring-sheet `data-dim` ids 1:1" with no carve-out, and `questionnaire.mjs` (`probes[d.id] || []`) therefore emits a tier-3 work-history questionnaire whose 12 dimension sections all have ZERO probes.
- **Finding (F13 [Nit], extends F5):** a disjoint-only fallback trigger leaves PARTIALLY-drifted pairs (hand-edited or `custom` archetype, e.g. 3 of 5 ids matching) still emitting silent probe-less sections.
- **Files & before → after (the method-phase design itself is kept — intentional Topgrading structure per the exemplar's header):**
  1. `interview-guide/SKILL.md:176` — add an explicit carve-out to the MUST: "except archetype `work-history`, whose reference areas are METHOD phases — see `{#work-history-weights}`"; fold the same one-line exception into the Archetypes section's work-history entry.
  2. `interview-feedback/scripts/questionnaire.mjs` — (a) when the reference area-id set and the scorecard `data-dim` set have an EMPTY intersection, fall back to emitting all lifted probes in one "method probes" section grouped by area instead of 12 probe-less dimension sections; (b) **[F13]** when the intersection is partial (non-empty, non-total), emit one stderr warning per unmatched `data-dim` id (e.g. `WARN: no probes lifted for dimension "<id>"`) — HTML output unchanged for conforming pairs. Add two selftest cases to run-tests.sh: disjoint-ids input and partial-drift input.
  3. `_shared/interview-guidelines/guidelines/work-history/interviewer-reference.html` header — extend the existing METHOD-phases comment to state this is the sanctioned exception to the 1:1 rule and that questionnaire emission uses the disjoint-ids fallback (same comment block also carries F16's calib-variant sentence).
- **Rationale:** the deviation is deliberate but undocumented against a MUST and silently degrades a consumer; documenting the exception + a deterministic fallback + drift-visible warnings repairs the consumer without rewriting a researched exemplar.
- **Blast radius:** interview-guide SKILL.md (2 sections), questionnaire.mjs + run-tests.sh selftest count (grep any count-claims per the coherence-gate lesson), work-history reference header. Re-run skill-eval + 4 hygiene lints on both skills. §K reconciliation: the guide owns the 1:1 rule (with carve-out); reference-skeleton.html's header comment stays SHOULD and cites it.

### F6 + F14 — Stop the additional/README directing writes into the installed corpus, with location-true text in each home
- **Finding (F6 [Should-fix]):** `guidelines/case-study/additional/README.md:3` — "Drop the role's actual case prompt or take-home brief into this folder per candidate role —" where "this folder" is the plugin-installed read-only corpus shared across all roles; reference-resolution.md §additional_docs names the correct home (the ROLE directory). Following the README as written mutates the plugin install and cross-contaminates roles.
- **Finding (F14 [Nit], amends F6):** the pass-1 fix had Setup copy the corrected README ("this bundled folder is the read-only template…") into every role dir — where that self-description is false, re-creating the location-confusion bug one directory over.
- **Files & before → after:**
  1. Bundled `_shared/interview-guidelines/guidelines/case-study/additional/README.md` — rewrite to say only: this is a read-only template scaffold; do not write here; setup copies an `additional/` dir into each role round; role docs go in `<role-dir>/guidelines/<round>/additional/` (cite reference-resolution.md §additional_docs).
  2. `interview-feedback/SKILL.md` Phase Setup — scaffold an `additional/` dir for EVERY round (matching the SKILL.md:261 layout line and the round-agnostic `additional_docs[]` contract), and write a role-dir README text defined inline in the setup phase — "Drop this round's supplementary docs (case prompt, take-home brief, …) here and register each in `role.json` `additional_docs[]`." — NOT a copy of the bundled README.
- **Rationale:** contradiction between substrate files where the wrong reading is destructive (plugin-install mutation, cross-role leakage); two location-specific texts make each README true where it lives; also fixes the case-study-only asymmetry.
- **Blast radius:** bundled README + interview-feedback SKILL.md setup phase. No scripts or tests reference README contents.

### F7 [Nit] — Correct `pmos:skill` meta attribution across the 19 corpus HTML files — SCOPED BY F10
- **Finding:** Every corpus HTML file carries `<meta name="pmos:skill" content="interview-feedback">`, including the 8 archetype reference+scorecard pairs whose own header comments attribute /interview-guide (e.g. work-history reference line 3 vs. its line 15 meta). Leftover from when the corpus lived under interview-feedback.
- **Files & before → after:** one pass over the 19 corpus HTML files aligning meta with each file's own header attribution — the 16 archetype pair files get `content="interview-guide"`; the two skeletons and `interviewer-effectiveness.html` (interview-feedback-owned per their headers) keep `content="interview-feedback"`. **[F10 scoping]** This applies to the AT-REST bundled files only; role-dir working copies made by F1's setup path are re-stamped `interview-feedback` at copy time (see F1 amendment 3), so filled scorecards are never attributed to interview-guide.
- **Rationale:** `pmos:skill` is the comments-overlay routing key to the originating skill's apply-edit shim; wrong-by-inheritance metadata will misroute the moment managerkit ships shims.
- **Blast radius:** metadata-only across 19 files; no script/test in this plugin reads `pmos:skill` (latent consumer: comments overlay).

### F8 [Nit] — Qualify bare `scripts/…` cites with the owning skill
- **Finding:** Substrate files cite consumer scripts by bare relative paths that resolve to nothing from the substrate's home and point at DIFFERENT skills with identical notation: `scorecard-skeleton.html:49` "enforced by scripts/validate-scorecard-anchors.mjs, never by the model." (lives in `interview-guide/scripts/`), while `scorecard-skeleton.html:7` cites "the filler (scripts/fill-scorecard.mjs)" (lives in `interview-feedback/scripts/`); repeated at `guidelines/work-history/level-ladder.md:7` (validator) and `reference-resolution.md:15` (filler).
- **Files & before → after:** `scorecard-skeleton.html:7` → "interview-feedback/scripts/fill-scorecard.mjs"; `scorecard-skeleton.html:49` and `guidelines/work-history/level-ladder.md:7` → "interview-guide/scripts/validate-scorecard-anchors.mjs"; `reference-resolution.md:15` → "interview-feedback/scripts/fill-scorecard.mjs parse".
- **Rationale:** a shared file citing two different skills' internals identically forces the reader to grep for each script's true home.
- **Blast radius:** comment/doc text only; no anchors, lints, or tests parse these cites.

### F12 [Nit] — Align scorecard-skeleton's weight-sum wording with the hard gate
- **Finding:** `scorecard-skeleton.html:12` — "integer weight; the weights across dims should sum to 100." — vs. the same file's line 124 ("data-weight values must sum to 100"), interview-guide/SKILL.md:182 ("must sum to 100"), and the deterministic validate-scorecard-anchors sum-gate (§H hard gate per level-ladder.md:99–100).
- **File & before → after:** line 12 "should sum to 100" → "must sum to 100" (one word).
- **Rationale:** a refusal-backed invariant stated as "should" at the machine-anchor contract line understates the gate; F5's SHOULD/MUST reconciliation covered area-id mapping but not this line.
- **Blast radius:** one word in a doc comment; nothing parses it.

### F15 [Should-fix] — Level swap must update the scorecard's human-visible weight prose, not just `data-weight` anchors
- **Finding:** F11's swap spec covered `data-weight` attributes only, but the bundled work-history scorecard carries senior-pm numbers in three additional homes: the rendered `.sub` banner "work-history round · Senior PM weighting (default; re-select with --seniority) · weights sum to 100" (:83), twelve rendered `<span class="weight">weight N</span>` labels (:204, :223, :242, …), and the head comment "the data-weight values below materialize the `senior-pm` COLUMN of level-ladder.md" (:15). Implemented as written, a director round ships anchors saying director while the interviewer-facing labels and banner say Senior PM — and F10's byte-for-byte `<head>` preservation in fill-scorecard.mjs carries the wrong-level head comment into every filled output.
- **Fix:** folded into F1 amendment 5 above — the swap enumerates all four weight homes; the `weight N` spans are regenerated from the just-set anchor values (anchor = source, span = projection); a label/anchor mismatch check is added to the copy step's acceptance.
- **Blast radius:** interview-feedback SKILL.md setup phase only (swap is model-performed); no bundled-file changes; one run-tests.sh expectation if the swap is test-enumerated.

### F16 [Should-fix] — F9's "ONE canonical shape — the product-sense/work-history form" named two divergent forms
- **Finding:** product-sense's calib is a qualitative "Good / avg / poor." one-liner; work-history's is numeric "4 / 2–3 / 1" score anchors keyed to the scorecard's data-v scale. No determinate normalization target; unclear whether work-history itself violates the guide's "a `.calib` good/average/poor line" wording.
- **Fix:** folded into F3+F9 item 1 above — canon = the product-sense qualitative form (skeleton + all 6 normalized references); work-history's numeric variant is blessed and documented as a sanctioned archetype variant (header sentence riding F5's edit; one parenthetical in interview-guide/SKILL.md:176).
- **Rationale:** the qualitative form matches the guide's existing wording verbatim and 5 of 6 normalization sources are prose; work-history's numeric anchors are load-bearing for its 1–4 scoring contract — rewriting a researched exemplar to lose them would be a regression.
- **Blast radius:** same files as F3+F9 plus SKILL.md:176 parenthetical; no anchors, scripts, or tests touched.

### F17 [Nit] — F9's blast radius was false: `.calib` styling lives in `<head><style>`, and the two exemplar rules diverge
- **Finding:** F9's "content edits inside `<main>` only" would ship unstyled `.calib` paragraphs in all 6 normalized references (their inline styles define no `.calib` rule); product-sense:33 and work-history:40 carry different `.calib` CSS rules, so "match the exemplars" is indeterminate at the style layer too.
- **Fix:** folded into F3+F9 item 4 above — each normalized reference gets the canonical `.calib` rule (product-sense:33's left-border form, same as the skeleton) in its `<head><style>`; work-history's rule stays (it styles the blessed numeric variant); F9's blast-radius line corrected.
- **Blast radius:** the same 6 files F9 touches (one `<style>` rule each) + skeleton. Reference `<head>` edits are safe — fill-scorecard.mjs's head preservation applies to scorecards, not references.

### F18 [Nit] — Corpus-last precedence let an archetype-MISMATCHED role generic beat the exact bundled archetype pair
- **Finding:** the pass-1 step-4 placement sat below the whole of step 3, whose second branch ("else the role's generic guideline", reference-resolution.md:11) could resolve a product-sense round to a behavioral-shaped generic while a researched product-sense pair sat one step lower; pre-fix or hand-assembled role dirs (untouched by F1's setup copy) would hit this forever, silently.
- **Fix:** folded into F1 amendment 2 above — the chain is split into 5 steps: exact-archetype role file → bundled exact-archetype pair → role generic. Reordering chosen over recording-only because recording leaves the wrong resolution in place; the fallback-recording note still lands at every step.
- **Blast radius:** reference-resolution.md §Reference resolution (same section F1 edits; renumbered 3→5 steps; "Non-interactive ambiguity" paragraph stays valid); any interview-feedback SKILL.md prose paraphrasing the chain re-checked; `tools/lint-phase-refs.sh` re-run.

### F19 [Nit] — Drop F1's `level-ladder.md` copy into work-history role dirs
- **Finding:** the ladder dependency is authoring-time only — F11's swap reads it from the substrate at copy time; grep of interview-feedback/ for "level-ladder" returns zero hits; the bundled scorecard's head comment (:17, "This is the one hardcoded copy of a ladder row; the ladder is the source of truth.") sanctions exactly ONE hardcoded copy; the SKILL.md:261 storage layout has no ladder slot. Role-dir copies would be N unsynced §K violations and undocumented litter.
- **Fix:** folded into F1 amendment 1 above — no ladder copy; `role.json` records the applied ladder row + a provenance pointer to the substrate ladder (e.g. `weights: { level: "director", source: "_shared/.../level-ladder.md" }`).
- **Blast radius:** interview-feedback SKILL.md setup phase (one sentence removed from F1's earlier spec); no substrate or script changes.

## Rejections

None across all three passes. No finding was invalid or ungrounded. (F9 corrected the pass-1 F3 rationale — the claim "all 8 bundled references carry `.calib`" was verified wrong and conceded; F15–F19 corrected the pass-1/2 amendments themselves and were verified and conceded likewise.)

## Open questions

None. No unresolved disagreements remain between reviewer and author at closeout.
