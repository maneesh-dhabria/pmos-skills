# Grill — 02_design.html (/artifact-critique, epic 260624-kkw)

**Date:** 2026-06-24 · **Mode:** define (route: skill), interactive

## Posture

The seed brief was **already deep-grilled** before this run (brief header: "Design grilled & locked
— deep grill 2026-06-24, 13 branches"), with decisions D0–D7 locked and the verdict scale, applicability
approach, and voice locked inline. A full re-interrogation of those branches would re-litigate settled
decisions — low value. The grill phase therefore focused **only on the brief's §6 "open items for
define"** — the genuinely unresolved decisions a define run must close before stories are plannable.

## Branches pressed (resolved via the define-decision interrogation)

| # | Open item (brief §6) | Resolution |
|---|---|---|
| Q1 | Story split granularity | **2 stories: substrate `fbd` → skill `aa8`.** Mirrors the proven 3jp substrate→consumer shape; each is one session/PR. Rejected: 1-story (substrate authoring load — 10-axis `axes.md`, the heuristic spine, the applicability map + findings schema — is substantial and separable from skill orchestration/voice/eval/emit). |
| Q2 | Structured-findings carrier (D1 / §6) | **Embedded `<script type="application/json">` block** inside the HTML (pmos-comments-style, sentinel-wrapped). Rejected: `.sections.json` companion (a sidecar, section-anchored not findings-shaped; violates "artifact is single source of truth"). |
| Q3 | Corpus access for few-shot + fixtures (§6) | **Vendor anonymized samples** under the feature folder so build + tests are self-contained. The real 5-doc corpus is confidential — none committed; story `fbd` produces anonymized doc-excerpt + critique-output pairs (≥2 doc-types + an AI case). |

## Claims pressure-tested against the live repo (per the design-doc-seed adoption guard)

- `_shared/reviewer-protocol.md`, `findings-dispositions.md`, `tier-matrix.md` — **all present** in
  pmos-toolkit `_shared/`. No dangling cites.
- `_shared/html-authoring/` substrate — present (template, render.js, conventions, index-generator).
- `/artifact` is a shipped skill (`SKILL.md` ≈ 39.4K) — brief's "≈38.5K" claim holds; D1/D2 delegation
  and the deferred-adoption (Inv-6) posture are grounded.
- `_shared/critique-rubric/` — confirmed **net-new** (no pre-existing collision).

## Residual / carried to build (not blocking the define merge)

- Exact `axes.md` per-axis check wording + the `doc-types.md` conditional rules are designed in shape here
  (§4.2, §4.3) and **authored in full in story `fbd`** from the anonymized corpus reasoning.
- Few-shot exemplar line selection happens in story `aa8` from the vendored samples.
- The `/artifact` adoption + `/artifact-sdlc` orchestration are explicit out-of-scope-v1 future stories.

**Outcome:** design locked for define; no blocking gaps. Proceed to story-split + per-story plans.
