# Verify — unify-primer-learnlist

**Date:** 2026-06-03
**Scope:** skill/prose refactor (no deployable code). Verification = test suite + FR compliance + regression + skill-eval reconciliation + release-prereq grading.

## 1. Test suite (all pass, re-run)

| Test | Result |
|---|---|
| `assert_substrate_skill_agnostic.sh` (+ `--selftest`) | PASS |
| `learn-list/tests/structure.test.sh` (28 checks) | PASS |
| `primer/tests/structure.test.sh` (19 checks) | PASS |
| `skill-eval-check.sh [D]` × primer + learn-list | PASS (18/18 each) |

## 2. Spec FR compliance (FR-1..23)

All 23 FRs satisfied:

- **Substrate (FR-1..6):** four docs (`intake`, `canon-discovery`, `outline`, `sourcing`) + relocated `source-tiers`/`sourcing-ladder`; `modes.md` removed; all skill-agnostic (FR-6 grep-enforced, test passes).
- **Intake/flags (FR-7..10):** primer keeps `--depth/--audience` via `intake.md`; learn-list adopts them; `--mode/--level` rejected with replacement-naming errors; caller sweep found no live functional callers (README row deferred to release-prereqs per §G).
- **Richness (FR-11/12):** shared verdict; primer reframes/carve-out, learn-list soft — reactions live in each SKILL.md, not the substrate.
- **primer overlay (FR-13..16):** floor = eval-time coverage signal; `≥3-source short-circuit` removed (test asserts no live short-circuit); per-H2 evidence = per-topic shortlist; closing adjacency pointer section, depth-scaled.
- **learn-list overlay (FR-17..19):** back half (rank/annotate/rabbit-holes/follow-list/paste-block) unchanged; PM-shaped description (reviewer `b-*` all pass); adjacency hops re-keyed to `--depth`.
- **skill-patterns conformance (FR-20):** all `[D]` + validated `[J]` pass for both skills.
- **Tests (FR-21..23):** all three present and passing.

## 3. Regression

- No stale referrers to `learn-list/reference/{source-tiers,sourcing-ladder,modes}.md` anywhere under `plugins/pmos-learnkit/`.
- Every `_shared/topic-research/X.md` inline reference in both SKILL.md resolves to a real file.
- `learn-list/reference/` now holds only `artifact-template.html` (intentional).

## 4. skill-eval reconciliation (Phase 6a re-run)

- All `[D]` pass; all `[J]` pass after orchestrator validation.
- **One informational observation (non-blocking):** reviewer flagged primer `a-name-verb-or-gerund` ("primer" is a noun). Downgraded to pass — quote `name: primer` is <40 chars (unsubstantiated per the determinism contract) AND pre-existing/out-of-scope (the refactor never touched the name). `accepted_residuals` empty.

## 5. Release-prerequisite grading (findings — /complete-dev deliverables, NOT blocks)

| Item | State | Owner |
|---|---|---|
| `plugin.json` × 2 version bump | at `0.8.2` (synced); **minor bump → 0.9.0** pending | /complete-dev |
| README rows for `/learn-list` (+ `/primer`) | `README.md:35` still shows `--mode` + non-PM framing; **update to `--depth`/`--audience` + PM framing** pending | /complete-dev |
| Changelog entry | pending | /complete-dev |
| `~/.pmos/learnings.md` `## /primer` + `## /learn-list` headers | present (lines 15, 414) | n/a |

## Verdict

**PASS.** No blockers. Release prerequisites enumerated above are handed to `/complete-dev` (Phase 8).
