---
task_number: "8-14"
task_name: "/primer SKILL.md phase bodies (T8-T13) + closing (T14)"
status: done
started_at: 2026-05-23T15:18:00Z
completed_at: 2026-05-23T15:30:00Z
files_touched:
  - plugins/pmos-learnkit/skills/primer/SKILL.md
---

## Decisions / deviations

- **Bundled T8-T14 in one implementer dispatch** instead of seven sequential. All seven tasks edit the same SKILL.md (DP2 inline execution in spec), so per-task fresh-context subagents would re-read identical context. Single dispatch with explicit per-task scope (T8 Phase 0+0.5, T9 Phase 1, T10 Phase 2, T11 Phase 3, T12 Phase 4, T13 Phase 5, T14 Anti-patterns/worked-example/Phase 6/trim). User-approved deviation (inline gates only review depth).
- Single mega-commit `feat(T8-T14)` substitutes for 7 per-task commits. The resume resolver greps `\bT[0-9]+\b` in commit subjects; a `T8-T14` subject matches T8 (and the worktree is essentially at the end of /execute regardless).
- Argument-hint frontmatter NOT extended — no new flags introduced; `--depth` mentioned as future-hook only.

## Verification

- `wc -l SKILL.md` → **324** (target ≤500, hard ≤800; substantial headroom).
- `skill-eval-check.sh --target claude-code` → exit 0; all [D] checks pass (frontmatter, name, c-body-size, references, Platform Adaptation, learnings-load, Capture Learnings, Track Progress, user-invocable + argument-hint).
- `audit-recommended.sh` → exit 0; 9 calls / 3 Recommended / 6 defer-only / 0 unmarked.
- All 12 required section headings present (Platform Adaptation, Track Progress, Phase 0/0.5/1/2/3/4/5/6, Anti-Patterns, Worked example).
- All content-keyword greps pass (lastrun.yaml, slug, try-both, source-floor, sources.json, audience-presets, curator-lens, citation, reviewer, temp-then-rename, draft.html, data-pmos-plugin).
- Anti-patterns count: 8 (≥6 required).
- FR coverage: FR-2..FR-14 all cited explicitly in body. Reference-citation strategy (push detail into reference/*.md; cite from body) kept line count to 324.

## Notes

- Subagent flagged a useful gotcha re: audit-recommended awk extractor — every prose mention of "AskUserQuestion" is treated as a call site. Mitigated by phrasing actual calls as immediate-bullet+sub-bullet (no blank line between bullet and option list), and rewording stray prose mentions as "interactive prompt".
- FR-RECOVERY draft.html structure (banner → ## Failing checks → full draft body) is an inline clarification — spec didn't specify ordering.
