# docs/pmos/features/2026-06-24_magazine-pipeline-robustness/stories/260624-xck/tasks.yaml
story: 260624-xck
spec: ../../02_design.html          # route: skill story cites the epic design_doc (G2)
tasks:
  - id: T1
    title: "Window resolution + require-a-window + loud unknown-flag reject (tracer bullet)"
    deps: []
    parallel: false
    acceptance:
      - "node scripts/magazine-run.js --selftest passes new assertions: unknown --flag exits non-zero naming it; resolveWindow({days:7}) yields lower bound now-7d ISO and no upper; resolveWindow({}) throws 'window required'"
      - "node scripts/magazine-run.js discover (no flags) prints the window-required message and exits 64"
      - "node scripts/magazine-run.js discover --bogus 1 names --bogus on stderr and exits 64"
      - "node scripts/magazine-run.js discover --feed tests/fixtures/<feed>.xml --days 3650 exits 0 and prints a snapshot array"
    status: pending
    evidence:
  - id: T2
    title: "Upper-bound filter in fetch-feed.js (range crawl)"
    deps: [T1]
    parallel: false
    acceptance:
      - "node scripts/fetch-feed.js --selftest passes: windowItems(items, since, max, until) returns only items with published <= until and > since; an item exactly at until is included (inclusive)"
      - "fetchOne(feed, since, max, until) pushes --until and cmdDiscover passes the resolved upper bound"
      - "node scripts/fetch-feed.js --file tests/fixtures/<feed>.xml --since 2020-01-01T00:00:00.000Z --until 2021-01-01T00:00:00.000Z returns only 2020 items"
    status: pending
    evidence:
  - id: T3
    title: "Date aliases + inclusive bare-date parsing"
    deps: [T2]
    parallel: false
    acceptance:
      - "selftest: resolveWindow({from:'2026-03-01',to:'2026-03-31'}) -> since 2026-03-01T00:00:00.000Z, until 2026-03-31T23:59:59.999Z"
      - "selftest: resolveWindow({since:'2026-03-01T12:00:00.000Z'}) passes ISO through verbatim; --from notadate throws a clear error naming the flag"
      - "node scripts/magazine-run.js discover --feed tests/fixtures/<feed>.xml --from 2020-01-01 --to 2020-12-31 exits 0 and snapshot contains only 2020 items"
    status: pending
    evidence:
  - id: T4
    title: "Persist single-latest discover snapshot"
    deps: [T1]
    parallel: true
    acceptance:
      - "selftest: after cmdDiscover, state.load(file).snapshot == {id:<ISO>, guids:[...]}; second cmdDiscover overwrites it (id changes, snapshot stays a single object not a list)"
      - "printed discover return value (snapshot array) is byte-unchanged from prior behavior"
      - "run discover twice on a fixture; state.json .snapshot is one object whose id changed and whose guids count matches the printed array length"
    status: pending
    evidence:
  - id: T5
    title: "Scope prep to the latest snapshot (+ D6 reuse semantics)"
    deps: [T4]
    parallel: false
    acceptance:
      - "selftest: with 3 discovered items where 2 are in state.snapshot.guids, cmdPrep crawls only the 2 in-snapshot items; the out-of-snapshot item stays 'discovered'"
      - "selftest: a snapshot guid already at 'summarized' is not re-crawled yet remains referenced (D6); with no state.snapshot, prep falls back to whole-ledger behavior (Inv-4)"
      - "--snapshot <id> is parsed-but-inert (one-line comment marks the hook)"
      - "proof-of-life: prep --root <root> with 1 in-snapshot + 1 out item -> only the in-snapshot item advances in state.json"
    status: pending
    evidence:
  - id: T6
    title: "Make the cursor watch-internal (stop interactive cursor advance) — resolves AC8"
    deps: [T1]
    parallel: false
    acceptance:
      - "grep enumerates every cursor writer; cmdEnqueue (watch producer) is left untouched"
      - "selftest asserts resolveWindow never consults st.cursors"
      - "interactive Phase-6 cursor-advance instruction + any interactive-only advanceCursors call removed; function retained for enqueue/drain"
      - "bash tests/watch.test.sh exits 0 (FR-7/FR-R4 cursor invariants intact); re-running discover twice leaves state.json.cursors unchanged"
    status: pending
    evidence:
  - id: T7
    title: "Rewrite the windowing docs to the real flag surface"
    deps: [T1, T2, T3, T6]
    parallel: false
    acceptance:
      - "SKILL.md Phase 2 discover signature replaced with the real window-required surface (--from/--to | --since/--until | --days N); bare discover documented as erroring; cursor noted watch-internal only"
      - "argument-hint keeps --days N and does not advertise --since"
      - "reference/pipeline.md §Windowing rewritten: explicit window required; cursor = watch forward-only poll memory; Phase-6 advanceCursors interactive instruction removed"
      - "grep -n 'since last run|--since <cursor>' SKILL.md reference/pipeline.md returns no interactive-default cursor language; bash tests/structure.test.sh exits 0"
    status: pending
    evidence:
  - id: T8
    title: "Dogfood / Utility Verification (TN-1)"
    deps: [T5, T6, T7]
    parallel: false
    acceptance:
      - "real Stage-A flow on fixtures: narrow --from/--to discover, then wider overlapping discover; narrow snapshot guid set is a subset of wide; state.json.snapshot is a single object after both runs"
      - "already-discovered items keep their status across the overlapping run (D6); prep crawls only the latest snapshot; bare-discover and unknown-flag both exit 64 with clear messages"
      - "blind judge given only 02_design.html + the dogfood transcript scores error-clarity, doc-truthfulness, reuse-intuitiveness >=4/5 each and emits DOGFOOD: SHIP | ITERATE"
    status: pending
    evidence:
  - id: T9
    title: "Final verification (TN)"
    deps: [T8]
    parallel: false
    acceptance:
      - "node scripts/magazine-run.js --selftest and node scripts/fetch-feed.js --selftest both PASS"
      - "bash tests/structure.test.sh, bash tests/watch.test.sh, bash tests/bundles.test.sh all exit 0"
      - "error paths forced: bare discover -> 64; discover --nope -> 64 naming flag; discover --from baddate -> clear date error"
      - "AC9 conformance: diff carries no version-bump/changelog/README/manifest-sync change; argument-hint matches parsed flags; no hard-coded user paths; temp fixture roots cleaned up"
    status: pending
    evidence:
